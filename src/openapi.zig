// Generated by zig-openapi-gen

const std = @import("std");
const root = @This();

pub const io = @import("io.zig");
pub const AuthConfig = struct {};

pub const Servers = struct {};

pub const Client = struct {
    allocator: std.mem.Allocator,
    client: std.http.Client,
    base_url: []const u8,
    base_url_owned: ?[]const u8 = null,
    auth_config: AuthConfig,

    pub fn WatchEvent(comptime T: type) type {
        return struct {
            type: []const u8,
            object: T,
        };
    }

    pub fn WatchStream(comptime T: type) type {
        return struct {
            allocator: std.mem.Allocator,
            req: *std.http.Client.Request,
            transfer_buf: []u8,
            reader: *std.io.Reader,
            current_line: ?[]u8 = null,
            current_parsed: ?std.json.Parsed(WatchEvent(T)) = null,

            const Self = @This();

            pub fn deinit(self: *@This()) void {
                if (self.current_parsed) |*p| p.deinit();
                if (self.current_line) |l| self.allocator.free(l);
                self.req.deinit();
                self.allocator.destroy(self.req);
                self.allocator.free(self.transfer_buf);
            }

            fn readLine(self: *@This()) !?[]u8 {
                var result = std.ArrayListUnmanaged(u8){};
                errdefer result.deinit(self.allocator);
                while (true) {
                    const buffered = self.reader.buffer[self.reader.seek..self.reader.end];
                    if (std.mem.indexOfScalar(u8, buffered, '\n')) |idx| {
                        try result.appendSlice(self.allocator, buffered[0..idx]);
                        self.reader.seek += idx + 1;
                        return try result.toOwnedSlice(self.allocator);
                    }
                    try result.appendSlice(self.allocator, buffered);
                    self.reader.seek = self.reader.end;
                    self.reader.fillMore() catch |err| switch (err) {
                        error.EndOfStream => {
                            if (result.items.len > 0) return try result.toOwnedSlice(self.allocator);
                            return null;
                        },
                        else => return err,
                    };
                }
            }

            pub fn next(self: *@This()) !?WatchEvent(T) {
                if (self.current_parsed) |*p| {
                    p.deinit();
                    self.current_parsed = null;
                }
                if (self.current_line) |l| {
                    self.allocator.free(l);
                    self.current_line = null;
                }

                const line = try self.readLine();
                if (line) |l| {
                    self.current_line = l;
                    const parsed = try std.json.parseFromSlice(WatchEvent(T), self.allocator, l, .{ .ignore_unknown_fields = true });
                    self.current_parsed = parsed;
                    return parsed.value;
                }
                return null;
            }
        };
    }
    pub fn init(allocator: std.mem.Allocator, auth_config: AuthConfig, options: anytype) !Client {
        var base_url: []const u8 = "";
        var base_url_owned: ?[]const u8 = null;
        _ = &base_url_owned;
        var has_base_url = false;
        if (@hasField(@TypeOf(options), "base_url")) {
            const val = options.base_url;
            if (@typeInfo(@TypeOf(val)) == .optional) {
                if (val) |v| {
                    base_url = v;
                    has_base_url = true;
                }
            } else {
                base_url = val;
                has_base_url = true;
            }
        }

        if (!has_base_url) {
            if (@hasDecl(Servers, "getUrl")) {
                const server_index = if (@hasField(@TypeOf(options), "server_index")) options.server_index else 0;
                const server_vars = if (@hasField(@TypeOf(options), "server_vars")) options.server_vars else .{};
                base_url = try Servers.getUrl(server_index, server_vars, allocator);
                base_url_owned = base_url;
            }
        }
        return .{ .allocator = allocator, .client = std.http.Client{ .allocator = allocator }, .base_url = base_url, .base_url_owned = base_url_owned, .auth_config = auth_config };
    }

    pub fn deinit(self: *Client) void {
        self.client.deinit();
        if (self.base_url_owned) |url| self.allocator.free(url);
    }

    pub fn request(self: *Client, method: std.http.Method, path: []const u8, options: anytype, response_type: type) !response_type {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}{s}", .{ self.base_url, path });

        // Query Params
        var has_query = false;
        if (@hasField(@TypeOf(options), "query")) {
            const query = options.query;
            const fields = std.meta.fields(@TypeOf(query));
            inline for (fields) |field| {
                const val = @field(query, field.name);
                if (@typeInfo(@TypeOf(val)) == .optional) {
                    if (val) |v| {
                        if (!has_query) {
                            try url_w.writeAll("?");
                            has_query = true;
                        } else try url_w.writeAll("&");
                        try url_w.print("{s}={any}", .{ field.name, v });
                    }
                } else {
                    if (!has_query) {
                        try url_w.writeAll("?");
                        has_query = true;
                    } else try url_w.writeAll("&");
                    try url_w.print("{s}={any}", .{ field.name, val });
                }
            }
        }

        const req_url = url_fbs.getWritten();
        const content_type = if (@hasField(@TypeOf(options), "content_type")) options.content_type else "application/json";

        const req_options = std.http.Client.RequestOptions{ .headers = .{ .content_type = .{ .override = content_type }, .authorization = if (@hasField(AuthConfig, "BearerToken"))
            if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit
        else
            .omit } };

        var req = try self.client.request(method, try std.Uri.parse(req_url), req_options);
        defer req.deinit();

        // Body
        if (@hasField(@TypeOf(options), "body")) {
            if (method.requestHasBody()) {
                const body = options.body;
                var req_body_writer = std.ArrayListUnmanaged(u8){};
                defer req_body_writer.deinit(self.allocator);
                try req_body_writer.writer(self.allocator).print("{f}", .{std.json.fmt(body, .{})});
                req.transfer_encoding = .{ .content_length = req_body_writer.items.len };
                _ = try req.sendBody(req_body_writer.items);
            } else {
                try req.sendBodiless();
            }
        } else {
            try req.sendBodiless();
        }

        var header_buf: [4096]u8 = undefined;
        var res = try req.receiveHead(&header_buf);

        if (response_type == void) {
            return;
        }

        var body_list = std.ArrayListUnmanaged(u8){};
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        const body_reader = res.reader(transfer_buf);
        while (true) {
            const byte = body_reader.takeByte() catch |err| switch (err) {
                error.EndOfStream => break,
                else => return err,
            };
            try body_list.append(self.allocator, byte);
        }

        // Parse response
        // Note: This assumes JSON response for now.
        const parsed = try std.json.parseFromSliceLeaky(response_type, self.allocator, body_list.items, .{ .ignore_unknown_fields = true });
        return parsed;
    }

    pub fn watch(self: *Client, comptime T: type, path: []const u8, options: anytype) !WatchStream(T) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}{s}", .{ self.base_url, path });

        // Query Params
        var has_query = false;
        if (@hasField(@TypeOf(options), "query")) {
            const query = options.query;
            const fields = std.meta.fields(@TypeOf(query));
            inline for (fields) |field| {
                const val = @field(query, field.name);
                if (@typeInfo(@TypeOf(val)) == .optional) {
                    if (val) |v| {
                        if (!has_query) {
                            try url_w.writeAll("?");
                            has_query = true;
                        } else try url_w.writeAll("&");
                        try url_w.print("{s}={any}", .{ field.name, v });
                    }
                } else {
                    if (!has_query) {
                        try url_w.writeAll("?");
                        has_query = true;
                    } else try url_w.writeAll("&");
                    try url_w.print("{s}={any}", .{ field.name, val });
                }
            }
        }

        // Force watch=true if not present
        if (!has_query) {
            try url_w.writeAll("?watch=true");
        } else {
            try url_w.writeAll("&watch=true");
        }

        const req_url = url_fbs.getWritten();
        const content_type = "application/json"; // Default for watch

        const req_options = .{ .headers = .{ .content_type = .{ .override = content_type }, .authorization = if (@hasField(AuthConfig, "BearerToken"))
            if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit
        else
            .omit } };

        const req = try self.client.request(.GET, try std.Uri.parse(req_url), req_options);
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);

        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        const res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(T){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }
    pub const GetServiceAccountIssuerOpenIDConfigurationResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetServiceAccountIssuerOpenIDConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetServiceAccountIssuerOpenIDConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetServiceAccountIssuerOpenIDConfigurationResponse = union(enum) {
        ok: GetServiceAccountIssuerOpenIDConfigurationResponse200,
        unauthorized: GetServiceAccountIssuerOpenIDConfigurationResponse401,
        default_response: GetServiceAccountIssuerOpenIDConfigurationResponseDefault,
    };

    /// get service account issuer OpenID configuration, also known as the 'OIDC discovery doc'
    pub fn getServiceAccountIssuerOpenIDConfiguration(self: *Client) !GetServiceAccountIssuerOpenIDConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/.well-known/openid-configuration/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetCoreAPIVersionsResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIVersions,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoreAPIVersionsResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoreAPIVersionsResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoreAPIVersionsResponse = union(enum) {
        ok: GetCoreAPIVersionsResponse200,
        unauthorized: GetCoreAPIVersionsResponse401,
        default_response: GetCoreAPIVersionsResponseDefault,
    };

    /// get available API versions
    pub fn getCoreAPIVersions(self: *Client) !GetCoreAPIVersionsResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIVersions, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetCoreV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoreV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoreV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoreV1APIResourcesResponse = union(enum) {
        ok: GetCoreV1APIResourcesResponse200,
        unauthorized: GetCoreV1APIResourcesResponse401,
        default_response: GetCoreV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getCoreV1APIResources(self: *Client) !GetCoreV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1ComponentStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.ComponentStatusList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ComponentStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ComponentStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ComponentStatusResponse = union(enum) {
        ok: ListCoreV1ComponentStatusResponse200,
        unauthorized: ListCoreV1ComponentStatusResponse401,
        default_response: ListCoreV1ComponentStatusResponseDefault,
    };

    pub const listCoreV1ComponentStatusOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list objects of kind ComponentStatus
    pub fn listCoreV1ComponentStatus(self: *Client, options: listCoreV1ComponentStatusOptions) !ListCoreV1ComponentStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/componentstatuses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ComponentStatusList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1ComponentStatus(self: *Client, options: listCoreV1ComponentStatusOptions) !WatchStream(root.io.k8s.api.core.v1.ComponentStatus) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/componentstatuses");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.ComponentStatus){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ReadCoreV1ComponentStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.ComponentStatus,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1ComponentStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1ComponentStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1ComponentStatusResponse = union(enum) {
        ok: ReadCoreV1ComponentStatusResponse200,
        unauthorized: ReadCoreV1ComponentStatusResponse401,
        default_response: ReadCoreV1ComponentStatusResponseDefault,
    };

    pub const readCoreV1ComponentStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ComponentStatus
    pub fn readCoreV1ComponentStatus(self: *Client, name: []const u8, options: readCoreV1ComponentStatusOptions) !ReadCoreV1ComponentStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/componentstatuses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ComponentStatus, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1ConfigMapForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.core.v1.ConfigMapList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ConfigMapForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ConfigMapForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ConfigMapForAllNamespacesResponse = union(enum) {
        ok: ListCoreV1ConfigMapForAllNamespacesResponse200,
        unauthorized: ListCoreV1ConfigMapForAllNamespacesResponse401,
        default_response: ListCoreV1ConfigMapForAllNamespacesResponseDefault,
    };

    pub const listCoreV1ConfigMapForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ConfigMap
    pub fn listCoreV1ConfigMapForAllNamespaces(self: *Client, options: listCoreV1ConfigMapForAllNamespacesOptions) !ListCoreV1ConfigMapForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/configmaps", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ConfigMapList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1ConfigMapForAllNamespaces(self: *Client, options: listCoreV1ConfigMapForAllNamespacesOptions) !WatchStream(root.io.k8s.api.core.v1.ConfigMap) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/configmaps");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.ConfigMap){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoreV1EndpointsForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.core.v1.EndpointsList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1EndpointsForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1EndpointsForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1EndpointsForAllNamespacesResponse = union(enum) {
        ok: ListCoreV1EndpointsForAllNamespacesResponse200,
        unauthorized: ListCoreV1EndpointsForAllNamespacesResponse401,
        default_response: ListCoreV1EndpointsForAllNamespacesResponseDefault,
    };

    pub const listCoreV1EndpointsForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Endpoints
    pub fn listCoreV1EndpointsForAllNamespaces(self: *Client, options: listCoreV1EndpointsForAllNamespacesOptions) !ListCoreV1EndpointsForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/endpoints", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.EndpointsList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1EndpointsForAllNamespaces(self: *Client, options: listCoreV1EndpointsForAllNamespacesOptions) !WatchStream(root.io.k8s.api.core.v1.Endpoints) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/endpoints");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.Endpoints){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoreV1EventForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.core.v1.EventList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1EventForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1EventForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1EventForAllNamespacesResponse = union(enum) {
        ok: ListCoreV1EventForAllNamespacesResponse200,
        unauthorized: ListCoreV1EventForAllNamespacesResponse401,
        default_response: ListCoreV1EventForAllNamespacesResponseDefault,
    };

    pub const listCoreV1EventForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Event
    pub fn listCoreV1EventForAllNamespaces(self: *Client, options: listCoreV1EventForAllNamespacesOptions) !ListCoreV1EventForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/events", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.EventList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1EventForAllNamespaces(self: *Client, options: listCoreV1EventForAllNamespacesOptions) !WatchStream(root.io.k8s.api.core.v1.Event) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/events");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.Event){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoreV1LimitRangeForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.core.v1.LimitRangeList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1LimitRangeForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1LimitRangeForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1LimitRangeForAllNamespacesResponse = union(enum) {
        ok: ListCoreV1LimitRangeForAllNamespacesResponse200,
        unauthorized: ListCoreV1LimitRangeForAllNamespacesResponse401,
        default_response: ListCoreV1LimitRangeForAllNamespacesResponseDefault,
    };

    pub const listCoreV1LimitRangeForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind LimitRange
    pub fn listCoreV1LimitRangeForAllNamespaces(self: *Client, options: listCoreV1LimitRangeForAllNamespacesOptions) !ListCoreV1LimitRangeForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/limitranges", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.LimitRangeList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1LimitRangeForAllNamespaces(self: *Client, options: listCoreV1LimitRangeForAllNamespacesOptions) !WatchStream(root.io.k8s.api.core.v1.LimitRange) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/limitranges");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.LimitRange){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoreV1NamespaceResponse200 = struct {
        body: root.io.k8s.api.core.v1.NamespaceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespaceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespaceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespaceResponse = union(enum) {
        ok: ListCoreV1NamespaceResponse200,
        unauthorized: ListCoreV1NamespaceResponse401,
        default_response: ListCoreV1NamespaceResponseDefault,
    };

    pub const listCoreV1NamespaceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Namespace
    pub fn listCoreV1Namespace(self: *Client, options: listCoreV1NamespaceOptions) !ListCoreV1NamespaceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.NamespaceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1Namespace(self: *Client, options: listCoreV1NamespaceOptions) !WatchStream(root.io.k8s.api.core.v1.Namespace) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/namespaces");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.Namespace){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NamespaceResponse200 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespaceResponse201 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespaceResponse202 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespaceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespaceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespaceResponse = union(enum) {
        ok: CreateCoreV1NamespaceResponse200,
        created: CreateCoreV1NamespaceResponse201,
        accepted: CreateCoreV1NamespaceResponse202,
        unauthorized: CreateCoreV1NamespaceResponse401,
        default_response: CreateCoreV1NamespaceResponseDefault,
    };

    pub const createCoreV1NamespaceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a Namespace
    pub fn createCoreV1Namespace(self: *Client, body: root.io.k8s.api.core.v1.Namespace, options: createCoreV1NamespaceOptions) !CreateCoreV1NamespaceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const CreateCoreV1NamespacedBindingResponse200 = struct {
        body: root.io.k8s.api.core.v1.Binding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedBindingResponse201 = struct {
        body: root.io.k8s.api.core.v1.Binding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedBindingResponse202 = struct {
        body: root.io.k8s.api.core.v1.Binding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedBindingResponse = union(enum) {
        ok: CreateCoreV1NamespacedBindingResponse200,
        created: CreateCoreV1NamespacedBindingResponse201,
        accepted: CreateCoreV1NamespacedBindingResponse202,
        unauthorized: CreateCoreV1NamespacedBindingResponse401,
        default_response: CreateCoreV1NamespacedBindingResponseDefault,
    };

    pub const createCoreV1NamespacedBindingOptions = struct {
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// create a Binding
    pub fn createCoreV1NamespacedBinding(self: *Client, namespace: []const u8, body: root.io.k8s.api.core.v1.Binding, options: createCoreV1NamespacedBindingOptions) !CreateCoreV1NamespacedBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/bindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Binding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Binding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Binding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1NamespacedConfigMapResponse200 = struct {
        body: root.io.k8s.api.core.v1.ConfigMapList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedConfigMapResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedConfigMapResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedConfigMapResponse = union(enum) {
        ok: ListCoreV1NamespacedConfigMapResponse200,
        unauthorized: ListCoreV1NamespacedConfigMapResponse401,
        default_response: ListCoreV1NamespacedConfigMapResponseDefault,
    };

    pub const listCoreV1NamespacedConfigMapOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ConfigMap
    pub fn listCoreV1NamespacedConfigMap(self: *Client, namespace: []const u8, options: listCoreV1NamespacedConfigMapOptions) !ListCoreV1NamespacedConfigMapResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/configmaps", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ConfigMapList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1NamespacedConfigMap(self: *Client, namespace: []const u8, options: listCoreV1NamespacedConfigMapOptions) !WatchStream(root.io.k8s.api.core.v1.ConfigMap) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/configmaps");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.ConfigMap){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NamespacedConfigMapResponse200 = struct {
        body: root.io.k8s.api.core.v1.ConfigMap,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedConfigMapResponse201 = struct {
        body: root.io.k8s.api.core.v1.ConfigMap,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedConfigMapResponse202 = struct {
        body: root.io.k8s.api.core.v1.ConfigMap,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedConfigMapResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedConfigMapResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedConfigMapResponse = union(enum) {
        ok: CreateCoreV1NamespacedConfigMapResponse200,
        created: CreateCoreV1NamespacedConfigMapResponse201,
        accepted: CreateCoreV1NamespacedConfigMapResponse202,
        unauthorized: CreateCoreV1NamespacedConfigMapResponse401,
        default_response: CreateCoreV1NamespacedConfigMapResponseDefault,
    };

    pub const createCoreV1NamespacedConfigMapOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ConfigMap
    pub fn createCoreV1NamespacedConfigMap(self: *Client, namespace: []const u8, body: root.io.k8s.api.core.v1.ConfigMap, options: createCoreV1NamespacedConfigMapOptions) !CreateCoreV1NamespacedConfigMapResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/configmaps", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ConfigMap, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ConfigMap, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ConfigMap, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionNamespacedConfigMapResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedConfigMapResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedConfigMapResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedConfigMapResponse = union(enum) {
        ok: DeleteCoreV1CollectionNamespacedConfigMapResponse200,
        unauthorized: DeleteCoreV1CollectionNamespacedConfigMapResponse401,
        default_response: DeleteCoreV1CollectionNamespacedConfigMapResponseDefault,
    };

    pub const deleteCoreV1CollectionNamespacedConfigMapOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ConfigMap
    pub fn deleteCoreV1CollectionNamespacedConfigMap(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionNamespacedConfigMapOptions) !DeleteCoreV1CollectionNamespacedConfigMapResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/configmaps", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedConfigMapResponse200 = struct {
        body: root.io.k8s.api.core.v1.ConfigMap,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedConfigMapResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedConfigMapResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedConfigMapResponse = union(enum) {
        ok: ReadCoreV1NamespacedConfigMapResponse200,
        unauthorized: ReadCoreV1NamespacedConfigMapResponse401,
        default_response: ReadCoreV1NamespacedConfigMapResponseDefault,
    };

    pub const readCoreV1NamespacedConfigMapOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ConfigMap
    pub fn readCoreV1NamespacedConfigMap(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedConfigMapOptions) !ReadCoreV1NamespacedConfigMapResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/configmaps/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ConfigMap, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedConfigMapResponse200 = struct {
        body: root.io.k8s.api.core.v1.ConfigMap,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedConfigMapResponse201 = struct {
        body: root.io.k8s.api.core.v1.ConfigMap,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedConfigMapResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedConfigMapResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedConfigMapResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedConfigMapResponse200,
        created: ReplaceCoreV1NamespacedConfigMapResponse201,
        unauthorized: ReplaceCoreV1NamespacedConfigMapResponse401,
        default_response: ReplaceCoreV1NamespacedConfigMapResponseDefault,
    };

    pub const replaceCoreV1NamespacedConfigMapOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ConfigMap
    pub fn replaceCoreV1NamespacedConfigMap(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.ConfigMap, options: replaceCoreV1NamespacedConfigMapOptions) !ReplaceCoreV1NamespacedConfigMapResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/configmaps/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ConfigMap, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ConfigMap, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NamespacedConfigMapResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedConfigMapResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedConfigMapResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedConfigMapResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedConfigMapResponse = union(enum) {
        ok: DeleteCoreV1NamespacedConfigMapResponse200,
        accepted: DeleteCoreV1NamespacedConfigMapResponse202,
        unauthorized: DeleteCoreV1NamespacedConfigMapResponse401,
        default_response: DeleteCoreV1NamespacedConfigMapResponseDefault,
    };

    pub const deleteCoreV1NamespacedConfigMapOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ConfigMap
    pub fn deleteCoreV1NamespacedConfigMap(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NamespacedConfigMapOptions) !DeleteCoreV1NamespacedConfigMapResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/configmaps/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedConfigMapResponse200 = struct {
        body: root.io.k8s.api.core.v1.ConfigMap,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedConfigMapResponse201 = struct {
        body: root.io.k8s.api.core.v1.ConfigMap,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedConfigMapResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedConfigMapResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedConfigMapResponse = union(enum) {
        ok: PatchCoreV1NamespacedConfigMapResponse200,
        created: PatchCoreV1NamespacedConfigMapResponse201,
        unauthorized: PatchCoreV1NamespacedConfigMapResponse401,
        default_response: PatchCoreV1NamespacedConfigMapResponseDefault,
    };

    pub const patchCoreV1NamespacedConfigMapOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ConfigMap
    pub fn patchCoreV1NamespacedConfigMap(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedConfigMapOptions) !PatchCoreV1NamespacedConfigMapResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/configmaps/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ConfigMap, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ConfigMap, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1NamespacedEndpointsResponse200 = struct {
        body: root.io.k8s.api.core.v1.EndpointsList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedEndpointsResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedEndpointsResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedEndpointsResponse = union(enum) {
        ok: ListCoreV1NamespacedEndpointsResponse200,
        unauthorized: ListCoreV1NamespacedEndpointsResponse401,
        default_response: ListCoreV1NamespacedEndpointsResponseDefault,
    };

    pub const listCoreV1NamespacedEndpointsOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Endpoints
    pub fn listCoreV1NamespacedEndpoints(self: *Client, namespace: []const u8, options: listCoreV1NamespacedEndpointsOptions) !ListCoreV1NamespacedEndpointsResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpoints", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.EndpointsList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1NamespacedEndpoints(self: *Client, namespace: []const u8, options: listCoreV1NamespacedEndpointsOptions) !WatchStream(root.io.k8s.api.core.v1.Endpoints) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/endpoints");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.Endpoints){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NamespacedEndpointsResponse200 = struct {
        body: root.io.k8s.api.core.v1.Endpoints,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedEndpointsResponse201 = struct {
        body: root.io.k8s.api.core.v1.Endpoints,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedEndpointsResponse202 = struct {
        body: root.io.k8s.api.core.v1.Endpoints,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedEndpointsResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedEndpointsResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedEndpointsResponse = union(enum) {
        ok: CreateCoreV1NamespacedEndpointsResponse200,
        created: CreateCoreV1NamespacedEndpointsResponse201,
        accepted: CreateCoreV1NamespacedEndpointsResponse202,
        unauthorized: CreateCoreV1NamespacedEndpointsResponse401,
        default_response: CreateCoreV1NamespacedEndpointsResponseDefault,
    };

    pub const createCoreV1NamespacedEndpointsOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create Endpoints
    pub fn createCoreV1NamespacedEndpoints(self: *Client, namespace: []const u8, body: root.io.k8s.api.core.v1.Endpoints, options: createCoreV1NamespacedEndpointsOptions) !CreateCoreV1NamespacedEndpointsResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpoints", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Endpoints, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Endpoints, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Endpoints, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionNamespacedEndpointsResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedEndpointsResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedEndpointsResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedEndpointsResponse = union(enum) {
        ok: DeleteCoreV1CollectionNamespacedEndpointsResponse200,
        unauthorized: DeleteCoreV1CollectionNamespacedEndpointsResponse401,
        default_response: DeleteCoreV1CollectionNamespacedEndpointsResponseDefault,
    };

    pub const deleteCoreV1CollectionNamespacedEndpointsOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Endpoints
    pub fn deleteCoreV1CollectionNamespacedEndpoints(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionNamespacedEndpointsOptions) !DeleteCoreV1CollectionNamespacedEndpointsResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpoints", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedEndpointsResponse200 = struct {
        body: root.io.k8s.api.core.v1.Endpoints,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedEndpointsResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedEndpointsResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedEndpointsResponse = union(enum) {
        ok: ReadCoreV1NamespacedEndpointsResponse200,
        unauthorized: ReadCoreV1NamespacedEndpointsResponse401,
        default_response: ReadCoreV1NamespacedEndpointsResponseDefault,
    };

    pub const readCoreV1NamespacedEndpointsOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Endpoints
    pub fn readCoreV1NamespacedEndpoints(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedEndpointsOptions) !ReadCoreV1NamespacedEndpointsResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpoints/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Endpoints, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedEndpointsResponse200 = struct {
        body: root.io.k8s.api.core.v1.Endpoints,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedEndpointsResponse201 = struct {
        body: root.io.k8s.api.core.v1.Endpoints,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedEndpointsResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedEndpointsResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedEndpointsResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedEndpointsResponse200,
        created: ReplaceCoreV1NamespacedEndpointsResponse201,
        unauthorized: ReplaceCoreV1NamespacedEndpointsResponse401,
        default_response: ReplaceCoreV1NamespacedEndpointsResponseDefault,
    };

    pub const replaceCoreV1NamespacedEndpointsOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Endpoints
    pub fn replaceCoreV1NamespacedEndpoints(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.Endpoints, options: replaceCoreV1NamespacedEndpointsOptions) !ReplaceCoreV1NamespacedEndpointsResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpoints/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Endpoints, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Endpoints, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NamespacedEndpointsResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedEndpointsResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedEndpointsResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedEndpointsResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedEndpointsResponse = union(enum) {
        ok: DeleteCoreV1NamespacedEndpointsResponse200,
        accepted: DeleteCoreV1NamespacedEndpointsResponse202,
        unauthorized: DeleteCoreV1NamespacedEndpointsResponse401,
        default_response: DeleteCoreV1NamespacedEndpointsResponseDefault,
    };

    pub const deleteCoreV1NamespacedEndpointsOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete Endpoints
    pub fn deleteCoreV1NamespacedEndpoints(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NamespacedEndpointsOptions) !DeleteCoreV1NamespacedEndpointsResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpoints/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedEndpointsResponse200 = struct {
        body: root.io.k8s.api.core.v1.Endpoints,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedEndpointsResponse201 = struct {
        body: root.io.k8s.api.core.v1.Endpoints,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedEndpointsResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedEndpointsResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedEndpointsResponse = union(enum) {
        ok: PatchCoreV1NamespacedEndpointsResponse200,
        created: PatchCoreV1NamespacedEndpointsResponse201,
        unauthorized: PatchCoreV1NamespacedEndpointsResponse401,
        default_response: PatchCoreV1NamespacedEndpointsResponseDefault,
    };

    pub const patchCoreV1NamespacedEndpointsOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Endpoints
    pub fn patchCoreV1NamespacedEndpoints(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedEndpointsOptions) !PatchCoreV1NamespacedEndpointsResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpoints/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Endpoints, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Endpoints, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.api.core.v1.EventList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedEventResponse = union(enum) {
        ok: ListCoreV1NamespacedEventResponse200,
        unauthorized: ListCoreV1NamespacedEventResponse401,
        default_response: ListCoreV1NamespacedEventResponseDefault,
    };

    pub const listCoreV1NamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Event
    pub fn listCoreV1NamespacedEvent(self: *Client, namespace: []const u8, options: listCoreV1NamespacedEventOptions) !ListCoreV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.EventList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1NamespacedEvent(self: *Client, namespace: []const u8, options: listCoreV1NamespacedEventOptions) !WatchStream(root.io.k8s.api.core.v1.Event) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/events");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.Event){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.api.core.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedEventResponse201 = struct {
        body: root.io.k8s.api.core.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedEventResponse202 = struct {
        body: root.io.k8s.api.core.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedEventResponse = union(enum) {
        ok: CreateCoreV1NamespacedEventResponse200,
        created: CreateCoreV1NamespacedEventResponse201,
        accepted: CreateCoreV1NamespacedEventResponse202,
        unauthorized: CreateCoreV1NamespacedEventResponse401,
        default_response: CreateCoreV1NamespacedEventResponseDefault,
    };

    pub const createCoreV1NamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create an Event
    pub fn createCoreV1NamespacedEvent(self: *Client, namespace: []const u8, body: root.io.k8s.api.core.v1.Event, options: createCoreV1NamespacedEventOptions) !CreateCoreV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionNamespacedEventResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedEventResponse = union(enum) {
        ok: DeleteCoreV1CollectionNamespacedEventResponse200,
        unauthorized: DeleteCoreV1CollectionNamespacedEventResponse401,
        default_response: DeleteCoreV1CollectionNamespacedEventResponseDefault,
    };

    pub const deleteCoreV1CollectionNamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Event
    pub fn deleteCoreV1CollectionNamespacedEvent(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionNamespacedEventOptions) !DeleteCoreV1CollectionNamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.api.core.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedEventResponse = union(enum) {
        ok: ReadCoreV1NamespacedEventResponse200,
        unauthorized: ReadCoreV1NamespacedEventResponse401,
        default_response: ReadCoreV1NamespacedEventResponseDefault,
    };

    pub const readCoreV1NamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Event
    pub fn readCoreV1NamespacedEvent(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedEventOptions) !ReadCoreV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.api.core.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedEventResponse201 = struct {
        body: root.io.k8s.api.core.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedEventResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedEventResponse200,
        created: ReplaceCoreV1NamespacedEventResponse201,
        unauthorized: ReplaceCoreV1NamespacedEventResponse401,
        default_response: ReplaceCoreV1NamespacedEventResponseDefault,
    };

    pub const replaceCoreV1NamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Event
    pub fn replaceCoreV1NamespacedEvent(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.Event, options: replaceCoreV1NamespacedEventOptions) !ReplaceCoreV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedEventResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedEventResponse = union(enum) {
        ok: DeleteCoreV1NamespacedEventResponse200,
        accepted: DeleteCoreV1NamespacedEventResponse202,
        unauthorized: DeleteCoreV1NamespacedEventResponse401,
        default_response: DeleteCoreV1NamespacedEventResponseDefault,
    };

    pub const deleteCoreV1NamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete an Event
    pub fn deleteCoreV1NamespacedEvent(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NamespacedEventOptions) !DeleteCoreV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.api.core.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedEventResponse201 = struct {
        body: root.io.k8s.api.core.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedEventResponse = union(enum) {
        ok: PatchCoreV1NamespacedEventResponse200,
        created: PatchCoreV1NamespacedEventResponse201,
        unauthorized: PatchCoreV1NamespacedEventResponse401,
        default_response: PatchCoreV1NamespacedEventResponseDefault,
    };

    pub const patchCoreV1NamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Event
    pub fn patchCoreV1NamespacedEvent(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedEventOptions) !PatchCoreV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1NamespacedLimitRangeResponse200 = struct {
        body: root.io.k8s.api.core.v1.LimitRangeList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedLimitRangeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedLimitRangeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedLimitRangeResponse = union(enum) {
        ok: ListCoreV1NamespacedLimitRangeResponse200,
        unauthorized: ListCoreV1NamespacedLimitRangeResponse401,
        default_response: ListCoreV1NamespacedLimitRangeResponseDefault,
    };

    pub const listCoreV1NamespacedLimitRangeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind LimitRange
    pub fn listCoreV1NamespacedLimitRange(self: *Client, namespace: []const u8, options: listCoreV1NamespacedLimitRangeOptions) !ListCoreV1NamespacedLimitRangeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/limitranges", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.LimitRangeList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1NamespacedLimitRange(self: *Client, namespace: []const u8, options: listCoreV1NamespacedLimitRangeOptions) !WatchStream(root.io.k8s.api.core.v1.LimitRange) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/limitranges");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.LimitRange){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NamespacedLimitRangeResponse200 = struct {
        body: root.io.k8s.api.core.v1.LimitRange,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedLimitRangeResponse201 = struct {
        body: root.io.k8s.api.core.v1.LimitRange,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedLimitRangeResponse202 = struct {
        body: root.io.k8s.api.core.v1.LimitRange,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedLimitRangeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedLimitRangeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedLimitRangeResponse = union(enum) {
        ok: CreateCoreV1NamespacedLimitRangeResponse200,
        created: CreateCoreV1NamespacedLimitRangeResponse201,
        accepted: CreateCoreV1NamespacedLimitRangeResponse202,
        unauthorized: CreateCoreV1NamespacedLimitRangeResponse401,
        default_response: CreateCoreV1NamespacedLimitRangeResponseDefault,
    };

    pub const createCoreV1NamespacedLimitRangeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a LimitRange
    pub fn createCoreV1NamespacedLimitRange(self: *Client, namespace: []const u8, body: root.io.k8s.api.core.v1.LimitRange, options: createCoreV1NamespacedLimitRangeOptions) !CreateCoreV1NamespacedLimitRangeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/limitranges", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.LimitRange, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.LimitRange, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.LimitRange, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionNamespacedLimitRangeResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedLimitRangeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedLimitRangeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedLimitRangeResponse = union(enum) {
        ok: DeleteCoreV1CollectionNamespacedLimitRangeResponse200,
        unauthorized: DeleteCoreV1CollectionNamespacedLimitRangeResponse401,
        default_response: DeleteCoreV1CollectionNamespacedLimitRangeResponseDefault,
    };

    pub const deleteCoreV1CollectionNamespacedLimitRangeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of LimitRange
    pub fn deleteCoreV1CollectionNamespacedLimitRange(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionNamespacedLimitRangeOptions) !DeleteCoreV1CollectionNamespacedLimitRangeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/limitranges", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedLimitRangeResponse200 = struct {
        body: root.io.k8s.api.core.v1.LimitRange,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedLimitRangeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedLimitRangeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedLimitRangeResponse = union(enum) {
        ok: ReadCoreV1NamespacedLimitRangeResponse200,
        unauthorized: ReadCoreV1NamespacedLimitRangeResponse401,
        default_response: ReadCoreV1NamespacedLimitRangeResponseDefault,
    };

    pub const readCoreV1NamespacedLimitRangeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified LimitRange
    pub fn readCoreV1NamespacedLimitRange(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedLimitRangeOptions) !ReadCoreV1NamespacedLimitRangeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/limitranges/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.LimitRange, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedLimitRangeResponse200 = struct {
        body: root.io.k8s.api.core.v1.LimitRange,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedLimitRangeResponse201 = struct {
        body: root.io.k8s.api.core.v1.LimitRange,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedLimitRangeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedLimitRangeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedLimitRangeResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedLimitRangeResponse200,
        created: ReplaceCoreV1NamespacedLimitRangeResponse201,
        unauthorized: ReplaceCoreV1NamespacedLimitRangeResponse401,
        default_response: ReplaceCoreV1NamespacedLimitRangeResponseDefault,
    };

    pub const replaceCoreV1NamespacedLimitRangeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified LimitRange
    pub fn replaceCoreV1NamespacedLimitRange(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.LimitRange, options: replaceCoreV1NamespacedLimitRangeOptions) !ReplaceCoreV1NamespacedLimitRangeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/limitranges/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.LimitRange, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.LimitRange, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NamespacedLimitRangeResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedLimitRangeResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedLimitRangeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedLimitRangeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedLimitRangeResponse = union(enum) {
        ok: DeleteCoreV1NamespacedLimitRangeResponse200,
        accepted: DeleteCoreV1NamespacedLimitRangeResponse202,
        unauthorized: DeleteCoreV1NamespacedLimitRangeResponse401,
        default_response: DeleteCoreV1NamespacedLimitRangeResponseDefault,
    };

    pub const deleteCoreV1NamespacedLimitRangeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a LimitRange
    pub fn deleteCoreV1NamespacedLimitRange(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NamespacedLimitRangeOptions) !DeleteCoreV1NamespacedLimitRangeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/limitranges/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedLimitRangeResponse200 = struct {
        body: root.io.k8s.api.core.v1.LimitRange,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedLimitRangeResponse201 = struct {
        body: root.io.k8s.api.core.v1.LimitRange,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedLimitRangeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedLimitRangeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedLimitRangeResponse = union(enum) {
        ok: PatchCoreV1NamespacedLimitRangeResponse200,
        created: PatchCoreV1NamespacedLimitRangeResponse201,
        unauthorized: PatchCoreV1NamespacedLimitRangeResponse401,
        default_response: PatchCoreV1NamespacedLimitRangeResponseDefault,
    };

    pub const patchCoreV1NamespacedLimitRangeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified LimitRange
    pub fn patchCoreV1NamespacedLimitRange(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedLimitRangeOptions) !PatchCoreV1NamespacedLimitRangeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/limitranges/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.LimitRange, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.LimitRange, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1NamespacedPersistentVolumeClaimResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaimList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedPersistentVolumeClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedPersistentVolumeClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedPersistentVolumeClaimResponse = union(enum) {
        ok: ListCoreV1NamespacedPersistentVolumeClaimResponse200,
        unauthorized: ListCoreV1NamespacedPersistentVolumeClaimResponse401,
        default_response: ListCoreV1NamespacedPersistentVolumeClaimResponseDefault,
    };

    pub const listCoreV1NamespacedPersistentVolumeClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PersistentVolumeClaim
    pub fn listCoreV1NamespacedPersistentVolumeClaim(self: *Client, namespace: []const u8, options: listCoreV1NamespacedPersistentVolumeClaimOptions) !ListCoreV1NamespacedPersistentVolumeClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/persistentvolumeclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaimList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1NamespacedPersistentVolumeClaim(self: *Client, namespace: []const u8, options: listCoreV1NamespacedPersistentVolumeClaimOptions) !WatchStream(root.io.k8s.api.core.v1.PersistentVolumeClaim) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/persistentvolumeclaims");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.PersistentVolumeClaim){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NamespacedPersistentVolumeClaimResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPersistentVolumeClaimResponse201 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPersistentVolumeClaimResponse202 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPersistentVolumeClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPersistentVolumeClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPersistentVolumeClaimResponse = union(enum) {
        ok: CreateCoreV1NamespacedPersistentVolumeClaimResponse200,
        created: CreateCoreV1NamespacedPersistentVolumeClaimResponse201,
        accepted: CreateCoreV1NamespacedPersistentVolumeClaimResponse202,
        unauthorized: CreateCoreV1NamespacedPersistentVolumeClaimResponse401,
        default_response: CreateCoreV1NamespacedPersistentVolumeClaimResponseDefault,
    };

    pub const createCoreV1NamespacedPersistentVolumeClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a PersistentVolumeClaim
    pub fn createCoreV1NamespacedPersistentVolumeClaim(self: *Client, namespace: []const u8, body: root.io.k8s.api.core.v1.PersistentVolumeClaim, options: createCoreV1NamespacedPersistentVolumeClaimOptions) !CreateCoreV1NamespacedPersistentVolumeClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/persistentvolumeclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionNamespacedPersistentVolumeClaimResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedPersistentVolumeClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedPersistentVolumeClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedPersistentVolumeClaimResponse = union(enum) {
        ok: DeleteCoreV1CollectionNamespacedPersistentVolumeClaimResponse200,
        unauthorized: DeleteCoreV1CollectionNamespacedPersistentVolumeClaimResponse401,
        default_response: DeleteCoreV1CollectionNamespacedPersistentVolumeClaimResponseDefault,
    };

    pub const deleteCoreV1CollectionNamespacedPersistentVolumeClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of PersistentVolumeClaim
    pub fn deleteCoreV1CollectionNamespacedPersistentVolumeClaim(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionNamespacedPersistentVolumeClaimOptions) !DeleteCoreV1CollectionNamespacedPersistentVolumeClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/persistentvolumeclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedPersistentVolumeClaimResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPersistentVolumeClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPersistentVolumeClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPersistentVolumeClaimResponse = union(enum) {
        ok: ReadCoreV1NamespacedPersistentVolumeClaimResponse200,
        unauthorized: ReadCoreV1NamespacedPersistentVolumeClaimResponse401,
        default_response: ReadCoreV1NamespacedPersistentVolumeClaimResponseDefault,
    };

    pub const readCoreV1NamespacedPersistentVolumeClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified PersistentVolumeClaim
    pub fn readCoreV1NamespacedPersistentVolumeClaim(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedPersistentVolumeClaimOptions) !ReadCoreV1NamespacedPersistentVolumeClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/persistentvolumeclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedPersistentVolumeClaimResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPersistentVolumeClaimResponse201 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPersistentVolumeClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPersistentVolumeClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPersistentVolumeClaimResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedPersistentVolumeClaimResponse200,
        created: ReplaceCoreV1NamespacedPersistentVolumeClaimResponse201,
        unauthorized: ReplaceCoreV1NamespacedPersistentVolumeClaimResponse401,
        default_response: ReplaceCoreV1NamespacedPersistentVolumeClaimResponseDefault,
    };

    pub const replaceCoreV1NamespacedPersistentVolumeClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified PersistentVolumeClaim
    pub fn replaceCoreV1NamespacedPersistentVolumeClaim(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.PersistentVolumeClaim, options: replaceCoreV1NamespacedPersistentVolumeClaimOptions) !ReplaceCoreV1NamespacedPersistentVolumeClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/persistentvolumeclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NamespacedPersistentVolumeClaimResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedPersistentVolumeClaimResponse202 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedPersistentVolumeClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedPersistentVolumeClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedPersistentVolumeClaimResponse = union(enum) {
        ok: DeleteCoreV1NamespacedPersistentVolumeClaimResponse200,
        accepted: DeleteCoreV1NamespacedPersistentVolumeClaimResponse202,
        unauthorized: DeleteCoreV1NamespacedPersistentVolumeClaimResponse401,
        default_response: DeleteCoreV1NamespacedPersistentVolumeClaimResponseDefault,
    };

    pub const deleteCoreV1NamespacedPersistentVolumeClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a PersistentVolumeClaim
    pub fn deleteCoreV1NamespacedPersistentVolumeClaim(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NamespacedPersistentVolumeClaimOptions) !DeleteCoreV1NamespacedPersistentVolumeClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/persistentvolumeclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedPersistentVolumeClaimResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPersistentVolumeClaimResponse201 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPersistentVolumeClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPersistentVolumeClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPersistentVolumeClaimResponse = union(enum) {
        ok: PatchCoreV1NamespacedPersistentVolumeClaimResponse200,
        created: PatchCoreV1NamespacedPersistentVolumeClaimResponse201,
        unauthorized: PatchCoreV1NamespacedPersistentVolumeClaimResponse401,
        default_response: PatchCoreV1NamespacedPersistentVolumeClaimResponseDefault,
    };

    pub const patchCoreV1NamespacedPersistentVolumeClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified PersistentVolumeClaim
    pub fn patchCoreV1NamespacedPersistentVolumeClaim(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedPersistentVolumeClaimOptions) !PatchCoreV1NamespacedPersistentVolumeClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/persistentvolumeclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedPersistentVolumeClaimStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPersistentVolumeClaimStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPersistentVolumeClaimStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPersistentVolumeClaimStatusResponse = union(enum) {
        ok: ReadCoreV1NamespacedPersistentVolumeClaimStatusResponse200,
        unauthorized: ReadCoreV1NamespacedPersistentVolumeClaimStatusResponse401,
        default_response: ReadCoreV1NamespacedPersistentVolumeClaimStatusResponseDefault,
    };

    pub const readCoreV1NamespacedPersistentVolumeClaimStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified PersistentVolumeClaim
    pub fn readCoreV1NamespacedPersistentVolumeClaimStatus(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedPersistentVolumeClaimStatusOptions) !ReadCoreV1NamespacedPersistentVolumeClaimStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/persistentvolumeclaims/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedPersistentVolumeClaimStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPersistentVolumeClaimStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPersistentVolumeClaimStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPersistentVolumeClaimStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPersistentVolumeClaimStatusResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedPersistentVolumeClaimStatusResponse200,
        created: ReplaceCoreV1NamespacedPersistentVolumeClaimStatusResponse201,
        unauthorized: ReplaceCoreV1NamespacedPersistentVolumeClaimStatusResponse401,
        default_response: ReplaceCoreV1NamespacedPersistentVolumeClaimStatusResponseDefault,
    };

    pub const replaceCoreV1NamespacedPersistentVolumeClaimStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified PersistentVolumeClaim
    pub fn replaceCoreV1NamespacedPersistentVolumeClaimStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.PersistentVolumeClaim, options: replaceCoreV1NamespacedPersistentVolumeClaimStatusOptions) !ReplaceCoreV1NamespacedPersistentVolumeClaimStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/persistentvolumeclaims/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedPersistentVolumeClaimStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPersistentVolumeClaimStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPersistentVolumeClaimStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPersistentVolumeClaimStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPersistentVolumeClaimStatusResponse = union(enum) {
        ok: PatchCoreV1NamespacedPersistentVolumeClaimStatusResponse200,
        created: PatchCoreV1NamespacedPersistentVolumeClaimStatusResponse201,
        unauthorized: PatchCoreV1NamespacedPersistentVolumeClaimStatusResponse401,
        default_response: PatchCoreV1NamespacedPersistentVolumeClaimStatusResponseDefault,
    };

    pub const patchCoreV1NamespacedPersistentVolumeClaimStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified PersistentVolumeClaim
    pub fn patchCoreV1NamespacedPersistentVolumeClaimStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedPersistentVolumeClaimStatusOptions) !PatchCoreV1NamespacedPersistentVolumeClaimStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/persistentvolumeclaims/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1NamespacedPodResponse200 = struct {
        body: root.io.k8s.api.core.v1.PodList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedPodResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedPodResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedPodResponse = union(enum) {
        ok: ListCoreV1NamespacedPodResponse200,
        unauthorized: ListCoreV1NamespacedPodResponse401,
        default_response: ListCoreV1NamespacedPodResponseDefault,
    };

    pub const listCoreV1NamespacedPodOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Pod
    pub fn listCoreV1NamespacedPod(self: *Client, namespace: []const u8, options: listCoreV1NamespacedPodOptions) !ListCoreV1NamespacedPodResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1NamespacedPod(self: *Client, namespace: []const u8, options: listCoreV1NamespacedPodOptions) !WatchStream(root.io.k8s.api.core.v1.Pod) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/pods");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.Pod){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NamespacedPodResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodResponse201 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodResponse202 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodResponse = union(enum) {
        ok: CreateCoreV1NamespacedPodResponse200,
        created: CreateCoreV1NamespacedPodResponse201,
        accepted: CreateCoreV1NamespacedPodResponse202,
        unauthorized: CreateCoreV1NamespacedPodResponse401,
        default_response: CreateCoreV1NamespacedPodResponseDefault,
    };

    pub const createCoreV1NamespacedPodOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a Pod
    pub fn createCoreV1NamespacedPod(self: *Client, namespace: []const u8, body: root.io.k8s.api.core.v1.Pod, options: createCoreV1NamespacedPodOptions) !CreateCoreV1NamespacedPodResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionNamespacedPodResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedPodResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedPodResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedPodResponse = union(enum) {
        ok: DeleteCoreV1CollectionNamespacedPodResponse200,
        unauthorized: DeleteCoreV1CollectionNamespacedPodResponse401,
        default_response: DeleteCoreV1CollectionNamespacedPodResponseDefault,
    };

    pub const deleteCoreV1CollectionNamespacedPodOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Pod
    pub fn deleteCoreV1CollectionNamespacedPod(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionNamespacedPodOptions) !DeleteCoreV1CollectionNamespacedPodResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedPodResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodResponse = union(enum) {
        ok: ReadCoreV1NamespacedPodResponse200,
        unauthorized: ReadCoreV1NamespacedPodResponse401,
        default_response: ReadCoreV1NamespacedPodResponseDefault,
    };

    pub const readCoreV1NamespacedPodOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Pod
    pub fn readCoreV1NamespacedPod(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedPodOptions) !ReadCoreV1NamespacedPodResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedPodResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodResponse201 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedPodResponse200,
        created: ReplaceCoreV1NamespacedPodResponse201,
        unauthorized: ReplaceCoreV1NamespacedPodResponse401,
        default_response: ReplaceCoreV1NamespacedPodResponseDefault,
    };

    pub const replaceCoreV1NamespacedPodOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Pod
    pub fn replaceCoreV1NamespacedPod(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.Pod, options: replaceCoreV1NamespacedPodOptions) !ReplaceCoreV1NamespacedPodResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NamespacedPodResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedPodResponse202 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedPodResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedPodResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedPodResponse = union(enum) {
        ok: DeleteCoreV1NamespacedPodResponse200,
        accepted: DeleteCoreV1NamespacedPodResponse202,
        unauthorized: DeleteCoreV1NamespacedPodResponse401,
        default_response: DeleteCoreV1NamespacedPodResponseDefault,
    };

    pub const deleteCoreV1NamespacedPodOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a Pod
    pub fn deleteCoreV1NamespacedPod(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NamespacedPodOptions) !DeleteCoreV1NamespacedPodResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedPodResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodResponse201 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodResponse = union(enum) {
        ok: PatchCoreV1NamespacedPodResponse200,
        created: PatchCoreV1NamespacedPodResponse201,
        unauthorized: PatchCoreV1NamespacedPodResponse401,
        default_response: PatchCoreV1NamespacedPodResponseDefault,
    };

    pub const patchCoreV1NamespacedPodOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Pod
    pub fn patchCoreV1NamespacedPod(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedPodOptions) !PatchCoreV1NamespacedPodResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1GetNamespacedPodAttachResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodAttachResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodAttachResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodAttachResponse = union(enum) {
        ok: ConnectCoreV1GetNamespacedPodAttachResponse200,
        unauthorized: ConnectCoreV1GetNamespacedPodAttachResponse401,
        default_response: ConnectCoreV1GetNamespacedPodAttachResponseDefault,
    };

    pub const connectCoreV1GetNamespacedPodAttachOptions = struct {
        /// The container in which to execute the command. Defaults to only container if there is only one container in the pod.
        container: ?[]const u8 = null,
        /// Stderr if true indicates that stderr is to be redirected for the attach call. Defaults to true.
        stderr: ?[]const u8 = null,
        /// Stdin if true, redirects the standard input stream of the pod for this call. Defaults to false.
        stdin: ?[]const u8 = null,
        /// Stdout if true indicates that stdout is to be redirected for the attach call. Defaults to true.
        stdout: ?[]const u8 = null,
        /// TTY if true indicates that a tty will be allocated for the attach call. This is passed through the container runtime so the tty is allocated on the worker node by the container runtime. Defaults to false.
        tty: ?[]const u8 = null,
    };
    /// connect GET requests to attach of Pod
    pub fn connectCoreV1GetNamespacedPodAttach(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1GetNamespacedPodAttachOptions) !ConnectCoreV1GetNamespacedPodAttachResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/attach", .{});
        var first_query = true;
        _ = &first_query;
        if (options.container) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("container=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.stderr) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("stderr=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.stdin) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("stdin=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.stdout) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("stdout=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.tty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("tty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PostNamespacedPodAttachResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodAttachResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodAttachResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodAttachResponse = union(enum) {
        ok: ConnectCoreV1PostNamespacedPodAttachResponse200,
        unauthorized: ConnectCoreV1PostNamespacedPodAttachResponse401,
        default_response: ConnectCoreV1PostNamespacedPodAttachResponseDefault,
    };

    pub const connectCoreV1PostNamespacedPodAttachOptions = struct {
        /// The container in which to execute the command. Defaults to only container if there is only one container in the pod.
        container: ?[]const u8 = null,
        /// Stderr if true indicates that stderr is to be redirected for the attach call. Defaults to true.
        stderr: ?[]const u8 = null,
        /// Stdin if true, redirects the standard input stream of the pod for this call. Defaults to false.
        stdin: ?[]const u8 = null,
        /// Stdout if true indicates that stdout is to be redirected for the attach call. Defaults to true.
        stdout: ?[]const u8 = null,
        /// TTY if true indicates that a tty will be allocated for the attach call. This is passed through the container runtime so the tty is allocated on the worker node by the container runtime. Defaults to false.
        tty: ?[]const u8 = null,
    };
    /// connect POST requests to attach of Pod
    pub fn connectCoreV1PostNamespacedPodAttach(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1PostNamespacedPodAttachOptions) !ConnectCoreV1PostNamespacedPodAttachResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/attach", .{});
        var first_query = true;
        _ = &first_query;
        if (options.container) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("container=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.stderr) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("stderr=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.stdin) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("stdin=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.stdout) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("stdout=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.tty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("tty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const CreateCoreV1NamespacedPodBindingResponse200 = struct {
        body: root.io.k8s.api.core.v1.Binding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodBindingResponse201 = struct {
        body: root.io.k8s.api.core.v1.Binding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodBindingResponse202 = struct {
        body: root.io.k8s.api.core.v1.Binding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodBindingResponse = union(enum) {
        ok: CreateCoreV1NamespacedPodBindingResponse200,
        created: CreateCoreV1NamespacedPodBindingResponse201,
        accepted: CreateCoreV1NamespacedPodBindingResponse202,
        unauthorized: CreateCoreV1NamespacedPodBindingResponse401,
        default_response: CreateCoreV1NamespacedPodBindingResponseDefault,
    };

    pub const createCoreV1NamespacedPodBindingOptions = struct {
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// create binding of a Pod
    pub fn createCoreV1NamespacedPodBinding(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.Binding, options: createCoreV1NamespacedPodBindingOptions) !CreateCoreV1NamespacedPodBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/binding", .{});
        var first_query = true;
        _ = &first_query;
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Binding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Binding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Binding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedPodEphemeralcontainersResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodEphemeralcontainersResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodEphemeralcontainersResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodEphemeralcontainersResponse = union(enum) {
        ok: ReadCoreV1NamespacedPodEphemeralcontainersResponse200,
        unauthorized: ReadCoreV1NamespacedPodEphemeralcontainersResponse401,
        default_response: ReadCoreV1NamespacedPodEphemeralcontainersResponseDefault,
    };

    pub const readCoreV1NamespacedPodEphemeralcontainersOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read ephemeralcontainers of the specified Pod
    pub fn readCoreV1NamespacedPodEphemeralcontainers(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedPodEphemeralcontainersOptions) !ReadCoreV1NamespacedPodEphemeralcontainersResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/ephemeralcontainers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedPodEphemeralcontainersResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodEphemeralcontainersResponse201 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodEphemeralcontainersResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodEphemeralcontainersResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodEphemeralcontainersResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedPodEphemeralcontainersResponse200,
        created: ReplaceCoreV1NamespacedPodEphemeralcontainersResponse201,
        unauthorized: ReplaceCoreV1NamespacedPodEphemeralcontainersResponse401,
        default_response: ReplaceCoreV1NamespacedPodEphemeralcontainersResponseDefault,
    };

    pub const replaceCoreV1NamespacedPodEphemeralcontainersOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace ephemeralcontainers of the specified Pod
    pub fn replaceCoreV1NamespacedPodEphemeralcontainers(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.Pod, options: replaceCoreV1NamespacedPodEphemeralcontainersOptions) !ReplaceCoreV1NamespacedPodEphemeralcontainersResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/ephemeralcontainers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedPodEphemeralcontainersResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodEphemeralcontainersResponse201 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodEphemeralcontainersResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodEphemeralcontainersResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodEphemeralcontainersResponse = union(enum) {
        ok: PatchCoreV1NamespacedPodEphemeralcontainersResponse200,
        created: PatchCoreV1NamespacedPodEphemeralcontainersResponse201,
        unauthorized: PatchCoreV1NamespacedPodEphemeralcontainersResponse401,
        default_response: PatchCoreV1NamespacedPodEphemeralcontainersResponseDefault,
    };

    pub const patchCoreV1NamespacedPodEphemeralcontainersOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update ephemeralcontainers of the specified Pod
    pub fn patchCoreV1NamespacedPodEphemeralcontainers(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedPodEphemeralcontainersOptions) !PatchCoreV1NamespacedPodEphemeralcontainersResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/ephemeralcontainers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const CreateCoreV1NamespacedPodEvictionResponse200 = struct {
        body: root.io.k8s.api.policy.v1.Eviction,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodEvictionResponse201 = struct {
        body: root.io.k8s.api.policy.v1.Eviction,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodEvictionResponse202 = struct {
        body: root.io.k8s.api.policy.v1.Eviction,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodEvictionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodEvictionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodEvictionResponse = union(enum) {
        ok: CreateCoreV1NamespacedPodEvictionResponse200,
        created: CreateCoreV1NamespacedPodEvictionResponse201,
        accepted: CreateCoreV1NamespacedPodEvictionResponse202,
        unauthorized: CreateCoreV1NamespacedPodEvictionResponse401,
        default_response: CreateCoreV1NamespacedPodEvictionResponseDefault,
    };

    pub const createCoreV1NamespacedPodEvictionOptions = struct {
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// create eviction of a Pod
    pub fn createCoreV1NamespacedPodEviction(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.policy.v1.Eviction, options: createCoreV1NamespacedPodEvictionOptions) !CreateCoreV1NamespacedPodEvictionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/eviction", .{});
        var first_query = true;
        _ = &first_query;
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.Eviction, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.Eviction, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.Eviction, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1GetNamespacedPodExecResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodExecResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodExecResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodExecResponse = union(enum) {
        ok: ConnectCoreV1GetNamespacedPodExecResponse200,
        unauthorized: ConnectCoreV1GetNamespacedPodExecResponse401,
        default_response: ConnectCoreV1GetNamespacedPodExecResponseDefault,
    };

    pub const connectCoreV1GetNamespacedPodExecOptions = struct {
        /// Command is the remote command to execute. argv array. Not executed within a shell.
        command: ?[]const u8 = null,
        /// Container in which to execute the command. Defaults to only container if there is only one container in the pod.
        container: ?[]const u8 = null,
        /// Redirect the standard error stream of the pod for this call.
        stderr: ?[]const u8 = null,
        /// Redirect the standard input stream of the pod for this call. Defaults to false.
        stdin: ?[]const u8 = null,
        /// Redirect the standard output stream of the pod for this call.
        stdout: ?[]const u8 = null,
        /// TTY if true indicates that a tty will be allocated for the exec call. Defaults to false.
        tty: ?[]const u8 = null,
    };
    /// connect GET requests to exec of Pod
    pub fn connectCoreV1GetNamespacedPodExec(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1GetNamespacedPodExecOptions) !ConnectCoreV1GetNamespacedPodExecResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/exec", .{});
        var first_query = true;
        _ = &first_query;
        if (options.command) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("command=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.container) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("container=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.stderr) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("stderr=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.stdin) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("stdin=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.stdout) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("stdout=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.tty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("tty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PostNamespacedPodExecResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodExecResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodExecResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodExecResponse = union(enum) {
        ok: ConnectCoreV1PostNamespacedPodExecResponse200,
        unauthorized: ConnectCoreV1PostNamespacedPodExecResponse401,
        default_response: ConnectCoreV1PostNamespacedPodExecResponseDefault,
    };

    pub const connectCoreV1PostNamespacedPodExecOptions = struct {
        /// Command is the remote command to execute. argv array. Not executed within a shell.
        command: ?[]const u8 = null,
        /// Container in which to execute the command. Defaults to only container if there is only one container in the pod.
        container: ?[]const u8 = null,
        /// Redirect the standard error stream of the pod for this call.
        stderr: ?[]const u8 = null,
        /// Redirect the standard input stream of the pod for this call. Defaults to false.
        stdin: ?[]const u8 = null,
        /// Redirect the standard output stream of the pod for this call.
        stdout: ?[]const u8 = null,
        /// TTY if true indicates that a tty will be allocated for the exec call. Defaults to false.
        tty: ?[]const u8 = null,
    };
    /// connect POST requests to exec of Pod
    pub fn connectCoreV1PostNamespacedPodExec(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1PostNamespacedPodExecOptions) !ConnectCoreV1PostNamespacedPodExecResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/exec", .{});
        var first_query = true;
        _ = &first_query;
        if (options.command) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("command=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.container) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("container=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.stderr) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("stderr=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.stdin) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("stdin=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.stdout) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("stdout=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.tty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("tty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedPodLogResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodLogResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodLogResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodLogResponse = union(enum) {
        ok: ReadCoreV1NamespacedPodLogResponse200,
        unauthorized: ReadCoreV1NamespacedPodLogResponse401,
        default_response: ReadCoreV1NamespacedPodLogResponseDefault,
    };

    pub const readCoreV1NamespacedPodLogOptions = struct {
        /// The container for which to stream logs. Defaults to only container if there is one container in the pod.
        container: ?[]const u8 = null,
        /// Follow the log stream of the pod. Defaults to false.
        follow: ?[]const u8 = null,
        /// insecureSkipTLSVerifyBackend indicates that the apiserver should not confirm the validity of the serving certificate of the backend it is connecting to.  This will make the HTTPS connection between the apiserver and the backend insecure. This means the apiserver cannot verify the log data it is receiving came from the real kubelet.  If the kubelet is configured to verify the apiserver's TLS credentials, it does not mean the connection to the real kubelet is vulnerable to a man in the middle attack (e.g. an attacker could not intercept the actual log data coming from the real kubelet).
        insecureSkipTLSVerifyBackend: ?[]const u8 = null,
        /// If set, the number of bytes to read from the server before terminating the log output. This may not display a complete final line of logging, and may return slightly more or slightly less than the specified limit.
        limitBytes: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// Return previous terminated container logs. Defaults to false.
        previous: ?[]const u8 = null,
        /// A relative time in seconds before the current time from which to show logs. If this value precedes the time a pod was started, only logs since the pod start will be returned. If this value is in the future, no logs will be returned. Only one of sinceSeconds or sinceTime may be specified.
        sinceSeconds: ?[]const u8 = null,
        /// Specify which container log stream to return to the client. Acceptable values are "All", "Stdout" and "Stderr". If not specified, "All" is used, and both stdout and stderr are returned interleaved. Note that when "TailLines" is specified, "Stream" can only be set to nil or "All".
        stream: ?[]const u8 = null,
        /// If set, the number of lines from the end of the logs to show. If not specified, logs are shown from the creation of the container or sinceSeconds or sinceTime. Note that when "TailLines" is specified, "Stream" can only be set to nil or "All".
        tailLines: ?[]const u8 = null,
        /// If true, add an RFC3339 or RFC3339Nano timestamp at the beginning of every line of log output. Defaults to false.
        timestamps: ?[]const u8 = null,
    };
    /// read log of the specified Pod
    pub fn readCoreV1NamespacedPodLog(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedPodLogOptions) !ReadCoreV1NamespacedPodLogResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/log", .{});
        var first_query = true;
        _ = &first_query;
        if (options.container) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("container=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.follow) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("follow=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.insecureSkipTLSVerifyBackend) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("insecureSkipTLSVerifyBackend=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limitBytes) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limitBytes=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.previous) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("previous=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sinceSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sinceSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.stream) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("stream=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.tailLines) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("tailLines=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timestamps) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timestamps=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1GetNamespacedPodPortforwardResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodPortforwardResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodPortforwardResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodPortforwardResponse = union(enum) {
        ok: ConnectCoreV1GetNamespacedPodPortforwardResponse200,
        unauthorized: ConnectCoreV1GetNamespacedPodPortforwardResponse401,
        default_response: ConnectCoreV1GetNamespacedPodPortforwardResponseDefault,
    };

    pub const connectCoreV1GetNamespacedPodPortforwardOptions = struct {
        /// List of ports to forward Required when using WebSockets
        ports: ?[]const u8 = null,
    };
    /// connect GET requests to portforward of Pod
    pub fn connectCoreV1GetNamespacedPodPortforward(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1GetNamespacedPodPortforwardOptions) !ConnectCoreV1GetNamespacedPodPortforwardResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/portforward", .{});
        var first_query = true;
        _ = &first_query;
        if (options.ports) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ports=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PostNamespacedPodPortforwardResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodPortforwardResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodPortforwardResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodPortforwardResponse = union(enum) {
        ok: ConnectCoreV1PostNamespacedPodPortforwardResponse200,
        unauthorized: ConnectCoreV1PostNamespacedPodPortforwardResponse401,
        default_response: ConnectCoreV1PostNamespacedPodPortforwardResponseDefault,
    };

    pub const connectCoreV1PostNamespacedPodPortforwardOptions = struct {
        /// List of ports to forward Required when using WebSockets
        ports: ?[]const u8 = null,
    };
    /// connect POST requests to portforward of Pod
    pub fn connectCoreV1PostNamespacedPodPortforward(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1PostNamespacedPodPortforwardOptions) !ConnectCoreV1PostNamespacedPodPortforwardResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/portforward", .{});
        var first_query = true;
        _ = &first_query;
        if (options.ports) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ports=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1GetNamespacedPodProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodProxyResponse = union(enum) {
        ok: ConnectCoreV1GetNamespacedPodProxyResponse200,
        unauthorized: ConnectCoreV1GetNamespacedPodProxyResponse401,
        default_response: ConnectCoreV1GetNamespacedPodProxyResponseDefault,
    };

    pub const connectCoreV1GetNamespacedPodProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        path: ?[]const u8 = null,
    };
    /// connect GET requests to proxy of Pod
    pub fn connectCoreV1GetNamespacedPodProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1GetNamespacedPodProxyOptions) !ConnectCoreV1GetNamespacedPodProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PostNamespacedPodProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodProxyResponse = union(enum) {
        ok: ConnectCoreV1PostNamespacedPodProxyResponse200,
        unauthorized: ConnectCoreV1PostNamespacedPodProxyResponse401,
        default_response: ConnectCoreV1PostNamespacedPodProxyResponseDefault,
    };

    pub const connectCoreV1PostNamespacedPodProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        path: ?[]const u8 = null,
    };
    /// connect POST requests to proxy of Pod
    pub fn connectCoreV1PostNamespacedPodProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1PostNamespacedPodProxyOptions) !ConnectCoreV1PostNamespacedPodProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PutNamespacedPodProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNamespacedPodProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNamespacedPodProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNamespacedPodProxyResponse = union(enum) {
        ok: ConnectCoreV1PutNamespacedPodProxyResponse200,
        unauthorized: ConnectCoreV1PutNamespacedPodProxyResponse401,
        default_response: ConnectCoreV1PutNamespacedPodProxyResponseDefault,
    };

    pub const connectCoreV1PutNamespacedPodProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        path: ?[]const u8 = null,
    };
    /// connect PUT requests to proxy of Pod
    pub fn connectCoreV1PutNamespacedPodProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1PutNamespacedPodProxyOptions) !ConnectCoreV1PutNamespacedPodProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1DeleteNamespacedPodProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNamespacedPodProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNamespacedPodProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNamespacedPodProxyResponse = union(enum) {
        ok: ConnectCoreV1DeleteNamespacedPodProxyResponse200,
        unauthorized: ConnectCoreV1DeleteNamespacedPodProxyResponse401,
        default_response: ConnectCoreV1DeleteNamespacedPodProxyResponseDefault,
    };

    pub const connectCoreV1DeleteNamespacedPodProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        path: ?[]const u8 = null,
    };
    /// connect DELETE requests to proxy of Pod
    pub fn connectCoreV1DeleteNamespacedPodProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1DeleteNamespacedPodProxyOptions) !ConnectCoreV1DeleteNamespacedPodProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PatchNamespacedPodProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNamespacedPodProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNamespacedPodProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNamespacedPodProxyResponse = union(enum) {
        ok: ConnectCoreV1PatchNamespacedPodProxyResponse200,
        unauthorized: ConnectCoreV1PatchNamespacedPodProxyResponse401,
        default_response: ConnectCoreV1PatchNamespacedPodProxyResponseDefault,
    };

    pub const connectCoreV1PatchNamespacedPodProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        path: ?[]const u8 = null,
    };
    /// connect PATCH requests to proxy of Pod
    pub fn connectCoreV1PatchNamespacedPodProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1PatchNamespacedPodProxyOptions) !ConnectCoreV1PatchNamespacedPodProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1OptionsNamespacedPodProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNamespacedPodProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNamespacedPodProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNamespacedPodProxyResponse = union(enum) {
        ok: ConnectCoreV1OptionsNamespacedPodProxyResponse200,
        unauthorized: ConnectCoreV1OptionsNamespacedPodProxyResponse401,
        default_response: ConnectCoreV1OptionsNamespacedPodProxyResponseDefault,
    };

    pub const connectCoreV1OptionsNamespacedPodProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        path: ?[]const u8 = null,
    };
    /// connect OPTIONS requests to proxy of Pod
    pub fn connectCoreV1OptionsNamespacedPodProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1OptionsNamespacedPodProxyOptions) !ConnectCoreV1OptionsNamespacedPodProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .OPTIONS, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "OPTIONS", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1HeadNamespacedPodProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNamespacedPodProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNamespacedPodProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNamespacedPodProxyResponse = union(enum) {
        ok: ConnectCoreV1HeadNamespacedPodProxyResponse200,
        unauthorized: ConnectCoreV1HeadNamespacedPodProxyResponse401,
        default_response: ConnectCoreV1HeadNamespacedPodProxyResponseDefault,
    };

    pub const connectCoreV1HeadNamespacedPodProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        path: ?[]const u8 = null,
    };
    /// connect HEAD requests to proxy of Pod
    pub fn connectCoreV1HeadNamespacedPodProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1HeadNamespacedPodProxyOptions) !ConnectCoreV1HeadNamespacedPodProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .HEAD, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "HEAD", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1GetNamespacedPodProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedPodProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1GetNamespacedPodProxyWithPathResponse200,
        unauthorized: ConnectCoreV1GetNamespacedPodProxyWithPathResponse401,
        default_response: ConnectCoreV1GetNamespacedPodProxyWithPathResponseDefault,
    };

    pub const connectCoreV1GetNamespacedPodProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        pathQuery: ?[]const u8 = null,
    };
    /// connect GET requests to proxy of Pod
    pub fn connectCoreV1GetNamespacedPodProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1GetNamespacedPodProxyWithPathOptions) !ConnectCoreV1GetNamespacedPodProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PostNamespacedPodProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedPodProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1PostNamespacedPodProxyWithPathResponse200,
        unauthorized: ConnectCoreV1PostNamespacedPodProxyWithPathResponse401,
        default_response: ConnectCoreV1PostNamespacedPodProxyWithPathResponseDefault,
    };

    pub const connectCoreV1PostNamespacedPodProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        pathQuery: ?[]const u8 = null,
    };
    /// connect POST requests to proxy of Pod
    pub fn connectCoreV1PostNamespacedPodProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1PostNamespacedPodProxyWithPathOptions) !ConnectCoreV1PostNamespacedPodProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PutNamespacedPodProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNamespacedPodProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNamespacedPodProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNamespacedPodProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1PutNamespacedPodProxyWithPathResponse200,
        unauthorized: ConnectCoreV1PutNamespacedPodProxyWithPathResponse401,
        default_response: ConnectCoreV1PutNamespacedPodProxyWithPathResponseDefault,
    };

    pub const connectCoreV1PutNamespacedPodProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        pathQuery: ?[]const u8 = null,
    };
    /// connect PUT requests to proxy of Pod
    pub fn connectCoreV1PutNamespacedPodProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1PutNamespacedPodProxyWithPathOptions) !ConnectCoreV1PutNamespacedPodProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1DeleteNamespacedPodProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNamespacedPodProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNamespacedPodProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNamespacedPodProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1DeleteNamespacedPodProxyWithPathResponse200,
        unauthorized: ConnectCoreV1DeleteNamespacedPodProxyWithPathResponse401,
        default_response: ConnectCoreV1DeleteNamespacedPodProxyWithPathResponseDefault,
    };

    pub const connectCoreV1DeleteNamespacedPodProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        pathQuery: ?[]const u8 = null,
    };
    /// connect DELETE requests to proxy of Pod
    pub fn connectCoreV1DeleteNamespacedPodProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1DeleteNamespacedPodProxyWithPathOptions) !ConnectCoreV1DeleteNamespacedPodProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PatchNamespacedPodProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNamespacedPodProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNamespacedPodProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNamespacedPodProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1PatchNamespacedPodProxyWithPathResponse200,
        unauthorized: ConnectCoreV1PatchNamespacedPodProxyWithPathResponse401,
        default_response: ConnectCoreV1PatchNamespacedPodProxyWithPathResponseDefault,
    };

    pub const connectCoreV1PatchNamespacedPodProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        pathQuery: ?[]const u8 = null,
    };
    /// connect PATCH requests to proxy of Pod
    pub fn connectCoreV1PatchNamespacedPodProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1PatchNamespacedPodProxyWithPathOptions) !ConnectCoreV1PatchNamespacedPodProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1OptionsNamespacedPodProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNamespacedPodProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNamespacedPodProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNamespacedPodProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1OptionsNamespacedPodProxyWithPathResponse200,
        unauthorized: ConnectCoreV1OptionsNamespacedPodProxyWithPathResponse401,
        default_response: ConnectCoreV1OptionsNamespacedPodProxyWithPathResponseDefault,
    };

    pub const connectCoreV1OptionsNamespacedPodProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        pathQuery: ?[]const u8 = null,
    };
    /// connect OPTIONS requests to proxy of Pod
    pub fn connectCoreV1OptionsNamespacedPodProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1OptionsNamespacedPodProxyWithPathOptions) !ConnectCoreV1OptionsNamespacedPodProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .OPTIONS, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "OPTIONS", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1HeadNamespacedPodProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNamespacedPodProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNamespacedPodProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNamespacedPodProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1HeadNamespacedPodProxyWithPathResponse200,
        unauthorized: ConnectCoreV1HeadNamespacedPodProxyWithPathResponse401,
        default_response: ConnectCoreV1HeadNamespacedPodProxyWithPathResponseDefault,
    };

    pub const connectCoreV1HeadNamespacedPodProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to pod.
        pathQuery: ?[]const u8 = null,
    };
    /// connect HEAD requests to proxy of Pod
    pub fn connectCoreV1HeadNamespacedPodProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1HeadNamespacedPodProxyWithPathOptions) !ConnectCoreV1HeadNamespacedPodProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .HEAD, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "HEAD", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedPodResizeResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodResizeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodResizeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodResizeResponse = union(enum) {
        ok: ReadCoreV1NamespacedPodResizeResponse200,
        unauthorized: ReadCoreV1NamespacedPodResizeResponse401,
        default_response: ReadCoreV1NamespacedPodResizeResponseDefault,
    };

    pub const readCoreV1NamespacedPodResizeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read resize of the specified Pod
    pub fn readCoreV1NamespacedPodResize(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedPodResizeOptions) !ReadCoreV1NamespacedPodResizeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/resize", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedPodResizeResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodResizeResponse201 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodResizeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodResizeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodResizeResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedPodResizeResponse200,
        created: ReplaceCoreV1NamespacedPodResizeResponse201,
        unauthorized: ReplaceCoreV1NamespacedPodResizeResponse401,
        default_response: ReplaceCoreV1NamespacedPodResizeResponseDefault,
    };

    pub const replaceCoreV1NamespacedPodResizeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace resize of the specified Pod
    pub fn replaceCoreV1NamespacedPodResize(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.Pod, options: replaceCoreV1NamespacedPodResizeOptions) !ReplaceCoreV1NamespacedPodResizeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/resize", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedPodResizeResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodResizeResponse201 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodResizeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodResizeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodResizeResponse = union(enum) {
        ok: PatchCoreV1NamespacedPodResizeResponse200,
        created: PatchCoreV1NamespacedPodResizeResponse201,
        unauthorized: PatchCoreV1NamespacedPodResizeResponse401,
        default_response: PatchCoreV1NamespacedPodResizeResponseDefault,
    };

    pub const patchCoreV1NamespacedPodResizeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update resize of the specified Pod
    pub fn patchCoreV1NamespacedPodResize(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedPodResizeOptions) !PatchCoreV1NamespacedPodResizeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/resize", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedPodStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodStatusResponse = union(enum) {
        ok: ReadCoreV1NamespacedPodStatusResponse200,
        unauthorized: ReadCoreV1NamespacedPodStatusResponse401,
        default_response: ReadCoreV1NamespacedPodStatusResponseDefault,
    };

    pub const readCoreV1NamespacedPodStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified Pod
    pub fn readCoreV1NamespacedPodStatus(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedPodStatusOptions) !ReadCoreV1NamespacedPodStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedPodStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodStatusResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedPodStatusResponse200,
        created: ReplaceCoreV1NamespacedPodStatusResponse201,
        unauthorized: ReplaceCoreV1NamespacedPodStatusResponse401,
        default_response: ReplaceCoreV1NamespacedPodStatusResponseDefault,
    };

    pub const replaceCoreV1NamespacedPodStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified Pod
    pub fn replaceCoreV1NamespacedPodStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.Pod, options: replaceCoreV1NamespacedPodStatusOptions) !ReplaceCoreV1NamespacedPodStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedPodStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.Pod,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodStatusResponse = union(enum) {
        ok: PatchCoreV1NamespacedPodStatusResponse200,
        created: PatchCoreV1NamespacedPodStatusResponse201,
        unauthorized: PatchCoreV1NamespacedPodStatusResponse401,
        default_response: PatchCoreV1NamespacedPodStatusResponseDefault,
    };

    pub const patchCoreV1NamespacedPodStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified Pod
    pub fn patchCoreV1NamespacedPodStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedPodStatusOptions) !PatchCoreV1NamespacedPodStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Pod, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1NamespacedPodTemplateResponse200 = struct {
        body: root.io.k8s.api.core.v1.PodTemplateList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedPodTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedPodTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedPodTemplateResponse = union(enum) {
        ok: ListCoreV1NamespacedPodTemplateResponse200,
        unauthorized: ListCoreV1NamespacedPodTemplateResponse401,
        default_response: ListCoreV1NamespacedPodTemplateResponseDefault,
    };

    pub const listCoreV1NamespacedPodTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PodTemplate
    pub fn listCoreV1NamespacedPodTemplate(self: *Client, namespace: []const u8, options: listCoreV1NamespacedPodTemplateOptions) !ListCoreV1NamespacedPodTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodTemplateList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1NamespacedPodTemplate(self: *Client, namespace: []const u8, options: listCoreV1NamespacedPodTemplateOptions) !WatchStream(root.io.k8s.api.core.v1.PodTemplate) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/podtemplates");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.PodTemplate){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NamespacedPodTemplateResponse200 = struct {
        body: root.io.k8s.api.core.v1.PodTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodTemplateResponse201 = struct {
        body: root.io.k8s.api.core.v1.PodTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodTemplateResponse202 = struct {
        body: root.io.k8s.api.core.v1.PodTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedPodTemplateResponse = union(enum) {
        ok: CreateCoreV1NamespacedPodTemplateResponse200,
        created: CreateCoreV1NamespacedPodTemplateResponse201,
        accepted: CreateCoreV1NamespacedPodTemplateResponse202,
        unauthorized: CreateCoreV1NamespacedPodTemplateResponse401,
        default_response: CreateCoreV1NamespacedPodTemplateResponseDefault,
    };

    pub const createCoreV1NamespacedPodTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a PodTemplate
    pub fn createCoreV1NamespacedPodTemplate(self: *Client, namespace: []const u8, body: root.io.k8s.api.core.v1.PodTemplate, options: createCoreV1NamespacedPodTemplateOptions) !CreateCoreV1NamespacedPodTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionNamespacedPodTemplateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedPodTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedPodTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedPodTemplateResponse = union(enum) {
        ok: DeleteCoreV1CollectionNamespacedPodTemplateResponse200,
        unauthorized: DeleteCoreV1CollectionNamespacedPodTemplateResponse401,
        default_response: DeleteCoreV1CollectionNamespacedPodTemplateResponseDefault,
    };

    pub const deleteCoreV1CollectionNamespacedPodTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of PodTemplate
    pub fn deleteCoreV1CollectionNamespacedPodTemplate(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionNamespacedPodTemplateOptions) !DeleteCoreV1CollectionNamespacedPodTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedPodTemplateResponse200 = struct {
        body: root.io.k8s.api.core.v1.PodTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedPodTemplateResponse = union(enum) {
        ok: ReadCoreV1NamespacedPodTemplateResponse200,
        unauthorized: ReadCoreV1NamespacedPodTemplateResponse401,
        default_response: ReadCoreV1NamespacedPodTemplateResponseDefault,
    };

    pub const readCoreV1NamespacedPodTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified PodTemplate
    pub fn readCoreV1NamespacedPodTemplate(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedPodTemplateOptions) !ReadCoreV1NamespacedPodTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedPodTemplateResponse200 = struct {
        body: root.io.k8s.api.core.v1.PodTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodTemplateResponse201 = struct {
        body: root.io.k8s.api.core.v1.PodTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedPodTemplateResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedPodTemplateResponse200,
        created: ReplaceCoreV1NamespacedPodTemplateResponse201,
        unauthorized: ReplaceCoreV1NamespacedPodTemplateResponse401,
        default_response: ReplaceCoreV1NamespacedPodTemplateResponseDefault,
    };

    pub const replaceCoreV1NamespacedPodTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified PodTemplate
    pub fn replaceCoreV1NamespacedPodTemplate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.PodTemplate, options: replaceCoreV1NamespacedPodTemplateOptions) !ReplaceCoreV1NamespacedPodTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NamespacedPodTemplateResponse200 = struct {
        body: root.io.k8s.api.core.v1.PodTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedPodTemplateResponse202 = struct {
        body: root.io.k8s.api.core.v1.PodTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedPodTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedPodTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedPodTemplateResponse = union(enum) {
        ok: DeleteCoreV1NamespacedPodTemplateResponse200,
        accepted: DeleteCoreV1NamespacedPodTemplateResponse202,
        unauthorized: DeleteCoreV1NamespacedPodTemplateResponse401,
        default_response: DeleteCoreV1NamespacedPodTemplateResponseDefault,
    };

    pub const deleteCoreV1NamespacedPodTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a PodTemplate
    pub fn deleteCoreV1NamespacedPodTemplate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NamespacedPodTemplateOptions) !DeleteCoreV1NamespacedPodTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedPodTemplateResponse200 = struct {
        body: root.io.k8s.api.core.v1.PodTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodTemplateResponse201 = struct {
        body: root.io.k8s.api.core.v1.PodTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedPodTemplateResponse = union(enum) {
        ok: PatchCoreV1NamespacedPodTemplateResponse200,
        created: PatchCoreV1NamespacedPodTemplateResponse201,
        unauthorized: PatchCoreV1NamespacedPodTemplateResponse401,
        default_response: PatchCoreV1NamespacedPodTemplateResponseDefault,
    };

    pub const patchCoreV1NamespacedPodTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified PodTemplate
    pub fn patchCoreV1NamespacedPodTemplate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedPodTemplateOptions) !PatchCoreV1NamespacedPodTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1NamespacedReplicationControllerResponse200 = struct {
        body: root.io.k8s.api.core.v1.ReplicationControllerList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedReplicationControllerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedReplicationControllerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedReplicationControllerResponse = union(enum) {
        ok: ListCoreV1NamespacedReplicationControllerResponse200,
        unauthorized: ListCoreV1NamespacedReplicationControllerResponse401,
        default_response: ListCoreV1NamespacedReplicationControllerResponseDefault,
    };

    pub const listCoreV1NamespacedReplicationControllerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ReplicationController
    pub fn listCoreV1NamespacedReplicationController(self: *Client, namespace: []const u8, options: listCoreV1NamespacedReplicationControllerOptions) !ListCoreV1NamespacedReplicationControllerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationControllerList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1NamespacedReplicationController(self: *Client, namespace: []const u8, options: listCoreV1NamespacedReplicationControllerOptions) !WatchStream(root.io.k8s.api.core.v1.ReplicationController) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/replicationcontrollers");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.ReplicationController){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NamespacedReplicationControllerResponse200 = struct {
        body: root.io.k8s.api.core.v1.ReplicationController,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedReplicationControllerResponse201 = struct {
        body: root.io.k8s.api.core.v1.ReplicationController,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedReplicationControllerResponse202 = struct {
        body: root.io.k8s.api.core.v1.ReplicationController,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedReplicationControllerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedReplicationControllerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedReplicationControllerResponse = union(enum) {
        ok: CreateCoreV1NamespacedReplicationControllerResponse200,
        created: CreateCoreV1NamespacedReplicationControllerResponse201,
        accepted: CreateCoreV1NamespacedReplicationControllerResponse202,
        unauthorized: CreateCoreV1NamespacedReplicationControllerResponse401,
        default_response: CreateCoreV1NamespacedReplicationControllerResponseDefault,
    };

    pub const createCoreV1NamespacedReplicationControllerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ReplicationController
    pub fn createCoreV1NamespacedReplicationController(self: *Client, namespace: []const u8, body: root.io.k8s.api.core.v1.ReplicationController, options: createCoreV1NamespacedReplicationControllerOptions) !CreateCoreV1NamespacedReplicationControllerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationController, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationController, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationController, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionNamespacedReplicationControllerResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedReplicationControllerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedReplicationControllerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedReplicationControllerResponse = union(enum) {
        ok: DeleteCoreV1CollectionNamespacedReplicationControllerResponse200,
        unauthorized: DeleteCoreV1CollectionNamespacedReplicationControllerResponse401,
        default_response: DeleteCoreV1CollectionNamespacedReplicationControllerResponseDefault,
    };

    pub const deleteCoreV1CollectionNamespacedReplicationControllerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ReplicationController
    pub fn deleteCoreV1CollectionNamespacedReplicationController(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionNamespacedReplicationControllerOptions) !DeleteCoreV1CollectionNamespacedReplicationControllerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedReplicationControllerResponse200 = struct {
        body: root.io.k8s.api.core.v1.ReplicationController,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedReplicationControllerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedReplicationControllerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedReplicationControllerResponse = union(enum) {
        ok: ReadCoreV1NamespacedReplicationControllerResponse200,
        unauthorized: ReadCoreV1NamespacedReplicationControllerResponse401,
        default_response: ReadCoreV1NamespacedReplicationControllerResponseDefault,
    };

    pub const readCoreV1NamespacedReplicationControllerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ReplicationController
    pub fn readCoreV1NamespacedReplicationController(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedReplicationControllerOptions) !ReadCoreV1NamespacedReplicationControllerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationController, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedReplicationControllerResponse200 = struct {
        body: root.io.k8s.api.core.v1.ReplicationController,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedReplicationControllerResponse201 = struct {
        body: root.io.k8s.api.core.v1.ReplicationController,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedReplicationControllerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedReplicationControllerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedReplicationControllerResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedReplicationControllerResponse200,
        created: ReplaceCoreV1NamespacedReplicationControllerResponse201,
        unauthorized: ReplaceCoreV1NamespacedReplicationControllerResponse401,
        default_response: ReplaceCoreV1NamespacedReplicationControllerResponseDefault,
    };

    pub const replaceCoreV1NamespacedReplicationControllerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ReplicationController
    pub fn replaceCoreV1NamespacedReplicationController(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.ReplicationController, options: replaceCoreV1NamespacedReplicationControllerOptions) !ReplaceCoreV1NamespacedReplicationControllerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationController, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationController, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NamespacedReplicationControllerResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedReplicationControllerResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedReplicationControllerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedReplicationControllerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedReplicationControllerResponse = union(enum) {
        ok: DeleteCoreV1NamespacedReplicationControllerResponse200,
        accepted: DeleteCoreV1NamespacedReplicationControllerResponse202,
        unauthorized: DeleteCoreV1NamespacedReplicationControllerResponse401,
        default_response: DeleteCoreV1NamespacedReplicationControllerResponseDefault,
    };

    pub const deleteCoreV1NamespacedReplicationControllerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ReplicationController
    pub fn deleteCoreV1NamespacedReplicationController(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NamespacedReplicationControllerOptions) !DeleteCoreV1NamespacedReplicationControllerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedReplicationControllerResponse200 = struct {
        body: root.io.k8s.api.core.v1.ReplicationController,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedReplicationControllerResponse201 = struct {
        body: root.io.k8s.api.core.v1.ReplicationController,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedReplicationControllerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedReplicationControllerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedReplicationControllerResponse = union(enum) {
        ok: PatchCoreV1NamespacedReplicationControllerResponse200,
        created: PatchCoreV1NamespacedReplicationControllerResponse201,
        unauthorized: PatchCoreV1NamespacedReplicationControllerResponse401,
        default_response: PatchCoreV1NamespacedReplicationControllerResponseDefault,
    };

    pub const patchCoreV1NamespacedReplicationControllerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ReplicationController
    pub fn patchCoreV1NamespacedReplicationController(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedReplicationControllerOptions) !PatchCoreV1NamespacedReplicationControllerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationController, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationController, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedReplicationControllerScaleResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedReplicationControllerScaleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedReplicationControllerScaleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedReplicationControllerScaleResponse = union(enum) {
        ok: ReadCoreV1NamespacedReplicationControllerScaleResponse200,
        unauthorized: ReadCoreV1NamespacedReplicationControllerScaleResponse401,
        default_response: ReadCoreV1NamespacedReplicationControllerScaleResponseDefault,
    };

    pub const readCoreV1NamespacedReplicationControllerScaleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read scale of the specified ReplicationController
    pub fn readCoreV1NamespacedReplicationControllerScale(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedReplicationControllerScaleOptions) !ReadCoreV1NamespacedReplicationControllerScaleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/scale", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedReplicationControllerScaleResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedReplicationControllerScaleResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedReplicationControllerScaleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedReplicationControllerScaleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedReplicationControllerScaleResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedReplicationControllerScaleResponse200,
        created: ReplaceCoreV1NamespacedReplicationControllerScaleResponse201,
        unauthorized: ReplaceCoreV1NamespacedReplicationControllerScaleResponse401,
        default_response: ReplaceCoreV1NamespacedReplicationControllerScaleResponseDefault,
    };

    pub const replaceCoreV1NamespacedReplicationControllerScaleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace scale of the specified ReplicationController
    pub fn replaceCoreV1NamespacedReplicationControllerScale(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.autoscaling.v1.Scale, options: replaceCoreV1NamespacedReplicationControllerScaleOptions) !ReplaceCoreV1NamespacedReplicationControllerScaleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/scale", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedReplicationControllerScaleResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedReplicationControllerScaleResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedReplicationControllerScaleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedReplicationControllerScaleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedReplicationControllerScaleResponse = union(enum) {
        ok: PatchCoreV1NamespacedReplicationControllerScaleResponse200,
        created: PatchCoreV1NamespacedReplicationControllerScaleResponse201,
        unauthorized: PatchCoreV1NamespacedReplicationControllerScaleResponse401,
        default_response: PatchCoreV1NamespacedReplicationControllerScaleResponseDefault,
    };

    pub const patchCoreV1NamespacedReplicationControllerScaleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update scale of the specified ReplicationController
    pub fn patchCoreV1NamespacedReplicationControllerScale(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedReplicationControllerScaleOptions) !PatchCoreV1NamespacedReplicationControllerScaleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/scale", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedReplicationControllerStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.ReplicationController,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedReplicationControllerStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedReplicationControllerStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedReplicationControllerStatusResponse = union(enum) {
        ok: ReadCoreV1NamespacedReplicationControllerStatusResponse200,
        unauthorized: ReadCoreV1NamespacedReplicationControllerStatusResponse401,
        default_response: ReadCoreV1NamespacedReplicationControllerStatusResponseDefault,
    };

    pub const readCoreV1NamespacedReplicationControllerStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified ReplicationController
    pub fn readCoreV1NamespacedReplicationControllerStatus(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedReplicationControllerStatusOptions) !ReadCoreV1NamespacedReplicationControllerStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationController, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedReplicationControllerStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.ReplicationController,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedReplicationControllerStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.ReplicationController,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedReplicationControllerStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedReplicationControllerStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedReplicationControllerStatusResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedReplicationControllerStatusResponse200,
        created: ReplaceCoreV1NamespacedReplicationControllerStatusResponse201,
        unauthorized: ReplaceCoreV1NamespacedReplicationControllerStatusResponse401,
        default_response: ReplaceCoreV1NamespacedReplicationControllerStatusResponseDefault,
    };

    pub const replaceCoreV1NamespacedReplicationControllerStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified ReplicationController
    pub fn replaceCoreV1NamespacedReplicationControllerStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.ReplicationController, options: replaceCoreV1NamespacedReplicationControllerStatusOptions) !ReplaceCoreV1NamespacedReplicationControllerStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationController, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationController, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedReplicationControllerStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.ReplicationController,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedReplicationControllerStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.ReplicationController,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedReplicationControllerStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedReplicationControllerStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedReplicationControllerStatusResponse = union(enum) {
        ok: PatchCoreV1NamespacedReplicationControllerStatusResponse200,
        created: PatchCoreV1NamespacedReplicationControllerStatusResponse201,
        unauthorized: PatchCoreV1NamespacedReplicationControllerStatusResponse401,
        default_response: PatchCoreV1NamespacedReplicationControllerStatusResponseDefault,
    };

    pub const patchCoreV1NamespacedReplicationControllerStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified ReplicationController
    pub fn patchCoreV1NamespacedReplicationControllerStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedReplicationControllerStatusOptions) !PatchCoreV1NamespacedReplicationControllerStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationController, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationController, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1NamespacedResourceQuotaResponse200 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuotaList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedResourceQuotaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedResourceQuotaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedResourceQuotaResponse = union(enum) {
        ok: ListCoreV1NamespacedResourceQuotaResponse200,
        unauthorized: ListCoreV1NamespacedResourceQuotaResponse401,
        default_response: ListCoreV1NamespacedResourceQuotaResponseDefault,
    };

    pub const listCoreV1NamespacedResourceQuotaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceQuota
    pub fn listCoreV1NamespacedResourceQuota(self: *Client, namespace: []const u8, options: listCoreV1NamespacedResourceQuotaOptions) !ListCoreV1NamespacedResourceQuotaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourcequotas", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuotaList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1NamespacedResourceQuota(self: *Client, namespace: []const u8, options: listCoreV1NamespacedResourceQuotaOptions) !WatchStream(root.io.k8s.api.core.v1.ResourceQuota) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/resourcequotas");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.ResourceQuota){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NamespacedResourceQuotaResponse200 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedResourceQuotaResponse201 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedResourceQuotaResponse202 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedResourceQuotaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedResourceQuotaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedResourceQuotaResponse = union(enum) {
        ok: CreateCoreV1NamespacedResourceQuotaResponse200,
        created: CreateCoreV1NamespacedResourceQuotaResponse201,
        accepted: CreateCoreV1NamespacedResourceQuotaResponse202,
        unauthorized: CreateCoreV1NamespacedResourceQuotaResponse401,
        default_response: CreateCoreV1NamespacedResourceQuotaResponseDefault,
    };

    pub const createCoreV1NamespacedResourceQuotaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ResourceQuota
    pub fn createCoreV1NamespacedResourceQuota(self: *Client, namespace: []const u8, body: root.io.k8s.api.core.v1.ResourceQuota, options: createCoreV1NamespacedResourceQuotaOptions) !CreateCoreV1NamespacedResourceQuotaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourcequotas", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionNamespacedResourceQuotaResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedResourceQuotaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedResourceQuotaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedResourceQuotaResponse = union(enum) {
        ok: DeleteCoreV1CollectionNamespacedResourceQuotaResponse200,
        unauthorized: DeleteCoreV1CollectionNamespacedResourceQuotaResponse401,
        default_response: DeleteCoreV1CollectionNamespacedResourceQuotaResponseDefault,
    };

    pub const deleteCoreV1CollectionNamespacedResourceQuotaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ResourceQuota
    pub fn deleteCoreV1CollectionNamespacedResourceQuota(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionNamespacedResourceQuotaOptions) !DeleteCoreV1CollectionNamespacedResourceQuotaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourcequotas", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedResourceQuotaResponse200 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedResourceQuotaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedResourceQuotaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedResourceQuotaResponse = union(enum) {
        ok: ReadCoreV1NamespacedResourceQuotaResponse200,
        unauthorized: ReadCoreV1NamespacedResourceQuotaResponse401,
        default_response: ReadCoreV1NamespacedResourceQuotaResponseDefault,
    };

    pub const readCoreV1NamespacedResourceQuotaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ResourceQuota
    pub fn readCoreV1NamespacedResourceQuota(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedResourceQuotaOptions) !ReadCoreV1NamespacedResourceQuotaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourcequotas/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedResourceQuotaResponse200 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedResourceQuotaResponse201 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedResourceQuotaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedResourceQuotaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedResourceQuotaResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedResourceQuotaResponse200,
        created: ReplaceCoreV1NamespacedResourceQuotaResponse201,
        unauthorized: ReplaceCoreV1NamespacedResourceQuotaResponse401,
        default_response: ReplaceCoreV1NamespacedResourceQuotaResponseDefault,
    };

    pub const replaceCoreV1NamespacedResourceQuotaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ResourceQuota
    pub fn replaceCoreV1NamespacedResourceQuota(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.ResourceQuota, options: replaceCoreV1NamespacedResourceQuotaOptions) !ReplaceCoreV1NamespacedResourceQuotaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourcequotas/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NamespacedResourceQuotaResponse200 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedResourceQuotaResponse202 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedResourceQuotaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedResourceQuotaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedResourceQuotaResponse = union(enum) {
        ok: DeleteCoreV1NamespacedResourceQuotaResponse200,
        accepted: DeleteCoreV1NamespacedResourceQuotaResponse202,
        unauthorized: DeleteCoreV1NamespacedResourceQuotaResponse401,
        default_response: DeleteCoreV1NamespacedResourceQuotaResponseDefault,
    };

    pub const deleteCoreV1NamespacedResourceQuotaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ResourceQuota
    pub fn deleteCoreV1NamespacedResourceQuota(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NamespacedResourceQuotaOptions) !DeleteCoreV1NamespacedResourceQuotaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourcequotas/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedResourceQuotaResponse200 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedResourceQuotaResponse201 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedResourceQuotaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedResourceQuotaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedResourceQuotaResponse = union(enum) {
        ok: PatchCoreV1NamespacedResourceQuotaResponse200,
        created: PatchCoreV1NamespacedResourceQuotaResponse201,
        unauthorized: PatchCoreV1NamespacedResourceQuotaResponse401,
        default_response: PatchCoreV1NamespacedResourceQuotaResponseDefault,
    };

    pub const patchCoreV1NamespacedResourceQuotaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ResourceQuota
    pub fn patchCoreV1NamespacedResourceQuota(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedResourceQuotaOptions) !PatchCoreV1NamespacedResourceQuotaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourcequotas/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedResourceQuotaStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedResourceQuotaStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedResourceQuotaStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedResourceQuotaStatusResponse = union(enum) {
        ok: ReadCoreV1NamespacedResourceQuotaStatusResponse200,
        unauthorized: ReadCoreV1NamespacedResourceQuotaStatusResponse401,
        default_response: ReadCoreV1NamespacedResourceQuotaStatusResponseDefault,
    };

    pub const readCoreV1NamespacedResourceQuotaStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified ResourceQuota
    pub fn readCoreV1NamespacedResourceQuotaStatus(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedResourceQuotaStatusOptions) !ReadCoreV1NamespacedResourceQuotaStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourcequotas/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedResourceQuotaStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedResourceQuotaStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedResourceQuotaStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedResourceQuotaStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedResourceQuotaStatusResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedResourceQuotaStatusResponse200,
        created: ReplaceCoreV1NamespacedResourceQuotaStatusResponse201,
        unauthorized: ReplaceCoreV1NamespacedResourceQuotaStatusResponse401,
        default_response: ReplaceCoreV1NamespacedResourceQuotaStatusResponseDefault,
    };

    pub const replaceCoreV1NamespacedResourceQuotaStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified ResourceQuota
    pub fn replaceCoreV1NamespacedResourceQuotaStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.ResourceQuota, options: replaceCoreV1NamespacedResourceQuotaStatusOptions) !ReplaceCoreV1NamespacedResourceQuotaStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourcequotas/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedResourceQuotaStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedResourceQuotaStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuota,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedResourceQuotaStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedResourceQuotaStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedResourceQuotaStatusResponse = union(enum) {
        ok: PatchCoreV1NamespacedResourceQuotaStatusResponse200,
        created: PatchCoreV1NamespacedResourceQuotaStatusResponse201,
        unauthorized: PatchCoreV1NamespacedResourceQuotaStatusResponse401,
        default_response: PatchCoreV1NamespacedResourceQuotaStatusResponseDefault,
    };

    pub const patchCoreV1NamespacedResourceQuotaStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified ResourceQuota
    pub fn patchCoreV1NamespacedResourceQuotaStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedResourceQuotaStatusOptions) !PatchCoreV1NamespacedResourceQuotaStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourcequotas/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuota, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1NamespacedSecretResponse200 = struct {
        body: root.io.k8s.api.core.v1.SecretList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedSecretResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedSecretResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedSecretResponse = union(enum) {
        ok: ListCoreV1NamespacedSecretResponse200,
        unauthorized: ListCoreV1NamespacedSecretResponse401,
        default_response: ListCoreV1NamespacedSecretResponseDefault,
    };

    pub const listCoreV1NamespacedSecretOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Secret
    pub fn listCoreV1NamespacedSecret(self: *Client, namespace: []const u8, options: listCoreV1NamespacedSecretOptions) !ListCoreV1NamespacedSecretResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/secrets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.SecretList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1NamespacedSecret(self: *Client, namespace: []const u8, options: listCoreV1NamespacedSecretOptions) !WatchStream(root.io.k8s.api.core.v1.Secret) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/secrets");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.Secret){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NamespacedSecretResponse200 = struct {
        body: root.io.k8s.api.core.v1.Secret,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedSecretResponse201 = struct {
        body: root.io.k8s.api.core.v1.Secret,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedSecretResponse202 = struct {
        body: root.io.k8s.api.core.v1.Secret,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedSecretResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedSecretResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedSecretResponse = union(enum) {
        ok: CreateCoreV1NamespacedSecretResponse200,
        created: CreateCoreV1NamespacedSecretResponse201,
        accepted: CreateCoreV1NamespacedSecretResponse202,
        unauthorized: CreateCoreV1NamespacedSecretResponse401,
        default_response: CreateCoreV1NamespacedSecretResponseDefault,
    };

    pub const createCoreV1NamespacedSecretOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a Secret
    pub fn createCoreV1NamespacedSecret(self: *Client, namespace: []const u8, body: root.io.k8s.api.core.v1.Secret, options: createCoreV1NamespacedSecretOptions) !CreateCoreV1NamespacedSecretResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/secrets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Secret, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Secret, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Secret, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionNamespacedSecretResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedSecretResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedSecretResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedSecretResponse = union(enum) {
        ok: DeleteCoreV1CollectionNamespacedSecretResponse200,
        unauthorized: DeleteCoreV1CollectionNamespacedSecretResponse401,
        default_response: DeleteCoreV1CollectionNamespacedSecretResponseDefault,
    };

    pub const deleteCoreV1CollectionNamespacedSecretOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Secret
    pub fn deleteCoreV1CollectionNamespacedSecret(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionNamespacedSecretOptions) !DeleteCoreV1CollectionNamespacedSecretResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/secrets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedSecretResponse200 = struct {
        body: root.io.k8s.api.core.v1.Secret,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedSecretResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedSecretResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedSecretResponse = union(enum) {
        ok: ReadCoreV1NamespacedSecretResponse200,
        unauthorized: ReadCoreV1NamespacedSecretResponse401,
        default_response: ReadCoreV1NamespacedSecretResponseDefault,
    };

    pub const readCoreV1NamespacedSecretOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Secret
    pub fn readCoreV1NamespacedSecret(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedSecretOptions) !ReadCoreV1NamespacedSecretResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/secrets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Secret, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedSecretResponse200 = struct {
        body: root.io.k8s.api.core.v1.Secret,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedSecretResponse201 = struct {
        body: root.io.k8s.api.core.v1.Secret,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedSecretResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedSecretResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedSecretResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedSecretResponse200,
        created: ReplaceCoreV1NamespacedSecretResponse201,
        unauthorized: ReplaceCoreV1NamespacedSecretResponse401,
        default_response: ReplaceCoreV1NamespacedSecretResponseDefault,
    };

    pub const replaceCoreV1NamespacedSecretOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Secret
    pub fn replaceCoreV1NamespacedSecret(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.Secret, options: replaceCoreV1NamespacedSecretOptions) !ReplaceCoreV1NamespacedSecretResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/secrets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Secret, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Secret, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NamespacedSecretResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedSecretResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedSecretResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedSecretResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedSecretResponse = union(enum) {
        ok: DeleteCoreV1NamespacedSecretResponse200,
        accepted: DeleteCoreV1NamespacedSecretResponse202,
        unauthorized: DeleteCoreV1NamespacedSecretResponse401,
        default_response: DeleteCoreV1NamespacedSecretResponseDefault,
    };

    pub const deleteCoreV1NamespacedSecretOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a Secret
    pub fn deleteCoreV1NamespacedSecret(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NamespacedSecretOptions) !DeleteCoreV1NamespacedSecretResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/secrets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedSecretResponse200 = struct {
        body: root.io.k8s.api.core.v1.Secret,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedSecretResponse201 = struct {
        body: root.io.k8s.api.core.v1.Secret,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedSecretResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedSecretResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedSecretResponse = union(enum) {
        ok: PatchCoreV1NamespacedSecretResponse200,
        created: PatchCoreV1NamespacedSecretResponse201,
        unauthorized: PatchCoreV1NamespacedSecretResponse401,
        default_response: PatchCoreV1NamespacedSecretResponseDefault,
    };

    pub const patchCoreV1NamespacedSecretOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Secret
    pub fn patchCoreV1NamespacedSecret(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedSecretOptions) !PatchCoreV1NamespacedSecretResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/secrets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Secret, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Secret, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1NamespacedServiceAccountResponse200 = struct {
        body: root.io.k8s.api.core.v1.ServiceAccountList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedServiceAccountResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedServiceAccountResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedServiceAccountResponse = union(enum) {
        ok: ListCoreV1NamespacedServiceAccountResponse200,
        unauthorized: ListCoreV1NamespacedServiceAccountResponse401,
        default_response: ListCoreV1NamespacedServiceAccountResponseDefault,
    };

    pub const listCoreV1NamespacedServiceAccountOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ServiceAccount
    pub fn listCoreV1NamespacedServiceAccount(self: *Client, namespace: []const u8, options: listCoreV1NamespacedServiceAccountOptions) !ListCoreV1NamespacedServiceAccountResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/serviceaccounts", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceAccountList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1NamespacedServiceAccount(self: *Client, namespace: []const u8, options: listCoreV1NamespacedServiceAccountOptions) !WatchStream(root.io.k8s.api.core.v1.ServiceAccount) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/serviceaccounts");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.ServiceAccount){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NamespacedServiceAccountResponse200 = struct {
        body: root.io.k8s.api.core.v1.ServiceAccount,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceAccountResponse201 = struct {
        body: root.io.k8s.api.core.v1.ServiceAccount,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceAccountResponse202 = struct {
        body: root.io.k8s.api.core.v1.ServiceAccount,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceAccountResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceAccountResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceAccountResponse = union(enum) {
        ok: CreateCoreV1NamespacedServiceAccountResponse200,
        created: CreateCoreV1NamespacedServiceAccountResponse201,
        accepted: CreateCoreV1NamespacedServiceAccountResponse202,
        unauthorized: CreateCoreV1NamespacedServiceAccountResponse401,
        default_response: CreateCoreV1NamespacedServiceAccountResponseDefault,
    };

    pub const createCoreV1NamespacedServiceAccountOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ServiceAccount
    pub fn createCoreV1NamespacedServiceAccount(self: *Client, namespace: []const u8, body: root.io.k8s.api.core.v1.ServiceAccount, options: createCoreV1NamespacedServiceAccountOptions) !CreateCoreV1NamespacedServiceAccountResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/serviceaccounts", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceAccount, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceAccount, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceAccount, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionNamespacedServiceAccountResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedServiceAccountResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedServiceAccountResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedServiceAccountResponse = union(enum) {
        ok: DeleteCoreV1CollectionNamespacedServiceAccountResponse200,
        unauthorized: DeleteCoreV1CollectionNamespacedServiceAccountResponse401,
        default_response: DeleteCoreV1CollectionNamespacedServiceAccountResponseDefault,
    };

    pub const deleteCoreV1CollectionNamespacedServiceAccountOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ServiceAccount
    pub fn deleteCoreV1CollectionNamespacedServiceAccount(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionNamespacedServiceAccountOptions) !DeleteCoreV1CollectionNamespacedServiceAccountResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/serviceaccounts", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedServiceAccountResponse200 = struct {
        body: root.io.k8s.api.core.v1.ServiceAccount,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedServiceAccountResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedServiceAccountResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedServiceAccountResponse = union(enum) {
        ok: ReadCoreV1NamespacedServiceAccountResponse200,
        unauthorized: ReadCoreV1NamespacedServiceAccountResponse401,
        default_response: ReadCoreV1NamespacedServiceAccountResponseDefault,
    };

    pub const readCoreV1NamespacedServiceAccountOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ServiceAccount
    pub fn readCoreV1NamespacedServiceAccount(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedServiceAccountOptions) !ReadCoreV1NamespacedServiceAccountResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/serviceaccounts/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceAccount, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedServiceAccountResponse200 = struct {
        body: root.io.k8s.api.core.v1.ServiceAccount,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedServiceAccountResponse201 = struct {
        body: root.io.k8s.api.core.v1.ServiceAccount,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedServiceAccountResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedServiceAccountResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedServiceAccountResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedServiceAccountResponse200,
        created: ReplaceCoreV1NamespacedServiceAccountResponse201,
        unauthorized: ReplaceCoreV1NamespacedServiceAccountResponse401,
        default_response: ReplaceCoreV1NamespacedServiceAccountResponseDefault,
    };

    pub const replaceCoreV1NamespacedServiceAccountOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ServiceAccount
    pub fn replaceCoreV1NamespacedServiceAccount(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.ServiceAccount, options: replaceCoreV1NamespacedServiceAccountOptions) !ReplaceCoreV1NamespacedServiceAccountResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/serviceaccounts/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceAccount, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceAccount, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NamespacedServiceAccountResponse200 = struct {
        body: root.io.k8s.api.core.v1.ServiceAccount,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedServiceAccountResponse202 = struct {
        body: root.io.k8s.api.core.v1.ServiceAccount,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedServiceAccountResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedServiceAccountResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedServiceAccountResponse = union(enum) {
        ok: DeleteCoreV1NamespacedServiceAccountResponse200,
        accepted: DeleteCoreV1NamespacedServiceAccountResponse202,
        unauthorized: DeleteCoreV1NamespacedServiceAccountResponse401,
        default_response: DeleteCoreV1NamespacedServiceAccountResponseDefault,
    };

    pub const deleteCoreV1NamespacedServiceAccountOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ServiceAccount
    pub fn deleteCoreV1NamespacedServiceAccount(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NamespacedServiceAccountOptions) !DeleteCoreV1NamespacedServiceAccountResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/serviceaccounts/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceAccount, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceAccount, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedServiceAccountResponse200 = struct {
        body: root.io.k8s.api.core.v1.ServiceAccount,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedServiceAccountResponse201 = struct {
        body: root.io.k8s.api.core.v1.ServiceAccount,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedServiceAccountResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedServiceAccountResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedServiceAccountResponse = union(enum) {
        ok: PatchCoreV1NamespacedServiceAccountResponse200,
        created: PatchCoreV1NamespacedServiceAccountResponse201,
        unauthorized: PatchCoreV1NamespacedServiceAccountResponse401,
        default_response: PatchCoreV1NamespacedServiceAccountResponseDefault,
    };

    pub const patchCoreV1NamespacedServiceAccountOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ServiceAccount
    pub fn patchCoreV1NamespacedServiceAccount(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedServiceAccountOptions) !PatchCoreV1NamespacedServiceAccountResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/serviceaccounts/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceAccount, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceAccount, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const CreateCoreV1NamespacedServiceAccountTokenResponse200 = struct {
        body: root.io.k8s.api.authentication.v1.TokenRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceAccountTokenResponse201 = struct {
        body: root.io.k8s.api.authentication.v1.TokenRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceAccountTokenResponse202 = struct {
        body: root.io.k8s.api.authentication.v1.TokenRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceAccountTokenResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceAccountTokenResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceAccountTokenResponse = union(enum) {
        ok: CreateCoreV1NamespacedServiceAccountTokenResponse200,
        created: CreateCoreV1NamespacedServiceAccountTokenResponse201,
        accepted: CreateCoreV1NamespacedServiceAccountTokenResponse202,
        unauthorized: CreateCoreV1NamespacedServiceAccountTokenResponse401,
        default_response: CreateCoreV1NamespacedServiceAccountTokenResponseDefault,
    };

    pub const createCoreV1NamespacedServiceAccountTokenOptions = struct {
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// create token of a ServiceAccount
    pub fn createCoreV1NamespacedServiceAccountToken(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.authentication.v1.TokenRequest, options: createCoreV1NamespacedServiceAccountTokenOptions) !CreateCoreV1NamespacedServiceAccountTokenResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/serviceaccounts/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/token", .{});
        var first_query = true;
        _ = &first_query;
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authentication.v1.TokenRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authentication.v1.TokenRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authentication.v1.TokenRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1NamespacedServiceResponse200 = struct {
        body: root.io.k8s.api.core.v1.ServiceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NamespacedServiceResponse = union(enum) {
        ok: ListCoreV1NamespacedServiceResponse200,
        unauthorized: ListCoreV1NamespacedServiceResponse401,
        default_response: ListCoreV1NamespacedServiceResponseDefault,
    };

    pub const listCoreV1NamespacedServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Service
    pub fn listCoreV1NamespacedService(self: *Client, namespace: []const u8, options: listCoreV1NamespacedServiceOptions) !ListCoreV1NamespacedServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1NamespacedService(self: *Client, namespace: []const u8, options: listCoreV1NamespacedServiceOptions) !WatchStream(root.io.k8s.api.core.v1.Service) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/services");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.Service){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NamespacedServiceResponse200 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceResponse201 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceResponse202 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NamespacedServiceResponse = union(enum) {
        ok: CreateCoreV1NamespacedServiceResponse200,
        created: CreateCoreV1NamespacedServiceResponse201,
        accepted: CreateCoreV1NamespacedServiceResponse202,
        unauthorized: CreateCoreV1NamespacedServiceResponse401,
        default_response: CreateCoreV1NamespacedServiceResponseDefault,
    };

    pub const createCoreV1NamespacedServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a Service
    pub fn createCoreV1NamespacedService(self: *Client, namespace: []const u8, body: root.io.k8s.api.core.v1.Service, options: createCoreV1NamespacedServiceOptions) !CreateCoreV1NamespacedServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionNamespacedServiceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNamespacedServiceResponse = union(enum) {
        ok: DeleteCoreV1CollectionNamespacedServiceResponse200,
        unauthorized: DeleteCoreV1CollectionNamespacedServiceResponse401,
        default_response: DeleteCoreV1CollectionNamespacedServiceResponseDefault,
    };

    pub const deleteCoreV1CollectionNamespacedServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Service
    pub fn deleteCoreV1CollectionNamespacedService(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionNamespacedServiceOptions) !DeleteCoreV1CollectionNamespacedServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedServiceResponse200 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedServiceResponse = union(enum) {
        ok: ReadCoreV1NamespacedServiceResponse200,
        unauthorized: ReadCoreV1NamespacedServiceResponse401,
        default_response: ReadCoreV1NamespacedServiceResponseDefault,
    };

    pub const readCoreV1NamespacedServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Service
    pub fn readCoreV1NamespacedService(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedServiceOptions) !ReadCoreV1NamespacedServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedServiceResponse200 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedServiceResponse201 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedServiceResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedServiceResponse200,
        created: ReplaceCoreV1NamespacedServiceResponse201,
        unauthorized: ReplaceCoreV1NamespacedServiceResponse401,
        default_response: ReplaceCoreV1NamespacedServiceResponseDefault,
    };

    pub const replaceCoreV1NamespacedServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Service
    pub fn replaceCoreV1NamespacedService(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.Service, options: replaceCoreV1NamespacedServiceOptions) !ReplaceCoreV1NamespacedServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NamespacedServiceResponse200 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedServiceResponse202 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespacedServiceResponse = union(enum) {
        ok: DeleteCoreV1NamespacedServiceResponse200,
        accepted: DeleteCoreV1NamespacedServiceResponse202,
        unauthorized: DeleteCoreV1NamespacedServiceResponse401,
        default_response: DeleteCoreV1NamespacedServiceResponseDefault,
    };

    pub const deleteCoreV1NamespacedServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a Service
    pub fn deleteCoreV1NamespacedService(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NamespacedServiceOptions) !DeleteCoreV1NamespacedServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedServiceResponse200 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedServiceResponse201 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedServiceResponse = union(enum) {
        ok: PatchCoreV1NamespacedServiceResponse200,
        created: PatchCoreV1NamespacedServiceResponse201,
        unauthorized: PatchCoreV1NamespacedServiceResponse401,
        default_response: PatchCoreV1NamespacedServiceResponseDefault,
    };

    pub const patchCoreV1NamespacedServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Service
    pub fn patchCoreV1NamespacedService(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedServiceOptions) !PatchCoreV1NamespacedServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1GetNamespacedServiceProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedServiceProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedServiceProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedServiceProxyResponse = union(enum) {
        ok: ConnectCoreV1GetNamespacedServiceProxyResponse200,
        unauthorized: ConnectCoreV1GetNamespacedServiceProxyResponse401,
        default_response: ConnectCoreV1GetNamespacedServiceProxyResponseDefault,
    };

    pub const connectCoreV1GetNamespacedServiceProxyOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        path: ?[]const u8 = null,
    };
    /// connect GET requests to proxy of Service
    pub fn connectCoreV1GetNamespacedServiceProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1GetNamespacedServiceProxyOptions) !ConnectCoreV1GetNamespacedServiceProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PostNamespacedServiceProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedServiceProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedServiceProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedServiceProxyResponse = union(enum) {
        ok: ConnectCoreV1PostNamespacedServiceProxyResponse200,
        unauthorized: ConnectCoreV1PostNamespacedServiceProxyResponse401,
        default_response: ConnectCoreV1PostNamespacedServiceProxyResponseDefault,
    };

    pub const connectCoreV1PostNamespacedServiceProxyOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        path: ?[]const u8 = null,
    };
    /// connect POST requests to proxy of Service
    pub fn connectCoreV1PostNamespacedServiceProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1PostNamespacedServiceProxyOptions) !ConnectCoreV1PostNamespacedServiceProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PutNamespacedServiceProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNamespacedServiceProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNamespacedServiceProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNamespacedServiceProxyResponse = union(enum) {
        ok: ConnectCoreV1PutNamespacedServiceProxyResponse200,
        unauthorized: ConnectCoreV1PutNamespacedServiceProxyResponse401,
        default_response: ConnectCoreV1PutNamespacedServiceProxyResponseDefault,
    };

    pub const connectCoreV1PutNamespacedServiceProxyOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        path: ?[]const u8 = null,
    };
    /// connect PUT requests to proxy of Service
    pub fn connectCoreV1PutNamespacedServiceProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1PutNamespacedServiceProxyOptions) !ConnectCoreV1PutNamespacedServiceProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1DeleteNamespacedServiceProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNamespacedServiceProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNamespacedServiceProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNamespacedServiceProxyResponse = union(enum) {
        ok: ConnectCoreV1DeleteNamespacedServiceProxyResponse200,
        unauthorized: ConnectCoreV1DeleteNamespacedServiceProxyResponse401,
        default_response: ConnectCoreV1DeleteNamespacedServiceProxyResponseDefault,
    };

    pub const connectCoreV1DeleteNamespacedServiceProxyOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        path: ?[]const u8 = null,
    };
    /// connect DELETE requests to proxy of Service
    pub fn connectCoreV1DeleteNamespacedServiceProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1DeleteNamespacedServiceProxyOptions) !ConnectCoreV1DeleteNamespacedServiceProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PatchNamespacedServiceProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNamespacedServiceProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNamespacedServiceProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNamespacedServiceProxyResponse = union(enum) {
        ok: ConnectCoreV1PatchNamespacedServiceProxyResponse200,
        unauthorized: ConnectCoreV1PatchNamespacedServiceProxyResponse401,
        default_response: ConnectCoreV1PatchNamespacedServiceProxyResponseDefault,
    };

    pub const connectCoreV1PatchNamespacedServiceProxyOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        path: ?[]const u8 = null,
    };
    /// connect PATCH requests to proxy of Service
    pub fn connectCoreV1PatchNamespacedServiceProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1PatchNamespacedServiceProxyOptions) !ConnectCoreV1PatchNamespacedServiceProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1OptionsNamespacedServiceProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNamespacedServiceProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNamespacedServiceProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNamespacedServiceProxyResponse = union(enum) {
        ok: ConnectCoreV1OptionsNamespacedServiceProxyResponse200,
        unauthorized: ConnectCoreV1OptionsNamespacedServiceProxyResponse401,
        default_response: ConnectCoreV1OptionsNamespacedServiceProxyResponseDefault,
    };

    pub const connectCoreV1OptionsNamespacedServiceProxyOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        path: ?[]const u8 = null,
    };
    /// connect OPTIONS requests to proxy of Service
    pub fn connectCoreV1OptionsNamespacedServiceProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1OptionsNamespacedServiceProxyOptions) !ConnectCoreV1OptionsNamespacedServiceProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .OPTIONS, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "OPTIONS", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1HeadNamespacedServiceProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNamespacedServiceProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNamespacedServiceProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNamespacedServiceProxyResponse = union(enum) {
        ok: ConnectCoreV1HeadNamespacedServiceProxyResponse200,
        unauthorized: ConnectCoreV1HeadNamespacedServiceProxyResponse401,
        default_response: ConnectCoreV1HeadNamespacedServiceProxyResponseDefault,
    };

    pub const connectCoreV1HeadNamespacedServiceProxyOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        path: ?[]const u8 = null,
    };
    /// connect HEAD requests to proxy of Service
    pub fn connectCoreV1HeadNamespacedServiceProxy(self: *Client, name: []const u8, namespace: []const u8, options: connectCoreV1HeadNamespacedServiceProxyOptions) !ConnectCoreV1HeadNamespacedServiceProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .HEAD, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "HEAD", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1GetNamespacedServiceProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedServiceProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedServiceProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNamespacedServiceProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1GetNamespacedServiceProxyWithPathResponse200,
        unauthorized: ConnectCoreV1GetNamespacedServiceProxyWithPathResponse401,
        default_response: ConnectCoreV1GetNamespacedServiceProxyWithPathResponseDefault,
    };

    pub const connectCoreV1GetNamespacedServiceProxyWithPathOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        pathQuery: ?[]const u8 = null,
    };
    /// connect GET requests to proxy of Service
    pub fn connectCoreV1GetNamespacedServiceProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1GetNamespacedServiceProxyWithPathOptions) !ConnectCoreV1GetNamespacedServiceProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PostNamespacedServiceProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedServiceProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedServiceProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNamespacedServiceProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1PostNamespacedServiceProxyWithPathResponse200,
        unauthorized: ConnectCoreV1PostNamespacedServiceProxyWithPathResponse401,
        default_response: ConnectCoreV1PostNamespacedServiceProxyWithPathResponseDefault,
    };

    pub const connectCoreV1PostNamespacedServiceProxyWithPathOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        pathQuery: ?[]const u8 = null,
    };
    /// connect POST requests to proxy of Service
    pub fn connectCoreV1PostNamespacedServiceProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1PostNamespacedServiceProxyWithPathOptions) !ConnectCoreV1PostNamespacedServiceProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PutNamespacedServiceProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNamespacedServiceProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNamespacedServiceProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNamespacedServiceProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1PutNamespacedServiceProxyWithPathResponse200,
        unauthorized: ConnectCoreV1PutNamespacedServiceProxyWithPathResponse401,
        default_response: ConnectCoreV1PutNamespacedServiceProxyWithPathResponseDefault,
    };

    pub const connectCoreV1PutNamespacedServiceProxyWithPathOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        pathQuery: ?[]const u8 = null,
    };
    /// connect PUT requests to proxy of Service
    pub fn connectCoreV1PutNamespacedServiceProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1PutNamespacedServiceProxyWithPathOptions) !ConnectCoreV1PutNamespacedServiceProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1DeleteNamespacedServiceProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNamespacedServiceProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNamespacedServiceProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNamespacedServiceProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1DeleteNamespacedServiceProxyWithPathResponse200,
        unauthorized: ConnectCoreV1DeleteNamespacedServiceProxyWithPathResponse401,
        default_response: ConnectCoreV1DeleteNamespacedServiceProxyWithPathResponseDefault,
    };

    pub const connectCoreV1DeleteNamespacedServiceProxyWithPathOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        pathQuery: ?[]const u8 = null,
    };
    /// connect DELETE requests to proxy of Service
    pub fn connectCoreV1DeleteNamespacedServiceProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1DeleteNamespacedServiceProxyWithPathOptions) !ConnectCoreV1DeleteNamespacedServiceProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PatchNamespacedServiceProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNamespacedServiceProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNamespacedServiceProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNamespacedServiceProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1PatchNamespacedServiceProxyWithPathResponse200,
        unauthorized: ConnectCoreV1PatchNamespacedServiceProxyWithPathResponse401,
        default_response: ConnectCoreV1PatchNamespacedServiceProxyWithPathResponseDefault,
    };

    pub const connectCoreV1PatchNamespacedServiceProxyWithPathOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        pathQuery: ?[]const u8 = null,
    };
    /// connect PATCH requests to proxy of Service
    pub fn connectCoreV1PatchNamespacedServiceProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1PatchNamespacedServiceProxyWithPathOptions) !ConnectCoreV1PatchNamespacedServiceProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1OptionsNamespacedServiceProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNamespacedServiceProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNamespacedServiceProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNamespacedServiceProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1OptionsNamespacedServiceProxyWithPathResponse200,
        unauthorized: ConnectCoreV1OptionsNamespacedServiceProxyWithPathResponse401,
        default_response: ConnectCoreV1OptionsNamespacedServiceProxyWithPathResponseDefault,
    };

    pub const connectCoreV1OptionsNamespacedServiceProxyWithPathOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        pathQuery: ?[]const u8 = null,
    };
    /// connect OPTIONS requests to proxy of Service
    pub fn connectCoreV1OptionsNamespacedServiceProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1OptionsNamespacedServiceProxyWithPathOptions) !ConnectCoreV1OptionsNamespacedServiceProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .OPTIONS, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "OPTIONS", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1HeadNamespacedServiceProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNamespacedServiceProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNamespacedServiceProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNamespacedServiceProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1HeadNamespacedServiceProxyWithPathResponse200,
        unauthorized: ConnectCoreV1HeadNamespacedServiceProxyWithPathResponse401,
        default_response: ConnectCoreV1HeadNamespacedServiceProxyWithPathResponseDefault,
    };

    pub const connectCoreV1HeadNamespacedServiceProxyWithPathOptions = struct {
        /// Path is the part of URLs that include service endpoints, suffixes, and parameters to use for the current proxy request to service. For example, the whole request URL is http://localhost/api/v1/namespaces/kube-system/services/elasticsearch-logging/_search?q=user:kimchy. Path is _search?q=user:kimchy.
        pathQuery: ?[]const u8 = null,
    };
    /// connect HEAD requests to proxy of Service
    pub fn connectCoreV1HeadNamespacedServiceProxyWithPath(self: *Client, name: []const u8, namespace: []const u8, path: []const u8, options: connectCoreV1HeadNamespacedServiceProxyWithPathOptions) !ConnectCoreV1HeadNamespacedServiceProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .HEAD, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "HEAD", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespacedServiceStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedServiceStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedServiceStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespacedServiceStatusResponse = union(enum) {
        ok: ReadCoreV1NamespacedServiceStatusResponse200,
        unauthorized: ReadCoreV1NamespacedServiceStatusResponse401,
        default_response: ReadCoreV1NamespacedServiceStatusResponseDefault,
    };

    pub const readCoreV1NamespacedServiceStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified Service
    pub fn readCoreV1NamespacedServiceStatus(self: *Client, name: []const u8, namespace: []const u8, options: readCoreV1NamespacedServiceStatusOptions) !ReadCoreV1NamespacedServiceStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespacedServiceStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedServiceStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedServiceStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedServiceStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespacedServiceStatusResponse = union(enum) {
        ok: ReplaceCoreV1NamespacedServiceStatusResponse200,
        created: ReplaceCoreV1NamespacedServiceStatusResponse201,
        unauthorized: ReplaceCoreV1NamespacedServiceStatusResponse401,
        default_response: ReplaceCoreV1NamespacedServiceStatusResponseDefault,
    };

    pub const replaceCoreV1NamespacedServiceStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified Service
    pub fn replaceCoreV1NamespacedServiceStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.core.v1.Service, options: replaceCoreV1NamespacedServiceStatusOptions) !ReplaceCoreV1NamespacedServiceStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespacedServiceStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedServiceStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.Service,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedServiceStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedServiceStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespacedServiceStatusResponse = union(enum) {
        ok: PatchCoreV1NamespacedServiceStatusResponse200,
        created: PatchCoreV1NamespacedServiceStatusResponse201,
        unauthorized: PatchCoreV1NamespacedServiceStatusResponse401,
        default_response: PatchCoreV1NamespacedServiceStatusResponseDefault,
    };

    pub const patchCoreV1NamespacedServiceStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified Service
    pub fn patchCoreV1NamespacedServiceStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespacedServiceStatusOptions) !PatchCoreV1NamespacedServiceStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Service, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespaceResponse200 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespaceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespaceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespaceResponse = union(enum) {
        ok: ReadCoreV1NamespaceResponse200,
        unauthorized: ReadCoreV1NamespaceResponse401,
        default_response: ReadCoreV1NamespaceResponseDefault,
    };

    pub const readCoreV1NamespaceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Namespace
    pub fn readCoreV1Namespace(self: *Client, name: []const u8, options: readCoreV1NamespaceOptions) !ReadCoreV1NamespaceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespaceResponse200 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespaceResponse201 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespaceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespaceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespaceResponse = union(enum) {
        ok: ReplaceCoreV1NamespaceResponse200,
        created: ReplaceCoreV1NamespaceResponse201,
        unauthorized: ReplaceCoreV1NamespaceResponse401,
        default_response: ReplaceCoreV1NamespaceResponseDefault,
    };

    pub const replaceCoreV1NamespaceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Namespace
    pub fn replaceCoreV1Namespace(self: *Client, name: []const u8, body: root.io.k8s.api.core.v1.Namespace, options: replaceCoreV1NamespaceOptions) !ReplaceCoreV1NamespaceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NamespaceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespaceResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespaceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespaceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NamespaceResponse = union(enum) {
        ok: DeleteCoreV1NamespaceResponse200,
        accepted: DeleteCoreV1NamespaceResponse202,
        unauthorized: DeleteCoreV1NamespaceResponse401,
        default_response: DeleteCoreV1NamespaceResponseDefault,
    };

    pub const deleteCoreV1NamespaceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a Namespace
    pub fn deleteCoreV1Namespace(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NamespaceOptions) !DeleteCoreV1NamespaceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespaceResponse200 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespaceResponse201 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespaceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespaceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespaceResponse = union(enum) {
        ok: PatchCoreV1NamespaceResponse200,
        created: PatchCoreV1NamespaceResponse201,
        unauthorized: PatchCoreV1NamespaceResponse401,
        default_response: PatchCoreV1NamespaceResponseDefault,
    };

    pub const patchCoreV1NamespaceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Namespace
    pub fn patchCoreV1Namespace(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespaceOptions) !PatchCoreV1NamespaceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespaceFinalizeResponse200 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespaceFinalizeResponse201 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespaceFinalizeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespaceFinalizeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespaceFinalizeResponse = union(enum) {
        ok: ReplaceCoreV1NamespaceFinalizeResponse200,
        created: ReplaceCoreV1NamespaceFinalizeResponse201,
        unauthorized: ReplaceCoreV1NamespaceFinalizeResponse401,
        default_response: ReplaceCoreV1NamespaceFinalizeResponseDefault,
    };

    pub const replaceCoreV1NamespaceFinalizeOptions = struct {
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// replace finalize of the specified Namespace
    pub fn replaceCoreV1NamespaceFinalize(self: *Client, name: []const u8, body: root.io.k8s.api.core.v1.Namespace, options: replaceCoreV1NamespaceFinalizeOptions) !ReplaceCoreV1NamespaceFinalizeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/finalize", .{});
        var first_query = true;
        _ = &first_query;
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NamespaceStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespaceStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespaceStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NamespaceStatusResponse = union(enum) {
        ok: ReadCoreV1NamespaceStatusResponse200,
        unauthorized: ReadCoreV1NamespaceStatusResponse401,
        default_response: ReadCoreV1NamespaceStatusResponseDefault,
    };

    pub const readCoreV1NamespaceStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified Namespace
    pub fn readCoreV1NamespaceStatus(self: *Client, name: []const u8, options: readCoreV1NamespaceStatusOptions) !ReadCoreV1NamespaceStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NamespaceStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespaceStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespaceStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespaceStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NamespaceStatusResponse = union(enum) {
        ok: ReplaceCoreV1NamespaceStatusResponse200,
        created: ReplaceCoreV1NamespaceStatusResponse201,
        unauthorized: ReplaceCoreV1NamespaceStatusResponse401,
        default_response: ReplaceCoreV1NamespaceStatusResponseDefault,
    };

    pub const replaceCoreV1NamespaceStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified Namespace
    pub fn replaceCoreV1NamespaceStatus(self: *Client, name: []const u8, body: root.io.k8s.api.core.v1.Namespace, options: replaceCoreV1NamespaceStatusOptions) !ReplaceCoreV1NamespaceStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NamespaceStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespaceStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.Namespace,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespaceStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespaceStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NamespaceStatusResponse = union(enum) {
        ok: PatchCoreV1NamespaceStatusResponse200,
        created: PatchCoreV1NamespaceStatusResponse201,
        unauthorized: PatchCoreV1NamespaceStatusResponse401,
        default_response: PatchCoreV1NamespaceStatusResponseDefault,
    };

    pub const patchCoreV1NamespaceStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified Namespace
    pub fn patchCoreV1NamespaceStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NamespaceStatusOptions) !PatchCoreV1NamespaceStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/namespaces/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Namespace, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1NodeResponse200 = struct {
        body: root.io.k8s.api.core.v1.NodeList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1NodeResponse = union(enum) {
        ok: ListCoreV1NodeResponse200,
        unauthorized: ListCoreV1NodeResponse401,
        default_response: ListCoreV1NodeResponseDefault,
    };

    pub const listCoreV1NodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Node
    pub fn listCoreV1Node(self: *Client, options: listCoreV1NodeOptions) !ListCoreV1NodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.NodeList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1Node(self: *Client, options: listCoreV1NodeOptions) !WatchStream(root.io.k8s.api.core.v1.Node) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/nodes");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.Node){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1NodeResponse200 = struct {
        body: root.io.k8s.api.core.v1.Node,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NodeResponse201 = struct {
        body: root.io.k8s.api.core.v1.Node,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NodeResponse202 = struct {
        body: root.io.k8s.api.core.v1.Node,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1NodeResponse = union(enum) {
        ok: CreateCoreV1NodeResponse200,
        created: CreateCoreV1NodeResponse201,
        accepted: CreateCoreV1NodeResponse202,
        unauthorized: CreateCoreV1NodeResponse401,
        default_response: CreateCoreV1NodeResponseDefault,
    };

    pub const createCoreV1NodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a Node
    pub fn createCoreV1Node(self: *Client, body: root.io.k8s.api.core.v1.Node, options: createCoreV1NodeOptions) !CreateCoreV1NodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Node, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Node, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Node, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionNodeResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionNodeResponse = union(enum) {
        ok: DeleteCoreV1CollectionNodeResponse200,
        unauthorized: DeleteCoreV1CollectionNodeResponse401,
        default_response: DeleteCoreV1CollectionNodeResponseDefault,
    };

    pub const deleteCoreV1CollectionNodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Node
    pub fn deleteCoreV1CollectionNode(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionNodeOptions) !DeleteCoreV1CollectionNodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NodeResponse200 = struct {
        body: root.io.k8s.api.core.v1.Node,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NodeResponse = union(enum) {
        ok: ReadCoreV1NodeResponse200,
        unauthorized: ReadCoreV1NodeResponse401,
        default_response: ReadCoreV1NodeResponseDefault,
    };

    pub const readCoreV1NodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Node
    pub fn readCoreV1Node(self: *Client, name: []const u8, options: readCoreV1NodeOptions) !ReadCoreV1NodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Node, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NodeResponse200 = struct {
        body: root.io.k8s.api.core.v1.Node,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NodeResponse201 = struct {
        body: root.io.k8s.api.core.v1.Node,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NodeResponse = union(enum) {
        ok: ReplaceCoreV1NodeResponse200,
        created: ReplaceCoreV1NodeResponse201,
        unauthorized: ReplaceCoreV1NodeResponse401,
        default_response: ReplaceCoreV1NodeResponseDefault,
    };

    pub const replaceCoreV1NodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Node
    pub fn replaceCoreV1Node(self: *Client, name: []const u8, body: root.io.k8s.api.core.v1.Node, options: replaceCoreV1NodeOptions) !ReplaceCoreV1NodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Node, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Node, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1NodeResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NodeResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1NodeResponse = union(enum) {
        ok: DeleteCoreV1NodeResponse200,
        accepted: DeleteCoreV1NodeResponse202,
        unauthorized: DeleteCoreV1NodeResponse401,
        default_response: DeleteCoreV1NodeResponseDefault,
    };

    pub const deleteCoreV1NodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a Node
    pub fn deleteCoreV1Node(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1NodeOptions) !DeleteCoreV1NodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NodeResponse200 = struct {
        body: root.io.k8s.api.core.v1.Node,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NodeResponse201 = struct {
        body: root.io.k8s.api.core.v1.Node,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NodeResponse = union(enum) {
        ok: PatchCoreV1NodeResponse200,
        created: PatchCoreV1NodeResponse201,
        unauthorized: PatchCoreV1NodeResponse401,
        default_response: PatchCoreV1NodeResponseDefault,
    };

    pub const patchCoreV1NodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Node
    pub fn patchCoreV1Node(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NodeOptions) !PatchCoreV1NodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Node, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Node, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1GetNodeProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNodeProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNodeProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNodeProxyResponse = union(enum) {
        ok: ConnectCoreV1GetNodeProxyResponse200,
        unauthorized: ConnectCoreV1GetNodeProxyResponse401,
        default_response: ConnectCoreV1GetNodeProxyResponseDefault,
    };

    pub const connectCoreV1GetNodeProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        path: ?[]const u8 = null,
    };
    /// connect GET requests to proxy of Node
    pub fn connectCoreV1GetNodeProxy(self: *Client, name: []const u8, options: connectCoreV1GetNodeProxyOptions) !ConnectCoreV1GetNodeProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PostNodeProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNodeProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNodeProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNodeProxyResponse = union(enum) {
        ok: ConnectCoreV1PostNodeProxyResponse200,
        unauthorized: ConnectCoreV1PostNodeProxyResponse401,
        default_response: ConnectCoreV1PostNodeProxyResponseDefault,
    };

    pub const connectCoreV1PostNodeProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        path: ?[]const u8 = null,
    };
    /// connect POST requests to proxy of Node
    pub fn connectCoreV1PostNodeProxy(self: *Client, name: []const u8, options: connectCoreV1PostNodeProxyOptions) !ConnectCoreV1PostNodeProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PutNodeProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNodeProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNodeProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNodeProxyResponse = union(enum) {
        ok: ConnectCoreV1PutNodeProxyResponse200,
        unauthorized: ConnectCoreV1PutNodeProxyResponse401,
        default_response: ConnectCoreV1PutNodeProxyResponseDefault,
    };

    pub const connectCoreV1PutNodeProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        path: ?[]const u8 = null,
    };
    /// connect PUT requests to proxy of Node
    pub fn connectCoreV1PutNodeProxy(self: *Client, name: []const u8, options: connectCoreV1PutNodeProxyOptions) !ConnectCoreV1PutNodeProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1DeleteNodeProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNodeProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNodeProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNodeProxyResponse = union(enum) {
        ok: ConnectCoreV1DeleteNodeProxyResponse200,
        unauthorized: ConnectCoreV1DeleteNodeProxyResponse401,
        default_response: ConnectCoreV1DeleteNodeProxyResponseDefault,
    };

    pub const connectCoreV1DeleteNodeProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        path: ?[]const u8 = null,
    };
    /// connect DELETE requests to proxy of Node
    pub fn connectCoreV1DeleteNodeProxy(self: *Client, name: []const u8, options: connectCoreV1DeleteNodeProxyOptions) !ConnectCoreV1DeleteNodeProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PatchNodeProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNodeProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNodeProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNodeProxyResponse = union(enum) {
        ok: ConnectCoreV1PatchNodeProxyResponse200,
        unauthorized: ConnectCoreV1PatchNodeProxyResponse401,
        default_response: ConnectCoreV1PatchNodeProxyResponseDefault,
    };

    pub const connectCoreV1PatchNodeProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        path: ?[]const u8 = null,
    };
    /// connect PATCH requests to proxy of Node
    pub fn connectCoreV1PatchNodeProxy(self: *Client, name: []const u8, options: connectCoreV1PatchNodeProxyOptions) !ConnectCoreV1PatchNodeProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1OptionsNodeProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNodeProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNodeProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNodeProxyResponse = union(enum) {
        ok: ConnectCoreV1OptionsNodeProxyResponse200,
        unauthorized: ConnectCoreV1OptionsNodeProxyResponse401,
        default_response: ConnectCoreV1OptionsNodeProxyResponseDefault,
    };

    pub const connectCoreV1OptionsNodeProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        path: ?[]const u8 = null,
    };
    /// connect OPTIONS requests to proxy of Node
    pub fn connectCoreV1OptionsNodeProxy(self: *Client, name: []const u8, options: connectCoreV1OptionsNodeProxyOptions) !ConnectCoreV1OptionsNodeProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .OPTIONS, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "OPTIONS", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1HeadNodeProxyResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNodeProxyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNodeProxyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNodeProxyResponse = union(enum) {
        ok: ConnectCoreV1HeadNodeProxyResponse200,
        unauthorized: ConnectCoreV1HeadNodeProxyResponse401,
        default_response: ConnectCoreV1HeadNodeProxyResponseDefault,
    };

    pub const connectCoreV1HeadNodeProxyOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        path: ?[]const u8 = null,
    };
    /// connect HEAD requests to proxy of Node
    pub fn connectCoreV1HeadNodeProxy(self: *Client, name: []const u8, options: connectCoreV1HeadNodeProxyOptions) !ConnectCoreV1HeadNodeProxyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy", .{});
        var first_query = true;
        _ = &first_query;
        if (options.path) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .HEAD, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "HEAD", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1GetNodeProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNodeProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNodeProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1GetNodeProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1GetNodeProxyWithPathResponse200,
        unauthorized: ConnectCoreV1GetNodeProxyWithPathResponse401,
        default_response: ConnectCoreV1GetNodeProxyWithPathResponseDefault,
    };

    pub const connectCoreV1GetNodeProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        pathQuery: ?[]const u8 = null,
    };
    /// connect GET requests to proxy of Node
    pub fn connectCoreV1GetNodeProxyWithPath(self: *Client, name: []const u8, path: []const u8, options: connectCoreV1GetNodeProxyWithPathOptions) !ConnectCoreV1GetNodeProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PostNodeProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNodeProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNodeProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PostNodeProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1PostNodeProxyWithPathResponse200,
        unauthorized: ConnectCoreV1PostNodeProxyWithPathResponse401,
        default_response: ConnectCoreV1PostNodeProxyWithPathResponseDefault,
    };

    pub const connectCoreV1PostNodeProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        pathQuery: ?[]const u8 = null,
    };
    /// connect POST requests to proxy of Node
    pub fn connectCoreV1PostNodeProxyWithPath(self: *Client, name: []const u8, path: []const u8, options: connectCoreV1PostNodeProxyWithPathOptions) !ConnectCoreV1PostNodeProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PutNodeProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNodeProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNodeProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PutNodeProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1PutNodeProxyWithPathResponse200,
        unauthorized: ConnectCoreV1PutNodeProxyWithPathResponse401,
        default_response: ConnectCoreV1PutNodeProxyWithPathResponseDefault,
    };

    pub const connectCoreV1PutNodeProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        pathQuery: ?[]const u8 = null,
    };
    /// connect PUT requests to proxy of Node
    pub fn connectCoreV1PutNodeProxyWithPath(self: *Client, name: []const u8, path: []const u8, options: connectCoreV1PutNodeProxyWithPathOptions) !ConnectCoreV1PutNodeProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1DeleteNodeProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNodeProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNodeProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1DeleteNodeProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1DeleteNodeProxyWithPathResponse200,
        unauthorized: ConnectCoreV1DeleteNodeProxyWithPathResponse401,
        default_response: ConnectCoreV1DeleteNodeProxyWithPathResponseDefault,
    };

    pub const connectCoreV1DeleteNodeProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        pathQuery: ?[]const u8 = null,
    };
    /// connect DELETE requests to proxy of Node
    pub fn connectCoreV1DeleteNodeProxyWithPath(self: *Client, name: []const u8, path: []const u8, options: connectCoreV1DeleteNodeProxyWithPathOptions) !ConnectCoreV1DeleteNodeProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1PatchNodeProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNodeProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNodeProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1PatchNodeProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1PatchNodeProxyWithPathResponse200,
        unauthorized: ConnectCoreV1PatchNodeProxyWithPathResponse401,
        default_response: ConnectCoreV1PatchNodeProxyWithPathResponseDefault,
    };

    pub const connectCoreV1PatchNodeProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        pathQuery: ?[]const u8 = null,
    };
    /// connect PATCH requests to proxy of Node
    pub fn connectCoreV1PatchNodeProxyWithPath(self: *Client, name: []const u8, path: []const u8, options: connectCoreV1PatchNodeProxyWithPathOptions) !ConnectCoreV1PatchNodeProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1OptionsNodeProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNodeProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNodeProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1OptionsNodeProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1OptionsNodeProxyWithPathResponse200,
        unauthorized: ConnectCoreV1OptionsNodeProxyWithPathResponse401,
        default_response: ConnectCoreV1OptionsNodeProxyWithPathResponseDefault,
    };

    pub const connectCoreV1OptionsNodeProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        pathQuery: ?[]const u8 = null,
    };
    /// connect OPTIONS requests to proxy of Node
    pub fn connectCoreV1OptionsNodeProxyWithPath(self: *Client, name: []const u8, path: []const u8, options: connectCoreV1OptionsNodeProxyWithPathOptions) !ConnectCoreV1OptionsNodeProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .OPTIONS, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "OPTIONS", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ConnectCoreV1HeadNodeProxyWithPathResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNodeProxyWithPathResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNodeProxyWithPathResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ConnectCoreV1HeadNodeProxyWithPathResponse = union(enum) {
        ok: ConnectCoreV1HeadNodeProxyWithPathResponse200,
        unauthorized: ConnectCoreV1HeadNodeProxyWithPathResponse401,
        default_response: ConnectCoreV1HeadNodeProxyWithPathResponseDefault,
    };

    pub const connectCoreV1HeadNodeProxyWithPathOptions = struct {
        /// Path is the URL path to use for the current proxy request to node.
        pathQuery: ?[]const u8 = null,
    };
    /// connect HEAD requests to proxy of Node
    pub fn connectCoreV1HeadNodeProxyWithPath(self: *Client, name: []const u8, path: []const u8, options: connectCoreV1HeadNodeProxyWithPathOptions) !ConnectCoreV1HeadNodeProxyWithPathResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/proxy/", .{});
        try urlEncode(url_w, path);
        var first_query = true;
        _ = &first_query;
        if (options.pathQuery) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("path=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .HEAD, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "HEAD", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1NodeStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.Node,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NodeStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NodeStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1NodeStatusResponse = union(enum) {
        ok: ReadCoreV1NodeStatusResponse200,
        unauthorized: ReadCoreV1NodeStatusResponse401,
        default_response: ReadCoreV1NodeStatusResponseDefault,
    };

    pub const readCoreV1NodeStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified Node
    pub fn readCoreV1NodeStatus(self: *Client, name: []const u8, options: readCoreV1NodeStatusOptions) !ReadCoreV1NodeStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Node, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1NodeStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.Node,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NodeStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.Node,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NodeStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NodeStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1NodeStatusResponse = union(enum) {
        ok: ReplaceCoreV1NodeStatusResponse200,
        created: ReplaceCoreV1NodeStatusResponse201,
        unauthorized: ReplaceCoreV1NodeStatusResponse401,
        default_response: ReplaceCoreV1NodeStatusResponseDefault,
    };

    pub const replaceCoreV1NodeStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified Node
    pub fn replaceCoreV1NodeStatus(self: *Client, name: []const u8, body: root.io.k8s.api.core.v1.Node, options: replaceCoreV1NodeStatusOptions) !ReplaceCoreV1NodeStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Node, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Node, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1NodeStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.Node,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NodeStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.Node,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NodeStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NodeStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1NodeStatusResponse = union(enum) {
        ok: PatchCoreV1NodeStatusResponse200,
        created: PatchCoreV1NodeStatusResponse201,
        unauthorized: PatchCoreV1NodeStatusResponse401,
        default_response: PatchCoreV1NodeStatusResponseDefault,
    };

    pub const patchCoreV1NodeStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified Node
    pub fn patchCoreV1NodeStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1NodeStatusOptions) !PatchCoreV1NodeStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/nodes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Node, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.Node, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1PersistentVolumeClaimForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeClaimList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1PersistentVolumeClaimForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1PersistentVolumeClaimForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1PersistentVolumeClaimForAllNamespacesResponse = union(enum) {
        ok: ListCoreV1PersistentVolumeClaimForAllNamespacesResponse200,
        unauthorized: ListCoreV1PersistentVolumeClaimForAllNamespacesResponse401,
        default_response: ListCoreV1PersistentVolumeClaimForAllNamespacesResponseDefault,
    };

    pub const listCoreV1PersistentVolumeClaimForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PersistentVolumeClaim
    pub fn listCoreV1PersistentVolumeClaimForAllNamespaces(self: *Client, options: listCoreV1PersistentVolumeClaimForAllNamespacesOptions) !ListCoreV1PersistentVolumeClaimForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/persistentvolumeclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeClaimList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1PersistentVolumeClaimForAllNamespaces(self: *Client, options: listCoreV1PersistentVolumeClaimForAllNamespacesOptions) !WatchStream(root.io.k8s.api.core.v1.PersistentVolumeClaim) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/persistentvolumeclaims");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.PersistentVolumeClaim){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoreV1PersistentVolumeResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolumeList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1PersistentVolumeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1PersistentVolumeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1PersistentVolumeResponse = union(enum) {
        ok: ListCoreV1PersistentVolumeResponse200,
        unauthorized: ListCoreV1PersistentVolumeResponse401,
        default_response: ListCoreV1PersistentVolumeResponseDefault,
    };

    pub const listCoreV1PersistentVolumeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PersistentVolume
    pub fn listCoreV1PersistentVolume(self: *Client, options: listCoreV1PersistentVolumeOptions) !ListCoreV1PersistentVolumeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/persistentvolumes", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolumeList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1PersistentVolume(self: *Client, options: listCoreV1PersistentVolumeOptions) !WatchStream(root.io.k8s.api.core.v1.PersistentVolume) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/persistentvolumes");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.PersistentVolume){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoreV1PersistentVolumeResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1PersistentVolumeResponse201 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1PersistentVolumeResponse202 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1PersistentVolumeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1PersistentVolumeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoreV1PersistentVolumeResponse = union(enum) {
        ok: CreateCoreV1PersistentVolumeResponse200,
        created: CreateCoreV1PersistentVolumeResponse201,
        accepted: CreateCoreV1PersistentVolumeResponse202,
        unauthorized: CreateCoreV1PersistentVolumeResponse401,
        default_response: CreateCoreV1PersistentVolumeResponseDefault,
    };

    pub const createCoreV1PersistentVolumeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a PersistentVolume
    pub fn createCoreV1PersistentVolume(self: *Client, body: root.io.k8s.api.core.v1.PersistentVolume, options: createCoreV1PersistentVolumeOptions) !CreateCoreV1PersistentVolumeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/persistentvolumes", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1CollectionPersistentVolumeResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionPersistentVolumeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionPersistentVolumeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1CollectionPersistentVolumeResponse = union(enum) {
        ok: DeleteCoreV1CollectionPersistentVolumeResponse200,
        unauthorized: DeleteCoreV1CollectionPersistentVolumeResponse401,
        default_response: DeleteCoreV1CollectionPersistentVolumeResponseDefault,
    };

    pub const deleteCoreV1CollectionPersistentVolumeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of PersistentVolume
    pub fn deleteCoreV1CollectionPersistentVolume(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1CollectionPersistentVolumeOptions) !DeleteCoreV1CollectionPersistentVolumeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/persistentvolumes", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1PersistentVolumeResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1PersistentVolumeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1PersistentVolumeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1PersistentVolumeResponse = union(enum) {
        ok: ReadCoreV1PersistentVolumeResponse200,
        unauthorized: ReadCoreV1PersistentVolumeResponse401,
        default_response: ReadCoreV1PersistentVolumeResponseDefault,
    };

    pub const readCoreV1PersistentVolumeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified PersistentVolume
    pub fn readCoreV1PersistentVolume(self: *Client, name: []const u8, options: readCoreV1PersistentVolumeOptions) !ReadCoreV1PersistentVolumeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/persistentvolumes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1PersistentVolumeResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1PersistentVolumeResponse201 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1PersistentVolumeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1PersistentVolumeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1PersistentVolumeResponse = union(enum) {
        ok: ReplaceCoreV1PersistentVolumeResponse200,
        created: ReplaceCoreV1PersistentVolumeResponse201,
        unauthorized: ReplaceCoreV1PersistentVolumeResponse401,
        default_response: ReplaceCoreV1PersistentVolumeResponseDefault,
    };

    pub const replaceCoreV1PersistentVolumeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified PersistentVolume
    pub fn replaceCoreV1PersistentVolume(self: *Client, name: []const u8, body: root.io.k8s.api.core.v1.PersistentVolume, options: replaceCoreV1PersistentVolumeOptions) !ReplaceCoreV1PersistentVolumeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/persistentvolumes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoreV1PersistentVolumeResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1PersistentVolumeResponse202 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1PersistentVolumeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1PersistentVolumeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoreV1PersistentVolumeResponse = union(enum) {
        ok: DeleteCoreV1PersistentVolumeResponse200,
        accepted: DeleteCoreV1PersistentVolumeResponse202,
        unauthorized: DeleteCoreV1PersistentVolumeResponse401,
        default_response: DeleteCoreV1PersistentVolumeResponseDefault,
    };

    pub const deleteCoreV1PersistentVolumeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a PersistentVolume
    pub fn deleteCoreV1PersistentVolume(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoreV1PersistentVolumeOptions) !DeleteCoreV1PersistentVolumeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/persistentvolumes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1PersistentVolumeResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1PersistentVolumeResponse201 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1PersistentVolumeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1PersistentVolumeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1PersistentVolumeResponse = union(enum) {
        ok: PatchCoreV1PersistentVolumeResponse200,
        created: PatchCoreV1PersistentVolumeResponse201,
        unauthorized: PatchCoreV1PersistentVolumeResponse401,
        default_response: PatchCoreV1PersistentVolumeResponseDefault,
    };

    pub const patchCoreV1PersistentVolumeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified PersistentVolume
    pub fn patchCoreV1PersistentVolume(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1PersistentVolumeOptions) !PatchCoreV1PersistentVolumeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/persistentvolumes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoreV1PersistentVolumeStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1PersistentVolumeStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1PersistentVolumeStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoreV1PersistentVolumeStatusResponse = union(enum) {
        ok: ReadCoreV1PersistentVolumeStatusResponse200,
        unauthorized: ReadCoreV1PersistentVolumeStatusResponse401,
        default_response: ReadCoreV1PersistentVolumeStatusResponseDefault,
    };

    pub const readCoreV1PersistentVolumeStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified PersistentVolume
    pub fn readCoreV1PersistentVolumeStatus(self: *Client, name: []const u8, options: readCoreV1PersistentVolumeStatusOptions) !ReadCoreV1PersistentVolumeStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/persistentvolumes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoreV1PersistentVolumeStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1PersistentVolumeStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1PersistentVolumeStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1PersistentVolumeStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoreV1PersistentVolumeStatusResponse = union(enum) {
        ok: ReplaceCoreV1PersistentVolumeStatusResponse200,
        created: ReplaceCoreV1PersistentVolumeStatusResponse201,
        unauthorized: ReplaceCoreV1PersistentVolumeStatusResponse401,
        default_response: ReplaceCoreV1PersistentVolumeStatusResponseDefault,
    };

    pub const replaceCoreV1PersistentVolumeStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified PersistentVolume
    pub fn replaceCoreV1PersistentVolumeStatus(self: *Client, name: []const u8, body: root.io.k8s.api.core.v1.PersistentVolume, options: replaceCoreV1PersistentVolumeStatusOptions) !ReplaceCoreV1PersistentVolumeStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/persistentvolumes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoreV1PersistentVolumeStatusResponse200 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1PersistentVolumeStatusResponse201 = struct {
        body: root.io.k8s.api.core.v1.PersistentVolume,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1PersistentVolumeStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1PersistentVolumeStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoreV1PersistentVolumeStatusResponse = union(enum) {
        ok: PatchCoreV1PersistentVolumeStatusResponse200,
        created: PatchCoreV1PersistentVolumeStatusResponse201,
        unauthorized: PatchCoreV1PersistentVolumeStatusResponse401,
        default_response: PatchCoreV1PersistentVolumeStatusResponseDefault,
    };

    pub const patchCoreV1PersistentVolumeStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified PersistentVolume
    pub fn patchCoreV1PersistentVolumeStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoreV1PersistentVolumeStatusOptions) !PatchCoreV1PersistentVolumeStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/persistentvolumes/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PersistentVolume, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoreV1PodForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.core.v1.PodList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1PodForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1PodForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1PodForAllNamespacesResponse = union(enum) {
        ok: ListCoreV1PodForAllNamespacesResponse200,
        unauthorized: ListCoreV1PodForAllNamespacesResponse401,
        default_response: ListCoreV1PodForAllNamespacesResponseDefault,
    };

    pub const listCoreV1PodForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Pod
    pub fn listCoreV1PodForAllNamespaces(self: *Client, options: listCoreV1PodForAllNamespacesOptions) !ListCoreV1PodForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/pods", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1PodForAllNamespaces(self: *Client, options: listCoreV1PodForAllNamespacesOptions) !WatchStream(root.io.k8s.api.core.v1.Pod) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/pods");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.Pod){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoreV1PodTemplateForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.core.v1.PodTemplateList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1PodTemplateForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1PodTemplateForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1PodTemplateForAllNamespacesResponse = union(enum) {
        ok: ListCoreV1PodTemplateForAllNamespacesResponse200,
        unauthorized: ListCoreV1PodTemplateForAllNamespacesResponse401,
        default_response: ListCoreV1PodTemplateForAllNamespacesResponseDefault,
    };

    pub const listCoreV1PodTemplateForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PodTemplate
    pub fn listCoreV1PodTemplateForAllNamespaces(self: *Client, options: listCoreV1PodTemplateForAllNamespacesOptions) !ListCoreV1PodTemplateForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/podtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.PodTemplateList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1PodTemplateForAllNamespaces(self: *Client, options: listCoreV1PodTemplateForAllNamespacesOptions) !WatchStream(root.io.k8s.api.core.v1.PodTemplate) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/podtemplates");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.PodTemplate){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoreV1ReplicationControllerForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.core.v1.ReplicationControllerList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ReplicationControllerForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ReplicationControllerForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ReplicationControllerForAllNamespacesResponse = union(enum) {
        ok: ListCoreV1ReplicationControllerForAllNamespacesResponse200,
        unauthorized: ListCoreV1ReplicationControllerForAllNamespacesResponse401,
        default_response: ListCoreV1ReplicationControllerForAllNamespacesResponseDefault,
    };

    pub const listCoreV1ReplicationControllerForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ReplicationController
    pub fn listCoreV1ReplicationControllerForAllNamespaces(self: *Client, options: listCoreV1ReplicationControllerForAllNamespacesOptions) !ListCoreV1ReplicationControllerForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/replicationcontrollers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ReplicationControllerList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1ReplicationControllerForAllNamespaces(self: *Client, options: listCoreV1ReplicationControllerForAllNamespacesOptions) !WatchStream(root.io.k8s.api.core.v1.ReplicationController) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/replicationcontrollers");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.ReplicationController){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoreV1ResourceQuotaForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.core.v1.ResourceQuotaList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ResourceQuotaForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ResourceQuotaForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ResourceQuotaForAllNamespacesResponse = union(enum) {
        ok: ListCoreV1ResourceQuotaForAllNamespacesResponse200,
        unauthorized: ListCoreV1ResourceQuotaForAllNamespacesResponse401,
        default_response: ListCoreV1ResourceQuotaForAllNamespacesResponseDefault,
    };

    pub const listCoreV1ResourceQuotaForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceQuota
    pub fn listCoreV1ResourceQuotaForAllNamespaces(self: *Client, options: listCoreV1ResourceQuotaForAllNamespacesOptions) !ListCoreV1ResourceQuotaForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/resourcequotas", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ResourceQuotaList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1ResourceQuotaForAllNamespaces(self: *Client, options: listCoreV1ResourceQuotaForAllNamespacesOptions) !WatchStream(root.io.k8s.api.core.v1.ResourceQuota) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/resourcequotas");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.ResourceQuota){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoreV1SecretForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.core.v1.SecretList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1SecretForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1SecretForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1SecretForAllNamespacesResponse = union(enum) {
        ok: ListCoreV1SecretForAllNamespacesResponse200,
        unauthorized: ListCoreV1SecretForAllNamespacesResponse401,
        default_response: ListCoreV1SecretForAllNamespacesResponseDefault,
    };

    pub const listCoreV1SecretForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Secret
    pub fn listCoreV1SecretForAllNamespaces(self: *Client, options: listCoreV1SecretForAllNamespacesOptions) !ListCoreV1SecretForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/secrets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.SecretList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1SecretForAllNamespaces(self: *Client, options: listCoreV1SecretForAllNamespacesOptions) !WatchStream(root.io.k8s.api.core.v1.Secret) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/secrets");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.Secret){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoreV1ServiceAccountForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.core.v1.ServiceAccountList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ServiceAccountForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ServiceAccountForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ServiceAccountForAllNamespacesResponse = union(enum) {
        ok: ListCoreV1ServiceAccountForAllNamespacesResponse200,
        unauthorized: ListCoreV1ServiceAccountForAllNamespacesResponse401,
        default_response: ListCoreV1ServiceAccountForAllNamespacesResponseDefault,
    };

    pub const listCoreV1ServiceAccountForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ServiceAccount
    pub fn listCoreV1ServiceAccountForAllNamespaces(self: *Client, options: listCoreV1ServiceAccountForAllNamespacesOptions) !ListCoreV1ServiceAccountForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/serviceaccounts", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceAccountList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1ServiceAccountForAllNamespaces(self: *Client, options: listCoreV1ServiceAccountForAllNamespacesOptions) !WatchStream(root.io.k8s.api.core.v1.ServiceAccount) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/serviceaccounts");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.ServiceAccount){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoreV1ServiceForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.core.v1.ServiceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ServiceForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ServiceForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoreV1ServiceForAllNamespacesResponse = union(enum) {
        ok: ListCoreV1ServiceForAllNamespacesResponse200,
        unauthorized: ListCoreV1ServiceForAllNamespacesResponse401,
        default_response: ListCoreV1ServiceForAllNamespacesResponseDefault,
    };

    pub const listCoreV1ServiceForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Service
    pub fn listCoreV1ServiceForAllNamespaces(self: *Client, options: listCoreV1ServiceForAllNamespacesOptions) !ListCoreV1ServiceForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/services", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.core.v1.ServiceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoreV1ServiceForAllNamespaces(self: *Client, options: listCoreV1ServiceForAllNamespacesOptions) !WatchStream(root.io.k8s.api.core.v1.Service) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/api/v1/services");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.core.v1.Service){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const WatchCoreV1ConfigMapListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ConfigMapListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ConfigMapListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ConfigMapListForAllNamespacesResponse = union(enum) {
        ok: WatchCoreV1ConfigMapListForAllNamespacesResponse200,
        unauthorized: WatchCoreV1ConfigMapListForAllNamespacesResponse401,
        default_response: WatchCoreV1ConfigMapListForAllNamespacesResponseDefault,
    };

    pub const watchCoreV1ConfigMapListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ConfigMap. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1ConfigMapListForAllNamespaces(self: *Client, options: watchCoreV1ConfigMapListForAllNamespacesOptions) !WatchCoreV1ConfigMapListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/configmaps", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1EndpointsListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1EndpointsListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1EndpointsListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1EndpointsListForAllNamespacesResponse = union(enum) {
        ok: WatchCoreV1EndpointsListForAllNamespacesResponse200,
        unauthorized: WatchCoreV1EndpointsListForAllNamespacesResponse401,
        default_response: WatchCoreV1EndpointsListForAllNamespacesResponseDefault,
    };

    pub const watchCoreV1EndpointsListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Endpoints. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1EndpointsListForAllNamespaces(self: *Client, options: watchCoreV1EndpointsListForAllNamespacesOptions) !WatchCoreV1EndpointsListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/endpoints", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1EventListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1EventListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1EventListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1EventListForAllNamespacesResponse = union(enum) {
        ok: WatchCoreV1EventListForAllNamespacesResponse200,
        unauthorized: WatchCoreV1EventListForAllNamespacesResponse401,
        default_response: WatchCoreV1EventListForAllNamespacesResponseDefault,
    };

    pub const watchCoreV1EventListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Event. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1EventListForAllNamespaces(self: *Client, options: watchCoreV1EventListForAllNamespacesOptions) !WatchCoreV1EventListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/events", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1LimitRangeListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1LimitRangeListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1LimitRangeListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1LimitRangeListForAllNamespacesResponse = union(enum) {
        ok: WatchCoreV1LimitRangeListForAllNamespacesResponse200,
        unauthorized: WatchCoreV1LimitRangeListForAllNamespacesResponse401,
        default_response: WatchCoreV1LimitRangeListForAllNamespacesResponseDefault,
    };

    pub const watchCoreV1LimitRangeListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of LimitRange. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1LimitRangeListForAllNamespaces(self: *Client, options: watchCoreV1LimitRangeListForAllNamespacesOptions) !WatchCoreV1LimitRangeListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/limitranges", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespaceListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespaceListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespaceListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespaceListResponse = union(enum) {
        ok: WatchCoreV1NamespaceListResponse200,
        unauthorized: WatchCoreV1NamespaceListResponse401,
        default_response: WatchCoreV1NamespaceListResponseDefault,
    };

    pub const watchCoreV1NamespaceListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Namespace. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NamespaceList(self: *Client, options: watchCoreV1NamespaceListOptions) !WatchCoreV1NamespaceListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedConfigMapListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedConfigMapListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedConfigMapListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedConfigMapListResponse = union(enum) {
        ok: WatchCoreV1NamespacedConfigMapListResponse200,
        unauthorized: WatchCoreV1NamespacedConfigMapListResponse401,
        default_response: WatchCoreV1NamespacedConfigMapListResponseDefault,
    };

    pub const watchCoreV1NamespacedConfigMapListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ConfigMap. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NamespacedConfigMapList(self: *Client, namespace: []const u8, options: watchCoreV1NamespacedConfigMapListOptions) !WatchCoreV1NamespacedConfigMapListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/configmaps", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedConfigMapResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedConfigMapResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedConfigMapResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedConfigMapResponse = union(enum) {
        ok: WatchCoreV1NamespacedConfigMapResponse200,
        unauthorized: WatchCoreV1NamespacedConfigMapResponse401,
        default_response: WatchCoreV1NamespacedConfigMapResponseDefault,
    };

    pub const watchCoreV1NamespacedConfigMapOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ConfigMap. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1NamespacedConfigMap(self: *Client, name: []const u8, namespace: []const u8, options: watchCoreV1NamespacedConfigMapOptions) !WatchCoreV1NamespacedConfigMapResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/configmaps/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedEndpointsListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedEndpointsListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedEndpointsListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedEndpointsListResponse = union(enum) {
        ok: WatchCoreV1NamespacedEndpointsListResponse200,
        unauthorized: WatchCoreV1NamespacedEndpointsListResponse401,
        default_response: WatchCoreV1NamespacedEndpointsListResponseDefault,
    };

    pub const watchCoreV1NamespacedEndpointsListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Endpoints. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NamespacedEndpointsList(self: *Client, namespace: []const u8, options: watchCoreV1NamespacedEndpointsListOptions) !WatchCoreV1NamespacedEndpointsListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpoints", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedEndpointsResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedEndpointsResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedEndpointsResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedEndpointsResponse = union(enum) {
        ok: WatchCoreV1NamespacedEndpointsResponse200,
        unauthorized: WatchCoreV1NamespacedEndpointsResponse401,
        default_response: WatchCoreV1NamespacedEndpointsResponseDefault,
    };

    pub const watchCoreV1NamespacedEndpointsOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Endpoints. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1NamespacedEndpoints(self: *Client, name: []const u8, namespace: []const u8, options: watchCoreV1NamespacedEndpointsOptions) !WatchCoreV1NamespacedEndpointsResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpoints/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedEventListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedEventListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedEventListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedEventListResponse = union(enum) {
        ok: WatchCoreV1NamespacedEventListResponse200,
        unauthorized: WatchCoreV1NamespacedEventListResponse401,
        default_response: WatchCoreV1NamespacedEventListResponseDefault,
    };

    pub const watchCoreV1NamespacedEventListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Event. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NamespacedEventList(self: *Client, namespace: []const u8, options: watchCoreV1NamespacedEventListOptions) !WatchCoreV1NamespacedEventListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedEventResponse = union(enum) {
        ok: WatchCoreV1NamespacedEventResponse200,
        unauthorized: WatchCoreV1NamespacedEventResponse401,
        default_response: WatchCoreV1NamespacedEventResponseDefault,
    };

    pub const watchCoreV1NamespacedEventOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Event. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1NamespacedEvent(self: *Client, name: []const u8, namespace: []const u8, options: watchCoreV1NamespacedEventOptions) !WatchCoreV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedLimitRangeListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedLimitRangeListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedLimitRangeListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedLimitRangeListResponse = union(enum) {
        ok: WatchCoreV1NamespacedLimitRangeListResponse200,
        unauthorized: WatchCoreV1NamespacedLimitRangeListResponse401,
        default_response: WatchCoreV1NamespacedLimitRangeListResponseDefault,
    };

    pub const watchCoreV1NamespacedLimitRangeListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of LimitRange. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NamespacedLimitRangeList(self: *Client, namespace: []const u8, options: watchCoreV1NamespacedLimitRangeListOptions) !WatchCoreV1NamespacedLimitRangeListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/limitranges", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedLimitRangeResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedLimitRangeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedLimitRangeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedLimitRangeResponse = union(enum) {
        ok: WatchCoreV1NamespacedLimitRangeResponse200,
        unauthorized: WatchCoreV1NamespacedLimitRangeResponse401,
        default_response: WatchCoreV1NamespacedLimitRangeResponseDefault,
    };

    pub const watchCoreV1NamespacedLimitRangeOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind LimitRange. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1NamespacedLimitRange(self: *Client, name: []const u8, namespace: []const u8, options: watchCoreV1NamespacedLimitRangeOptions) !WatchCoreV1NamespacedLimitRangeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/limitranges/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedPersistentVolumeClaimListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPersistentVolumeClaimListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPersistentVolumeClaimListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPersistentVolumeClaimListResponse = union(enum) {
        ok: WatchCoreV1NamespacedPersistentVolumeClaimListResponse200,
        unauthorized: WatchCoreV1NamespacedPersistentVolumeClaimListResponse401,
        default_response: WatchCoreV1NamespacedPersistentVolumeClaimListResponseDefault,
    };

    pub const watchCoreV1NamespacedPersistentVolumeClaimListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PersistentVolumeClaim. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NamespacedPersistentVolumeClaimList(self: *Client, namespace: []const u8, options: watchCoreV1NamespacedPersistentVolumeClaimListOptions) !WatchCoreV1NamespacedPersistentVolumeClaimListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/persistentvolumeclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedPersistentVolumeClaimResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPersistentVolumeClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPersistentVolumeClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPersistentVolumeClaimResponse = union(enum) {
        ok: WatchCoreV1NamespacedPersistentVolumeClaimResponse200,
        unauthorized: WatchCoreV1NamespacedPersistentVolumeClaimResponse401,
        default_response: WatchCoreV1NamespacedPersistentVolumeClaimResponseDefault,
    };

    pub const watchCoreV1NamespacedPersistentVolumeClaimOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind PersistentVolumeClaim. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1NamespacedPersistentVolumeClaim(self: *Client, name: []const u8, namespace: []const u8, options: watchCoreV1NamespacedPersistentVolumeClaimOptions) !WatchCoreV1NamespacedPersistentVolumeClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/persistentvolumeclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedPodListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPodListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPodListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPodListResponse = union(enum) {
        ok: WatchCoreV1NamespacedPodListResponse200,
        unauthorized: WatchCoreV1NamespacedPodListResponse401,
        default_response: WatchCoreV1NamespacedPodListResponseDefault,
    };

    pub const watchCoreV1NamespacedPodListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Pod. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NamespacedPodList(self: *Client, namespace: []const u8, options: watchCoreV1NamespacedPodListOptions) !WatchCoreV1NamespacedPodListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedPodResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPodResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPodResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPodResponse = union(enum) {
        ok: WatchCoreV1NamespacedPodResponse200,
        unauthorized: WatchCoreV1NamespacedPodResponse401,
        default_response: WatchCoreV1NamespacedPodResponseDefault,
    };

    pub const watchCoreV1NamespacedPodOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Pod. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1NamespacedPod(self: *Client, name: []const u8, namespace: []const u8, options: watchCoreV1NamespacedPodOptions) !WatchCoreV1NamespacedPodResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/pods/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedPodTemplateListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPodTemplateListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPodTemplateListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPodTemplateListResponse = union(enum) {
        ok: WatchCoreV1NamespacedPodTemplateListResponse200,
        unauthorized: WatchCoreV1NamespacedPodTemplateListResponse401,
        default_response: WatchCoreV1NamespacedPodTemplateListResponseDefault,
    };

    pub const watchCoreV1NamespacedPodTemplateListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PodTemplate. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NamespacedPodTemplateList(self: *Client, namespace: []const u8, options: watchCoreV1NamespacedPodTemplateListOptions) !WatchCoreV1NamespacedPodTemplateListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedPodTemplateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPodTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPodTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedPodTemplateResponse = union(enum) {
        ok: WatchCoreV1NamespacedPodTemplateResponse200,
        unauthorized: WatchCoreV1NamespacedPodTemplateResponse401,
        default_response: WatchCoreV1NamespacedPodTemplateResponseDefault,
    };

    pub const watchCoreV1NamespacedPodTemplateOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind PodTemplate. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1NamespacedPodTemplate(self: *Client, name: []const u8, namespace: []const u8, options: watchCoreV1NamespacedPodTemplateOptions) !WatchCoreV1NamespacedPodTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedReplicationControllerListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedReplicationControllerListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedReplicationControllerListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedReplicationControllerListResponse = union(enum) {
        ok: WatchCoreV1NamespacedReplicationControllerListResponse200,
        unauthorized: WatchCoreV1NamespacedReplicationControllerListResponse401,
        default_response: WatchCoreV1NamespacedReplicationControllerListResponseDefault,
    };

    pub const watchCoreV1NamespacedReplicationControllerListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ReplicationController. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NamespacedReplicationControllerList(self: *Client, namespace: []const u8, options: watchCoreV1NamespacedReplicationControllerListOptions) !WatchCoreV1NamespacedReplicationControllerListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedReplicationControllerResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedReplicationControllerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedReplicationControllerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedReplicationControllerResponse = union(enum) {
        ok: WatchCoreV1NamespacedReplicationControllerResponse200,
        unauthorized: WatchCoreV1NamespacedReplicationControllerResponse401,
        default_response: WatchCoreV1NamespacedReplicationControllerResponseDefault,
    };

    pub const watchCoreV1NamespacedReplicationControllerOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ReplicationController. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1NamespacedReplicationController(self: *Client, name: []const u8, namespace: []const u8, options: watchCoreV1NamespacedReplicationControllerOptions) !WatchCoreV1NamespacedReplicationControllerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicationcontrollers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedResourceQuotaListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedResourceQuotaListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedResourceQuotaListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedResourceQuotaListResponse = union(enum) {
        ok: WatchCoreV1NamespacedResourceQuotaListResponse200,
        unauthorized: WatchCoreV1NamespacedResourceQuotaListResponse401,
        default_response: WatchCoreV1NamespacedResourceQuotaListResponseDefault,
    };

    pub const watchCoreV1NamespacedResourceQuotaListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceQuota. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NamespacedResourceQuotaList(self: *Client, namespace: []const u8, options: watchCoreV1NamespacedResourceQuotaListOptions) !WatchCoreV1NamespacedResourceQuotaListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourcequotas", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedResourceQuotaResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedResourceQuotaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedResourceQuotaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedResourceQuotaResponse = union(enum) {
        ok: WatchCoreV1NamespacedResourceQuotaResponse200,
        unauthorized: WatchCoreV1NamespacedResourceQuotaResponse401,
        default_response: WatchCoreV1NamespacedResourceQuotaResponseDefault,
    };

    pub const watchCoreV1NamespacedResourceQuotaOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ResourceQuota. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1NamespacedResourceQuota(self: *Client, name: []const u8, namespace: []const u8, options: watchCoreV1NamespacedResourceQuotaOptions) !WatchCoreV1NamespacedResourceQuotaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourcequotas/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedSecretListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedSecretListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedSecretListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedSecretListResponse = union(enum) {
        ok: WatchCoreV1NamespacedSecretListResponse200,
        unauthorized: WatchCoreV1NamespacedSecretListResponse401,
        default_response: WatchCoreV1NamespacedSecretListResponseDefault,
    };

    pub const watchCoreV1NamespacedSecretListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Secret. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NamespacedSecretList(self: *Client, namespace: []const u8, options: watchCoreV1NamespacedSecretListOptions) !WatchCoreV1NamespacedSecretListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/secrets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedSecretResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedSecretResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedSecretResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedSecretResponse = union(enum) {
        ok: WatchCoreV1NamespacedSecretResponse200,
        unauthorized: WatchCoreV1NamespacedSecretResponse401,
        default_response: WatchCoreV1NamespacedSecretResponseDefault,
    };

    pub const watchCoreV1NamespacedSecretOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Secret. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1NamespacedSecret(self: *Client, name: []const u8, namespace: []const u8, options: watchCoreV1NamespacedSecretOptions) !WatchCoreV1NamespacedSecretResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/secrets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedServiceAccountListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedServiceAccountListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedServiceAccountListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedServiceAccountListResponse = union(enum) {
        ok: WatchCoreV1NamespacedServiceAccountListResponse200,
        unauthorized: WatchCoreV1NamespacedServiceAccountListResponse401,
        default_response: WatchCoreV1NamespacedServiceAccountListResponseDefault,
    };

    pub const watchCoreV1NamespacedServiceAccountListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ServiceAccount. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NamespacedServiceAccountList(self: *Client, namespace: []const u8, options: watchCoreV1NamespacedServiceAccountListOptions) !WatchCoreV1NamespacedServiceAccountListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/serviceaccounts", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedServiceAccountResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedServiceAccountResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedServiceAccountResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedServiceAccountResponse = union(enum) {
        ok: WatchCoreV1NamespacedServiceAccountResponse200,
        unauthorized: WatchCoreV1NamespacedServiceAccountResponse401,
        default_response: WatchCoreV1NamespacedServiceAccountResponseDefault,
    };

    pub const watchCoreV1NamespacedServiceAccountOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ServiceAccount. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1NamespacedServiceAccount(self: *Client, name: []const u8, namespace: []const u8, options: watchCoreV1NamespacedServiceAccountOptions) !WatchCoreV1NamespacedServiceAccountResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/serviceaccounts/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedServiceListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedServiceListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedServiceListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedServiceListResponse = union(enum) {
        ok: WatchCoreV1NamespacedServiceListResponse200,
        unauthorized: WatchCoreV1NamespacedServiceListResponse401,
        default_response: WatchCoreV1NamespacedServiceListResponseDefault,
    };

    pub const watchCoreV1NamespacedServiceListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Service. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NamespacedServiceList(self: *Client, namespace: []const u8, options: watchCoreV1NamespacedServiceListOptions) !WatchCoreV1NamespacedServiceListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespacedServiceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespacedServiceResponse = union(enum) {
        ok: WatchCoreV1NamespacedServiceResponse200,
        unauthorized: WatchCoreV1NamespacedServiceResponse401,
        default_response: WatchCoreV1NamespacedServiceResponseDefault,
    };

    pub const watchCoreV1NamespacedServiceOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Service. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1NamespacedService(self: *Client, name: []const u8, namespace: []const u8, options: watchCoreV1NamespacedServiceOptions) !WatchCoreV1NamespacedServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/services/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NamespaceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespaceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespaceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NamespaceResponse = union(enum) {
        ok: WatchCoreV1NamespaceResponse200,
        unauthorized: WatchCoreV1NamespaceResponse401,
        default_response: WatchCoreV1NamespaceResponseDefault,
    };

    pub const watchCoreV1NamespaceOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Namespace. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1Namespace(self: *Client, name: []const u8, options: watchCoreV1NamespaceOptions) !WatchCoreV1NamespaceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/namespaces/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NodeListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NodeListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NodeListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NodeListResponse = union(enum) {
        ok: WatchCoreV1NodeListResponse200,
        unauthorized: WatchCoreV1NodeListResponse401,
        default_response: WatchCoreV1NodeListResponseDefault,
    };

    pub const watchCoreV1NodeListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Node. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1NodeList(self: *Client, options: watchCoreV1NodeListOptions) !WatchCoreV1NodeListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/nodes", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1NodeResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1NodeResponse = union(enum) {
        ok: WatchCoreV1NodeResponse200,
        unauthorized: WatchCoreV1NodeResponse401,
        default_response: WatchCoreV1NodeResponseDefault,
    };

    pub const watchCoreV1NodeOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Node. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1Node(self: *Client, name: []const u8, options: watchCoreV1NodeOptions) !WatchCoreV1NodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/nodes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1PersistentVolumeClaimListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PersistentVolumeClaimListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PersistentVolumeClaimListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PersistentVolumeClaimListForAllNamespacesResponse = union(enum) {
        ok: WatchCoreV1PersistentVolumeClaimListForAllNamespacesResponse200,
        unauthorized: WatchCoreV1PersistentVolumeClaimListForAllNamespacesResponse401,
        default_response: WatchCoreV1PersistentVolumeClaimListForAllNamespacesResponseDefault,
    };

    pub const watchCoreV1PersistentVolumeClaimListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PersistentVolumeClaim. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1PersistentVolumeClaimListForAllNamespaces(self: *Client, options: watchCoreV1PersistentVolumeClaimListForAllNamespacesOptions) !WatchCoreV1PersistentVolumeClaimListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/persistentvolumeclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1PersistentVolumeListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PersistentVolumeListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PersistentVolumeListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PersistentVolumeListResponse = union(enum) {
        ok: WatchCoreV1PersistentVolumeListResponse200,
        unauthorized: WatchCoreV1PersistentVolumeListResponse401,
        default_response: WatchCoreV1PersistentVolumeListResponseDefault,
    };

    pub const watchCoreV1PersistentVolumeListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PersistentVolume. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1PersistentVolumeList(self: *Client, options: watchCoreV1PersistentVolumeListOptions) !WatchCoreV1PersistentVolumeListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/persistentvolumes", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1PersistentVolumeResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PersistentVolumeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PersistentVolumeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PersistentVolumeResponse = union(enum) {
        ok: WatchCoreV1PersistentVolumeResponse200,
        unauthorized: WatchCoreV1PersistentVolumeResponse401,
        default_response: WatchCoreV1PersistentVolumeResponseDefault,
    };

    pub const watchCoreV1PersistentVolumeOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind PersistentVolume. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoreV1PersistentVolume(self: *Client, name: []const u8, options: watchCoreV1PersistentVolumeOptions) !WatchCoreV1PersistentVolumeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/persistentvolumes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1PodListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PodListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PodListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PodListForAllNamespacesResponse = union(enum) {
        ok: WatchCoreV1PodListForAllNamespacesResponse200,
        unauthorized: WatchCoreV1PodListForAllNamespacesResponse401,
        default_response: WatchCoreV1PodListForAllNamespacesResponseDefault,
    };

    pub const watchCoreV1PodListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Pod. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1PodListForAllNamespaces(self: *Client, options: watchCoreV1PodListForAllNamespacesOptions) !WatchCoreV1PodListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/pods", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1PodTemplateListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PodTemplateListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PodTemplateListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1PodTemplateListForAllNamespacesResponse = union(enum) {
        ok: WatchCoreV1PodTemplateListForAllNamespacesResponse200,
        unauthorized: WatchCoreV1PodTemplateListForAllNamespacesResponse401,
        default_response: WatchCoreV1PodTemplateListForAllNamespacesResponseDefault,
    };

    pub const watchCoreV1PodTemplateListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PodTemplate. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1PodTemplateListForAllNamespaces(self: *Client, options: watchCoreV1PodTemplateListForAllNamespacesOptions) !WatchCoreV1PodTemplateListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/podtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1ReplicationControllerListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ReplicationControllerListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ReplicationControllerListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ReplicationControllerListForAllNamespacesResponse = union(enum) {
        ok: WatchCoreV1ReplicationControllerListForAllNamespacesResponse200,
        unauthorized: WatchCoreV1ReplicationControllerListForAllNamespacesResponse401,
        default_response: WatchCoreV1ReplicationControllerListForAllNamespacesResponseDefault,
    };

    pub const watchCoreV1ReplicationControllerListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ReplicationController. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1ReplicationControllerListForAllNamespaces(self: *Client, options: watchCoreV1ReplicationControllerListForAllNamespacesOptions) !WatchCoreV1ReplicationControllerListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/replicationcontrollers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1ResourceQuotaListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ResourceQuotaListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ResourceQuotaListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ResourceQuotaListForAllNamespacesResponse = union(enum) {
        ok: WatchCoreV1ResourceQuotaListForAllNamespacesResponse200,
        unauthorized: WatchCoreV1ResourceQuotaListForAllNamespacesResponse401,
        default_response: WatchCoreV1ResourceQuotaListForAllNamespacesResponseDefault,
    };

    pub const watchCoreV1ResourceQuotaListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceQuota. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1ResourceQuotaListForAllNamespaces(self: *Client, options: watchCoreV1ResourceQuotaListForAllNamespacesOptions) !WatchCoreV1ResourceQuotaListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/resourcequotas", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1SecretListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1SecretListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1SecretListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1SecretListForAllNamespacesResponse = union(enum) {
        ok: WatchCoreV1SecretListForAllNamespacesResponse200,
        unauthorized: WatchCoreV1SecretListForAllNamespacesResponse401,
        default_response: WatchCoreV1SecretListForAllNamespacesResponseDefault,
    };

    pub const watchCoreV1SecretListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Secret. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1SecretListForAllNamespaces(self: *Client, options: watchCoreV1SecretListForAllNamespacesOptions) !WatchCoreV1SecretListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/secrets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1ServiceAccountListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ServiceAccountListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ServiceAccountListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ServiceAccountListForAllNamespacesResponse = union(enum) {
        ok: WatchCoreV1ServiceAccountListForAllNamespacesResponse200,
        unauthorized: WatchCoreV1ServiceAccountListForAllNamespacesResponse401,
        default_response: WatchCoreV1ServiceAccountListForAllNamespacesResponseDefault,
    };

    pub const watchCoreV1ServiceAccountListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ServiceAccount. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1ServiceAccountListForAllNamespaces(self: *Client, options: watchCoreV1ServiceAccountListForAllNamespacesOptions) !WatchCoreV1ServiceAccountListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/serviceaccounts", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoreV1ServiceListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ServiceListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ServiceListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoreV1ServiceListForAllNamespacesResponse = union(enum) {
        ok: WatchCoreV1ServiceListForAllNamespacesResponse200,
        unauthorized: WatchCoreV1ServiceListForAllNamespacesResponse401,
        default_response: WatchCoreV1ServiceListForAllNamespacesResponseDefault,
    };

    pub const watchCoreV1ServiceListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Service. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoreV1ServiceListForAllNamespaces(self: *Client, options: watchCoreV1ServiceListForAllNamespacesOptions) !WatchCoreV1ServiceListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/api/v1/watch/services", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAPIVersionsResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroupList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAPIVersionsResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAPIVersionsResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAPIVersionsResponse = union(enum) {
        ok: GetAPIVersionsResponse200,
        unauthorized: GetAPIVersionsResponse401,
        default_response: GetAPIVersionsResponseDefault,
    };

    /// get available API versions
    pub fn getAPIVersions(self: *Client) !GetAPIVersionsResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroupList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAdmissionregistrationAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAdmissionregistrationAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAdmissionregistrationAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAdmissionregistrationAPIGroupResponse = union(enum) {
        ok: GetAdmissionregistrationAPIGroupResponse200,
        unauthorized: GetAdmissionregistrationAPIGroupResponse401,
        default_response: GetAdmissionregistrationAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getAdmissionregistrationAPIGroup(self: *Client) !GetAdmissionregistrationAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAdmissionregistrationV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAdmissionregistrationV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAdmissionregistrationV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAdmissionregistrationV1APIResourcesResponse = union(enum) {
        ok: GetAdmissionregistrationV1APIResourcesResponse200,
        unauthorized: GetAdmissionregistrationV1APIResourcesResponse401,
        default_response: GetAdmissionregistrationV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getAdmissionregistrationV1APIResources(self: *Client) !GetAdmissionregistrationV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAdmissionregistrationV1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1MutatingAdmissionPolicyResponse = union(enum) {
        ok: ListAdmissionregistrationV1MutatingAdmissionPolicyResponse200,
        unauthorized: ListAdmissionregistrationV1MutatingAdmissionPolicyResponse401,
        default_response: ListAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault,
    };

    pub const listAdmissionregistrationV1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind MutatingAdmissionPolicy
    pub fn listAdmissionregistrationV1MutatingAdmissionPolicy(self: *Client, options: listAdmissionregistrationV1MutatingAdmissionPolicyOptions) !ListAdmissionregistrationV1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAdmissionregistrationV1MutatingAdmissionPolicy(self: *Client, options: listAdmissionregistrationV1MutatingAdmissionPolicyOptions) !WatchStream(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicies");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAdmissionregistrationV1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingAdmissionPolicyResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingAdmissionPolicyResponse202 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingAdmissionPolicyResponse = union(enum) {
        ok: CreateAdmissionregistrationV1MutatingAdmissionPolicyResponse200,
        created: CreateAdmissionregistrationV1MutatingAdmissionPolicyResponse201,
        accepted: CreateAdmissionregistrationV1MutatingAdmissionPolicyResponse202,
        unauthorized: CreateAdmissionregistrationV1MutatingAdmissionPolicyResponse401,
        default_response: CreateAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault,
    };

    pub const createAdmissionregistrationV1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a MutatingAdmissionPolicy
    pub fn createAdmissionregistrationV1MutatingAdmissionPolicy(self: *Client, body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy, options: createAdmissionregistrationV1MutatingAdmissionPolicyOptions) !CreateAdmissionregistrationV1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyResponse200,
        unauthorized: DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyResponse401,
        default_response: DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyResponseDefault,
    };

    pub const deleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of MutatingAdmissionPolicy
    pub fn deleteAdmissionregistrationV1CollectionMutatingAdmissionPolicy(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyOptions) !DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAdmissionregistrationV1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1MutatingAdmissionPolicyResponse = union(enum) {
        ok: ReadAdmissionregistrationV1MutatingAdmissionPolicyResponse200,
        unauthorized: ReadAdmissionregistrationV1MutatingAdmissionPolicyResponse401,
        default_response: ReadAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault,
    };

    pub const readAdmissionregistrationV1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified MutatingAdmissionPolicy
    pub fn readAdmissionregistrationV1MutatingAdmissionPolicy(self: *Client, name: []const u8, options: readAdmissionregistrationV1MutatingAdmissionPolicyOptions) !ReadAdmissionregistrationV1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAdmissionregistrationV1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1MutatingAdmissionPolicyResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1MutatingAdmissionPolicyResponse = union(enum) {
        ok: ReplaceAdmissionregistrationV1MutatingAdmissionPolicyResponse200,
        created: ReplaceAdmissionregistrationV1MutatingAdmissionPolicyResponse201,
        unauthorized: ReplaceAdmissionregistrationV1MutatingAdmissionPolicyResponse401,
        default_response: ReplaceAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault,
    };

    pub const replaceAdmissionregistrationV1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified MutatingAdmissionPolicy
    pub fn replaceAdmissionregistrationV1MutatingAdmissionPolicy(self: *Client, name: []const u8, body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy, options: replaceAdmissionregistrationV1MutatingAdmissionPolicyOptions) !ReplaceAdmissionregistrationV1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1MutatingAdmissionPolicyResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1MutatingAdmissionPolicyResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1MutatingAdmissionPolicyResponse200,
        accepted: DeleteAdmissionregistrationV1MutatingAdmissionPolicyResponse202,
        unauthorized: DeleteAdmissionregistrationV1MutatingAdmissionPolicyResponse401,
        default_response: DeleteAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault,
    };

    pub const deleteAdmissionregistrationV1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a MutatingAdmissionPolicy
    pub fn deleteAdmissionregistrationV1MutatingAdmissionPolicy(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1MutatingAdmissionPolicyOptions) !DeleteAdmissionregistrationV1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAdmissionregistrationV1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1MutatingAdmissionPolicyResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1MutatingAdmissionPolicyResponse = union(enum) {
        ok: PatchAdmissionregistrationV1MutatingAdmissionPolicyResponse200,
        created: PatchAdmissionregistrationV1MutatingAdmissionPolicyResponse201,
        unauthorized: PatchAdmissionregistrationV1MutatingAdmissionPolicyResponse401,
        default_response: PatchAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault,
    };

    pub const patchAdmissionregistrationV1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified MutatingAdmissionPolicy
    pub fn patchAdmissionregistrationV1MutatingAdmissionPolicy(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAdmissionregistrationV1MutatingAdmissionPolicyOptions) !PatchAdmissionregistrationV1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBindingList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: ListAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200,
        unauthorized: ListAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401,
        default_response: ListAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const listAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind MutatingAdmissionPolicyBinding
    pub fn listAdmissionregistrationV1MutatingAdmissionPolicyBinding(self: *Client, options: listAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions) !ListAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBindingList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAdmissionregistrationV1MutatingAdmissionPolicyBinding(self: *Client, options: listAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions) !WatchStream(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicybindings");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse202 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: CreateAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200,
        created: CreateAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse201,
        accepted: CreateAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse202,
        unauthorized: CreateAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401,
        default_response: CreateAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const createAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a MutatingAdmissionPolicyBinding
    pub fn createAdmissionregistrationV1MutatingAdmissionPolicyBinding(self: *Client, body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding, options: createAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions) !CreateAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyBindingResponse200,
        unauthorized: DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyBindingResponse401,
        default_response: DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const deleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of MutatingAdmissionPolicyBinding
    pub fn deleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyBinding(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyBindingOptions) !DeleteAdmissionregistrationV1CollectionMutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: ReadAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200,
        unauthorized: ReadAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401,
        default_response: ReadAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const readAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified MutatingAdmissionPolicyBinding
    pub fn readAdmissionregistrationV1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, options: readAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions) !ReadAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: ReplaceAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200,
        created: ReplaceAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse201,
        unauthorized: ReplaceAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401,
        default_response: ReplaceAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const replaceAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified MutatingAdmissionPolicyBinding
    pub fn replaceAdmissionregistrationV1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding, options: replaceAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions) !ReplaceAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200,
        accepted: DeleteAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse202,
        unauthorized: DeleteAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401,
        default_response: DeleteAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const deleteAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a MutatingAdmissionPolicyBinding
    pub fn deleteAdmissionregistrationV1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions) !DeleteAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: PatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200,
        created: PatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse201,
        unauthorized: PatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401,
        default_response: PatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const patchAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified MutatingAdmissionPolicyBinding
    pub fn patchAdmissionregistrationV1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions) !PatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAdmissionregistrationV1MutatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfigurationList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1MutatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1MutatingWebhookConfigurationResponse = union(enum) {
        ok: ListAdmissionregistrationV1MutatingWebhookConfigurationResponse200,
        unauthorized: ListAdmissionregistrationV1MutatingWebhookConfigurationResponse401,
        default_response: ListAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault,
    };

    pub const listAdmissionregistrationV1MutatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind MutatingWebhookConfiguration
    pub fn listAdmissionregistrationV1MutatingWebhookConfiguration(self: *Client, options: listAdmissionregistrationV1MutatingWebhookConfigurationOptions) !ListAdmissionregistrationV1MutatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingwebhookconfigurations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfigurationList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAdmissionregistrationV1MutatingWebhookConfiguration(self: *Client, options: listAdmissionregistrationV1MutatingWebhookConfigurationOptions) !WatchStream(root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/admissionregistration.k8s.io/v1/mutatingwebhookconfigurations");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAdmissionregistrationV1MutatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingWebhookConfigurationResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingWebhookConfigurationResponse202 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1MutatingWebhookConfigurationResponse = union(enum) {
        ok: CreateAdmissionregistrationV1MutatingWebhookConfigurationResponse200,
        created: CreateAdmissionregistrationV1MutatingWebhookConfigurationResponse201,
        accepted: CreateAdmissionregistrationV1MutatingWebhookConfigurationResponse202,
        unauthorized: CreateAdmissionregistrationV1MutatingWebhookConfigurationResponse401,
        default_response: CreateAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault,
    };

    pub const createAdmissionregistrationV1MutatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a MutatingWebhookConfiguration
    pub fn createAdmissionregistrationV1MutatingWebhookConfiguration(self: *Client, body: root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration, options: createAdmissionregistrationV1MutatingWebhookConfigurationOptions) !CreateAdmissionregistrationV1MutatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingwebhookconfigurations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1CollectionMutatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionMutatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionMutatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionMutatingWebhookConfigurationResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1CollectionMutatingWebhookConfigurationResponse200,
        unauthorized: DeleteAdmissionregistrationV1CollectionMutatingWebhookConfigurationResponse401,
        default_response: DeleteAdmissionregistrationV1CollectionMutatingWebhookConfigurationResponseDefault,
    };

    pub const deleteAdmissionregistrationV1CollectionMutatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of MutatingWebhookConfiguration
    pub fn deleteAdmissionregistrationV1CollectionMutatingWebhookConfiguration(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1CollectionMutatingWebhookConfigurationOptions) !DeleteAdmissionregistrationV1CollectionMutatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingwebhookconfigurations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAdmissionregistrationV1MutatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1MutatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1MutatingWebhookConfigurationResponse = union(enum) {
        ok: ReadAdmissionregistrationV1MutatingWebhookConfigurationResponse200,
        unauthorized: ReadAdmissionregistrationV1MutatingWebhookConfigurationResponse401,
        default_response: ReadAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault,
    };

    pub const readAdmissionregistrationV1MutatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified MutatingWebhookConfiguration
    pub fn readAdmissionregistrationV1MutatingWebhookConfiguration(self: *Client, name: []const u8, options: readAdmissionregistrationV1MutatingWebhookConfigurationOptions) !ReadAdmissionregistrationV1MutatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingwebhookconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAdmissionregistrationV1MutatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1MutatingWebhookConfigurationResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1MutatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1MutatingWebhookConfigurationResponse = union(enum) {
        ok: ReplaceAdmissionregistrationV1MutatingWebhookConfigurationResponse200,
        created: ReplaceAdmissionregistrationV1MutatingWebhookConfigurationResponse201,
        unauthorized: ReplaceAdmissionregistrationV1MutatingWebhookConfigurationResponse401,
        default_response: ReplaceAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault,
    };

    pub const replaceAdmissionregistrationV1MutatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified MutatingWebhookConfiguration
    pub fn replaceAdmissionregistrationV1MutatingWebhookConfiguration(self: *Client, name: []const u8, body: root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration, options: replaceAdmissionregistrationV1MutatingWebhookConfigurationOptions) !ReplaceAdmissionregistrationV1MutatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingwebhookconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1MutatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1MutatingWebhookConfigurationResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1MutatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1MutatingWebhookConfigurationResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1MutatingWebhookConfigurationResponse200,
        accepted: DeleteAdmissionregistrationV1MutatingWebhookConfigurationResponse202,
        unauthorized: DeleteAdmissionregistrationV1MutatingWebhookConfigurationResponse401,
        default_response: DeleteAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault,
    };

    pub const deleteAdmissionregistrationV1MutatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a MutatingWebhookConfiguration
    pub fn deleteAdmissionregistrationV1MutatingWebhookConfiguration(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1MutatingWebhookConfigurationOptions) !DeleteAdmissionregistrationV1MutatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingwebhookconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAdmissionregistrationV1MutatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1MutatingWebhookConfigurationResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1MutatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1MutatingWebhookConfigurationResponse = union(enum) {
        ok: PatchAdmissionregistrationV1MutatingWebhookConfigurationResponse200,
        created: PatchAdmissionregistrationV1MutatingWebhookConfigurationResponse201,
        unauthorized: PatchAdmissionregistrationV1MutatingWebhookConfigurationResponse401,
        default_response: PatchAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault,
    };

    pub const patchAdmissionregistrationV1MutatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified MutatingWebhookConfiguration
    pub fn patchAdmissionregistrationV1MutatingWebhookConfiguration(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAdmissionregistrationV1MutatingWebhookConfigurationOptions) !PatchAdmissionregistrationV1MutatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/mutatingwebhookconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.MutatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAdmissionregistrationV1ValidatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1ValidatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1ValidatingAdmissionPolicyResponse = union(enum) {
        ok: ListAdmissionregistrationV1ValidatingAdmissionPolicyResponse200,
        unauthorized: ListAdmissionregistrationV1ValidatingAdmissionPolicyResponse401,
        default_response: ListAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault,
    };

    pub const listAdmissionregistrationV1ValidatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ValidatingAdmissionPolicy
    pub fn listAdmissionregistrationV1ValidatingAdmissionPolicy(self: *Client, options: listAdmissionregistrationV1ValidatingAdmissionPolicyOptions) !ListAdmissionregistrationV1ValidatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAdmissionregistrationV1ValidatingAdmissionPolicy(self: *Client, options: listAdmissionregistrationV1ValidatingAdmissionPolicyOptions) !WatchStream(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicies");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAdmissionregistrationV1ValidatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingAdmissionPolicyResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingAdmissionPolicyResponse202 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingAdmissionPolicyResponse = union(enum) {
        ok: CreateAdmissionregistrationV1ValidatingAdmissionPolicyResponse200,
        created: CreateAdmissionregistrationV1ValidatingAdmissionPolicyResponse201,
        accepted: CreateAdmissionregistrationV1ValidatingAdmissionPolicyResponse202,
        unauthorized: CreateAdmissionregistrationV1ValidatingAdmissionPolicyResponse401,
        default_response: CreateAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault,
    };

    pub const createAdmissionregistrationV1ValidatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ValidatingAdmissionPolicy
    pub fn createAdmissionregistrationV1ValidatingAdmissionPolicy(self: *Client, body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, options: createAdmissionregistrationV1ValidatingAdmissionPolicyOptions) !CreateAdmissionregistrationV1ValidatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyResponse200,
        unauthorized: DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyResponse401,
        default_response: DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyResponseDefault,
    };

    pub const deleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ValidatingAdmissionPolicy
    pub fn deleteAdmissionregistrationV1CollectionValidatingAdmissionPolicy(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyOptions) !DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAdmissionregistrationV1ValidatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1ValidatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1ValidatingAdmissionPolicyResponse = union(enum) {
        ok: ReadAdmissionregistrationV1ValidatingAdmissionPolicyResponse200,
        unauthorized: ReadAdmissionregistrationV1ValidatingAdmissionPolicyResponse401,
        default_response: ReadAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault,
    };

    pub const readAdmissionregistrationV1ValidatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ValidatingAdmissionPolicy
    pub fn readAdmissionregistrationV1ValidatingAdmissionPolicy(self: *Client, name: []const u8, options: readAdmissionregistrationV1ValidatingAdmissionPolicyOptions) !ReadAdmissionregistrationV1ValidatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyResponse = union(enum) {
        ok: ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyResponse200,
        created: ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyResponse201,
        unauthorized: ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyResponse401,
        default_response: ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault,
    };

    pub const replaceAdmissionregistrationV1ValidatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ValidatingAdmissionPolicy
    pub fn replaceAdmissionregistrationV1ValidatingAdmissionPolicy(self: *Client, name: []const u8, body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, options: replaceAdmissionregistrationV1ValidatingAdmissionPolicyOptions) !ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1ValidatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1ValidatingAdmissionPolicyResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1ValidatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1ValidatingAdmissionPolicyResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1ValidatingAdmissionPolicyResponse200,
        accepted: DeleteAdmissionregistrationV1ValidatingAdmissionPolicyResponse202,
        unauthorized: DeleteAdmissionregistrationV1ValidatingAdmissionPolicyResponse401,
        default_response: DeleteAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault,
    };

    pub const deleteAdmissionregistrationV1ValidatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ValidatingAdmissionPolicy
    pub fn deleteAdmissionregistrationV1ValidatingAdmissionPolicy(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1ValidatingAdmissionPolicyOptions) !DeleteAdmissionregistrationV1ValidatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse = union(enum) {
        ok: PatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse200,
        created: PatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse201,
        unauthorized: PatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse401,
        default_response: PatchAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault,
    };

    pub const patchAdmissionregistrationV1ValidatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ValidatingAdmissionPolicy
    pub fn patchAdmissionregistrationV1ValidatingAdmissionPolicy(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAdmissionregistrationV1ValidatingAdmissionPolicyOptions) !PatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse = union(enum) {
        ok: ReadAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse200,
        unauthorized: ReadAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse401,
        default_response: ReadAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponseDefault,
    };

    pub const readAdmissionregistrationV1ValidatingAdmissionPolicyStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified ValidatingAdmissionPolicy
    pub fn readAdmissionregistrationV1ValidatingAdmissionPolicyStatus(self: *Client, name: []const u8, options: readAdmissionregistrationV1ValidatingAdmissionPolicyStatusOptions) !ReadAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse = union(enum) {
        ok: ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse200,
        created: ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse201,
        unauthorized: ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse401,
        default_response: ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponseDefault,
    };

    pub const replaceAdmissionregistrationV1ValidatingAdmissionPolicyStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified ValidatingAdmissionPolicy
    pub fn replaceAdmissionregistrationV1ValidatingAdmissionPolicyStatus(self: *Client, name: []const u8, body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, options: replaceAdmissionregistrationV1ValidatingAdmissionPolicyStatusOptions) !ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse = union(enum) {
        ok: PatchAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse200,
        created: PatchAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse201,
        unauthorized: PatchAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse401,
        default_response: PatchAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponseDefault,
    };

    pub const patchAdmissionregistrationV1ValidatingAdmissionPolicyStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified ValidatingAdmissionPolicy
    pub fn patchAdmissionregistrationV1ValidatingAdmissionPolicyStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAdmissionregistrationV1ValidatingAdmissionPolicyStatusOptions) !PatchAdmissionregistrationV1ValidatingAdmissionPolicyStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBindingList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse = union(enum) {
        ok: ListAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200,
        unauthorized: ListAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401,
        default_response: ListAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault,
    };

    pub const listAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ValidatingAdmissionPolicyBinding
    pub fn listAdmissionregistrationV1ValidatingAdmissionPolicyBinding(self: *Client, options: listAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions) !ListAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBindingList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAdmissionregistrationV1ValidatingAdmissionPolicyBinding(self: *Client, options: listAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions) !WatchStream(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicybindings");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse202 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse = union(enum) {
        ok: CreateAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200,
        created: CreateAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse201,
        accepted: CreateAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse202,
        unauthorized: CreateAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401,
        default_response: CreateAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault,
    };

    pub const createAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ValidatingAdmissionPolicyBinding
    pub fn createAdmissionregistrationV1ValidatingAdmissionPolicyBinding(self: *Client, body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding, options: createAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions) !CreateAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyBindingResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyBindingResponse200,
        unauthorized: DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyBindingResponse401,
        default_response: DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyBindingResponseDefault,
    };

    pub const deleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ValidatingAdmissionPolicyBinding
    pub fn deleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyBinding(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyBindingOptions) !DeleteAdmissionregistrationV1CollectionValidatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse = union(enum) {
        ok: ReadAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200,
        unauthorized: ReadAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401,
        default_response: ReadAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault,
    };

    pub const readAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ValidatingAdmissionPolicyBinding
    pub fn readAdmissionregistrationV1ValidatingAdmissionPolicyBinding(self: *Client, name: []const u8, options: readAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions) !ReadAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse = union(enum) {
        ok: ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200,
        created: ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse201,
        unauthorized: ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401,
        default_response: ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault,
    };

    pub const replaceAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ValidatingAdmissionPolicyBinding
    pub fn replaceAdmissionregistrationV1ValidatingAdmissionPolicyBinding(self: *Client, name: []const u8, body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding, options: replaceAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions) !ReplaceAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200,
        accepted: DeleteAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse202,
        unauthorized: DeleteAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401,
        default_response: DeleteAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault,
    };

    pub const deleteAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ValidatingAdmissionPolicyBinding
    pub fn deleteAdmissionregistrationV1ValidatingAdmissionPolicyBinding(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions) !DeleteAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse = union(enum) {
        ok: PatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200,
        created: PatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse201,
        unauthorized: PatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401,
        default_response: PatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault,
    };

    pub const patchAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ValidatingAdmissionPolicyBinding
    pub fn patchAdmissionregistrationV1ValidatingAdmissionPolicyBinding(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions) !PatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAdmissionregistrationV1ValidatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfigurationList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1ValidatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1ValidatingWebhookConfigurationResponse = union(enum) {
        ok: ListAdmissionregistrationV1ValidatingWebhookConfigurationResponse200,
        unauthorized: ListAdmissionregistrationV1ValidatingWebhookConfigurationResponse401,
        default_response: ListAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault,
    };

    pub const listAdmissionregistrationV1ValidatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ValidatingWebhookConfiguration
    pub fn listAdmissionregistrationV1ValidatingWebhookConfiguration(self: *Client, options: listAdmissionregistrationV1ValidatingWebhookConfigurationOptions) !ListAdmissionregistrationV1ValidatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingwebhookconfigurations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfigurationList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAdmissionregistrationV1ValidatingWebhookConfiguration(self: *Client, options: listAdmissionregistrationV1ValidatingWebhookConfigurationOptions) !WatchStream(root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/admissionregistration.k8s.io/v1/validatingwebhookconfigurations");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAdmissionregistrationV1ValidatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingWebhookConfigurationResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingWebhookConfigurationResponse202 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1ValidatingWebhookConfigurationResponse = union(enum) {
        ok: CreateAdmissionregistrationV1ValidatingWebhookConfigurationResponse200,
        created: CreateAdmissionregistrationV1ValidatingWebhookConfigurationResponse201,
        accepted: CreateAdmissionregistrationV1ValidatingWebhookConfigurationResponse202,
        unauthorized: CreateAdmissionregistrationV1ValidatingWebhookConfigurationResponse401,
        default_response: CreateAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault,
    };

    pub const createAdmissionregistrationV1ValidatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ValidatingWebhookConfiguration
    pub fn createAdmissionregistrationV1ValidatingWebhookConfiguration(self: *Client, body: root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration, options: createAdmissionregistrationV1ValidatingWebhookConfigurationOptions) !CreateAdmissionregistrationV1ValidatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingwebhookconfigurations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1CollectionValidatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionValidatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionValidatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1CollectionValidatingWebhookConfigurationResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1CollectionValidatingWebhookConfigurationResponse200,
        unauthorized: DeleteAdmissionregistrationV1CollectionValidatingWebhookConfigurationResponse401,
        default_response: DeleteAdmissionregistrationV1CollectionValidatingWebhookConfigurationResponseDefault,
    };

    pub const deleteAdmissionregistrationV1CollectionValidatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ValidatingWebhookConfiguration
    pub fn deleteAdmissionregistrationV1CollectionValidatingWebhookConfiguration(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1CollectionValidatingWebhookConfigurationOptions) !DeleteAdmissionregistrationV1CollectionValidatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingwebhookconfigurations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAdmissionregistrationV1ValidatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1ValidatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1ValidatingWebhookConfigurationResponse = union(enum) {
        ok: ReadAdmissionregistrationV1ValidatingWebhookConfigurationResponse200,
        unauthorized: ReadAdmissionregistrationV1ValidatingWebhookConfigurationResponse401,
        default_response: ReadAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault,
    };

    pub const readAdmissionregistrationV1ValidatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ValidatingWebhookConfiguration
    pub fn readAdmissionregistrationV1ValidatingWebhookConfiguration(self: *Client, name: []const u8, options: readAdmissionregistrationV1ValidatingWebhookConfigurationOptions) !ReadAdmissionregistrationV1ValidatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingwebhookconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAdmissionregistrationV1ValidatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingWebhookConfigurationResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1ValidatingWebhookConfigurationResponse = union(enum) {
        ok: ReplaceAdmissionregistrationV1ValidatingWebhookConfigurationResponse200,
        created: ReplaceAdmissionregistrationV1ValidatingWebhookConfigurationResponse201,
        unauthorized: ReplaceAdmissionregistrationV1ValidatingWebhookConfigurationResponse401,
        default_response: ReplaceAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault,
    };

    pub const replaceAdmissionregistrationV1ValidatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ValidatingWebhookConfiguration
    pub fn replaceAdmissionregistrationV1ValidatingWebhookConfiguration(self: *Client, name: []const u8, body: root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration, options: replaceAdmissionregistrationV1ValidatingWebhookConfigurationOptions) !ReplaceAdmissionregistrationV1ValidatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingwebhookconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1ValidatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1ValidatingWebhookConfigurationResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1ValidatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1ValidatingWebhookConfigurationResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1ValidatingWebhookConfigurationResponse200,
        accepted: DeleteAdmissionregistrationV1ValidatingWebhookConfigurationResponse202,
        unauthorized: DeleteAdmissionregistrationV1ValidatingWebhookConfigurationResponse401,
        default_response: DeleteAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault,
    };

    pub const deleteAdmissionregistrationV1ValidatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ValidatingWebhookConfiguration
    pub fn deleteAdmissionregistrationV1ValidatingWebhookConfiguration(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1ValidatingWebhookConfigurationOptions) !DeleteAdmissionregistrationV1ValidatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingwebhookconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse = union(enum) {
        ok: PatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse200,
        created: PatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse201,
        unauthorized: PatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse401,
        default_response: PatchAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault,
    };

    pub const patchAdmissionregistrationV1ValidatingWebhookConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ValidatingWebhookConfiguration
    pub fn patchAdmissionregistrationV1ValidatingWebhookConfiguration(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAdmissionregistrationV1ValidatingWebhookConfigurationOptions) !PatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/validatingwebhookconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1.ValidatingWebhookConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyListResponse = union(enum) {
        ok: WatchAdmissionregistrationV1MutatingAdmissionPolicyListResponse200,
        unauthorized: WatchAdmissionregistrationV1MutatingAdmissionPolicyListResponse401,
        default_response: WatchAdmissionregistrationV1MutatingAdmissionPolicyListResponseDefault,
    };

    pub const watchAdmissionregistrationV1MutatingAdmissionPolicyListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of MutatingAdmissionPolicy. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAdmissionregistrationV1MutatingAdmissionPolicyList(self: *Client, options: watchAdmissionregistrationV1MutatingAdmissionPolicyListOptions) !WatchAdmissionregistrationV1MutatingAdmissionPolicyListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/watch/mutatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyResponse = union(enum) {
        ok: WatchAdmissionregistrationV1MutatingAdmissionPolicyResponse200,
        unauthorized: WatchAdmissionregistrationV1MutatingAdmissionPolicyResponse401,
        default_response: WatchAdmissionregistrationV1MutatingAdmissionPolicyResponseDefault,
    };

    pub const watchAdmissionregistrationV1MutatingAdmissionPolicyOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind MutatingAdmissionPolicy. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAdmissionregistrationV1MutatingAdmissionPolicy(self: *Client, name: []const u8, options: watchAdmissionregistrationV1MutatingAdmissionPolicyOptions) !WatchAdmissionregistrationV1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/watch/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingListResponse = union(enum) {
        ok: WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingListResponse200,
        unauthorized: WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingListResponse401,
        default_response: WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingListResponseDefault,
    };

    pub const watchAdmissionregistrationV1MutatingAdmissionPolicyBindingListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of MutatingAdmissionPolicyBinding. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAdmissionregistrationV1MutatingAdmissionPolicyBindingList(self: *Client, options: watchAdmissionregistrationV1MutatingAdmissionPolicyBindingListOptions) !WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/watch/mutatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse200,
        unauthorized: WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse401,
        default_response: WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const watchAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind MutatingAdmissionPolicyBinding. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAdmissionregistrationV1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, options: watchAdmissionregistrationV1MutatingAdmissionPolicyBindingOptions) !WatchAdmissionregistrationV1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/watch/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1MutatingWebhookConfigurationListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingWebhookConfigurationListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingWebhookConfigurationListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingWebhookConfigurationListResponse = union(enum) {
        ok: WatchAdmissionregistrationV1MutatingWebhookConfigurationListResponse200,
        unauthorized: WatchAdmissionregistrationV1MutatingWebhookConfigurationListResponse401,
        default_response: WatchAdmissionregistrationV1MutatingWebhookConfigurationListResponseDefault,
    };

    pub const watchAdmissionregistrationV1MutatingWebhookConfigurationListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of MutatingWebhookConfiguration. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAdmissionregistrationV1MutatingWebhookConfigurationList(self: *Client, options: watchAdmissionregistrationV1MutatingWebhookConfigurationListOptions) !WatchAdmissionregistrationV1MutatingWebhookConfigurationListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/watch/mutatingwebhookconfigurations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1MutatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1MutatingWebhookConfigurationResponse = union(enum) {
        ok: WatchAdmissionregistrationV1MutatingWebhookConfigurationResponse200,
        unauthorized: WatchAdmissionregistrationV1MutatingWebhookConfigurationResponse401,
        default_response: WatchAdmissionregistrationV1MutatingWebhookConfigurationResponseDefault,
    };

    pub const watchAdmissionregistrationV1MutatingWebhookConfigurationOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind MutatingWebhookConfiguration. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAdmissionregistrationV1MutatingWebhookConfiguration(self: *Client, name: []const u8, options: watchAdmissionregistrationV1MutatingWebhookConfigurationOptions) !WatchAdmissionregistrationV1MutatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/watch/mutatingwebhookconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyListResponse = union(enum) {
        ok: WatchAdmissionregistrationV1ValidatingAdmissionPolicyListResponse200,
        unauthorized: WatchAdmissionregistrationV1ValidatingAdmissionPolicyListResponse401,
        default_response: WatchAdmissionregistrationV1ValidatingAdmissionPolicyListResponseDefault,
    };

    pub const watchAdmissionregistrationV1ValidatingAdmissionPolicyListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ValidatingAdmissionPolicy. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAdmissionregistrationV1ValidatingAdmissionPolicyList(self: *Client, options: watchAdmissionregistrationV1ValidatingAdmissionPolicyListOptions) !WatchAdmissionregistrationV1ValidatingAdmissionPolicyListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/watch/validatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse = union(enum) {
        ok: WatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse200,
        unauthorized: WatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse401,
        default_response: WatchAdmissionregistrationV1ValidatingAdmissionPolicyResponseDefault,
    };

    pub const watchAdmissionregistrationV1ValidatingAdmissionPolicyOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ValidatingAdmissionPolicy. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAdmissionregistrationV1ValidatingAdmissionPolicy(self: *Client, name: []const u8, options: watchAdmissionregistrationV1ValidatingAdmissionPolicyOptions) !WatchAdmissionregistrationV1ValidatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/watch/validatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingListResponse = union(enum) {
        ok: WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingListResponse200,
        unauthorized: WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingListResponse401,
        default_response: WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingListResponseDefault,
    };

    pub const watchAdmissionregistrationV1ValidatingAdmissionPolicyBindingListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ValidatingAdmissionPolicyBinding. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAdmissionregistrationV1ValidatingAdmissionPolicyBindingList(self: *Client, options: watchAdmissionregistrationV1ValidatingAdmissionPolicyBindingListOptions) !WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/watch/validatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse = union(enum) {
        ok: WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse200,
        unauthorized: WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse401,
        default_response: WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponseDefault,
    };

    pub const watchAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ValidatingAdmissionPolicyBinding. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAdmissionregistrationV1ValidatingAdmissionPolicyBinding(self: *Client, name: []const u8, options: watchAdmissionregistrationV1ValidatingAdmissionPolicyBindingOptions) !WatchAdmissionregistrationV1ValidatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/watch/validatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1ValidatingWebhookConfigurationListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingWebhookConfigurationListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingWebhookConfigurationListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingWebhookConfigurationListResponse = union(enum) {
        ok: WatchAdmissionregistrationV1ValidatingWebhookConfigurationListResponse200,
        unauthorized: WatchAdmissionregistrationV1ValidatingWebhookConfigurationListResponse401,
        default_response: WatchAdmissionregistrationV1ValidatingWebhookConfigurationListResponseDefault,
    };

    pub const watchAdmissionregistrationV1ValidatingWebhookConfigurationListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ValidatingWebhookConfiguration. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAdmissionregistrationV1ValidatingWebhookConfigurationList(self: *Client, options: watchAdmissionregistrationV1ValidatingWebhookConfigurationListOptions) !WatchAdmissionregistrationV1ValidatingWebhookConfigurationListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/watch/validatingwebhookconfigurations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse = union(enum) {
        ok: WatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse200,
        unauthorized: WatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse401,
        default_response: WatchAdmissionregistrationV1ValidatingWebhookConfigurationResponseDefault,
    };

    pub const watchAdmissionregistrationV1ValidatingWebhookConfigurationOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ValidatingWebhookConfiguration. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAdmissionregistrationV1ValidatingWebhookConfiguration(self: *Client, name: []const u8, options: watchAdmissionregistrationV1ValidatingWebhookConfigurationOptions) !WatchAdmissionregistrationV1ValidatingWebhookConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1/watch/validatingwebhookconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAdmissionregistrationV1alpha1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAdmissionregistrationV1alpha1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAdmissionregistrationV1alpha1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAdmissionregistrationV1alpha1APIResourcesResponse = union(enum) {
        ok: GetAdmissionregistrationV1alpha1APIResourcesResponse200,
        unauthorized: GetAdmissionregistrationV1alpha1APIResourcesResponse401,
        default_response: GetAdmissionregistrationV1alpha1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getAdmissionregistrationV1alpha1APIResources(self: *Client) !GetAdmissionregistrationV1alpha1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse = union(enum) {
        ok: ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200,
        unauthorized: ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401,
        default_response: ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault,
    };

    pub const listAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind MutatingAdmissionPolicy
    pub fn listAdmissionregistrationV1alpha1MutatingAdmissionPolicy(self: *Client, options: listAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions) !ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAdmissionregistrationV1alpha1MutatingAdmissionPolicy(self: *Client, options: listAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions) !WatchStream(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicies");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse202 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse = union(enum) {
        ok: CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200,
        created: CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse201,
        accepted: CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse202,
        unauthorized: CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401,
        default_response: CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault,
    };

    pub const createAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a MutatingAdmissionPolicy
    pub fn createAdmissionregistrationV1alpha1MutatingAdmissionPolicy(self: *Client, body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy, options: createAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions) !CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyResponse200,
        unauthorized: DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyResponse401,
        default_response: DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyResponseDefault,
    };

    pub const deleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of MutatingAdmissionPolicy
    pub fn deleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicy(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyOptions) !DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse = union(enum) {
        ok: ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200,
        unauthorized: ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401,
        default_response: ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault,
    };

    pub const readAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified MutatingAdmissionPolicy
    pub fn readAdmissionregistrationV1alpha1MutatingAdmissionPolicy(self: *Client, name: []const u8, options: readAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions) !ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse = union(enum) {
        ok: ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200,
        created: ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse201,
        unauthorized: ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401,
        default_response: ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault,
    };

    pub const replaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified MutatingAdmissionPolicy
    pub fn replaceAdmissionregistrationV1alpha1MutatingAdmissionPolicy(self: *Client, name: []const u8, body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy, options: replaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions) !ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200,
        accepted: DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse202,
        unauthorized: DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401,
        default_response: DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault,
    };

    pub const deleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a MutatingAdmissionPolicy
    pub fn deleteAdmissionregistrationV1alpha1MutatingAdmissionPolicy(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions) !DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse = union(enum) {
        ok: PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200,
        created: PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse201,
        unauthorized: PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401,
        default_response: PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault,
    };

    pub const patchAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified MutatingAdmissionPolicy
    pub fn patchAdmissionregistrationV1alpha1MutatingAdmissionPolicy(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions) !PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBindingList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200,
        unauthorized: ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401,
        default_response: ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const listAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind MutatingAdmissionPolicyBinding
    pub fn listAdmissionregistrationV1alpha1MutatingAdmissionPolicyBinding(self: *Client, options: listAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions) !ListAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBindingList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAdmissionregistrationV1alpha1MutatingAdmissionPolicyBinding(self: *Client, options: listAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions) !WatchStream(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicybindings");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse202 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200,
        created: CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse201,
        accepted: CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse202,
        unauthorized: CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401,
        default_response: CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const createAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a MutatingAdmissionPolicyBinding
    pub fn createAdmissionregistrationV1alpha1MutatingAdmissionPolicyBinding(self: *Client, body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding, options: createAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions) !CreateAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyBindingResponse200,
        unauthorized: DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyBindingResponse401,
        default_response: DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const deleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of MutatingAdmissionPolicyBinding
    pub fn deleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyBinding(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyBindingOptions) !DeleteAdmissionregistrationV1alpha1CollectionMutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200,
        unauthorized: ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401,
        default_response: ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const readAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified MutatingAdmissionPolicyBinding
    pub fn readAdmissionregistrationV1alpha1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, options: readAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions) !ReadAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200,
        created: ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse201,
        unauthorized: ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401,
        default_response: ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const replaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified MutatingAdmissionPolicyBinding
    pub fn replaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding, options: replaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions) !ReplaceAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200,
        accepted: DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse202,
        unauthorized: DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401,
        default_response: DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const deleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a MutatingAdmissionPolicyBinding
    pub fn deleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions) !DeleteAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200,
        created: PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse201,
        unauthorized: PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401,
        default_response: PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const patchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified MutatingAdmissionPolicyBinding
    pub fn patchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions) !PatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1alpha1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyListResponse = union(enum) {
        ok: WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyListResponse200,
        unauthorized: WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyListResponse401,
        default_response: WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyListResponseDefault,
    };

    pub const watchAdmissionregistrationV1alpha1MutatingAdmissionPolicyListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of MutatingAdmissionPolicy. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAdmissionregistrationV1alpha1MutatingAdmissionPolicyList(self: *Client, options: watchAdmissionregistrationV1alpha1MutatingAdmissionPolicyListOptions) !WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/watch/mutatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse = union(enum) {
        ok: WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse200,
        unauthorized: WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse401,
        default_response: WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponseDefault,
    };

    pub const watchAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind MutatingAdmissionPolicy. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAdmissionregistrationV1alpha1MutatingAdmissionPolicy(self: *Client, name: []const u8, options: watchAdmissionregistrationV1alpha1MutatingAdmissionPolicyOptions) !WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/watch/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingListResponse = union(enum) {
        ok: WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingListResponse200,
        unauthorized: WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingListResponse401,
        default_response: WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingListResponseDefault,
    };

    pub const watchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of MutatingAdmissionPolicyBinding. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingList(self: *Client, options: watchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingListOptions) !WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/watch/mutatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse200,
        unauthorized: WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse401,
        default_response: WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const watchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind MutatingAdmissionPolicyBinding. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, options: watchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingOptions) !WatchAdmissionregistrationV1alpha1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1alpha1/watch/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAdmissionregistrationV1beta1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAdmissionregistrationV1beta1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAdmissionregistrationV1beta1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAdmissionregistrationV1beta1APIResourcesResponse = union(enum) {
        ok: GetAdmissionregistrationV1beta1APIResourcesResponse200,
        unauthorized: GetAdmissionregistrationV1beta1APIResourcesResponse401,
        default_response: GetAdmissionregistrationV1beta1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getAdmissionregistrationV1beta1APIResources(self: *Client) !GetAdmissionregistrationV1beta1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse = union(enum) {
        ok: ListAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200,
        unauthorized: ListAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401,
        default_response: ListAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault,
    };

    pub const listAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind MutatingAdmissionPolicy
    pub fn listAdmissionregistrationV1beta1MutatingAdmissionPolicy(self: *Client, options: listAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions) !ListAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAdmissionregistrationV1beta1MutatingAdmissionPolicy(self: *Client, options: listAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions) !WatchStream(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicies");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse202 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse = union(enum) {
        ok: CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200,
        created: CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse201,
        accepted: CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse202,
        unauthorized: CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401,
        default_response: CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault,
    };

    pub const createAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a MutatingAdmissionPolicy
    pub fn createAdmissionregistrationV1beta1MutatingAdmissionPolicy(self: *Client, body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy, options: createAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions) !CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyResponse200,
        unauthorized: DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyResponse401,
        default_response: DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyResponseDefault,
    };

    pub const deleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of MutatingAdmissionPolicy
    pub fn deleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicy(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyOptions) !DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse = union(enum) {
        ok: ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200,
        unauthorized: ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401,
        default_response: ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault,
    };

    pub const readAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified MutatingAdmissionPolicy
    pub fn readAdmissionregistrationV1beta1MutatingAdmissionPolicy(self: *Client, name: []const u8, options: readAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions) !ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse = union(enum) {
        ok: ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200,
        created: ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse201,
        unauthorized: ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401,
        default_response: ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault,
    };

    pub const replaceAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified MutatingAdmissionPolicy
    pub fn replaceAdmissionregistrationV1beta1MutatingAdmissionPolicy(self: *Client, name: []const u8, body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy, options: replaceAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions) !ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200,
        accepted: DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse202,
        unauthorized: DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401,
        default_response: DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault,
    };

    pub const deleteAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a MutatingAdmissionPolicy
    pub fn deleteAdmissionregistrationV1beta1MutatingAdmissionPolicy(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions) !DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse = union(enum) {
        ok: PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200,
        created: PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse201,
        unauthorized: PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401,
        default_response: PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault,
    };

    pub const patchAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified MutatingAdmissionPolicy
    pub fn patchAdmissionregistrationV1beta1MutatingAdmissionPolicy(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions) !PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBindingList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: ListAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200,
        unauthorized: ListAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401,
        default_response: ListAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const listAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind MutatingAdmissionPolicyBinding
    pub fn listAdmissionregistrationV1beta1MutatingAdmissionPolicyBinding(self: *Client, options: listAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions) !ListAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBindingList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAdmissionregistrationV1beta1MutatingAdmissionPolicyBinding(self: *Client, options: listAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions) !WatchStream(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicybindings");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse202 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200,
        created: CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse201,
        accepted: CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse202,
        unauthorized: CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401,
        default_response: CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const createAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a MutatingAdmissionPolicyBinding
    pub fn createAdmissionregistrationV1beta1MutatingAdmissionPolicyBinding(self: *Client, body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding, options: createAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions) !CreateAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyBindingResponse200,
        unauthorized: DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyBindingResponse401,
        default_response: DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const deleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of MutatingAdmissionPolicyBinding
    pub fn deleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyBinding(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyBindingOptions) !DeleteAdmissionregistrationV1beta1CollectionMutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200,
        unauthorized: ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401,
        default_response: ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const readAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified MutatingAdmissionPolicyBinding
    pub fn readAdmissionregistrationV1beta1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, options: readAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions) !ReadAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200,
        created: ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse201,
        unauthorized: ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401,
        default_response: ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const replaceAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified MutatingAdmissionPolicyBinding
    pub fn replaceAdmissionregistrationV1beta1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding, options: replaceAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions) !ReplaceAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200,
        accepted: DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse202,
        unauthorized: DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401,
        default_response: DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const deleteAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a MutatingAdmissionPolicyBinding
    pub fn deleteAdmissionregistrationV1beta1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions) !DeleteAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse201 = struct {
        body: root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200,
        created: PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse201,
        unauthorized: PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401,
        default_response: PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const patchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified MutatingAdmissionPolicyBinding
    pub fn patchAdmissionregistrationV1beta1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions) !PatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.admissionregistration.v1beta1.MutatingAdmissionPolicyBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyListResponse = union(enum) {
        ok: WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyListResponse200,
        unauthorized: WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyListResponse401,
        default_response: WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyListResponseDefault,
    };

    pub const watchAdmissionregistrationV1beta1MutatingAdmissionPolicyListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of MutatingAdmissionPolicy. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAdmissionregistrationV1beta1MutatingAdmissionPolicyList(self: *Client, options: watchAdmissionregistrationV1beta1MutatingAdmissionPolicyListOptions) !WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/watch/mutatingadmissionpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse = union(enum) {
        ok: WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse200,
        unauthorized: WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse401,
        default_response: WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponseDefault,
    };

    pub const watchAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind MutatingAdmissionPolicy. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAdmissionregistrationV1beta1MutatingAdmissionPolicy(self: *Client, name: []const u8, options: watchAdmissionregistrationV1beta1MutatingAdmissionPolicyOptions) !WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/watch/mutatingadmissionpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingListResponse = union(enum) {
        ok: WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingListResponse200,
        unauthorized: WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingListResponse401,
        default_response: WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingListResponseDefault,
    };

    pub const watchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of MutatingAdmissionPolicyBinding. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingList(self: *Client, options: watchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingListOptions) !WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/watch/mutatingadmissionpolicybindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse = union(enum) {
        ok: WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse200,
        unauthorized: WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse401,
        default_response: WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponseDefault,
    };

    pub const watchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind MutatingAdmissionPolicyBinding. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAdmissionregistrationV1beta1MutatingAdmissionPolicyBinding(self: *Client, name: []const u8, options: watchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingOptions) !WatchAdmissionregistrationV1beta1MutatingAdmissionPolicyBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/admissionregistration.k8s.io/v1beta1/watch/mutatingadmissionpolicybindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetApiextensionsAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetApiextensionsAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetApiextensionsAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetApiextensionsAPIGroupResponse = union(enum) {
        ok: GetApiextensionsAPIGroupResponse200,
        unauthorized: GetApiextensionsAPIGroupResponse401,
        default_response: GetApiextensionsAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getApiextensionsAPIGroup(self: *Client) !GetApiextensionsAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetApiextensionsV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetApiextensionsV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetApiextensionsV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetApiextensionsV1APIResourcesResponse = union(enum) {
        ok: GetApiextensionsV1APIResourcesResponse200,
        unauthorized: GetApiextensionsV1APIResourcesResponse401,
        default_response: GetApiextensionsV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getApiextensionsV1APIResources(self: *Client) !GetApiextensionsV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListApiextensionsV1CustomResourceDefinitionResponse200 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinitionList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListApiextensionsV1CustomResourceDefinitionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListApiextensionsV1CustomResourceDefinitionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListApiextensionsV1CustomResourceDefinitionResponse = union(enum) {
        ok: ListApiextensionsV1CustomResourceDefinitionResponse200,
        unauthorized: ListApiextensionsV1CustomResourceDefinitionResponse401,
        default_response: ListApiextensionsV1CustomResourceDefinitionResponseDefault,
    };

    pub const listApiextensionsV1CustomResourceDefinitionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind CustomResourceDefinition
    pub fn listApiextensionsV1CustomResourceDefinition(self: *Client, options: listApiextensionsV1CustomResourceDefinitionOptions) !ListApiextensionsV1CustomResourceDefinitionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/v1/customresourcedefinitions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinitionList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistApiextensionsV1CustomResourceDefinition(self: *Client, options: listApiextensionsV1CustomResourceDefinitionOptions) !WatchStream(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/apiextensions.k8s.io/v1/customresourcedefinitions");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateApiextensionsV1CustomResourceDefinitionResponse200 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateApiextensionsV1CustomResourceDefinitionResponse201 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateApiextensionsV1CustomResourceDefinitionResponse202 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateApiextensionsV1CustomResourceDefinitionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateApiextensionsV1CustomResourceDefinitionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateApiextensionsV1CustomResourceDefinitionResponse = union(enum) {
        ok: CreateApiextensionsV1CustomResourceDefinitionResponse200,
        created: CreateApiextensionsV1CustomResourceDefinitionResponse201,
        accepted: CreateApiextensionsV1CustomResourceDefinitionResponse202,
        unauthorized: CreateApiextensionsV1CustomResourceDefinitionResponse401,
        default_response: CreateApiextensionsV1CustomResourceDefinitionResponseDefault,
    };

    pub const createApiextensionsV1CustomResourceDefinitionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a CustomResourceDefinition
    pub fn createApiextensionsV1CustomResourceDefinition(self: *Client, body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, options: createApiextensionsV1CustomResourceDefinitionOptions) !CreateApiextensionsV1CustomResourceDefinitionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/v1/customresourcedefinitions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteApiextensionsV1CollectionCustomResourceDefinitionResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiextensionsV1CollectionCustomResourceDefinitionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiextensionsV1CollectionCustomResourceDefinitionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiextensionsV1CollectionCustomResourceDefinitionResponse = union(enum) {
        ok: DeleteApiextensionsV1CollectionCustomResourceDefinitionResponse200,
        unauthorized: DeleteApiextensionsV1CollectionCustomResourceDefinitionResponse401,
        default_response: DeleteApiextensionsV1CollectionCustomResourceDefinitionResponseDefault,
    };

    pub const deleteApiextensionsV1CollectionCustomResourceDefinitionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of CustomResourceDefinition
    pub fn deleteApiextensionsV1CollectionCustomResourceDefinition(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteApiextensionsV1CollectionCustomResourceDefinitionOptions) !DeleteApiextensionsV1CollectionCustomResourceDefinitionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/v1/customresourcedefinitions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadApiextensionsV1CustomResourceDefinitionResponse200 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadApiextensionsV1CustomResourceDefinitionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadApiextensionsV1CustomResourceDefinitionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadApiextensionsV1CustomResourceDefinitionResponse = union(enum) {
        ok: ReadApiextensionsV1CustomResourceDefinitionResponse200,
        unauthorized: ReadApiextensionsV1CustomResourceDefinitionResponse401,
        default_response: ReadApiextensionsV1CustomResourceDefinitionResponseDefault,
    };

    pub const readApiextensionsV1CustomResourceDefinitionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified CustomResourceDefinition
    pub fn readApiextensionsV1CustomResourceDefinition(self: *Client, name: []const u8, options: readApiextensionsV1CustomResourceDefinitionOptions) !ReadApiextensionsV1CustomResourceDefinitionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/v1/customresourcedefinitions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceApiextensionsV1CustomResourceDefinitionResponse200 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiextensionsV1CustomResourceDefinitionResponse201 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiextensionsV1CustomResourceDefinitionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiextensionsV1CustomResourceDefinitionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiextensionsV1CustomResourceDefinitionResponse = union(enum) {
        ok: ReplaceApiextensionsV1CustomResourceDefinitionResponse200,
        created: ReplaceApiextensionsV1CustomResourceDefinitionResponse201,
        unauthorized: ReplaceApiextensionsV1CustomResourceDefinitionResponse401,
        default_response: ReplaceApiextensionsV1CustomResourceDefinitionResponseDefault,
    };

    pub const replaceApiextensionsV1CustomResourceDefinitionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified CustomResourceDefinition
    pub fn replaceApiextensionsV1CustomResourceDefinition(self: *Client, name: []const u8, body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, options: replaceApiextensionsV1CustomResourceDefinitionOptions) !ReplaceApiextensionsV1CustomResourceDefinitionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/v1/customresourcedefinitions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteApiextensionsV1CustomResourceDefinitionResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiextensionsV1CustomResourceDefinitionResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiextensionsV1CustomResourceDefinitionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiextensionsV1CustomResourceDefinitionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiextensionsV1CustomResourceDefinitionResponse = union(enum) {
        ok: DeleteApiextensionsV1CustomResourceDefinitionResponse200,
        accepted: DeleteApiextensionsV1CustomResourceDefinitionResponse202,
        unauthorized: DeleteApiextensionsV1CustomResourceDefinitionResponse401,
        default_response: DeleteApiextensionsV1CustomResourceDefinitionResponseDefault,
    };

    pub const deleteApiextensionsV1CustomResourceDefinitionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a CustomResourceDefinition
    pub fn deleteApiextensionsV1CustomResourceDefinition(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteApiextensionsV1CustomResourceDefinitionOptions) !DeleteApiextensionsV1CustomResourceDefinitionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/v1/customresourcedefinitions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchApiextensionsV1CustomResourceDefinitionResponse200 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiextensionsV1CustomResourceDefinitionResponse201 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiextensionsV1CustomResourceDefinitionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiextensionsV1CustomResourceDefinitionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiextensionsV1CustomResourceDefinitionResponse = union(enum) {
        ok: PatchApiextensionsV1CustomResourceDefinitionResponse200,
        created: PatchApiextensionsV1CustomResourceDefinitionResponse201,
        unauthorized: PatchApiextensionsV1CustomResourceDefinitionResponse401,
        default_response: PatchApiextensionsV1CustomResourceDefinitionResponseDefault,
    };

    pub const patchApiextensionsV1CustomResourceDefinitionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified CustomResourceDefinition
    pub fn patchApiextensionsV1CustomResourceDefinition(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchApiextensionsV1CustomResourceDefinitionOptions) !PatchApiextensionsV1CustomResourceDefinitionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/v1/customresourcedefinitions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadApiextensionsV1CustomResourceDefinitionStatusResponse200 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadApiextensionsV1CustomResourceDefinitionStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadApiextensionsV1CustomResourceDefinitionStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadApiextensionsV1CustomResourceDefinitionStatusResponse = union(enum) {
        ok: ReadApiextensionsV1CustomResourceDefinitionStatusResponse200,
        unauthorized: ReadApiextensionsV1CustomResourceDefinitionStatusResponse401,
        default_response: ReadApiextensionsV1CustomResourceDefinitionStatusResponseDefault,
    };

    pub const readApiextensionsV1CustomResourceDefinitionStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified CustomResourceDefinition
    pub fn readApiextensionsV1CustomResourceDefinitionStatus(self: *Client, name: []const u8, options: readApiextensionsV1CustomResourceDefinitionStatusOptions) !ReadApiextensionsV1CustomResourceDefinitionStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/v1/customresourcedefinitions/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceApiextensionsV1CustomResourceDefinitionStatusResponse200 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiextensionsV1CustomResourceDefinitionStatusResponse201 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiextensionsV1CustomResourceDefinitionStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiextensionsV1CustomResourceDefinitionStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiextensionsV1CustomResourceDefinitionStatusResponse = union(enum) {
        ok: ReplaceApiextensionsV1CustomResourceDefinitionStatusResponse200,
        created: ReplaceApiextensionsV1CustomResourceDefinitionStatusResponse201,
        unauthorized: ReplaceApiextensionsV1CustomResourceDefinitionStatusResponse401,
        default_response: ReplaceApiextensionsV1CustomResourceDefinitionStatusResponseDefault,
    };

    pub const replaceApiextensionsV1CustomResourceDefinitionStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified CustomResourceDefinition
    pub fn replaceApiextensionsV1CustomResourceDefinitionStatus(self: *Client, name: []const u8, body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, options: replaceApiextensionsV1CustomResourceDefinitionStatusOptions) !ReplaceApiextensionsV1CustomResourceDefinitionStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/v1/customresourcedefinitions/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchApiextensionsV1CustomResourceDefinitionStatusResponse200 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiextensionsV1CustomResourceDefinitionStatusResponse201 = struct {
        body: root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiextensionsV1CustomResourceDefinitionStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiextensionsV1CustomResourceDefinitionStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiextensionsV1CustomResourceDefinitionStatusResponse = union(enum) {
        ok: PatchApiextensionsV1CustomResourceDefinitionStatusResponse200,
        created: PatchApiextensionsV1CustomResourceDefinitionStatusResponse201,
        unauthorized: PatchApiextensionsV1CustomResourceDefinitionStatusResponse401,
        default_response: PatchApiextensionsV1CustomResourceDefinitionStatusResponseDefault,
    };

    pub const patchApiextensionsV1CustomResourceDefinitionStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified CustomResourceDefinition
    pub fn patchApiextensionsV1CustomResourceDefinitionStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchApiextensionsV1CustomResourceDefinitionStatusOptions) !PatchApiextensionsV1CustomResourceDefinitionStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/v1/customresourcedefinitions/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apiextensionsApiserver.pkg.apis.apiextensions.v1.CustomResourceDefinition, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchApiextensionsV1CustomResourceDefinitionListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchApiextensionsV1CustomResourceDefinitionListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchApiextensionsV1CustomResourceDefinitionListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchApiextensionsV1CustomResourceDefinitionListResponse = union(enum) {
        ok: WatchApiextensionsV1CustomResourceDefinitionListResponse200,
        unauthorized: WatchApiextensionsV1CustomResourceDefinitionListResponse401,
        default_response: WatchApiextensionsV1CustomResourceDefinitionListResponseDefault,
    };

    pub const watchApiextensionsV1CustomResourceDefinitionListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of CustomResourceDefinition. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchApiextensionsV1CustomResourceDefinitionList(self: *Client, options: watchApiextensionsV1CustomResourceDefinitionListOptions) !WatchApiextensionsV1CustomResourceDefinitionListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/v1/watch/customresourcedefinitions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchApiextensionsV1CustomResourceDefinitionResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchApiextensionsV1CustomResourceDefinitionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchApiextensionsV1CustomResourceDefinitionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchApiextensionsV1CustomResourceDefinitionResponse = union(enum) {
        ok: WatchApiextensionsV1CustomResourceDefinitionResponse200,
        unauthorized: WatchApiextensionsV1CustomResourceDefinitionResponse401,
        default_response: WatchApiextensionsV1CustomResourceDefinitionResponseDefault,
    };

    pub const watchApiextensionsV1CustomResourceDefinitionOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind CustomResourceDefinition. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchApiextensionsV1CustomResourceDefinition(self: *Client, name: []const u8, options: watchApiextensionsV1CustomResourceDefinitionOptions) !WatchApiextensionsV1CustomResourceDefinitionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiextensions.k8s.io/v1/watch/customresourcedefinitions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetApiregistrationAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetApiregistrationAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetApiregistrationAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetApiregistrationAPIGroupResponse = union(enum) {
        ok: GetApiregistrationAPIGroupResponse200,
        unauthorized: GetApiregistrationAPIGroupResponse401,
        default_response: GetApiregistrationAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getApiregistrationAPIGroup(self: *Client) !GetApiregistrationAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetApiregistrationV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetApiregistrationV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetApiregistrationV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetApiregistrationV1APIResourcesResponse = union(enum) {
        ok: GetApiregistrationV1APIResourcesResponse200,
        unauthorized: GetApiregistrationV1APIResourcesResponse401,
        default_response: GetApiregistrationV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getApiregistrationV1APIResources(self: *Client) !GetApiregistrationV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListApiregistrationV1APIServiceResponse200 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIServiceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListApiregistrationV1APIServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListApiregistrationV1APIServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListApiregistrationV1APIServiceResponse = union(enum) {
        ok: ListApiregistrationV1APIServiceResponse200,
        unauthorized: ListApiregistrationV1APIServiceResponse401,
        default_response: ListApiregistrationV1APIServiceResponseDefault,
    };

    pub const listApiregistrationV1APIServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind APIService
    pub fn listApiregistrationV1APIService(self: *Client, options: listApiregistrationV1APIServiceOptions) !ListApiregistrationV1APIServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/v1/apiservices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIServiceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistApiregistrationV1APIService(self: *Client, options: listApiregistrationV1APIServiceOptions) !WatchStream(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/apiregistration.k8s.io/v1/apiservices");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateApiregistrationV1APIServiceResponse200 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateApiregistrationV1APIServiceResponse201 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateApiregistrationV1APIServiceResponse202 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateApiregistrationV1APIServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateApiregistrationV1APIServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateApiregistrationV1APIServiceResponse = union(enum) {
        ok: CreateApiregistrationV1APIServiceResponse200,
        created: CreateApiregistrationV1APIServiceResponse201,
        accepted: CreateApiregistrationV1APIServiceResponse202,
        unauthorized: CreateApiregistrationV1APIServiceResponse401,
        default_response: CreateApiregistrationV1APIServiceResponseDefault,
    };

    pub const createApiregistrationV1APIServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create an APIService
    pub fn createApiregistrationV1APIService(self: *Client, body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, options: createApiregistrationV1APIServiceOptions) !CreateApiregistrationV1APIServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/v1/apiservices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteApiregistrationV1CollectionAPIServiceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiregistrationV1CollectionAPIServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiregistrationV1CollectionAPIServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiregistrationV1CollectionAPIServiceResponse = union(enum) {
        ok: DeleteApiregistrationV1CollectionAPIServiceResponse200,
        unauthorized: DeleteApiregistrationV1CollectionAPIServiceResponse401,
        default_response: DeleteApiregistrationV1CollectionAPIServiceResponseDefault,
    };

    pub const deleteApiregistrationV1CollectionAPIServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of APIService
    pub fn deleteApiregistrationV1CollectionAPIService(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteApiregistrationV1CollectionAPIServiceOptions) !DeleteApiregistrationV1CollectionAPIServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/v1/apiservices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadApiregistrationV1APIServiceResponse200 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadApiregistrationV1APIServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadApiregistrationV1APIServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadApiregistrationV1APIServiceResponse = union(enum) {
        ok: ReadApiregistrationV1APIServiceResponse200,
        unauthorized: ReadApiregistrationV1APIServiceResponse401,
        default_response: ReadApiregistrationV1APIServiceResponseDefault,
    };

    pub const readApiregistrationV1APIServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified APIService
    pub fn readApiregistrationV1APIService(self: *Client, name: []const u8, options: readApiregistrationV1APIServiceOptions) !ReadApiregistrationV1APIServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/v1/apiservices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceApiregistrationV1APIServiceResponse200 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiregistrationV1APIServiceResponse201 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiregistrationV1APIServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiregistrationV1APIServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiregistrationV1APIServiceResponse = union(enum) {
        ok: ReplaceApiregistrationV1APIServiceResponse200,
        created: ReplaceApiregistrationV1APIServiceResponse201,
        unauthorized: ReplaceApiregistrationV1APIServiceResponse401,
        default_response: ReplaceApiregistrationV1APIServiceResponseDefault,
    };

    pub const replaceApiregistrationV1APIServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified APIService
    pub fn replaceApiregistrationV1APIService(self: *Client, name: []const u8, body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, options: replaceApiregistrationV1APIServiceOptions) !ReplaceApiregistrationV1APIServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/v1/apiservices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteApiregistrationV1APIServiceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiregistrationV1APIServiceResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiregistrationV1APIServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiregistrationV1APIServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteApiregistrationV1APIServiceResponse = union(enum) {
        ok: DeleteApiregistrationV1APIServiceResponse200,
        accepted: DeleteApiregistrationV1APIServiceResponse202,
        unauthorized: DeleteApiregistrationV1APIServiceResponse401,
        default_response: DeleteApiregistrationV1APIServiceResponseDefault,
    };

    pub const deleteApiregistrationV1APIServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete an APIService
    pub fn deleteApiregistrationV1APIService(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteApiregistrationV1APIServiceOptions) !DeleteApiregistrationV1APIServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/v1/apiservices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchApiregistrationV1APIServiceResponse200 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiregistrationV1APIServiceResponse201 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiregistrationV1APIServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiregistrationV1APIServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiregistrationV1APIServiceResponse = union(enum) {
        ok: PatchApiregistrationV1APIServiceResponse200,
        created: PatchApiregistrationV1APIServiceResponse201,
        unauthorized: PatchApiregistrationV1APIServiceResponse401,
        default_response: PatchApiregistrationV1APIServiceResponseDefault,
    };

    pub const patchApiregistrationV1APIServiceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified APIService
    pub fn patchApiregistrationV1APIService(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchApiregistrationV1APIServiceOptions) !PatchApiregistrationV1APIServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/v1/apiservices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadApiregistrationV1APIServiceStatusResponse200 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadApiregistrationV1APIServiceStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadApiregistrationV1APIServiceStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadApiregistrationV1APIServiceStatusResponse = union(enum) {
        ok: ReadApiregistrationV1APIServiceStatusResponse200,
        unauthorized: ReadApiregistrationV1APIServiceStatusResponse401,
        default_response: ReadApiregistrationV1APIServiceStatusResponseDefault,
    };

    pub const readApiregistrationV1APIServiceStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified APIService
    pub fn readApiregistrationV1APIServiceStatus(self: *Client, name: []const u8, options: readApiregistrationV1APIServiceStatusOptions) !ReadApiregistrationV1APIServiceStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/v1/apiservices/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceApiregistrationV1APIServiceStatusResponse200 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiregistrationV1APIServiceStatusResponse201 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiregistrationV1APIServiceStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiregistrationV1APIServiceStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceApiregistrationV1APIServiceStatusResponse = union(enum) {
        ok: ReplaceApiregistrationV1APIServiceStatusResponse200,
        created: ReplaceApiregistrationV1APIServiceStatusResponse201,
        unauthorized: ReplaceApiregistrationV1APIServiceStatusResponse401,
        default_response: ReplaceApiregistrationV1APIServiceStatusResponseDefault,
    };

    pub const replaceApiregistrationV1APIServiceStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified APIService
    pub fn replaceApiregistrationV1APIServiceStatus(self: *Client, name: []const u8, body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, options: replaceApiregistrationV1APIServiceStatusOptions) !ReplaceApiregistrationV1APIServiceStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/v1/apiservices/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchApiregistrationV1APIServiceStatusResponse200 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiregistrationV1APIServiceStatusResponse201 = struct {
        body: root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiregistrationV1APIServiceStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiregistrationV1APIServiceStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchApiregistrationV1APIServiceStatusResponse = union(enum) {
        ok: PatchApiregistrationV1APIServiceStatusResponse200,
        created: PatchApiregistrationV1APIServiceStatusResponse201,
        unauthorized: PatchApiregistrationV1APIServiceStatusResponse401,
        default_response: PatchApiregistrationV1APIServiceStatusResponseDefault,
    };

    pub const patchApiregistrationV1APIServiceStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified APIService
    pub fn patchApiregistrationV1APIServiceStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchApiregistrationV1APIServiceStatusOptions) !PatchApiregistrationV1APIServiceStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/v1/apiservices/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.kubeAggregator.pkg.apis.apiregistration.v1.APIService, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchApiregistrationV1APIServiceListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchApiregistrationV1APIServiceListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchApiregistrationV1APIServiceListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchApiregistrationV1APIServiceListResponse = union(enum) {
        ok: WatchApiregistrationV1APIServiceListResponse200,
        unauthorized: WatchApiregistrationV1APIServiceListResponse401,
        default_response: WatchApiregistrationV1APIServiceListResponseDefault,
    };

    pub const watchApiregistrationV1APIServiceListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of APIService. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchApiregistrationV1APIServiceList(self: *Client, options: watchApiregistrationV1APIServiceListOptions) !WatchApiregistrationV1APIServiceListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/v1/watch/apiservices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchApiregistrationV1APIServiceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchApiregistrationV1APIServiceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchApiregistrationV1APIServiceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchApiregistrationV1APIServiceResponse = union(enum) {
        ok: WatchApiregistrationV1APIServiceResponse200,
        unauthorized: WatchApiregistrationV1APIServiceResponse401,
        default_response: WatchApiregistrationV1APIServiceResponseDefault,
    };

    pub const watchApiregistrationV1APIServiceOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind APIService. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchApiregistrationV1APIService(self: *Client, name: []const u8, options: watchApiregistrationV1APIServiceOptions) !WatchApiregistrationV1APIServiceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apiregistration.k8s.io/v1/watch/apiservices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAppsAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAppsAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAppsAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAppsAPIGroupResponse = union(enum) {
        ok: GetAppsAPIGroupResponse200,
        unauthorized: GetAppsAPIGroupResponse401,
        default_response: GetAppsAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getAppsAPIGroup(self: *Client) !GetAppsAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAppsV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAppsV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAppsV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAppsV1APIResourcesResponse = union(enum) {
        ok: GetAppsV1APIResourcesResponse200,
        unauthorized: GetAppsV1APIResourcesResponse401,
        default_response: GetAppsV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getAppsV1APIResources(self: *Client) !GetAppsV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAppsV1ControllerRevisionForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ControllerRevisionList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1ControllerRevisionForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1ControllerRevisionForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1ControllerRevisionForAllNamespacesResponse = union(enum) {
        ok: ListAppsV1ControllerRevisionForAllNamespacesResponse200,
        unauthorized: ListAppsV1ControllerRevisionForAllNamespacesResponse401,
        default_response: ListAppsV1ControllerRevisionForAllNamespacesResponseDefault,
    };

    pub const listAppsV1ControllerRevisionForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ControllerRevision
    pub fn listAppsV1ControllerRevisionForAllNamespaces(self: *Client, options: listAppsV1ControllerRevisionForAllNamespacesOptions) !ListAppsV1ControllerRevisionForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/controllerrevisions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ControllerRevisionList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAppsV1ControllerRevisionForAllNamespaces(self: *Client, options: listAppsV1ControllerRevisionForAllNamespacesOptions) !WatchStream(root.io.k8s.api.apps.v1.ControllerRevision) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/apps/v1/controllerrevisions");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.apps.v1.ControllerRevision){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListAppsV1DaemonSetForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSetList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1DaemonSetForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1DaemonSetForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1DaemonSetForAllNamespacesResponse = union(enum) {
        ok: ListAppsV1DaemonSetForAllNamespacesResponse200,
        unauthorized: ListAppsV1DaemonSetForAllNamespacesResponse401,
        default_response: ListAppsV1DaemonSetForAllNamespacesResponseDefault,
    };

    pub const listAppsV1DaemonSetForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind DaemonSet
    pub fn listAppsV1DaemonSetForAllNamespaces(self: *Client, options: listAppsV1DaemonSetForAllNamespacesOptions) !ListAppsV1DaemonSetForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/daemonsets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSetList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAppsV1DaemonSetForAllNamespaces(self: *Client, options: listAppsV1DaemonSetForAllNamespacesOptions) !WatchStream(root.io.k8s.api.apps.v1.DaemonSet) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/apps/v1/daemonsets");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.apps.v1.DaemonSet){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListAppsV1DeploymentForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.apps.v1.DeploymentList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1DeploymentForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1DeploymentForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1DeploymentForAllNamespacesResponse = union(enum) {
        ok: ListAppsV1DeploymentForAllNamespacesResponse200,
        unauthorized: ListAppsV1DeploymentForAllNamespacesResponse401,
        default_response: ListAppsV1DeploymentForAllNamespacesResponseDefault,
    };

    pub const listAppsV1DeploymentForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Deployment
    pub fn listAppsV1DeploymentForAllNamespaces(self: *Client, options: listAppsV1DeploymentForAllNamespacesOptions) !ListAppsV1DeploymentForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/deployments", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DeploymentList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAppsV1DeploymentForAllNamespaces(self: *Client, options: listAppsV1DeploymentForAllNamespacesOptions) !WatchStream(root.io.k8s.api.apps.v1.Deployment) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/apps/v1/deployments");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.apps.v1.Deployment){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListAppsV1NamespacedControllerRevisionResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ControllerRevisionList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedControllerRevisionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedControllerRevisionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedControllerRevisionResponse = union(enum) {
        ok: ListAppsV1NamespacedControllerRevisionResponse200,
        unauthorized: ListAppsV1NamespacedControllerRevisionResponse401,
        default_response: ListAppsV1NamespacedControllerRevisionResponseDefault,
    };

    pub const listAppsV1NamespacedControllerRevisionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ControllerRevision
    pub fn listAppsV1NamespacedControllerRevision(self: *Client, namespace: []const u8, options: listAppsV1NamespacedControllerRevisionOptions) !ListAppsV1NamespacedControllerRevisionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/controllerrevisions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ControllerRevisionList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAppsV1NamespacedControllerRevision(self: *Client, namespace: []const u8, options: listAppsV1NamespacedControllerRevisionOptions) !WatchStream(root.io.k8s.api.apps.v1.ControllerRevision) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/apps/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/controllerrevisions");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.apps.v1.ControllerRevision){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAppsV1NamespacedControllerRevisionResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ControllerRevision,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedControllerRevisionResponse201 = struct {
        body: root.io.k8s.api.apps.v1.ControllerRevision,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedControllerRevisionResponse202 = struct {
        body: root.io.k8s.api.apps.v1.ControllerRevision,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedControllerRevisionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedControllerRevisionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedControllerRevisionResponse = union(enum) {
        ok: CreateAppsV1NamespacedControllerRevisionResponse200,
        created: CreateAppsV1NamespacedControllerRevisionResponse201,
        accepted: CreateAppsV1NamespacedControllerRevisionResponse202,
        unauthorized: CreateAppsV1NamespacedControllerRevisionResponse401,
        default_response: CreateAppsV1NamespacedControllerRevisionResponseDefault,
    };

    pub const createAppsV1NamespacedControllerRevisionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ControllerRevision
    pub fn createAppsV1NamespacedControllerRevision(self: *Client, namespace: []const u8, body: root.io.k8s.api.apps.v1.ControllerRevision, options: createAppsV1NamespacedControllerRevisionOptions) !CreateAppsV1NamespacedControllerRevisionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/controllerrevisions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ControllerRevision, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ControllerRevision, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ControllerRevision, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAppsV1CollectionNamespacedControllerRevisionResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedControllerRevisionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedControllerRevisionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedControllerRevisionResponse = union(enum) {
        ok: DeleteAppsV1CollectionNamespacedControllerRevisionResponse200,
        unauthorized: DeleteAppsV1CollectionNamespacedControllerRevisionResponse401,
        default_response: DeleteAppsV1CollectionNamespacedControllerRevisionResponseDefault,
    };

    pub const deleteAppsV1CollectionNamespacedControllerRevisionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ControllerRevision
    pub fn deleteAppsV1CollectionNamespacedControllerRevision(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAppsV1CollectionNamespacedControllerRevisionOptions) !DeleteAppsV1CollectionNamespacedControllerRevisionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/controllerrevisions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAppsV1NamespacedControllerRevisionResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ControllerRevision,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedControllerRevisionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedControllerRevisionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedControllerRevisionResponse = union(enum) {
        ok: ReadAppsV1NamespacedControllerRevisionResponse200,
        unauthorized: ReadAppsV1NamespacedControllerRevisionResponse401,
        default_response: ReadAppsV1NamespacedControllerRevisionResponseDefault,
    };

    pub const readAppsV1NamespacedControllerRevisionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ControllerRevision
    pub fn readAppsV1NamespacedControllerRevision(self: *Client, name: []const u8, namespace: []const u8, options: readAppsV1NamespacedControllerRevisionOptions) !ReadAppsV1NamespacedControllerRevisionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/controllerrevisions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ControllerRevision, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAppsV1NamespacedControllerRevisionResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ControllerRevision,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedControllerRevisionResponse201 = struct {
        body: root.io.k8s.api.apps.v1.ControllerRevision,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedControllerRevisionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedControllerRevisionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedControllerRevisionResponse = union(enum) {
        ok: ReplaceAppsV1NamespacedControllerRevisionResponse200,
        created: ReplaceAppsV1NamespacedControllerRevisionResponse201,
        unauthorized: ReplaceAppsV1NamespacedControllerRevisionResponse401,
        default_response: ReplaceAppsV1NamespacedControllerRevisionResponseDefault,
    };

    pub const replaceAppsV1NamespacedControllerRevisionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ControllerRevision
    pub fn replaceAppsV1NamespacedControllerRevision(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.apps.v1.ControllerRevision, options: replaceAppsV1NamespacedControllerRevisionOptions) !ReplaceAppsV1NamespacedControllerRevisionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/controllerrevisions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ControllerRevision, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ControllerRevision, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAppsV1NamespacedControllerRevisionResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedControllerRevisionResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedControllerRevisionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedControllerRevisionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedControllerRevisionResponse = union(enum) {
        ok: DeleteAppsV1NamespacedControllerRevisionResponse200,
        accepted: DeleteAppsV1NamespacedControllerRevisionResponse202,
        unauthorized: DeleteAppsV1NamespacedControllerRevisionResponse401,
        default_response: DeleteAppsV1NamespacedControllerRevisionResponseDefault,
    };

    pub const deleteAppsV1NamespacedControllerRevisionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ControllerRevision
    pub fn deleteAppsV1NamespacedControllerRevision(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAppsV1NamespacedControllerRevisionOptions) !DeleteAppsV1NamespacedControllerRevisionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/controllerrevisions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAppsV1NamespacedControllerRevisionResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ControllerRevision,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedControllerRevisionResponse201 = struct {
        body: root.io.k8s.api.apps.v1.ControllerRevision,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedControllerRevisionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedControllerRevisionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedControllerRevisionResponse = union(enum) {
        ok: PatchAppsV1NamespacedControllerRevisionResponse200,
        created: PatchAppsV1NamespacedControllerRevisionResponse201,
        unauthorized: PatchAppsV1NamespacedControllerRevisionResponse401,
        default_response: PatchAppsV1NamespacedControllerRevisionResponseDefault,
    };

    pub const patchAppsV1NamespacedControllerRevisionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ControllerRevision
    pub fn patchAppsV1NamespacedControllerRevision(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAppsV1NamespacedControllerRevisionOptions) !PatchAppsV1NamespacedControllerRevisionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/controllerrevisions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ControllerRevision, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ControllerRevision, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAppsV1NamespacedDaemonSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSetList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedDaemonSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedDaemonSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedDaemonSetResponse = union(enum) {
        ok: ListAppsV1NamespacedDaemonSetResponse200,
        unauthorized: ListAppsV1NamespacedDaemonSetResponse401,
        default_response: ListAppsV1NamespacedDaemonSetResponseDefault,
    };

    pub const listAppsV1NamespacedDaemonSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind DaemonSet
    pub fn listAppsV1NamespacedDaemonSet(self: *Client, namespace: []const u8, options: listAppsV1NamespacedDaemonSetOptions) !ListAppsV1NamespacedDaemonSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/daemonsets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSetList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAppsV1NamespacedDaemonSet(self: *Client, namespace: []const u8, options: listAppsV1NamespacedDaemonSetOptions) !WatchStream(root.io.k8s.api.apps.v1.DaemonSet) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/apps/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/daemonsets");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.apps.v1.DaemonSet){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAppsV1NamespacedDaemonSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedDaemonSetResponse201 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedDaemonSetResponse202 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedDaemonSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedDaemonSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedDaemonSetResponse = union(enum) {
        ok: CreateAppsV1NamespacedDaemonSetResponse200,
        created: CreateAppsV1NamespacedDaemonSetResponse201,
        accepted: CreateAppsV1NamespacedDaemonSetResponse202,
        unauthorized: CreateAppsV1NamespacedDaemonSetResponse401,
        default_response: CreateAppsV1NamespacedDaemonSetResponseDefault,
    };

    pub const createAppsV1NamespacedDaemonSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a DaemonSet
    pub fn createAppsV1NamespacedDaemonSet(self: *Client, namespace: []const u8, body: root.io.k8s.api.apps.v1.DaemonSet, options: createAppsV1NamespacedDaemonSetOptions) !CreateAppsV1NamespacedDaemonSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/daemonsets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAppsV1CollectionNamespacedDaemonSetResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedDaemonSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedDaemonSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedDaemonSetResponse = union(enum) {
        ok: DeleteAppsV1CollectionNamespacedDaemonSetResponse200,
        unauthorized: DeleteAppsV1CollectionNamespacedDaemonSetResponse401,
        default_response: DeleteAppsV1CollectionNamespacedDaemonSetResponseDefault,
    };

    pub const deleteAppsV1CollectionNamespacedDaemonSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of DaemonSet
    pub fn deleteAppsV1CollectionNamespacedDaemonSet(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAppsV1CollectionNamespacedDaemonSetOptions) !DeleteAppsV1CollectionNamespacedDaemonSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/daemonsets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAppsV1NamespacedDaemonSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDaemonSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDaemonSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDaemonSetResponse = union(enum) {
        ok: ReadAppsV1NamespacedDaemonSetResponse200,
        unauthorized: ReadAppsV1NamespacedDaemonSetResponse401,
        default_response: ReadAppsV1NamespacedDaemonSetResponseDefault,
    };

    pub const readAppsV1NamespacedDaemonSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified DaemonSet
    pub fn readAppsV1NamespacedDaemonSet(self: *Client, name: []const u8, namespace: []const u8, options: readAppsV1NamespacedDaemonSetOptions) !ReadAppsV1NamespacedDaemonSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/daemonsets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAppsV1NamespacedDaemonSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDaemonSetResponse201 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDaemonSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDaemonSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDaemonSetResponse = union(enum) {
        ok: ReplaceAppsV1NamespacedDaemonSetResponse200,
        created: ReplaceAppsV1NamespacedDaemonSetResponse201,
        unauthorized: ReplaceAppsV1NamespacedDaemonSetResponse401,
        default_response: ReplaceAppsV1NamespacedDaemonSetResponseDefault,
    };

    pub const replaceAppsV1NamespacedDaemonSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified DaemonSet
    pub fn replaceAppsV1NamespacedDaemonSet(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.apps.v1.DaemonSet, options: replaceAppsV1NamespacedDaemonSetOptions) !ReplaceAppsV1NamespacedDaemonSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/daemonsets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAppsV1NamespacedDaemonSetResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedDaemonSetResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedDaemonSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedDaemonSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedDaemonSetResponse = union(enum) {
        ok: DeleteAppsV1NamespacedDaemonSetResponse200,
        accepted: DeleteAppsV1NamespacedDaemonSetResponse202,
        unauthorized: DeleteAppsV1NamespacedDaemonSetResponse401,
        default_response: DeleteAppsV1NamespacedDaemonSetResponseDefault,
    };

    pub const deleteAppsV1NamespacedDaemonSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a DaemonSet
    pub fn deleteAppsV1NamespacedDaemonSet(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAppsV1NamespacedDaemonSetOptions) !DeleteAppsV1NamespacedDaemonSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/daemonsets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAppsV1NamespacedDaemonSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDaemonSetResponse201 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDaemonSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDaemonSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDaemonSetResponse = union(enum) {
        ok: PatchAppsV1NamespacedDaemonSetResponse200,
        created: PatchAppsV1NamespacedDaemonSetResponse201,
        unauthorized: PatchAppsV1NamespacedDaemonSetResponse401,
        default_response: PatchAppsV1NamespacedDaemonSetResponseDefault,
    };

    pub const patchAppsV1NamespacedDaemonSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified DaemonSet
    pub fn patchAppsV1NamespacedDaemonSet(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAppsV1NamespacedDaemonSetOptions) !PatchAppsV1NamespacedDaemonSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/daemonsets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAppsV1NamespacedDaemonSetStatusResponse200 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDaemonSetStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDaemonSetStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDaemonSetStatusResponse = union(enum) {
        ok: ReadAppsV1NamespacedDaemonSetStatusResponse200,
        unauthorized: ReadAppsV1NamespacedDaemonSetStatusResponse401,
        default_response: ReadAppsV1NamespacedDaemonSetStatusResponseDefault,
    };

    pub const readAppsV1NamespacedDaemonSetStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified DaemonSet
    pub fn readAppsV1NamespacedDaemonSetStatus(self: *Client, name: []const u8, namespace: []const u8, options: readAppsV1NamespacedDaemonSetStatusOptions) !ReadAppsV1NamespacedDaemonSetStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/daemonsets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAppsV1NamespacedDaemonSetStatusResponse200 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDaemonSetStatusResponse201 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDaemonSetStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDaemonSetStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDaemonSetStatusResponse = union(enum) {
        ok: ReplaceAppsV1NamespacedDaemonSetStatusResponse200,
        created: ReplaceAppsV1NamespacedDaemonSetStatusResponse201,
        unauthorized: ReplaceAppsV1NamespacedDaemonSetStatusResponse401,
        default_response: ReplaceAppsV1NamespacedDaemonSetStatusResponseDefault,
    };

    pub const replaceAppsV1NamespacedDaemonSetStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified DaemonSet
    pub fn replaceAppsV1NamespacedDaemonSetStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.apps.v1.DaemonSet, options: replaceAppsV1NamespacedDaemonSetStatusOptions) !ReplaceAppsV1NamespacedDaemonSetStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/daemonsets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAppsV1NamespacedDaemonSetStatusResponse200 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDaemonSetStatusResponse201 = struct {
        body: root.io.k8s.api.apps.v1.DaemonSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDaemonSetStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDaemonSetStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDaemonSetStatusResponse = union(enum) {
        ok: PatchAppsV1NamespacedDaemonSetStatusResponse200,
        created: PatchAppsV1NamespacedDaemonSetStatusResponse201,
        unauthorized: PatchAppsV1NamespacedDaemonSetStatusResponse401,
        default_response: PatchAppsV1NamespacedDaemonSetStatusResponseDefault,
    };

    pub const patchAppsV1NamespacedDaemonSetStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified DaemonSet
    pub fn patchAppsV1NamespacedDaemonSetStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAppsV1NamespacedDaemonSetStatusOptions) !PatchAppsV1NamespacedDaemonSetStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/daemonsets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DaemonSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAppsV1NamespacedDeploymentResponse200 = struct {
        body: root.io.k8s.api.apps.v1.DeploymentList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedDeploymentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedDeploymentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedDeploymentResponse = union(enum) {
        ok: ListAppsV1NamespacedDeploymentResponse200,
        unauthorized: ListAppsV1NamespacedDeploymentResponse401,
        default_response: ListAppsV1NamespacedDeploymentResponseDefault,
    };

    pub const listAppsV1NamespacedDeploymentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Deployment
    pub fn listAppsV1NamespacedDeployment(self: *Client, namespace: []const u8, options: listAppsV1NamespacedDeploymentOptions) !ListAppsV1NamespacedDeploymentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.DeploymentList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAppsV1NamespacedDeployment(self: *Client, namespace: []const u8, options: listAppsV1NamespacedDeploymentOptions) !WatchStream(root.io.k8s.api.apps.v1.Deployment) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/apps/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/deployments");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.apps.v1.Deployment){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAppsV1NamespacedDeploymentResponse200 = struct {
        body: root.io.k8s.api.apps.v1.Deployment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedDeploymentResponse201 = struct {
        body: root.io.k8s.api.apps.v1.Deployment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedDeploymentResponse202 = struct {
        body: root.io.k8s.api.apps.v1.Deployment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedDeploymentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedDeploymentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedDeploymentResponse = union(enum) {
        ok: CreateAppsV1NamespacedDeploymentResponse200,
        created: CreateAppsV1NamespacedDeploymentResponse201,
        accepted: CreateAppsV1NamespacedDeploymentResponse202,
        unauthorized: CreateAppsV1NamespacedDeploymentResponse401,
        default_response: CreateAppsV1NamespacedDeploymentResponseDefault,
    };

    pub const createAppsV1NamespacedDeploymentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a Deployment
    pub fn createAppsV1NamespacedDeployment(self: *Client, namespace: []const u8, body: root.io.k8s.api.apps.v1.Deployment, options: createAppsV1NamespacedDeploymentOptions) !CreateAppsV1NamespacedDeploymentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.Deployment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.Deployment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.Deployment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAppsV1CollectionNamespacedDeploymentResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedDeploymentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedDeploymentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedDeploymentResponse = union(enum) {
        ok: DeleteAppsV1CollectionNamespacedDeploymentResponse200,
        unauthorized: DeleteAppsV1CollectionNamespacedDeploymentResponse401,
        default_response: DeleteAppsV1CollectionNamespacedDeploymentResponseDefault,
    };

    pub const deleteAppsV1CollectionNamespacedDeploymentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Deployment
    pub fn deleteAppsV1CollectionNamespacedDeployment(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAppsV1CollectionNamespacedDeploymentOptions) !DeleteAppsV1CollectionNamespacedDeploymentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAppsV1NamespacedDeploymentResponse200 = struct {
        body: root.io.k8s.api.apps.v1.Deployment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDeploymentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDeploymentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDeploymentResponse = union(enum) {
        ok: ReadAppsV1NamespacedDeploymentResponse200,
        unauthorized: ReadAppsV1NamespacedDeploymentResponse401,
        default_response: ReadAppsV1NamespacedDeploymentResponseDefault,
    };

    pub const readAppsV1NamespacedDeploymentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Deployment
    pub fn readAppsV1NamespacedDeployment(self: *Client, name: []const u8, namespace: []const u8, options: readAppsV1NamespacedDeploymentOptions) !ReadAppsV1NamespacedDeploymentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.Deployment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAppsV1NamespacedDeploymentResponse200 = struct {
        body: root.io.k8s.api.apps.v1.Deployment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDeploymentResponse201 = struct {
        body: root.io.k8s.api.apps.v1.Deployment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDeploymentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDeploymentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDeploymentResponse = union(enum) {
        ok: ReplaceAppsV1NamespacedDeploymentResponse200,
        created: ReplaceAppsV1NamespacedDeploymentResponse201,
        unauthorized: ReplaceAppsV1NamespacedDeploymentResponse401,
        default_response: ReplaceAppsV1NamespacedDeploymentResponseDefault,
    };

    pub const replaceAppsV1NamespacedDeploymentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Deployment
    pub fn replaceAppsV1NamespacedDeployment(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.apps.v1.Deployment, options: replaceAppsV1NamespacedDeploymentOptions) !ReplaceAppsV1NamespacedDeploymentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.Deployment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.Deployment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAppsV1NamespacedDeploymentResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedDeploymentResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedDeploymentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedDeploymentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedDeploymentResponse = union(enum) {
        ok: DeleteAppsV1NamespacedDeploymentResponse200,
        accepted: DeleteAppsV1NamespacedDeploymentResponse202,
        unauthorized: DeleteAppsV1NamespacedDeploymentResponse401,
        default_response: DeleteAppsV1NamespacedDeploymentResponseDefault,
    };

    pub const deleteAppsV1NamespacedDeploymentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a Deployment
    pub fn deleteAppsV1NamespacedDeployment(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAppsV1NamespacedDeploymentOptions) !DeleteAppsV1NamespacedDeploymentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAppsV1NamespacedDeploymentResponse200 = struct {
        body: root.io.k8s.api.apps.v1.Deployment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDeploymentResponse201 = struct {
        body: root.io.k8s.api.apps.v1.Deployment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDeploymentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDeploymentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDeploymentResponse = union(enum) {
        ok: PatchAppsV1NamespacedDeploymentResponse200,
        created: PatchAppsV1NamespacedDeploymentResponse201,
        unauthorized: PatchAppsV1NamespacedDeploymentResponse401,
        default_response: PatchAppsV1NamespacedDeploymentResponseDefault,
    };

    pub const patchAppsV1NamespacedDeploymentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Deployment
    pub fn patchAppsV1NamespacedDeployment(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAppsV1NamespacedDeploymentOptions) !PatchAppsV1NamespacedDeploymentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.Deployment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.Deployment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAppsV1NamespacedDeploymentScaleResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDeploymentScaleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDeploymentScaleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDeploymentScaleResponse = union(enum) {
        ok: ReadAppsV1NamespacedDeploymentScaleResponse200,
        unauthorized: ReadAppsV1NamespacedDeploymentScaleResponse401,
        default_response: ReadAppsV1NamespacedDeploymentScaleResponseDefault,
    };

    pub const readAppsV1NamespacedDeploymentScaleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read scale of the specified Deployment
    pub fn readAppsV1NamespacedDeploymentScale(self: *Client, name: []const u8, namespace: []const u8, options: readAppsV1NamespacedDeploymentScaleOptions) !ReadAppsV1NamespacedDeploymentScaleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/scale", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAppsV1NamespacedDeploymentScaleResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDeploymentScaleResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDeploymentScaleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDeploymentScaleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDeploymentScaleResponse = union(enum) {
        ok: ReplaceAppsV1NamespacedDeploymentScaleResponse200,
        created: ReplaceAppsV1NamespacedDeploymentScaleResponse201,
        unauthorized: ReplaceAppsV1NamespacedDeploymentScaleResponse401,
        default_response: ReplaceAppsV1NamespacedDeploymentScaleResponseDefault,
    };

    pub const replaceAppsV1NamespacedDeploymentScaleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace scale of the specified Deployment
    pub fn replaceAppsV1NamespacedDeploymentScale(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.autoscaling.v1.Scale, options: replaceAppsV1NamespacedDeploymentScaleOptions) !ReplaceAppsV1NamespacedDeploymentScaleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/scale", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAppsV1NamespacedDeploymentScaleResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDeploymentScaleResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDeploymentScaleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDeploymentScaleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDeploymentScaleResponse = union(enum) {
        ok: PatchAppsV1NamespacedDeploymentScaleResponse200,
        created: PatchAppsV1NamespacedDeploymentScaleResponse201,
        unauthorized: PatchAppsV1NamespacedDeploymentScaleResponse401,
        default_response: PatchAppsV1NamespacedDeploymentScaleResponseDefault,
    };

    pub const patchAppsV1NamespacedDeploymentScaleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update scale of the specified Deployment
    pub fn patchAppsV1NamespacedDeploymentScale(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAppsV1NamespacedDeploymentScaleOptions) !PatchAppsV1NamespacedDeploymentScaleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/scale", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAppsV1NamespacedDeploymentStatusResponse200 = struct {
        body: root.io.k8s.api.apps.v1.Deployment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDeploymentStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDeploymentStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedDeploymentStatusResponse = union(enum) {
        ok: ReadAppsV1NamespacedDeploymentStatusResponse200,
        unauthorized: ReadAppsV1NamespacedDeploymentStatusResponse401,
        default_response: ReadAppsV1NamespacedDeploymentStatusResponseDefault,
    };

    pub const readAppsV1NamespacedDeploymentStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified Deployment
    pub fn readAppsV1NamespacedDeploymentStatus(self: *Client, name: []const u8, namespace: []const u8, options: readAppsV1NamespacedDeploymentStatusOptions) !ReadAppsV1NamespacedDeploymentStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.Deployment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAppsV1NamespacedDeploymentStatusResponse200 = struct {
        body: root.io.k8s.api.apps.v1.Deployment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDeploymentStatusResponse201 = struct {
        body: root.io.k8s.api.apps.v1.Deployment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDeploymentStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDeploymentStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedDeploymentStatusResponse = union(enum) {
        ok: ReplaceAppsV1NamespacedDeploymentStatusResponse200,
        created: ReplaceAppsV1NamespacedDeploymentStatusResponse201,
        unauthorized: ReplaceAppsV1NamespacedDeploymentStatusResponse401,
        default_response: ReplaceAppsV1NamespacedDeploymentStatusResponseDefault,
    };

    pub const replaceAppsV1NamespacedDeploymentStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified Deployment
    pub fn replaceAppsV1NamespacedDeploymentStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.apps.v1.Deployment, options: replaceAppsV1NamespacedDeploymentStatusOptions) !ReplaceAppsV1NamespacedDeploymentStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.Deployment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.Deployment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAppsV1NamespacedDeploymentStatusResponse200 = struct {
        body: root.io.k8s.api.apps.v1.Deployment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDeploymentStatusResponse201 = struct {
        body: root.io.k8s.api.apps.v1.Deployment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDeploymentStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDeploymentStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedDeploymentStatusResponse = union(enum) {
        ok: PatchAppsV1NamespacedDeploymentStatusResponse200,
        created: PatchAppsV1NamespacedDeploymentStatusResponse201,
        unauthorized: PatchAppsV1NamespacedDeploymentStatusResponse401,
        default_response: PatchAppsV1NamespacedDeploymentStatusResponseDefault,
    };

    pub const patchAppsV1NamespacedDeploymentStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified Deployment
    pub fn patchAppsV1NamespacedDeploymentStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAppsV1NamespacedDeploymentStatusOptions) !PatchAppsV1NamespacedDeploymentStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.Deployment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.Deployment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAppsV1NamespacedReplicaSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSetList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedReplicaSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedReplicaSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedReplicaSetResponse = union(enum) {
        ok: ListAppsV1NamespacedReplicaSetResponse200,
        unauthorized: ListAppsV1NamespacedReplicaSetResponse401,
        default_response: ListAppsV1NamespacedReplicaSetResponseDefault,
    };

    pub const listAppsV1NamespacedReplicaSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ReplicaSet
    pub fn listAppsV1NamespacedReplicaSet(self: *Client, namespace: []const u8, options: listAppsV1NamespacedReplicaSetOptions) !ListAppsV1NamespacedReplicaSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSetList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAppsV1NamespacedReplicaSet(self: *Client, namespace: []const u8, options: listAppsV1NamespacedReplicaSetOptions) !WatchStream(root.io.k8s.api.apps.v1.ReplicaSet) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/apps/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/replicasets");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.apps.v1.ReplicaSet){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAppsV1NamespacedReplicaSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedReplicaSetResponse201 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedReplicaSetResponse202 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedReplicaSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedReplicaSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedReplicaSetResponse = union(enum) {
        ok: CreateAppsV1NamespacedReplicaSetResponse200,
        created: CreateAppsV1NamespacedReplicaSetResponse201,
        accepted: CreateAppsV1NamespacedReplicaSetResponse202,
        unauthorized: CreateAppsV1NamespacedReplicaSetResponse401,
        default_response: CreateAppsV1NamespacedReplicaSetResponseDefault,
    };

    pub const createAppsV1NamespacedReplicaSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ReplicaSet
    pub fn createAppsV1NamespacedReplicaSet(self: *Client, namespace: []const u8, body: root.io.k8s.api.apps.v1.ReplicaSet, options: createAppsV1NamespacedReplicaSetOptions) !CreateAppsV1NamespacedReplicaSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAppsV1CollectionNamespacedReplicaSetResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedReplicaSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedReplicaSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedReplicaSetResponse = union(enum) {
        ok: DeleteAppsV1CollectionNamespacedReplicaSetResponse200,
        unauthorized: DeleteAppsV1CollectionNamespacedReplicaSetResponse401,
        default_response: DeleteAppsV1CollectionNamespacedReplicaSetResponseDefault,
    };

    pub const deleteAppsV1CollectionNamespacedReplicaSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ReplicaSet
    pub fn deleteAppsV1CollectionNamespacedReplicaSet(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAppsV1CollectionNamespacedReplicaSetOptions) !DeleteAppsV1CollectionNamespacedReplicaSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAppsV1NamespacedReplicaSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedReplicaSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedReplicaSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedReplicaSetResponse = union(enum) {
        ok: ReadAppsV1NamespacedReplicaSetResponse200,
        unauthorized: ReadAppsV1NamespacedReplicaSetResponse401,
        default_response: ReadAppsV1NamespacedReplicaSetResponseDefault,
    };

    pub const readAppsV1NamespacedReplicaSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ReplicaSet
    pub fn readAppsV1NamespacedReplicaSet(self: *Client, name: []const u8, namespace: []const u8, options: readAppsV1NamespacedReplicaSetOptions) !ReadAppsV1NamespacedReplicaSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAppsV1NamespacedReplicaSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedReplicaSetResponse201 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedReplicaSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedReplicaSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedReplicaSetResponse = union(enum) {
        ok: ReplaceAppsV1NamespacedReplicaSetResponse200,
        created: ReplaceAppsV1NamespacedReplicaSetResponse201,
        unauthorized: ReplaceAppsV1NamespacedReplicaSetResponse401,
        default_response: ReplaceAppsV1NamespacedReplicaSetResponseDefault,
    };

    pub const replaceAppsV1NamespacedReplicaSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ReplicaSet
    pub fn replaceAppsV1NamespacedReplicaSet(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.apps.v1.ReplicaSet, options: replaceAppsV1NamespacedReplicaSetOptions) !ReplaceAppsV1NamespacedReplicaSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAppsV1NamespacedReplicaSetResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedReplicaSetResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedReplicaSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedReplicaSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedReplicaSetResponse = union(enum) {
        ok: DeleteAppsV1NamespacedReplicaSetResponse200,
        accepted: DeleteAppsV1NamespacedReplicaSetResponse202,
        unauthorized: DeleteAppsV1NamespacedReplicaSetResponse401,
        default_response: DeleteAppsV1NamespacedReplicaSetResponseDefault,
    };

    pub const deleteAppsV1NamespacedReplicaSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ReplicaSet
    pub fn deleteAppsV1NamespacedReplicaSet(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAppsV1NamespacedReplicaSetOptions) !DeleteAppsV1NamespacedReplicaSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAppsV1NamespacedReplicaSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedReplicaSetResponse201 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedReplicaSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedReplicaSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedReplicaSetResponse = union(enum) {
        ok: PatchAppsV1NamespacedReplicaSetResponse200,
        created: PatchAppsV1NamespacedReplicaSetResponse201,
        unauthorized: PatchAppsV1NamespacedReplicaSetResponse401,
        default_response: PatchAppsV1NamespacedReplicaSetResponseDefault,
    };

    pub const patchAppsV1NamespacedReplicaSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ReplicaSet
    pub fn patchAppsV1NamespacedReplicaSet(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAppsV1NamespacedReplicaSetOptions) !PatchAppsV1NamespacedReplicaSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAppsV1NamespacedReplicaSetScaleResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedReplicaSetScaleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedReplicaSetScaleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedReplicaSetScaleResponse = union(enum) {
        ok: ReadAppsV1NamespacedReplicaSetScaleResponse200,
        unauthorized: ReadAppsV1NamespacedReplicaSetScaleResponse401,
        default_response: ReadAppsV1NamespacedReplicaSetScaleResponseDefault,
    };

    pub const readAppsV1NamespacedReplicaSetScaleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read scale of the specified ReplicaSet
    pub fn readAppsV1NamespacedReplicaSetScale(self: *Client, name: []const u8, namespace: []const u8, options: readAppsV1NamespacedReplicaSetScaleOptions) !ReadAppsV1NamespacedReplicaSetScaleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/scale", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAppsV1NamespacedReplicaSetScaleResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedReplicaSetScaleResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedReplicaSetScaleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedReplicaSetScaleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedReplicaSetScaleResponse = union(enum) {
        ok: ReplaceAppsV1NamespacedReplicaSetScaleResponse200,
        created: ReplaceAppsV1NamespacedReplicaSetScaleResponse201,
        unauthorized: ReplaceAppsV1NamespacedReplicaSetScaleResponse401,
        default_response: ReplaceAppsV1NamespacedReplicaSetScaleResponseDefault,
    };

    pub const replaceAppsV1NamespacedReplicaSetScaleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace scale of the specified ReplicaSet
    pub fn replaceAppsV1NamespacedReplicaSetScale(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.autoscaling.v1.Scale, options: replaceAppsV1NamespacedReplicaSetScaleOptions) !ReplaceAppsV1NamespacedReplicaSetScaleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/scale", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAppsV1NamespacedReplicaSetScaleResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedReplicaSetScaleResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedReplicaSetScaleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedReplicaSetScaleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedReplicaSetScaleResponse = union(enum) {
        ok: PatchAppsV1NamespacedReplicaSetScaleResponse200,
        created: PatchAppsV1NamespacedReplicaSetScaleResponse201,
        unauthorized: PatchAppsV1NamespacedReplicaSetScaleResponse401,
        default_response: PatchAppsV1NamespacedReplicaSetScaleResponseDefault,
    };

    pub const patchAppsV1NamespacedReplicaSetScaleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update scale of the specified ReplicaSet
    pub fn patchAppsV1NamespacedReplicaSetScale(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAppsV1NamespacedReplicaSetScaleOptions) !PatchAppsV1NamespacedReplicaSetScaleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/scale", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAppsV1NamespacedReplicaSetStatusResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedReplicaSetStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedReplicaSetStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedReplicaSetStatusResponse = union(enum) {
        ok: ReadAppsV1NamespacedReplicaSetStatusResponse200,
        unauthorized: ReadAppsV1NamespacedReplicaSetStatusResponse401,
        default_response: ReadAppsV1NamespacedReplicaSetStatusResponseDefault,
    };

    pub const readAppsV1NamespacedReplicaSetStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified ReplicaSet
    pub fn readAppsV1NamespacedReplicaSetStatus(self: *Client, name: []const u8, namespace: []const u8, options: readAppsV1NamespacedReplicaSetStatusOptions) !ReadAppsV1NamespacedReplicaSetStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAppsV1NamespacedReplicaSetStatusResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedReplicaSetStatusResponse201 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedReplicaSetStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedReplicaSetStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedReplicaSetStatusResponse = union(enum) {
        ok: ReplaceAppsV1NamespacedReplicaSetStatusResponse200,
        created: ReplaceAppsV1NamespacedReplicaSetStatusResponse201,
        unauthorized: ReplaceAppsV1NamespacedReplicaSetStatusResponse401,
        default_response: ReplaceAppsV1NamespacedReplicaSetStatusResponseDefault,
    };

    pub const replaceAppsV1NamespacedReplicaSetStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified ReplicaSet
    pub fn replaceAppsV1NamespacedReplicaSetStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.apps.v1.ReplicaSet, options: replaceAppsV1NamespacedReplicaSetStatusOptions) !ReplaceAppsV1NamespacedReplicaSetStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAppsV1NamespacedReplicaSetStatusResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedReplicaSetStatusResponse201 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedReplicaSetStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedReplicaSetStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedReplicaSetStatusResponse = union(enum) {
        ok: PatchAppsV1NamespacedReplicaSetStatusResponse200,
        created: PatchAppsV1NamespacedReplicaSetStatusResponse201,
        unauthorized: PatchAppsV1NamespacedReplicaSetStatusResponse401,
        default_response: PatchAppsV1NamespacedReplicaSetStatusResponseDefault,
    };

    pub const patchAppsV1NamespacedReplicaSetStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified ReplicaSet
    pub fn patchAppsV1NamespacedReplicaSetStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAppsV1NamespacedReplicaSetStatusOptions) !PatchAppsV1NamespacedReplicaSetStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAppsV1NamespacedStatefulSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSetList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedStatefulSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedStatefulSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1NamespacedStatefulSetResponse = union(enum) {
        ok: ListAppsV1NamespacedStatefulSetResponse200,
        unauthorized: ListAppsV1NamespacedStatefulSetResponse401,
        default_response: ListAppsV1NamespacedStatefulSetResponseDefault,
    };

    pub const listAppsV1NamespacedStatefulSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind StatefulSet
    pub fn listAppsV1NamespacedStatefulSet(self: *Client, namespace: []const u8, options: listAppsV1NamespacedStatefulSetOptions) !ListAppsV1NamespacedStatefulSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSetList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAppsV1NamespacedStatefulSet(self: *Client, namespace: []const u8, options: listAppsV1NamespacedStatefulSetOptions) !WatchStream(root.io.k8s.api.apps.v1.StatefulSet) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/apps/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/statefulsets");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.apps.v1.StatefulSet){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAppsV1NamespacedStatefulSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedStatefulSetResponse201 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedStatefulSetResponse202 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedStatefulSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedStatefulSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAppsV1NamespacedStatefulSetResponse = union(enum) {
        ok: CreateAppsV1NamespacedStatefulSetResponse200,
        created: CreateAppsV1NamespacedStatefulSetResponse201,
        accepted: CreateAppsV1NamespacedStatefulSetResponse202,
        unauthorized: CreateAppsV1NamespacedStatefulSetResponse401,
        default_response: CreateAppsV1NamespacedStatefulSetResponseDefault,
    };

    pub const createAppsV1NamespacedStatefulSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a StatefulSet
    pub fn createAppsV1NamespacedStatefulSet(self: *Client, namespace: []const u8, body: root.io.k8s.api.apps.v1.StatefulSet, options: createAppsV1NamespacedStatefulSetOptions) !CreateAppsV1NamespacedStatefulSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAppsV1CollectionNamespacedStatefulSetResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedStatefulSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedStatefulSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1CollectionNamespacedStatefulSetResponse = union(enum) {
        ok: DeleteAppsV1CollectionNamespacedStatefulSetResponse200,
        unauthorized: DeleteAppsV1CollectionNamespacedStatefulSetResponse401,
        default_response: DeleteAppsV1CollectionNamespacedStatefulSetResponseDefault,
    };

    pub const deleteAppsV1CollectionNamespacedStatefulSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of StatefulSet
    pub fn deleteAppsV1CollectionNamespacedStatefulSet(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAppsV1CollectionNamespacedStatefulSetOptions) !DeleteAppsV1CollectionNamespacedStatefulSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAppsV1NamespacedStatefulSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedStatefulSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedStatefulSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedStatefulSetResponse = union(enum) {
        ok: ReadAppsV1NamespacedStatefulSetResponse200,
        unauthorized: ReadAppsV1NamespacedStatefulSetResponse401,
        default_response: ReadAppsV1NamespacedStatefulSetResponseDefault,
    };

    pub const readAppsV1NamespacedStatefulSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified StatefulSet
    pub fn readAppsV1NamespacedStatefulSet(self: *Client, name: []const u8, namespace: []const u8, options: readAppsV1NamespacedStatefulSetOptions) !ReadAppsV1NamespacedStatefulSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAppsV1NamespacedStatefulSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedStatefulSetResponse201 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedStatefulSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedStatefulSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedStatefulSetResponse = union(enum) {
        ok: ReplaceAppsV1NamespacedStatefulSetResponse200,
        created: ReplaceAppsV1NamespacedStatefulSetResponse201,
        unauthorized: ReplaceAppsV1NamespacedStatefulSetResponse401,
        default_response: ReplaceAppsV1NamespacedStatefulSetResponseDefault,
    };

    pub const replaceAppsV1NamespacedStatefulSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified StatefulSet
    pub fn replaceAppsV1NamespacedStatefulSet(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.apps.v1.StatefulSet, options: replaceAppsV1NamespacedStatefulSetOptions) !ReplaceAppsV1NamespacedStatefulSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAppsV1NamespacedStatefulSetResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedStatefulSetResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedStatefulSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedStatefulSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAppsV1NamespacedStatefulSetResponse = union(enum) {
        ok: DeleteAppsV1NamespacedStatefulSetResponse200,
        accepted: DeleteAppsV1NamespacedStatefulSetResponse202,
        unauthorized: DeleteAppsV1NamespacedStatefulSetResponse401,
        default_response: DeleteAppsV1NamespacedStatefulSetResponseDefault,
    };

    pub const deleteAppsV1NamespacedStatefulSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a StatefulSet
    pub fn deleteAppsV1NamespacedStatefulSet(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAppsV1NamespacedStatefulSetOptions) !DeleteAppsV1NamespacedStatefulSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAppsV1NamespacedStatefulSetResponse200 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedStatefulSetResponse201 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedStatefulSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedStatefulSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedStatefulSetResponse = union(enum) {
        ok: PatchAppsV1NamespacedStatefulSetResponse200,
        created: PatchAppsV1NamespacedStatefulSetResponse201,
        unauthorized: PatchAppsV1NamespacedStatefulSetResponse401,
        default_response: PatchAppsV1NamespacedStatefulSetResponseDefault,
    };

    pub const patchAppsV1NamespacedStatefulSetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified StatefulSet
    pub fn patchAppsV1NamespacedStatefulSet(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAppsV1NamespacedStatefulSetOptions) !PatchAppsV1NamespacedStatefulSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAppsV1NamespacedStatefulSetScaleResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedStatefulSetScaleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedStatefulSetScaleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedStatefulSetScaleResponse = union(enum) {
        ok: ReadAppsV1NamespacedStatefulSetScaleResponse200,
        unauthorized: ReadAppsV1NamespacedStatefulSetScaleResponse401,
        default_response: ReadAppsV1NamespacedStatefulSetScaleResponseDefault,
    };

    pub const readAppsV1NamespacedStatefulSetScaleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read scale of the specified StatefulSet
    pub fn readAppsV1NamespacedStatefulSetScale(self: *Client, name: []const u8, namespace: []const u8, options: readAppsV1NamespacedStatefulSetScaleOptions) !ReadAppsV1NamespacedStatefulSetScaleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/scale", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAppsV1NamespacedStatefulSetScaleResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedStatefulSetScaleResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedStatefulSetScaleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedStatefulSetScaleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedStatefulSetScaleResponse = union(enum) {
        ok: ReplaceAppsV1NamespacedStatefulSetScaleResponse200,
        created: ReplaceAppsV1NamespacedStatefulSetScaleResponse201,
        unauthorized: ReplaceAppsV1NamespacedStatefulSetScaleResponse401,
        default_response: ReplaceAppsV1NamespacedStatefulSetScaleResponseDefault,
    };

    pub const replaceAppsV1NamespacedStatefulSetScaleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace scale of the specified StatefulSet
    pub fn replaceAppsV1NamespacedStatefulSetScale(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.autoscaling.v1.Scale, options: replaceAppsV1NamespacedStatefulSetScaleOptions) !ReplaceAppsV1NamespacedStatefulSetScaleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/scale", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAppsV1NamespacedStatefulSetScaleResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedStatefulSetScaleResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v1.Scale,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedStatefulSetScaleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedStatefulSetScaleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedStatefulSetScaleResponse = union(enum) {
        ok: PatchAppsV1NamespacedStatefulSetScaleResponse200,
        created: PatchAppsV1NamespacedStatefulSetScaleResponse201,
        unauthorized: PatchAppsV1NamespacedStatefulSetScaleResponse401,
        default_response: PatchAppsV1NamespacedStatefulSetScaleResponseDefault,
    };

    pub const patchAppsV1NamespacedStatefulSetScaleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update scale of the specified StatefulSet
    pub fn patchAppsV1NamespacedStatefulSetScale(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAppsV1NamespacedStatefulSetScaleOptions) !PatchAppsV1NamespacedStatefulSetScaleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/scale", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.Scale, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAppsV1NamespacedStatefulSetStatusResponse200 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedStatefulSetStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedStatefulSetStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAppsV1NamespacedStatefulSetStatusResponse = union(enum) {
        ok: ReadAppsV1NamespacedStatefulSetStatusResponse200,
        unauthorized: ReadAppsV1NamespacedStatefulSetStatusResponse401,
        default_response: ReadAppsV1NamespacedStatefulSetStatusResponseDefault,
    };

    pub const readAppsV1NamespacedStatefulSetStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified StatefulSet
    pub fn readAppsV1NamespacedStatefulSetStatus(self: *Client, name: []const u8, namespace: []const u8, options: readAppsV1NamespacedStatefulSetStatusOptions) !ReadAppsV1NamespacedStatefulSetStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAppsV1NamespacedStatefulSetStatusResponse200 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedStatefulSetStatusResponse201 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedStatefulSetStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedStatefulSetStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAppsV1NamespacedStatefulSetStatusResponse = union(enum) {
        ok: ReplaceAppsV1NamespacedStatefulSetStatusResponse200,
        created: ReplaceAppsV1NamespacedStatefulSetStatusResponse201,
        unauthorized: ReplaceAppsV1NamespacedStatefulSetStatusResponse401,
        default_response: ReplaceAppsV1NamespacedStatefulSetStatusResponseDefault,
    };

    pub const replaceAppsV1NamespacedStatefulSetStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified StatefulSet
    pub fn replaceAppsV1NamespacedStatefulSetStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.apps.v1.StatefulSet, options: replaceAppsV1NamespacedStatefulSetStatusOptions) !ReplaceAppsV1NamespacedStatefulSetStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAppsV1NamespacedStatefulSetStatusResponse200 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedStatefulSetStatusResponse201 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSet,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedStatefulSetStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedStatefulSetStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAppsV1NamespacedStatefulSetStatusResponse = union(enum) {
        ok: PatchAppsV1NamespacedStatefulSetStatusResponse200,
        created: PatchAppsV1NamespacedStatefulSetStatusResponse201,
        unauthorized: PatchAppsV1NamespacedStatefulSetStatusResponse401,
        default_response: PatchAppsV1NamespacedStatefulSetStatusResponseDefault,
    };

    pub const patchAppsV1NamespacedStatefulSetStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified StatefulSet
    pub fn patchAppsV1NamespacedStatefulSetStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAppsV1NamespacedStatefulSetStatusOptions) !PatchAppsV1NamespacedStatefulSetStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSet, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAppsV1ReplicaSetForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.apps.v1.ReplicaSetList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1ReplicaSetForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1ReplicaSetForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1ReplicaSetForAllNamespacesResponse = union(enum) {
        ok: ListAppsV1ReplicaSetForAllNamespacesResponse200,
        unauthorized: ListAppsV1ReplicaSetForAllNamespacesResponse401,
        default_response: ListAppsV1ReplicaSetForAllNamespacesResponseDefault,
    };

    pub const listAppsV1ReplicaSetForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ReplicaSet
    pub fn listAppsV1ReplicaSetForAllNamespaces(self: *Client, options: listAppsV1ReplicaSetForAllNamespacesOptions) !ListAppsV1ReplicaSetForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/replicasets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.ReplicaSetList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAppsV1ReplicaSetForAllNamespaces(self: *Client, options: listAppsV1ReplicaSetForAllNamespacesOptions) !WatchStream(root.io.k8s.api.apps.v1.ReplicaSet) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/apps/v1/replicasets");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.apps.v1.ReplicaSet){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListAppsV1StatefulSetForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.apps.v1.StatefulSetList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1StatefulSetForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1StatefulSetForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAppsV1StatefulSetForAllNamespacesResponse = union(enum) {
        ok: ListAppsV1StatefulSetForAllNamespacesResponse200,
        unauthorized: ListAppsV1StatefulSetForAllNamespacesResponse401,
        default_response: ListAppsV1StatefulSetForAllNamespacesResponseDefault,
    };

    pub const listAppsV1StatefulSetForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind StatefulSet
    pub fn listAppsV1StatefulSetForAllNamespaces(self: *Client, options: listAppsV1StatefulSetForAllNamespacesOptions) !ListAppsV1StatefulSetForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/statefulsets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apps.v1.StatefulSetList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAppsV1StatefulSetForAllNamespaces(self: *Client, options: listAppsV1StatefulSetForAllNamespacesOptions) !WatchStream(root.io.k8s.api.apps.v1.StatefulSet) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/apps/v1/statefulsets");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.apps.v1.StatefulSet){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const WatchAppsV1ControllerRevisionListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1ControllerRevisionListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1ControllerRevisionListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1ControllerRevisionListForAllNamespacesResponse = union(enum) {
        ok: WatchAppsV1ControllerRevisionListForAllNamespacesResponse200,
        unauthorized: WatchAppsV1ControllerRevisionListForAllNamespacesResponse401,
        default_response: WatchAppsV1ControllerRevisionListForAllNamespacesResponseDefault,
    };

    pub const watchAppsV1ControllerRevisionListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ControllerRevision. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAppsV1ControllerRevisionListForAllNamespaces(self: *Client, options: watchAppsV1ControllerRevisionListForAllNamespacesOptions) !WatchAppsV1ControllerRevisionListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/controllerrevisions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1DaemonSetListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1DaemonSetListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1DaemonSetListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1DaemonSetListForAllNamespacesResponse = union(enum) {
        ok: WatchAppsV1DaemonSetListForAllNamespacesResponse200,
        unauthorized: WatchAppsV1DaemonSetListForAllNamespacesResponse401,
        default_response: WatchAppsV1DaemonSetListForAllNamespacesResponseDefault,
    };

    pub const watchAppsV1DaemonSetListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of DaemonSet. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAppsV1DaemonSetListForAllNamespaces(self: *Client, options: watchAppsV1DaemonSetListForAllNamespacesOptions) !WatchAppsV1DaemonSetListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/daemonsets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1DeploymentListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1DeploymentListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1DeploymentListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1DeploymentListForAllNamespacesResponse = union(enum) {
        ok: WatchAppsV1DeploymentListForAllNamespacesResponse200,
        unauthorized: WatchAppsV1DeploymentListForAllNamespacesResponse401,
        default_response: WatchAppsV1DeploymentListForAllNamespacesResponseDefault,
    };

    pub const watchAppsV1DeploymentListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Deployment. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAppsV1DeploymentListForAllNamespaces(self: *Client, options: watchAppsV1DeploymentListForAllNamespacesOptions) !WatchAppsV1DeploymentListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/deployments", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1NamespacedControllerRevisionListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedControllerRevisionListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedControllerRevisionListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedControllerRevisionListResponse = union(enum) {
        ok: WatchAppsV1NamespacedControllerRevisionListResponse200,
        unauthorized: WatchAppsV1NamespacedControllerRevisionListResponse401,
        default_response: WatchAppsV1NamespacedControllerRevisionListResponseDefault,
    };

    pub const watchAppsV1NamespacedControllerRevisionListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ControllerRevision. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAppsV1NamespacedControllerRevisionList(self: *Client, namespace: []const u8, options: watchAppsV1NamespacedControllerRevisionListOptions) !WatchAppsV1NamespacedControllerRevisionListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/controllerrevisions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1NamespacedControllerRevisionResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedControllerRevisionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedControllerRevisionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedControllerRevisionResponse = union(enum) {
        ok: WatchAppsV1NamespacedControllerRevisionResponse200,
        unauthorized: WatchAppsV1NamespacedControllerRevisionResponse401,
        default_response: WatchAppsV1NamespacedControllerRevisionResponseDefault,
    };

    pub const watchAppsV1NamespacedControllerRevisionOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ControllerRevision. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAppsV1NamespacedControllerRevision(self: *Client, name: []const u8, namespace: []const u8, options: watchAppsV1NamespacedControllerRevisionOptions) !WatchAppsV1NamespacedControllerRevisionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/controllerrevisions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1NamespacedDaemonSetListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedDaemonSetListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedDaemonSetListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedDaemonSetListResponse = union(enum) {
        ok: WatchAppsV1NamespacedDaemonSetListResponse200,
        unauthorized: WatchAppsV1NamespacedDaemonSetListResponse401,
        default_response: WatchAppsV1NamespacedDaemonSetListResponseDefault,
    };

    pub const watchAppsV1NamespacedDaemonSetListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of DaemonSet. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAppsV1NamespacedDaemonSetList(self: *Client, namespace: []const u8, options: watchAppsV1NamespacedDaemonSetListOptions) !WatchAppsV1NamespacedDaemonSetListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/daemonsets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1NamespacedDaemonSetResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedDaemonSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedDaemonSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedDaemonSetResponse = union(enum) {
        ok: WatchAppsV1NamespacedDaemonSetResponse200,
        unauthorized: WatchAppsV1NamespacedDaemonSetResponse401,
        default_response: WatchAppsV1NamespacedDaemonSetResponseDefault,
    };

    pub const watchAppsV1NamespacedDaemonSetOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind DaemonSet. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAppsV1NamespacedDaemonSet(self: *Client, name: []const u8, namespace: []const u8, options: watchAppsV1NamespacedDaemonSetOptions) !WatchAppsV1NamespacedDaemonSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/daemonsets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1NamespacedDeploymentListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedDeploymentListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedDeploymentListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedDeploymentListResponse = union(enum) {
        ok: WatchAppsV1NamespacedDeploymentListResponse200,
        unauthorized: WatchAppsV1NamespacedDeploymentListResponse401,
        default_response: WatchAppsV1NamespacedDeploymentListResponseDefault,
    };

    pub const watchAppsV1NamespacedDeploymentListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Deployment. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAppsV1NamespacedDeploymentList(self: *Client, namespace: []const u8, options: watchAppsV1NamespacedDeploymentListOptions) !WatchAppsV1NamespacedDeploymentListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1NamespacedDeploymentResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedDeploymentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedDeploymentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedDeploymentResponse = union(enum) {
        ok: WatchAppsV1NamespacedDeploymentResponse200,
        unauthorized: WatchAppsV1NamespacedDeploymentResponse401,
        default_response: WatchAppsV1NamespacedDeploymentResponseDefault,
    };

    pub const watchAppsV1NamespacedDeploymentOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Deployment. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAppsV1NamespacedDeployment(self: *Client, name: []const u8, namespace: []const u8, options: watchAppsV1NamespacedDeploymentOptions) !WatchAppsV1NamespacedDeploymentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/deployments/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1NamespacedReplicaSetListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedReplicaSetListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedReplicaSetListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedReplicaSetListResponse = union(enum) {
        ok: WatchAppsV1NamespacedReplicaSetListResponse200,
        unauthorized: WatchAppsV1NamespacedReplicaSetListResponse401,
        default_response: WatchAppsV1NamespacedReplicaSetListResponseDefault,
    };

    pub const watchAppsV1NamespacedReplicaSetListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ReplicaSet. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAppsV1NamespacedReplicaSetList(self: *Client, namespace: []const u8, options: watchAppsV1NamespacedReplicaSetListOptions) !WatchAppsV1NamespacedReplicaSetListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1NamespacedReplicaSetResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedReplicaSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedReplicaSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedReplicaSetResponse = union(enum) {
        ok: WatchAppsV1NamespacedReplicaSetResponse200,
        unauthorized: WatchAppsV1NamespacedReplicaSetResponse401,
        default_response: WatchAppsV1NamespacedReplicaSetResponseDefault,
    };

    pub const watchAppsV1NamespacedReplicaSetOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ReplicaSet. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAppsV1NamespacedReplicaSet(self: *Client, name: []const u8, namespace: []const u8, options: watchAppsV1NamespacedReplicaSetOptions) !WatchAppsV1NamespacedReplicaSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/replicasets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1NamespacedStatefulSetListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedStatefulSetListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedStatefulSetListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedStatefulSetListResponse = union(enum) {
        ok: WatchAppsV1NamespacedStatefulSetListResponse200,
        unauthorized: WatchAppsV1NamespacedStatefulSetListResponse401,
        default_response: WatchAppsV1NamespacedStatefulSetListResponseDefault,
    };

    pub const watchAppsV1NamespacedStatefulSetListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of StatefulSet. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAppsV1NamespacedStatefulSetList(self: *Client, namespace: []const u8, options: watchAppsV1NamespacedStatefulSetListOptions) !WatchAppsV1NamespacedStatefulSetListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1NamespacedStatefulSetResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedStatefulSetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedStatefulSetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1NamespacedStatefulSetResponse = union(enum) {
        ok: WatchAppsV1NamespacedStatefulSetResponse200,
        unauthorized: WatchAppsV1NamespacedStatefulSetResponse401,
        default_response: WatchAppsV1NamespacedStatefulSetResponseDefault,
    };

    pub const watchAppsV1NamespacedStatefulSetOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind StatefulSet. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAppsV1NamespacedStatefulSet(self: *Client, name: []const u8, namespace: []const u8, options: watchAppsV1NamespacedStatefulSetOptions) !WatchAppsV1NamespacedStatefulSetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/statefulsets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1ReplicaSetListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1ReplicaSetListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1ReplicaSetListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1ReplicaSetListForAllNamespacesResponse = union(enum) {
        ok: WatchAppsV1ReplicaSetListForAllNamespacesResponse200,
        unauthorized: WatchAppsV1ReplicaSetListForAllNamespacesResponse401,
        default_response: WatchAppsV1ReplicaSetListForAllNamespacesResponseDefault,
    };

    pub const watchAppsV1ReplicaSetListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ReplicaSet. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAppsV1ReplicaSetListForAllNamespaces(self: *Client, options: watchAppsV1ReplicaSetListForAllNamespacesOptions) !WatchAppsV1ReplicaSetListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/replicasets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAppsV1StatefulSetListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1StatefulSetListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1StatefulSetListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAppsV1StatefulSetListForAllNamespacesResponse = union(enum) {
        ok: WatchAppsV1StatefulSetListForAllNamespacesResponse200,
        unauthorized: WatchAppsV1StatefulSetListForAllNamespacesResponse401,
        default_response: WatchAppsV1StatefulSetListForAllNamespacesResponseDefault,
    };

    pub const watchAppsV1StatefulSetListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of StatefulSet. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAppsV1StatefulSetListForAllNamespaces(self: *Client, options: watchAppsV1StatefulSetListForAllNamespacesOptions) !WatchAppsV1StatefulSetListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/apps/v1/watch/statefulsets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAuthenticationAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAuthenticationAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAuthenticationAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAuthenticationAPIGroupResponse = union(enum) {
        ok: GetAuthenticationAPIGroupResponse200,
        unauthorized: GetAuthenticationAPIGroupResponse401,
        default_response: GetAuthenticationAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getAuthenticationAPIGroup(self: *Client) !GetAuthenticationAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/authentication.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAuthenticationV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAuthenticationV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAuthenticationV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAuthenticationV1APIResourcesResponse = union(enum) {
        ok: GetAuthenticationV1APIResourcesResponse200,
        unauthorized: GetAuthenticationV1APIResourcesResponse401,
        default_response: GetAuthenticationV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getAuthenticationV1APIResources(self: *Client) !GetAuthenticationV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/authentication.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const CreateAuthenticationV1SelfSubjectReviewResponse200 = struct {
        body: root.io.k8s.api.authentication.v1.SelfSubjectReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthenticationV1SelfSubjectReviewResponse201 = struct {
        body: root.io.k8s.api.authentication.v1.SelfSubjectReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthenticationV1SelfSubjectReviewResponse202 = struct {
        body: root.io.k8s.api.authentication.v1.SelfSubjectReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthenticationV1SelfSubjectReviewResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthenticationV1SelfSubjectReviewResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthenticationV1SelfSubjectReviewResponse = union(enum) {
        ok: CreateAuthenticationV1SelfSubjectReviewResponse200,
        created: CreateAuthenticationV1SelfSubjectReviewResponse201,
        accepted: CreateAuthenticationV1SelfSubjectReviewResponse202,
        unauthorized: CreateAuthenticationV1SelfSubjectReviewResponse401,
        default_response: CreateAuthenticationV1SelfSubjectReviewResponseDefault,
    };

    pub const createAuthenticationV1SelfSubjectReviewOptions = struct {
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// create a SelfSubjectReview
    pub fn createAuthenticationV1SelfSubjectReview(self: *Client, body: root.io.k8s.api.authentication.v1.SelfSubjectReview, options: createAuthenticationV1SelfSubjectReviewOptions) !CreateAuthenticationV1SelfSubjectReviewResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/authentication.k8s.io/v1/selfsubjectreviews", .{});
        var first_query = true;
        _ = &first_query;
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authentication.v1.SelfSubjectReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authentication.v1.SelfSubjectReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authentication.v1.SelfSubjectReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const CreateAuthenticationV1TokenReviewResponse200 = struct {
        body: root.io.k8s.api.authentication.v1.TokenReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthenticationV1TokenReviewResponse201 = struct {
        body: root.io.k8s.api.authentication.v1.TokenReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthenticationV1TokenReviewResponse202 = struct {
        body: root.io.k8s.api.authentication.v1.TokenReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthenticationV1TokenReviewResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthenticationV1TokenReviewResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthenticationV1TokenReviewResponse = union(enum) {
        ok: CreateAuthenticationV1TokenReviewResponse200,
        created: CreateAuthenticationV1TokenReviewResponse201,
        accepted: CreateAuthenticationV1TokenReviewResponse202,
        unauthorized: CreateAuthenticationV1TokenReviewResponse401,
        default_response: CreateAuthenticationV1TokenReviewResponseDefault,
    };

    pub const createAuthenticationV1TokenReviewOptions = struct {
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// create a TokenReview
    pub fn createAuthenticationV1TokenReview(self: *Client, body: root.io.k8s.api.authentication.v1.TokenReview, options: createAuthenticationV1TokenReviewOptions) !CreateAuthenticationV1TokenReviewResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/authentication.k8s.io/v1/tokenreviews", .{});
        var first_query = true;
        _ = &first_query;
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authentication.v1.TokenReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authentication.v1.TokenReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authentication.v1.TokenReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAuthorizationAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAuthorizationAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAuthorizationAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAuthorizationAPIGroupResponse = union(enum) {
        ok: GetAuthorizationAPIGroupResponse200,
        unauthorized: GetAuthorizationAPIGroupResponse401,
        default_response: GetAuthorizationAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getAuthorizationAPIGroup(self: *Client) !GetAuthorizationAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/authorization.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAuthorizationV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAuthorizationV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAuthorizationV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAuthorizationV1APIResourcesResponse = union(enum) {
        ok: GetAuthorizationV1APIResourcesResponse200,
        unauthorized: GetAuthorizationV1APIResourcesResponse401,
        default_response: GetAuthorizationV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getAuthorizationV1APIResources(self: *Client) !GetAuthorizationV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/authorization.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const CreateAuthorizationV1NamespacedLocalSubjectAccessReviewResponse200 = struct {
        body: root.io.k8s.api.authorization.v1.LocalSubjectAccessReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1NamespacedLocalSubjectAccessReviewResponse201 = struct {
        body: root.io.k8s.api.authorization.v1.LocalSubjectAccessReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1NamespacedLocalSubjectAccessReviewResponse202 = struct {
        body: root.io.k8s.api.authorization.v1.LocalSubjectAccessReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1NamespacedLocalSubjectAccessReviewResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1NamespacedLocalSubjectAccessReviewResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1NamespacedLocalSubjectAccessReviewResponse = union(enum) {
        ok: CreateAuthorizationV1NamespacedLocalSubjectAccessReviewResponse200,
        created: CreateAuthorizationV1NamespacedLocalSubjectAccessReviewResponse201,
        accepted: CreateAuthorizationV1NamespacedLocalSubjectAccessReviewResponse202,
        unauthorized: CreateAuthorizationV1NamespacedLocalSubjectAccessReviewResponse401,
        default_response: CreateAuthorizationV1NamespacedLocalSubjectAccessReviewResponseDefault,
    };

    pub const createAuthorizationV1NamespacedLocalSubjectAccessReviewOptions = struct {
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// create a LocalSubjectAccessReview
    pub fn createAuthorizationV1NamespacedLocalSubjectAccessReview(self: *Client, namespace: []const u8, body: root.io.k8s.api.authorization.v1.LocalSubjectAccessReview, options: createAuthorizationV1NamespacedLocalSubjectAccessReviewOptions) !CreateAuthorizationV1NamespacedLocalSubjectAccessReviewResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/localsubjectaccessreviews", .{});
        var first_query = true;
        _ = &first_query;
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authorization.v1.LocalSubjectAccessReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authorization.v1.LocalSubjectAccessReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authorization.v1.LocalSubjectAccessReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const CreateAuthorizationV1SelfSubjectAccessReviewResponse200 = struct {
        body: root.io.k8s.api.authorization.v1.SelfSubjectAccessReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SelfSubjectAccessReviewResponse201 = struct {
        body: root.io.k8s.api.authorization.v1.SelfSubjectAccessReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SelfSubjectAccessReviewResponse202 = struct {
        body: root.io.k8s.api.authorization.v1.SelfSubjectAccessReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SelfSubjectAccessReviewResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SelfSubjectAccessReviewResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SelfSubjectAccessReviewResponse = union(enum) {
        ok: CreateAuthorizationV1SelfSubjectAccessReviewResponse200,
        created: CreateAuthorizationV1SelfSubjectAccessReviewResponse201,
        accepted: CreateAuthorizationV1SelfSubjectAccessReviewResponse202,
        unauthorized: CreateAuthorizationV1SelfSubjectAccessReviewResponse401,
        default_response: CreateAuthorizationV1SelfSubjectAccessReviewResponseDefault,
    };

    pub const createAuthorizationV1SelfSubjectAccessReviewOptions = struct {
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// create a SelfSubjectAccessReview
    pub fn createAuthorizationV1SelfSubjectAccessReview(self: *Client, body: root.io.k8s.api.authorization.v1.SelfSubjectAccessReview, options: createAuthorizationV1SelfSubjectAccessReviewOptions) !CreateAuthorizationV1SelfSubjectAccessReviewResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/authorization.k8s.io/v1/selfsubjectaccessreviews", .{});
        var first_query = true;
        _ = &first_query;
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authorization.v1.SelfSubjectAccessReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authorization.v1.SelfSubjectAccessReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authorization.v1.SelfSubjectAccessReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const CreateAuthorizationV1SelfSubjectRulesReviewResponse200 = struct {
        body: root.io.k8s.api.authorization.v1.SelfSubjectRulesReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SelfSubjectRulesReviewResponse201 = struct {
        body: root.io.k8s.api.authorization.v1.SelfSubjectRulesReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SelfSubjectRulesReviewResponse202 = struct {
        body: root.io.k8s.api.authorization.v1.SelfSubjectRulesReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SelfSubjectRulesReviewResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SelfSubjectRulesReviewResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SelfSubjectRulesReviewResponse = union(enum) {
        ok: CreateAuthorizationV1SelfSubjectRulesReviewResponse200,
        created: CreateAuthorizationV1SelfSubjectRulesReviewResponse201,
        accepted: CreateAuthorizationV1SelfSubjectRulesReviewResponse202,
        unauthorized: CreateAuthorizationV1SelfSubjectRulesReviewResponse401,
        default_response: CreateAuthorizationV1SelfSubjectRulesReviewResponseDefault,
    };

    pub const createAuthorizationV1SelfSubjectRulesReviewOptions = struct {
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// create a SelfSubjectRulesReview
    pub fn createAuthorizationV1SelfSubjectRulesReview(self: *Client, body: root.io.k8s.api.authorization.v1.SelfSubjectRulesReview, options: createAuthorizationV1SelfSubjectRulesReviewOptions) !CreateAuthorizationV1SelfSubjectRulesReviewResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/authorization.k8s.io/v1/selfsubjectrulesreviews", .{});
        var first_query = true;
        _ = &first_query;
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authorization.v1.SelfSubjectRulesReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authorization.v1.SelfSubjectRulesReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authorization.v1.SelfSubjectRulesReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const CreateAuthorizationV1SubjectAccessReviewResponse200 = struct {
        body: root.io.k8s.api.authorization.v1.SubjectAccessReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SubjectAccessReviewResponse201 = struct {
        body: root.io.k8s.api.authorization.v1.SubjectAccessReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SubjectAccessReviewResponse202 = struct {
        body: root.io.k8s.api.authorization.v1.SubjectAccessReview,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SubjectAccessReviewResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SubjectAccessReviewResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAuthorizationV1SubjectAccessReviewResponse = union(enum) {
        ok: CreateAuthorizationV1SubjectAccessReviewResponse200,
        created: CreateAuthorizationV1SubjectAccessReviewResponse201,
        accepted: CreateAuthorizationV1SubjectAccessReviewResponse202,
        unauthorized: CreateAuthorizationV1SubjectAccessReviewResponse401,
        default_response: CreateAuthorizationV1SubjectAccessReviewResponseDefault,
    };

    pub const createAuthorizationV1SubjectAccessReviewOptions = struct {
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// create a SubjectAccessReview
    pub fn createAuthorizationV1SubjectAccessReview(self: *Client, body: root.io.k8s.api.authorization.v1.SubjectAccessReview, options: createAuthorizationV1SubjectAccessReviewOptions) !CreateAuthorizationV1SubjectAccessReviewResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/authorization.k8s.io/v1/subjectaccessreviews", .{});
        var first_query = true;
        _ = &first_query;
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authorization.v1.SubjectAccessReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authorization.v1.SubjectAccessReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.authorization.v1.SubjectAccessReview, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAutoscalingAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAutoscalingAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAutoscalingAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAutoscalingAPIGroupResponse = union(enum) {
        ok: GetAutoscalingAPIGroupResponse200,
        unauthorized: GetAutoscalingAPIGroupResponse401,
        default_response: GetAutoscalingAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getAutoscalingAPIGroup(self: *Client) !GetAutoscalingAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAutoscalingV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAutoscalingV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAutoscalingV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAutoscalingV1APIResourcesResponse = union(enum) {
        ok: GetAutoscalingV1APIResourcesResponse200,
        unauthorized: GetAutoscalingV1APIResourcesResponse401,
        default_response: GetAutoscalingV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getAutoscalingV1APIResources(self: *Client) !GetAutoscalingV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAutoscalingV1HorizontalPodAutoscalerForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscalerList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAutoscalingV1HorizontalPodAutoscalerForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAutoscalingV1HorizontalPodAutoscalerForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAutoscalingV1HorizontalPodAutoscalerForAllNamespacesResponse = union(enum) {
        ok: ListAutoscalingV1HorizontalPodAutoscalerForAllNamespacesResponse200,
        unauthorized: ListAutoscalingV1HorizontalPodAutoscalerForAllNamespacesResponse401,
        default_response: ListAutoscalingV1HorizontalPodAutoscalerForAllNamespacesResponseDefault,
    };

    pub const listAutoscalingV1HorizontalPodAutoscalerForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind HorizontalPodAutoscaler
    pub fn listAutoscalingV1HorizontalPodAutoscalerForAllNamespaces(self: *Client, options: listAutoscalingV1HorizontalPodAutoscalerForAllNamespacesOptions) !ListAutoscalingV1HorizontalPodAutoscalerForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/horizontalpodautoscalers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscalerList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAutoscalingV1HorizontalPodAutoscalerForAllNamespaces(self: *Client, options: listAutoscalingV1HorizontalPodAutoscalerForAllNamespacesOptions) !WatchStream(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/autoscaling/v1/horizontalpodautoscalers");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscalerList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAutoscalingV1NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: ListAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200,
        unauthorized: ListAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401,
        default_response: ListAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const listAutoscalingV1NamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind HorizontalPodAutoscaler
    pub fn listAutoscalingV1NamespacedHorizontalPodAutoscaler(self: *Client, namespace: []const u8, options: listAutoscalingV1NamespacedHorizontalPodAutoscalerOptions) !ListAutoscalingV1NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscalerList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAutoscalingV1NamespacedHorizontalPodAutoscaler(self: *Client, namespace: []const u8, options: listAutoscalingV1NamespacedHorizontalPodAutoscalerOptions) !WatchStream(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/autoscaling/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/horizontalpodautoscalers");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAutoscalingV1NamespacedHorizontalPodAutoscalerResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAutoscalingV1NamespacedHorizontalPodAutoscalerResponse202 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAutoscalingV1NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: CreateAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200,
        created: CreateAutoscalingV1NamespacedHorizontalPodAutoscalerResponse201,
        accepted: CreateAutoscalingV1NamespacedHorizontalPodAutoscalerResponse202,
        unauthorized: CreateAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401,
        default_response: CreateAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const createAutoscalingV1NamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a HorizontalPodAutoscaler
    pub fn createAutoscalingV1NamespacedHorizontalPodAutoscaler(self: *Client, namespace: []const u8, body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, options: createAutoscalingV1NamespacedHorizontalPodAutoscalerOptions) !CreateAutoscalingV1NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: DeleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscalerResponse200,
        unauthorized: DeleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscalerResponse401,
        default_response: DeleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const deleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of HorizontalPodAutoscaler
    pub fn deleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscaler(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscalerOptions) !DeleteAutoscalingV1CollectionNamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAutoscalingV1NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: ReadAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200,
        unauthorized: ReadAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401,
        default_response: ReadAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const readAutoscalingV1NamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified HorizontalPodAutoscaler
    pub fn readAutoscalingV1NamespacedHorizontalPodAutoscaler(self: *Client, name: []const u8, namespace: []const u8, options: readAutoscalingV1NamespacedHorizontalPodAutoscalerOptions) !ReadAutoscalingV1NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200,
        created: ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerResponse201,
        unauthorized: ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401,
        default_response: ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const replaceAutoscalingV1NamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified HorizontalPodAutoscaler
    pub fn replaceAutoscalingV1NamespacedHorizontalPodAutoscaler(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, options: replaceAutoscalingV1NamespacedHorizontalPodAutoscalerOptions) !ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV1NamespacedHorizontalPodAutoscalerResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV1NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: DeleteAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200,
        accepted: DeleteAutoscalingV1NamespacedHorizontalPodAutoscalerResponse202,
        unauthorized: DeleteAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401,
        default_response: DeleteAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const deleteAutoscalingV1NamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a HorizontalPodAutoscaler
    pub fn deleteAutoscalingV1NamespacedHorizontalPodAutoscaler(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAutoscalingV1NamespacedHorizontalPodAutoscalerOptions) !DeleteAutoscalingV1NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: PatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200,
        created: PatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse201,
        unauthorized: PatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401,
        default_response: PatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const patchAutoscalingV1NamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified HorizontalPodAutoscaler
    pub fn patchAutoscalingV1NamespacedHorizontalPodAutoscaler(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAutoscalingV1NamespacedHorizontalPodAutoscalerOptions) !PatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse = union(enum) {
        ok: ReadAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse200,
        unauthorized: ReadAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse401,
        default_response: ReadAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponseDefault,
    };

    pub const readAutoscalingV1NamespacedHorizontalPodAutoscalerStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified HorizontalPodAutoscaler
    pub fn readAutoscalingV1NamespacedHorizontalPodAutoscalerStatus(self: *Client, name: []const u8, namespace: []const u8, options: readAutoscalingV1NamespacedHorizontalPodAutoscalerStatusOptions) !ReadAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse = union(enum) {
        ok: ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse200,
        created: ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse201,
        unauthorized: ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse401,
        default_response: ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponseDefault,
    };

    pub const replaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified HorizontalPodAutoscaler
    pub fn replaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, options: replaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatusOptions) !ReplaceAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse = union(enum) {
        ok: PatchAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse200,
        created: PatchAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse201,
        unauthorized: PatchAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse401,
        default_response: PatchAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponseDefault,
    };

    pub const patchAutoscalingV1NamespacedHorizontalPodAutoscalerStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified HorizontalPodAutoscaler
    pub fn patchAutoscalingV1NamespacedHorizontalPodAutoscalerStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAutoscalingV1NamespacedHorizontalPodAutoscalerStatusOptions) !PatchAutoscalingV1NamespacedHorizontalPodAutoscalerStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v1.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAutoscalingV1HorizontalPodAutoscalerListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV1HorizontalPodAutoscalerListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV1HorizontalPodAutoscalerListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV1HorizontalPodAutoscalerListForAllNamespacesResponse = union(enum) {
        ok: WatchAutoscalingV1HorizontalPodAutoscalerListForAllNamespacesResponse200,
        unauthorized: WatchAutoscalingV1HorizontalPodAutoscalerListForAllNamespacesResponse401,
        default_response: WatchAutoscalingV1HorizontalPodAutoscalerListForAllNamespacesResponseDefault,
    };

    pub const watchAutoscalingV1HorizontalPodAutoscalerListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of HorizontalPodAutoscaler. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAutoscalingV1HorizontalPodAutoscalerListForAllNamespaces(self: *Client, options: watchAutoscalingV1HorizontalPodAutoscalerListForAllNamespacesOptions) !WatchAutoscalingV1HorizontalPodAutoscalerListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/watch/horizontalpodautoscalers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAutoscalingV1NamespacedHorizontalPodAutoscalerListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV1NamespacedHorizontalPodAutoscalerListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV1NamespacedHorizontalPodAutoscalerListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV1NamespacedHorizontalPodAutoscalerListResponse = union(enum) {
        ok: WatchAutoscalingV1NamespacedHorizontalPodAutoscalerListResponse200,
        unauthorized: WatchAutoscalingV1NamespacedHorizontalPodAutoscalerListResponse401,
        default_response: WatchAutoscalingV1NamespacedHorizontalPodAutoscalerListResponseDefault,
    };

    pub const watchAutoscalingV1NamespacedHorizontalPodAutoscalerListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of HorizontalPodAutoscaler. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAutoscalingV1NamespacedHorizontalPodAutoscalerList(self: *Client, namespace: []const u8, options: watchAutoscalingV1NamespacedHorizontalPodAutoscalerListOptions) !WatchAutoscalingV1NamespacedHorizontalPodAutoscalerListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: WatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse200,
        unauthorized: WatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse401,
        default_response: WatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const watchAutoscalingV1NamespacedHorizontalPodAutoscalerOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind HorizontalPodAutoscaler. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAutoscalingV1NamespacedHorizontalPodAutoscaler(self: *Client, name: []const u8, namespace: []const u8, options: watchAutoscalingV1NamespacedHorizontalPodAutoscalerOptions) !WatchAutoscalingV1NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetAutoscalingV2APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAutoscalingV2APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAutoscalingV2APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetAutoscalingV2APIResourcesResponse = union(enum) {
        ok: GetAutoscalingV2APIResourcesResponse200,
        unauthorized: GetAutoscalingV2APIResourcesResponse401,
        default_response: GetAutoscalingV2APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getAutoscalingV2APIResources(self: *Client) !GetAutoscalingV2APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListAutoscalingV2HorizontalPodAutoscalerForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscalerList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAutoscalingV2HorizontalPodAutoscalerForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAutoscalingV2HorizontalPodAutoscalerForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAutoscalingV2HorizontalPodAutoscalerForAllNamespacesResponse = union(enum) {
        ok: ListAutoscalingV2HorizontalPodAutoscalerForAllNamespacesResponse200,
        unauthorized: ListAutoscalingV2HorizontalPodAutoscalerForAllNamespacesResponse401,
        default_response: ListAutoscalingV2HorizontalPodAutoscalerForAllNamespacesResponseDefault,
    };

    pub const listAutoscalingV2HorizontalPodAutoscalerForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind HorizontalPodAutoscaler
    pub fn listAutoscalingV2HorizontalPodAutoscalerForAllNamespaces(self: *Client, options: listAutoscalingV2HorizontalPodAutoscalerForAllNamespacesOptions) !ListAutoscalingV2HorizontalPodAutoscalerForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/horizontalpodautoscalers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscalerList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAutoscalingV2HorizontalPodAutoscalerForAllNamespaces(self: *Client, options: listAutoscalingV2HorizontalPodAutoscalerForAllNamespacesOptions) !WatchStream(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/autoscaling/v2/horizontalpodautoscalers");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscalerList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListAutoscalingV2NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: ListAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200,
        unauthorized: ListAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401,
        default_response: ListAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const listAutoscalingV2NamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind HorizontalPodAutoscaler
    pub fn listAutoscalingV2NamespacedHorizontalPodAutoscaler(self: *Client, namespace: []const u8, options: listAutoscalingV2NamespacedHorizontalPodAutoscalerOptions) !ListAutoscalingV2NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscalerList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistAutoscalingV2NamespacedHorizontalPodAutoscaler(self: *Client, namespace: []const u8, options: listAutoscalingV2NamespacedHorizontalPodAutoscalerOptions) !WatchStream(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/autoscaling/v2/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/horizontalpodautoscalers");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAutoscalingV2NamespacedHorizontalPodAutoscalerResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAutoscalingV2NamespacedHorizontalPodAutoscalerResponse202 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateAutoscalingV2NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: CreateAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200,
        created: CreateAutoscalingV2NamespacedHorizontalPodAutoscalerResponse201,
        accepted: CreateAutoscalingV2NamespacedHorizontalPodAutoscalerResponse202,
        unauthorized: CreateAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401,
        default_response: CreateAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const createAutoscalingV2NamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a HorizontalPodAutoscaler
    pub fn createAutoscalingV2NamespacedHorizontalPodAutoscaler(self: *Client, namespace: []const u8, body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, options: createAutoscalingV2NamespacedHorizontalPodAutoscalerOptions) !CreateAutoscalingV2NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAutoscalingV2CollectionNamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV2CollectionNamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV2CollectionNamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV2CollectionNamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: DeleteAutoscalingV2CollectionNamespacedHorizontalPodAutoscalerResponse200,
        unauthorized: DeleteAutoscalingV2CollectionNamespacedHorizontalPodAutoscalerResponse401,
        default_response: DeleteAutoscalingV2CollectionNamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const deleteAutoscalingV2CollectionNamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of HorizontalPodAutoscaler
    pub fn deleteAutoscalingV2CollectionNamespacedHorizontalPodAutoscaler(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAutoscalingV2CollectionNamespacedHorizontalPodAutoscalerOptions) !DeleteAutoscalingV2CollectionNamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAutoscalingV2NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: ReadAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200,
        unauthorized: ReadAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401,
        default_response: ReadAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const readAutoscalingV2NamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified HorizontalPodAutoscaler
    pub fn readAutoscalingV2NamespacedHorizontalPodAutoscaler(self: *Client, name: []const u8, namespace: []const u8, options: readAutoscalingV2NamespacedHorizontalPodAutoscalerOptions) !ReadAutoscalingV2NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200,
        created: ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerResponse201,
        unauthorized: ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401,
        default_response: ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const replaceAutoscalingV2NamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified HorizontalPodAutoscaler
    pub fn replaceAutoscalingV2NamespacedHorizontalPodAutoscaler(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, options: replaceAutoscalingV2NamespacedHorizontalPodAutoscalerOptions) !ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV2NamespacedHorizontalPodAutoscalerResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteAutoscalingV2NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: DeleteAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200,
        accepted: DeleteAutoscalingV2NamespacedHorizontalPodAutoscalerResponse202,
        unauthorized: DeleteAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401,
        default_response: DeleteAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const deleteAutoscalingV2NamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a HorizontalPodAutoscaler
    pub fn deleteAutoscalingV2NamespacedHorizontalPodAutoscaler(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteAutoscalingV2NamespacedHorizontalPodAutoscalerOptions) !DeleteAutoscalingV2NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: PatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200,
        created: PatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse201,
        unauthorized: PatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401,
        default_response: PatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const patchAutoscalingV2NamespacedHorizontalPodAutoscalerOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified HorizontalPodAutoscaler
    pub fn patchAutoscalingV2NamespacedHorizontalPodAutoscaler(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAutoscalingV2NamespacedHorizontalPodAutoscalerOptions) !PatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse = union(enum) {
        ok: ReadAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse200,
        unauthorized: ReadAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse401,
        default_response: ReadAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponseDefault,
    };

    pub const readAutoscalingV2NamespacedHorizontalPodAutoscalerStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified HorizontalPodAutoscaler
    pub fn readAutoscalingV2NamespacedHorizontalPodAutoscalerStatus(self: *Client, name: []const u8, namespace: []const u8, options: readAutoscalingV2NamespacedHorizontalPodAutoscalerStatusOptions) !ReadAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse = union(enum) {
        ok: ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse200,
        created: ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse201,
        unauthorized: ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse401,
        default_response: ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponseDefault,
    };

    pub const replaceAutoscalingV2NamespacedHorizontalPodAutoscalerStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified HorizontalPodAutoscaler
    pub fn replaceAutoscalingV2NamespacedHorizontalPodAutoscalerStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, options: replaceAutoscalingV2NamespacedHorizontalPodAutoscalerStatusOptions) !ReplaceAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse200 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse201 = struct {
        body: root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse = union(enum) {
        ok: PatchAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse200,
        created: PatchAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse201,
        unauthorized: PatchAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse401,
        default_response: PatchAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponseDefault,
    };

    pub const patchAutoscalingV2NamespacedHorizontalPodAutoscalerStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified HorizontalPodAutoscaler
    pub fn patchAutoscalingV2NamespacedHorizontalPodAutoscalerStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchAutoscalingV2NamespacedHorizontalPodAutoscalerStatusOptions) !PatchAutoscalingV2NamespacedHorizontalPodAutoscalerStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.autoscaling.v2.HorizontalPodAutoscaler, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAutoscalingV2HorizontalPodAutoscalerListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV2HorizontalPodAutoscalerListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV2HorizontalPodAutoscalerListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV2HorizontalPodAutoscalerListForAllNamespacesResponse = union(enum) {
        ok: WatchAutoscalingV2HorizontalPodAutoscalerListForAllNamespacesResponse200,
        unauthorized: WatchAutoscalingV2HorizontalPodAutoscalerListForAllNamespacesResponse401,
        default_response: WatchAutoscalingV2HorizontalPodAutoscalerListForAllNamespacesResponseDefault,
    };

    pub const watchAutoscalingV2HorizontalPodAutoscalerListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of HorizontalPodAutoscaler. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAutoscalingV2HorizontalPodAutoscalerListForAllNamespaces(self: *Client, options: watchAutoscalingV2HorizontalPodAutoscalerListForAllNamespacesOptions) !WatchAutoscalingV2HorizontalPodAutoscalerListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/watch/horizontalpodautoscalers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAutoscalingV2NamespacedHorizontalPodAutoscalerListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV2NamespacedHorizontalPodAutoscalerListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV2NamespacedHorizontalPodAutoscalerListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV2NamespacedHorizontalPodAutoscalerListResponse = union(enum) {
        ok: WatchAutoscalingV2NamespacedHorizontalPodAutoscalerListResponse200,
        unauthorized: WatchAutoscalingV2NamespacedHorizontalPodAutoscalerListResponse401,
        default_response: WatchAutoscalingV2NamespacedHorizontalPodAutoscalerListResponseDefault,
    };

    pub const watchAutoscalingV2NamespacedHorizontalPodAutoscalerListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of HorizontalPodAutoscaler. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchAutoscalingV2NamespacedHorizontalPodAutoscalerList(self: *Client, namespace: []const u8, options: watchAutoscalingV2NamespacedHorizontalPodAutoscalerListOptions) !WatchAutoscalingV2NamespacedHorizontalPodAutoscalerListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse = union(enum) {
        ok: WatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse200,
        unauthorized: WatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse401,
        default_response: WatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponseDefault,
    };

    pub const watchAutoscalingV2NamespacedHorizontalPodAutoscalerOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind HorizontalPodAutoscaler. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchAutoscalingV2NamespacedHorizontalPodAutoscaler(self: *Client, name: []const u8, namespace: []const u8, options: watchAutoscalingV2NamespacedHorizontalPodAutoscalerOptions) !WatchAutoscalingV2NamespacedHorizontalPodAutoscalerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/autoscaling/v2/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/horizontalpodautoscalers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetBatchAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetBatchAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetBatchAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetBatchAPIGroupResponse = union(enum) {
        ok: GetBatchAPIGroupResponse200,
        unauthorized: GetBatchAPIGroupResponse401,
        default_response: GetBatchAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getBatchAPIGroup(self: *Client) !GetBatchAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetBatchV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetBatchV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetBatchV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetBatchV1APIResourcesResponse = union(enum) {
        ok: GetBatchV1APIResourcesResponse200,
        unauthorized: GetBatchV1APIResourcesResponse401,
        default_response: GetBatchV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getBatchV1APIResources(self: *Client) !GetBatchV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListBatchV1CronJobForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.batch.v1.CronJobList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListBatchV1CronJobForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListBatchV1CronJobForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListBatchV1CronJobForAllNamespacesResponse = union(enum) {
        ok: ListBatchV1CronJobForAllNamespacesResponse200,
        unauthorized: ListBatchV1CronJobForAllNamespacesResponse401,
        default_response: ListBatchV1CronJobForAllNamespacesResponseDefault,
    };

    pub const listBatchV1CronJobForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind CronJob
    pub fn listBatchV1CronJobForAllNamespaces(self: *Client, options: listBatchV1CronJobForAllNamespacesOptions) !ListBatchV1CronJobForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/cronjobs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJobList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistBatchV1CronJobForAllNamespaces(self: *Client, options: listBatchV1CronJobForAllNamespacesOptions) !WatchStream(root.io.k8s.api.batch.v1.CronJob) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/batch/v1/cronjobs");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.batch.v1.CronJob){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListBatchV1JobForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.batch.v1.JobList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListBatchV1JobForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListBatchV1JobForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListBatchV1JobForAllNamespacesResponse = union(enum) {
        ok: ListBatchV1JobForAllNamespacesResponse200,
        unauthorized: ListBatchV1JobForAllNamespacesResponse401,
        default_response: ListBatchV1JobForAllNamespacesResponseDefault,
    };

    pub const listBatchV1JobForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Job
    pub fn listBatchV1JobForAllNamespaces(self: *Client, options: listBatchV1JobForAllNamespacesOptions) !ListBatchV1JobForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/jobs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.JobList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistBatchV1JobForAllNamespaces(self: *Client, options: listBatchV1JobForAllNamespacesOptions) !WatchStream(root.io.k8s.api.batch.v1.Job) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/batch/v1/jobs");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.batch.v1.Job){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListBatchV1NamespacedCronJobResponse200 = struct {
        body: root.io.k8s.api.batch.v1.CronJobList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListBatchV1NamespacedCronJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListBatchV1NamespacedCronJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListBatchV1NamespacedCronJobResponse = union(enum) {
        ok: ListBatchV1NamespacedCronJobResponse200,
        unauthorized: ListBatchV1NamespacedCronJobResponse401,
        default_response: ListBatchV1NamespacedCronJobResponseDefault,
    };

    pub const listBatchV1NamespacedCronJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind CronJob
    pub fn listBatchV1NamespacedCronJob(self: *Client, namespace: []const u8, options: listBatchV1NamespacedCronJobOptions) !ListBatchV1NamespacedCronJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/cronjobs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJobList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistBatchV1NamespacedCronJob(self: *Client, namespace: []const u8, options: listBatchV1NamespacedCronJobOptions) !WatchStream(root.io.k8s.api.batch.v1.CronJob) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/batch/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/cronjobs");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.batch.v1.CronJob){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateBatchV1NamespacedCronJobResponse200 = struct {
        body: root.io.k8s.api.batch.v1.CronJob,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateBatchV1NamespacedCronJobResponse201 = struct {
        body: root.io.k8s.api.batch.v1.CronJob,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateBatchV1NamespacedCronJobResponse202 = struct {
        body: root.io.k8s.api.batch.v1.CronJob,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateBatchV1NamespacedCronJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateBatchV1NamespacedCronJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateBatchV1NamespacedCronJobResponse = union(enum) {
        ok: CreateBatchV1NamespacedCronJobResponse200,
        created: CreateBatchV1NamespacedCronJobResponse201,
        accepted: CreateBatchV1NamespacedCronJobResponse202,
        unauthorized: CreateBatchV1NamespacedCronJobResponse401,
        default_response: CreateBatchV1NamespacedCronJobResponseDefault,
    };

    pub const createBatchV1NamespacedCronJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a CronJob
    pub fn createBatchV1NamespacedCronJob(self: *Client, namespace: []const u8, body: root.io.k8s.api.batch.v1.CronJob, options: createBatchV1NamespacedCronJobOptions) !CreateBatchV1NamespacedCronJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/cronjobs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJob, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJob, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJob, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteBatchV1CollectionNamespacedCronJobResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1CollectionNamespacedCronJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1CollectionNamespacedCronJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1CollectionNamespacedCronJobResponse = union(enum) {
        ok: DeleteBatchV1CollectionNamespacedCronJobResponse200,
        unauthorized: DeleteBatchV1CollectionNamespacedCronJobResponse401,
        default_response: DeleteBatchV1CollectionNamespacedCronJobResponseDefault,
    };

    pub const deleteBatchV1CollectionNamespacedCronJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of CronJob
    pub fn deleteBatchV1CollectionNamespacedCronJob(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteBatchV1CollectionNamespacedCronJobOptions) !DeleteBatchV1CollectionNamespacedCronJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/cronjobs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadBatchV1NamespacedCronJobResponse200 = struct {
        body: root.io.k8s.api.batch.v1.CronJob,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadBatchV1NamespacedCronJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadBatchV1NamespacedCronJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadBatchV1NamespacedCronJobResponse = union(enum) {
        ok: ReadBatchV1NamespacedCronJobResponse200,
        unauthorized: ReadBatchV1NamespacedCronJobResponse401,
        default_response: ReadBatchV1NamespacedCronJobResponseDefault,
    };

    pub const readBatchV1NamespacedCronJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified CronJob
    pub fn readBatchV1NamespacedCronJob(self: *Client, name: []const u8, namespace: []const u8, options: readBatchV1NamespacedCronJobOptions) !ReadBatchV1NamespacedCronJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/cronjobs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJob, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceBatchV1NamespacedCronJobResponse200 = struct {
        body: root.io.k8s.api.batch.v1.CronJob,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedCronJobResponse201 = struct {
        body: root.io.k8s.api.batch.v1.CronJob,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedCronJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedCronJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedCronJobResponse = union(enum) {
        ok: ReplaceBatchV1NamespacedCronJobResponse200,
        created: ReplaceBatchV1NamespacedCronJobResponse201,
        unauthorized: ReplaceBatchV1NamespacedCronJobResponse401,
        default_response: ReplaceBatchV1NamespacedCronJobResponseDefault,
    };

    pub const replaceBatchV1NamespacedCronJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified CronJob
    pub fn replaceBatchV1NamespacedCronJob(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.batch.v1.CronJob, options: replaceBatchV1NamespacedCronJobOptions) !ReplaceBatchV1NamespacedCronJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/cronjobs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJob, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJob, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteBatchV1NamespacedCronJobResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1NamespacedCronJobResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1NamespacedCronJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1NamespacedCronJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1NamespacedCronJobResponse = union(enum) {
        ok: DeleteBatchV1NamespacedCronJobResponse200,
        accepted: DeleteBatchV1NamespacedCronJobResponse202,
        unauthorized: DeleteBatchV1NamespacedCronJobResponse401,
        default_response: DeleteBatchV1NamespacedCronJobResponseDefault,
    };

    pub const deleteBatchV1NamespacedCronJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a CronJob
    pub fn deleteBatchV1NamespacedCronJob(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteBatchV1NamespacedCronJobOptions) !DeleteBatchV1NamespacedCronJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/cronjobs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchBatchV1NamespacedCronJobResponse200 = struct {
        body: root.io.k8s.api.batch.v1.CronJob,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedCronJobResponse201 = struct {
        body: root.io.k8s.api.batch.v1.CronJob,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedCronJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedCronJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedCronJobResponse = union(enum) {
        ok: PatchBatchV1NamespacedCronJobResponse200,
        created: PatchBatchV1NamespacedCronJobResponse201,
        unauthorized: PatchBatchV1NamespacedCronJobResponse401,
        default_response: PatchBatchV1NamespacedCronJobResponseDefault,
    };

    pub const patchBatchV1NamespacedCronJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified CronJob
    pub fn patchBatchV1NamespacedCronJob(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchBatchV1NamespacedCronJobOptions) !PatchBatchV1NamespacedCronJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/cronjobs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJob, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJob, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadBatchV1NamespacedCronJobStatusResponse200 = struct {
        body: root.io.k8s.api.batch.v1.CronJob,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadBatchV1NamespacedCronJobStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadBatchV1NamespacedCronJobStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadBatchV1NamespacedCronJobStatusResponse = union(enum) {
        ok: ReadBatchV1NamespacedCronJobStatusResponse200,
        unauthorized: ReadBatchV1NamespacedCronJobStatusResponse401,
        default_response: ReadBatchV1NamespacedCronJobStatusResponseDefault,
    };

    pub const readBatchV1NamespacedCronJobStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified CronJob
    pub fn readBatchV1NamespacedCronJobStatus(self: *Client, name: []const u8, namespace: []const u8, options: readBatchV1NamespacedCronJobStatusOptions) !ReadBatchV1NamespacedCronJobStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/cronjobs/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJob, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceBatchV1NamespacedCronJobStatusResponse200 = struct {
        body: root.io.k8s.api.batch.v1.CronJob,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedCronJobStatusResponse201 = struct {
        body: root.io.k8s.api.batch.v1.CronJob,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedCronJobStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedCronJobStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedCronJobStatusResponse = union(enum) {
        ok: ReplaceBatchV1NamespacedCronJobStatusResponse200,
        created: ReplaceBatchV1NamespacedCronJobStatusResponse201,
        unauthorized: ReplaceBatchV1NamespacedCronJobStatusResponse401,
        default_response: ReplaceBatchV1NamespacedCronJobStatusResponseDefault,
    };

    pub const replaceBatchV1NamespacedCronJobStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified CronJob
    pub fn replaceBatchV1NamespacedCronJobStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.batch.v1.CronJob, options: replaceBatchV1NamespacedCronJobStatusOptions) !ReplaceBatchV1NamespacedCronJobStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/cronjobs/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJob, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJob, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchBatchV1NamespacedCronJobStatusResponse200 = struct {
        body: root.io.k8s.api.batch.v1.CronJob,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedCronJobStatusResponse201 = struct {
        body: root.io.k8s.api.batch.v1.CronJob,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedCronJobStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedCronJobStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedCronJobStatusResponse = union(enum) {
        ok: PatchBatchV1NamespacedCronJobStatusResponse200,
        created: PatchBatchV1NamespacedCronJobStatusResponse201,
        unauthorized: PatchBatchV1NamespacedCronJobStatusResponse401,
        default_response: PatchBatchV1NamespacedCronJobStatusResponseDefault,
    };

    pub const patchBatchV1NamespacedCronJobStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified CronJob
    pub fn patchBatchV1NamespacedCronJobStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchBatchV1NamespacedCronJobStatusOptions) !PatchBatchV1NamespacedCronJobStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/cronjobs/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJob, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.CronJob, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListBatchV1NamespacedJobResponse200 = struct {
        body: root.io.k8s.api.batch.v1.JobList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListBatchV1NamespacedJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListBatchV1NamespacedJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListBatchV1NamespacedJobResponse = union(enum) {
        ok: ListBatchV1NamespacedJobResponse200,
        unauthorized: ListBatchV1NamespacedJobResponse401,
        default_response: ListBatchV1NamespacedJobResponseDefault,
    };

    pub const listBatchV1NamespacedJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Job
    pub fn listBatchV1NamespacedJob(self: *Client, namespace: []const u8, options: listBatchV1NamespacedJobOptions) !ListBatchV1NamespacedJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/jobs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.JobList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistBatchV1NamespacedJob(self: *Client, namespace: []const u8, options: listBatchV1NamespacedJobOptions) !WatchStream(root.io.k8s.api.batch.v1.Job) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/batch/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/jobs");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.batch.v1.Job){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateBatchV1NamespacedJobResponse200 = struct {
        body: root.io.k8s.api.batch.v1.Job,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateBatchV1NamespacedJobResponse201 = struct {
        body: root.io.k8s.api.batch.v1.Job,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateBatchV1NamespacedJobResponse202 = struct {
        body: root.io.k8s.api.batch.v1.Job,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateBatchV1NamespacedJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateBatchV1NamespacedJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateBatchV1NamespacedJobResponse = union(enum) {
        ok: CreateBatchV1NamespacedJobResponse200,
        created: CreateBatchV1NamespacedJobResponse201,
        accepted: CreateBatchV1NamespacedJobResponse202,
        unauthorized: CreateBatchV1NamespacedJobResponse401,
        default_response: CreateBatchV1NamespacedJobResponseDefault,
    };

    pub const createBatchV1NamespacedJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a Job
    pub fn createBatchV1NamespacedJob(self: *Client, namespace: []const u8, body: root.io.k8s.api.batch.v1.Job, options: createBatchV1NamespacedJobOptions) !CreateBatchV1NamespacedJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/jobs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.Job, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.Job, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.Job, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteBatchV1CollectionNamespacedJobResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1CollectionNamespacedJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1CollectionNamespacedJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1CollectionNamespacedJobResponse = union(enum) {
        ok: DeleteBatchV1CollectionNamespacedJobResponse200,
        unauthorized: DeleteBatchV1CollectionNamespacedJobResponse401,
        default_response: DeleteBatchV1CollectionNamespacedJobResponseDefault,
    };

    pub const deleteBatchV1CollectionNamespacedJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Job
    pub fn deleteBatchV1CollectionNamespacedJob(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteBatchV1CollectionNamespacedJobOptions) !DeleteBatchV1CollectionNamespacedJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/jobs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadBatchV1NamespacedJobResponse200 = struct {
        body: root.io.k8s.api.batch.v1.Job,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadBatchV1NamespacedJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadBatchV1NamespacedJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadBatchV1NamespacedJobResponse = union(enum) {
        ok: ReadBatchV1NamespacedJobResponse200,
        unauthorized: ReadBatchV1NamespacedJobResponse401,
        default_response: ReadBatchV1NamespacedJobResponseDefault,
    };

    pub const readBatchV1NamespacedJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Job
    pub fn readBatchV1NamespacedJob(self: *Client, name: []const u8, namespace: []const u8, options: readBatchV1NamespacedJobOptions) !ReadBatchV1NamespacedJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/jobs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.Job, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceBatchV1NamespacedJobResponse200 = struct {
        body: root.io.k8s.api.batch.v1.Job,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedJobResponse201 = struct {
        body: root.io.k8s.api.batch.v1.Job,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedJobResponse = union(enum) {
        ok: ReplaceBatchV1NamespacedJobResponse200,
        created: ReplaceBatchV1NamespacedJobResponse201,
        unauthorized: ReplaceBatchV1NamespacedJobResponse401,
        default_response: ReplaceBatchV1NamespacedJobResponseDefault,
    };

    pub const replaceBatchV1NamespacedJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Job
    pub fn replaceBatchV1NamespacedJob(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.batch.v1.Job, options: replaceBatchV1NamespacedJobOptions) !ReplaceBatchV1NamespacedJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/jobs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.Job, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.Job, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteBatchV1NamespacedJobResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1NamespacedJobResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1NamespacedJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1NamespacedJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteBatchV1NamespacedJobResponse = union(enum) {
        ok: DeleteBatchV1NamespacedJobResponse200,
        accepted: DeleteBatchV1NamespacedJobResponse202,
        unauthorized: DeleteBatchV1NamespacedJobResponse401,
        default_response: DeleteBatchV1NamespacedJobResponseDefault,
    };

    pub const deleteBatchV1NamespacedJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a Job
    pub fn deleteBatchV1NamespacedJob(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteBatchV1NamespacedJobOptions) !DeleteBatchV1NamespacedJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/jobs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchBatchV1NamespacedJobResponse200 = struct {
        body: root.io.k8s.api.batch.v1.Job,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedJobResponse201 = struct {
        body: root.io.k8s.api.batch.v1.Job,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedJobResponse = union(enum) {
        ok: PatchBatchV1NamespacedJobResponse200,
        created: PatchBatchV1NamespacedJobResponse201,
        unauthorized: PatchBatchV1NamespacedJobResponse401,
        default_response: PatchBatchV1NamespacedJobResponseDefault,
    };

    pub const patchBatchV1NamespacedJobOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Job
    pub fn patchBatchV1NamespacedJob(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchBatchV1NamespacedJobOptions) !PatchBatchV1NamespacedJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/jobs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.Job, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.Job, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadBatchV1NamespacedJobStatusResponse200 = struct {
        body: root.io.k8s.api.batch.v1.Job,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadBatchV1NamespacedJobStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadBatchV1NamespacedJobStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadBatchV1NamespacedJobStatusResponse = union(enum) {
        ok: ReadBatchV1NamespacedJobStatusResponse200,
        unauthorized: ReadBatchV1NamespacedJobStatusResponse401,
        default_response: ReadBatchV1NamespacedJobStatusResponseDefault,
    };

    pub const readBatchV1NamespacedJobStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified Job
    pub fn readBatchV1NamespacedJobStatus(self: *Client, name: []const u8, namespace: []const u8, options: readBatchV1NamespacedJobStatusOptions) !ReadBatchV1NamespacedJobStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/jobs/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.Job, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceBatchV1NamespacedJobStatusResponse200 = struct {
        body: root.io.k8s.api.batch.v1.Job,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedJobStatusResponse201 = struct {
        body: root.io.k8s.api.batch.v1.Job,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedJobStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedJobStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceBatchV1NamespacedJobStatusResponse = union(enum) {
        ok: ReplaceBatchV1NamespacedJobStatusResponse200,
        created: ReplaceBatchV1NamespacedJobStatusResponse201,
        unauthorized: ReplaceBatchV1NamespacedJobStatusResponse401,
        default_response: ReplaceBatchV1NamespacedJobStatusResponseDefault,
    };

    pub const replaceBatchV1NamespacedJobStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified Job
    pub fn replaceBatchV1NamespacedJobStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.batch.v1.Job, options: replaceBatchV1NamespacedJobStatusOptions) !ReplaceBatchV1NamespacedJobStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/jobs/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.Job, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.Job, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchBatchV1NamespacedJobStatusResponse200 = struct {
        body: root.io.k8s.api.batch.v1.Job,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedJobStatusResponse201 = struct {
        body: root.io.k8s.api.batch.v1.Job,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedJobStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedJobStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchBatchV1NamespacedJobStatusResponse = union(enum) {
        ok: PatchBatchV1NamespacedJobStatusResponse200,
        created: PatchBatchV1NamespacedJobStatusResponse201,
        unauthorized: PatchBatchV1NamespacedJobStatusResponse401,
        default_response: PatchBatchV1NamespacedJobStatusResponseDefault,
    };

    pub const patchBatchV1NamespacedJobStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified Job
    pub fn patchBatchV1NamespacedJobStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchBatchV1NamespacedJobStatusOptions) !PatchBatchV1NamespacedJobStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/jobs/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.Job, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.batch.v1.Job, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchBatchV1CronJobListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1CronJobListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1CronJobListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1CronJobListForAllNamespacesResponse = union(enum) {
        ok: WatchBatchV1CronJobListForAllNamespacesResponse200,
        unauthorized: WatchBatchV1CronJobListForAllNamespacesResponse401,
        default_response: WatchBatchV1CronJobListForAllNamespacesResponseDefault,
    };

    pub const watchBatchV1CronJobListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of CronJob. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchBatchV1CronJobListForAllNamespaces(self: *Client, options: watchBatchV1CronJobListForAllNamespacesOptions) !WatchBatchV1CronJobListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/watch/cronjobs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchBatchV1JobListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1JobListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1JobListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1JobListForAllNamespacesResponse = union(enum) {
        ok: WatchBatchV1JobListForAllNamespacesResponse200,
        unauthorized: WatchBatchV1JobListForAllNamespacesResponse401,
        default_response: WatchBatchV1JobListForAllNamespacesResponseDefault,
    };

    pub const watchBatchV1JobListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Job. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchBatchV1JobListForAllNamespaces(self: *Client, options: watchBatchV1JobListForAllNamespacesOptions) !WatchBatchV1JobListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/watch/jobs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchBatchV1NamespacedCronJobListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1NamespacedCronJobListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1NamespacedCronJobListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1NamespacedCronJobListResponse = union(enum) {
        ok: WatchBatchV1NamespacedCronJobListResponse200,
        unauthorized: WatchBatchV1NamespacedCronJobListResponse401,
        default_response: WatchBatchV1NamespacedCronJobListResponseDefault,
    };

    pub const watchBatchV1NamespacedCronJobListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of CronJob. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchBatchV1NamespacedCronJobList(self: *Client, namespace: []const u8, options: watchBatchV1NamespacedCronJobListOptions) !WatchBatchV1NamespacedCronJobListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/cronjobs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchBatchV1NamespacedCronJobResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1NamespacedCronJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1NamespacedCronJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1NamespacedCronJobResponse = union(enum) {
        ok: WatchBatchV1NamespacedCronJobResponse200,
        unauthorized: WatchBatchV1NamespacedCronJobResponse401,
        default_response: WatchBatchV1NamespacedCronJobResponseDefault,
    };

    pub const watchBatchV1NamespacedCronJobOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind CronJob. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchBatchV1NamespacedCronJob(self: *Client, name: []const u8, namespace: []const u8, options: watchBatchV1NamespacedCronJobOptions) !WatchBatchV1NamespacedCronJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/cronjobs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchBatchV1NamespacedJobListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1NamespacedJobListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1NamespacedJobListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1NamespacedJobListResponse = union(enum) {
        ok: WatchBatchV1NamespacedJobListResponse200,
        unauthorized: WatchBatchV1NamespacedJobListResponse401,
        default_response: WatchBatchV1NamespacedJobListResponseDefault,
    };

    pub const watchBatchV1NamespacedJobListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Job. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchBatchV1NamespacedJobList(self: *Client, namespace: []const u8, options: watchBatchV1NamespacedJobListOptions) !WatchBatchV1NamespacedJobListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/jobs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchBatchV1NamespacedJobResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1NamespacedJobResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1NamespacedJobResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchBatchV1NamespacedJobResponse = union(enum) {
        ok: WatchBatchV1NamespacedJobResponse200,
        unauthorized: WatchBatchV1NamespacedJobResponse401,
        default_response: WatchBatchV1NamespacedJobResponseDefault,
    };

    pub const watchBatchV1NamespacedJobOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Job. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchBatchV1NamespacedJob(self: *Client, name: []const u8, namespace: []const u8, options: watchBatchV1NamespacedJobOptions) !WatchBatchV1NamespacedJobResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/batch/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/jobs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetCertificatesAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCertificatesAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCertificatesAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCertificatesAPIGroupResponse = union(enum) {
        ok: GetCertificatesAPIGroupResponse200,
        unauthorized: GetCertificatesAPIGroupResponse401,
        default_response: GetCertificatesAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getCertificatesAPIGroup(self: *Client) !GetCertificatesAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetCertificatesV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCertificatesV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCertificatesV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCertificatesV1APIResourcesResponse = union(enum) {
        ok: GetCertificatesV1APIResourcesResponse200,
        unauthorized: GetCertificatesV1APIResourcesResponse401,
        default_response: GetCertificatesV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getCertificatesV1APIResources(self: *Client) !GetCertificatesV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCertificatesV1CertificateSigningRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequestList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1CertificateSigningRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1CertificateSigningRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1CertificateSigningRequestResponse = union(enum) {
        ok: ListCertificatesV1CertificateSigningRequestResponse200,
        unauthorized: ListCertificatesV1CertificateSigningRequestResponse401,
        default_response: ListCertificatesV1CertificateSigningRequestResponseDefault,
    };

    pub const listCertificatesV1CertificateSigningRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind CertificateSigningRequest
    pub fn listCertificatesV1CertificateSigningRequest(self: *Client, options: listCertificatesV1CertificateSigningRequestOptions) !ListCertificatesV1CertificateSigningRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/certificatesigningrequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequestList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCertificatesV1CertificateSigningRequest(self: *Client, options: listCertificatesV1CertificateSigningRequestOptions) !WatchStream(root.io.k8s.api.certificates.v1.CertificateSigningRequest) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/certificates.k8s.io/v1/certificatesigningrequests");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.certificates.v1.CertificateSigningRequest){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCertificatesV1CertificateSigningRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1CertificateSigningRequestResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1CertificateSigningRequestResponse202 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1CertificateSigningRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1CertificateSigningRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1CertificateSigningRequestResponse = union(enum) {
        ok: CreateCertificatesV1CertificateSigningRequestResponse200,
        created: CreateCertificatesV1CertificateSigningRequestResponse201,
        accepted: CreateCertificatesV1CertificateSigningRequestResponse202,
        unauthorized: CreateCertificatesV1CertificateSigningRequestResponse401,
        default_response: CreateCertificatesV1CertificateSigningRequestResponseDefault,
    };

    pub const createCertificatesV1CertificateSigningRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a CertificateSigningRequest
    pub fn createCertificatesV1CertificateSigningRequest(self: *Client, body: root.io.k8s.api.certificates.v1.CertificateSigningRequest, options: createCertificatesV1CertificateSigningRequestOptions) !CreateCertificatesV1CertificateSigningRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/certificatesigningrequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCertificatesV1CollectionCertificateSigningRequestResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1CollectionCertificateSigningRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1CollectionCertificateSigningRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1CollectionCertificateSigningRequestResponse = union(enum) {
        ok: DeleteCertificatesV1CollectionCertificateSigningRequestResponse200,
        unauthorized: DeleteCertificatesV1CollectionCertificateSigningRequestResponse401,
        default_response: DeleteCertificatesV1CollectionCertificateSigningRequestResponseDefault,
    };

    pub const deleteCertificatesV1CollectionCertificateSigningRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of CertificateSigningRequest
    pub fn deleteCertificatesV1CollectionCertificateSigningRequest(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCertificatesV1CollectionCertificateSigningRequestOptions) !DeleteCertificatesV1CollectionCertificateSigningRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/certificatesigningrequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCertificatesV1CertificateSigningRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1CertificateSigningRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1CertificateSigningRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1CertificateSigningRequestResponse = union(enum) {
        ok: ReadCertificatesV1CertificateSigningRequestResponse200,
        unauthorized: ReadCertificatesV1CertificateSigningRequestResponse401,
        default_response: ReadCertificatesV1CertificateSigningRequestResponseDefault,
    };

    pub const readCertificatesV1CertificateSigningRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified CertificateSigningRequest
    pub fn readCertificatesV1CertificateSigningRequest(self: *Client, name: []const u8, options: readCertificatesV1CertificateSigningRequestOptions) !ReadCertificatesV1CertificateSigningRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/certificatesigningrequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCertificatesV1CertificateSigningRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1CertificateSigningRequestResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1CertificateSigningRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1CertificateSigningRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1CertificateSigningRequestResponse = union(enum) {
        ok: ReplaceCertificatesV1CertificateSigningRequestResponse200,
        created: ReplaceCertificatesV1CertificateSigningRequestResponse201,
        unauthorized: ReplaceCertificatesV1CertificateSigningRequestResponse401,
        default_response: ReplaceCertificatesV1CertificateSigningRequestResponseDefault,
    };

    pub const replaceCertificatesV1CertificateSigningRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified CertificateSigningRequest
    pub fn replaceCertificatesV1CertificateSigningRequest(self: *Client, name: []const u8, body: root.io.k8s.api.certificates.v1.CertificateSigningRequest, options: replaceCertificatesV1CertificateSigningRequestOptions) !ReplaceCertificatesV1CertificateSigningRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/certificatesigningrequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCertificatesV1CertificateSigningRequestResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1CertificateSigningRequestResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1CertificateSigningRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1CertificateSigningRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1CertificateSigningRequestResponse = union(enum) {
        ok: DeleteCertificatesV1CertificateSigningRequestResponse200,
        accepted: DeleteCertificatesV1CertificateSigningRequestResponse202,
        unauthorized: DeleteCertificatesV1CertificateSigningRequestResponse401,
        default_response: DeleteCertificatesV1CertificateSigningRequestResponseDefault,
    };

    pub const deleteCertificatesV1CertificateSigningRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a CertificateSigningRequest
    pub fn deleteCertificatesV1CertificateSigningRequest(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCertificatesV1CertificateSigningRequestOptions) !DeleteCertificatesV1CertificateSigningRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/certificatesigningrequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCertificatesV1CertificateSigningRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1CertificateSigningRequestResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1CertificateSigningRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1CertificateSigningRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1CertificateSigningRequestResponse = union(enum) {
        ok: PatchCertificatesV1CertificateSigningRequestResponse200,
        created: PatchCertificatesV1CertificateSigningRequestResponse201,
        unauthorized: PatchCertificatesV1CertificateSigningRequestResponse401,
        default_response: PatchCertificatesV1CertificateSigningRequestResponseDefault,
    };

    pub const patchCertificatesV1CertificateSigningRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified CertificateSigningRequest
    pub fn patchCertificatesV1CertificateSigningRequest(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCertificatesV1CertificateSigningRequestOptions) !PatchCertificatesV1CertificateSigningRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/certificatesigningrequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCertificatesV1CertificateSigningRequestApprovalResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1CertificateSigningRequestApprovalResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1CertificateSigningRequestApprovalResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1CertificateSigningRequestApprovalResponse = union(enum) {
        ok: ReadCertificatesV1CertificateSigningRequestApprovalResponse200,
        unauthorized: ReadCertificatesV1CertificateSigningRequestApprovalResponse401,
        default_response: ReadCertificatesV1CertificateSigningRequestApprovalResponseDefault,
    };

    pub const readCertificatesV1CertificateSigningRequestApprovalOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read approval of the specified CertificateSigningRequest
    pub fn readCertificatesV1CertificateSigningRequestApproval(self: *Client, name: []const u8, options: readCertificatesV1CertificateSigningRequestApprovalOptions) !ReadCertificatesV1CertificateSigningRequestApprovalResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/certificatesigningrequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/approval", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCertificatesV1CertificateSigningRequestApprovalResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1CertificateSigningRequestApprovalResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1CertificateSigningRequestApprovalResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1CertificateSigningRequestApprovalResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1CertificateSigningRequestApprovalResponse = union(enum) {
        ok: ReplaceCertificatesV1CertificateSigningRequestApprovalResponse200,
        created: ReplaceCertificatesV1CertificateSigningRequestApprovalResponse201,
        unauthorized: ReplaceCertificatesV1CertificateSigningRequestApprovalResponse401,
        default_response: ReplaceCertificatesV1CertificateSigningRequestApprovalResponseDefault,
    };

    pub const replaceCertificatesV1CertificateSigningRequestApprovalOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace approval of the specified CertificateSigningRequest
    pub fn replaceCertificatesV1CertificateSigningRequestApproval(self: *Client, name: []const u8, body: root.io.k8s.api.certificates.v1.CertificateSigningRequest, options: replaceCertificatesV1CertificateSigningRequestApprovalOptions) !ReplaceCertificatesV1CertificateSigningRequestApprovalResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/certificatesigningrequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/approval", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCertificatesV1CertificateSigningRequestApprovalResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1CertificateSigningRequestApprovalResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1CertificateSigningRequestApprovalResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1CertificateSigningRequestApprovalResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1CertificateSigningRequestApprovalResponse = union(enum) {
        ok: PatchCertificatesV1CertificateSigningRequestApprovalResponse200,
        created: PatchCertificatesV1CertificateSigningRequestApprovalResponse201,
        unauthorized: PatchCertificatesV1CertificateSigningRequestApprovalResponse401,
        default_response: PatchCertificatesV1CertificateSigningRequestApprovalResponseDefault,
    };

    pub const patchCertificatesV1CertificateSigningRequestApprovalOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update approval of the specified CertificateSigningRequest
    pub fn patchCertificatesV1CertificateSigningRequestApproval(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCertificatesV1CertificateSigningRequestApprovalOptions) !PatchCertificatesV1CertificateSigningRequestApprovalResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/certificatesigningrequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/approval", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCertificatesV1CertificateSigningRequestStatusResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1CertificateSigningRequestStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1CertificateSigningRequestStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1CertificateSigningRequestStatusResponse = union(enum) {
        ok: ReadCertificatesV1CertificateSigningRequestStatusResponse200,
        unauthorized: ReadCertificatesV1CertificateSigningRequestStatusResponse401,
        default_response: ReadCertificatesV1CertificateSigningRequestStatusResponseDefault,
    };

    pub const readCertificatesV1CertificateSigningRequestStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified CertificateSigningRequest
    pub fn readCertificatesV1CertificateSigningRequestStatus(self: *Client, name: []const u8, options: readCertificatesV1CertificateSigningRequestStatusOptions) !ReadCertificatesV1CertificateSigningRequestStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/certificatesigningrequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCertificatesV1CertificateSigningRequestStatusResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1CertificateSigningRequestStatusResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1CertificateSigningRequestStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1CertificateSigningRequestStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1CertificateSigningRequestStatusResponse = union(enum) {
        ok: ReplaceCertificatesV1CertificateSigningRequestStatusResponse200,
        created: ReplaceCertificatesV1CertificateSigningRequestStatusResponse201,
        unauthorized: ReplaceCertificatesV1CertificateSigningRequestStatusResponse401,
        default_response: ReplaceCertificatesV1CertificateSigningRequestStatusResponseDefault,
    };

    pub const replaceCertificatesV1CertificateSigningRequestStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified CertificateSigningRequest
    pub fn replaceCertificatesV1CertificateSigningRequestStatus(self: *Client, name: []const u8, body: root.io.k8s.api.certificates.v1.CertificateSigningRequest, options: replaceCertificatesV1CertificateSigningRequestStatusOptions) !ReplaceCertificatesV1CertificateSigningRequestStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/certificatesigningrequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCertificatesV1CertificateSigningRequestStatusResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1CertificateSigningRequestStatusResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.CertificateSigningRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1CertificateSigningRequestStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1CertificateSigningRequestStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1CertificateSigningRequestStatusResponse = union(enum) {
        ok: PatchCertificatesV1CertificateSigningRequestStatusResponse200,
        created: PatchCertificatesV1CertificateSigningRequestStatusResponse201,
        unauthorized: PatchCertificatesV1CertificateSigningRequestStatusResponse401,
        default_response: PatchCertificatesV1CertificateSigningRequestStatusResponseDefault,
    };

    pub const patchCertificatesV1CertificateSigningRequestStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified CertificateSigningRequest
    pub fn patchCertificatesV1CertificateSigningRequestStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCertificatesV1CertificateSigningRequestStatusOptions) !PatchCertificatesV1CertificateSigningRequestStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/certificatesigningrequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.CertificateSigningRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCertificatesV1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.ClusterTrustBundleList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1ClusterTrustBundleResponse = union(enum) {
        ok: ListCertificatesV1ClusterTrustBundleResponse200,
        unauthorized: ListCertificatesV1ClusterTrustBundleResponse401,
        default_response: ListCertificatesV1ClusterTrustBundleResponseDefault,
    };

    pub const listCertificatesV1ClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ClusterTrustBundle
    pub fn listCertificatesV1ClusterTrustBundle(self: *Client, options: listCertificatesV1ClusterTrustBundleOptions) !ListCertificatesV1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/clustertrustbundles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.ClusterTrustBundleList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCertificatesV1ClusterTrustBundle(self: *Client, options: listCertificatesV1ClusterTrustBundleOptions) !WatchStream(root.io.k8s.api.certificates.v1.ClusterTrustBundle) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/certificates.k8s.io/v1/clustertrustbundles");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.certificates.v1.ClusterTrustBundle){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCertificatesV1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1ClusterTrustBundleResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1ClusterTrustBundleResponse202 = struct {
        body: root.io.k8s.api.certificates.v1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1ClusterTrustBundleResponse = union(enum) {
        ok: CreateCertificatesV1ClusterTrustBundleResponse200,
        created: CreateCertificatesV1ClusterTrustBundleResponse201,
        accepted: CreateCertificatesV1ClusterTrustBundleResponse202,
        unauthorized: CreateCertificatesV1ClusterTrustBundleResponse401,
        default_response: CreateCertificatesV1ClusterTrustBundleResponseDefault,
    };

    pub const createCertificatesV1ClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ClusterTrustBundle
    pub fn createCertificatesV1ClusterTrustBundle(self: *Client, body: root.io.k8s.api.certificates.v1.ClusterTrustBundle, options: createCertificatesV1ClusterTrustBundleOptions) !CreateCertificatesV1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/clustertrustbundles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCertificatesV1CollectionClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1CollectionClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1CollectionClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1CollectionClusterTrustBundleResponse = union(enum) {
        ok: DeleteCertificatesV1CollectionClusterTrustBundleResponse200,
        unauthorized: DeleteCertificatesV1CollectionClusterTrustBundleResponse401,
        default_response: DeleteCertificatesV1CollectionClusterTrustBundleResponseDefault,
    };

    pub const deleteCertificatesV1CollectionClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ClusterTrustBundle
    pub fn deleteCertificatesV1CollectionClusterTrustBundle(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCertificatesV1CollectionClusterTrustBundleOptions) !DeleteCertificatesV1CollectionClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/clustertrustbundles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCertificatesV1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1ClusterTrustBundleResponse = union(enum) {
        ok: ReadCertificatesV1ClusterTrustBundleResponse200,
        unauthorized: ReadCertificatesV1ClusterTrustBundleResponse401,
        default_response: ReadCertificatesV1ClusterTrustBundleResponseDefault,
    };

    pub const readCertificatesV1ClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ClusterTrustBundle
    pub fn readCertificatesV1ClusterTrustBundle(self: *Client, name: []const u8, options: readCertificatesV1ClusterTrustBundleOptions) !ReadCertificatesV1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/clustertrustbundles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCertificatesV1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1ClusterTrustBundleResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1ClusterTrustBundleResponse = union(enum) {
        ok: ReplaceCertificatesV1ClusterTrustBundleResponse200,
        created: ReplaceCertificatesV1ClusterTrustBundleResponse201,
        unauthorized: ReplaceCertificatesV1ClusterTrustBundleResponse401,
        default_response: ReplaceCertificatesV1ClusterTrustBundleResponseDefault,
    };

    pub const replaceCertificatesV1ClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ClusterTrustBundle
    pub fn replaceCertificatesV1ClusterTrustBundle(self: *Client, name: []const u8, body: root.io.k8s.api.certificates.v1.ClusterTrustBundle, options: replaceCertificatesV1ClusterTrustBundleOptions) !ReplaceCertificatesV1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/clustertrustbundles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCertificatesV1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1ClusterTrustBundleResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1ClusterTrustBundleResponse = union(enum) {
        ok: DeleteCertificatesV1ClusterTrustBundleResponse200,
        accepted: DeleteCertificatesV1ClusterTrustBundleResponse202,
        unauthorized: DeleteCertificatesV1ClusterTrustBundleResponse401,
        default_response: DeleteCertificatesV1ClusterTrustBundleResponseDefault,
    };

    pub const deleteCertificatesV1ClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ClusterTrustBundle
    pub fn deleteCertificatesV1ClusterTrustBundle(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCertificatesV1ClusterTrustBundleOptions) !DeleteCertificatesV1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/clustertrustbundles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCertificatesV1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1ClusterTrustBundleResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1ClusterTrustBundleResponse = union(enum) {
        ok: PatchCertificatesV1ClusterTrustBundleResponse200,
        created: PatchCertificatesV1ClusterTrustBundleResponse201,
        unauthorized: PatchCertificatesV1ClusterTrustBundleResponse401,
        default_response: PatchCertificatesV1ClusterTrustBundleResponseDefault,
    };

    pub const patchCertificatesV1ClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ClusterTrustBundle
    pub fn patchCertificatesV1ClusterTrustBundle(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCertificatesV1ClusterTrustBundleOptions) !PatchCertificatesV1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/clustertrustbundles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCertificatesV1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequestList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: ListCertificatesV1NamespacedPodCertificateRequestResponse200,
        unauthorized: ListCertificatesV1NamespacedPodCertificateRequestResponse401,
        default_response: ListCertificatesV1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const listCertificatesV1NamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PodCertificateRequest
    pub fn listCertificatesV1NamespacedPodCertificateRequest(self: *Client, namespace: []const u8, options: listCertificatesV1NamespacedPodCertificateRequestOptions) !ListCertificatesV1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequestList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCertificatesV1NamespacedPodCertificateRequest(self: *Client, namespace: []const u8, options: listCertificatesV1NamespacedPodCertificateRequestOptions) !WatchStream(root.io.k8s.api.certificates.v1.PodCertificateRequest) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/certificates.k8s.io/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/podcertificaterequests");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.certificates.v1.PodCertificateRequest){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCertificatesV1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1NamespacedPodCertificateRequestResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1NamespacedPodCertificateRequestResponse202 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: CreateCertificatesV1NamespacedPodCertificateRequestResponse200,
        created: CreateCertificatesV1NamespacedPodCertificateRequestResponse201,
        accepted: CreateCertificatesV1NamespacedPodCertificateRequestResponse202,
        unauthorized: CreateCertificatesV1NamespacedPodCertificateRequestResponse401,
        default_response: CreateCertificatesV1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const createCertificatesV1NamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a PodCertificateRequest
    pub fn createCertificatesV1NamespacedPodCertificateRequest(self: *Client, namespace: []const u8, body: root.io.k8s.api.certificates.v1.PodCertificateRequest, options: createCertificatesV1NamespacedPodCertificateRequestOptions) !CreateCertificatesV1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCertificatesV1CollectionNamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1CollectionNamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1CollectionNamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1CollectionNamespacedPodCertificateRequestResponse = union(enum) {
        ok: DeleteCertificatesV1CollectionNamespacedPodCertificateRequestResponse200,
        unauthorized: DeleteCertificatesV1CollectionNamespacedPodCertificateRequestResponse401,
        default_response: DeleteCertificatesV1CollectionNamespacedPodCertificateRequestResponseDefault,
    };

    pub const deleteCertificatesV1CollectionNamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of PodCertificateRequest
    pub fn deleteCertificatesV1CollectionNamespacedPodCertificateRequest(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCertificatesV1CollectionNamespacedPodCertificateRequestOptions) !DeleteCertificatesV1CollectionNamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCertificatesV1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: ReadCertificatesV1NamespacedPodCertificateRequestResponse200,
        unauthorized: ReadCertificatesV1NamespacedPodCertificateRequestResponse401,
        default_response: ReadCertificatesV1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const readCertificatesV1NamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified PodCertificateRequest
    pub fn readCertificatesV1NamespacedPodCertificateRequest(self: *Client, name: []const u8, namespace: []const u8, options: readCertificatesV1NamespacedPodCertificateRequestOptions) !ReadCertificatesV1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCertificatesV1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1NamespacedPodCertificateRequestResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: ReplaceCertificatesV1NamespacedPodCertificateRequestResponse200,
        created: ReplaceCertificatesV1NamespacedPodCertificateRequestResponse201,
        unauthorized: ReplaceCertificatesV1NamespacedPodCertificateRequestResponse401,
        default_response: ReplaceCertificatesV1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const replaceCertificatesV1NamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified PodCertificateRequest
    pub fn replaceCertificatesV1NamespacedPodCertificateRequest(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.certificates.v1.PodCertificateRequest, options: replaceCertificatesV1NamespacedPodCertificateRequestOptions) !ReplaceCertificatesV1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCertificatesV1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1NamespacedPodCertificateRequestResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: DeleteCertificatesV1NamespacedPodCertificateRequestResponse200,
        accepted: DeleteCertificatesV1NamespacedPodCertificateRequestResponse202,
        unauthorized: DeleteCertificatesV1NamespacedPodCertificateRequestResponse401,
        default_response: DeleteCertificatesV1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const deleteCertificatesV1NamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a PodCertificateRequest
    pub fn deleteCertificatesV1NamespacedPodCertificateRequest(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCertificatesV1NamespacedPodCertificateRequestOptions) !DeleteCertificatesV1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCertificatesV1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1NamespacedPodCertificateRequestResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: PatchCertificatesV1NamespacedPodCertificateRequestResponse200,
        created: PatchCertificatesV1NamespacedPodCertificateRequestResponse201,
        unauthorized: PatchCertificatesV1NamespacedPodCertificateRequestResponse401,
        default_response: PatchCertificatesV1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const patchCertificatesV1NamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified PodCertificateRequest
    pub fn patchCertificatesV1NamespacedPodCertificateRequest(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCertificatesV1NamespacedPodCertificateRequestOptions) !PatchCertificatesV1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCertificatesV1NamespacedPodCertificateRequestStatusResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1NamespacedPodCertificateRequestStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1NamespacedPodCertificateRequestStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1NamespacedPodCertificateRequestStatusResponse = union(enum) {
        ok: ReadCertificatesV1NamespacedPodCertificateRequestStatusResponse200,
        unauthorized: ReadCertificatesV1NamespacedPodCertificateRequestStatusResponse401,
        default_response: ReadCertificatesV1NamespacedPodCertificateRequestStatusResponseDefault,
    };

    pub const readCertificatesV1NamespacedPodCertificateRequestStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified PodCertificateRequest
    pub fn readCertificatesV1NamespacedPodCertificateRequestStatus(self: *Client, name: []const u8, namespace: []const u8, options: readCertificatesV1NamespacedPodCertificateRequestStatusOptions) !ReadCertificatesV1NamespacedPodCertificateRequestStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCertificatesV1NamespacedPodCertificateRequestStatusResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1NamespacedPodCertificateRequestStatusResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1NamespacedPodCertificateRequestStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1NamespacedPodCertificateRequestStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1NamespacedPodCertificateRequestStatusResponse = union(enum) {
        ok: ReplaceCertificatesV1NamespacedPodCertificateRequestStatusResponse200,
        created: ReplaceCertificatesV1NamespacedPodCertificateRequestStatusResponse201,
        unauthorized: ReplaceCertificatesV1NamespacedPodCertificateRequestStatusResponse401,
        default_response: ReplaceCertificatesV1NamespacedPodCertificateRequestStatusResponseDefault,
    };

    pub const replaceCertificatesV1NamespacedPodCertificateRequestStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified PodCertificateRequest
    pub fn replaceCertificatesV1NamespacedPodCertificateRequestStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.certificates.v1.PodCertificateRequest, options: replaceCertificatesV1NamespacedPodCertificateRequestStatusOptions) !ReplaceCertificatesV1NamespacedPodCertificateRequestStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCertificatesV1NamespacedPodCertificateRequestStatusResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1NamespacedPodCertificateRequestStatusResponse201 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1NamespacedPodCertificateRequestStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1NamespacedPodCertificateRequestStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1NamespacedPodCertificateRequestStatusResponse = union(enum) {
        ok: PatchCertificatesV1NamespacedPodCertificateRequestStatusResponse200,
        created: PatchCertificatesV1NamespacedPodCertificateRequestStatusResponse201,
        unauthorized: PatchCertificatesV1NamespacedPodCertificateRequestStatusResponse401,
        default_response: PatchCertificatesV1NamespacedPodCertificateRequestStatusResponseDefault,
    };

    pub const patchCertificatesV1NamespacedPodCertificateRequestStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified PodCertificateRequest
    pub fn patchCertificatesV1NamespacedPodCertificateRequestStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCertificatesV1NamespacedPodCertificateRequestStatusOptions) !PatchCertificatesV1NamespacedPodCertificateRequestStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCertificatesV1PodCertificateRequestForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.certificates.v1.PodCertificateRequestList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1PodCertificateRequestForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1PodCertificateRequestForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1PodCertificateRequestForAllNamespacesResponse = union(enum) {
        ok: ListCertificatesV1PodCertificateRequestForAllNamespacesResponse200,
        unauthorized: ListCertificatesV1PodCertificateRequestForAllNamespacesResponse401,
        default_response: ListCertificatesV1PodCertificateRequestForAllNamespacesResponseDefault,
    };

    pub const listCertificatesV1PodCertificateRequestForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PodCertificateRequest
    pub fn listCertificatesV1PodCertificateRequestForAllNamespaces(self: *Client, options: listCertificatesV1PodCertificateRequestForAllNamespacesOptions) !ListCertificatesV1PodCertificateRequestForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/podcertificaterequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1.PodCertificateRequestList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCertificatesV1PodCertificateRequestForAllNamespaces(self: *Client, options: listCertificatesV1PodCertificateRequestForAllNamespacesOptions) !WatchStream(root.io.k8s.api.certificates.v1.PodCertificateRequest) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/certificates.k8s.io/v1/podcertificaterequests");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.certificates.v1.PodCertificateRequest){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const WatchCertificatesV1CertificateSigningRequestListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1CertificateSigningRequestListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1CertificateSigningRequestListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1CertificateSigningRequestListResponse = union(enum) {
        ok: WatchCertificatesV1CertificateSigningRequestListResponse200,
        unauthorized: WatchCertificatesV1CertificateSigningRequestListResponse401,
        default_response: WatchCertificatesV1CertificateSigningRequestListResponseDefault,
    };

    pub const watchCertificatesV1CertificateSigningRequestListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of CertificateSigningRequest. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCertificatesV1CertificateSigningRequestList(self: *Client, options: watchCertificatesV1CertificateSigningRequestListOptions) !WatchCertificatesV1CertificateSigningRequestListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/watch/certificatesigningrequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCertificatesV1CertificateSigningRequestResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1CertificateSigningRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1CertificateSigningRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1CertificateSigningRequestResponse = union(enum) {
        ok: WatchCertificatesV1CertificateSigningRequestResponse200,
        unauthorized: WatchCertificatesV1CertificateSigningRequestResponse401,
        default_response: WatchCertificatesV1CertificateSigningRequestResponseDefault,
    };

    pub const watchCertificatesV1CertificateSigningRequestOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind CertificateSigningRequest. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCertificatesV1CertificateSigningRequest(self: *Client, name: []const u8, options: watchCertificatesV1CertificateSigningRequestOptions) !WatchCertificatesV1CertificateSigningRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/watch/certificatesigningrequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCertificatesV1ClusterTrustBundleListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1ClusterTrustBundleListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1ClusterTrustBundleListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1ClusterTrustBundleListResponse = union(enum) {
        ok: WatchCertificatesV1ClusterTrustBundleListResponse200,
        unauthorized: WatchCertificatesV1ClusterTrustBundleListResponse401,
        default_response: WatchCertificatesV1ClusterTrustBundleListResponseDefault,
    };

    pub const watchCertificatesV1ClusterTrustBundleListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ClusterTrustBundle. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCertificatesV1ClusterTrustBundleList(self: *Client, options: watchCertificatesV1ClusterTrustBundleListOptions) !WatchCertificatesV1ClusterTrustBundleListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/watch/clustertrustbundles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCertificatesV1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1ClusterTrustBundleResponse = union(enum) {
        ok: WatchCertificatesV1ClusterTrustBundleResponse200,
        unauthorized: WatchCertificatesV1ClusterTrustBundleResponse401,
        default_response: WatchCertificatesV1ClusterTrustBundleResponseDefault,
    };

    pub const watchCertificatesV1ClusterTrustBundleOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ClusterTrustBundle. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCertificatesV1ClusterTrustBundle(self: *Client, name: []const u8, options: watchCertificatesV1ClusterTrustBundleOptions) !WatchCertificatesV1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/watch/clustertrustbundles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCertificatesV1NamespacedPodCertificateRequestListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1NamespacedPodCertificateRequestListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1NamespacedPodCertificateRequestListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1NamespacedPodCertificateRequestListResponse = union(enum) {
        ok: WatchCertificatesV1NamespacedPodCertificateRequestListResponse200,
        unauthorized: WatchCertificatesV1NamespacedPodCertificateRequestListResponse401,
        default_response: WatchCertificatesV1NamespacedPodCertificateRequestListResponseDefault,
    };

    pub const watchCertificatesV1NamespacedPodCertificateRequestListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PodCertificateRequest. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCertificatesV1NamespacedPodCertificateRequestList(self: *Client, namespace: []const u8, options: watchCertificatesV1NamespacedPodCertificateRequestListOptions) !WatchCertificatesV1NamespacedPodCertificateRequestListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCertificatesV1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: WatchCertificatesV1NamespacedPodCertificateRequestResponse200,
        unauthorized: WatchCertificatesV1NamespacedPodCertificateRequestResponse401,
        default_response: WatchCertificatesV1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const watchCertificatesV1NamespacedPodCertificateRequestOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind PodCertificateRequest. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCertificatesV1NamespacedPodCertificateRequest(self: *Client, name: []const u8, namespace: []const u8, options: watchCertificatesV1NamespacedPodCertificateRequestOptions) !WatchCertificatesV1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCertificatesV1PodCertificateRequestListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1PodCertificateRequestListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1PodCertificateRequestListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1PodCertificateRequestListForAllNamespacesResponse = union(enum) {
        ok: WatchCertificatesV1PodCertificateRequestListForAllNamespacesResponse200,
        unauthorized: WatchCertificatesV1PodCertificateRequestListForAllNamespacesResponse401,
        default_response: WatchCertificatesV1PodCertificateRequestListForAllNamespacesResponseDefault,
    };

    pub const watchCertificatesV1PodCertificateRequestListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PodCertificateRequest. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCertificatesV1PodCertificateRequestListForAllNamespaces(self: *Client, options: watchCertificatesV1PodCertificateRequestListForAllNamespacesOptions) !WatchCertificatesV1PodCertificateRequestListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1/watch/podcertificaterequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetCertificatesV1beta1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCertificatesV1beta1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCertificatesV1beta1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCertificatesV1beta1APIResourcesResponse = union(enum) {
        ok: GetCertificatesV1beta1APIResourcesResponse200,
        unauthorized: GetCertificatesV1beta1APIResourcesResponse401,
        default_response: GetCertificatesV1beta1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getCertificatesV1beta1APIResources(self: *Client) !GetCertificatesV1beta1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCertificatesV1beta1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.ClusterTrustBundleList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1beta1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1beta1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1beta1ClusterTrustBundleResponse = union(enum) {
        ok: ListCertificatesV1beta1ClusterTrustBundleResponse200,
        unauthorized: ListCertificatesV1beta1ClusterTrustBundleResponse401,
        default_response: ListCertificatesV1beta1ClusterTrustBundleResponseDefault,
    };

    pub const listCertificatesV1beta1ClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ClusterTrustBundle
    pub fn listCertificatesV1beta1ClusterTrustBundle(self: *Client, options: listCertificatesV1beta1ClusterTrustBundleOptions) !ListCertificatesV1beta1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/clustertrustbundles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.ClusterTrustBundleList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCertificatesV1beta1ClusterTrustBundle(self: *Client, options: listCertificatesV1beta1ClusterTrustBundleOptions) !WatchStream(root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/certificates.k8s.io/v1beta1/clustertrustbundles");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCertificatesV1beta1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1beta1ClusterTrustBundleResponse201 = struct {
        body: root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1beta1ClusterTrustBundleResponse202 = struct {
        body: root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1beta1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1beta1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1beta1ClusterTrustBundleResponse = union(enum) {
        ok: CreateCertificatesV1beta1ClusterTrustBundleResponse200,
        created: CreateCertificatesV1beta1ClusterTrustBundleResponse201,
        accepted: CreateCertificatesV1beta1ClusterTrustBundleResponse202,
        unauthorized: CreateCertificatesV1beta1ClusterTrustBundleResponse401,
        default_response: CreateCertificatesV1beta1ClusterTrustBundleResponseDefault,
    };

    pub const createCertificatesV1beta1ClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ClusterTrustBundle
    pub fn createCertificatesV1beta1ClusterTrustBundle(self: *Client, body: root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle, options: createCertificatesV1beta1ClusterTrustBundleOptions) !CreateCertificatesV1beta1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/clustertrustbundles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCertificatesV1beta1CollectionClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1CollectionClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1CollectionClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1CollectionClusterTrustBundleResponse = union(enum) {
        ok: DeleteCertificatesV1beta1CollectionClusterTrustBundleResponse200,
        unauthorized: DeleteCertificatesV1beta1CollectionClusterTrustBundleResponse401,
        default_response: DeleteCertificatesV1beta1CollectionClusterTrustBundleResponseDefault,
    };

    pub const deleteCertificatesV1beta1CollectionClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ClusterTrustBundle
    pub fn deleteCertificatesV1beta1CollectionClusterTrustBundle(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCertificatesV1beta1CollectionClusterTrustBundleOptions) !DeleteCertificatesV1beta1CollectionClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/clustertrustbundles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCertificatesV1beta1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1beta1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1beta1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1beta1ClusterTrustBundleResponse = union(enum) {
        ok: ReadCertificatesV1beta1ClusterTrustBundleResponse200,
        unauthorized: ReadCertificatesV1beta1ClusterTrustBundleResponse401,
        default_response: ReadCertificatesV1beta1ClusterTrustBundleResponseDefault,
    };

    pub const readCertificatesV1beta1ClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ClusterTrustBundle
    pub fn readCertificatesV1beta1ClusterTrustBundle(self: *Client, name: []const u8, options: readCertificatesV1beta1ClusterTrustBundleOptions) !ReadCertificatesV1beta1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/clustertrustbundles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCertificatesV1beta1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1beta1ClusterTrustBundleResponse201 = struct {
        body: root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1beta1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1beta1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1beta1ClusterTrustBundleResponse = union(enum) {
        ok: ReplaceCertificatesV1beta1ClusterTrustBundleResponse200,
        created: ReplaceCertificatesV1beta1ClusterTrustBundleResponse201,
        unauthorized: ReplaceCertificatesV1beta1ClusterTrustBundleResponse401,
        default_response: ReplaceCertificatesV1beta1ClusterTrustBundleResponseDefault,
    };

    pub const replaceCertificatesV1beta1ClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ClusterTrustBundle
    pub fn replaceCertificatesV1beta1ClusterTrustBundle(self: *Client, name: []const u8, body: root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle, options: replaceCertificatesV1beta1ClusterTrustBundleOptions) !ReplaceCertificatesV1beta1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/clustertrustbundles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCertificatesV1beta1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1ClusterTrustBundleResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1ClusterTrustBundleResponse = union(enum) {
        ok: DeleteCertificatesV1beta1ClusterTrustBundleResponse200,
        accepted: DeleteCertificatesV1beta1ClusterTrustBundleResponse202,
        unauthorized: DeleteCertificatesV1beta1ClusterTrustBundleResponse401,
        default_response: DeleteCertificatesV1beta1ClusterTrustBundleResponseDefault,
    };

    pub const deleteCertificatesV1beta1ClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ClusterTrustBundle
    pub fn deleteCertificatesV1beta1ClusterTrustBundle(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCertificatesV1beta1ClusterTrustBundleOptions) !DeleteCertificatesV1beta1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/clustertrustbundles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCertificatesV1beta1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1beta1ClusterTrustBundleResponse201 = struct {
        body: root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1beta1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1beta1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1beta1ClusterTrustBundleResponse = union(enum) {
        ok: PatchCertificatesV1beta1ClusterTrustBundleResponse200,
        created: PatchCertificatesV1beta1ClusterTrustBundleResponse201,
        unauthorized: PatchCertificatesV1beta1ClusterTrustBundleResponse401,
        default_response: PatchCertificatesV1beta1ClusterTrustBundleResponseDefault,
    };

    pub const patchCertificatesV1beta1ClusterTrustBundleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ClusterTrustBundle
    pub fn patchCertificatesV1beta1ClusterTrustBundle(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCertificatesV1beta1ClusterTrustBundleOptions) !PatchCertificatesV1beta1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/clustertrustbundles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.ClusterTrustBundle, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCertificatesV1beta1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequestList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1beta1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1beta1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: ListCertificatesV1beta1NamespacedPodCertificateRequestResponse200,
        unauthorized: ListCertificatesV1beta1NamespacedPodCertificateRequestResponse401,
        default_response: ListCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const listCertificatesV1beta1NamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PodCertificateRequest
    pub fn listCertificatesV1beta1NamespacedPodCertificateRequest(self: *Client, namespace: []const u8, options: listCertificatesV1beta1NamespacedPodCertificateRequestOptions) !ListCertificatesV1beta1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequestList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCertificatesV1beta1NamespacedPodCertificateRequest(self: *Client, namespace: []const u8, options: listCertificatesV1beta1NamespacedPodCertificateRequestOptions) !WatchStream(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/certificates.k8s.io/v1beta1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/podcertificaterequests");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCertificatesV1beta1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1beta1NamespacedPodCertificateRequestResponse201 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1beta1NamespacedPodCertificateRequestResponse202 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1beta1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCertificatesV1beta1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: CreateCertificatesV1beta1NamespacedPodCertificateRequestResponse200,
        created: CreateCertificatesV1beta1NamespacedPodCertificateRequestResponse201,
        accepted: CreateCertificatesV1beta1NamespacedPodCertificateRequestResponse202,
        unauthorized: CreateCertificatesV1beta1NamespacedPodCertificateRequestResponse401,
        default_response: CreateCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const createCertificatesV1beta1NamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a PodCertificateRequest
    pub fn createCertificatesV1beta1NamespacedPodCertificateRequest(self: *Client, namespace: []const u8, body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, options: createCertificatesV1beta1NamespacedPodCertificateRequestOptions) !CreateCertificatesV1beta1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCertificatesV1beta1CollectionNamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1CollectionNamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1CollectionNamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1CollectionNamespacedPodCertificateRequestResponse = union(enum) {
        ok: DeleteCertificatesV1beta1CollectionNamespacedPodCertificateRequestResponse200,
        unauthorized: DeleteCertificatesV1beta1CollectionNamespacedPodCertificateRequestResponse401,
        default_response: DeleteCertificatesV1beta1CollectionNamespacedPodCertificateRequestResponseDefault,
    };

    pub const deleteCertificatesV1beta1CollectionNamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of PodCertificateRequest
    pub fn deleteCertificatesV1beta1CollectionNamespacedPodCertificateRequest(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCertificatesV1beta1CollectionNamespacedPodCertificateRequestOptions) !DeleteCertificatesV1beta1CollectionNamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCertificatesV1beta1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1beta1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1beta1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: ReadCertificatesV1beta1NamespacedPodCertificateRequestResponse200,
        unauthorized: ReadCertificatesV1beta1NamespacedPodCertificateRequestResponse401,
        default_response: ReadCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const readCertificatesV1beta1NamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified PodCertificateRequest
    pub fn readCertificatesV1beta1NamespacedPodCertificateRequest(self: *Client, name: []const u8, namespace: []const u8, options: readCertificatesV1beta1NamespacedPodCertificateRequestOptions) !ReadCertificatesV1beta1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCertificatesV1beta1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1beta1NamespacedPodCertificateRequestResponse201 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1beta1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1beta1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: ReplaceCertificatesV1beta1NamespacedPodCertificateRequestResponse200,
        created: ReplaceCertificatesV1beta1NamespacedPodCertificateRequestResponse201,
        unauthorized: ReplaceCertificatesV1beta1NamespacedPodCertificateRequestResponse401,
        default_response: ReplaceCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const replaceCertificatesV1beta1NamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified PodCertificateRequest
    pub fn replaceCertificatesV1beta1NamespacedPodCertificateRequest(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, options: replaceCertificatesV1beta1NamespacedPodCertificateRequestOptions) !ReplaceCertificatesV1beta1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCertificatesV1beta1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1NamespacedPodCertificateRequestResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCertificatesV1beta1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: DeleteCertificatesV1beta1NamespacedPodCertificateRequestResponse200,
        accepted: DeleteCertificatesV1beta1NamespacedPodCertificateRequestResponse202,
        unauthorized: DeleteCertificatesV1beta1NamespacedPodCertificateRequestResponse401,
        default_response: DeleteCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const deleteCertificatesV1beta1NamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a PodCertificateRequest
    pub fn deleteCertificatesV1beta1NamespacedPodCertificateRequest(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCertificatesV1beta1NamespacedPodCertificateRequestOptions) !DeleteCertificatesV1beta1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCertificatesV1beta1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1beta1NamespacedPodCertificateRequestResponse201 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1beta1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1beta1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: PatchCertificatesV1beta1NamespacedPodCertificateRequestResponse200,
        created: PatchCertificatesV1beta1NamespacedPodCertificateRequestResponse201,
        unauthorized: PatchCertificatesV1beta1NamespacedPodCertificateRequestResponse401,
        default_response: PatchCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const patchCertificatesV1beta1NamespacedPodCertificateRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified PodCertificateRequest
    pub fn patchCertificatesV1beta1NamespacedPodCertificateRequest(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCertificatesV1beta1NamespacedPodCertificateRequestOptions) !PatchCertificatesV1beta1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1beta1NamespacedPodCertificateRequestStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse = union(enum) {
        ok: ReadCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse200,
        unauthorized: ReadCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse401,
        default_response: ReadCertificatesV1beta1NamespacedPodCertificateRequestStatusResponseDefault,
    };

    pub const readCertificatesV1beta1NamespacedPodCertificateRequestStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified PodCertificateRequest
    pub fn readCertificatesV1beta1NamespacedPodCertificateRequestStatus(self: *Client, name: []const u8, namespace: []const u8, options: readCertificatesV1beta1NamespacedPodCertificateRequestStatusOptions) !ReadCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse201 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1beta1NamespacedPodCertificateRequestStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse = union(enum) {
        ok: ReplaceCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse200,
        created: ReplaceCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse201,
        unauthorized: ReplaceCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse401,
        default_response: ReplaceCertificatesV1beta1NamespacedPodCertificateRequestStatusResponseDefault,
    };

    pub const replaceCertificatesV1beta1NamespacedPodCertificateRequestStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified PodCertificateRequest
    pub fn replaceCertificatesV1beta1NamespacedPodCertificateRequestStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, options: replaceCertificatesV1beta1NamespacedPodCertificateRequestStatusOptions) !ReplaceCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse201 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1beta1NamespacedPodCertificateRequestStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse = union(enum) {
        ok: PatchCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse200,
        created: PatchCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse201,
        unauthorized: PatchCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse401,
        default_response: PatchCertificatesV1beta1NamespacedPodCertificateRequestStatusResponseDefault,
    };

    pub const patchCertificatesV1beta1NamespacedPodCertificateRequestStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified PodCertificateRequest
    pub fn patchCertificatesV1beta1NamespacedPodCertificateRequestStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCertificatesV1beta1NamespacedPodCertificateRequestStatusOptions) !PatchCertificatesV1beta1NamespacedPodCertificateRequestStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCertificatesV1beta1PodCertificateRequestForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.certificates.v1beta1.PodCertificateRequestList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1beta1PodCertificateRequestForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1beta1PodCertificateRequestForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCertificatesV1beta1PodCertificateRequestForAllNamespacesResponse = union(enum) {
        ok: ListCertificatesV1beta1PodCertificateRequestForAllNamespacesResponse200,
        unauthorized: ListCertificatesV1beta1PodCertificateRequestForAllNamespacesResponse401,
        default_response: ListCertificatesV1beta1PodCertificateRequestForAllNamespacesResponseDefault,
    };

    pub const listCertificatesV1beta1PodCertificateRequestForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PodCertificateRequest
    pub fn listCertificatesV1beta1PodCertificateRequestForAllNamespaces(self: *Client, options: listCertificatesV1beta1PodCertificateRequestForAllNamespacesOptions) !ListCertificatesV1beta1PodCertificateRequestForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/podcertificaterequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.certificates.v1beta1.PodCertificateRequestList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCertificatesV1beta1PodCertificateRequestForAllNamespaces(self: *Client, options: listCertificatesV1beta1PodCertificateRequestForAllNamespacesOptions) !WatchStream(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/certificates.k8s.io/v1beta1/podcertificaterequests");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.certificates.v1beta1.PodCertificateRequest){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const WatchCertificatesV1beta1ClusterTrustBundleListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1ClusterTrustBundleListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1ClusterTrustBundleListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1ClusterTrustBundleListResponse = union(enum) {
        ok: WatchCertificatesV1beta1ClusterTrustBundleListResponse200,
        unauthorized: WatchCertificatesV1beta1ClusterTrustBundleListResponse401,
        default_response: WatchCertificatesV1beta1ClusterTrustBundleListResponseDefault,
    };

    pub const watchCertificatesV1beta1ClusterTrustBundleListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ClusterTrustBundle. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCertificatesV1beta1ClusterTrustBundleList(self: *Client, options: watchCertificatesV1beta1ClusterTrustBundleListOptions) !WatchCertificatesV1beta1ClusterTrustBundleListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/watch/clustertrustbundles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCertificatesV1beta1ClusterTrustBundleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1ClusterTrustBundleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1ClusterTrustBundleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1ClusterTrustBundleResponse = union(enum) {
        ok: WatchCertificatesV1beta1ClusterTrustBundleResponse200,
        unauthorized: WatchCertificatesV1beta1ClusterTrustBundleResponse401,
        default_response: WatchCertificatesV1beta1ClusterTrustBundleResponseDefault,
    };

    pub const watchCertificatesV1beta1ClusterTrustBundleOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ClusterTrustBundle. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCertificatesV1beta1ClusterTrustBundle(self: *Client, name: []const u8, options: watchCertificatesV1beta1ClusterTrustBundleOptions) !WatchCertificatesV1beta1ClusterTrustBundleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/watch/clustertrustbundles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCertificatesV1beta1NamespacedPodCertificateRequestListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1NamespacedPodCertificateRequestListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1NamespacedPodCertificateRequestListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1NamespacedPodCertificateRequestListResponse = union(enum) {
        ok: WatchCertificatesV1beta1NamespacedPodCertificateRequestListResponse200,
        unauthorized: WatchCertificatesV1beta1NamespacedPodCertificateRequestListResponse401,
        default_response: WatchCertificatesV1beta1NamespacedPodCertificateRequestListResponseDefault,
    };

    pub const watchCertificatesV1beta1NamespacedPodCertificateRequestListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PodCertificateRequest. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCertificatesV1beta1NamespacedPodCertificateRequestList(self: *Client, namespace: []const u8, options: watchCertificatesV1beta1NamespacedPodCertificateRequestListOptions) !WatchCertificatesV1beta1NamespacedPodCertificateRequestListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCertificatesV1beta1NamespacedPodCertificateRequestResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1NamespacedPodCertificateRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1NamespacedPodCertificateRequestResponse = union(enum) {
        ok: WatchCertificatesV1beta1NamespacedPodCertificateRequestResponse200,
        unauthorized: WatchCertificatesV1beta1NamespacedPodCertificateRequestResponse401,
        default_response: WatchCertificatesV1beta1NamespacedPodCertificateRequestResponseDefault,
    };

    pub const watchCertificatesV1beta1NamespacedPodCertificateRequestOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind PodCertificateRequest. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCertificatesV1beta1NamespacedPodCertificateRequest(self: *Client, name: []const u8, namespace: []const u8, options: watchCertificatesV1beta1NamespacedPodCertificateRequestOptions) !WatchCertificatesV1beta1NamespacedPodCertificateRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podcertificaterequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCertificatesV1beta1PodCertificateRequestListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1PodCertificateRequestListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1PodCertificateRequestListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCertificatesV1beta1PodCertificateRequestListForAllNamespacesResponse = union(enum) {
        ok: WatchCertificatesV1beta1PodCertificateRequestListForAllNamespacesResponse200,
        unauthorized: WatchCertificatesV1beta1PodCertificateRequestListForAllNamespacesResponse401,
        default_response: WatchCertificatesV1beta1PodCertificateRequestListForAllNamespacesResponseDefault,
    };

    pub const watchCertificatesV1beta1PodCertificateRequestListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PodCertificateRequest. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCertificatesV1beta1PodCertificateRequestListForAllNamespaces(self: *Client, options: watchCertificatesV1beta1PodCertificateRequestListForAllNamespacesOptions) !WatchCertificatesV1beta1PodCertificateRequestListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/certificates.k8s.io/v1beta1/watch/podcertificaterequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetCoordinationAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoordinationAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoordinationAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoordinationAPIGroupResponse = union(enum) {
        ok: GetCoordinationAPIGroupResponse200,
        unauthorized: GetCoordinationAPIGroupResponse401,
        default_response: GetCoordinationAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getCoordinationAPIGroup(self: *Client) !GetCoordinationAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetCoordinationV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoordinationV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoordinationV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoordinationV1APIResourcesResponse = union(enum) {
        ok: GetCoordinationV1APIResourcesResponse200,
        unauthorized: GetCoordinationV1APIResourcesResponse401,
        default_response: GetCoordinationV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getCoordinationV1APIResources(self: *Client) !GetCoordinationV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoordinationV1LeaseForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.coordination.v1.LeaseList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1LeaseForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1LeaseForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1LeaseForAllNamespacesResponse = union(enum) {
        ok: ListCoordinationV1LeaseForAllNamespacesResponse200,
        unauthorized: ListCoordinationV1LeaseForAllNamespacesResponse401,
        default_response: ListCoordinationV1LeaseForAllNamespacesResponseDefault,
    };

    pub const listCoordinationV1LeaseForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Lease
    pub fn listCoordinationV1LeaseForAllNamespaces(self: *Client, options: listCoordinationV1LeaseForAllNamespacesOptions) !ListCoordinationV1LeaseForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1/leases", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1.LeaseList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoordinationV1LeaseForAllNamespaces(self: *Client, options: listCoordinationV1LeaseForAllNamespacesOptions) !WatchStream(root.io.k8s.api.coordination.v1.Lease) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/coordination.k8s.io/v1/leases");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.coordination.v1.Lease){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoordinationV1NamespacedLeaseResponse200 = struct {
        body: root.io.k8s.api.coordination.v1.LeaseList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1NamespacedLeaseResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1NamespacedLeaseResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1NamespacedLeaseResponse = union(enum) {
        ok: ListCoordinationV1NamespacedLeaseResponse200,
        unauthorized: ListCoordinationV1NamespacedLeaseResponse401,
        default_response: ListCoordinationV1NamespacedLeaseResponseDefault,
    };

    pub const listCoordinationV1NamespacedLeaseOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Lease
    pub fn listCoordinationV1NamespacedLease(self: *Client, namespace: []const u8, options: listCoordinationV1NamespacedLeaseOptions) !ListCoordinationV1NamespacedLeaseResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leases", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1.LeaseList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoordinationV1NamespacedLease(self: *Client, namespace: []const u8, options: listCoordinationV1NamespacedLeaseOptions) !WatchStream(root.io.k8s.api.coordination.v1.Lease) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/coordination.k8s.io/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/leases");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.coordination.v1.Lease){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoordinationV1NamespacedLeaseResponse200 = struct {
        body: root.io.k8s.api.coordination.v1.Lease,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1NamespacedLeaseResponse201 = struct {
        body: root.io.k8s.api.coordination.v1.Lease,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1NamespacedLeaseResponse202 = struct {
        body: root.io.k8s.api.coordination.v1.Lease,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1NamespacedLeaseResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1NamespacedLeaseResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1NamespacedLeaseResponse = union(enum) {
        ok: CreateCoordinationV1NamespacedLeaseResponse200,
        created: CreateCoordinationV1NamespacedLeaseResponse201,
        accepted: CreateCoordinationV1NamespacedLeaseResponse202,
        unauthorized: CreateCoordinationV1NamespacedLeaseResponse401,
        default_response: CreateCoordinationV1NamespacedLeaseResponseDefault,
    };

    pub const createCoordinationV1NamespacedLeaseOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a Lease
    pub fn createCoordinationV1NamespacedLease(self: *Client, namespace: []const u8, body: root.io.k8s.api.coordination.v1.Lease, options: createCoordinationV1NamespacedLeaseOptions) !CreateCoordinationV1NamespacedLeaseResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leases", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1.Lease, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1.Lease, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1.Lease, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoordinationV1CollectionNamespacedLeaseResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1CollectionNamespacedLeaseResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1CollectionNamespacedLeaseResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1CollectionNamespacedLeaseResponse = union(enum) {
        ok: DeleteCoordinationV1CollectionNamespacedLeaseResponse200,
        unauthorized: DeleteCoordinationV1CollectionNamespacedLeaseResponse401,
        default_response: DeleteCoordinationV1CollectionNamespacedLeaseResponseDefault,
    };

    pub const deleteCoordinationV1CollectionNamespacedLeaseOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Lease
    pub fn deleteCoordinationV1CollectionNamespacedLease(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoordinationV1CollectionNamespacedLeaseOptions) !DeleteCoordinationV1CollectionNamespacedLeaseResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leases", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoordinationV1NamespacedLeaseResponse200 = struct {
        body: root.io.k8s.api.coordination.v1.Lease,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoordinationV1NamespacedLeaseResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoordinationV1NamespacedLeaseResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoordinationV1NamespacedLeaseResponse = union(enum) {
        ok: ReadCoordinationV1NamespacedLeaseResponse200,
        unauthorized: ReadCoordinationV1NamespacedLeaseResponse401,
        default_response: ReadCoordinationV1NamespacedLeaseResponseDefault,
    };

    pub const readCoordinationV1NamespacedLeaseOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Lease
    pub fn readCoordinationV1NamespacedLease(self: *Client, name: []const u8, namespace: []const u8, options: readCoordinationV1NamespacedLeaseOptions) !ReadCoordinationV1NamespacedLeaseResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leases/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1.Lease, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoordinationV1NamespacedLeaseResponse200 = struct {
        body: root.io.k8s.api.coordination.v1.Lease,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoordinationV1NamespacedLeaseResponse201 = struct {
        body: root.io.k8s.api.coordination.v1.Lease,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoordinationV1NamespacedLeaseResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoordinationV1NamespacedLeaseResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoordinationV1NamespacedLeaseResponse = union(enum) {
        ok: ReplaceCoordinationV1NamespacedLeaseResponse200,
        created: ReplaceCoordinationV1NamespacedLeaseResponse201,
        unauthorized: ReplaceCoordinationV1NamespacedLeaseResponse401,
        default_response: ReplaceCoordinationV1NamespacedLeaseResponseDefault,
    };

    pub const replaceCoordinationV1NamespacedLeaseOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Lease
    pub fn replaceCoordinationV1NamespacedLease(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.coordination.v1.Lease, options: replaceCoordinationV1NamespacedLeaseOptions) !ReplaceCoordinationV1NamespacedLeaseResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leases/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1.Lease, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1.Lease, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoordinationV1NamespacedLeaseResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1NamespacedLeaseResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1NamespacedLeaseResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1NamespacedLeaseResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1NamespacedLeaseResponse = union(enum) {
        ok: DeleteCoordinationV1NamespacedLeaseResponse200,
        accepted: DeleteCoordinationV1NamespacedLeaseResponse202,
        unauthorized: DeleteCoordinationV1NamespacedLeaseResponse401,
        default_response: DeleteCoordinationV1NamespacedLeaseResponseDefault,
    };

    pub const deleteCoordinationV1NamespacedLeaseOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a Lease
    pub fn deleteCoordinationV1NamespacedLease(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoordinationV1NamespacedLeaseOptions) !DeleteCoordinationV1NamespacedLeaseResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leases/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoordinationV1NamespacedLeaseResponse200 = struct {
        body: root.io.k8s.api.coordination.v1.Lease,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoordinationV1NamespacedLeaseResponse201 = struct {
        body: root.io.k8s.api.coordination.v1.Lease,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoordinationV1NamespacedLeaseResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoordinationV1NamespacedLeaseResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoordinationV1NamespacedLeaseResponse = union(enum) {
        ok: PatchCoordinationV1NamespacedLeaseResponse200,
        created: PatchCoordinationV1NamespacedLeaseResponse201,
        unauthorized: PatchCoordinationV1NamespacedLeaseResponse401,
        default_response: PatchCoordinationV1NamespacedLeaseResponseDefault,
    };

    pub const patchCoordinationV1NamespacedLeaseOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Lease
    pub fn patchCoordinationV1NamespacedLease(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoordinationV1NamespacedLeaseOptions) !PatchCoordinationV1NamespacedLeaseResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leases/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1.Lease, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1.Lease, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoordinationV1LeaseListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1LeaseListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1LeaseListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1LeaseListForAllNamespacesResponse = union(enum) {
        ok: WatchCoordinationV1LeaseListForAllNamespacesResponse200,
        unauthorized: WatchCoordinationV1LeaseListForAllNamespacesResponse401,
        default_response: WatchCoordinationV1LeaseListForAllNamespacesResponseDefault,
    };

    pub const watchCoordinationV1LeaseListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Lease. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoordinationV1LeaseListForAllNamespaces(self: *Client, options: watchCoordinationV1LeaseListForAllNamespacesOptions) !WatchCoordinationV1LeaseListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1/watch/leases", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoordinationV1NamespacedLeaseListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1NamespacedLeaseListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1NamespacedLeaseListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1NamespacedLeaseListResponse = union(enum) {
        ok: WatchCoordinationV1NamespacedLeaseListResponse200,
        unauthorized: WatchCoordinationV1NamespacedLeaseListResponse401,
        default_response: WatchCoordinationV1NamespacedLeaseListResponseDefault,
    };

    pub const watchCoordinationV1NamespacedLeaseListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Lease. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoordinationV1NamespacedLeaseList(self: *Client, namespace: []const u8, options: watchCoordinationV1NamespacedLeaseListOptions) !WatchCoordinationV1NamespacedLeaseListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leases", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoordinationV1NamespacedLeaseResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1NamespacedLeaseResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1NamespacedLeaseResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1NamespacedLeaseResponse = union(enum) {
        ok: WatchCoordinationV1NamespacedLeaseResponse200,
        unauthorized: WatchCoordinationV1NamespacedLeaseResponse401,
        default_response: WatchCoordinationV1NamespacedLeaseResponseDefault,
    };

    pub const watchCoordinationV1NamespacedLeaseOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Lease. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoordinationV1NamespacedLease(self: *Client, name: []const u8, namespace: []const u8, options: watchCoordinationV1NamespacedLeaseOptions) !WatchCoordinationV1NamespacedLeaseResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leases/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetCoordinationV1alpha2APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoordinationV1alpha2APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoordinationV1alpha2APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoordinationV1alpha2APIResourcesResponse = union(enum) {
        ok: GetCoordinationV1alpha2APIResourcesResponse200,
        unauthorized: GetCoordinationV1alpha2APIResourcesResponse401,
        default_response: GetCoordinationV1alpha2APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getCoordinationV1alpha2APIResources(self: *Client) !GetCoordinationV1alpha2APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1alpha2/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoordinationV1alpha2LeaseCandidateForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.coordination.v1alpha2.LeaseCandidateList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1alpha2LeaseCandidateForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1alpha2LeaseCandidateForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1alpha2LeaseCandidateForAllNamespacesResponse = union(enum) {
        ok: ListCoordinationV1alpha2LeaseCandidateForAllNamespacesResponse200,
        unauthorized: ListCoordinationV1alpha2LeaseCandidateForAllNamespacesResponse401,
        default_response: ListCoordinationV1alpha2LeaseCandidateForAllNamespacesResponseDefault,
    };

    pub const listCoordinationV1alpha2LeaseCandidateForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind LeaseCandidate
    pub fn listCoordinationV1alpha2LeaseCandidateForAllNamespaces(self: *Client, options: listCoordinationV1alpha2LeaseCandidateForAllNamespacesOptions) !ListCoordinationV1alpha2LeaseCandidateForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1alpha2/leasecandidates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1alpha2.LeaseCandidateList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoordinationV1alpha2LeaseCandidateForAllNamespaces(self: *Client, options: listCoordinationV1alpha2LeaseCandidateForAllNamespacesOptions) !WatchStream(root.io.k8s.api.coordination.v1alpha2.LeaseCandidate) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/coordination.k8s.io/v1alpha2/leasecandidates");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.coordination.v1alpha2.LeaseCandidate){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoordinationV1alpha2NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.api.coordination.v1alpha2.LeaseCandidateList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1alpha2NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1alpha2NamespacedLeaseCandidateResponse = union(enum) {
        ok: ListCoordinationV1alpha2NamespacedLeaseCandidateResponse200,
        unauthorized: ListCoordinationV1alpha2NamespacedLeaseCandidateResponse401,
        default_response: ListCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault,
    };

    pub const listCoordinationV1alpha2NamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind LeaseCandidate
    pub fn listCoordinationV1alpha2NamespacedLeaseCandidate(self: *Client, namespace: []const u8, options: listCoordinationV1alpha2NamespacedLeaseCandidateOptions) !ListCoordinationV1alpha2NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1alpha2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1alpha2.LeaseCandidateList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoordinationV1alpha2NamespacedLeaseCandidate(self: *Client, namespace: []const u8, options: listCoordinationV1alpha2NamespacedLeaseCandidateOptions) !WatchStream(root.io.k8s.api.coordination.v1alpha2.LeaseCandidate) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/coordination.k8s.io/v1alpha2/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/leasecandidates");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.coordination.v1alpha2.LeaseCandidate){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoordinationV1alpha2NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.api.coordination.v1alpha2.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1alpha2NamespacedLeaseCandidateResponse201 = struct {
        body: root.io.k8s.api.coordination.v1alpha2.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1alpha2NamespacedLeaseCandidateResponse202 = struct {
        body: root.io.k8s.api.coordination.v1alpha2.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1alpha2NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1alpha2NamespacedLeaseCandidateResponse = union(enum) {
        ok: CreateCoordinationV1alpha2NamespacedLeaseCandidateResponse200,
        created: CreateCoordinationV1alpha2NamespacedLeaseCandidateResponse201,
        accepted: CreateCoordinationV1alpha2NamespacedLeaseCandidateResponse202,
        unauthorized: CreateCoordinationV1alpha2NamespacedLeaseCandidateResponse401,
        default_response: CreateCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault,
    };

    pub const createCoordinationV1alpha2NamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a LeaseCandidate
    pub fn createCoordinationV1alpha2NamespacedLeaseCandidate(self: *Client, namespace: []const u8, body: root.io.k8s.api.coordination.v1alpha2.LeaseCandidate, options: createCoordinationV1alpha2NamespacedLeaseCandidateOptions) !CreateCoordinationV1alpha2NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1alpha2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1alpha2.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1alpha2.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1alpha2.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoordinationV1alpha2CollectionNamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1alpha2CollectionNamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1alpha2CollectionNamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1alpha2CollectionNamespacedLeaseCandidateResponse = union(enum) {
        ok: DeleteCoordinationV1alpha2CollectionNamespacedLeaseCandidateResponse200,
        unauthorized: DeleteCoordinationV1alpha2CollectionNamespacedLeaseCandidateResponse401,
        default_response: DeleteCoordinationV1alpha2CollectionNamespacedLeaseCandidateResponseDefault,
    };

    pub const deleteCoordinationV1alpha2CollectionNamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of LeaseCandidate
    pub fn deleteCoordinationV1alpha2CollectionNamespacedLeaseCandidate(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoordinationV1alpha2CollectionNamespacedLeaseCandidateOptions) !DeleteCoordinationV1alpha2CollectionNamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1alpha2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoordinationV1alpha2NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.api.coordination.v1alpha2.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoordinationV1alpha2NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoordinationV1alpha2NamespacedLeaseCandidateResponse = union(enum) {
        ok: ReadCoordinationV1alpha2NamespacedLeaseCandidateResponse200,
        unauthorized: ReadCoordinationV1alpha2NamespacedLeaseCandidateResponse401,
        default_response: ReadCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault,
    };

    pub const readCoordinationV1alpha2NamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified LeaseCandidate
    pub fn readCoordinationV1alpha2NamespacedLeaseCandidate(self: *Client, name: []const u8, namespace: []const u8, options: readCoordinationV1alpha2NamespacedLeaseCandidateOptions) !ReadCoordinationV1alpha2NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1alpha2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1alpha2.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoordinationV1alpha2NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.api.coordination.v1alpha2.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoordinationV1alpha2NamespacedLeaseCandidateResponse201 = struct {
        body: root.io.k8s.api.coordination.v1alpha2.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoordinationV1alpha2NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoordinationV1alpha2NamespacedLeaseCandidateResponse = union(enum) {
        ok: ReplaceCoordinationV1alpha2NamespacedLeaseCandidateResponse200,
        created: ReplaceCoordinationV1alpha2NamespacedLeaseCandidateResponse201,
        unauthorized: ReplaceCoordinationV1alpha2NamespacedLeaseCandidateResponse401,
        default_response: ReplaceCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault,
    };

    pub const replaceCoordinationV1alpha2NamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified LeaseCandidate
    pub fn replaceCoordinationV1alpha2NamespacedLeaseCandidate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.coordination.v1alpha2.LeaseCandidate, options: replaceCoordinationV1alpha2NamespacedLeaseCandidateOptions) !ReplaceCoordinationV1alpha2NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1alpha2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1alpha2.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1alpha2.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoordinationV1alpha2NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1alpha2NamespacedLeaseCandidateResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1alpha2NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1alpha2NamespacedLeaseCandidateResponse = union(enum) {
        ok: DeleteCoordinationV1alpha2NamespacedLeaseCandidateResponse200,
        accepted: DeleteCoordinationV1alpha2NamespacedLeaseCandidateResponse202,
        unauthorized: DeleteCoordinationV1alpha2NamespacedLeaseCandidateResponse401,
        default_response: DeleteCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault,
    };

    pub const deleteCoordinationV1alpha2NamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a LeaseCandidate
    pub fn deleteCoordinationV1alpha2NamespacedLeaseCandidate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoordinationV1alpha2NamespacedLeaseCandidateOptions) !DeleteCoordinationV1alpha2NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1alpha2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoordinationV1alpha2NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.api.coordination.v1alpha2.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoordinationV1alpha2NamespacedLeaseCandidateResponse201 = struct {
        body: root.io.k8s.api.coordination.v1alpha2.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoordinationV1alpha2NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoordinationV1alpha2NamespacedLeaseCandidateResponse = union(enum) {
        ok: PatchCoordinationV1alpha2NamespacedLeaseCandidateResponse200,
        created: PatchCoordinationV1alpha2NamespacedLeaseCandidateResponse201,
        unauthorized: PatchCoordinationV1alpha2NamespacedLeaseCandidateResponse401,
        default_response: PatchCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault,
    };

    pub const patchCoordinationV1alpha2NamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified LeaseCandidate
    pub fn patchCoordinationV1alpha2NamespacedLeaseCandidate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoordinationV1alpha2NamespacedLeaseCandidateOptions) !PatchCoordinationV1alpha2NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1alpha2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1alpha2.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1alpha2.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoordinationV1alpha2LeaseCandidateListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1alpha2LeaseCandidateListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1alpha2LeaseCandidateListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1alpha2LeaseCandidateListForAllNamespacesResponse = union(enum) {
        ok: WatchCoordinationV1alpha2LeaseCandidateListForAllNamespacesResponse200,
        unauthorized: WatchCoordinationV1alpha2LeaseCandidateListForAllNamespacesResponse401,
        default_response: WatchCoordinationV1alpha2LeaseCandidateListForAllNamespacesResponseDefault,
    };

    pub const watchCoordinationV1alpha2LeaseCandidateListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of LeaseCandidate. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoordinationV1alpha2LeaseCandidateListForAllNamespaces(self: *Client, options: watchCoordinationV1alpha2LeaseCandidateListForAllNamespacesOptions) !WatchCoordinationV1alpha2LeaseCandidateListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1alpha2/watch/leasecandidates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoordinationV1alpha2NamespacedLeaseCandidateListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1alpha2NamespacedLeaseCandidateListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1alpha2NamespacedLeaseCandidateListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1alpha2NamespacedLeaseCandidateListResponse = union(enum) {
        ok: WatchCoordinationV1alpha2NamespacedLeaseCandidateListResponse200,
        unauthorized: WatchCoordinationV1alpha2NamespacedLeaseCandidateListResponse401,
        default_response: WatchCoordinationV1alpha2NamespacedLeaseCandidateListResponseDefault,
    };

    pub const watchCoordinationV1alpha2NamespacedLeaseCandidateListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of LeaseCandidate. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoordinationV1alpha2NamespacedLeaseCandidateList(self: *Client, namespace: []const u8, options: watchCoordinationV1alpha2NamespacedLeaseCandidateListOptions) !WatchCoordinationV1alpha2NamespacedLeaseCandidateListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1alpha2/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoordinationV1alpha2NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1alpha2NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1alpha2NamespacedLeaseCandidateResponse = union(enum) {
        ok: WatchCoordinationV1alpha2NamespacedLeaseCandidateResponse200,
        unauthorized: WatchCoordinationV1alpha2NamespacedLeaseCandidateResponse401,
        default_response: WatchCoordinationV1alpha2NamespacedLeaseCandidateResponseDefault,
    };

    pub const watchCoordinationV1alpha2NamespacedLeaseCandidateOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind LeaseCandidate. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoordinationV1alpha2NamespacedLeaseCandidate(self: *Client, name: []const u8, namespace: []const u8, options: watchCoordinationV1alpha2NamespacedLeaseCandidateOptions) !WatchCoordinationV1alpha2NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1alpha2/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetCoordinationV1beta1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoordinationV1beta1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoordinationV1beta1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCoordinationV1beta1APIResourcesResponse = union(enum) {
        ok: GetCoordinationV1beta1APIResourcesResponse200,
        unauthorized: GetCoordinationV1beta1APIResourcesResponse401,
        default_response: GetCoordinationV1beta1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getCoordinationV1beta1APIResources(self: *Client) !GetCoordinationV1beta1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1beta1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListCoordinationV1beta1LeaseCandidateForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.coordination.v1beta1.LeaseCandidateList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1beta1LeaseCandidateForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1beta1LeaseCandidateForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1beta1LeaseCandidateForAllNamespacesResponse = union(enum) {
        ok: ListCoordinationV1beta1LeaseCandidateForAllNamespacesResponse200,
        unauthorized: ListCoordinationV1beta1LeaseCandidateForAllNamespacesResponse401,
        default_response: ListCoordinationV1beta1LeaseCandidateForAllNamespacesResponseDefault,
    };

    pub const listCoordinationV1beta1LeaseCandidateForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind LeaseCandidate
    pub fn listCoordinationV1beta1LeaseCandidateForAllNamespaces(self: *Client, options: listCoordinationV1beta1LeaseCandidateForAllNamespacesOptions) !ListCoordinationV1beta1LeaseCandidateForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1beta1/leasecandidates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1beta1.LeaseCandidateList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoordinationV1beta1LeaseCandidateForAllNamespaces(self: *Client, options: listCoordinationV1beta1LeaseCandidateForAllNamespacesOptions) !WatchStream(root.io.k8s.api.coordination.v1beta1.LeaseCandidate) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/coordination.k8s.io/v1beta1/leasecandidates");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.coordination.v1beta1.LeaseCandidate){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListCoordinationV1beta1NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.api.coordination.v1beta1.LeaseCandidateList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1beta1NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1beta1NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListCoordinationV1beta1NamespacedLeaseCandidateResponse = union(enum) {
        ok: ListCoordinationV1beta1NamespacedLeaseCandidateResponse200,
        unauthorized: ListCoordinationV1beta1NamespacedLeaseCandidateResponse401,
        default_response: ListCoordinationV1beta1NamespacedLeaseCandidateResponseDefault,
    };

    pub const listCoordinationV1beta1NamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind LeaseCandidate
    pub fn listCoordinationV1beta1NamespacedLeaseCandidate(self: *Client, namespace: []const u8, options: listCoordinationV1beta1NamespacedLeaseCandidateOptions) !ListCoordinationV1beta1NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1beta1.LeaseCandidateList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistCoordinationV1beta1NamespacedLeaseCandidate(self: *Client, namespace: []const u8, options: listCoordinationV1beta1NamespacedLeaseCandidateOptions) !WatchStream(root.io.k8s.api.coordination.v1beta1.LeaseCandidate) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/coordination.k8s.io/v1beta1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/leasecandidates");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.coordination.v1beta1.LeaseCandidate){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateCoordinationV1beta1NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.api.coordination.v1beta1.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1beta1NamespacedLeaseCandidateResponse201 = struct {
        body: root.io.k8s.api.coordination.v1beta1.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1beta1NamespacedLeaseCandidateResponse202 = struct {
        body: root.io.k8s.api.coordination.v1beta1.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1beta1NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1beta1NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateCoordinationV1beta1NamespacedLeaseCandidateResponse = union(enum) {
        ok: CreateCoordinationV1beta1NamespacedLeaseCandidateResponse200,
        created: CreateCoordinationV1beta1NamespacedLeaseCandidateResponse201,
        accepted: CreateCoordinationV1beta1NamespacedLeaseCandidateResponse202,
        unauthorized: CreateCoordinationV1beta1NamespacedLeaseCandidateResponse401,
        default_response: CreateCoordinationV1beta1NamespacedLeaseCandidateResponseDefault,
    };

    pub const createCoordinationV1beta1NamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a LeaseCandidate
    pub fn createCoordinationV1beta1NamespacedLeaseCandidate(self: *Client, namespace: []const u8, body: root.io.k8s.api.coordination.v1beta1.LeaseCandidate, options: createCoordinationV1beta1NamespacedLeaseCandidateOptions) !CreateCoordinationV1beta1NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1beta1.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1beta1.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1beta1.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoordinationV1beta1CollectionNamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1beta1CollectionNamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1beta1CollectionNamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1beta1CollectionNamespacedLeaseCandidateResponse = union(enum) {
        ok: DeleteCoordinationV1beta1CollectionNamespacedLeaseCandidateResponse200,
        unauthorized: DeleteCoordinationV1beta1CollectionNamespacedLeaseCandidateResponse401,
        default_response: DeleteCoordinationV1beta1CollectionNamespacedLeaseCandidateResponseDefault,
    };

    pub const deleteCoordinationV1beta1CollectionNamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of LeaseCandidate
    pub fn deleteCoordinationV1beta1CollectionNamespacedLeaseCandidate(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoordinationV1beta1CollectionNamespacedLeaseCandidateOptions) !DeleteCoordinationV1beta1CollectionNamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadCoordinationV1beta1NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.api.coordination.v1beta1.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoordinationV1beta1NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoordinationV1beta1NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadCoordinationV1beta1NamespacedLeaseCandidateResponse = union(enum) {
        ok: ReadCoordinationV1beta1NamespacedLeaseCandidateResponse200,
        unauthorized: ReadCoordinationV1beta1NamespacedLeaseCandidateResponse401,
        default_response: ReadCoordinationV1beta1NamespacedLeaseCandidateResponseDefault,
    };

    pub const readCoordinationV1beta1NamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified LeaseCandidate
    pub fn readCoordinationV1beta1NamespacedLeaseCandidate(self: *Client, name: []const u8, namespace: []const u8, options: readCoordinationV1beta1NamespacedLeaseCandidateOptions) !ReadCoordinationV1beta1NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1beta1.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceCoordinationV1beta1NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.api.coordination.v1beta1.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoordinationV1beta1NamespacedLeaseCandidateResponse201 = struct {
        body: root.io.k8s.api.coordination.v1beta1.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoordinationV1beta1NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoordinationV1beta1NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceCoordinationV1beta1NamespacedLeaseCandidateResponse = union(enum) {
        ok: ReplaceCoordinationV1beta1NamespacedLeaseCandidateResponse200,
        created: ReplaceCoordinationV1beta1NamespacedLeaseCandidateResponse201,
        unauthorized: ReplaceCoordinationV1beta1NamespacedLeaseCandidateResponse401,
        default_response: ReplaceCoordinationV1beta1NamespacedLeaseCandidateResponseDefault,
    };

    pub const replaceCoordinationV1beta1NamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified LeaseCandidate
    pub fn replaceCoordinationV1beta1NamespacedLeaseCandidate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.coordination.v1beta1.LeaseCandidate, options: replaceCoordinationV1beta1NamespacedLeaseCandidateOptions) !ReplaceCoordinationV1beta1NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1beta1.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1beta1.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteCoordinationV1beta1NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1beta1NamespacedLeaseCandidateResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1beta1NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1beta1NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteCoordinationV1beta1NamespacedLeaseCandidateResponse = union(enum) {
        ok: DeleteCoordinationV1beta1NamespacedLeaseCandidateResponse200,
        accepted: DeleteCoordinationV1beta1NamespacedLeaseCandidateResponse202,
        unauthorized: DeleteCoordinationV1beta1NamespacedLeaseCandidateResponse401,
        default_response: DeleteCoordinationV1beta1NamespacedLeaseCandidateResponseDefault,
    };

    pub const deleteCoordinationV1beta1NamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a LeaseCandidate
    pub fn deleteCoordinationV1beta1NamespacedLeaseCandidate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteCoordinationV1beta1NamespacedLeaseCandidateOptions) !DeleteCoordinationV1beta1NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchCoordinationV1beta1NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.api.coordination.v1beta1.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoordinationV1beta1NamespacedLeaseCandidateResponse201 = struct {
        body: root.io.k8s.api.coordination.v1beta1.LeaseCandidate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoordinationV1beta1NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoordinationV1beta1NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchCoordinationV1beta1NamespacedLeaseCandidateResponse = union(enum) {
        ok: PatchCoordinationV1beta1NamespacedLeaseCandidateResponse200,
        created: PatchCoordinationV1beta1NamespacedLeaseCandidateResponse201,
        unauthorized: PatchCoordinationV1beta1NamespacedLeaseCandidateResponse401,
        default_response: PatchCoordinationV1beta1NamespacedLeaseCandidateResponseDefault,
    };

    pub const patchCoordinationV1beta1NamespacedLeaseCandidateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified LeaseCandidate
    pub fn patchCoordinationV1beta1NamespacedLeaseCandidate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchCoordinationV1beta1NamespacedLeaseCandidateOptions) !PatchCoordinationV1beta1NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1beta1.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.coordination.v1beta1.LeaseCandidate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoordinationV1beta1LeaseCandidateListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1beta1LeaseCandidateListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1beta1LeaseCandidateListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1beta1LeaseCandidateListForAllNamespacesResponse = union(enum) {
        ok: WatchCoordinationV1beta1LeaseCandidateListForAllNamespacesResponse200,
        unauthorized: WatchCoordinationV1beta1LeaseCandidateListForAllNamespacesResponse401,
        default_response: WatchCoordinationV1beta1LeaseCandidateListForAllNamespacesResponseDefault,
    };

    pub const watchCoordinationV1beta1LeaseCandidateListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of LeaseCandidate. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoordinationV1beta1LeaseCandidateListForAllNamespaces(self: *Client, options: watchCoordinationV1beta1LeaseCandidateListForAllNamespacesOptions) !WatchCoordinationV1beta1LeaseCandidateListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1beta1/watch/leasecandidates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoordinationV1beta1NamespacedLeaseCandidateListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1beta1NamespacedLeaseCandidateListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1beta1NamespacedLeaseCandidateListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1beta1NamespacedLeaseCandidateListResponse = union(enum) {
        ok: WatchCoordinationV1beta1NamespacedLeaseCandidateListResponse200,
        unauthorized: WatchCoordinationV1beta1NamespacedLeaseCandidateListResponse401,
        default_response: WatchCoordinationV1beta1NamespacedLeaseCandidateListResponseDefault,
    };

    pub const watchCoordinationV1beta1NamespacedLeaseCandidateListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of LeaseCandidate. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchCoordinationV1beta1NamespacedLeaseCandidateList(self: *Client, namespace: []const u8, options: watchCoordinationV1beta1NamespacedLeaseCandidateListOptions) !WatchCoordinationV1beta1NamespacedLeaseCandidateListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1beta1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchCoordinationV1beta1NamespacedLeaseCandidateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1beta1NamespacedLeaseCandidateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1beta1NamespacedLeaseCandidateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchCoordinationV1beta1NamespacedLeaseCandidateResponse = union(enum) {
        ok: WatchCoordinationV1beta1NamespacedLeaseCandidateResponse200,
        unauthorized: WatchCoordinationV1beta1NamespacedLeaseCandidateResponse401,
        default_response: WatchCoordinationV1beta1NamespacedLeaseCandidateResponseDefault,
    };

    pub const watchCoordinationV1beta1NamespacedLeaseCandidateOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind LeaseCandidate. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchCoordinationV1beta1NamespacedLeaseCandidate(self: *Client, name: []const u8, namespace: []const u8, options: watchCoordinationV1beta1NamespacedLeaseCandidateOptions) !WatchCoordinationV1beta1NamespacedLeaseCandidateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/coordination.k8s.io/v1beta1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/leasecandidates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetDiscoveryAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetDiscoveryAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetDiscoveryAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetDiscoveryAPIGroupResponse = union(enum) {
        ok: GetDiscoveryAPIGroupResponse200,
        unauthorized: GetDiscoveryAPIGroupResponse401,
        default_response: GetDiscoveryAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getDiscoveryAPIGroup(self: *Client) !GetDiscoveryAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/discovery.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetDiscoveryV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetDiscoveryV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetDiscoveryV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetDiscoveryV1APIResourcesResponse = union(enum) {
        ok: GetDiscoveryV1APIResourcesResponse200,
        unauthorized: GetDiscoveryV1APIResourcesResponse401,
        default_response: GetDiscoveryV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getDiscoveryV1APIResources(self: *Client) !GetDiscoveryV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/discovery.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListDiscoveryV1EndpointSliceForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.discovery.v1.EndpointSliceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListDiscoveryV1EndpointSliceForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListDiscoveryV1EndpointSliceForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListDiscoveryV1EndpointSliceForAllNamespacesResponse = union(enum) {
        ok: ListDiscoveryV1EndpointSliceForAllNamespacesResponse200,
        unauthorized: ListDiscoveryV1EndpointSliceForAllNamespacesResponse401,
        default_response: ListDiscoveryV1EndpointSliceForAllNamespacesResponseDefault,
    };

    pub const listDiscoveryV1EndpointSliceForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind EndpointSlice
    pub fn listDiscoveryV1EndpointSliceForAllNamespaces(self: *Client, options: listDiscoveryV1EndpointSliceForAllNamespacesOptions) !ListDiscoveryV1EndpointSliceForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/discovery.k8s.io/v1/endpointslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.discovery.v1.EndpointSliceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistDiscoveryV1EndpointSliceForAllNamespaces(self: *Client, options: listDiscoveryV1EndpointSliceForAllNamespacesOptions) !WatchStream(root.io.k8s.api.discovery.v1.EndpointSlice) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/discovery.k8s.io/v1/endpointslices");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.discovery.v1.EndpointSlice){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListDiscoveryV1NamespacedEndpointSliceResponse200 = struct {
        body: root.io.k8s.api.discovery.v1.EndpointSliceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListDiscoveryV1NamespacedEndpointSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListDiscoveryV1NamespacedEndpointSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListDiscoveryV1NamespacedEndpointSliceResponse = union(enum) {
        ok: ListDiscoveryV1NamespacedEndpointSliceResponse200,
        unauthorized: ListDiscoveryV1NamespacedEndpointSliceResponse401,
        default_response: ListDiscoveryV1NamespacedEndpointSliceResponseDefault,
    };

    pub const listDiscoveryV1NamespacedEndpointSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind EndpointSlice
    pub fn listDiscoveryV1NamespacedEndpointSlice(self: *Client, namespace: []const u8, options: listDiscoveryV1NamespacedEndpointSliceOptions) !ListDiscoveryV1NamespacedEndpointSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/discovery.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpointslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.discovery.v1.EndpointSliceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistDiscoveryV1NamespacedEndpointSlice(self: *Client, namespace: []const u8, options: listDiscoveryV1NamespacedEndpointSliceOptions) !WatchStream(root.io.k8s.api.discovery.v1.EndpointSlice) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/discovery.k8s.io/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/endpointslices");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.discovery.v1.EndpointSlice){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateDiscoveryV1NamespacedEndpointSliceResponse200 = struct {
        body: root.io.k8s.api.discovery.v1.EndpointSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateDiscoveryV1NamespacedEndpointSliceResponse201 = struct {
        body: root.io.k8s.api.discovery.v1.EndpointSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateDiscoveryV1NamespacedEndpointSliceResponse202 = struct {
        body: root.io.k8s.api.discovery.v1.EndpointSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateDiscoveryV1NamespacedEndpointSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateDiscoveryV1NamespacedEndpointSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateDiscoveryV1NamespacedEndpointSliceResponse = union(enum) {
        ok: CreateDiscoveryV1NamespacedEndpointSliceResponse200,
        created: CreateDiscoveryV1NamespacedEndpointSliceResponse201,
        accepted: CreateDiscoveryV1NamespacedEndpointSliceResponse202,
        unauthorized: CreateDiscoveryV1NamespacedEndpointSliceResponse401,
        default_response: CreateDiscoveryV1NamespacedEndpointSliceResponseDefault,
    };

    pub const createDiscoveryV1NamespacedEndpointSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create an EndpointSlice
    pub fn createDiscoveryV1NamespacedEndpointSlice(self: *Client, namespace: []const u8, body: root.io.k8s.api.discovery.v1.EndpointSlice, options: createDiscoveryV1NamespacedEndpointSliceOptions) !CreateDiscoveryV1NamespacedEndpointSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/discovery.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpointslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.discovery.v1.EndpointSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.discovery.v1.EndpointSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.discovery.v1.EndpointSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteDiscoveryV1CollectionNamespacedEndpointSliceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteDiscoveryV1CollectionNamespacedEndpointSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteDiscoveryV1CollectionNamespacedEndpointSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteDiscoveryV1CollectionNamespacedEndpointSliceResponse = union(enum) {
        ok: DeleteDiscoveryV1CollectionNamespacedEndpointSliceResponse200,
        unauthorized: DeleteDiscoveryV1CollectionNamespacedEndpointSliceResponse401,
        default_response: DeleteDiscoveryV1CollectionNamespacedEndpointSliceResponseDefault,
    };

    pub const deleteDiscoveryV1CollectionNamespacedEndpointSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of EndpointSlice
    pub fn deleteDiscoveryV1CollectionNamespacedEndpointSlice(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteDiscoveryV1CollectionNamespacedEndpointSliceOptions) !DeleteDiscoveryV1CollectionNamespacedEndpointSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/discovery.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpointslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadDiscoveryV1NamespacedEndpointSliceResponse200 = struct {
        body: root.io.k8s.api.discovery.v1.EndpointSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadDiscoveryV1NamespacedEndpointSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadDiscoveryV1NamespacedEndpointSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadDiscoveryV1NamespacedEndpointSliceResponse = union(enum) {
        ok: ReadDiscoveryV1NamespacedEndpointSliceResponse200,
        unauthorized: ReadDiscoveryV1NamespacedEndpointSliceResponse401,
        default_response: ReadDiscoveryV1NamespacedEndpointSliceResponseDefault,
    };

    pub const readDiscoveryV1NamespacedEndpointSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified EndpointSlice
    pub fn readDiscoveryV1NamespacedEndpointSlice(self: *Client, name: []const u8, namespace: []const u8, options: readDiscoveryV1NamespacedEndpointSliceOptions) !ReadDiscoveryV1NamespacedEndpointSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/discovery.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpointslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.discovery.v1.EndpointSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceDiscoveryV1NamespacedEndpointSliceResponse200 = struct {
        body: root.io.k8s.api.discovery.v1.EndpointSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceDiscoveryV1NamespacedEndpointSliceResponse201 = struct {
        body: root.io.k8s.api.discovery.v1.EndpointSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceDiscoveryV1NamespacedEndpointSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceDiscoveryV1NamespacedEndpointSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceDiscoveryV1NamespacedEndpointSliceResponse = union(enum) {
        ok: ReplaceDiscoveryV1NamespacedEndpointSliceResponse200,
        created: ReplaceDiscoveryV1NamespacedEndpointSliceResponse201,
        unauthorized: ReplaceDiscoveryV1NamespacedEndpointSliceResponse401,
        default_response: ReplaceDiscoveryV1NamespacedEndpointSliceResponseDefault,
    };

    pub const replaceDiscoveryV1NamespacedEndpointSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified EndpointSlice
    pub fn replaceDiscoveryV1NamespacedEndpointSlice(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.discovery.v1.EndpointSlice, options: replaceDiscoveryV1NamespacedEndpointSliceOptions) !ReplaceDiscoveryV1NamespacedEndpointSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/discovery.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpointslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.discovery.v1.EndpointSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.discovery.v1.EndpointSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteDiscoveryV1NamespacedEndpointSliceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteDiscoveryV1NamespacedEndpointSliceResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteDiscoveryV1NamespacedEndpointSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteDiscoveryV1NamespacedEndpointSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteDiscoveryV1NamespacedEndpointSliceResponse = union(enum) {
        ok: DeleteDiscoveryV1NamespacedEndpointSliceResponse200,
        accepted: DeleteDiscoveryV1NamespacedEndpointSliceResponse202,
        unauthorized: DeleteDiscoveryV1NamespacedEndpointSliceResponse401,
        default_response: DeleteDiscoveryV1NamespacedEndpointSliceResponseDefault,
    };

    pub const deleteDiscoveryV1NamespacedEndpointSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete an EndpointSlice
    pub fn deleteDiscoveryV1NamespacedEndpointSlice(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteDiscoveryV1NamespacedEndpointSliceOptions) !DeleteDiscoveryV1NamespacedEndpointSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/discovery.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpointslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchDiscoveryV1NamespacedEndpointSliceResponse200 = struct {
        body: root.io.k8s.api.discovery.v1.EndpointSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchDiscoveryV1NamespacedEndpointSliceResponse201 = struct {
        body: root.io.k8s.api.discovery.v1.EndpointSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchDiscoveryV1NamespacedEndpointSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchDiscoveryV1NamespacedEndpointSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchDiscoveryV1NamespacedEndpointSliceResponse = union(enum) {
        ok: PatchDiscoveryV1NamespacedEndpointSliceResponse200,
        created: PatchDiscoveryV1NamespacedEndpointSliceResponse201,
        unauthorized: PatchDiscoveryV1NamespacedEndpointSliceResponse401,
        default_response: PatchDiscoveryV1NamespacedEndpointSliceResponseDefault,
    };

    pub const patchDiscoveryV1NamespacedEndpointSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified EndpointSlice
    pub fn patchDiscoveryV1NamespacedEndpointSlice(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchDiscoveryV1NamespacedEndpointSliceOptions) !PatchDiscoveryV1NamespacedEndpointSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/discovery.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpointslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.discovery.v1.EndpointSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.discovery.v1.EndpointSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchDiscoveryV1EndpointSliceListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchDiscoveryV1EndpointSliceListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchDiscoveryV1EndpointSliceListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchDiscoveryV1EndpointSliceListForAllNamespacesResponse = union(enum) {
        ok: WatchDiscoveryV1EndpointSliceListForAllNamespacesResponse200,
        unauthorized: WatchDiscoveryV1EndpointSliceListForAllNamespacesResponse401,
        default_response: WatchDiscoveryV1EndpointSliceListForAllNamespacesResponseDefault,
    };

    pub const watchDiscoveryV1EndpointSliceListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of EndpointSlice. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchDiscoveryV1EndpointSliceListForAllNamespaces(self: *Client, options: watchDiscoveryV1EndpointSliceListForAllNamespacesOptions) !WatchDiscoveryV1EndpointSliceListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/discovery.k8s.io/v1/watch/endpointslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchDiscoveryV1NamespacedEndpointSliceListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchDiscoveryV1NamespacedEndpointSliceListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchDiscoveryV1NamespacedEndpointSliceListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchDiscoveryV1NamespacedEndpointSliceListResponse = union(enum) {
        ok: WatchDiscoveryV1NamespacedEndpointSliceListResponse200,
        unauthorized: WatchDiscoveryV1NamespacedEndpointSliceListResponse401,
        default_response: WatchDiscoveryV1NamespacedEndpointSliceListResponseDefault,
    };

    pub const watchDiscoveryV1NamespacedEndpointSliceListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of EndpointSlice. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchDiscoveryV1NamespacedEndpointSliceList(self: *Client, namespace: []const u8, options: watchDiscoveryV1NamespacedEndpointSliceListOptions) !WatchDiscoveryV1NamespacedEndpointSliceListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/discovery.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpointslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchDiscoveryV1NamespacedEndpointSliceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchDiscoveryV1NamespacedEndpointSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchDiscoveryV1NamespacedEndpointSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchDiscoveryV1NamespacedEndpointSliceResponse = union(enum) {
        ok: WatchDiscoveryV1NamespacedEndpointSliceResponse200,
        unauthorized: WatchDiscoveryV1NamespacedEndpointSliceResponse401,
        default_response: WatchDiscoveryV1NamespacedEndpointSliceResponseDefault,
    };

    pub const watchDiscoveryV1NamespacedEndpointSliceOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind EndpointSlice. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchDiscoveryV1NamespacedEndpointSlice(self: *Client, name: []const u8, namespace: []const u8, options: watchDiscoveryV1NamespacedEndpointSliceOptions) !WatchDiscoveryV1NamespacedEndpointSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/discovery.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/endpointslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetEventsAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetEventsAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetEventsAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetEventsAPIGroupResponse = union(enum) {
        ok: GetEventsAPIGroupResponse200,
        unauthorized: GetEventsAPIGroupResponse401,
        default_response: GetEventsAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getEventsAPIGroup(self: *Client) !GetEventsAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/events.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetEventsV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetEventsV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetEventsV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetEventsV1APIResourcesResponse = union(enum) {
        ok: GetEventsV1APIResourcesResponse200,
        unauthorized: GetEventsV1APIResourcesResponse401,
        default_response: GetEventsV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getEventsV1APIResources(self: *Client) !GetEventsV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/events.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListEventsV1EventForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.events.v1.EventList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListEventsV1EventForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListEventsV1EventForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListEventsV1EventForAllNamespacesResponse = union(enum) {
        ok: ListEventsV1EventForAllNamespacesResponse200,
        unauthorized: ListEventsV1EventForAllNamespacesResponse401,
        default_response: ListEventsV1EventForAllNamespacesResponseDefault,
    };

    pub const listEventsV1EventForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Event
    pub fn listEventsV1EventForAllNamespaces(self: *Client, options: listEventsV1EventForAllNamespacesOptions) !ListEventsV1EventForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/events.k8s.io/v1/events", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.events.v1.EventList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistEventsV1EventForAllNamespaces(self: *Client, options: listEventsV1EventForAllNamespacesOptions) !WatchStream(root.io.k8s.api.events.v1.Event) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/events.k8s.io/v1/events");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.events.v1.Event){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListEventsV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.api.events.v1.EventList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListEventsV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListEventsV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListEventsV1NamespacedEventResponse = union(enum) {
        ok: ListEventsV1NamespacedEventResponse200,
        unauthorized: ListEventsV1NamespacedEventResponse401,
        default_response: ListEventsV1NamespacedEventResponseDefault,
    };

    pub const listEventsV1NamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Event
    pub fn listEventsV1NamespacedEvent(self: *Client, namespace: []const u8, options: listEventsV1NamespacedEventOptions) !ListEventsV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/events.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.events.v1.EventList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistEventsV1NamespacedEvent(self: *Client, namespace: []const u8, options: listEventsV1NamespacedEventOptions) !WatchStream(root.io.k8s.api.events.v1.Event) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/events.k8s.io/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/events");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.events.v1.Event){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateEventsV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.api.events.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateEventsV1NamespacedEventResponse201 = struct {
        body: root.io.k8s.api.events.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateEventsV1NamespacedEventResponse202 = struct {
        body: root.io.k8s.api.events.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateEventsV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateEventsV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateEventsV1NamespacedEventResponse = union(enum) {
        ok: CreateEventsV1NamespacedEventResponse200,
        created: CreateEventsV1NamespacedEventResponse201,
        accepted: CreateEventsV1NamespacedEventResponse202,
        unauthorized: CreateEventsV1NamespacedEventResponse401,
        default_response: CreateEventsV1NamespacedEventResponseDefault,
    };

    pub const createEventsV1NamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create an Event
    pub fn createEventsV1NamespacedEvent(self: *Client, namespace: []const u8, body: root.io.k8s.api.events.v1.Event, options: createEventsV1NamespacedEventOptions) !CreateEventsV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/events.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.events.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.events.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.events.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteEventsV1CollectionNamespacedEventResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteEventsV1CollectionNamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteEventsV1CollectionNamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteEventsV1CollectionNamespacedEventResponse = union(enum) {
        ok: DeleteEventsV1CollectionNamespacedEventResponse200,
        unauthorized: DeleteEventsV1CollectionNamespacedEventResponse401,
        default_response: DeleteEventsV1CollectionNamespacedEventResponseDefault,
    };

    pub const deleteEventsV1CollectionNamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Event
    pub fn deleteEventsV1CollectionNamespacedEvent(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteEventsV1CollectionNamespacedEventOptions) !DeleteEventsV1CollectionNamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/events.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadEventsV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.api.events.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadEventsV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadEventsV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadEventsV1NamespacedEventResponse = union(enum) {
        ok: ReadEventsV1NamespacedEventResponse200,
        unauthorized: ReadEventsV1NamespacedEventResponse401,
        default_response: ReadEventsV1NamespacedEventResponseDefault,
    };

    pub const readEventsV1NamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Event
    pub fn readEventsV1NamespacedEvent(self: *Client, name: []const u8, namespace: []const u8, options: readEventsV1NamespacedEventOptions) !ReadEventsV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/events.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.events.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceEventsV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.api.events.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceEventsV1NamespacedEventResponse201 = struct {
        body: root.io.k8s.api.events.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceEventsV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceEventsV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceEventsV1NamespacedEventResponse = union(enum) {
        ok: ReplaceEventsV1NamespacedEventResponse200,
        created: ReplaceEventsV1NamespacedEventResponse201,
        unauthorized: ReplaceEventsV1NamespacedEventResponse401,
        default_response: ReplaceEventsV1NamespacedEventResponseDefault,
    };

    pub const replaceEventsV1NamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Event
    pub fn replaceEventsV1NamespacedEvent(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.events.v1.Event, options: replaceEventsV1NamespacedEventOptions) !ReplaceEventsV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/events.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.events.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.events.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteEventsV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteEventsV1NamespacedEventResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteEventsV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteEventsV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteEventsV1NamespacedEventResponse = union(enum) {
        ok: DeleteEventsV1NamespacedEventResponse200,
        accepted: DeleteEventsV1NamespacedEventResponse202,
        unauthorized: DeleteEventsV1NamespacedEventResponse401,
        default_response: DeleteEventsV1NamespacedEventResponseDefault,
    };

    pub const deleteEventsV1NamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete an Event
    pub fn deleteEventsV1NamespacedEvent(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteEventsV1NamespacedEventOptions) !DeleteEventsV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/events.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchEventsV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.api.events.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchEventsV1NamespacedEventResponse201 = struct {
        body: root.io.k8s.api.events.v1.Event,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchEventsV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchEventsV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchEventsV1NamespacedEventResponse = union(enum) {
        ok: PatchEventsV1NamespacedEventResponse200,
        created: PatchEventsV1NamespacedEventResponse201,
        unauthorized: PatchEventsV1NamespacedEventResponse401,
        default_response: PatchEventsV1NamespacedEventResponseDefault,
    };

    pub const patchEventsV1NamespacedEventOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Event
    pub fn patchEventsV1NamespacedEvent(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchEventsV1NamespacedEventOptions) !PatchEventsV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/events.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.events.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.events.v1.Event, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchEventsV1EventListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchEventsV1EventListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchEventsV1EventListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchEventsV1EventListForAllNamespacesResponse = union(enum) {
        ok: WatchEventsV1EventListForAllNamespacesResponse200,
        unauthorized: WatchEventsV1EventListForAllNamespacesResponse401,
        default_response: WatchEventsV1EventListForAllNamespacesResponseDefault,
    };

    pub const watchEventsV1EventListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Event. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchEventsV1EventListForAllNamespaces(self: *Client, options: watchEventsV1EventListForAllNamespacesOptions) !WatchEventsV1EventListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/events.k8s.io/v1/watch/events", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchEventsV1NamespacedEventListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchEventsV1NamespacedEventListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchEventsV1NamespacedEventListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchEventsV1NamespacedEventListResponse = union(enum) {
        ok: WatchEventsV1NamespacedEventListResponse200,
        unauthorized: WatchEventsV1NamespacedEventListResponse401,
        default_response: WatchEventsV1NamespacedEventListResponseDefault,
    };

    pub const watchEventsV1NamespacedEventListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Event. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchEventsV1NamespacedEventList(self: *Client, namespace: []const u8, options: watchEventsV1NamespacedEventListOptions) !WatchEventsV1NamespacedEventListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/events.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchEventsV1NamespacedEventResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchEventsV1NamespacedEventResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchEventsV1NamespacedEventResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchEventsV1NamespacedEventResponse = union(enum) {
        ok: WatchEventsV1NamespacedEventResponse200,
        unauthorized: WatchEventsV1NamespacedEventResponse401,
        default_response: WatchEventsV1NamespacedEventResponseDefault,
    };

    pub const watchEventsV1NamespacedEventOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Event. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchEventsV1NamespacedEvent(self: *Client, name: []const u8, namespace: []const u8, options: watchEventsV1NamespacedEventOptions) !WatchEventsV1NamespacedEventResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/events.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/events/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetFlowcontrolApiserverAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetFlowcontrolApiserverAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetFlowcontrolApiserverAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetFlowcontrolApiserverAPIGroupResponse = union(enum) {
        ok: GetFlowcontrolApiserverAPIGroupResponse200,
        unauthorized: GetFlowcontrolApiserverAPIGroupResponse401,
        default_response: GetFlowcontrolApiserverAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getFlowcontrolApiserverAPIGroup(self: *Client) !GetFlowcontrolApiserverAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetFlowcontrolApiserverV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetFlowcontrolApiserverV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetFlowcontrolApiserverV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetFlowcontrolApiserverV1APIResourcesResponse = union(enum) {
        ok: GetFlowcontrolApiserverV1APIResourcesResponse200,
        unauthorized: GetFlowcontrolApiserverV1APIResourcesResponse401,
        default_response: GetFlowcontrolApiserverV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getFlowcontrolApiserverV1APIResources(self: *Client) !GetFlowcontrolApiserverV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListFlowcontrolApiserverV1FlowSchemaResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchemaList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListFlowcontrolApiserverV1FlowSchemaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListFlowcontrolApiserverV1FlowSchemaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListFlowcontrolApiserverV1FlowSchemaResponse = union(enum) {
        ok: ListFlowcontrolApiserverV1FlowSchemaResponse200,
        unauthorized: ListFlowcontrolApiserverV1FlowSchemaResponse401,
        default_response: ListFlowcontrolApiserverV1FlowSchemaResponseDefault,
    };

    pub const listFlowcontrolApiserverV1FlowSchemaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind FlowSchema
    pub fn listFlowcontrolApiserverV1FlowSchema(self: *Client, options: listFlowcontrolApiserverV1FlowSchemaOptions) !ListFlowcontrolApiserverV1FlowSchemaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/flowschemas", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchemaList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistFlowcontrolApiserverV1FlowSchema(self: *Client, options: listFlowcontrolApiserverV1FlowSchemaOptions) !WatchStream(root.io.k8s.api.flowcontrol.v1.FlowSchema) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/flowcontrol.apiserver.k8s.io/v1/flowschemas");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.flowcontrol.v1.FlowSchema){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateFlowcontrolApiserverV1FlowSchemaResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchema,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateFlowcontrolApiserverV1FlowSchemaResponse201 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchema,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateFlowcontrolApiserverV1FlowSchemaResponse202 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchema,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateFlowcontrolApiserverV1FlowSchemaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateFlowcontrolApiserverV1FlowSchemaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateFlowcontrolApiserverV1FlowSchemaResponse = union(enum) {
        ok: CreateFlowcontrolApiserverV1FlowSchemaResponse200,
        created: CreateFlowcontrolApiserverV1FlowSchemaResponse201,
        accepted: CreateFlowcontrolApiserverV1FlowSchemaResponse202,
        unauthorized: CreateFlowcontrolApiserverV1FlowSchemaResponse401,
        default_response: CreateFlowcontrolApiserverV1FlowSchemaResponseDefault,
    };

    pub const createFlowcontrolApiserverV1FlowSchemaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a FlowSchema
    pub fn createFlowcontrolApiserverV1FlowSchema(self: *Client, body: root.io.k8s.api.flowcontrol.v1.FlowSchema, options: createFlowcontrolApiserverV1FlowSchemaOptions) !CreateFlowcontrolApiserverV1FlowSchemaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/flowschemas", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchema, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchema, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchema, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteFlowcontrolApiserverV1CollectionFlowSchemaResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1CollectionFlowSchemaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1CollectionFlowSchemaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1CollectionFlowSchemaResponse = union(enum) {
        ok: DeleteFlowcontrolApiserverV1CollectionFlowSchemaResponse200,
        unauthorized: DeleteFlowcontrolApiserverV1CollectionFlowSchemaResponse401,
        default_response: DeleteFlowcontrolApiserverV1CollectionFlowSchemaResponseDefault,
    };

    pub const deleteFlowcontrolApiserverV1CollectionFlowSchemaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of FlowSchema
    pub fn deleteFlowcontrolApiserverV1CollectionFlowSchema(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteFlowcontrolApiserverV1CollectionFlowSchemaOptions) !DeleteFlowcontrolApiserverV1CollectionFlowSchemaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/flowschemas", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadFlowcontrolApiserverV1FlowSchemaResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchema,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadFlowcontrolApiserverV1FlowSchemaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadFlowcontrolApiserverV1FlowSchemaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadFlowcontrolApiserverV1FlowSchemaResponse = union(enum) {
        ok: ReadFlowcontrolApiserverV1FlowSchemaResponse200,
        unauthorized: ReadFlowcontrolApiserverV1FlowSchemaResponse401,
        default_response: ReadFlowcontrolApiserverV1FlowSchemaResponseDefault,
    };

    pub const readFlowcontrolApiserverV1FlowSchemaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified FlowSchema
    pub fn readFlowcontrolApiserverV1FlowSchema(self: *Client, name: []const u8, options: readFlowcontrolApiserverV1FlowSchemaOptions) !ReadFlowcontrolApiserverV1FlowSchemaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/flowschemas/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchema, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceFlowcontrolApiserverV1FlowSchemaResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchema,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1FlowSchemaResponse201 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchema,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1FlowSchemaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1FlowSchemaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1FlowSchemaResponse = union(enum) {
        ok: ReplaceFlowcontrolApiserverV1FlowSchemaResponse200,
        created: ReplaceFlowcontrolApiserverV1FlowSchemaResponse201,
        unauthorized: ReplaceFlowcontrolApiserverV1FlowSchemaResponse401,
        default_response: ReplaceFlowcontrolApiserverV1FlowSchemaResponseDefault,
    };

    pub const replaceFlowcontrolApiserverV1FlowSchemaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified FlowSchema
    pub fn replaceFlowcontrolApiserverV1FlowSchema(self: *Client, name: []const u8, body: root.io.k8s.api.flowcontrol.v1.FlowSchema, options: replaceFlowcontrolApiserverV1FlowSchemaOptions) !ReplaceFlowcontrolApiserverV1FlowSchemaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/flowschemas/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchema, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchema, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteFlowcontrolApiserverV1FlowSchemaResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1FlowSchemaResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1FlowSchemaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1FlowSchemaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1FlowSchemaResponse = union(enum) {
        ok: DeleteFlowcontrolApiserverV1FlowSchemaResponse200,
        accepted: DeleteFlowcontrolApiserverV1FlowSchemaResponse202,
        unauthorized: DeleteFlowcontrolApiserverV1FlowSchemaResponse401,
        default_response: DeleteFlowcontrolApiserverV1FlowSchemaResponseDefault,
    };

    pub const deleteFlowcontrolApiserverV1FlowSchemaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a FlowSchema
    pub fn deleteFlowcontrolApiserverV1FlowSchema(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteFlowcontrolApiserverV1FlowSchemaOptions) !DeleteFlowcontrolApiserverV1FlowSchemaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/flowschemas/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchFlowcontrolApiserverV1FlowSchemaResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchema,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1FlowSchemaResponse201 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchema,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1FlowSchemaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1FlowSchemaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1FlowSchemaResponse = union(enum) {
        ok: PatchFlowcontrolApiserverV1FlowSchemaResponse200,
        created: PatchFlowcontrolApiserverV1FlowSchemaResponse201,
        unauthorized: PatchFlowcontrolApiserverV1FlowSchemaResponse401,
        default_response: PatchFlowcontrolApiserverV1FlowSchemaResponseDefault,
    };

    pub const patchFlowcontrolApiserverV1FlowSchemaOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified FlowSchema
    pub fn patchFlowcontrolApiserverV1FlowSchema(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchFlowcontrolApiserverV1FlowSchemaOptions) !PatchFlowcontrolApiserverV1FlowSchemaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/flowschemas/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchema, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchema, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadFlowcontrolApiserverV1FlowSchemaStatusResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchema,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadFlowcontrolApiserverV1FlowSchemaStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadFlowcontrolApiserverV1FlowSchemaStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadFlowcontrolApiserverV1FlowSchemaStatusResponse = union(enum) {
        ok: ReadFlowcontrolApiserverV1FlowSchemaStatusResponse200,
        unauthorized: ReadFlowcontrolApiserverV1FlowSchemaStatusResponse401,
        default_response: ReadFlowcontrolApiserverV1FlowSchemaStatusResponseDefault,
    };

    pub const readFlowcontrolApiserverV1FlowSchemaStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified FlowSchema
    pub fn readFlowcontrolApiserverV1FlowSchemaStatus(self: *Client, name: []const u8, options: readFlowcontrolApiserverV1FlowSchemaStatusOptions) !ReadFlowcontrolApiserverV1FlowSchemaStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/flowschemas/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchema, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceFlowcontrolApiserverV1FlowSchemaStatusResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchema,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1FlowSchemaStatusResponse201 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchema,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1FlowSchemaStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1FlowSchemaStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1FlowSchemaStatusResponse = union(enum) {
        ok: ReplaceFlowcontrolApiserverV1FlowSchemaStatusResponse200,
        created: ReplaceFlowcontrolApiserverV1FlowSchemaStatusResponse201,
        unauthorized: ReplaceFlowcontrolApiserverV1FlowSchemaStatusResponse401,
        default_response: ReplaceFlowcontrolApiserverV1FlowSchemaStatusResponseDefault,
    };

    pub const replaceFlowcontrolApiserverV1FlowSchemaStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified FlowSchema
    pub fn replaceFlowcontrolApiserverV1FlowSchemaStatus(self: *Client, name: []const u8, body: root.io.k8s.api.flowcontrol.v1.FlowSchema, options: replaceFlowcontrolApiserverV1FlowSchemaStatusOptions) !ReplaceFlowcontrolApiserverV1FlowSchemaStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/flowschemas/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchema, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchema, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchFlowcontrolApiserverV1FlowSchemaStatusResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchema,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1FlowSchemaStatusResponse201 = struct {
        body: root.io.k8s.api.flowcontrol.v1.FlowSchema,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1FlowSchemaStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1FlowSchemaStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1FlowSchemaStatusResponse = union(enum) {
        ok: PatchFlowcontrolApiserverV1FlowSchemaStatusResponse200,
        created: PatchFlowcontrolApiserverV1FlowSchemaStatusResponse201,
        unauthorized: PatchFlowcontrolApiserverV1FlowSchemaStatusResponse401,
        default_response: PatchFlowcontrolApiserverV1FlowSchemaStatusResponseDefault,
    };

    pub const patchFlowcontrolApiserverV1FlowSchemaStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified FlowSchema
    pub fn patchFlowcontrolApiserverV1FlowSchemaStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchFlowcontrolApiserverV1FlowSchemaStatusOptions) !PatchFlowcontrolApiserverV1FlowSchemaStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/flowschemas/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchema, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.FlowSchema, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListFlowcontrolApiserverV1PriorityLevelConfigurationResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfigurationList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListFlowcontrolApiserverV1PriorityLevelConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListFlowcontrolApiserverV1PriorityLevelConfigurationResponse = union(enum) {
        ok: ListFlowcontrolApiserverV1PriorityLevelConfigurationResponse200,
        unauthorized: ListFlowcontrolApiserverV1PriorityLevelConfigurationResponse401,
        default_response: ListFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault,
    };

    pub const listFlowcontrolApiserverV1PriorityLevelConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PriorityLevelConfiguration
    pub fn listFlowcontrolApiserverV1PriorityLevelConfiguration(self: *Client, options: listFlowcontrolApiserverV1PriorityLevelConfigurationOptions) !ListFlowcontrolApiserverV1PriorityLevelConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/prioritylevelconfigurations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfigurationList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistFlowcontrolApiserverV1PriorityLevelConfiguration(self: *Client, options: listFlowcontrolApiserverV1PriorityLevelConfigurationOptions) !WatchStream(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/flowcontrol.apiserver.k8s.io/v1/prioritylevelconfigurations");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateFlowcontrolApiserverV1PriorityLevelConfigurationResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateFlowcontrolApiserverV1PriorityLevelConfigurationResponse201 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateFlowcontrolApiserverV1PriorityLevelConfigurationResponse202 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateFlowcontrolApiserverV1PriorityLevelConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateFlowcontrolApiserverV1PriorityLevelConfigurationResponse = union(enum) {
        ok: CreateFlowcontrolApiserverV1PriorityLevelConfigurationResponse200,
        created: CreateFlowcontrolApiserverV1PriorityLevelConfigurationResponse201,
        accepted: CreateFlowcontrolApiserverV1PriorityLevelConfigurationResponse202,
        unauthorized: CreateFlowcontrolApiserverV1PriorityLevelConfigurationResponse401,
        default_response: CreateFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault,
    };

    pub const createFlowcontrolApiserverV1PriorityLevelConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a PriorityLevelConfiguration
    pub fn createFlowcontrolApiserverV1PriorityLevelConfiguration(self: *Client, body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, options: createFlowcontrolApiserverV1PriorityLevelConfigurationOptions) !CreateFlowcontrolApiserverV1PriorityLevelConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/prioritylevelconfigurations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteFlowcontrolApiserverV1CollectionPriorityLevelConfigurationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1CollectionPriorityLevelConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1CollectionPriorityLevelConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1CollectionPriorityLevelConfigurationResponse = union(enum) {
        ok: DeleteFlowcontrolApiserverV1CollectionPriorityLevelConfigurationResponse200,
        unauthorized: DeleteFlowcontrolApiserverV1CollectionPriorityLevelConfigurationResponse401,
        default_response: DeleteFlowcontrolApiserverV1CollectionPriorityLevelConfigurationResponseDefault,
    };

    pub const deleteFlowcontrolApiserverV1CollectionPriorityLevelConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of PriorityLevelConfiguration
    pub fn deleteFlowcontrolApiserverV1CollectionPriorityLevelConfiguration(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteFlowcontrolApiserverV1CollectionPriorityLevelConfigurationOptions) !DeleteFlowcontrolApiserverV1CollectionPriorityLevelConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/prioritylevelconfigurations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadFlowcontrolApiserverV1PriorityLevelConfigurationResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadFlowcontrolApiserverV1PriorityLevelConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadFlowcontrolApiserverV1PriorityLevelConfigurationResponse = union(enum) {
        ok: ReadFlowcontrolApiserverV1PriorityLevelConfigurationResponse200,
        unauthorized: ReadFlowcontrolApiserverV1PriorityLevelConfigurationResponse401,
        default_response: ReadFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault,
    };

    pub const readFlowcontrolApiserverV1PriorityLevelConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified PriorityLevelConfiguration
    pub fn readFlowcontrolApiserverV1PriorityLevelConfiguration(self: *Client, name: []const u8, options: readFlowcontrolApiserverV1PriorityLevelConfigurationOptions) !ReadFlowcontrolApiserverV1PriorityLevelConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/prioritylevelconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationResponse201 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationResponse = union(enum) {
        ok: ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationResponse200,
        created: ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationResponse201,
        unauthorized: ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationResponse401,
        default_response: ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault,
    };

    pub const replaceFlowcontrolApiserverV1PriorityLevelConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified PriorityLevelConfiguration
    pub fn replaceFlowcontrolApiserverV1PriorityLevelConfiguration(self: *Client, name: []const u8, body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, options: replaceFlowcontrolApiserverV1PriorityLevelConfigurationOptions) !ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/prioritylevelconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteFlowcontrolApiserverV1PriorityLevelConfigurationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1PriorityLevelConfigurationResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1PriorityLevelConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteFlowcontrolApiserverV1PriorityLevelConfigurationResponse = union(enum) {
        ok: DeleteFlowcontrolApiserverV1PriorityLevelConfigurationResponse200,
        accepted: DeleteFlowcontrolApiserverV1PriorityLevelConfigurationResponse202,
        unauthorized: DeleteFlowcontrolApiserverV1PriorityLevelConfigurationResponse401,
        default_response: DeleteFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault,
    };

    pub const deleteFlowcontrolApiserverV1PriorityLevelConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a PriorityLevelConfiguration
    pub fn deleteFlowcontrolApiserverV1PriorityLevelConfiguration(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteFlowcontrolApiserverV1PriorityLevelConfigurationOptions) !DeleteFlowcontrolApiserverV1PriorityLevelConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/prioritylevelconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse201 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse = union(enum) {
        ok: PatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse200,
        created: PatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse201,
        unauthorized: PatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse401,
        default_response: PatchFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault,
    };

    pub const patchFlowcontrolApiserverV1PriorityLevelConfigurationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified PriorityLevelConfiguration
    pub fn patchFlowcontrolApiserverV1PriorityLevelConfiguration(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchFlowcontrolApiserverV1PriorityLevelConfigurationOptions) !PatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/prioritylevelconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse = union(enum) {
        ok: ReadFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse200,
        unauthorized: ReadFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse401,
        default_response: ReadFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponseDefault,
    };

    pub const readFlowcontrolApiserverV1PriorityLevelConfigurationStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified PriorityLevelConfiguration
    pub fn readFlowcontrolApiserverV1PriorityLevelConfigurationStatus(self: *Client, name: []const u8, options: readFlowcontrolApiserverV1PriorityLevelConfigurationStatusOptions) !ReadFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/prioritylevelconfigurations/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse201 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse = union(enum) {
        ok: ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse200,
        created: ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse201,
        unauthorized: ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse401,
        default_response: ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponseDefault,
    };

    pub const replaceFlowcontrolApiserverV1PriorityLevelConfigurationStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified PriorityLevelConfiguration
    pub fn replaceFlowcontrolApiserverV1PriorityLevelConfigurationStatus(self: *Client, name: []const u8, body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, options: replaceFlowcontrolApiserverV1PriorityLevelConfigurationStatusOptions) !ReplaceFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/prioritylevelconfigurations/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse200 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse201 = struct {
        body: root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse = union(enum) {
        ok: PatchFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse200,
        created: PatchFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse201,
        unauthorized: PatchFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse401,
        default_response: PatchFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponseDefault,
    };

    pub const patchFlowcontrolApiserverV1PriorityLevelConfigurationStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified PriorityLevelConfiguration
    pub fn patchFlowcontrolApiserverV1PriorityLevelConfigurationStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchFlowcontrolApiserverV1PriorityLevelConfigurationStatusOptions) !PatchFlowcontrolApiserverV1PriorityLevelConfigurationStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/prioritylevelconfigurations/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.flowcontrol.v1.PriorityLevelConfiguration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchFlowcontrolApiserverV1FlowSchemaListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchFlowcontrolApiserverV1FlowSchemaListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchFlowcontrolApiserverV1FlowSchemaListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchFlowcontrolApiserverV1FlowSchemaListResponse = union(enum) {
        ok: WatchFlowcontrolApiserverV1FlowSchemaListResponse200,
        unauthorized: WatchFlowcontrolApiserverV1FlowSchemaListResponse401,
        default_response: WatchFlowcontrolApiserverV1FlowSchemaListResponseDefault,
    };

    pub const watchFlowcontrolApiserverV1FlowSchemaListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of FlowSchema. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchFlowcontrolApiserverV1FlowSchemaList(self: *Client, options: watchFlowcontrolApiserverV1FlowSchemaListOptions) !WatchFlowcontrolApiserverV1FlowSchemaListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/watch/flowschemas", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchFlowcontrolApiserverV1FlowSchemaResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchFlowcontrolApiserverV1FlowSchemaResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchFlowcontrolApiserverV1FlowSchemaResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchFlowcontrolApiserverV1FlowSchemaResponse = union(enum) {
        ok: WatchFlowcontrolApiserverV1FlowSchemaResponse200,
        unauthorized: WatchFlowcontrolApiserverV1FlowSchemaResponse401,
        default_response: WatchFlowcontrolApiserverV1FlowSchemaResponseDefault,
    };

    pub const watchFlowcontrolApiserverV1FlowSchemaOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind FlowSchema. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchFlowcontrolApiserverV1FlowSchema(self: *Client, name: []const u8, options: watchFlowcontrolApiserverV1FlowSchemaOptions) !WatchFlowcontrolApiserverV1FlowSchemaResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/watch/flowschemas/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchFlowcontrolApiserverV1PriorityLevelConfigurationListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchFlowcontrolApiserverV1PriorityLevelConfigurationListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchFlowcontrolApiserverV1PriorityLevelConfigurationListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchFlowcontrolApiserverV1PriorityLevelConfigurationListResponse = union(enum) {
        ok: WatchFlowcontrolApiserverV1PriorityLevelConfigurationListResponse200,
        unauthorized: WatchFlowcontrolApiserverV1PriorityLevelConfigurationListResponse401,
        default_response: WatchFlowcontrolApiserverV1PriorityLevelConfigurationListResponseDefault,
    };

    pub const watchFlowcontrolApiserverV1PriorityLevelConfigurationListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PriorityLevelConfiguration. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchFlowcontrolApiserverV1PriorityLevelConfigurationList(self: *Client, options: watchFlowcontrolApiserverV1PriorityLevelConfigurationListOptions) !WatchFlowcontrolApiserverV1PriorityLevelConfigurationListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/watch/prioritylevelconfigurations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse = union(enum) {
        ok: WatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse200,
        unauthorized: WatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse401,
        default_response: WatchFlowcontrolApiserverV1PriorityLevelConfigurationResponseDefault,
    };

    pub const watchFlowcontrolApiserverV1PriorityLevelConfigurationOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind PriorityLevelConfiguration. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchFlowcontrolApiserverV1PriorityLevelConfiguration(self: *Client, name: []const u8, options: watchFlowcontrolApiserverV1PriorityLevelConfigurationOptions) !WatchFlowcontrolApiserverV1PriorityLevelConfigurationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/flowcontrol.apiserver.k8s.io/v1/watch/prioritylevelconfigurations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetInternalApiserverAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetInternalApiserverAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetInternalApiserverAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetInternalApiserverAPIGroupResponse = union(enum) {
        ok: GetInternalApiserverAPIGroupResponse200,
        unauthorized: GetInternalApiserverAPIGroupResponse401,
        default_response: GetInternalApiserverAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getInternalApiserverAPIGroup(self: *Client) !GetInternalApiserverAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetInternalApiserverV1alpha1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetInternalApiserverV1alpha1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetInternalApiserverV1alpha1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetInternalApiserverV1alpha1APIResourcesResponse = union(enum) {
        ok: GetInternalApiserverV1alpha1APIResourcesResponse200,
        unauthorized: GetInternalApiserverV1alpha1APIResourcesResponse401,
        default_response: GetInternalApiserverV1alpha1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getInternalApiserverV1alpha1APIResources(self: *Client) !GetInternalApiserverV1alpha1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/v1alpha1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListInternalApiserverV1alpha1StorageVersionResponse200 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersionList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListInternalApiserverV1alpha1StorageVersionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListInternalApiserverV1alpha1StorageVersionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListInternalApiserverV1alpha1StorageVersionResponse = union(enum) {
        ok: ListInternalApiserverV1alpha1StorageVersionResponse200,
        unauthorized: ListInternalApiserverV1alpha1StorageVersionResponse401,
        default_response: ListInternalApiserverV1alpha1StorageVersionResponseDefault,
    };

    pub const listInternalApiserverV1alpha1StorageVersionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind StorageVersion
    pub fn listInternalApiserverV1alpha1StorageVersion(self: *Client, options: listInternalApiserverV1alpha1StorageVersionOptions) !ListInternalApiserverV1alpha1StorageVersionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/v1alpha1/storageversions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersionList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistInternalApiserverV1alpha1StorageVersion(self: *Client, options: listInternalApiserverV1alpha1StorageVersionOptions) !WatchStream(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/internal.apiserver.k8s.io/v1alpha1/storageversions");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateInternalApiserverV1alpha1StorageVersionResponse200 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateInternalApiserverV1alpha1StorageVersionResponse201 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateInternalApiserverV1alpha1StorageVersionResponse202 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateInternalApiserverV1alpha1StorageVersionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateInternalApiserverV1alpha1StorageVersionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateInternalApiserverV1alpha1StorageVersionResponse = union(enum) {
        ok: CreateInternalApiserverV1alpha1StorageVersionResponse200,
        created: CreateInternalApiserverV1alpha1StorageVersionResponse201,
        accepted: CreateInternalApiserverV1alpha1StorageVersionResponse202,
        unauthorized: CreateInternalApiserverV1alpha1StorageVersionResponse401,
        default_response: CreateInternalApiserverV1alpha1StorageVersionResponseDefault,
    };

    pub const createInternalApiserverV1alpha1StorageVersionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a StorageVersion
    pub fn createInternalApiserverV1alpha1StorageVersion(self: *Client, body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, options: createInternalApiserverV1alpha1StorageVersionOptions) !CreateInternalApiserverV1alpha1StorageVersionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/v1alpha1/storageversions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteInternalApiserverV1alpha1CollectionStorageVersionResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteInternalApiserverV1alpha1CollectionStorageVersionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteInternalApiserverV1alpha1CollectionStorageVersionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteInternalApiserverV1alpha1CollectionStorageVersionResponse = union(enum) {
        ok: DeleteInternalApiserverV1alpha1CollectionStorageVersionResponse200,
        unauthorized: DeleteInternalApiserverV1alpha1CollectionStorageVersionResponse401,
        default_response: DeleteInternalApiserverV1alpha1CollectionStorageVersionResponseDefault,
    };

    pub const deleteInternalApiserverV1alpha1CollectionStorageVersionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of StorageVersion
    pub fn deleteInternalApiserverV1alpha1CollectionStorageVersion(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteInternalApiserverV1alpha1CollectionStorageVersionOptions) !DeleteInternalApiserverV1alpha1CollectionStorageVersionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/v1alpha1/storageversions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadInternalApiserverV1alpha1StorageVersionResponse200 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadInternalApiserverV1alpha1StorageVersionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadInternalApiserverV1alpha1StorageVersionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadInternalApiserverV1alpha1StorageVersionResponse = union(enum) {
        ok: ReadInternalApiserverV1alpha1StorageVersionResponse200,
        unauthorized: ReadInternalApiserverV1alpha1StorageVersionResponse401,
        default_response: ReadInternalApiserverV1alpha1StorageVersionResponseDefault,
    };

    pub const readInternalApiserverV1alpha1StorageVersionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified StorageVersion
    pub fn readInternalApiserverV1alpha1StorageVersion(self: *Client, name: []const u8, options: readInternalApiserverV1alpha1StorageVersionOptions) !ReadInternalApiserverV1alpha1StorageVersionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/v1alpha1/storageversions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceInternalApiserverV1alpha1StorageVersionResponse200 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceInternalApiserverV1alpha1StorageVersionResponse201 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceInternalApiserverV1alpha1StorageVersionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceInternalApiserverV1alpha1StorageVersionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceInternalApiserverV1alpha1StorageVersionResponse = union(enum) {
        ok: ReplaceInternalApiserverV1alpha1StorageVersionResponse200,
        created: ReplaceInternalApiserverV1alpha1StorageVersionResponse201,
        unauthorized: ReplaceInternalApiserverV1alpha1StorageVersionResponse401,
        default_response: ReplaceInternalApiserverV1alpha1StorageVersionResponseDefault,
    };

    pub const replaceInternalApiserverV1alpha1StorageVersionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified StorageVersion
    pub fn replaceInternalApiserverV1alpha1StorageVersion(self: *Client, name: []const u8, body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, options: replaceInternalApiserverV1alpha1StorageVersionOptions) !ReplaceInternalApiserverV1alpha1StorageVersionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/v1alpha1/storageversions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteInternalApiserverV1alpha1StorageVersionResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteInternalApiserverV1alpha1StorageVersionResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteInternalApiserverV1alpha1StorageVersionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteInternalApiserverV1alpha1StorageVersionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteInternalApiserverV1alpha1StorageVersionResponse = union(enum) {
        ok: DeleteInternalApiserverV1alpha1StorageVersionResponse200,
        accepted: DeleteInternalApiserverV1alpha1StorageVersionResponse202,
        unauthorized: DeleteInternalApiserverV1alpha1StorageVersionResponse401,
        default_response: DeleteInternalApiserverV1alpha1StorageVersionResponseDefault,
    };

    pub const deleteInternalApiserverV1alpha1StorageVersionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a StorageVersion
    pub fn deleteInternalApiserverV1alpha1StorageVersion(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteInternalApiserverV1alpha1StorageVersionOptions) !DeleteInternalApiserverV1alpha1StorageVersionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/v1alpha1/storageversions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchInternalApiserverV1alpha1StorageVersionResponse200 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchInternalApiserverV1alpha1StorageVersionResponse201 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchInternalApiserverV1alpha1StorageVersionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchInternalApiserverV1alpha1StorageVersionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchInternalApiserverV1alpha1StorageVersionResponse = union(enum) {
        ok: PatchInternalApiserverV1alpha1StorageVersionResponse200,
        created: PatchInternalApiserverV1alpha1StorageVersionResponse201,
        unauthorized: PatchInternalApiserverV1alpha1StorageVersionResponse401,
        default_response: PatchInternalApiserverV1alpha1StorageVersionResponseDefault,
    };

    pub const patchInternalApiserverV1alpha1StorageVersionOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified StorageVersion
    pub fn patchInternalApiserverV1alpha1StorageVersion(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchInternalApiserverV1alpha1StorageVersionOptions) !PatchInternalApiserverV1alpha1StorageVersionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/v1alpha1/storageversions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadInternalApiserverV1alpha1StorageVersionStatusResponse200 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadInternalApiserverV1alpha1StorageVersionStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadInternalApiserverV1alpha1StorageVersionStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadInternalApiserverV1alpha1StorageVersionStatusResponse = union(enum) {
        ok: ReadInternalApiserverV1alpha1StorageVersionStatusResponse200,
        unauthorized: ReadInternalApiserverV1alpha1StorageVersionStatusResponse401,
        default_response: ReadInternalApiserverV1alpha1StorageVersionStatusResponseDefault,
    };

    pub const readInternalApiserverV1alpha1StorageVersionStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified StorageVersion
    pub fn readInternalApiserverV1alpha1StorageVersionStatus(self: *Client, name: []const u8, options: readInternalApiserverV1alpha1StorageVersionStatusOptions) !ReadInternalApiserverV1alpha1StorageVersionStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/v1alpha1/storageversions/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceInternalApiserverV1alpha1StorageVersionStatusResponse200 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceInternalApiserverV1alpha1StorageVersionStatusResponse201 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceInternalApiserverV1alpha1StorageVersionStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceInternalApiserverV1alpha1StorageVersionStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceInternalApiserverV1alpha1StorageVersionStatusResponse = union(enum) {
        ok: ReplaceInternalApiserverV1alpha1StorageVersionStatusResponse200,
        created: ReplaceInternalApiserverV1alpha1StorageVersionStatusResponse201,
        unauthorized: ReplaceInternalApiserverV1alpha1StorageVersionStatusResponse401,
        default_response: ReplaceInternalApiserverV1alpha1StorageVersionStatusResponseDefault,
    };

    pub const replaceInternalApiserverV1alpha1StorageVersionStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified StorageVersion
    pub fn replaceInternalApiserverV1alpha1StorageVersionStatus(self: *Client, name: []const u8, body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, options: replaceInternalApiserverV1alpha1StorageVersionStatusOptions) !ReplaceInternalApiserverV1alpha1StorageVersionStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/v1alpha1/storageversions/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchInternalApiserverV1alpha1StorageVersionStatusResponse200 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchInternalApiserverV1alpha1StorageVersionStatusResponse201 = struct {
        body: root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchInternalApiserverV1alpha1StorageVersionStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchInternalApiserverV1alpha1StorageVersionStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchInternalApiserverV1alpha1StorageVersionStatusResponse = union(enum) {
        ok: PatchInternalApiserverV1alpha1StorageVersionStatusResponse200,
        created: PatchInternalApiserverV1alpha1StorageVersionStatusResponse201,
        unauthorized: PatchInternalApiserverV1alpha1StorageVersionStatusResponse401,
        default_response: PatchInternalApiserverV1alpha1StorageVersionStatusResponseDefault,
    };

    pub const patchInternalApiserverV1alpha1StorageVersionStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified StorageVersion
    pub fn patchInternalApiserverV1alpha1StorageVersionStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchInternalApiserverV1alpha1StorageVersionStatusOptions) !PatchInternalApiserverV1alpha1StorageVersionStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/v1alpha1/storageversions/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.apiserverinternal.v1alpha1.StorageVersion, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchInternalApiserverV1alpha1StorageVersionListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchInternalApiserverV1alpha1StorageVersionListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchInternalApiserverV1alpha1StorageVersionListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchInternalApiserverV1alpha1StorageVersionListResponse = union(enum) {
        ok: WatchInternalApiserverV1alpha1StorageVersionListResponse200,
        unauthorized: WatchInternalApiserverV1alpha1StorageVersionListResponse401,
        default_response: WatchInternalApiserverV1alpha1StorageVersionListResponseDefault,
    };

    pub const watchInternalApiserverV1alpha1StorageVersionListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of StorageVersion. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchInternalApiserverV1alpha1StorageVersionList(self: *Client, options: watchInternalApiserverV1alpha1StorageVersionListOptions) !WatchInternalApiserverV1alpha1StorageVersionListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/v1alpha1/watch/storageversions", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchInternalApiserverV1alpha1StorageVersionResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchInternalApiserverV1alpha1StorageVersionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchInternalApiserverV1alpha1StorageVersionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchInternalApiserverV1alpha1StorageVersionResponse = union(enum) {
        ok: WatchInternalApiserverV1alpha1StorageVersionResponse200,
        unauthorized: WatchInternalApiserverV1alpha1StorageVersionResponse401,
        default_response: WatchInternalApiserverV1alpha1StorageVersionResponseDefault,
    };

    pub const watchInternalApiserverV1alpha1StorageVersionOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind StorageVersion. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchInternalApiserverV1alpha1StorageVersion(self: *Client, name: []const u8, options: watchInternalApiserverV1alpha1StorageVersionOptions) !WatchInternalApiserverV1alpha1StorageVersionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/internal.apiserver.k8s.io/v1alpha1/watch/storageversions/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetNetworkingAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetNetworkingAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetNetworkingAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetNetworkingAPIGroupResponse = union(enum) {
        ok: GetNetworkingAPIGroupResponse200,
        unauthorized: GetNetworkingAPIGroupResponse401,
        default_response: GetNetworkingAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getNetworkingAPIGroup(self: *Client) !GetNetworkingAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetNetworkingV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetNetworkingV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetNetworkingV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetNetworkingV1APIResourcesResponse = union(enum) {
        ok: GetNetworkingV1APIResourcesResponse200,
        unauthorized: GetNetworkingV1APIResourcesResponse401,
        default_response: GetNetworkingV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getNetworkingV1APIResources(self: *Client) !GetNetworkingV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListNetworkingV1IngressClassResponse200 = struct {
        body: root.io.k8s.api.networking.v1.IngressClassList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1IngressClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1IngressClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1IngressClassResponse = union(enum) {
        ok: ListNetworkingV1IngressClassResponse200,
        unauthorized: ListNetworkingV1IngressClassResponse401,
        default_response: ListNetworkingV1IngressClassResponseDefault,
    };

    pub const listNetworkingV1IngressClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind IngressClass
    pub fn listNetworkingV1IngressClass(self: *Client, options: listNetworkingV1IngressClassOptions) !ListNetworkingV1IngressClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ingressclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IngressClassList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistNetworkingV1IngressClass(self: *Client, options: listNetworkingV1IngressClassOptions) !WatchStream(root.io.k8s.api.networking.v1.IngressClass) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/networking.k8s.io/v1/ingressclasses");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.networking.v1.IngressClass){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateNetworkingV1IngressClassResponse200 = struct {
        body: root.io.k8s.api.networking.v1.IngressClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1IngressClassResponse201 = struct {
        body: root.io.k8s.api.networking.v1.IngressClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1IngressClassResponse202 = struct {
        body: root.io.k8s.api.networking.v1.IngressClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1IngressClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1IngressClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1IngressClassResponse = union(enum) {
        ok: CreateNetworkingV1IngressClassResponse200,
        created: CreateNetworkingV1IngressClassResponse201,
        accepted: CreateNetworkingV1IngressClassResponse202,
        unauthorized: CreateNetworkingV1IngressClassResponse401,
        default_response: CreateNetworkingV1IngressClassResponseDefault,
    };

    pub const createNetworkingV1IngressClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create an IngressClass
    pub fn createNetworkingV1IngressClass(self: *Client, body: root.io.k8s.api.networking.v1.IngressClass, options: createNetworkingV1IngressClassOptions) !CreateNetworkingV1IngressClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ingressclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IngressClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IngressClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IngressClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteNetworkingV1CollectionIngressClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionIngressClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionIngressClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionIngressClassResponse = union(enum) {
        ok: DeleteNetworkingV1CollectionIngressClassResponse200,
        unauthorized: DeleteNetworkingV1CollectionIngressClassResponse401,
        default_response: DeleteNetworkingV1CollectionIngressClassResponseDefault,
    };

    pub const deleteNetworkingV1CollectionIngressClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of IngressClass
    pub fn deleteNetworkingV1CollectionIngressClass(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteNetworkingV1CollectionIngressClassOptions) !DeleteNetworkingV1CollectionIngressClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ingressclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadNetworkingV1IngressClassResponse200 = struct {
        body: root.io.k8s.api.networking.v1.IngressClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1IngressClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1IngressClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1IngressClassResponse = union(enum) {
        ok: ReadNetworkingV1IngressClassResponse200,
        unauthorized: ReadNetworkingV1IngressClassResponse401,
        default_response: ReadNetworkingV1IngressClassResponseDefault,
    };

    pub const readNetworkingV1IngressClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified IngressClass
    pub fn readNetworkingV1IngressClass(self: *Client, name: []const u8, options: readNetworkingV1IngressClassOptions) !ReadNetworkingV1IngressClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ingressclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IngressClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceNetworkingV1IngressClassResponse200 = struct {
        body: root.io.k8s.api.networking.v1.IngressClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1IngressClassResponse201 = struct {
        body: root.io.k8s.api.networking.v1.IngressClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1IngressClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1IngressClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1IngressClassResponse = union(enum) {
        ok: ReplaceNetworkingV1IngressClassResponse200,
        created: ReplaceNetworkingV1IngressClassResponse201,
        unauthorized: ReplaceNetworkingV1IngressClassResponse401,
        default_response: ReplaceNetworkingV1IngressClassResponseDefault,
    };

    pub const replaceNetworkingV1IngressClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified IngressClass
    pub fn replaceNetworkingV1IngressClass(self: *Client, name: []const u8, body: root.io.k8s.api.networking.v1.IngressClass, options: replaceNetworkingV1IngressClassOptions) !ReplaceNetworkingV1IngressClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ingressclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IngressClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IngressClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteNetworkingV1IngressClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1IngressClassResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1IngressClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1IngressClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1IngressClassResponse = union(enum) {
        ok: DeleteNetworkingV1IngressClassResponse200,
        accepted: DeleteNetworkingV1IngressClassResponse202,
        unauthorized: DeleteNetworkingV1IngressClassResponse401,
        default_response: DeleteNetworkingV1IngressClassResponseDefault,
    };

    pub const deleteNetworkingV1IngressClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete an IngressClass
    pub fn deleteNetworkingV1IngressClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteNetworkingV1IngressClassOptions) !DeleteNetworkingV1IngressClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ingressclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchNetworkingV1IngressClassResponse200 = struct {
        body: root.io.k8s.api.networking.v1.IngressClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1IngressClassResponse201 = struct {
        body: root.io.k8s.api.networking.v1.IngressClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1IngressClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1IngressClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1IngressClassResponse = union(enum) {
        ok: PatchNetworkingV1IngressClassResponse200,
        created: PatchNetworkingV1IngressClassResponse201,
        unauthorized: PatchNetworkingV1IngressClassResponse401,
        default_response: PatchNetworkingV1IngressClassResponseDefault,
    };

    pub const patchNetworkingV1IngressClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified IngressClass
    pub fn patchNetworkingV1IngressClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchNetworkingV1IngressClassOptions) !PatchNetworkingV1IngressClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ingressclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IngressClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IngressClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListNetworkingV1IngressForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.networking.v1.IngressList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1IngressForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1IngressForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1IngressForAllNamespacesResponse = union(enum) {
        ok: ListNetworkingV1IngressForAllNamespacesResponse200,
        unauthorized: ListNetworkingV1IngressForAllNamespacesResponse401,
        default_response: ListNetworkingV1IngressForAllNamespacesResponseDefault,
    };

    pub const listNetworkingV1IngressForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Ingress
    pub fn listNetworkingV1IngressForAllNamespaces(self: *Client, options: listNetworkingV1IngressForAllNamespacesOptions) !ListNetworkingV1IngressForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ingresses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IngressList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistNetworkingV1IngressForAllNamespaces(self: *Client, options: listNetworkingV1IngressForAllNamespacesOptions) !WatchStream(root.io.k8s.api.networking.v1.Ingress) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/networking.k8s.io/v1/ingresses");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.networking.v1.Ingress){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListNetworkingV1IPAddressResponse200 = struct {
        body: root.io.k8s.api.networking.v1.IPAddressList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1IPAddressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1IPAddressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1IPAddressResponse = union(enum) {
        ok: ListNetworkingV1IPAddressResponse200,
        unauthorized: ListNetworkingV1IPAddressResponse401,
        default_response: ListNetworkingV1IPAddressResponseDefault,
    };

    pub const listNetworkingV1IPAddressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind IPAddress
    pub fn listNetworkingV1IPAddress(self: *Client, options: listNetworkingV1IPAddressOptions) !ListNetworkingV1IPAddressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ipaddresses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IPAddressList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistNetworkingV1IPAddress(self: *Client, options: listNetworkingV1IPAddressOptions) !WatchStream(root.io.k8s.api.networking.v1.IPAddress) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/networking.k8s.io/v1/ipaddresses");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.networking.v1.IPAddress){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateNetworkingV1IPAddressResponse200 = struct {
        body: root.io.k8s.api.networking.v1.IPAddress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1IPAddressResponse201 = struct {
        body: root.io.k8s.api.networking.v1.IPAddress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1IPAddressResponse202 = struct {
        body: root.io.k8s.api.networking.v1.IPAddress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1IPAddressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1IPAddressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1IPAddressResponse = union(enum) {
        ok: CreateNetworkingV1IPAddressResponse200,
        created: CreateNetworkingV1IPAddressResponse201,
        accepted: CreateNetworkingV1IPAddressResponse202,
        unauthorized: CreateNetworkingV1IPAddressResponse401,
        default_response: CreateNetworkingV1IPAddressResponseDefault,
    };

    pub const createNetworkingV1IPAddressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create an IPAddress
    pub fn createNetworkingV1IPAddress(self: *Client, body: root.io.k8s.api.networking.v1.IPAddress, options: createNetworkingV1IPAddressOptions) !CreateNetworkingV1IPAddressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ipaddresses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IPAddress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IPAddress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IPAddress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteNetworkingV1CollectionIPAddressResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionIPAddressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionIPAddressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionIPAddressResponse = union(enum) {
        ok: DeleteNetworkingV1CollectionIPAddressResponse200,
        unauthorized: DeleteNetworkingV1CollectionIPAddressResponse401,
        default_response: DeleteNetworkingV1CollectionIPAddressResponseDefault,
    };

    pub const deleteNetworkingV1CollectionIPAddressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of IPAddress
    pub fn deleteNetworkingV1CollectionIPAddress(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteNetworkingV1CollectionIPAddressOptions) !DeleteNetworkingV1CollectionIPAddressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ipaddresses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadNetworkingV1IPAddressResponse200 = struct {
        body: root.io.k8s.api.networking.v1.IPAddress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1IPAddressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1IPAddressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1IPAddressResponse = union(enum) {
        ok: ReadNetworkingV1IPAddressResponse200,
        unauthorized: ReadNetworkingV1IPAddressResponse401,
        default_response: ReadNetworkingV1IPAddressResponseDefault,
    };

    pub const readNetworkingV1IPAddressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified IPAddress
    pub fn readNetworkingV1IPAddress(self: *Client, name: []const u8, options: readNetworkingV1IPAddressOptions) !ReadNetworkingV1IPAddressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ipaddresses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IPAddress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceNetworkingV1IPAddressResponse200 = struct {
        body: root.io.k8s.api.networking.v1.IPAddress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1IPAddressResponse201 = struct {
        body: root.io.k8s.api.networking.v1.IPAddress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1IPAddressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1IPAddressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1IPAddressResponse = union(enum) {
        ok: ReplaceNetworkingV1IPAddressResponse200,
        created: ReplaceNetworkingV1IPAddressResponse201,
        unauthorized: ReplaceNetworkingV1IPAddressResponse401,
        default_response: ReplaceNetworkingV1IPAddressResponseDefault,
    };

    pub const replaceNetworkingV1IPAddressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified IPAddress
    pub fn replaceNetworkingV1IPAddress(self: *Client, name: []const u8, body: root.io.k8s.api.networking.v1.IPAddress, options: replaceNetworkingV1IPAddressOptions) !ReplaceNetworkingV1IPAddressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ipaddresses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IPAddress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IPAddress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteNetworkingV1IPAddressResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1IPAddressResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1IPAddressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1IPAddressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1IPAddressResponse = union(enum) {
        ok: DeleteNetworkingV1IPAddressResponse200,
        accepted: DeleteNetworkingV1IPAddressResponse202,
        unauthorized: DeleteNetworkingV1IPAddressResponse401,
        default_response: DeleteNetworkingV1IPAddressResponseDefault,
    };

    pub const deleteNetworkingV1IPAddressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete an IPAddress
    pub fn deleteNetworkingV1IPAddress(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteNetworkingV1IPAddressOptions) !DeleteNetworkingV1IPAddressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ipaddresses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchNetworkingV1IPAddressResponse200 = struct {
        body: root.io.k8s.api.networking.v1.IPAddress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1IPAddressResponse201 = struct {
        body: root.io.k8s.api.networking.v1.IPAddress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1IPAddressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1IPAddressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1IPAddressResponse = union(enum) {
        ok: PatchNetworkingV1IPAddressResponse200,
        created: PatchNetworkingV1IPAddressResponse201,
        unauthorized: PatchNetworkingV1IPAddressResponse401,
        default_response: PatchNetworkingV1IPAddressResponseDefault,
    };

    pub const patchNetworkingV1IPAddressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified IPAddress
    pub fn patchNetworkingV1IPAddress(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchNetworkingV1IPAddressOptions) !PatchNetworkingV1IPAddressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/ipaddresses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IPAddress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IPAddress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListNetworkingV1NamespacedIngressResponse200 = struct {
        body: root.io.k8s.api.networking.v1.IngressList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1NamespacedIngressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1NamespacedIngressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1NamespacedIngressResponse = union(enum) {
        ok: ListNetworkingV1NamespacedIngressResponse200,
        unauthorized: ListNetworkingV1NamespacedIngressResponse401,
        default_response: ListNetworkingV1NamespacedIngressResponseDefault,
    };

    pub const listNetworkingV1NamespacedIngressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Ingress
    pub fn listNetworkingV1NamespacedIngress(self: *Client, namespace: []const u8, options: listNetworkingV1NamespacedIngressOptions) !ListNetworkingV1NamespacedIngressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/ingresses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.IngressList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistNetworkingV1NamespacedIngress(self: *Client, namespace: []const u8, options: listNetworkingV1NamespacedIngressOptions) !WatchStream(root.io.k8s.api.networking.v1.Ingress) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/networking.k8s.io/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/ingresses");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.networking.v1.Ingress){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateNetworkingV1NamespacedIngressResponse200 = struct {
        body: root.io.k8s.api.networking.v1.Ingress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1NamespacedIngressResponse201 = struct {
        body: root.io.k8s.api.networking.v1.Ingress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1NamespacedIngressResponse202 = struct {
        body: root.io.k8s.api.networking.v1.Ingress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1NamespacedIngressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1NamespacedIngressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1NamespacedIngressResponse = union(enum) {
        ok: CreateNetworkingV1NamespacedIngressResponse200,
        created: CreateNetworkingV1NamespacedIngressResponse201,
        accepted: CreateNetworkingV1NamespacedIngressResponse202,
        unauthorized: CreateNetworkingV1NamespacedIngressResponse401,
        default_response: CreateNetworkingV1NamespacedIngressResponseDefault,
    };

    pub const createNetworkingV1NamespacedIngressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create an Ingress
    pub fn createNetworkingV1NamespacedIngress(self: *Client, namespace: []const u8, body: root.io.k8s.api.networking.v1.Ingress, options: createNetworkingV1NamespacedIngressOptions) !CreateNetworkingV1NamespacedIngressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/ingresses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.Ingress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.Ingress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.Ingress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteNetworkingV1CollectionNamespacedIngressResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionNamespacedIngressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionNamespacedIngressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionNamespacedIngressResponse = union(enum) {
        ok: DeleteNetworkingV1CollectionNamespacedIngressResponse200,
        unauthorized: DeleteNetworkingV1CollectionNamespacedIngressResponse401,
        default_response: DeleteNetworkingV1CollectionNamespacedIngressResponseDefault,
    };

    pub const deleteNetworkingV1CollectionNamespacedIngressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Ingress
    pub fn deleteNetworkingV1CollectionNamespacedIngress(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteNetworkingV1CollectionNamespacedIngressOptions) !DeleteNetworkingV1CollectionNamespacedIngressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/ingresses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadNetworkingV1NamespacedIngressResponse200 = struct {
        body: root.io.k8s.api.networking.v1.Ingress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1NamespacedIngressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1NamespacedIngressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1NamespacedIngressResponse = union(enum) {
        ok: ReadNetworkingV1NamespacedIngressResponse200,
        unauthorized: ReadNetworkingV1NamespacedIngressResponse401,
        default_response: ReadNetworkingV1NamespacedIngressResponseDefault,
    };

    pub const readNetworkingV1NamespacedIngressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Ingress
    pub fn readNetworkingV1NamespacedIngress(self: *Client, name: []const u8, namespace: []const u8, options: readNetworkingV1NamespacedIngressOptions) !ReadNetworkingV1NamespacedIngressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/ingresses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.Ingress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceNetworkingV1NamespacedIngressResponse200 = struct {
        body: root.io.k8s.api.networking.v1.Ingress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1NamespacedIngressResponse201 = struct {
        body: root.io.k8s.api.networking.v1.Ingress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1NamespacedIngressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1NamespacedIngressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1NamespacedIngressResponse = union(enum) {
        ok: ReplaceNetworkingV1NamespacedIngressResponse200,
        created: ReplaceNetworkingV1NamespacedIngressResponse201,
        unauthorized: ReplaceNetworkingV1NamespacedIngressResponse401,
        default_response: ReplaceNetworkingV1NamespacedIngressResponseDefault,
    };

    pub const replaceNetworkingV1NamespacedIngressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Ingress
    pub fn replaceNetworkingV1NamespacedIngress(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.networking.v1.Ingress, options: replaceNetworkingV1NamespacedIngressOptions) !ReplaceNetworkingV1NamespacedIngressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/ingresses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.Ingress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.Ingress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteNetworkingV1NamespacedIngressResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1NamespacedIngressResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1NamespacedIngressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1NamespacedIngressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1NamespacedIngressResponse = union(enum) {
        ok: DeleteNetworkingV1NamespacedIngressResponse200,
        accepted: DeleteNetworkingV1NamespacedIngressResponse202,
        unauthorized: DeleteNetworkingV1NamespacedIngressResponse401,
        default_response: DeleteNetworkingV1NamespacedIngressResponseDefault,
    };

    pub const deleteNetworkingV1NamespacedIngressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete an Ingress
    pub fn deleteNetworkingV1NamespacedIngress(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteNetworkingV1NamespacedIngressOptions) !DeleteNetworkingV1NamespacedIngressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/ingresses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchNetworkingV1NamespacedIngressResponse200 = struct {
        body: root.io.k8s.api.networking.v1.Ingress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1NamespacedIngressResponse201 = struct {
        body: root.io.k8s.api.networking.v1.Ingress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1NamespacedIngressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1NamespacedIngressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1NamespacedIngressResponse = union(enum) {
        ok: PatchNetworkingV1NamespacedIngressResponse200,
        created: PatchNetworkingV1NamespacedIngressResponse201,
        unauthorized: PatchNetworkingV1NamespacedIngressResponse401,
        default_response: PatchNetworkingV1NamespacedIngressResponseDefault,
    };

    pub const patchNetworkingV1NamespacedIngressOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Ingress
    pub fn patchNetworkingV1NamespacedIngress(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchNetworkingV1NamespacedIngressOptions) !PatchNetworkingV1NamespacedIngressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/ingresses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.Ingress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.Ingress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadNetworkingV1NamespacedIngressStatusResponse200 = struct {
        body: root.io.k8s.api.networking.v1.Ingress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1NamespacedIngressStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1NamespacedIngressStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1NamespacedIngressStatusResponse = union(enum) {
        ok: ReadNetworkingV1NamespacedIngressStatusResponse200,
        unauthorized: ReadNetworkingV1NamespacedIngressStatusResponse401,
        default_response: ReadNetworkingV1NamespacedIngressStatusResponseDefault,
    };

    pub const readNetworkingV1NamespacedIngressStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified Ingress
    pub fn readNetworkingV1NamespacedIngressStatus(self: *Client, name: []const u8, namespace: []const u8, options: readNetworkingV1NamespacedIngressStatusOptions) !ReadNetworkingV1NamespacedIngressStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/ingresses/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.Ingress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceNetworkingV1NamespacedIngressStatusResponse200 = struct {
        body: root.io.k8s.api.networking.v1.Ingress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1NamespacedIngressStatusResponse201 = struct {
        body: root.io.k8s.api.networking.v1.Ingress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1NamespacedIngressStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1NamespacedIngressStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1NamespacedIngressStatusResponse = union(enum) {
        ok: ReplaceNetworkingV1NamespacedIngressStatusResponse200,
        created: ReplaceNetworkingV1NamespacedIngressStatusResponse201,
        unauthorized: ReplaceNetworkingV1NamespacedIngressStatusResponse401,
        default_response: ReplaceNetworkingV1NamespacedIngressStatusResponseDefault,
    };

    pub const replaceNetworkingV1NamespacedIngressStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified Ingress
    pub fn replaceNetworkingV1NamespacedIngressStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.networking.v1.Ingress, options: replaceNetworkingV1NamespacedIngressStatusOptions) !ReplaceNetworkingV1NamespacedIngressStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/ingresses/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.Ingress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.Ingress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchNetworkingV1NamespacedIngressStatusResponse200 = struct {
        body: root.io.k8s.api.networking.v1.Ingress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1NamespacedIngressStatusResponse201 = struct {
        body: root.io.k8s.api.networking.v1.Ingress,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1NamespacedIngressStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1NamespacedIngressStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1NamespacedIngressStatusResponse = union(enum) {
        ok: PatchNetworkingV1NamespacedIngressStatusResponse200,
        created: PatchNetworkingV1NamespacedIngressStatusResponse201,
        unauthorized: PatchNetworkingV1NamespacedIngressStatusResponse401,
        default_response: PatchNetworkingV1NamespacedIngressStatusResponseDefault,
    };

    pub const patchNetworkingV1NamespacedIngressStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified Ingress
    pub fn patchNetworkingV1NamespacedIngressStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchNetworkingV1NamespacedIngressStatusOptions) !PatchNetworkingV1NamespacedIngressStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/ingresses/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.Ingress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.Ingress, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListNetworkingV1NamespacedNetworkPolicyResponse200 = struct {
        body: root.io.k8s.api.networking.v1.NetworkPolicyList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1NamespacedNetworkPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1NamespacedNetworkPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1NamespacedNetworkPolicyResponse = union(enum) {
        ok: ListNetworkingV1NamespacedNetworkPolicyResponse200,
        unauthorized: ListNetworkingV1NamespacedNetworkPolicyResponse401,
        default_response: ListNetworkingV1NamespacedNetworkPolicyResponseDefault,
    };

    pub const listNetworkingV1NamespacedNetworkPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind NetworkPolicy
    pub fn listNetworkingV1NamespacedNetworkPolicy(self: *Client, namespace: []const u8, options: listNetworkingV1NamespacedNetworkPolicyOptions) !ListNetworkingV1NamespacedNetworkPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/networkpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.NetworkPolicyList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistNetworkingV1NamespacedNetworkPolicy(self: *Client, namespace: []const u8, options: listNetworkingV1NamespacedNetworkPolicyOptions) !WatchStream(root.io.k8s.api.networking.v1.NetworkPolicy) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/networking.k8s.io/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/networkpolicies");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.networking.v1.NetworkPolicy){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateNetworkingV1NamespacedNetworkPolicyResponse200 = struct {
        body: root.io.k8s.api.networking.v1.NetworkPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1NamespacedNetworkPolicyResponse201 = struct {
        body: root.io.k8s.api.networking.v1.NetworkPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1NamespacedNetworkPolicyResponse202 = struct {
        body: root.io.k8s.api.networking.v1.NetworkPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1NamespacedNetworkPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1NamespacedNetworkPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1NamespacedNetworkPolicyResponse = union(enum) {
        ok: CreateNetworkingV1NamespacedNetworkPolicyResponse200,
        created: CreateNetworkingV1NamespacedNetworkPolicyResponse201,
        accepted: CreateNetworkingV1NamespacedNetworkPolicyResponse202,
        unauthorized: CreateNetworkingV1NamespacedNetworkPolicyResponse401,
        default_response: CreateNetworkingV1NamespacedNetworkPolicyResponseDefault,
    };

    pub const createNetworkingV1NamespacedNetworkPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a NetworkPolicy
    pub fn createNetworkingV1NamespacedNetworkPolicy(self: *Client, namespace: []const u8, body: root.io.k8s.api.networking.v1.NetworkPolicy, options: createNetworkingV1NamespacedNetworkPolicyOptions) !CreateNetworkingV1NamespacedNetworkPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/networkpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.NetworkPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.NetworkPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.NetworkPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteNetworkingV1CollectionNamespacedNetworkPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionNamespacedNetworkPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionNamespacedNetworkPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionNamespacedNetworkPolicyResponse = union(enum) {
        ok: DeleteNetworkingV1CollectionNamespacedNetworkPolicyResponse200,
        unauthorized: DeleteNetworkingV1CollectionNamespacedNetworkPolicyResponse401,
        default_response: DeleteNetworkingV1CollectionNamespacedNetworkPolicyResponseDefault,
    };

    pub const deleteNetworkingV1CollectionNamespacedNetworkPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of NetworkPolicy
    pub fn deleteNetworkingV1CollectionNamespacedNetworkPolicy(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteNetworkingV1CollectionNamespacedNetworkPolicyOptions) !DeleteNetworkingV1CollectionNamespacedNetworkPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/networkpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadNetworkingV1NamespacedNetworkPolicyResponse200 = struct {
        body: root.io.k8s.api.networking.v1.NetworkPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1NamespacedNetworkPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1NamespacedNetworkPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1NamespacedNetworkPolicyResponse = union(enum) {
        ok: ReadNetworkingV1NamespacedNetworkPolicyResponse200,
        unauthorized: ReadNetworkingV1NamespacedNetworkPolicyResponse401,
        default_response: ReadNetworkingV1NamespacedNetworkPolicyResponseDefault,
    };

    pub const readNetworkingV1NamespacedNetworkPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified NetworkPolicy
    pub fn readNetworkingV1NamespacedNetworkPolicy(self: *Client, name: []const u8, namespace: []const u8, options: readNetworkingV1NamespacedNetworkPolicyOptions) !ReadNetworkingV1NamespacedNetworkPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/networkpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.NetworkPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceNetworkingV1NamespacedNetworkPolicyResponse200 = struct {
        body: root.io.k8s.api.networking.v1.NetworkPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1NamespacedNetworkPolicyResponse201 = struct {
        body: root.io.k8s.api.networking.v1.NetworkPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1NamespacedNetworkPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1NamespacedNetworkPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1NamespacedNetworkPolicyResponse = union(enum) {
        ok: ReplaceNetworkingV1NamespacedNetworkPolicyResponse200,
        created: ReplaceNetworkingV1NamespacedNetworkPolicyResponse201,
        unauthorized: ReplaceNetworkingV1NamespacedNetworkPolicyResponse401,
        default_response: ReplaceNetworkingV1NamespacedNetworkPolicyResponseDefault,
    };

    pub const replaceNetworkingV1NamespacedNetworkPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified NetworkPolicy
    pub fn replaceNetworkingV1NamespacedNetworkPolicy(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.networking.v1.NetworkPolicy, options: replaceNetworkingV1NamespacedNetworkPolicyOptions) !ReplaceNetworkingV1NamespacedNetworkPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/networkpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.NetworkPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.NetworkPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteNetworkingV1NamespacedNetworkPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1NamespacedNetworkPolicyResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1NamespacedNetworkPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1NamespacedNetworkPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1NamespacedNetworkPolicyResponse = union(enum) {
        ok: DeleteNetworkingV1NamespacedNetworkPolicyResponse200,
        accepted: DeleteNetworkingV1NamespacedNetworkPolicyResponse202,
        unauthorized: DeleteNetworkingV1NamespacedNetworkPolicyResponse401,
        default_response: DeleteNetworkingV1NamespacedNetworkPolicyResponseDefault,
    };

    pub const deleteNetworkingV1NamespacedNetworkPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a NetworkPolicy
    pub fn deleteNetworkingV1NamespacedNetworkPolicy(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteNetworkingV1NamespacedNetworkPolicyOptions) !DeleteNetworkingV1NamespacedNetworkPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/networkpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchNetworkingV1NamespacedNetworkPolicyResponse200 = struct {
        body: root.io.k8s.api.networking.v1.NetworkPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1NamespacedNetworkPolicyResponse201 = struct {
        body: root.io.k8s.api.networking.v1.NetworkPolicy,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1NamespacedNetworkPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1NamespacedNetworkPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1NamespacedNetworkPolicyResponse = union(enum) {
        ok: PatchNetworkingV1NamespacedNetworkPolicyResponse200,
        created: PatchNetworkingV1NamespacedNetworkPolicyResponse201,
        unauthorized: PatchNetworkingV1NamespacedNetworkPolicyResponse401,
        default_response: PatchNetworkingV1NamespacedNetworkPolicyResponseDefault,
    };

    pub const patchNetworkingV1NamespacedNetworkPolicyOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified NetworkPolicy
    pub fn patchNetworkingV1NamespacedNetworkPolicy(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchNetworkingV1NamespacedNetworkPolicyOptions) !PatchNetworkingV1NamespacedNetworkPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/networkpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.NetworkPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.NetworkPolicy, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListNetworkingV1NetworkPolicyForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.networking.v1.NetworkPolicyList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1NetworkPolicyForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1NetworkPolicyForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1NetworkPolicyForAllNamespacesResponse = union(enum) {
        ok: ListNetworkingV1NetworkPolicyForAllNamespacesResponse200,
        unauthorized: ListNetworkingV1NetworkPolicyForAllNamespacesResponse401,
        default_response: ListNetworkingV1NetworkPolicyForAllNamespacesResponseDefault,
    };

    pub const listNetworkingV1NetworkPolicyForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind NetworkPolicy
    pub fn listNetworkingV1NetworkPolicyForAllNamespaces(self: *Client, options: listNetworkingV1NetworkPolicyForAllNamespacesOptions) !ListNetworkingV1NetworkPolicyForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/networkpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.NetworkPolicyList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistNetworkingV1NetworkPolicyForAllNamespaces(self: *Client, options: listNetworkingV1NetworkPolicyForAllNamespacesOptions) !WatchStream(root.io.k8s.api.networking.v1.NetworkPolicy) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/networking.k8s.io/v1/networkpolicies");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.networking.v1.NetworkPolicy){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListNetworkingV1ServiceCIDRResponse200 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDRList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1ServiceCIDRResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1ServiceCIDRResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNetworkingV1ServiceCIDRResponse = union(enum) {
        ok: ListNetworkingV1ServiceCIDRResponse200,
        unauthorized: ListNetworkingV1ServiceCIDRResponse401,
        default_response: ListNetworkingV1ServiceCIDRResponseDefault,
    };

    pub const listNetworkingV1ServiceCIDROptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ServiceCIDR
    pub fn listNetworkingV1ServiceCIDR(self: *Client, options: listNetworkingV1ServiceCIDROptions) !ListNetworkingV1ServiceCIDRResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/servicecidrs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDRList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistNetworkingV1ServiceCIDR(self: *Client, options: listNetworkingV1ServiceCIDROptions) !WatchStream(root.io.k8s.api.networking.v1.ServiceCIDR) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/networking.k8s.io/v1/servicecidrs");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.networking.v1.ServiceCIDR){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateNetworkingV1ServiceCIDRResponse200 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDR,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1ServiceCIDRResponse201 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDR,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1ServiceCIDRResponse202 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDR,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1ServiceCIDRResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1ServiceCIDRResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNetworkingV1ServiceCIDRResponse = union(enum) {
        ok: CreateNetworkingV1ServiceCIDRResponse200,
        created: CreateNetworkingV1ServiceCIDRResponse201,
        accepted: CreateNetworkingV1ServiceCIDRResponse202,
        unauthorized: CreateNetworkingV1ServiceCIDRResponse401,
        default_response: CreateNetworkingV1ServiceCIDRResponseDefault,
    };

    pub const createNetworkingV1ServiceCIDROptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ServiceCIDR
    pub fn createNetworkingV1ServiceCIDR(self: *Client, body: root.io.k8s.api.networking.v1.ServiceCIDR, options: createNetworkingV1ServiceCIDROptions) !CreateNetworkingV1ServiceCIDRResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/servicecidrs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDR, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDR, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDR, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteNetworkingV1CollectionServiceCIDRResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionServiceCIDRResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionServiceCIDRResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1CollectionServiceCIDRResponse = union(enum) {
        ok: DeleteNetworkingV1CollectionServiceCIDRResponse200,
        unauthorized: DeleteNetworkingV1CollectionServiceCIDRResponse401,
        default_response: DeleteNetworkingV1CollectionServiceCIDRResponseDefault,
    };

    pub const deleteNetworkingV1CollectionServiceCIDROptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ServiceCIDR
    pub fn deleteNetworkingV1CollectionServiceCIDR(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteNetworkingV1CollectionServiceCIDROptions) !DeleteNetworkingV1CollectionServiceCIDRResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/servicecidrs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadNetworkingV1ServiceCIDRResponse200 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDR,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1ServiceCIDRResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1ServiceCIDRResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1ServiceCIDRResponse = union(enum) {
        ok: ReadNetworkingV1ServiceCIDRResponse200,
        unauthorized: ReadNetworkingV1ServiceCIDRResponse401,
        default_response: ReadNetworkingV1ServiceCIDRResponseDefault,
    };

    pub const readNetworkingV1ServiceCIDROptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ServiceCIDR
    pub fn readNetworkingV1ServiceCIDR(self: *Client, name: []const u8, options: readNetworkingV1ServiceCIDROptions) !ReadNetworkingV1ServiceCIDRResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/servicecidrs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDR, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceNetworkingV1ServiceCIDRResponse200 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDR,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1ServiceCIDRResponse201 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDR,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1ServiceCIDRResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1ServiceCIDRResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1ServiceCIDRResponse = union(enum) {
        ok: ReplaceNetworkingV1ServiceCIDRResponse200,
        created: ReplaceNetworkingV1ServiceCIDRResponse201,
        unauthorized: ReplaceNetworkingV1ServiceCIDRResponse401,
        default_response: ReplaceNetworkingV1ServiceCIDRResponseDefault,
    };

    pub const replaceNetworkingV1ServiceCIDROptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ServiceCIDR
    pub fn replaceNetworkingV1ServiceCIDR(self: *Client, name: []const u8, body: root.io.k8s.api.networking.v1.ServiceCIDR, options: replaceNetworkingV1ServiceCIDROptions) !ReplaceNetworkingV1ServiceCIDRResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/servicecidrs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDR, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDR, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteNetworkingV1ServiceCIDRResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1ServiceCIDRResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1ServiceCIDRResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1ServiceCIDRResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNetworkingV1ServiceCIDRResponse = union(enum) {
        ok: DeleteNetworkingV1ServiceCIDRResponse200,
        accepted: DeleteNetworkingV1ServiceCIDRResponse202,
        unauthorized: DeleteNetworkingV1ServiceCIDRResponse401,
        default_response: DeleteNetworkingV1ServiceCIDRResponseDefault,
    };

    pub const deleteNetworkingV1ServiceCIDROptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ServiceCIDR
    pub fn deleteNetworkingV1ServiceCIDR(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteNetworkingV1ServiceCIDROptions) !DeleteNetworkingV1ServiceCIDRResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/servicecidrs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchNetworkingV1ServiceCIDRResponse200 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDR,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1ServiceCIDRResponse201 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDR,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1ServiceCIDRResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1ServiceCIDRResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1ServiceCIDRResponse = union(enum) {
        ok: PatchNetworkingV1ServiceCIDRResponse200,
        created: PatchNetworkingV1ServiceCIDRResponse201,
        unauthorized: PatchNetworkingV1ServiceCIDRResponse401,
        default_response: PatchNetworkingV1ServiceCIDRResponseDefault,
    };

    pub const patchNetworkingV1ServiceCIDROptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ServiceCIDR
    pub fn patchNetworkingV1ServiceCIDR(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchNetworkingV1ServiceCIDROptions) !PatchNetworkingV1ServiceCIDRResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/servicecidrs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDR, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDR, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadNetworkingV1ServiceCIDRStatusResponse200 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDR,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1ServiceCIDRStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1ServiceCIDRStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNetworkingV1ServiceCIDRStatusResponse = union(enum) {
        ok: ReadNetworkingV1ServiceCIDRStatusResponse200,
        unauthorized: ReadNetworkingV1ServiceCIDRStatusResponse401,
        default_response: ReadNetworkingV1ServiceCIDRStatusResponseDefault,
    };

    pub const readNetworkingV1ServiceCIDRStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified ServiceCIDR
    pub fn readNetworkingV1ServiceCIDRStatus(self: *Client, name: []const u8, options: readNetworkingV1ServiceCIDRStatusOptions) !ReadNetworkingV1ServiceCIDRStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/servicecidrs/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDR, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceNetworkingV1ServiceCIDRStatusResponse200 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDR,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1ServiceCIDRStatusResponse201 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDR,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1ServiceCIDRStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1ServiceCIDRStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNetworkingV1ServiceCIDRStatusResponse = union(enum) {
        ok: ReplaceNetworkingV1ServiceCIDRStatusResponse200,
        created: ReplaceNetworkingV1ServiceCIDRStatusResponse201,
        unauthorized: ReplaceNetworkingV1ServiceCIDRStatusResponse401,
        default_response: ReplaceNetworkingV1ServiceCIDRStatusResponseDefault,
    };

    pub const replaceNetworkingV1ServiceCIDRStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified ServiceCIDR
    pub fn replaceNetworkingV1ServiceCIDRStatus(self: *Client, name: []const u8, body: root.io.k8s.api.networking.v1.ServiceCIDR, options: replaceNetworkingV1ServiceCIDRStatusOptions) !ReplaceNetworkingV1ServiceCIDRStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/servicecidrs/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDR, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDR, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchNetworkingV1ServiceCIDRStatusResponse200 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDR,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1ServiceCIDRStatusResponse201 = struct {
        body: root.io.k8s.api.networking.v1.ServiceCIDR,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1ServiceCIDRStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1ServiceCIDRStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNetworkingV1ServiceCIDRStatusResponse = union(enum) {
        ok: PatchNetworkingV1ServiceCIDRStatusResponse200,
        created: PatchNetworkingV1ServiceCIDRStatusResponse201,
        unauthorized: PatchNetworkingV1ServiceCIDRStatusResponse401,
        default_response: PatchNetworkingV1ServiceCIDRStatusResponseDefault,
    };

    pub const patchNetworkingV1ServiceCIDRStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified ServiceCIDR
    pub fn patchNetworkingV1ServiceCIDRStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchNetworkingV1ServiceCIDRStatusOptions) !PatchNetworkingV1ServiceCIDRStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/servicecidrs/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDR, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.networking.v1.ServiceCIDR, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNetworkingV1IngressClassListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IngressClassListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IngressClassListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IngressClassListResponse = union(enum) {
        ok: WatchNetworkingV1IngressClassListResponse200,
        unauthorized: WatchNetworkingV1IngressClassListResponse401,
        default_response: WatchNetworkingV1IngressClassListResponseDefault,
    };

    pub const watchNetworkingV1IngressClassListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of IngressClass. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchNetworkingV1IngressClassList(self: *Client, options: watchNetworkingV1IngressClassListOptions) !WatchNetworkingV1IngressClassListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/watch/ingressclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNetworkingV1IngressClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IngressClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IngressClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IngressClassResponse = union(enum) {
        ok: WatchNetworkingV1IngressClassResponse200,
        unauthorized: WatchNetworkingV1IngressClassResponse401,
        default_response: WatchNetworkingV1IngressClassResponseDefault,
    };

    pub const watchNetworkingV1IngressClassOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind IngressClass. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchNetworkingV1IngressClass(self: *Client, name: []const u8, options: watchNetworkingV1IngressClassOptions) !WatchNetworkingV1IngressClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/watch/ingressclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNetworkingV1IngressListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IngressListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IngressListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IngressListForAllNamespacesResponse = union(enum) {
        ok: WatchNetworkingV1IngressListForAllNamespacesResponse200,
        unauthorized: WatchNetworkingV1IngressListForAllNamespacesResponse401,
        default_response: WatchNetworkingV1IngressListForAllNamespacesResponseDefault,
    };

    pub const watchNetworkingV1IngressListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Ingress. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchNetworkingV1IngressListForAllNamespaces(self: *Client, options: watchNetworkingV1IngressListForAllNamespacesOptions) !WatchNetworkingV1IngressListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/watch/ingresses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNetworkingV1IPAddressListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IPAddressListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IPAddressListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IPAddressListResponse = union(enum) {
        ok: WatchNetworkingV1IPAddressListResponse200,
        unauthorized: WatchNetworkingV1IPAddressListResponse401,
        default_response: WatchNetworkingV1IPAddressListResponseDefault,
    };

    pub const watchNetworkingV1IPAddressListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of IPAddress. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchNetworkingV1IPAddressList(self: *Client, options: watchNetworkingV1IPAddressListOptions) !WatchNetworkingV1IPAddressListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/watch/ipaddresses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNetworkingV1IPAddressResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IPAddressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IPAddressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1IPAddressResponse = union(enum) {
        ok: WatchNetworkingV1IPAddressResponse200,
        unauthorized: WatchNetworkingV1IPAddressResponse401,
        default_response: WatchNetworkingV1IPAddressResponseDefault,
    };

    pub const watchNetworkingV1IPAddressOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind IPAddress. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchNetworkingV1IPAddress(self: *Client, name: []const u8, options: watchNetworkingV1IPAddressOptions) !WatchNetworkingV1IPAddressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/watch/ipaddresses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNetworkingV1NamespacedIngressListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NamespacedIngressListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NamespacedIngressListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NamespacedIngressListResponse = union(enum) {
        ok: WatchNetworkingV1NamespacedIngressListResponse200,
        unauthorized: WatchNetworkingV1NamespacedIngressListResponse401,
        default_response: WatchNetworkingV1NamespacedIngressListResponseDefault,
    };

    pub const watchNetworkingV1NamespacedIngressListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Ingress. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchNetworkingV1NamespacedIngressList(self: *Client, namespace: []const u8, options: watchNetworkingV1NamespacedIngressListOptions) !WatchNetworkingV1NamespacedIngressListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/ingresses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNetworkingV1NamespacedIngressResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NamespacedIngressResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NamespacedIngressResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NamespacedIngressResponse = union(enum) {
        ok: WatchNetworkingV1NamespacedIngressResponse200,
        unauthorized: WatchNetworkingV1NamespacedIngressResponse401,
        default_response: WatchNetworkingV1NamespacedIngressResponseDefault,
    };

    pub const watchNetworkingV1NamespacedIngressOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Ingress. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchNetworkingV1NamespacedIngress(self: *Client, name: []const u8, namespace: []const u8, options: watchNetworkingV1NamespacedIngressOptions) !WatchNetworkingV1NamespacedIngressResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/ingresses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNetworkingV1NamespacedNetworkPolicyListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NamespacedNetworkPolicyListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NamespacedNetworkPolicyListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NamespacedNetworkPolicyListResponse = union(enum) {
        ok: WatchNetworkingV1NamespacedNetworkPolicyListResponse200,
        unauthorized: WatchNetworkingV1NamespacedNetworkPolicyListResponse401,
        default_response: WatchNetworkingV1NamespacedNetworkPolicyListResponseDefault,
    };

    pub const watchNetworkingV1NamespacedNetworkPolicyListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of NetworkPolicy. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchNetworkingV1NamespacedNetworkPolicyList(self: *Client, namespace: []const u8, options: watchNetworkingV1NamespacedNetworkPolicyListOptions) !WatchNetworkingV1NamespacedNetworkPolicyListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/networkpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNetworkingV1NamespacedNetworkPolicyResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NamespacedNetworkPolicyResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NamespacedNetworkPolicyResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NamespacedNetworkPolicyResponse = union(enum) {
        ok: WatchNetworkingV1NamespacedNetworkPolicyResponse200,
        unauthorized: WatchNetworkingV1NamespacedNetworkPolicyResponse401,
        default_response: WatchNetworkingV1NamespacedNetworkPolicyResponseDefault,
    };

    pub const watchNetworkingV1NamespacedNetworkPolicyOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind NetworkPolicy. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchNetworkingV1NamespacedNetworkPolicy(self: *Client, name: []const u8, namespace: []const u8, options: watchNetworkingV1NamespacedNetworkPolicyOptions) !WatchNetworkingV1NamespacedNetworkPolicyResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/networkpolicies/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNetworkingV1NetworkPolicyListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NetworkPolicyListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NetworkPolicyListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1NetworkPolicyListForAllNamespacesResponse = union(enum) {
        ok: WatchNetworkingV1NetworkPolicyListForAllNamespacesResponse200,
        unauthorized: WatchNetworkingV1NetworkPolicyListForAllNamespacesResponse401,
        default_response: WatchNetworkingV1NetworkPolicyListForAllNamespacesResponseDefault,
    };

    pub const watchNetworkingV1NetworkPolicyListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of NetworkPolicy. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchNetworkingV1NetworkPolicyListForAllNamespaces(self: *Client, options: watchNetworkingV1NetworkPolicyListForAllNamespacesOptions) !WatchNetworkingV1NetworkPolicyListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/watch/networkpolicies", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNetworkingV1ServiceCIDRListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1ServiceCIDRListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1ServiceCIDRListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1ServiceCIDRListResponse = union(enum) {
        ok: WatchNetworkingV1ServiceCIDRListResponse200,
        unauthorized: WatchNetworkingV1ServiceCIDRListResponse401,
        default_response: WatchNetworkingV1ServiceCIDRListResponseDefault,
    };

    pub const watchNetworkingV1ServiceCIDRListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ServiceCIDR. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchNetworkingV1ServiceCIDRList(self: *Client, options: watchNetworkingV1ServiceCIDRListOptions) !WatchNetworkingV1ServiceCIDRListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/watch/servicecidrs", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNetworkingV1ServiceCIDRResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1ServiceCIDRResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1ServiceCIDRResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNetworkingV1ServiceCIDRResponse = union(enum) {
        ok: WatchNetworkingV1ServiceCIDRResponse200,
        unauthorized: WatchNetworkingV1ServiceCIDRResponse401,
        default_response: WatchNetworkingV1ServiceCIDRResponseDefault,
    };

    pub const watchNetworkingV1ServiceCIDROptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ServiceCIDR. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchNetworkingV1ServiceCIDR(self: *Client, name: []const u8, options: watchNetworkingV1ServiceCIDROptions) !WatchNetworkingV1ServiceCIDRResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/networking.k8s.io/v1/watch/servicecidrs/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetNodeAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetNodeAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetNodeAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetNodeAPIGroupResponse = union(enum) {
        ok: GetNodeAPIGroupResponse200,
        unauthorized: GetNodeAPIGroupResponse401,
        default_response: GetNodeAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getNodeAPIGroup(self: *Client) !GetNodeAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/node.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetNodeV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetNodeV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetNodeV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetNodeV1APIResourcesResponse = union(enum) {
        ok: GetNodeV1APIResourcesResponse200,
        unauthorized: GetNodeV1APIResourcesResponse401,
        default_response: GetNodeV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getNodeV1APIResources(self: *Client) !GetNodeV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/node.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListNodeV1RuntimeClassResponse200 = struct {
        body: root.io.k8s.api.node.v1.RuntimeClassList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNodeV1RuntimeClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNodeV1RuntimeClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListNodeV1RuntimeClassResponse = union(enum) {
        ok: ListNodeV1RuntimeClassResponse200,
        unauthorized: ListNodeV1RuntimeClassResponse401,
        default_response: ListNodeV1RuntimeClassResponseDefault,
    };

    pub const listNodeV1RuntimeClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind RuntimeClass
    pub fn listNodeV1RuntimeClass(self: *Client, options: listNodeV1RuntimeClassOptions) !ListNodeV1RuntimeClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/node.k8s.io/v1/runtimeclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.node.v1.RuntimeClassList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistNodeV1RuntimeClass(self: *Client, options: listNodeV1RuntimeClassOptions) !WatchStream(root.io.k8s.api.node.v1.RuntimeClass) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/node.k8s.io/v1/runtimeclasses");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.node.v1.RuntimeClass){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateNodeV1RuntimeClassResponse200 = struct {
        body: root.io.k8s.api.node.v1.RuntimeClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNodeV1RuntimeClassResponse201 = struct {
        body: root.io.k8s.api.node.v1.RuntimeClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNodeV1RuntimeClassResponse202 = struct {
        body: root.io.k8s.api.node.v1.RuntimeClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNodeV1RuntimeClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNodeV1RuntimeClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateNodeV1RuntimeClassResponse = union(enum) {
        ok: CreateNodeV1RuntimeClassResponse200,
        created: CreateNodeV1RuntimeClassResponse201,
        accepted: CreateNodeV1RuntimeClassResponse202,
        unauthorized: CreateNodeV1RuntimeClassResponse401,
        default_response: CreateNodeV1RuntimeClassResponseDefault,
    };

    pub const createNodeV1RuntimeClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a RuntimeClass
    pub fn createNodeV1RuntimeClass(self: *Client, body: root.io.k8s.api.node.v1.RuntimeClass, options: createNodeV1RuntimeClassOptions) !CreateNodeV1RuntimeClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/node.k8s.io/v1/runtimeclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.node.v1.RuntimeClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.node.v1.RuntimeClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.node.v1.RuntimeClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteNodeV1CollectionRuntimeClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNodeV1CollectionRuntimeClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNodeV1CollectionRuntimeClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNodeV1CollectionRuntimeClassResponse = union(enum) {
        ok: DeleteNodeV1CollectionRuntimeClassResponse200,
        unauthorized: DeleteNodeV1CollectionRuntimeClassResponse401,
        default_response: DeleteNodeV1CollectionRuntimeClassResponseDefault,
    };

    pub const deleteNodeV1CollectionRuntimeClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of RuntimeClass
    pub fn deleteNodeV1CollectionRuntimeClass(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteNodeV1CollectionRuntimeClassOptions) !DeleteNodeV1CollectionRuntimeClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/node.k8s.io/v1/runtimeclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadNodeV1RuntimeClassResponse200 = struct {
        body: root.io.k8s.api.node.v1.RuntimeClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNodeV1RuntimeClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNodeV1RuntimeClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadNodeV1RuntimeClassResponse = union(enum) {
        ok: ReadNodeV1RuntimeClassResponse200,
        unauthorized: ReadNodeV1RuntimeClassResponse401,
        default_response: ReadNodeV1RuntimeClassResponseDefault,
    };

    pub const readNodeV1RuntimeClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified RuntimeClass
    pub fn readNodeV1RuntimeClass(self: *Client, name: []const u8, options: readNodeV1RuntimeClassOptions) !ReadNodeV1RuntimeClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/node.k8s.io/v1/runtimeclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.node.v1.RuntimeClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceNodeV1RuntimeClassResponse200 = struct {
        body: root.io.k8s.api.node.v1.RuntimeClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNodeV1RuntimeClassResponse201 = struct {
        body: root.io.k8s.api.node.v1.RuntimeClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNodeV1RuntimeClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNodeV1RuntimeClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceNodeV1RuntimeClassResponse = union(enum) {
        ok: ReplaceNodeV1RuntimeClassResponse200,
        created: ReplaceNodeV1RuntimeClassResponse201,
        unauthorized: ReplaceNodeV1RuntimeClassResponse401,
        default_response: ReplaceNodeV1RuntimeClassResponseDefault,
    };

    pub const replaceNodeV1RuntimeClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified RuntimeClass
    pub fn replaceNodeV1RuntimeClass(self: *Client, name: []const u8, body: root.io.k8s.api.node.v1.RuntimeClass, options: replaceNodeV1RuntimeClassOptions) !ReplaceNodeV1RuntimeClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/node.k8s.io/v1/runtimeclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.node.v1.RuntimeClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.node.v1.RuntimeClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteNodeV1RuntimeClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNodeV1RuntimeClassResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNodeV1RuntimeClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNodeV1RuntimeClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteNodeV1RuntimeClassResponse = union(enum) {
        ok: DeleteNodeV1RuntimeClassResponse200,
        accepted: DeleteNodeV1RuntimeClassResponse202,
        unauthorized: DeleteNodeV1RuntimeClassResponse401,
        default_response: DeleteNodeV1RuntimeClassResponseDefault,
    };

    pub const deleteNodeV1RuntimeClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a RuntimeClass
    pub fn deleteNodeV1RuntimeClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteNodeV1RuntimeClassOptions) !DeleteNodeV1RuntimeClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/node.k8s.io/v1/runtimeclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchNodeV1RuntimeClassResponse200 = struct {
        body: root.io.k8s.api.node.v1.RuntimeClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNodeV1RuntimeClassResponse201 = struct {
        body: root.io.k8s.api.node.v1.RuntimeClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNodeV1RuntimeClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNodeV1RuntimeClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchNodeV1RuntimeClassResponse = union(enum) {
        ok: PatchNodeV1RuntimeClassResponse200,
        created: PatchNodeV1RuntimeClassResponse201,
        unauthorized: PatchNodeV1RuntimeClassResponse401,
        default_response: PatchNodeV1RuntimeClassResponseDefault,
    };

    pub const patchNodeV1RuntimeClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified RuntimeClass
    pub fn patchNodeV1RuntimeClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchNodeV1RuntimeClassOptions) !PatchNodeV1RuntimeClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/node.k8s.io/v1/runtimeclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.node.v1.RuntimeClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.node.v1.RuntimeClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNodeV1RuntimeClassListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNodeV1RuntimeClassListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNodeV1RuntimeClassListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNodeV1RuntimeClassListResponse = union(enum) {
        ok: WatchNodeV1RuntimeClassListResponse200,
        unauthorized: WatchNodeV1RuntimeClassListResponse401,
        default_response: WatchNodeV1RuntimeClassListResponseDefault,
    };

    pub const watchNodeV1RuntimeClassListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of RuntimeClass. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchNodeV1RuntimeClassList(self: *Client, options: watchNodeV1RuntimeClassListOptions) !WatchNodeV1RuntimeClassListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/node.k8s.io/v1/watch/runtimeclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchNodeV1RuntimeClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNodeV1RuntimeClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNodeV1RuntimeClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchNodeV1RuntimeClassResponse = union(enum) {
        ok: WatchNodeV1RuntimeClassResponse200,
        unauthorized: WatchNodeV1RuntimeClassResponse401,
        default_response: WatchNodeV1RuntimeClassResponseDefault,
    };

    pub const watchNodeV1RuntimeClassOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind RuntimeClass. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchNodeV1RuntimeClass(self: *Client, name: []const u8, options: watchNodeV1RuntimeClassOptions) !WatchNodeV1RuntimeClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/node.k8s.io/v1/watch/runtimeclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetPolicyAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetPolicyAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetPolicyAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetPolicyAPIGroupResponse = union(enum) {
        ok: GetPolicyAPIGroupResponse200,
        unauthorized: GetPolicyAPIGroupResponse401,
        default_response: GetPolicyAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getPolicyAPIGroup(self: *Client) !GetPolicyAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetPolicyV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetPolicyV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetPolicyV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetPolicyV1APIResourcesResponse = union(enum) {
        ok: GetPolicyV1APIResourcesResponse200,
        unauthorized: GetPolicyV1APIResourcesResponse401,
        default_response: GetPolicyV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getPolicyV1APIResources(self: *Client) !GetPolicyV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListPolicyV1NamespacedPodDisruptionBudgetResponse200 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudgetList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListPolicyV1NamespacedPodDisruptionBudgetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListPolicyV1NamespacedPodDisruptionBudgetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListPolicyV1NamespacedPodDisruptionBudgetResponse = union(enum) {
        ok: ListPolicyV1NamespacedPodDisruptionBudgetResponse200,
        unauthorized: ListPolicyV1NamespacedPodDisruptionBudgetResponse401,
        default_response: ListPolicyV1NamespacedPodDisruptionBudgetResponseDefault,
    };

    pub const listPolicyV1NamespacedPodDisruptionBudgetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PodDisruptionBudget
    pub fn listPolicyV1NamespacedPodDisruptionBudget(self: *Client, namespace: []const u8, options: listPolicyV1NamespacedPodDisruptionBudgetOptions) !ListPolicyV1NamespacedPodDisruptionBudgetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/poddisruptionbudgets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudgetList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistPolicyV1NamespacedPodDisruptionBudget(self: *Client, namespace: []const u8, options: listPolicyV1NamespacedPodDisruptionBudgetOptions) !WatchStream(root.io.k8s.api.policy.v1.PodDisruptionBudget) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/policy/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/poddisruptionbudgets");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.policy.v1.PodDisruptionBudget){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreatePolicyV1NamespacedPodDisruptionBudgetResponse200 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudget,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreatePolicyV1NamespacedPodDisruptionBudgetResponse201 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudget,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreatePolicyV1NamespacedPodDisruptionBudgetResponse202 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudget,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreatePolicyV1NamespacedPodDisruptionBudgetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreatePolicyV1NamespacedPodDisruptionBudgetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreatePolicyV1NamespacedPodDisruptionBudgetResponse = union(enum) {
        ok: CreatePolicyV1NamespacedPodDisruptionBudgetResponse200,
        created: CreatePolicyV1NamespacedPodDisruptionBudgetResponse201,
        accepted: CreatePolicyV1NamespacedPodDisruptionBudgetResponse202,
        unauthorized: CreatePolicyV1NamespacedPodDisruptionBudgetResponse401,
        default_response: CreatePolicyV1NamespacedPodDisruptionBudgetResponseDefault,
    };

    pub const createPolicyV1NamespacedPodDisruptionBudgetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a PodDisruptionBudget
    pub fn createPolicyV1NamespacedPodDisruptionBudget(self: *Client, namespace: []const u8, body: root.io.k8s.api.policy.v1.PodDisruptionBudget, options: createPolicyV1NamespacedPodDisruptionBudgetOptions) !CreatePolicyV1NamespacedPodDisruptionBudgetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/poddisruptionbudgets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudget, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudget, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudget, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeletePolicyV1CollectionNamespacedPodDisruptionBudgetResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeletePolicyV1CollectionNamespacedPodDisruptionBudgetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeletePolicyV1CollectionNamespacedPodDisruptionBudgetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeletePolicyV1CollectionNamespacedPodDisruptionBudgetResponse = union(enum) {
        ok: DeletePolicyV1CollectionNamespacedPodDisruptionBudgetResponse200,
        unauthorized: DeletePolicyV1CollectionNamespacedPodDisruptionBudgetResponse401,
        default_response: DeletePolicyV1CollectionNamespacedPodDisruptionBudgetResponseDefault,
    };

    pub const deletePolicyV1CollectionNamespacedPodDisruptionBudgetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of PodDisruptionBudget
    pub fn deletePolicyV1CollectionNamespacedPodDisruptionBudget(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deletePolicyV1CollectionNamespacedPodDisruptionBudgetOptions) !DeletePolicyV1CollectionNamespacedPodDisruptionBudgetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/poddisruptionbudgets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadPolicyV1NamespacedPodDisruptionBudgetResponse200 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudget,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadPolicyV1NamespacedPodDisruptionBudgetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadPolicyV1NamespacedPodDisruptionBudgetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadPolicyV1NamespacedPodDisruptionBudgetResponse = union(enum) {
        ok: ReadPolicyV1NamespacedPodDisruptionBudgetResponse200,
        unauthorized: ReadPolicyV1NamespacedPodDisruptionBudgetResponse401,
        default_response: ReadPolicyV1NamespacedPodDisruptionBudgetResponseDefault,
    };

    pub const readPolicyV1NamespacedPodDisruptionBudgetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified PodDisruptionBudget
    pub fn readPolicyV1NamespacedPodDisruptionBudget(self: *Client, name: []const u8, namespace: []const u8, options: readPolicyV1NamespacedPodDisruptionBudgetOptions) !ReadPolicyV1NamespacedPodDisruptionBudgetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/poddisruptionbudgets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudget, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplacePolicyV1NamespacedPodDisruptionBudgetResponse200 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudget,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplacePolicyV1NamespacedPodDisruptionBudgetResponse201 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudget,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplacePolicyV1NamespacedPodDisruptionBudgetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplacePolicyV1NamespacedPodDisruptionBudgetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplacePolicyV1NamespacedPodDisruptionBudgetResponse = union(enum) {
        ok: ReplacePolicyV1NamespacedPodDisruptionBudgetResponse200,
        created: ReplacePolicyV1NamespacedPodDisruptionBudgetResponse201,
        unauthorized: ReplacePolicyV1NamespacedPodDisruptionBudgetResponse401,
        default_response: ReplacePolicyV1NamespacedPodDisruptionBudgetResponseDefault,
    };

    pub const replacePolicyV1NamespacedPodDisruptionBudgetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified PodDisruptionBudget
    pub fn replacePolicyV1NamespacedPodDisruptionBudget(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.policy.v1.PodDisruptionBudget, options: replacePolicyV1NamespacedPodDisruptionBudgetOptions) !ReplacePolicyV1NamespacedPodDisruptionBudgetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/poddisruptionbudgets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudget, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudget, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeletePolicyV1NamespacedPodDisruptionBudgetResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeletePolicyV1NamespacedPodDisruptionBudgetResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeletePolicyV1NamespacedPodDisruptionBudgetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeletePolicyV1NamespacedPodDisruptionBudgetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeletePolicyV1NamespacedPodDisruptionBudgetResponse = union(enum) {
        ok: DeletePolicyV1NamespacedPodDisruptionBudgetResponse200,
        accepted: DeletePolicyV1NamespacedPodDisruptionBudgetResponse202,
        unauthorized: DeletePolicyV1NamespacedPodDisruptionBudgetResponse401,
        default_response: DeletePolicyV1NamespacedPodDisruptionBudgetResponseDefault,
    };

    pub const deletePolicyV1NamespacedPodDisruptionBudgetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a PodDisruptionBudget
    pub fn deletePolicyV1NamespacedPodDisruptionBudget(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deletePolicyV1NamespacedPodDisruptionBudgetOptions) !DeletePolicyV1NamespacedPodDisruptionBudgetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/poddisruptionbudgets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchPolicyV1NamespacedPodDisruptionBudgetResponse200 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudget,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchPolicyV1NamespacedPodDisruptionBudgetResponse201 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudget,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchPolicyV1NamespacedPodDisruptionBudgetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchPolicyV1NamespacedPodDisruptionBudgetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchPolicyV1NamespacedPodDisruptionBudgetResponse = union(enum) {
        ok: PatchPolicyV1NamespacedPodDisruptionBudgetResponse200,
        created: PatchPolicyV1NamespacedPodDisruptionBudgetResponse201,
        unauthorized: PatchPolicyV1NamespacedPodDisruptionBudgetResponse401,
        default_response: PatchPolicyV1NamespacedPodDisruptionBudgetResponseDefault,
    };

    pub const patchPolicyV1NamespacedPodDisruptionBudgetOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified PodDisruptionBudget
    pub fn patchPolicyV1NamespacedPodDisruptionBudget(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchPolicyV1NamespacedPodDisruptionBudgetOptions) !PatchPolicyV1NamespacedPodDisruptionBudgetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/poddisruptionbudgets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudget, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudget, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadPolicyV1NamespacedPodDisruptionBudgetStatusResponse200 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudget,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadPolicyV1NamespacedPodDisruptionBudgetStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadPolicyV1NamespacedPodDisruptionBudgetStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadPolicyV1NamespacedPodDisruptionBudgetStatusResponse = union(enum) {
        ok: ReadPolicyV1NamespacedPodDisruptionBudgetStatusResponse200,
        unauthorized: ReadPolicyV1NamespacedPodDisruptionBudgetStatusResponse401,
        default_response: ReadPolicyV1NamespacedPodDisruptionBudgetStatusResponseDefault,
    };

    pub const readPolicyV1NamespacedPodDisruptionBudgetStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified PodDisruptionBudget
    pub fn readPolicyV1NamespacedPodDisruptionBudgetStatus(self: *Client, name: []const u8, namespace: []const u8, options: readPolicyV1NamespacedPodDisruptionBudgetStatusOptions) !ReadPolicyV1NamespacedPodDisruptionBudgetStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/poddisruptionbudgets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudget, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplacePolicyV1NamespacedPodDisruptionBudgetStatusResponse200 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudget,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplacePolicyV1NamespacedPodDisruptionBudgetStatusResponse201 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudget,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplacePolicyV1NamespacedPodDisruptionBudgetStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplacePolicyV1NamespacedPodDisruptionBudgetStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplacePolicyV1NamespacedPodDisruptionBudgetStatusResponse = union(enum) {
        ok: ReplacePolicyV1NamespacedPodDisruptionBudgetStatusResponse200,
        created: ReplacePolicyV1NamespacedPodDisruptionBudgetStatusResponse201,
        unauthorized: ReplacePolicyV1NamespacedPodDisruptionBudgetStatusResponse401,
        default_response: ReplacePolicyV1NamespacedPodDisruptionBudgetStatusResponseDefault,
    };

    pub const replacePolicyV1NamespacedPodDisruptionBudgetStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified PodDisruptionBudget
    pub fn replacePolicyV1NamespacedPodDisruptionBudgetStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.policy.v1.PodDisruptionBudget, options: replacePolicyV1NamespacedPodDisruptionBudgetStatusOptions) !ReplacePolicyV1NamespacedPodDisruptionBudgetStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/poddisruptionbudgets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudget, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudget, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchPolicyV1NamespacedPodDisruptionBudgetStatusResponse200 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudget,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchPolicyV1NamespacedPodDisruptionBudgetStatusResponse201 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudget,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchPolicyV1NamespacedPodDisruptionBudgetStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchPolicyV1NamespacedPodDisruptionBudgetStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchPolicyV1NamespacedPodDisruptionBudgetStatusResponse = union(enum) {
        ok: PatchPolicyV1NamespacedPodDisruptionBudgetStatusResponse200,
        created: PatchPolicyV1NamespacedPodDisruptionBudgetStatusResponse201,
        unauthorized: PatchPolicyV1NamespacedPodDisruptionBudgetStatusResponse401,
        default_response: PatchPolicyV1NamespacedPodDisruptionBudgetStatusResponseDefault,
    };

    pub const patchPolicyV1NamespacedPodDisruptionBudgetStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified PodDisruptionBudget
    pub fn patchPolicyV1NamespacedPodDisruptionBudgetStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchPolicyV1NamespacedPodDisruptionBudgetStatusOptions) !PatchPolicyV1NamespacedPodDisruptionBudgetStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/poddisruptionbudgets/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudget, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudget, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListPolicyV1PodDisruptionBudgetForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.policy.v1.PodDisruptionBudgetList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListPolicyV1PodDisruptionBudgetForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListPolicyV1PodDisruptionBudgetForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListPolicyV1PodDisruptionBudgetForAllNamespacesResponse = union(enum) {
        ok: ListPolicyV1PodDisruptionBudgetForAllNamespacesResponse200,
        unauthorized: ListPolicyV1PodDisruptionBudgetForAllNamespacesResponse401,
        default_response: ListPolicyV1PodDisruptionBudgetForAllNamespacesResponseDefault,
    };

    pub const listPolicyV1PodDisruptionBudgetForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PodDisruptionBudget
    pub fn listPolicyV1PodDisruptionBudgetForAllNamespaces(self: *Client, options: listPolicyV1PodDisruptionBudgetForAllNamespacesOptions) !ListPolicyV1PodDisruptionBudgetForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/poddisruptionbudgets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.policy.v1.PodDisruptionBudgetList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistPolicyV1PodDisruptionBudgetForAllNamespaces(self: *Client, options: listPolicyV1PodDisruptionBudgetForAllNamespacesOptions) !WatchStream(root.io.k8s.api.policy.v1.PodDisruptionBudget) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/policy/v1/poddisruptionbudgets");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.policy.v1.PodDisruptionBudget){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const WatchPolicyV1NamespacedPodDisruptionBudgetListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchPolicyV1NamespacedPodDisruptionBudgetListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchPolicyV1NamespacedPodDisruptionBudgetListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchPolicyV1NamespacedPodDisruptionBudgetListResponse = union(enum) {
        ok: WatchPolicyV1NamespacedPodDisruptionBudgetListResponse200,
        unauthorized: WatchPolicyV1NamespacedPodDisruptionBudgetListResponse401,
        default_response: WatchPolicyV1NamespacedPodDisruptionBudgetListResponseDefault,
    };

    pub const watchPolicyV1NamespacedPodDisruptionBudgetListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PodDisruptionBudget. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchPolicyV1NamespacedPodDisruptionBudgetList(self: *Client, namespace: []const u8, options: watchPolicyV1NamespacedPodDisruptionBudgetListOptions) !WatchPolicyV1NamespacedPodDisruptionBudgetListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/poddisruptionbudgets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchPolicyV1NamespacedPodDisruptionBudgetResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchPolicyV1NamespacedPodDisruptionBudgetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchPolicyV1NamespacedPodDisruptionBudgetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchPolicyV1NamespacedPodDisruptionBudgetResponse = union(enum) {
        ok: WatchPolicyV1NamespacedPodDisruptionBudgetResponse200,
        unauthorized: WatchPolicyV1NamespacedPodDisruptionBudgetResponse401,
        default_response: WatchPolicyV1NamespacedPodDisruptionBudgetResponseDefault,
    };

    pub const watchPolicyV1NamespacedPodDisruptionBudgetOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind PodDisruptionBudget. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchPolicyV1NamespacedPodDisruptionBudget(self: *Client, name: []const u8, namespace: []const u8, options: watchPolicyV1NamespacedPodDisruptionBudgetOptions) !WatchPolicyV1NamespacedPodDisruptionBudgetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/poddisruptionbudgets/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchPolicyV1PodDisruptionBudgetListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchPolicyV1PodDisruptionBudgetListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchPolicyV1PodDisruptionBudgetListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchPolicyV1PodDisruptionBudgetListForAllNamespacesResponse = union(enum) {
        ok: WatchPolicyV1PodDisruptionBudgetListForAllNamespacesResponse200,
        unauthorized: WatchPolicyV1PodDisruptionBudgetListForAllNamespacesResponse401,
        default_response: WatchPolicyV1PodDisruptionBudgetListForAllNamespacesResponseDefault,
    };

    pub const watchPolicyV1PodDisruptionBudgetListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PodDisruptionBudget. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchPolicyV1PodDisruptionBudgetListForAllNamespaces(self: *Client, options: watchPolicyV1PodDisruptionBudgetListForAllNamespacesOptions) !WatchPolicyV1PodDisruptionBudgetListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/policy/v1/watch/poddisruptionbudgets", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetRbacAuthorizationAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetRbacAuthorizationAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetRbacAuthorizationAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetRbacAuthorizationAPIGroupResponse = union(enum) {
        ok: GetRbacAuthorizationAPIGroupResponse200,
        unauthorized: GetRbacAuthorizationAPIGroupResponse401,
        default_response: GetRbacAuthorizationAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getRbacAuthorizationAPIGroup(self: *Client) !GetRbacAuthorizationAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetRbacAuthorizationV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetRbacAuthorizationV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetRbacAuthorizationV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetRbacAuthorizationV1APIResourcesResponse = union(enum) {
        ok: GetRbacAuthorizationV1APIResourcesResponse200,
        unauthorized: GetRbacAuthorizationV1APIResourcesResponse401,
        default_response: GetRbacAuthorizationV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getRbacAuthorizationV1APIResources(self: *Client) !GetRbacAuthorizationV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListRbacAuthorizationV1ClusterRoleBindingResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRoleBindingList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1ClusterRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1ClusterRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1ClusterRoleBindingResponse = union(enum) {
        ok: ListRbacAuthorizationV1ClusterRoleBindingResponse200,
        unauthorized: ListRbacAuthorizationV1ClusterRoleBindingResponse401,
        default_response: ListRbacAuthorizationV1ClusterRoleBindingResponseDefault,
    };

    pub const listRbacAuthorizationV1ClusterRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ClusterRoleBinding
    pub fn listRbacAuthorizationV1ClusterRoleBinding(self: *Client, options: listRbacAuthorizationV1ClusterRoleBindingOptions) !ListRbacAuthorizationV1ClusterRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterrolebindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRoleBindingList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistRbacAuthorizationV1ClusterRoleBinding(self: *Client, options: listRbacAuthorizationV1ClusterRoleBindingOptions) !WatchStream(root.io.k8s.api.rbac.v1.ClusterRoleBinding) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/rbac.authorization.k8s.io/v1/clusterrolebindings");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.rbac.v1.ClusterRoleBinding){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateRbacAuthorizationV1ClusterRoleBindingResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1ClusterRoleBindingResponse201 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1ClusterRoleBindingResponse202 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1ClusterRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1ClusterRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1ClusterRoleBindingResponse = union(enum) {
        ok: CreateRbacAuthorizationV1ClusterRoleBindingResponse200,
        created: CreateRbacAuthorizationV1ClusterRoleBindingResponse201,
        accepted: CreateRbacAuthorizationV1ClusterRoleBindingResponse202,
        unauthorized: CreateRbacAuthorizationV1ClusterRoleBindingResponse401,
        default_response: CreateRbacAuthorizationV1ClusterRoleBindingResponseDefault,
    };

    pub const createRbacAuthorizationV1ClusterRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ClusterRoleBinding
    pub fn createRbacAuthorizationV1ClusterRoleBinding(self: *Client, body: root.io.k8s.api.rbac.v1.ClusterRoleBinding, options: createRbacAuthorizationV1ClusterRoleBindingOptions) !CreateRbacAuthorizationV1ClusterRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterrolebindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteRbacAuthorizationV1CollectionClusterRoleBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1CollectionClusterRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1CollectionClusterRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1CollectionClusterRoleBindingResponse = union(enum) {
        ok: DeleteRbacAuthorizationV1CollectionClusterRoleBindingResponse200,
        unauthorized: DeleteRbacAuthorizationV1CollectionClusterRoleBindingResponse401,
        default_response: DeleteRbacAuthorizationV1CollectionClusterRoleBindingResponseDefault,
    };

    pub const deleteRbacAuthorizationV1CollectionClusterRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ClusterRoleBinding
    pub fn deleteRbacAuthorizationV1CollectionClusterRoleBinding(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteRbacAuthorizationV1CollectionClusterRoleBindingOptions) !DeleteRbacAuthorizationV1CollectionClusterRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterrolebindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadRbacAuthorizationV1ClusterRoleBindingResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadRbacAuthorizationV1ClusterRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadRbacAuthorizationV1ClusterRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadRbacAuthorizationV1ClusterRoleBindingResponse = union(enum) {
        ok: ReadRbacAuthorizationV1ClusterRoleBindingResponse200,
        unauthorized: ReadRbacAuthorizationV1ClusterRoleBindingResponse401,
        default_response: ReadRbacAuthorizationV1ClusterRoleBindingResponseDefault,
    };

    pub const readRbacAuthorizationV1ClusterRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ClusterRoleBinding
    pub fn readRbacAuthorizationV1ClusterRoleBinding(self: *Client, name: []const u8, options: readRbacAuthorizationV1ClusterRoleBindingOptions) !ReadRbacAuthorizationV1ClusterRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterrolebindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceRbacAuthorizationV1ClusterRoleBindingResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1ClusterRoleBindingResponse201 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1ClusterRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1ClusterRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1ClusterRoleBindingResponse = union(enum) {
        ok: ReplaceRbacAuthorizationV1ClusterRoleBindingResponse200,
        created: ReplaceRbacAuthorizationV1ClusterRoleBindingResponse201,
        unauthorized: ReplaceRbacAuthorizationV1ClusterRoleBindingResponse401,
        default_response: ReplaceRbacAuthorizationV1ClusterRoleBindingResponseDefault,
    };

    pub const replaceRbacAuthorizationV1ClusterRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ClusterRoleBinding
    pub fn replaceRbacAuthorizationV1ClusterRoleBinding(self: *Client, name: []const u8, body: root.io.k8s.api.rbac.v1.ClusterRoleBinding, options: replaceRbacAuthorizationV1ClusterRoleBindingOptions) !ReplaceRbacAuthorizationV1ClusterRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterrolebindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteRbacAuthorizationV1ClusterRoleBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1ClusterRoleBindingResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1ClusterRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1ClusterRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1ClusterRoleBindingResponse = union(enum) {
        ok: DeleteRbacAuthorizationV1ClusterRoleBindingResponse200,
        accepted: DeleteRbacAuthorizationV1ClusterRoleBindingResponse202,
        unauthorized: DeleteRbacAuthorizationV1ClusterRoleBindingResponse401,
        default_response: DeleteRbacAuthorizationV1ClusterRoleBindingResponseDefault,
    };

    pub const deleteRbacAuthorizationV1ClusterRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ClusterRoleBinding
    pub fn deleteRbacAuthorizationV1ClusterRoleBinding(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteRbacAuthorizationV1ClusterRoleBindingOptions) !DeleteRbacAuthorizationV1ClusterRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterrolebindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchRbacAuthorizationV1ClusterRoleBindingResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1ClusterRoleBindingResponse201 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1ClusterRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1ClusterRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1ClusterRoleBindingResponse = union(enum) {
        ok: PatchRbacAuthorizationV1ClusterRoleBindingResponse200,
        created: PatchRbacAuthorizationV1ClusterRoleBindingResponse201,
        unauthorized: PatchRbacAuthorizationV1ClusterRoleBindingResponse401,
        default_response: PatchRbacAuthorizationV1ClusterRoleBindingResponseDefault,
    };

    pub const patchRbacAuthorizationV1ClusterRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ClusterRoleBinding
    pub fn patchRbacAuthorizationV1ClusterRoleBinding(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchRbacAuthorizationV1ClusterRoleBindingOptions) !PatchRbacAuthorizationV1ClusterRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterrolebindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListRbacAuthorizationV1ClusterRoleResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRoleList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1ClusterRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1ClusterRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1ClusterRoleResponse = union(enum) {
        ok: ListRbacAuthorizationV1ClusterRoleResponse200,
        unauthorized: ListRbacAuthorizationV1ClusterRoleResponse401,
        default_response: ListRbacAuthorizationV1ClusterRoleResponseDefault,
    };

    pub const listRbacAuthorizationV1ClusterRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ClusterRole
    pub fn listRbacAuthorizationV1ClusterRole(self: *Client, options: listRbacAuthorizationV1ClusterRoleOptions) !ListRbacAuthorizationV1ClusterRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterroles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRoleList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistRbacAuthorizationV1ClusterRole(self: *Client, options: listRbacAuthorizationV1ClusterRoleOptions) !WatchStream(root.io.k8s.api.rbac.v1.ClusterRole) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/rbac.authorization.k8s.io/v1/clusterroles");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.rbac.v1.ClusterRole){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateRbacAuthorizationV1ClusterRoleResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRole,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1ClusterRoleResponse201 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRole,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1ClusterRoleResponse202 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRole,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1ClusterRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1ClusterRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1ClusterRoleResponse = union(enum) {
        ok: CreateRbacAuthorizationV1ClusterRoleResponse200,
        created: CreateRbacAuthorizationV1ClusterRoleResponse201,
        accepted: CreateRbacAuthorizationV1ClusterRoleResponse202,
        unauthorized: CreateRbacAuthorizationV1ClusterRoleResponse401,
        default_response: CreateRbacAuthorizationV1ClusterRoleResponseDefault,
    };

    pub const createRbacAuthorizationV1ClusterRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ClusterRole
    pub fn createRbacAuthorizationV1ClusterRole(self: *Client, body: root.io.k8s.api.rbac.v1.ClusterRole, options: createRbacAuthorizationV1ClusterRoleOptions) !CreateRbacAuthorizationV1ClusterRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterroles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRole, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRole, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRole, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteRbacAuthorizationV1CollectionClusterRoleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1CollectionClusterRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1CollectionClusterRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1CollectionClusterRoleResponse = union(enum) {
        ok: DeleteRbacAuthorizationV1CollectionClusterRoleResponse200,
        unauthorized: DeleteRbacAuthorizationV1CollectionClusterRoleResponse401,
        default_response: DeleteRbacAuthorizationV1CollectionClusterRoleResponseDefault,
    };

    pub const deleteRbacAuthorizationV1CollectionClusterRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ClusterRole
    pub fn deleteRbacAuthorizationV1CollectionClusterRole(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteRbacAuthorizationV1CollectionClusterRoleOptions) !DeleteRbacAuthorizationV1CollectionClusterRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterroles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadRbacAuthorizationV1ClusterRoleResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRole,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadRbacAuthorizationV1ClusterRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadRbacAuthorizationV1ClusterRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadRbacAuthorizationV1ClusterRoleResponse = union(enum) {
        ok: ReadRbacAuthorizationV1ClusterRoleResponse200,
        unauthorized: ReadRbacAuthorizationV1ClusterRoleResponse401,
        default_response: ReadRbacAuthorizationV1ClusterRoleResponseDefault,
    };

    pub const readRbacAuthorizationV1ClusterRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ClusterRole
    pub fn readRbacAuthorizationV1ClusterRole(self: *Client, name: []const u8, options: readRbacAuthorizationV1ClusterRoleOptions) !ReadRbacAuthorizationV1ClusterRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterroles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRole, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceRbacAuthorizationV1ClusterRoleResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRole,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1ClusterRoleResponse201 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRole,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1ClusterRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1ClusterRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1ClusterRoleResponse = union(enum) {
        ok: ReplaceRbacAuthorizationV1ClusterRoleResponse200,
        created: ReplaceRbacAuthorizationV1ClusterRoleResponse201,
        unauthorized: ReplaceRbacAuthorizationV1ClusterRoleResponse401,
        default_response: ReplaceRbacAuthorizationV1ClusterRoleResponseDefault,
    };

    pub const replaceRbacAuthorizationV1ClusterRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ClusterRole
    pub fn replaceRbacAuthorizationV1ClusterRole(self: *Client, name: []const u8, body: root.io.k8s.api.rbac.v1.ClusterRole, options: replaceRbacAuthorizationV1ClusterRoleOptions) !ReplaceRbacAuthorizationV1ClusterRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterroles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRole, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRole, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteRbacAuthorizationV1ClusterRoleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1ClusterRoleResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1ClusterRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1ClusterRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1ClusterRoleResponse = union(enum) {
        ok: DeleteRbacAuthorizationV1ClusterRoleResponse200,
        accepted: DeleteRbacAuthorizationV1ClusterRoleResponse202,
        unauthorized: DeleteRbacAuthorizationV1ClusterRoleResponse401,
        default_response: DeleteRbacAuthorizationV1ClusterRoleResponseDefault,
    };

    pub const deleteRbacAuthorizationV1ClusterRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ClusterRole
    pub fn deleteRbacAuthorizationV1ClusterRole(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteRbacAuthorizationV1ClusterRoleOptions) !DeleteRbacAuthorizationV1ClusterRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterroles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchRbacAuthorizationV1ClusterRoleResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRole,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1ClusterRoleResponse201 = struct {
        body: root.io.k8s.api.rbac.v1.ClusterRole,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1ClusterRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1ClusterRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1ClusterRoleResponse = union(enum) {
        ok: PatchRbacAuthorizationV1ClusterRoleResponse200,
        created: PatchRbacAuthorizationV1ClusterRoleResponse201,
        unauthorized: PatchRbacAuthorizationV1ClusterRoleResponse401,
        default_response: PatchRbacAuthorizationV1ClusterRoleResponseDefault,
    };

    pub const patchRbacAuthorizationV1ClusterRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ClusterRole
    pub fn patchRbacAuthorizationV1ClusterRole(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchRbacAuthorizationV1ClusterRoleOptions) !PatchRbacAuthorizationV1ClusterRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/clusterroles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRole, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.ClusterRole, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListRbacAuthorizationV1NamespacedRoleBindingResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.RoleBindingList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1NamespacedRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1NamespacedRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1NamespacedRoleBindingResponse = union(enum) {
        ok: ListRbacAuthorizationV1NamespacedRoleBindingResponse200,
        unauthorized: ListRbacAuthorizationV1NamespacedRoleBindingResponse401,
        default_response: ListRbacAuthorizationV1NamespacedRoleBindingResponseDefault,
    };

    pub const listRbacAuthorizationV1NamespacedRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind RoleBinding
    pub fn listRbacAuthorizationV1NamespacedRoleBinding(self: *Client, namespace: []const u8, options: listRbacAuthorizationV1NamespacedRoleBindingOptions) !ListRbacAuthorizationV1NamespacedRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/rolebindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.RoleBindingList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistRbacAuthorizationV1NamespacedRoleBinding(self: *Client, namespace: []const u8, options: listRbacAuthorizationV1NamespacedRoleBindingOptions) !WatchStream(root.io.k8s.api.rbac.v1.RoleBinding) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/rbac.authorization.k8s.io/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/rolebindings");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.rbac.v1.RoleBinding){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateRbacAuthorizationV1NamespacedRoleBindingResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.RoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1NamespacedRoleBindingResponse201 = struct {
        body: root.io.k8s.api.rbac.v1.RoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1NamespacedRoleBindingResponse202 = struct {
        body: root.io.k8s.api.rbac.v1.RoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1NamespacedRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1NamespacedRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1NamespacedRoleBindingResponse = union(enum) {
        ok: CreateRbacAuthorizationV1NamespacedRoleBindingResponse200,
        created: CreateRbacAuthorizationV1NamespacedRoleBindingResponse201,
        accepted: CreateRbacAuthorizationV1NamespacedRoleBindingResponse202,
        unauthorized: CreateRbacAuthorizationV1NamespacedRoleBindingResponse401,
        default_response: CreateRbacAuthorizationV1NamespacedRoleBindingResponseDefault,
    };

    pub const createRbacAuthorizationV1NamespacedRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a RoleBinding
    pub fn createRbacAuthorizationV1NamespacedRoleBinding(self: *Client, namespace: []const u8, body: root.io.k8s.api.rbac.v1.RoleBinding, options: createRbacAuthorizationV1NamespacedRoleBindingOptions) !CreateRbacAuthorizationV1NamespacedRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/rolebindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.RoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.RoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.RoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteRbacAuthorizationV1CollectionNamespacedRoleBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1CollectionNamespacedRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1CollectionNamespacedRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1CollectionNamespacedRoleBindingResponse = union(enum) {
        ok: DeleteRbacAuthorizationV1CollectionNamespacedRoleBindingResponse200,
        unauthorized: DeleteRbacAuthorizationV1CollectionNamespacedRoleBindingResponse401,
        default_response: DeleteRbacAuthorizationV1CollectionNamespacedRoleBindingResponseDefault,
    };

    pub const deleteRbacAuthorizationV1CollectionNamespacedRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of RoleBinding
    pub fn deleteRbacAuthorizationV1CollectionNamespacedRoleBinding(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteRbacAuthorizationV1CollectionNamespacedRoleBindingOptions) !DeleteRbacAuthorizationV1CollectionNamespacedRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/rolebindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadRbacAuthorizationV1NamespacedRoleBindingResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.RoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadRbacAuthorizationV1NamespacedRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadRbacAuthorizationV1NamespacedRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadRbacAuthorizationV1NamespacedRoleBindingResponse = union(enum) {
        ok: ReadRbacAuthorizationV1NamespacedRoleBindingResponse200,
        unauthorized: ReadRbacAuthorizationV1NamespacedRoleBindingResponse401,
        default_response: ReadRbacAuthorizationV1NamespacedRoleBindingResponseDefault,
    };

    pub const readRbacAuthorizationV1NamespacedRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified RoleBinding
    pub fn readRbacAuthorizationV1NamespacedRoleBinding(self: *Client, name: []const u8, namespace: []const u8, options: readRbacAuthorizationV1NamespacedRoleBindingOptions) !ReadRbacAuthorizationV1NamespacedRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/rolebindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.RoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceRbacAuthorizationV1NamespacedRoleBindingResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.RoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1NamespacedRoleBindingResponse201 = struct {
        body: root.io.k8s.api.rbac.v1.RoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1NamespacedRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1NamespacedRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1NamespacedRoleBindingResponse = union(enum) {
        ok: ReplaceRbacAuthorizationV1NamespacedRoleBindingResponse200,
        created: ReplaceRbacAuthorizationV1NamespacedRoleBindingResponse201,
        unauthorized: ReplaceRbacAuthorizationV1NamespacedRoleBindingResponse401,
        default_response: ReplaceRbacAuthorizationV1NamespacedRoleBindingResponseDefault,
    };

    pub const replaceRbacAuthorizationV1NamespacedRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified RoleBinding
    pub fn replaceRbacAuthorizationV1NamespacedRoleBinding(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.rbac.v1.RoleBinding, options: replaceRbacAuthorizationV1NamespacedRoleBindingOptions) !ReplaceRbacAuthorizationV1NamespacedRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/rolebindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.RoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.RoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteRbacAuthorizationV1NamespacedRoleBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1NamespacedRoleBindingResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1NamespacedRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1NamespacedRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1NamespacedRoleBindingResponse = union(enum) {
        ok: DeleteRbacAuthorizationV1NamespacedRoleBindingResponse200,
        accepted: DeleteRbacAuthorizationV1NamespacedRoleBindingResponse202,
        unauthorized: DeleteRbacAuthorizationV1NamespacedRoleBindingResponse401,
        default_response: DeleteRbacAuthorizationV1NamespacedRoleBindingResponseDefault,
    };

    pub const deleteRbacAuthorizationV1NamespacedRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a RoleBinding
    pub fn deleteRbacAuthorizationV1NamespacedRoleBinding(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteRbacAuthorizationV1NamespacedRoleBindingOptions) !DeleteRbacAuthorizationV1NamespacedRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/rolebindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchRbacAuthorizationV1NamespacedRoleBindingResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.RoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1NamespacedRoleBindingResponse201 = struct {
        body: root.io.k8s.api.rbac.v1.RoleBinding,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1NamespacedRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1NamespacedRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1NamespacedRoleBindingResponse = union(enum) {
        ok: PatchRbacAuthorizationV1NamespacedRoleBindingResponse200,
        created: PatchRbacAuthorizationV1NamespacedRoleBindingResponse201,
        unauthorized: PatchRbacAuthorizationV1NamespacedRoleBindingResponse401,
        default_response: PatchRbacAuthorizationV1NamespacedRoleBindingResponseDefault,
    };

    pub const patchRbacAuthorizationV1NamespacedRoleBindingOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified RoleBinding
    pub fn patchRbacAuthorizationV1NamespacedRoleBinding(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchRbacAuthorizationV1NamespacedRoleBindingOptions) !PatchRbacAuthorizationV1NamespacedRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/rolebindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.RoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.RoleBinding, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListRbacAuthorizationV1NamespacedRoleResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.RoleList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1NamespacedRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1NamespacedRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1NamespacedRoleResponse = union(enum) {
        ok: ListRbacAuthorizationV1NamespacedRoleResponse200,
        unauthorized: ListRbacAuthorizationV1NamespacedRoleResponse401,
        default_response: ListRbacAuthorizationV1NamespacedRoleResponseDefault,
    };

    pub const listRbacAuthorizationV1NamespacedRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Role
    pub fn listRbacAuthorizationV1NamespacedRole(self: *Client, namespace: []const u8, options: listRbacAuthorizationV1NamespacedRoleOptions) !ListRbacAuthorizationV1NamespacedRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/roles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.RoleList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistRbacAuthorizationV1NamespacedRole(self: *Client, namespace: []const u8, options: listRbacAuthorizationV1NamespacedRoleOptions) !WatchStream(root.io.k8s.api.rbac.v1.Role) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/rbac.authorization.k8s.io/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/roles");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.rbac.v1.Role){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateRbacAuthorizationV1NamespacedRoleResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.Role,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1NamespacedRoleResponse201 = struct {
        body: root.io.k8s.api.rbac.v1.Role,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1NamespacedRoleResponse202 = struct {
        body: root.io.k8s.api.rbac.v1.Role,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1NamespacedRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1NamespacedRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateRbacAuthorizationV1NamespacedRoleResponse = union(enum) {
        ok: CreateRbacAuthorizationV1NamespacedRoleResponse200,
        created: CreateRbacAuthorizationV1NamespacedRoleResponse201,
        accepted: CreateRbacAuthorizationV1NamespacedRoleResponse202,
        unauthorized: CreateRbacAuthorizationV1NamespacedRoleResponse401,
        default_response: CreateRbacAuthorizationV1NamespacedRoleResponseDefault,
    };

    pub const createRbacAuthorizationV1NamespacedRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a Role
    pub fn createRbacAuthorizationV1NamespacedRole(self: *Client, namespace: []const u8, body: root.io.k8s.api.rbac.v1.Role, options: createRbacAuthorizationV1NamespacedRoleOptions) !CreateRbacAuthorizationV1NamespacedRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/roles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.Role, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.Role, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.Role, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteRbacAuthorizationV1CollectionNamespacedRoleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1CollectionNamespacedRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1CollectionNamespacedRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1CollectionNamespacedRoleResponse = union(enum) {
        ok: DeleteRbacAuthorizationV1CollectionNamespacedRoleResponse200,
        unauthorized: DeleteRbacAuthorizationV1CollectionNamespacedRoleResponse401,
        default_response: DeleteRbacAuthorizationV1CollectionNamespacedRoleResponseDefault,
    };

    pub const deleteRbacAuthorizationV1CollectionNamespacedRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Role
    pub fn deleteRbacAuthorizationV1CollectionNamespacedRole(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteRbacAuthorizationV1CollectionNamespacedRoleOptions) !DeleteRbacAuthorizationV1CollectionNamespacedRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/roles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadRbacAuthorizationV1NamespacedRoleResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.Role,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadRbacAuthorizationV1NamespacedRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadRbacAuthorizationV1NamespacedRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadRbacAuthorizationV1NamespacedRoleResponse = union(enum) {
        ok: ReadRbacAuthorizationV1NamespacedRoleResponse200,
        unauthorized: ReadRbacAuthorizationV1NamespacedRoleResponse401,
        default_response: ReadRbacAuthorizationV1NamespacedRoleResponseDefault,
    };

    pub const readRbacAuthorizationV1NamespacedRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Role
    pub fn readRbacAuthorizationV1NamespacedRole(self: *Client, name: []const u8, namespace: []const u8, options: readRbacAuthorizationV1NamespacedRoleOptions) !ReadRbacAuthorizationV1NamespacedRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/roles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.Role, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceRbacAuthorizationV1NamespacedRoleResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.Role,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1NamespacedRoleResponse201 = struct {
        body: root.io.k8s.api.rbac.v1.Role,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1NamespacedRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1NamespacedRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceRbacAuthorizationV1NamespacedRoleResponse = union(enum) {
        ok: ReplaceRbacAuthorizationV1NamespacedRoleResponse200,
        created: ReplaceRbacAuthorizationV1NamespacedRoleResponse201,
        unauthorized: ReplaceRbacAuthorizationV1NamespacedRoleResponse401,
        default_response: ReplaceRbacAuthorizationV1NamespacedRoleResponseDefault,
    };

    pub const replaceRbacAuthorizationV1NamespacedRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Role
    pub fn replaceRbacAuthorizationV1NamespacedRole(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.rbac.v1.Role, options: replaceRbacAuthorizationV1NamespacedRoleOptions) !ReplaceRbacAuthorizationV1NamespacedRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/roles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.Role, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.Role, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteRbacAuthorizationV1NamespacedRoleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1NamespacedRoleResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1NamespacedRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1NamespacedRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteRbacAuthorizationV1NamespacedRoleResponse = union(enum) {
        ok: DeleteRbacAuthorizationV1NamespacedRoleResponse200,
        accepted: DeleteRbacAuthorizationV1NamespacedRoleResponse202,
        unauthorized: DeleteRbacAuthorizationV1NamespacedRoleResponse401,
        default_response: DeleteRbacAuthorizationV1NamespacedRoleResponseDefault,
    };

    pub const deleteRbacAuthorizationV1NamespacedRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a Role
    pub fn deleteRbacAuthorizationV1NamespacedRole(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteRbacAuthorizationV1NamespacedRoleOptions) !DeleteRbacAuthorizationV1NamespacedRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/roles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchRbacAuthorizationV1NamespacedRoleResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.Role,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1NamespacedRoleResponse201 = struct {
        body: root.io.k8s.api.rbac.v1.Role,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1NamespacedRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1NamespacedRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchRbacAuthorizationV1NamespacedRoleResponse = union(enum) {
        ok: PatchRbacAuthorizationV1NamespacedRoleResponse200,
        created: PatchRbacAuthorizationV1NamespacedRoleResponse201,
        unauthorized: PatchRbacAuthorizationV1NamespacedRoleResponse401,
        default_response: PatchRbacAuthorizationV1NamespacedRoleResponseDefault,
    };

    pub const patchRbacAuthorizationV1NamespacedRoleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Role
    pub fn patchRbacAuthorizationV1NamespacedRole(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchRbacAuthorizationV1NamespacedRoleOptions) !PatchRbacAuthorizationV1NamespacedRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/roles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.Role, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.Role, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListRbacAuthorizationV1RoleBindingForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.RoleBindingList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1RoleBindingForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1RoleBindingForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1RoleBindingForAllNamespacesResponse = union(enum) {
        ok: ListRbacAuthorizationV1RoleBindingForAllNamespacesResponse200,
        unauthorized: ListRbacAuthorizationV1RoleBindingForAllNamespacesResponse401,
        default_response: ListRbacAuthorizationV1RoleBindingForAllNamespacesResponseDefault,
    };

    pub const listRbacAuthorizationV1RoleBindingForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind RoleBinding
    pub fn listRbacAuthorizationV1RoleBindingForAllNamespaces(self: *Client, options: listRbacAuthorizationV1RoleBindingForAllNamespacesOptions) !ListRbacAuthorizationV1RoleBindingForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/rolebindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.RoleBindingList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistRbacAuthorizationV1RoleBindingForAllNamespaces(self: *Client, options: listRbacAuthorizationV1RoleBindingForAllNamespacesOptions) !WatchStream(root.io.k8s.api.rbac.v1.RoleBinding) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/rbac.authorization.k8s.io/v1/rolebindings");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.rbac.v1.RoleBinding){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListRbacAuthorizationV1RoleForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.rbac.v1.RoleList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1RoleForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1RoleForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListRbacAuthorizationV1RoleForAllNamespacesResponse = union(enum) {
        ok: ListRbacAuthorizationV1RoleForAllNamespacesResponse200,
        unauthorized: ListRbacAuthorizationV1RoleForAllNamespacesResponse401,
        default_response: ListRbacAuthorizationV1RoleForAllNamespacesResponseDefault,
    };

    pub const listRbacAuthorizationV1RoleForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Role
    pub fn listRbacAuthorizationV1RoleForAllNamespaces(self: *Client, options: listRbacAuthorizationV1RoleForAllNamespacesOptions) !ListRbacAuthorizationV1RoleForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/roles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.rbac.v1.RoleList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistRbacAuthorizationV1RoleForAllNamespaces(self: *Client, options: listRbacAuthorizationV1RoleForAllNamespacesOptions) !WatchStream(root.io.k8s.api.rbac.v1.Role) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/rbac.authorization.k8s.io/v1/roles");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.rbac.v1.Role){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const WatchRbacAuthorizationV1ClusterRoleBindingListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1ClusterRoleBindingListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1ClusterRoleBindingListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1ClusterRoleBindingListResponse = union(enum) {
        ok: WatchRbacAuthorizationV1ClusterRoleBindingListResponse200,
        unauthorized: WatchRbacAuthorizationV1ClusterRoleBindingListResponse401,
        default_response: WatchRbacAuthorizationV1ClusterRoleBindingListResponseDefault,
    };

    pub const watchRbacAuthorizationV1ClusterRoleBindingListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ClusterRoleBinding. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchRbacAuthorizationV1ClusterRoleBindingList(self: *Client, options: watchRbacAuthorizationV1ClusterRoleBindingListOptions) !WatchRbacAuthorizationV1ClusterRoleBindingListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/watch/clusterrolebindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchRbacAuthorizationV1ClusterRoleBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1ClusterRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1ClusterRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1ClusterRoleBindingResponse = union(enum) {
        ok: WatchRbacAuthorizationV1ClusterRoleBindingResponse200,
        unauthorized: WatchRbacAuthorizationV1ClusterRoleBindingResponse401,
        default_response: WatchRbacAuthorizationV1ClusterRoleBindingResponseDefault,
    };

    pub const watchRbacAuthorizationV1ClusterRoleBindingOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ClusterRoleBinding. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchRbacAuthorizationV1ClusterRoleBinding(self: *Client, name: []const u8, options: watchRbacAuthorizationV1ClusterRoleBindingOptions) !WatchRbacAuthorizationV1ClusterRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/watch/clusterrolebindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchRbacAuthorizationV1ClusterRoleListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1ClusterRoleListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1ClusterRoleListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1ClusterRoleListResponse = union(enum) {
        ok: WatchRbacAuthorizationV1ClusterRoleListResponse200,
        unauthorized: WatchRbacAuthorizationV1ClusterRoleListResponse401,
        default_response: WatchRbacAuthorizationV1ClusterRoleListResponseDefault,
    };

    pub const watchRbacAuthorizationV1ClusterRoleListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ClusterRole. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchRbacAuthorizationV1ClusterRoleList(self: *Client, options: watchRbacAuthorizationV1ClusterRoleListOptions) !WatchRbacAuthorizationV1ClusterRoleListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/watch/clusterroles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchRbacAuthorizationV1ClusterRoleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1ClusterRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1ClusterRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1ClusterRoleResponse = union(enum) {
        ok: WatchRbacAuthorizationV1ClusterRoleResponse200,
        unauthorized: WatchRbacAuthorizationV1ClusterRoleResponse401,
        default_response: WatchRbacAuthorizationV1ClusterRoleResponseDefault,
    };

    pub const watchRbacAuthorizationV1ClusterRoleOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ClusterRole. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchRbacAuthorizationV1ClusterRole(self: *Client, name: []const u8, options: watchRbacAuthorizationV1ClusterRoleOptions) !WatchRbacAuthorizationV1ClusterRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/watch/clusterroles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchRbacAuthorizationV1NamespacedRoleBindingListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1NamespacedRoleBindingListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1NamespacedRoleBindingListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1NamespacedRoleBindingListResponse = union(enum) {
        ok: WatchRbacAuthorizationV1NamespacedRoleBindingListResponse200,
        unauthorized: WatchRbacAuthorizationV1NamespacedRoleBindingListResponse401,
        default_response: WatchRbacAuthorizationV1NamespacedRoleBindingListResponseDefault,
    };

    pub const watchRbacAuthorizationV1NamespacedRoleBindingListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of RoleBinding. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchRbacAuthorizationV1NamespacedRoleBindingList(self: *Client, namespace: []const u8, options: watchRbacAuthorizationV1NamespacedRoleBindingListOptions) !WatchRbacAuthorizationV1NamespacedRoleBindingListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/rolebindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchRbacAuthorizationV1NamespacedRoleBindingResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1NamespacedRoleBindingResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1NamespacedRoleBindingResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1NamespacedRoleBindingResponse = union(enum) {
        ok: WatchRbacAuthorizationV1NamespacedRoleBindingResponse200,
        unauthorized: WatchRbacAuthorizationV1NamespacedRoleBindingResponse401,
        default_response: WatchRbacAuthorizationV1NamespacedRoleBindingResponseDefault,
    };

    pub const watchRbacAuthorizationV1NamespacedRoleBindingOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind RoleBinding. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchRbacAuthorizationV1NamespacedRoleBinding(self: *Client, name: []const u8, namespace: []const u8, options: watchRbacAuthorizationV1NamespacedRoleBindingOptions) !WatchRbacAuthorizationV1NamespacedRoleBindingResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/rolebindings/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchRbacAuthorizationV1NamespacedRoleListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1NamespacedRoleListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1NamespacedRoleListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1NamespacedRoleListResponse = union(enum) {
        ok: WatchRbacAuthorizationV1NamespacedRoleListResponse200,
        unauthorized: WatchRbacAuthorizationV1NamespacedRoleListResponse401,
        default_response: WatchRbacAuthorizationV1NamespacedRoleListResponseDefault,
    };

    pub const watchRbacAuthorizationV1NamespacedRoleListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Role. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchRbacAuthorizationV1NamespacedRoleList(self: *Client, namespace: []const u8, options: watchRbacAuthorizationV1NamespacedRoleListOptions) !WatchRbacAuthorizationV1NamespacedRoleListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/roles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchRbacAuthorizationV1NamespacedRoleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1NamespacedRoleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1NamespacedRoleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1NamespacedRoleResponse = union(enum) {
        ok: WatchRbacAuthorizationV1NamespacedRoleResponse200,
        unauthorized: WatchRbacAuthorizationV1NamespacedRoleResponse401,
        default_response: WatchRbacAuthorizationV1NamespacedRoleResponseDefault,
    };

    pub const watchRbacAuthorizationV1NamespacedRoleOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Role. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchRbacAuthorizationV1NamespacedRole(self: *Client, name: []const u8, namespace: []const u8, options: watchRbacAuthorizationV1NamespacedRoleOptions) !WatchRbacAuthorizationV1NamespacedRoleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/roles/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchRbacAuthorizationV1RoleBindingListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1RoleBindingListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1RoleBindingListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1RoleBindingListForAllNamespacesResponse = union(enum) {
        ok: WatchRbacAuthorizationV1RoleBindingListForAllNamespacesResponse200,
        unauthorized: WatchRbacAuthorizationV1RoleBindingListForAllNamespacesResponse401,
        default_response: WatchRbacAuthorizationV1RoleBindingListForAllNamespacesResponseDefault,
    };

    pub const watchRbacAuthorizationV1RoleBindingListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of RoleBinding. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchRbacAuthorizationV1RoleBindingListForAllNamespaces(self: *Client, options: watchRbacAuthorizationV1RoleBindingListForAllNamespacesOptions) !WatchRbacAuthorizationV1RoleBindingListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/watch/rolebindings", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchRbacAuthorizationV1RoleListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1RoleListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1RoleListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchRbacAuthorizationV1RoleListForAllNamespacesResponse = union(enum) {
        ok: WatchRbacAuthorizationV1RoleListForAllNamespacesResponse200,
        unauthorized: WatchRbacAuthorizationV1RoleListForAllNamespacesResponse401,
        default_response: WatchRbacAuthorizationV1RoleListForAllNamespacesResponseDefault,
    };

    pub const watchRbacAuthorizationV1RoleListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Role. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchRbacAuthorizationV1RoleListForAllNamespaces(self: *Client, options: watchRbacAuthorizationV1RoleListForAllNamespacesOptions) !WatchRbacAuthorizationV1RoleListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/rbac.authorization.k8s.io/v1/watch/roles", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetResourceAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceAPIGroupResponse = union(enum) {
        ok: GetResourceAPIGroupResponse200,
        unauthorized: GetResourceAPIGroupResponse401,
        default_response: GetResourceAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getResourceAPIGroup(self: *Client) !GetResourceAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetResourceV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceV1APIResourcesResponse = union(enum) {
        ok: GetResourceV1APIResourcesResponse200,
        unauthorized: GetResourceV1APIResourcesResponse401,
        default_response: GetResourceV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getResourceV1APIResources(self: *Client) !GetResourceV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceClassList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1DeviceClassResponse = union(enum) {
        ok: ListResourceV1DeviceClassResponse200,
        unauthorized: ListResourceV1DeviceClassResponse401,
        default_response: ListResourceV1DeviceClassResponseDefault,
    };

    pub const listResourceV1DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind DeviceClass
    pub fn listResourceV1DeviceClass(self: *Client, options: listResourceV1DeviceClassOptions) !ListResourceV1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/deviceclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceClassList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1DeviceClass(self: *Client, options: listResourceV1DeviceClassOptions) !WatchStream(root.io.k8s.api.resource.v1.DeviceClass) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1/deviceclasses");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1.DeviceClass){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1DeviceClassResponse201 = struct {
        body: root.io.k8s.api.resource.v1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1DeviceClassResponse202 = struct {
        body: root.io.k8s.api.resource.v1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1DeviceClassResponse = union(enum) {
        ok: CreateResourceV1DeviceClassResponse200,
        created: CreateResourceV1DeviceClassResponse201,
        accepted: CreateResourceV1DeviceClassResponse202,
        unauthorized: CreateResourceV1DeviceClassResponse401,
        default_response: CreateResourceV1DeviceClassResponseDefault,
    };

    pub const createResourceV1DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a DeviceClass
    pub fn createResourceV1DeviceClass(self: *Client, body: root.io.k8s.api.resource.v1.DeviceClass, options: createResourceV1DeviceClassOptions) !CreateResourceV1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/deviceclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1CollectionDeviceClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionDeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionDeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionDeviceClassResponse = union(enum) {
        ok: DeleteResourceV1CollectionDeviceClassResponse200,
        unauthorized: DeleteResourceV1CollectionDeviceClassResponse401,
        default_response: DeleteResourceV1CollectionDeviceClassResponseDefault,
    };

    pub const deleteResourceV1CollectionDeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of DeviceClass
    pub fn deleteResourceV1CollectionDeviceClass(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1CollectionDeviceClassOptions) !DeleteResourceV1CollectionDeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/deviceclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1DeviceClassResponse = union(enum) {
        ok: ReadResourceV1DeviceClassResponse200,
        unauthorized: ReadResourceV1DeviceClassResponse401,
        default_response: ReadResourceV1DeviceClassResponseDefault,
    };

    pub const readResourceV1DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified DeviceClass
    pub fn readResourceV1DeviceClass(self: *Client, name: []const u8, options: readResourceV1DeviceClassOptions) !ReadResourceV1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1DeviceClassResponse201 = struct {
        body: root.io.k8s.api.resource.v1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1DeviceClassResponse = union(enum) {
        ok: ReplaceResourceV1DeviceClassResponse200,
        created: ReplaceResourceV1DeviceClassResponse201,
        unauthorized: ReplaceResourceV1DeviceClassResponse401,
        default_response: ReplaceResourceV1DeviceClassResponseDefault,
    };

    pub const replaceResourceV1DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified DeviceClass
    pub fn replaceResourceV1DeviceClass(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1.DeviceClass, options: replaceResourceV1DeviceClassOptions) !ReplaceResourceV1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1DeviceClassResponse202 = struct {
        body: root.io.k8s.api.resource.v1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1DeviceClassResponse = union(enum) {
        ok: DeleteResourceV1DeviceClassResponse200,
        accepted: DeleteResourceV1DeviceClassResponse202,
        unauthorized: DeleteResourceV1DeviceClassResponse401,
        default_response: DeleteResourceV1DeviceClassResponseDefault,
    };

    pub const deleteResourceV1DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a DeviceClass
    pub fn deleteResourceV1DeviceClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1DeviceClassOptions) !DeleteResourceV1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1DeviceClassResponse201 = struct {
        body: root.io.k8s.api.resource.v1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1DeviceClassResponse = union(enum) {
        ok: PatchResourceV1DeviceClassResponse200,
        created: PatchResourceV1DeviceClassResponse201,
        unauthorized: PatchResourceV1DeviceClassResponse401,
        default_response: PatchResourceV1DeviceClassResponseDefault,
    };

    pub const patchResourceV1DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified DeviceClass
    pub fn patchResourceV1DeviceClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1DeviceClassOptions) !PatchResourceV1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRuleList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1DeviceTaintRuleResponse = union(enum) {
        ok: ListResourceV1DeviceTaintRuleResponse200,
        unauthorized: ListResourceV1DeviceTaintRuleResponse401,
        default_response: ListResourceV1DeviceTaintRuleResponseDefault,
    };

    pub const listResourceV1DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind DeviceTaintRule
    pub fn listResourceV1DeviceTaintRule(self: *Client, options: listResourceV1DeviceTaintRuleOptions) !ListResourceV1DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/devicetaintrules", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRuleList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1DeviceTaintRule(self: *Client, options: listResourceV1DeviceTaintRuleOptions) !WatchStream(root.io.k8s.api.resource.v1.DeviceTaintRule) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1/devicetaintrules");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1.DeviceTaintRule){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1DeviceTaintRuleResponse201 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1DeviceTaintRuleResponse202 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1DeviceTaintRuleResponse = union(enum) {
        ok: CreateResourceV1DeviceTaintRuleResponse200,
        created: CreateResourceV1DeviceTaintRuleResponse201,
        accepted: CreateResourceV1DeviceTaintRuleResponse202,
        unauthorized: CreateResourceV1DeviceTaintRuleResponse401,
        default_response: CreateResourceV1DeviceTaintRuleResponseDefault,
    };

    pub const createResourceV1DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a DeviceTaintRule
    pub fn createResourceV1DeviceTaintRule(self: *Client, body: root.io.k8s.api.resource.v1.DeviceTaintRule, options: createResourceV1DeviceTaintRuleOptions) !CreateResourceV1DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/devicetaintrules", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1CollectionDeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionDeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionDeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionDeviceTaintRuleResponse = union(enum) {
        ok: DeleteResourceV1CollectionDeviceTaintRuleResponse200,
        unauthorized: DeleteResourceV1CollectionDeviceTaintRuleResponse401,
        default_response: DeleteResourceV1CollectionDeviceTaintRuleResponseDefault,
    };

    pub const deleteResourceV1CollectionDeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of DeviceTaintRule
    pub fn deleteResourceV1CollectionDeviceTaintRule(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1CollectionDeviceTaintRuleOptions) !DeleteResourceV1CollectionDeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/devicetaintrules", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1DeviceTaintRuleResponse = union(enum) {
        ok: ReadResourceV1DeviceTaintRuleResponse200,
        unauthorized: ReadResourceV1DeviceTaintRuleResponse401,
        default_response: ReadResourceV1DeviceTaintRuleResponseDefault,
    };

    pub const readResourceV1DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified DeviceTaintRule
    pub fn readResourceV1DeviceTaintRule(self: *Client, name: []const u8, options: readResourceV1DeviceTaintRuleOptions) !ReadResourceV1DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1DeviceTaintRuleResponse201 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1DeviceTaintRuleResponse = union(enum) {
        ok: ReplaceResourceV1DeviceTaintRuleResponse200,
        created: ReplaceResourceV1DeviceTaintRuleResponse201,
        unauthorized: ReplaceResourceV1DeviceTaintRuleResponse401,
        default_response: ReplaceResourceV1DeviceTaintRuleResponseDefault,
    };

    pub const replaceResourceV1DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified DeviceTaintRule
    pub fn replaceResourceV1DeviceTaintRule(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1.DeviceTaintRule, options: replaceResourceV1DeviceTaintRuleOptions) !ReplaceResourceV1DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1DeviceTaintRuleResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1DeviceTaintRuleResponse = union(enum) {
        ok: DeleteResourceV1DeviceTaintRuleResponse200,
        accepted: DeleteResourceV1DeviceTaintRuleResponse202,
        unauthorized: DeleteResourceV1DeviceTaintRuleResponse401,
        default_response: DeleteResourceV1DeviceTaintRuleResponseDefault,
    };

    pub const deleteResourceV1DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a DeviceTaintRule
    pub fn deleteResourceV1DeviceTaintRule(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1DeviceTaintRuleOptions) !DeleteResourceV1DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1DeviceTaintRuleResponse201 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1DeviceTaintRuleResponse = union(enum) {
        ok: PatchResourceV1DeviceTaintRuleResponse200,
        created: PatchResourceV1DeviceTaintRuleResponse201,
        unauthorized: PatchResourceV1DeviceTaintRuleResponse401,
        default_response: PatchResourceV1DeviceTaintRuleResponseDefault,
    };

    pub const patchResourceV1DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified DeviceTaintRule
    pub fn patchResourceV1DeviceTaintRule(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1DeviceTaintRuleOptions) !PatchResourceV1DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1DeviceTaintRuleStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1DeviceTaintRuleStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1DeviceTaintRuleStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1DeviceTaintRuleStatusResponse = union(enum) {
        ok: ReadResourceV1DeviceTaintRuleStatusResponse200,
        unauthorized: ReadResourceV1DeviceTaintRuleStatusResponse401,
        default_response: ReadResourceV1DeviceTaintRuleStatusResponseDefault,
    };

    pub const readResourceV1DeviceTaintRuleStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified DeviceTaintRule
    pub fn readResourceV1DeviceTaintRuleStatus(self: *Client, name: []const u8, options: readResourceV1DeviceTaintRuleStatusOptions) !ReadResourceV1DeviceTaintRuleStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1DeviceTaintRuleStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1DeviceTaintRuleStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1DeviceTaintRuleStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1DeviceTaintRuleStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1DeviceTaintRuleStatusResponse = union(enum) {
        ok: ReplaceResourceV1DeviceTaintRuleStatusResponse200,
        created: ReplaceResourceV1DeviceTaintRuleStatusResponse201,
        unauthorized: ReplaceResourceV1DeviceTaintRuleStatusResponse401,
        default_response: ReplaceResourceV1DeviceTaintRuleStatusResponseDefault,
    };

    pub const replaceResourceV1DeviceTaintRuleStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified DeviceTaintRule
    pub fn replaceResourceV1DeviceTaintRuleStatus(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1.DeviceTaintRule, options: replaceResourceV1DeviceTaintRuleStatusOptions) !ReplaceResourceV1DeviceTaintRuleStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1DeviceTaintRuleStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1DeviceTaintRuleStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1DeviceTaintRuleStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1DeviceTaintRuleStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1DeviceTaintRuleStatusResponse = union(enum) {
        ok: PatchResourceV1DeviceTaintRuleStatusResponse200,
        created: PatchResourceV1DeviceTaintRuleStatusResponse201,
        unauthorized: PatchResourceV1DeviceTaintRuleStatusResponse401,
        default_response: PatchResourceV1DeviceTaintRuleStatusResponseDefault,
    };

    pub const patchResourceV1DeviceTaintRuleStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified DeviceTaintRule
    pub fn patchResourceV1DeviceTaintRuleStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1DeviceTaintRuleStatusOptions) !PatchResourceV1DeviceTaintRuleStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1NamespacedResourceClaimResponse = union(enum) {
        ok: ListResourceV1NamespacedResourceClaimResponse200,
        unauthorized: ListResourceV1NamespacedResourceClaimResponse401,
        default_response: ListResourceV1NamespacedResourceClaimResponseDefault,
    };

    pub const listResourceV1NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceClaim
    pub fn listResourceV1NamespacedResourceClaim(self: *Client, namespace: []const u8, options: listResourceV1NamespacedResourceClaimOptions) !ListResourceV1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1NamespacedResourceClaim(self: *Client, namespace: []const u8, options: listResourceV1NamespacedResourceClaimOptions) !WatchStream(root.io.k8s.api.resource.v1.ResourceClaim) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/resourceclaims");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1.ResourceClaim){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1NamespacedResourceClaimResponse201 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1NamespacedResourceClaimResponse202 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1NamespacedResourceClaimResponse = union(enum) {
        ok: CreateResourceV1NamespacedResourceClaimResponse200,
        created: CreateResourceV1NamespacedResourceClaimResponse201,
        accepted: CreateResourceV1NamespacedResourceClaimResponse202,
        unauthorized: CreateResourceV1NamespacedResourceClaimResponse401,
        default_response: CreateResourceV1NamespacedResourceClaimResponseDefault,
    };

    pub const createResourceV1NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ResourceClaim
    pub fn createResourceV1NamespacedResourceClaim(self: *Client, namespace: []const u8, body: root.io.k8s.api.resource.v1.ResourceClaim, options: createResourceV1NamespacedResourceClaimOptions) !CreateResourceV1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1CollectionNamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionNamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionNamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionNamespacedResourceClaimResponse = union(enum) {
        ok: DeleteResourceV1CollectionNamespacedResourceClaimResponse200,
        unauthorized: DeleteResourceV1CollectionNamespacedResourceClaimResponse401,
        default_response: DeleteResourceV1CollectionNamespacedResourceClaimResponseDefault,
    };

    pub const deleteResourceV1CollectionNamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ResourceClaim
    pub fn deleteResourceV1CollectionNamespacedResourceClaim(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1CollectionNamespacedResourceClaimOptions) !DeleteResourceV1CollectionNamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1NamespacedResourceClaimResponse = union(enum) {
        ok: ReadResourceV1NamespacedResourceClaimResponse200,
        unauthorized: ReadResourceV1NamespacedResourceClaimResponse401,
        default_response: ReadResourceV1NamespacedResourceClaimResponseDefault,
    };

    pub const readResourceV1NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ResourceClaim
    pub fn readResourceV1NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, options: readResourceV1NamespacedResourceClaimOptions) !ReadResourceV1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1NamespacedResourceClaimResponse201 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1NamespacedResourceClaimResponse = union(enum) {
        ok: ReplaceResourceV1NamespacedResourceClaimResponse200,
        created: ReplaceResourceV1NamespacedResourceClaimResponse201,
        unauthorized: ReplaceResourceV1NamespacedResourceClaimResponse401,
        default_response: ReplaceResourceV1NamespacedResourceClaimResponseDefault,
    };

    pub const replaceResourceV1NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ResourceClaim
    pub fn replaceResourceV1NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.resource.v1.ResourceClaim, options: replaceResourceV1NamespacedResourceClaimOptions) !ReplaceResourceV1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1NamespacedResourceClaimResponse202 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1NamespacedResourceClaimResponse = union(enum) {
        ok: DeleteResourceV1NamespacedResourceClaimResponse200,
        accepted: DeleteResourceV1NamespacedResourceClaimResponse202,
        unauthorized: DeleteResourceV1NamespacedResourceClaimResponse401,
        default_response: DeleteResourceV1NamespacedResourceClaimResponseDefault,
    };

    pub const deleteResourceV1NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ResourceClaim
    pub fn deleteResourceV1NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1NamespacedResourceClaimOptions) !DeleteResourceV1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1NamespacedResourceClaimResponse201 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1NamespacedResourceClaimResponse = union(enum) {
        ok: PatchResourceV1NamespacedResourceClaimResponse200,
        created: PatchResourceV1NamespacedResourceClaimResponse201,
        unauthorized: PatchResourceV1NamespacedResourceClaimResponse401,
        default_response: PatchResourceV1NamespacedResourceClaimResponseDefault,
    };

    pub const patchResourceV1NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ResourceClaim
    pub fn patchResourceV1NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1NamespacedResourceClaimOptions) !PatchResourceV1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1NamespacedResourceClaimStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1NamespacedResourceClaimStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1NamespacedResourceClaimStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1NamespacedResourceClaimStatusResponse = union(enum) {
        ok: ReadResourceV1NamespacedResourceClaimStatusResponse200,
        unauthorized: ReadResourceV1NamespacedResourceClaimStatusResponse401,
        default_response: ReadResourceV1NamespacedResourceClaimStatusResponseDefault,
    };

    pub const readResourceV1NamespacedResourceClaimStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified ResourceClaim
    pub fn readResourceV1NamespacedResourceClaimStatus(self: *Client, name: []const u8, namespace: []const u8, options: readResourceV1NamespacedResourceClaimStatusOptions) !ReadResourceV1NamespacedResourceClaimStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1NamespacedResourceClaimStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1NamespacedResourceClaimStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1NamespacedResourceClaimStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1NamespacedResourceClaimStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1NamespacedResourceClaimStatusResponse = union(enum) {
        ok: ReplaceResourceV1NamespacedResourceClaimStatusResponse200,
        created: ReplaceResourceV1NamespacedResourceClaimStatusResponse201,
        unauthorized: ReplaceResourceV1NamespacedResourceClaimStatusResponse401,
        default_response: ReplaceResourceV1NamespacedResourceClaimStatusResponseDefault,
    };

    pub const replaceResourceV1NamespacedResourceClaimStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified ResourceClaim
    pub fn replaceResourceV1NamespacedResourceClaimStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.resource.v1.ResourceClaim, options: replaceResourceV1NamespacedResourceClaimStatusOptions) !ReplaceResourceV1NamespacedResourceClaimStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1NamespacedResourceClaimStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1NamespacedResourceClaimStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1NamespacedResourceClaimStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1NamespacedResourceClaimStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1NamespacedResourceClaimStatusResponse = union(enum) {
        ok: PatchResourceV1NamespacedResourceClaimStatusResponse200,
        created: PatchResourceV1NamespacedResourceClaimStatusResponse201,
        unauthorized: PatchResourceV1NamespacedResourceClaimStatusResponse401,
        default_response: PatchResourceV1NamespacedResourceClaimStatusResponseDefault,
    };

    pub const patchResourceV1NamespacedResourceClaimStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified ResourceClaim
    pub fn patchResourceV1NamespacedResourceClaimStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1NamespacedResourceClaimStatusOptions) !PatchResourceV1NamespacedResourceClaimStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimTemplateList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: ListResourceV1NamespacedResourceClaimTemplateResponse200,
        unauthorized: ListResourceV1NamespacedResourceClaimTemplateResponse401,
        default_response: ListResourceV1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const listResourceV1NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceClaimTemplate
    pub fn listResourceV1NamespacedResourceClaimTemplate(self: *Client, namespace: []const u8, options: listResourceV1NamespacedResourceClaimTemplateOptions) !ListResourceV1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimTemplateList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1NamespacedResourceClaimTemplate(self: *Client, namespace: []const u8, options: listResourceV1NamespacedResourceClaimTemplateOptions) !WatchStream(root.io.k8s.api.resource.v1.ResourceClaimTemplate) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/resourceclaimtemplates");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1.ResourceClaimTemplate){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1NamespacedResourceClaimTemplateResponse201 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1NamespacedResourceClaimTemplateResponse202 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: CreateResourceV1NamespacedResourceClaimTemplateResponse200,
        created: CreateResourceV1NamespacedResourceClaimTemplateResponse201,
        accepted: CreateResourceV1NamespacedResourceClaimTemplateResponse202,
        unauthorized: CreateResourceV1NamespacedResourceClaimTemplateResponse401,
        default_response: CreateResourceV1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const createResourceV1NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ResourceClaimTemplate
    pub fn createResourceV1NamespacedResourceClaimTemplate(self: *Client, namespace: []const u8, body: root.io.k8s.api.resource.v1.ResourceClaimTemplate, options: createResourceV1NamespacedResourceClaimTemplateOptions) !CreateResourceV1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1CollectionNamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionNamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionNamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionNamespacedResourceClaimTemplateResponse = union(enum) {
        ok: DeleteResourceV1CollectionNamespacedResourceClaimTemplateResponse200,
        unauthorized: DeleteResourceV1CollectionNamespacedResourceClaimTemplateResponse401,
        default_response: DeleteResourceV1CollectionNamespacedResourceClaimTemplateResponseDefault,
    };

    pub const deleteResourceV1CollectionNamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ResourceClaimTemplate
    pub fn deleteResourceV1CollectionNamespacedResourceClaimTemplate(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1CollectionNamespacedResourceClaimTemplateOptions) !DeleteResourceV1CollectionNamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: ReadResourceV1NamespacedResourceClaimTemplateResponse200,
        unauthorized: ReadResourceV1NamespacedResourceClaimTemplateResponse401,
        default_response: ReadResourceV1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const readResourceV1NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ResourceClaimTemplate
    pub fn readResourceV1NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, options: readResourceV1NamespacedResourceClaimTemplateOptions) !ReadResourceV1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1NamespacedResourceClaimTemplateResponse201 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: ReplaceResourceV1NamespacedResourceClaimTemplateResponse200,
        created: ReplaceResourceV1NamespacedResourceClaimTemplateResponse201,
        unauthorized: ReplaceResourceV1NamespacedResourceClaimTemplateResponse401,
        default_response: ReplaceResourceV1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const replaceResourceV1NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ResourceClaimTemplate
    pub fn replaceResourceV1NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.resource.v1.ResourceClaimTemplate, options: replaceResourceV1NamespacedResourceClaimTemplateOptions) !ReplaceResourceV1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1NamespacedResourceClaimTemplateResponse202 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: DeleteResourceV1NamespacedResourceClaimTemplateResponse200,
        accepted: DeleteResourceV1NamespacedResourceClaimTemplateResponse202,
        unauthorized: DeleteResourceV1NamespacedResourceClaimTemplateResponse401,
        default_response: DeleteResourceV1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const deleteResourceV1NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ResourceClaimTemplate
    pub fn deleteResourceV1NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1NamespacedResourceClaimTemplateOptions) !DeleteResourceV1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1NamespacedResourceClaimTemplateResponse201 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: PatchResourceV1NamespacedResourceClaimTemplateResponse200,
        created: PatchResourceV1NamespacedResourceClaimTemplateResponse201,
        unauthorized: PatchResourceV1NamespacedResourceClaimTemplateResponse401,
        default_response: PatchResourceV1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const patchResourceV1NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ResourceClaimTemplate
    pub fn patchResourceV1NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1NamespacedResourceClaimTemplateOptions) !PatchResourceV1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1ResourceClaimForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1ResourceClaimForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1ResourceClaimForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1ResourceClaimForAllNamespacesResponse = union(enum) {
        ok: ListResourceV1ResourceClaimForAllNamespacesResponse200,
        unauthorized: ListResourceV1ResourceClaimForAllNamespacesResponse401,
        default_response: ListResourceV1ResourceClaimForAllNamespacesResponseDefault,
    };

    pub const listResourceV1ResourceClaimForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceClaim
    pub fn listResourceV1ResourceClaimForAllNamespaces(self: *Client, options: listResourceV1ResourceClaimForAllNamespacesOptions) !ListResourceV1ResourceClaimForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1ResourceClaimForAllNamespaces(self: *Client, options: listResourceV1ResourceClaimForAllNamespacesOptions) !WatchStream(root.io.k8s.api.resource.v1.ResourceClaim) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1/resourceclaims");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1.ResourceClaim){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListResourceV1ResourceClaimTemplateForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceClaimTemplateList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1ResourceClaimTemplateForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1ResourceClaimTemplateForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1ResourceClaimTemplateForAllNamespacesResponse = union(enum) {
        ok: ListResourceV1ResourceClaimTemplateForAllNamespacesResponse200,
        unauthorized: ListResourceV1ResourceClaimTemplateForAllNamespacesResponse401,
        default_response: ListResourceV1ResourceClaimTemplateForAllNamespacesResponseDefault,
    };

    pub const listResourceV1ResourceClaimTemplateForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceClaimTemplate
    pub fn listResourceV1ResourceClaimTemplateForAllNamespaces(self: *Client, options: listResourceV1ResourceClaimTemplateForAllNamespacesOptions) !ListResourceV1ResourceClaimTemplateForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceClaimTemplateList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1ResourceClaimTemplateForAllNamespaces(self: *Client, options: listResourceV1ResourceClaimTemplateForAllNamespacesOptions) !WatchStream(root.io.k8s.api.resource.v1.ResourceClaimTemplate) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1/resourceclaimtemplates");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1.ResourceClaimTemplate){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListResourceV1ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceSliceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1ResourceSliceResponse = union(enum) {
        ok: ListResourceV1ResourceSliceResponse200,
        unauthorized: ListResourceV1ResourceSliceResponse401,
        default_response: ListResourceV1ResourceSliceResponseDefault,
    };

    pub const listResourceV1ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceSlice
    pub fn listResourceV1ResourceSlice(self: *Client, options: listResourceV1ResourceSliceOptions) !ListResourceV1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/resourceslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceSliceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1ResourceSlice(self: *Client, options: listResourceV1ResourceSliceOptions) !WatchStream(root.io.k8s.api.resource.v1.ResourceSlice) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1/resourceslices");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1.ResourceSlice){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1ResourceSliceResponse201 = struct {
        body: root.io.k8s.api.resource.v1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1ResourceSliceResponse202 = struct {
        body: root.io.k8s.api.resource.v1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1ResourceSliceResponse = union(enum) {
        ok: CreateResourceV1ResourceSliceResponse200,
        created: CreateResourceV1ResourceSliceResponse201,
        accepted: CreateResourceV1ResourceSliceResponse202,
        unauthorized: CreateResourceV1ResourceSliceResponse401,
        default_response: CreateResourceV1ResourceSliceResponseDefault,
    };

    pub const createResourceV1ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ResourceSlice
    pub fn createResourceV1ResourceSlice(self: *Client, body: root.io.k8s.api.resource.v1.ResourceSlice, options: createResourceV1ResourceSliceOptions) !CreateResourceV1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/resourceslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1CollectionResourceSliceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1CollectionResourceSliceResponse = union(enum) {
        ok: DeleteResourceV1CollectionResourceSliceResponse200,
        unauthorized: DeleteResourceV1CollectionResourceSliceResponse401,
        default_response: DeleteResourceV1CollectionResourceSliceResponseDefault,
    };

    pub const deleteResourceV1CollectionResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ResourceSlice
    pub fn deleteResourceV1CollectionResourceSlice(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1CollectionResourceSliceOptions) !DeleteResourceV1CollectionResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/resourceslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1ResourceSliceResponse = union(enum) {
        ok: ReadResourceV1ResourceSliceResponse200,
        unauthorized: ReadResourceV1ResourceSliceResponse401,
        default_response: ReadResourceV1ResourceSliceResponseDefault,
    };

    pub const readResourceV1ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ResourceSlice
    pub fn readResourceV1ResourceSlice(self: *Client, name: []const u8, options: readResourceV1ResourceSliceOptions) !ReadResourceV1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1ResourceSliceResponse201 = struct {
        body: root.io.k8s.api.resource.v1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1ResourceSliceResponse = union(enum) {
        ok: ReplaceResourceV1ResourceSliceResponse200,
        created: ReplaceResourceV1ResourceSliceResponse201,
        unauthorized: ReplaceResourceV1ResourceSliceResponse401,
        default_response: ReplaceResourceV1ResourceSliceResponseDefault,
    };

    pub const replaceResourceV1ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ResourceSlice
    pub fn replaceResourceV1ResourceSlice(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1.ResourceSlice, options: replaceResourceV1ResourceSliceOptions) !ReplaceResourceV1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1ResourceSliceResponse202 = struct {
        body: root.io.k8s.api.resource.v1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1ResourceSliceResponse = union(enum) {
        ok: DeleteResourceV1ResourceSliceResponse200,
        accepted: DeleteResourceV1ResourceSliceResponse202,
        unauthorized: DeleteResourceV1ResourceSliceResponse401,
        default_response: DeleteResourceV1ResourceSliceResponseDefault,
    };

    pub const deleteResourceV1ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ResourceSlice
    pub fn deleteResourceV1ResourceSlice(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1ResourceSliceOptions) !DeleteResourceV1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1ResourceSliceResponse201 = struct {
        body: root.io.k8s.api.resource.v1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1ResourceSliceResponse = union(enum) {
        ok: PatchResourceV1ResourceSliceResponse200,
        created: PatchResourceV1ResourceSliceResponse201,
        unauthorized: PatchResourceV1ResourceSliceResponse401,
        default_response: PatchResourceV1ResourceSliceResponseDefault,
    };

    pub const patchResourceV1ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ResourceSlice
    pub fn patchResourceV1ResourceSlice(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1ResourceSliceOptions) !PatchResourceV1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1DeviceClassListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1DeviceClassListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1DeviceClassListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1DeviceClassListResponse = union(enum) {
        ok: WatchResourceV1DeviceClassListResponse200,
        unauthorized: WatchResourceV1DeviceClassListResponse401,
        default_response: WatchResourceV1DeviceClassListResponseDefault,
    };

    pub const watchResourceV1DeviceClassListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of DeviceClass. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1DeviceClassList(self: *Client, options: watchResourceV1DeviceClassListOptions) !WatchResourceV1DeviceClassListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/watch/deviceclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1DeviceClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1DeviceClassResponse = union(enum) {
        ok: WatchResourceV1DeviceClassResponse200,
        unauthorized: WatchResourceV1DeviceClassResponse401,
        default_response: WatchResourceV1DeviceClassResponseDefault,
    };

    pub const watchResourceV1DeviceClassOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind DeviceClass. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1DeviceClass(self: *Client, name: []const u8, options: watchResourceV1DeviceClassOptions) !WatchResourceV1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/watch/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1DeviceTaintRuleListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1DeviceTaintRuleListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1DeviceTaintRuleListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1DeviceTaintRuleListResponse = union(enum) {
        ok: WatchResourceV1DeviceTaintRuleListResponse200,
        unauthorized: WatchResourceV1DeviceTaintRuleListResponse401,
        default_response: WatchResourceV1DeviceTaintRuleListResponseDefault,
    };

    pub const watchResourceV1DeviceTaintRuleListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of DeviceTaintRule. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1DeviceTaintRuleList(self: *Client, options: watchResourceV1DeviceTaintRuleListOptions) !WatchResourceV1DeviceTaintRuleListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/watch/devicetaintrules", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1DeviceTaintRuleResponse = union(enum) {
        ok: WatchResourceV1DeviceTaintRuleResponse200,
        unauthorized: WatchResourceV1DeviceTaintRuleResponse401,
        default_response: WatchResourceV1DeviceTaintRuleResponseDefault,
    };

    pub const watchResourceV1DeviceTaintRuleOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind DeviceTaintRule. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1DeviceTaintRule(self: *Client, name: []const u8, options: watchResourceV1DeviceTaintRuleOptions) !WatchResourceV1DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/watch/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1NamespacedResourceClaimListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1NamespacedResourceClaimListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1NamespacedResourceClaimListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1NamespacedResourceClaimListResponse = union(enum) {
        ok: WatchResourceV1NamespacedResourceClaimListResponse200,
        unauthorized: WatchResourceV1NamespacedResourceClaimListResponse401,
        default_response: WatchResourceV1NamespacedResourceClaimListResponseDefault,
    };

    pub const watchResourceV1NamespacedResourceClaimListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceClaim. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1NamespacedResourceClaimList(self: *Client, namespace: []const u8, options: watchResourceV1NamespacedResourceClaimListOptions) !WatchResourceV1NamespacedResourceClaimListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1NamespacedResourceClaimResponse = union(enum) {
        ok: WatchResourceV1NamespacedResourceClaimResponse200,
        unauthorized: WatchResourceV1NamespacedResourceClaimResponse401,
        default_response: WatchResourceV1NamespacedResourceClaimResponseDefault,
    };

    pub const watchResourceV1NamespacedResourceClaimOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ResourceClaim. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, options: watchResourceV1NamespacedResourceClaimOptions) !WatchResourceV1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1NamespacedResourceClaimTemplateListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1NamespacedResourceClaimTemplateListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1NamespacedResourceClaimTemplateListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1NamespacedResourceClaimTemplateListResponse = union(enum) {
        ok: WatchResourceV1NamespacedResourceClaimTemplateListResponse200,
        unauthorized: WatchResourceV1NamespacedResourceClaimTemplateListResponse401,
        default_response: WatchResourceV1NamespacedResourceClaimTemplateListResponseDefault,
    };

    pub const watchResourceV1NamespacedResourceClaimTemplateListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceClaimTemplate. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1NamespacedResourceClaimTemplateList(self: *Client, namespace: []const u8, options: watchResourceV1NamespacedResourceClaimTemplateListOptions) !WatchResourceV1NamespacedResourceClaimTemplateListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: WatchResourceV1NamespacedResourceClaimTemplateResponse200,
        unauthorized: WatchResourceV1NamespacedResourceClaimTemplateResponse401,
        default_response: WatchResourceV1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const watchResourceV1NamespacedResourceClaimTemplateOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ResourceClaimTemplate. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, options: watchResourceV1NamespacedResourceClaimTemplateOptions) !WatchResourceV1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1ResourceClaimListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1ResourceClaimListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1ResourceClaimListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1ResourceClaimListForAllNamespacesResponse = union(enum) {
        ok: WatchResourceV1ResourceClaimListForAllNamespacesResponse200,
        unauthorized: WatchResourceV1ResourceClaimListForAllNamespacesResponse401,
        default_response: WatchResourceV1ResourceClaimListForAllNamespacesResponseDefault,
    };

    pub const watchResourceV1ResourceClaimListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceClaim. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1ResourceClaimListForAllNamespaces(self: *Client, options: watchResourceV1ResourceClaimListForAllNamespacesOptions) !WatchResourceV1ResourceClaimListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/watch/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1ResourceClaimTemplateListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1ResourceClaimTemplateListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1ResourceClaimTemplateListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1ResourceClaimTemplateListForAllNamespacesResponse = union(enum) {
        ok: WatchResourceV1ResourceClaimTemplateListForAllNamespacesResponse200,
        unauthorized: WatchResourceV1ResourceClaimTemplateListForAllNamespacesResponse401,
        default_response: WatchResourceV1ResourceClaimTemplateListForAllNamespacesResponseDefault,
    };

    pub const watchResourceV1ResourceClaimTemplateListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceClaimTemplate. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1ResourceClaimTemplateListForAllNamespaces(self: *Client, options: watchResourceV1ResourceClaimTemplateListForAllNamespacesOptions) !WatchResourceV1ResourceClaimTemplateListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/watch/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1ResourceSliceListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1ResourceSliceListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1ResourceSliceListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1ResourceSliceListResponse = union(enum) {
        ok: WatchResourceV1ResourceSliceListResponse200,
        unauthorized: WatchResourceV1ResourceSliceListResponse401,
        default_response: WatchResourceV1ResourceSliceListResponseDefault,
    };

    pub const watchResourceV1ResourceSliceListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceSlice. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1ResourceSliceList(self: *Client, options: watchResourceV1ResourceSliceListOptions) !WatchResourceV1ResourceSliceListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/watch/resourceslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1ResourceSliceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1ResourceSliceResponse = union(enum) {
        ok: WatchResourceV1ResourceSliceResponse200,
        unauthorized: WatchResourceV1ResourceSliceResponse401,
        default_response: WatchResourceV1ResourceSliceResponseDefault,
    };

    pub const watchResourceV1ResourceSliceOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ResourceSlice. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1ResourceSlice(self: *Client, name: []const u8, options: watchResourceV1ResourceSliceOptions) !WatchResourceV1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1/watch/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetResourceV1alpha3APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceV1alpha3APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceV1alpha3APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceV1alpha3APIResourcesResponse = union(enum) {
        ok: GetResourceV1alpha3APIResourcesResponse200,
        unauthorized: GetResourceV1alpha3APIResourcesResponse401,
        default_response: GetResourceV1alpha3APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getResourceV1alpha3APIResources(self: *Client) !GetResourceV1alpha3APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1alpha3DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRuleList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1alpha3DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1alpha3DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1alpha3DeviceTaintRuleResponse = union(enum) {
        ok: ListResourceV1alpha3DeviceTaintRuleResponse200,
        unauthorized: ListResourceV1alpha3DeviceTaintRuleResponse401,
        default_response: ListResourceV1alpha3DeviceTaintRuleResponseDefault,
    };

    pub const listResourceV1alpha3DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind DeviceTaintRule
    pub fn listResourceV1alpha3DeviceTaintRule(self: *Client, options: listResourceV1alpha3DeviceTaintRuleOptions) !ListResourceV1alpha3DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/devicetaintrules", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRuleList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1alpha3DeviceTaintRule(self: *Client, options: listResourceV1alpha3DeviceTaintRuleOptions) !WatchStream(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1alpha3/devicetaintrules");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1alpha3DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1alpha3DeviceTaintRuleResponse201 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1alpha3DeviceTaintRuleResponse202 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1alpha3DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1alpha3DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1alpha3DeviceTaintRuleResponse = union(enum) {
        ok: CreateResourceV1alpha3DeviceTaintRuleResponse200,
        created: CreateResourceV1alpha3DeviceTaintRuleResponse201,
        accepted: CreateResourceV1alpha3DeviceTaintRuleResponse202,
        unauthorized: CreateResourceV1alpha3DeviceTaintRuleResponse401,
        default_response: CreateResourceV1alpha3DeviceTaintRuleResponseDefault,
    };

    pub const createResourceV1alpha3DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a DeviceTaintRule
    pub fn createResourceV1alpha3DeviceTaintRule(self: *Client, body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, options: createResourceV1alpha3DeviceTaintRuleOptions) !CreateResourceV1alpha3DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/devicetaintrules", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1alpha3CollectionDeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3CollectionDeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3CollectionDeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3CollectionDeviceTaintRuleResponse = union(enum) {
        ok: DeleteResourceV1alpha3CollectionDeviceTaintRuleResponse200,
        unauthorized: DeleteResourceV1alpha3CollectionDeviceTaintRuleResponse401,
        default_response: DeleteResourceV1alpha3CollectionDeviceTaintRuleResponseDefault,
    };

    pub const deleteResourceV1alpha3CollectionDeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of DeviceTaintRule
    pub fn deleteResourceV1alpha3CollectionDeviceTaintRule(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1alpha3CollectionDeviceTaintRuleOptions) !DeleteResourceV1alpha3CollectionDeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/devicetaintrules", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1alpha3DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1alpha3DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1alpha3DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1alpha3DeviceTaintRuleResponse = union(enum) {
        ok: ReadResourceV1alpha3DeviceTaintRuleResponse200,
        unauthorized: ReadResourceV1alpha3DeviceTaintRuleResponse401,
        default_response: ReadResourceV1alpha3DeviceTaintRuleResponseDefault,
    };

    pub const readResourceV1alpha3DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified DeviceTaintRule
    pub fn readResourceV1alpha3DeviceTaintRule(self: *Client, name: []const u8, options: readResourceV1alpha3DeviceTaintRuleOptions) !ReadResourceV1alpha3DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1alpha3DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3DeviceTaintRuleResponse201 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3DeviceTaintRuleResponse = union(enum) {
        ok: ReplaceResourceV1alpha3DeviceTaintRuleResponse200,
        created: ReplaceResourceV1alpha3DeviceTaintRuleResponse201,
        unauthorized: ReplaceResourceV1alpha3DeviceTaintRuleResponse401,
        default_response: ReplaceResourceV1alpha3DeviceTaintRuleResponseDefault,
    };

    pub const replaceResourceV1alpha3DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified DeviceTaintRule
    pub fn replaceResourceV1alpha3DeviceTaintRule(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, options: replaceResourceV1alpha3DeviceTaintRuleOptions) !ReplaceResourceV1alpha3DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1alpha3DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3DeviceTaintRuleResponse202 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3DeviceTaintRuleResponse = union(enum) {
        ok: DeleteResourceV1alpha3DeviceTaintRuleResponse200,
        accepted: DeleteResourceV1alpha3DeviceTaintRuleResponse202,
        unauthorized: DeleteResourceV1alpha3DeviceTaintRuleResponse401,
        default_response: DeleteResourceV1alpha3DeviceTaintRuleResponseDefault,
    };

    pub const deleteResourceV1alpha3DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a DeviceTaintRule
    pub fn deleteResourceV1alpha3DeviceTaintRule(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1alpha3DeviceTaintRuleOptions) !DeleteResourceV1alpha3DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1alpha3DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3DeviceTaintRuleResponse201 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3DeviceTaintRuleResponse = union(enum) {
        ok: PatchResourceV1alpha3DeviceTaintRuleResponse200,
        created: PatchResourceV1alpha3DeviceTaintRuleResponse201,
        unauthorized: PatchResourceV1alpha3DeviceTaintRuleResponse401,
        default_response: PatchResourceV1alpha3DeviceTaintRuleResponseDefault,
    };

    pub const patchResourceV1alpha3DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified DeviceTaintRule
    pub fn patchResourceV1alpha3DeviceTaintRule(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1alpha3DeviceTaintRuleOptions) !PatchResourceV1alpha3DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1alpha3DeviceTaintRuleStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1alpha3DeviceTaintRuleStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1alpha3DeviceTaintRuleStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1alpha3DeviceTaintRuleStatusResponse = union(enum) {
        ok: ReadResourceV1alpha3DeviceTaintRuleStatusResponse200,
        unauthorized: ReadResourceV1alpha3DeviceTaintRuleStatusResponse401,
        default_response: ReadResourceV1alpha3DeviceTaintRuleStatusResponseDefault,
    };

    pub const readResourceV1alpha3DeviceTaintRuleStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified DeviceTaintRule
    pub fn readResourceV1alpha3DeviceTaintRuleStatus(self: *Client, name: []const u8, options: readResourceV1alpha3DeviceTaintRuleStatusOptions) !ReadResourceV1alpha3DeviceTaintRuleStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1alpha3DeviceTaintRuleStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3DeviceTaintRuleStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3DeviceTaintRuleStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3DeviceTaintRuleStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3DeviceTaintRuleStatusResponse = union(enum) {
        ok: ReplaceResourceV1alpha3DeviceTaintRuleStatusResponse200,
        created: ReplaceResourceV1alpha3DeviceTaintRuleStatusResponse201,
        unauthorized: ReplaceResourceV1alpha3DeviceTaintRuleStatusResponse401,
        default_response: ReplaceResourceV1alpha3DeviceTaintRuleStatusResponseDefault,
    };

    pub const replaceResourceV1alpha3DeviceTaintRuleStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified DeviceTaintRule
    pub fn replaceResourceV1alpha3DeviceTaintRuleStatus(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, options: replaceResourceV1alpha3DeviceTaintRuleStatusOptions) !ReplaceResourceV1alpha3DeviceTaintRuleStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1alpha3DeviceTaintRuleStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3DeviceTaintRuleStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1alpha3.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3DeviceTaintRuleStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3DeviceTaintRuleStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3DeviceTaintRuleStatusResponse = union(enum) {
        ok: PatchResourceV1alpha3DeviceTaintRuleStatusResponse200,
        created: PatchResourceV1alpha3DeviceTaintRuleStatusResponse201,
        unauthorized: PatchResourceV1alpha3DeviceTaintRuleStatusResponse401,
        default_response: PatchResourceV1alpha3DeviceTaintRuleStatusResponseDefault,
    };

    pub const patchResourceV1alpha3DeviceTaintRuleStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified DeviceTaintRule
    pub fn patchResourceV1alpha3DeviceTaintRuleStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1alpha3DeviceTaintRuleStatusOptions) !PatchResourceV1alpha3DeviceTaintRuleStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1alpha3ResourcePoolStatusRequestResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequestList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1alpha3ResourcePoolStatusRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1alpha3ResourcePoolStatusRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1alpha3ResourcePoolStatusRequestResponse = union(enum) {
        ok: ListResourceV1alpha3ResourcePoolStatusRequestResponse200,
        unauthorized: ListResourceV1alpha3ResourcePoolStatusRequestResponse401,
        default_response: ListResourceV1alpha3ResourcePoolStatusRequestResponseDefault,
    };

    pub const listResourceV1alpha3ResourcePoolStatusRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourcePoolStatusRequest
    pub fn listResourceV1alpha3ResourcePoolStatusRequest(self: *Client, options: listResourceV1alpha3ResourcePoolStatusRequestOptions) !ListResourceV1alpha3ResourcePoolStatusRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/resourcepoolstatusrequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequestList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1alpha3ResourcePoolStatusRequest(self: *Client, options: listResourceV1alpha3ResourcePoolStatusRequestOptions) !WatchStream(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1alpha3/resourcepoolstatusrequests");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1alpha3ResourcePoolStatusRequestResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1alpha3ResourcePoolStatusRequestResponse201 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1alpha3ResourcePoolStatusRequestResponse202 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1alpha3ResourcePoolStatusRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1alpha3ResourcePoolStatusRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1alpha3ResourcePoolStatusRequestResponse = union(enum) {
        ok: CreateResourceV1alpha3ResourcePoolStatusRequestResponse200,
        created: CreateResourceV1alpha3ResourcePoolStatusRequestResponse201,
        accepted: CreateResourceV1alpha3ResourcePoolStatusRequestResponse202,
        unauthorized: CreateResourceV1alpha3ResourcePoolStatusRequestResponse401,
        default_response: CreateResourceV1alpha3ResourcePoolStatusRequestResponseDefault,
    };

    pub const createResourceV1alpha3ResourcePoolStatusRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ResourcePoolStatusRequest
    pub fn createResourceV1alpha3ResourcePoolStatusRequest(self: *Client, body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, options: createResourceV1alpha3ResourcePoolStatusRequestOptions) !CreateResourceV1alpha3ResourcePoolStatusRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/resourcepoolstatusrequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1alpha3CollectionResourcePoolStatusRequestResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3CollectionResourcePoolStatusRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3CollectionResourcePoolStatusRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3CollectionResourcePoolStatusRequestResponse = union(enum) {
        ok: DeleteResourceV1alpha3CollectionResourcePoolStatusRequestResponse200,
        unauthorized: DeleteResourceV1alpha3CollectionResourcePoolStatusRequestResponse401,
        default_response: DeleteResourceV1alpha3CollectionResourcePoolStatusRequestResponseDefault,
    };

    pub const deleteResourceV1alpha3CollectionResourcePoolStatusRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ResourcePoolStatusRequest
    pub fn deleteResourceV1alpha3CollectionResourcePoolStatusRequest(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1alpha3CollectionResourcePoolStatusRequestOptions) !DeleteResourceV1alpha3CollectionResourcePoolStatusRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/resourcepoolstatusrequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1alpha3ResourcePoolStatusRequestResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1alpha3ResourcePoolStatusRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1alpha3ResourcePoolStatusRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1alpha3ResourcePoolStatusRequestResponse = union(enum) {
        ok: ReadResourceV1alpha3ResourcePoolStatusRequestResponse200,
        unauthorized: ReadResourceV1alpha3ResourcePoolStatusRequestResponse401,
        default_response: ReadResourceV1alpha3ResourcePoolStatusRequestResponseDefault,
    };

    pub const readResourceV1alpha3ResourcePoolStatusRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ResourcePoolStatusRequest
    pub fn readResourceV1alpha3ResourcePoolStatusRequest(self: *Client, name: []const u8, options: readResourceV1alpha3ResourcePoolStatusRequestOptions) !ReadResourceV1alpha3ResourcePoolStatusRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/resourcepoolstatusrequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1alpha3ResourcePoolStatusRequestResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3ResourcePoolStatusRequestResponse201 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3ResourcePoolStatusRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3ResourcePoolStatusRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3ResourcePoolStatusRequestResponse = union(enum) {
        ok: ReplaceResourceV1alpha3ResourcePoolStatusRequestResponse200,
        created: ReplaceResourceV1alpha3ResourcePoolStatusRequestResponse201,
        unauthorized: ReplaceResourceV1alpha3ResourcePoolStatusRequestResponse401,
        default_response: ReplaceResourceV1alpha3ResourcePoolStatusRequestResponseDefault,
    };

    pub const replaceResourceV1alpha3ResourcePoolStatusRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ResourcePoolStatusRequest
    pub fn replaceResourceV1alpha3ResourcePoolStatusRequest(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, options: replaceResourceV1alpha3ResourcePoolStatusRequestOptions) !ReplaceResourceV1alpha3ResourcePoolStatusRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/resourcepoolstatusrequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1alpha3ResourcePoolStatusRequestResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3ResourcePoolStatusRequestResponse202 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3ResourcePoolStatusRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3ResourcePoolStatusRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1alpha3ResourcePoolStatusRequestResponse = union(enum) {
        ok: DeleteResourceV1alpha3ResourcePoolStatusRequestResponse200,
        accepted: DeleteResourceV1alpha3ResourcePoolStatusRequestResponse202,
        unauthorized: DeleteResourceV1alpha3ResourcePoolStatusRequestResponse401,
        default_response: DeleteResourceV1alpha3ResourcePoolStatusRequestResponseDefault,
    };

    pub const deleteResourceV1alpha3ResourcePoolStatusRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ResourcePoolStatusRequest
    pub fn deleteResourceV1alpha3ResourcePoolStatusRequest(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1alpha3ResourcePoolStatusRequestOptions) !DeleteResourceV1alpha3ResourcePoolStatusRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/resourcepoolstatusrequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1alpha3ResourcePoolStatusRequestResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3ResourcePoolStatusRequestResponse201 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3ResourcePoolStatusRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3ResourcePoolStatusRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3ResourcePoolStatusRequestResponse = union(enum) {
        ok: PatchResourceV1alpha3ResourcePoolStatusRequestResponse200,
        created: PatchResourceV1alpha3ResourcePoolStatusRequestResponse201,
        unauthorized: PatchResourceV1alpha3ResourcePoolStatusRequestResponse401,
        default_response: PatchResourceV1alpha3ResourcePoolStatusRequestResponseDefault,
    };

    pub const patchResourceV1alpha3ResourcePoolStatusRequestOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ResourcePoolStatusRequest
    pub fn patchResourceV1alpha3ResourcePoolStatusRequest(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1alpha3ResourcePoolStatusRequestOptions) !PatchResourceV1alpha3ResourcePoolStatusRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/resourcepoolstatusrequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1alpha3ResourcePoolStatusRequestStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1alpha3ResourcePoolStatusRequestStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1alpha3ResourcePoolStatusRequestStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1alpha3ResourcePoolStatusRequestStatusResponse = union(enum) {
        ok: ReadResourceV1alpha3ResourcePoolStatusRequestStatusResponse200,
        unauthorized: ReadResourceV1alpha3ResourcePoolStatusRequestStatusResponse401,
        default_response: ReadResourceV1alpha3ResourcePoolStatusRequestStatusResponseDefault,
    };

    pub const readResourceV1alpha3ResourcePoolStatusRequestStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified ResourcePoolStatusRequest
    pub fn readResourceV1alpha3ResourcePoolStatusRequestStatus(self: *Client, name: []const u8, options: readResourceV1alpha3ResourcePoolStatusRequestStatusOptions) !ReadResourceV1alpha3ResourcePoolStatusRequestStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/resourcepoolstatusrequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1alpha3ResourcePoolStatusRequestStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3ResourcePoolStatusRequestStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3ResourcePoolStatusRequestStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3ResourcePoolStatusRequestStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1alpha3ResourcePoolStatusRequestStatusResponse = union(enum) {
        ok: ReplaceResourceV1alpha3ResourcePoolStatusRequestStatusResponse200,
        created: ReplaceResourceV1alpha3ResourcePoolStatusRequestStatusResponse201,
        unauthorized: ReplaceResourceV1alpha3ResourcePoolStatusRequestStatusResponse401,
        default_response: ReplaceResourceV1alpha3ResourcePoolStatusRequestStatusResponseDefault,
    };

    pub const replaceResourceV1alpha3ResourcePoolStatusRequestStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified ResourcePoolStatusRequest
    pub fn replaceResourceV1alpha3ResourcePoolStatusRequestStatus(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, options: replaceResourceV1alpha3ResourcePoolStatusRequestStatusOptions) !ReplaceResourceV1alpha3ResourcePoolStatusRequestStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/resourcepoolstatusrequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1alpha3ResourcePoolStatusRequestStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3ResourcePoolStatusRequestStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3ResourcePoolStatusRequestStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3ResourcePoolStatusRequestStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1alpha3ResourcePoolStatusRequestStatusResponse = union(enum) {
        ok: PatchResourceV1alpha3ResourcePoolStatusRequestStatusResponse200,
        created: PatchResourceV1alpha3ResourcePoolStatusRequestStatusResponse201,
        unauthorized: PatchResourceV1alpha3ResourcePoolStatusRequestStatusResponse401,
        default_response: PatchResourceV1alpha3ResourcePoolStatusRequestStatusResponseDefault,
    };

    pub const patchResourceV1alpha3ResourcePoolStatusRequestStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified ResourcePoolStatusRequest
    pub fn patchResourceV1alpha3ResourcePoolStatusRequestStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1alpha3ResourcePoolStatusRequestStatusOptions) !PatchResourceV1alpha3ResourcePoolStatusRequestStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/resourcepoolstatusrequests/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1alpha3.ResourcePoolStatusRequest, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1alpha3DeviceTaintRuleListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1alpha3DeviceTaintRuleListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1alpha3DeviceTaintRuleListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1alpha3DeviceTaintRuleListResponse = union(enum) {
        ok: WatchResourceV1alpha3DeviceTaintRuleListResponse200,
        unauthorized: WatchResourceV1alpha3DeviceTaintRuleListResponse401,
        default_response: WatchResourceV1alpha3DeviceTaintRuleListResponseDefault,
    };

    pub const watchResourceV1alpha3DeviceTaintRuleListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of DeviceTaintRule. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1alpha3DeviceTaintRuleList(self: *Client, options: watchResourceV1alpha3DeviceTaintRuleListOptions) !WatchResourceV1alpha3DeviceTaintRuleListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/watch/devicetaintrules", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1alpha3DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1alpha3DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1alpha3DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1alpha3DeviceTaintRuleResponse = union(enum) {
        ok: WatchResourceV1alpha3DeviceTaintRuleResponse200,
        unauthorized: WatchResourceV1alpha3DeviceTaintRuleResponse401,
        default_response: WatchResourceV1alpha3DeviceTaintRuleResponseDefault,
    };

    pub const watchResourceV1alpha3DeviceTaintRuleOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind DeviceTaintRule. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1alpha3DeviceTaintRule(self: *Client, name: []const u8, options: watchResourceV1alpha3DeviceTaintRuleOptions) !WatchResourceV1alpha3DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/watch/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1alpha3ResourcePoolStatusRequestListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1alpha3ResourcePoolStatusRequestListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1alpha3ResourcePoolStatusRequestListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1alpha3ResourcePoolStatusRequestListResponse = union(enum) {
        ok: WatchResourceV1alpha3ResourcePoolStatusRequestListResponse200,
        unauthorized: WatchResourceV1alpha3ResourcePoolStatusRequestListResponse401,
        default_response: WatchResourceV1alpha3ResourcePoolStatusRequestListResponseDefault,
    };

    pub const watchResourceV1alpha3ResourcePoolStatusRequestListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourcePoolStatusRequest. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1alpha3ResourcePoolStatusRequestList(self: *Client, options: watchResourceV1alpha3ResourcePoolStatusRequestListOptions) !WatchResourceV1alpha3ResourcePoolStatusRequestListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/watch/resourcepoolstatusrequests", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1alpha3ResourcePoolStatusRequestResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1alpha3ResourcePoolStatusRequestResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1alpha3ResourcePoolStatusRequestResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1alpha3ResourcePoolStatusRequestResponse = union(enum) {
        ok: WatchResourceV1alpha3ResourcePoolStatusRequestResponse200,
        unauthorized: WatchResourceV1alpha3ResourcePoolStatusRequestResponse401,
        default_response: WatchResourceV1alpha3ResourcePoolStatusRequestResponseDefault,
    };

    pub const watchResourceV1alpha3ResourcePoolStatusRequestOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ResourcePoolStatusRequest. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1alpha3ResourcePoolStatusRequest(self: *Client, name: []const u8, options: watchResourceV1alpha3ResourcePoolStatusRequestOptions) !WatchResourceV1alpha3ResourcePoolStatusRequestResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1alpha3/watch/resourcepoolstatusrequests/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetResourceV1beta1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceV1beta1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceV1beta1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceV1beta1APIResourcesResponse = union(enum) {
        ok: GetResourceV1beta1APIResourcesResponse200,
        unauthorized: GetResourceV1beta1APIResourcesResponse401,
        default_response: GetResourceV1beta1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getResourceV1beta1APIResources(self: *Client) !GetResourceV1beta1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1beta1DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.DeviceClassList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1DeviceClassResponse = union(enum) {
        ok: ListResourceV1beta1DeviceClassResponse200,
        unauthorized: ListResourceV1beta1DeviceClassResponse401,
        default_response: ListResourceV1beta1DeviceClassResponseDefault,
    };

    pub const listResourceV1beta1DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind DeviceClass
    pub fn listResourceV1beta1DeviceClass(self: *Client, options: listResourceV1beta1DeviceClassOptions) !ListResourceV1beta1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/deviceclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.DeviceClassList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1beta1DeviceClass(self: *Client, options: listResourceV1beta1DeviceClassOptions) !WatchStream(root.io.k8s.api.resource.v1beta1.DeviceClass) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1beta1/deviceclasses");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1beta1.DeviceClass){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1beta1DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1DeviceClassResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1DeviceClassResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1DeviceClassResponse = union(enum) {
        ok: CreateResourceV1beta1DeviceClassResponse200,
        created: CreateResourceV1beta1DeviceClassResponse201,
        accepted: CreateResourceV1beta1DeviceClassResponse202,
        unauthorized: CreateResourceV1beta1DeviceClassResponse401,
        default_response: CreateResourceV1beta1DeviceClassResponseDefault,
    };

    pub const createResourceV1beta1DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a DeviceClass
    pub fn createResourceV1beta1DeviceClass(self: *Client, body: root.io.k8s.api.resource.v1beta1.DeviceClass, options: createResourceV1beta1DeviceClassOptions) !CreateResourceV1beta1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/deviceclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta1CollectionDeviceClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1CollectionDeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1CollectionDeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1CollectionDeviceClassResponse = union(enum) {
        ok: DeleteResourceV1beta1CollectionDeviceClassResponse200,
        unauthorized: DeleteResourceV1beta1CollectionDeviceClassResponse401,
        default_response: DeleteResourceV1beta1CollectionDeviceClassResponseDefault,
    };

    pub const deleteResourceV1beta1CollectionDeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of DeviceClass
    pub fn deleteResourceV1beta1CollectionDeviceClass(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta1CollectionDeviceClassOptions) !DeleteResourceV1beta1CollectionDeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/deviceclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1beta1DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1DeviceClassResponse = union(enum) {
        ok: ReadResourceV1beta1DeviceClassResponse200,
        unauthorized: ReadResourceV1beta1DeviceClassResponse401,
        default_response: ReadResourceV1beta1DeviceClassResponseDefault,
    };

    pub const readResourceV1beta1DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified DeviceClass
    pub fn readResourceV1beta1DeviceClass(self: *Client, name: []const u8, options: readResourceV1beta1DeviceClassOptions) !ReadResourceV1beta1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1beta1DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1DeviceClassResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1DeviceClassResponse = union(enum) {
        ok: ReplaceResourceV1beta1DeviceClassResponse200,
        created: ReplaceResourceV1beta1DeviceClassResponse201,
        unauthorized: ReplaceResourceV1beta1DeviceClassResponse401,
        default_response: ReplaceResourceV1beta1DeviceClassResponseDefault,
    };

    pub const replaceResourceV1beta1DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified DeviceClass
    pub fn replaceResourceV1beta1DeviceClass(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1beta1.DeviceClass, options: replaceResourceV1beta1DeviceClassOptions) !ReplaceResourceV1beta1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta1DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1DeviceClassResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1DeviceClassResponse = union(enum) {
        ok: DeleteResourceV1beta1DeviceClassResponse200,
        accepted: DeleteResourceV1beta1DeviceClassResponse202,
        unauthorized: DeleteResourceV1beta1DeviceClassResponse401,
        default_response: DeleteResourceV1beta1DeviceClassResponseDefault,
    };

    pub const deleteResourceV1beta1DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a DeviceClass
    pub fn deleteResourceV1beta1DeviceClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta1DeviceClassOptions) !DeleteResourceV1beta1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1beta1DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1DeviceClassResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1DeviceClassResponse = union(enum) {
        ok: PatchResourceV1beta1DeviceClassResponse200,
        created: PatchResourceV1beta1DeviceClassResponse201,
        unauthorized: PatchResourceV1beta1DeviceClassResponse401,
        default_response: PatchResourceV1beta1DeviceClassResponseDefault,
    };

    pub const patchResourceV1beta1DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified DeviceClass
    pub fn patchResourceV1beta1DeviceClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1beta1DeviceClassOptions) !PatchResourceV1beta1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1beta1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1NamespacedResourceClaimResponse = union(enum) {
        ok: ListResourceV1beta1NamespacedResourceClaimResponse200,
        unauthorized: ListResourceV1beta1NamespacedResourceClaimResponse401,
        default_response: ListResourceV1beta1NamespacedResourceClaimResponseDefault,
    };

    pub const listResourceV1beta1NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceClaim
    pub fn listResourceV1beta1NamespacedResourceClaim(self: *Client, namespace: []const u8, options: listResourceV1beta1NamespacedResourceClaimOptions) !ListResourceV1beta1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1beta1NamespacedResourceClaim(self: *Client, namespace: []const u8, options: listResourceV1beta1NamespacedResourceClaimOptions) !WatchStream(root.io.k8s.api.resource.v1beta1.ResourceClaim) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1beta1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/resourceclaims");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1beta1.ResourceClaim){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1beta1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1NamespacedResourceClaimResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1NamespacedResourceClaimResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1NamespacedResourceClaimResponse = union(enum) {
        ok: CreateResourceV1beta1NamespacedResourceClaimResponse200,
        created: CreateResourceV1beta1NamespacedResourceClaimResponse201,
        accepted: CreateResourceV1beta1NamespacedResourceClaimResponse202,
        unauthorized: CreateResourceV1beta1NamespacedResourceClaimResponse401,
        default_response: CreateResourceV1beta1NamespacedResourceClaimResponseDefault,
    };

    pub const createResourceV1beta1NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ResourceClaim
    pub fn createResourceV1beta1NamespacedResourceClaim(self: *Client, namespace: []const u8, body: root.io.k8s.api.resource.v1beta1.ResourceClaim, options: createResourceV1beta1NamespacedResourceClaimOptions) !CreateResourceV1beta1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta1CollectionNamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1CollectionNamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1CollectionNamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1CollectionNamespacedResourceClaimResponse = union(enum) {
        ok: DeleteResourceV1beta1CollectionNamespacedResourceClaimResponse200,
        unauthorized: DeleteResourceV1beta1CollectionNamespacedResourceClaimResponse401,
        default_response: DeleteResourceV1beta1CollectionNamespacedResourceClaimResponseDefault,
    };

    pub const deleteResourceV1beta1CollectionNamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ResourceClaim
    pub fn deleteResourceV1beta1CollectionNamespacedResourceClaim(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta1CollectionNamespacedResourceClaimOptions) !DeleteResourceV1beta1CollectionNamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1beta1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1NamespacedResourceClaimResponse = union(enum) {
        ok: ReadResourceV1beta1NamespacedResourceClaimResponse200,
        unauthorized: ReadResourceV1beta1NamespacedResourceClaimResponse401,
        default_response: ReadResourceV1beta1NamespacedResourceClaimResponseDefault,
    };

    pub const readResourceV1beta1NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ResourceClaim
    pub fn readResourceV1beta1NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, options: readResourceV1beta1NamespacedResourceClaimOptions) !ReadResourceV1beta1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1beta1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1NamespacedResourceClaimResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1NamespacedResourceClaimResponse = union(enum) {
        ok: ReplaceResourceV1beta1NamespacedResourceClaimResponse200,
        created: ReplaceResourceV1beta1NamespacedResourceClaimResponse201,
        unauthorized: ReplaceResourceV1beta1NamespacedResourceClaimResponse401,
        default_response: ReplaceResourceV1beta1NamespacedResourceClaimResponseDefault,
    };

    pub const replaceResourceV1beta1NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ResourceClaim
    pub fn replaceResourceV1beta1NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.resource.v1beta1.ResourceClaim, options: replaceResourceV1beta1NamespacedResourceClaimOptions) !ReplaceResourceV1beta1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1NamespacedResourceClaimResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1NamespacedResourceClaimResponse = union(enum) {
        ok: DeleteResourceV1beta1NamespacedResourceClaimResponse200,
        accepted: DeleteResourceV1beta1NamespacedResourceClaimResponse202,
        unauthorized: DeleteResourceV1beta1NamespacedResourceClaimResponse401,
        default_response: DeleteResourceV1beta1NamespacedResourceClaimResponseDefault,
    };

    pub const deleteResourceV1beta1NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ResourceClaim
    pub fn deleteResourceV1beta1NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta1NamespacedResourceClaimOptions) !DeleteResourceV1beta1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1beta1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1NamespacedResourceClaimResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1NamespacedResourceClaimResponse = union(enum) {
        ok: PatchResourceV1beta1NamespacedResourceClaimResponse200,
        created: PatchResourceV1beta1NamespacedResourceClaimResponse201,
        unauthorized: PatchResourceV1beta1NamespacedResourceClaimResponse401,
        default_response: PatchResourceV1beta1NamespacedResourceClaimResponseDefault,
    };

    pub const patchResourceV1beta1NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ResourceClaim
    pub fn patchResourceV1beta1NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1beta1NamespacedResourceClaimOptions) !PatchResourceV1beta1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1beta1NamespacedResourceClaimStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1NamespacedResourceClaimStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1NamespacedResourceClaimStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1NamespacedResourceClaimStatusResponse = union(enum) {
        ok: ReadResourceV1beta1NamespacedResourceClaimStatusResponse200,
        unauthorized: ReadResourceV1beta1NamespacedResourceClaimStatusResponse401,
        default_response: ReadResourceV1beta1NamespacedResourceClaimStatusResponseDefault,
    };

    pub const readResourceV1beta1NamespacedResourceClaimStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified ResourceClaim
    pub fn readResourceV1beta1NamespacedResourceClaimStatus(self: *Client, name: []const u8, namespace: []const u8, options: readResourceV1beta1NamespacedResourceClaimStatusOptions) !ReadResourceV1beta1NamespacedResourceClaimStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1beta1NamespacedResourceClaimStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1NamespacedResourceClaimStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1NamespacedResourceClaimStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1NamespacedResourceClaimStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1NamespacedResourceClaimStatusResponse = union(enum) {
        ok: ReplaceResourceV1beta1NamespacedResourceClaimStatusResponse200,
        created: ReplaceResourceV1beta1NamespacedResourceClaimStatusResponse201,
        unauthorized: ReplaceResourceV1beta1NamespacedResourceClaimStatusResponse401,
        default_response: ReplaceResourceV1beta1NamespacedResourceClaimStatusResponseDefault,
    };

    pub const replaceResourceV1beta1NamespacedResourceClaimStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified ResourceClaim
    pub fn replaceResourceV1beta1NamespacedResourceClaimStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.resource.v1beta1.ResourceClaim, options: replaceResourceV1beta1NamespacedResourceClaimStatusOptions) !ReplaceResourceV1beta1NamespacedResourceClaimStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1beta1NamespacedResourceClaimStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1NamespacedResourceClaimStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1NamespacedResourceClaimStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1NamespacedResourceClaimStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1NamespacedResourceClaimStatusResponse = union(enum) {
        ok: PatchResourceV1beta1NamespacedResourceClaimStatusResponse200,
        created: PatchResourceV1beta1NamespacedResourceClaimStatusResponse201,
        unauthorized: PatchResourceV1beta1NamespacedResourceClaimStatusResponse401,
        default_response: PatchResourceV1beta1NamespacedResourceClaimStatusResponseDefault,
    };

    pub const patchResourceV1beta1NamespacedResourceClaimStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified ResourceClaim
    pub fn patchResourceV1beta1NamespacedResourceClaimStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1beta1NamespacedResourceClaimStatusOptions) !PatchResourceV1beta1NamespacedResourceClaimStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1beta1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplateList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: ListResourceV1beta1NamespacedResourceClaimTemplateResponse200,
        unauthorized: ListResourceV1beta1NamespacedResourceClaimTemplateResponse401,
        default_response: ListResourceV1beta1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const listResourceV1beta1NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceClaimTemplate
    pub fn listResourceV1beta1NamespacedResourceClaimTemplate(self: *Client, namespace: []const u8, options: listResourceV1beta1NamespacedResourceClaimTemplateOptions) !ListResourceV1beta1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplateList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1beta1NamespacedResourceClaimTemplate(self: *Client, namespace: []const u8, options: listResourceV1beta1NamespacedResourceClaimTemplateOptions) !WatchStream(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1beta1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/resourceclaimtemplates");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1beta1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1NamespacedResourceClaimTemplateResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1NamespacedResourceClaimTemplateResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: CreateResourceV1beta1NamespacedResourceClaimTemplateResponse200,
        created: CreateResourceV1beta1NamespacedResourceClaimTemplateResponse201,
        accepted: CreateResourceV1beta1NamespacedResourceClaimTemplateResponse202,
        unauthorized: CreateResourceV1beta1NamespacedResourceClaimTemplateResponse401,
        default_response: CreateResourceV1beta1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const createResourceV1beta1NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ResourceClaimTemplate
    pub fn createResourceV1beta1NamespacedResourceClaimTemplate(self: *Client, namespace: []const u8, body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate, options: createResourceV1beta1NamespacedResourceClaimTemplateOptions) !CreateResourceV1beta1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta1CollectionNamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1CollectionNamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1CollectionNamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1CollectionNamespacedResourceClaimTemplateResponse = union(enum) {
        ok: DeleteResourceV1beta1CollectionNamespacedResourceClaimTemplateResponse200,
        unauthorized: DeleteResourceV1beta1CollectionNamespacedResourceClaimTemplateResponse401,
        default_response: DeleteResourceV1beta1CollectionNamespacedResourceClaimTemplateResponseDefault,
    };

    pub const deleteResourceV1beta1CollectionNamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ResourceClaimTemplate
    pub fn deleteResourceV1beta1CollectionNamespacedResourceClaimTemplate(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta1CollectionNamespacedResourceClaimTemplateOptions) !DeleteResourceV1beta1CollectionNamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1beta1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: ReadResourceV1beta1NamespacedResourceClaimTemplateResponse200,
        unauthorized: ReadResourceV1beta1NamespacedResourceClaimTemplateResponse401,
        default_response: ReadResourceV1beta1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const readResourceV1beta1NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ResourceClaimTemplate
    pub fn readResourceV1beta1NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, options: readResourceV1beta1NamespacedResourceClaimTemplateOptions) !ReadResourceV1beta1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1beta1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1NamespacedResourceClaimTemplateResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: ReplaceResourceV1beta1NamespacedResourceClaimTemplateResponse200,
        created: ReplaceResourceV1beta1NamespacedResourceClaimTemplateResponse201,
        unauthorized: ReplaceResourceV1beta1NamespacedResourceClaimTemplateResponse401,
        default_response: ReplaceResourceV1beta1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const replaceResourceV1beta1NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ResourceClaimTemplate
    pub fn replaceResourceV1beta1NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate, options: replaceResourceV1beta1NamespacedResourceClaimTemplateOptions) !ReplaceResourceV1beta1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1NamespacedResourceClaimTemplateResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: DeleteResourceV1beta1NamespacedResourceClaimTemplateResponse200,
        accepted: DeleteResourceV1beta1NamespacedResourceClaimTemplateResponse202,
        unauthorized: DeleteResourceV1beta1NamespacedResourceClaimTemplateResponse401,
        default_response: DeleteResourceV1beta1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const deleteResourceV1beta1NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ResourceClaimTemplate
    pub fn deleteResourceV1beta1NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta1NamespacedResourceClaimTemplateOptions) !DeleteResourceV1beta1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1beta1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1NamespacedResourceClaimTemplateResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: PatchResourceV1beta1NamespacedResourceClaimTemplateResponse200,
        created: PatchResourceV1beta1NamespacedResourceClaimTemplateResponse201,
        unauthorized: PatchResourceV1beta1NamespacedResourceClaimTemplateResponse401,
        default_response: PatchResourceV1beta1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const patchResourceV1beta1NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ResourceClaimTemplate
    pub fn patchResourceV1beta1NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1beta1NamespacedResourceClaimTemplateOptions) !PatchResourceV1beta1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1beta1ResourceClaimForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1ResourceClaimForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1ResourceClaimForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1ResourceClaimForAllNamespacesResponse = union(enum) {
        ok: ListResourceV1beta1ResourceClaimForAllNamespacesResponse200,
        unauthorized: ListResourceV1beta1ResourceClaimForAllNamespacesResponse401,
        default_response: ListResourceV1beta1ResourceClaimForAllNamespacesResponseDefault,
    };

    pub const listResourceV1beta1ResourceClaimForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceClaim
    pub fn listResourceV1beta1ResourceClaimForAllNamespaces(self: *Client, options: listResourceV1beta1ResourceClaimForAllNamespacesOptions) !ListResourceV1beta1ResourceClaimForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1beta1ResourceClaimForAllNamespaces(self: *Client, options: listResourceV1beta1ResourceClaimForAllNamespacesOptions) !WatchStream(root.io.k8s.api.resource.v1beta1.ResourceClaim) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1beta1/resourceclaims");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1beta1.ResourceClaim){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListResourceV1beta1ResourceClaimTemplateForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceClaimTemplateList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1ResourceClaimTemplateForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1ResourceClaimTemplateForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1ResourceClaimTemplateForAllNamespacesResponse = union(enum) {
        ok: ListResourceV1beta1ResourceClaimTemplateForAllNamespacesResponse200,
        unauthorized: ListResourceV1beta1ResourceClaimTemplateForAllNamespacesResponse401,
        default_response: ListResourceV1beta1ResourceClaimTemplateForAllNamespacesResponseDefault,
    };

    pub const listResourceV1beta1ResourceClaimTemplateForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceClaimTemplate
    pub fn listResourceV1beta1ResourceClaimTemplateForAllNamespaces(self: *Client, options: listResourceV1beta1ResourceClaimTemplateForAllNamespacesOptions) !ListResourceV1beta1ResourceClaimTemplateForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplateList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1beta1ResourceClaimTemplateForAllNamespaces(self: *Client, options: listResourceV1beta1ResourceClaimTemplateForAllNamespacesOptions) !WatchStream(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1beta1/resourceclaimtemplates");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1beta1.ResourceClaimTemplate){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListResourceV1beta1ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceSliceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta1ResourceSliceResponse = union(enum) {
        ok: ListResourceV1beta1ResourceSliceResponse200,
        unauthorized: ListResourceV1beta1ResourceSliceResponse401,
        default_response: ListResourceV1beta1ResourceSliceResponseDefault,
    };

    pub const listResourceV1beta1ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceSlice
    pub fn listResourceV1beta1ResourceSlice(self: *Client, options: listResourceV1beta1ResourceSliceOptions) !ListResourceV1beta1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/resourceslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceSliceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1beta1ResourceSlice(self: *Client, options: listResourceV1beta1ResourceSliceOptions) !WatchStream(root.io.k8s.api.resource.v1beta1.ResourceSlice) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1beta1/resourceslices");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1beta1.ResourceSlice){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1beta1ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1ResourceSliceResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1ResourceSliceResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta1ResourceSliceResponse = union(enum) {
        ok: CreateResourceV1beta1ResourceSliceResponse200,
        created: CreateResourceV1beta1ResourceSliceResponse201,
        accepted: CreateResourceV1beta1ResourceSliceResponse202,
        unauthorized: CreateResourceV1beta1ResourceSliceResponse401,
        default_response: CreateResourceV1beta1ResourceSliceResponseDefault,
    };

    pub const createResourceV1beta1ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ResourceSlice
    pub fn createResourceV1beta1ResourceSlice(self: *Client, body: root.io.k8s.api.resource.v1beta1.ResourceSlice, options: createResourceV1beta1ResourceSliceOptions) !CreateResourceV1beta1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/resourceslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta1CollectionResourceSliceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1CollectionResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1CollectionResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1CollectionResourceSliceResponse = union(enum) {
        ok: DeleteResourceV1beta1CollectionResourceSliceResponse200,
        unauthorized: DeleteResourceV1beta1CollectionResourceSliceResponse401,
        default_response: DeleteResourceV1beta1CollectionResourceSliceResponseDefault,
    };

    pub const deleteResourceV1beta1CollectionResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ResourceSlice
    pub fn deleteResourceV1beta1CollectionResourceSlice(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta1CollectionResourceSliceOptions) !DeleteResourceV1beta1CollectionResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/resourceslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1beta1ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta1ResourceSliceResponse = union(enum) {
        ok: ReadResourceV1beta1ResourceSliceResponse200,
        unauthorized: ReadResourceV1beta1ResourceSliceResponse401,
        default_response: ReadResourceV1beta1ResourceSliceResponseDefault,
    };

    pub const readResourceV1beta1ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ResourceSlice
    pub fn readResourceV1beta1ResourceSlice(self: *Client, name: []const u8, options: readResourceV1beta1ResourceSliceOptions) !ReadResourceV1beta1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1beta1ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1ResourceSliceResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta1ResourceSliceResponse = union(enum) {
        ok: ReplaceResourceV1beta1ResourceSliceResponse200,
        created: ReplaceResourceV1beta1ResourceSliceResponse201,
        unauthorized: ReplaceResourceV1beta1ResourceSliceResponse401,
        default_response: ReplaceResourceV1beta1ResourceSliceResponseDefault,
    };

    pub const replaceResourceV1beta1ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ResourceSlice
    pub fn replaceResourceV1beta1ResourceSlice(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1beta1.ResourceSlice, options: replaceResourceV1beta1ResourceSliceOptions) !ReplaceResourceV1beta1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta1ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1ResourceSliceResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta1ResourceSliceResponse = union(enum) {
        ok: DeleteResourceV1beta1ResourceSliceResponse200,
        accepted: DeleteResourceV1beta1ResourceSliceResponse202,
        unauthorized: DeleteResourceV1beta1ResourceSliceResponse401,
        default_response: DeleteResourceV1beta1ResourceSliceResponseDefault,
    };

    pub const deleteResourceV1beta1ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ResourceSlice
    pub fn deleteResourceV1beta1ResourceSlice(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta1ResourceSliceOptions) !DeleteResourceV1beta1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1beta1ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1ResourceSliceResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta1.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta1ResourceSliceResponse = union(enum) {
        ok: PatchResourceV1beta1ResourceSliceResponse200,
        created: PatchResourceV1beta1ResourceSliceResponse201,
        unauthorized: PatchResourceV1beta1ResourceSliceResponse401,
        default_response: PatchResourceV1beta1ResourceSliceResponseDefault,
    };

    pub const patchResourceV1beta1ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ResourceSlice
    pub fn patchResourceV1beta1ResourceSlice(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1beta1ResourceSliceOptions) !PatchResourceV1beta1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta1.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta1DeviceClassListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1DeviceClassListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1DeviceClassListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1DeviceClassListResponse = union(enum) {
        ok: WatchResourceV1beta1DeviceClassListResponse200,
        unauthorized: WatchResourceV1beta1DeviceClassListResponse401,
        default_response: WatchResourceV1beta1DeviceClassListResponseDefault,
    };

    pub const watchResourceV1beta1DeviceClassListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of DeviceClass. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1beta1DeviceClassList(self: *Client, options: watchResourceV1beta1DeviceClassListOptions) !WatchResourceV1beta1DeviceClassListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/watch/deviceclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta1DeviceClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1DeviceClassResponse = union(enum) {
        ok: WatchResourceV1beta1DeviceClassResponse200,
        unauthorized: WatchResourceV1beta1DeviceClassResponse401,
        default_response: WatchResourceV1beta1DeviceClassResponseDefault,
    };

    pub const watchResourceV1beta1DeviceClassOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind DeviceClass. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1beta1DeviceClass(self: *Client, name: []const u8, options: watchResourceV1beta1DeviceClassOptions) !WatchResourceV1beta1DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/watch/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta1NamespacedResourceClaimListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1NamespacedResourceClaimListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1NamespacedResourceClaimListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1NamespacedResourceClaimListResponse = union(enum) {
        ok: WatchResourceV1beta1NamespacedResourceClaimListResponse200,
        unauthorized: WatchResourceV1beta1NamespacedResourceClaimListResponse401,
        default_response: WatchResourceV1beta1NamespacedResourceClaimListResponseDefault,
    };

    pub const watchResourceV1beta1NamespacedResourceClaimListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceClaim. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1beta1NamespacedResourceClaimList(self: *Client, namespace: []const u8, options: watchResourceV1beta1NamespacedResourceClaimListOptions) !WatchResourceV1beta1NamespacedResourceClaimListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta1NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1NamespacedResourceClaimResponse = union(enum) {
        ok: WatchResourceV1beta1NamespacedResourceClaimResponse200,
        unauthorized: WatchResourceV1beta1NamespacedResourceClaimResponse401,
        default_response: WatchResourceV1beta1NamespacedResourceClaimResponseDefault,
    };

    pub const watchResourceV1beta1NamespacedResourceClaimOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ResourceClaim. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1beta1NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, options: watchResourceV1beta1NamespacedResourceClaimOptions) !WatchResourceV1beta1NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta1NamespacedResourceClaimTemplateListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1NamespacedResourceClaimTemplateListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1NamespacedResourceClaimTemplateListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1NamespacedResourceClaimTemplateListResponse = union(enum) {
        ok: WatchResourceV1beta1NamespacedResourceClaimTemplateListResponse200,
        unauthorized: WatchResourceV1beta1NamespacedResourceClaimTemplateListResponse401,
        default_response: WatchResourceV1beta1NamespacedResourceClaimTemplateListResponseDefault,
    };

    pub const watchResourceV1beta1NamespacedResourceClaimTemplateListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceClaimTemplate. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1beta1NamespacedResourceClaimTemplateList(self: *Client, namespace: []const u8, options: watchResourceV1beta1NamespacedResourceClaimTemplateListOptions) !WatchResourceV1beta1NamespacedResourceClaimTemplateListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta1NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: WatchResourceV1beta1NamespacedResourceClaimTemplateResponse200,
        unauthorized: WatchResourceV1beta1NamespacedResourceClaimTemplateResponse401,
        default_response: WatchResourceV1beta1NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const watchResourceV1beta1NamespacedResourceClaimTemplateOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ResourceClaimTemplate. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1beta1NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, options: watchResourceV1beta1NamespacedResourceClaimTemplateOptions) !WatchResourceV1beta1NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta1ResourceClaimListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1ResourceClaimListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1ResourceClaimListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1ResourceClaimListForAllNamespacesResponse = union(enum) {
        ok: WatchResourceV1beta1ResourceClaimListForAllNamespacesResponse200,
        unauthorized: WatchResourceV1beta1ResourceClaimListForAllNamespacesResponse401,
        default_response: WatchResourceV1beta1ResourceClaimListForAllNamespacesResponseDefault,
    };

    pub const watchResourceV1beta1ResourceClaimListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceClaim. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1beta1ResourceClaimListForAllNamespaces(self: *Client, options: watchResourceV1beta1ResourceClaimListForAllNamespacesOptions) !WatchResourceV1beta1ResourceClaimListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/watch/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta1ResourceClaimTemplateListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1ResourceClaimTemplateListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1ResourceClaimTemplateListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1ResourceClaimTemplateListForAllNamespacesResponse = union(enum) {
        ok: WatchResourceV1beta1ResourceClaimTemplateListForAllNamespacesResponse200,
        unauthorized: WatchResourceV1beta1ResourceClaimTemplateListForAllNamespacesResponse401,
        default_response: WatchResourceV1beta1ResourceClaimTemplateListForAllNamespacesResponseDefault,
    };

    pub const watchResourceV1beta1ResourceClaimTemplateListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceClaimTemplate. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1beta1ResourceClaimTemplateListForAllNamespaces(self: *Client, options: watchResourceV1beta1ResourceClaimTemplateListForAllNamespacesOptions) !WatchResourceV1beta1ResourceClaimTemplateListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/watch/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta1ResourceSliceListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1ResourceSliceListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1ResourceSliceListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1ResourceSliceListResponse = union(enum) {
        ok: WatchResourceV1beta1ResourceSliceListResponse200,
        unauthorized: WatchResourceV1beta1ResourceSliceListResponse401,
        default_response: WatchResourceV1beta1ResourceSliceListResponseDefault,
    };

    pub const watchResourceV1beta1ResourceSliceListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceSlice. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1beta1ResourceSliceList(self: *Client, options: watchResourceV1beta1ResourceSliceListOptions) !WatchResourceV1beta1ResourceSliceListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/watch/resourceslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta1ResourceSliceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta1ResourceSliceResponse = union(enum) {
        ok: WatchResourceV1beta1ResourceSliceResponse200,
        unauthorized: WatchResourceV1beta1ResourceSliceResponse401,
        default_response: WatchResourceV1beta1ResourceSliceResponseDefault,
    };

    pub const watchResourceV1beta1ResourceSliceOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ResourceSlice. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1beta1ResourceSlice(self: *Client, name: []const u8, options: watchResourceV1beta1ResourceSliceOptions) !WatchResourceV1beta1ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta1/watch/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetResourceV1beta2APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceV1beta2APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceV1beta2APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetResourceV1beta2APIResourcesResponse = union(enum) {
        ok: GetResourceV1beta2APIResourcesResponse200,
        unauthorized: GetResourceV1beta2APIResourcesResponse401,
        default_response: GetResourceV1beta2APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getResourceV1beta2APIResources(self: *Client) !GetResourceV1beta2APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1beta2DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceClassList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2DeviceClassResponse = union(enum) {
        ok: ListResourceV1beta2DeviceClassResponse200,
        unauthorized: ListResourceV1beta2DeviceClassResponse401,
        default_response: ListResourceV1beta2DeviceClassResponseDefault,
    };

    pub const listResourceV1beta2DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind DeviceClass
    pub fn listResourceV1beta2DeviceClass(self: *Client, options: listResourceV1beta2DeviceClassOptions) !ListResourceV1beta2DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/deviceclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceClassList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1beta2DeviceClass(self: *Client, options: listResourceV1beta2DeviceClassOptions) !WatchStream(root.io.k8s.api.resource.v1beta2.DeviceClass) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1beta2/deviceclasses");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1beta2.DeviceClass){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1beta2DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2DeviceClassResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2DeviceClassResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2DeviceClassResponse = union(enum) {
        ok: CreateResourceV1beta2DeviceClassResponse200,
        created: CreateResourceV1beta2DeviceClassResponse201,
        accepted: CreateResourceV1beta2DeviceClassResponse202,
        unauthorized: CreateResourceV1beta2DeviceClassResponse401,
        default_response: CreateResourceV1beta2DeviceClassResponseDefault,
    };

    pub const createResourceV1beta2DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a DeviceClass
    pub fn createResourceV1beta2DeviceClass(self: *Client, body: root.io.k8s.api.resource.v1beta2.DeviceClass, options: createResourceV1beta2DeviceClassOptions) !CreateResourceV1beta2DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/deviceclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta2CollectionDeviceClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionDeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionDeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionDeviceClassResponse = union(enum) {
        ok: DeleteResourceV1beta2CollectionDeviceClassResponse200,
        unauthorized: DeleteResourceV1beta2CollectionDeviceClassResponse401,
        default_response: DeleteResourceV1beta2CollectionDeviceClassResponseDefault,
    };

    pub const deleteResourceV1beta2CollectionDeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of DeviceClass
    pub fn deleteResourceV1beta2CollectionDeviceClass(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta2CollectionDeviceClassOptions) !DeleteResourceV1beta2CollectionDeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/deviceclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1beta2DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2DeviceClassResponse = union(enum) {
        ok: ReadResourceV1beta2DeviceClassResponse200,
        unauthorized: ReadResourceV1beta2DeviceClassResponse401,
        default_response: ReadResourceV1beta2DeviceClassResponseDefault,
    };

    pub const readResourceV1beta2DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified DeviceClass
    pub fn readResourceV1beta2DeviceClass(self: *Client, name: []const u8, options: readResourceV1beta2DeviceClassOptions) !ReadResourceV1beta2DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1beta2DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2DeviceClassResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2DeviceClassResponse = union(enum) {
        ok: ReplaceResourceV1beta2DeviceClassResponse200,
        created: ReplaceResourceV1beta2DeviceClassResponse201,
        unauthorized: ReplaceResourceV1beta2DeviceClassResponse401,
        default_response: ReplaceResourceV1beta2DeviceClassResponseDefault,
    };

    pub const replaceResourceV1beta2DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified DeviceClass
    pub fn replaceResourceV1beta2DeviceClass(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1beta2.DeviceClass, options: replaceResourceV1beta2DeviceClassOptions) !ReplaceResourceV1beta2DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta2DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2DeviceClassResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2DeviceClassResponse = union(enum) {
        ok: DeleteResourceV1beta2DeviceClassResponse200,
        accepted: DeleteResourceV1beta2DeviceClassResponse202,
        unauthorized: DeleteResourceV1beta2DeviceClassResponse401,
        default_response: DeleteResourceV1beta2DeviceClassResponseDefault,
    };

    pub const deleteResourceV1beta2DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a DeviceClass
    pub fn deleteResourceV1beta2DeviceClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta2DeviceClassOptions) !DeleteResourceV1beta2DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1beta2DeviceClassResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2DeviceClassResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2DeviceClassResponse = union(enum) {
        ok: PatchResourceV1beta2DeviceClassResponse200,
        created: PatchResourceV1beta2DeviceClassResponse201,
        unauthorized: PatchResourceV1beta2DeviceClassResponse401,
        default_response: PatchResourceV1beta2DeviceClassResponseDefault,
    };

    pub const patchResourceV1beta2DeviceClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified DeviceClass
    pub fn patchResourceV1beta2DeviceClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1beta2DeviceClassOptions) !PatchResourceV1beta2DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1beta2DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRuleList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2DeviceTaintRuleResponse = union(enum) {
        ok: ListResourceV1beta2DeviceTaintRuleResponse200,
        unauthorized: ListResourceV1beta2DeviceTaintRuleResponse401,
        default_response: ListResourceV1beta2DeviceTaintRuleResponseDefault,
    };

    pub const listResourceV1beta2DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind DeviceTaintRule
    pub fn listResourceV1beta2DeviceTaintRule(self: *Client, options: listResourceV1beta2DeviceTaintRuleOptions) !ListResourceV1beta2DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/devicetaintrules", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRuleList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1beta2DeviceTaintRule(self: *Client, options: listResourceV1beta2DeviceTaintRuleOptions) !WatchStream(root.io.k8s.api.resource.v1beta2.DeviceTaintRule) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1beta2/devicetaintrules");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1beta2.DeviceTaintRule){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1beta2DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2DeviceTaintRuleResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2DeviceTaintRuleResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2DeviceTaintRuleResponse = union(enum) {
        ok: CreateResourceV1beta2DeviceTaintRuleResponse200,
        created: CreateResourceV1beta2DeviceTaintRuleResponse201,
        accepted: CreateResourceV1beta2DeviceTaintRuleResponse202,
        unauthorized: CreateResourceV1beta2DeviceTaintRuleResponse401,
        default_response: CreateResourceV1beta2DeviceTaintRuleResponseDefault,
    };

    pub const createResourceV1beta2DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a DeviceTaintRule
    pub fn createResourceV1beta2DeviceTaintRule(self: *Client, body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule, options: createResourceV1beta2DeviceTaintRuleOptions) !CreateResourceV1beta2DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/devicetaintrules", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta2CollectionDeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionDeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionDeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionDeviceTaintRuleResponse = union(enum) {
        ok: DeleteResourceV1beta2CollectionDeviceTaintRuleResponse200,
        unauthorized: DeleteResourceV1beta2CollectionDeviceTaintRuleResponse401,
        default_response: DeleteResourceV1beta2CollectionDeviceTaintRuleResponseDefault,
    };

    pub const deleteResourceV1beta2CollectionDeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of DeviceTaintRule
    pub fn deleteResourceV1beta2CollectionDeviceTaintRule(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta2CollectionDeviceTaintRuleOptions) !DeleteResourceV1beta2CollectionDeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/devicetaintrules", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1beta2DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2DeviceTaintRuleResponse = union(enum) {
        ok: ReadResourceV1beta2DeviceTaintRuleResponse200,
        unauthorized: ReadResourceV1beta2DeviceTaintRuleResponse401,
        default_response: ReadResourceV1beta2DeviceTaintRuleResponseDefault,
    };

    pub const readResourceV1beta2DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified DeviceTaintRule
    pub fn readResourceV1beta2DeviceTaintRule(self: *Client, name: []const u8, options: readResourceV1beta2DeviceTaintRuleOptions) !ReadResourceV1beta2DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1beta2DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2DeviceTaintRuleResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2DeviceTaintRuleResponse = union(enum) {
        ok: ReplaceResourceV1beta2DeviceTaintRuleResponse200,
        created: ReplaceResourceV1beta2DeviceTaintRuleResponse201,
        unauthorized: ReplaceResourceV1beta2DeviceTaintRuleResponse401,
        default_response: ReplaceResourceV1beta2DeviceTaintRuleResponseDefault,
    };

    pub const replaceResourceV1beta2DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified DeviceTaintRule
    pub fn replaceResourceV1beta2DeviceTaintRule(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule, options: replaceResourceV1beta2DeviceTaintRuleOptions) !ReplaceResourceV1beta2DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta2DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2DeviceTaintRuleResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2DeviceTaintRuleResponse = union(enum) {
        ok: DeleteResourceV1beta2DeviceTaintRuleResponse200,
        accepted: DeleteResourceV1beta2DeviceTaintRuleResponse202,
        unauthorized: DeleteResourceV1beta2DeviceTaintRuleResponse401,
        default_response: DeleteResourceV1beta2DeviceTaintRuleResponseDefault,
    };

    pub const deleteResourceV1beta2DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a DeviceTaintRule
    pub fn deleteResourceV1beta2DeviceTaintRule(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta2DeviceTaintRuleOptions) !DeleteResourceV1beta2DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1beta2DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2DeviceTaintRuleResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2DeviceTaintRuleResponse = union(enum) {
        ok: PatchResourceV1beta2DeviceTaintRuleResponse200,
        created: PatchResourceV1beta2DeviceTaintRuleResponse201,
        unauthorized: PatchResourceV1beta2DeviceTaintRuleResponse401,
        default_response: PatchResourceV1beta2DeviceTaintRuleResponseDefault,
    };

    pub const patchResourceV1beta2DeviceTaintRuleOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified DeviceTaintRule
    pub fn patchResourceV1beta2DeviceTaintRule(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1beta2DeviceTaintRuleOptions) !PatchResourceV1beta2DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1beta2DeviceTaintRuleStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2DeviceTaintRuleStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2DeviceTaintRuleStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2DeviceTaintRuleStatusResponse = union(enum) {
        ok: ReadResourceV1beta2DeviceTaintRuleStatusResponse200,
        unauthorized: ReadResourceV1beta2DeviceTaintRuleStatusResponse401,
        default_response: ReadResourceV1beta2DeviceTaintRuleStatusResponseDefault,
    };

    pub const readResourceV1beta2DeviceTaintRuleStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified DeviceTaintRule
    pub fn readResourceV1beta2DeviceTaintRuleStatus(self: *Client, name: []const u8, options: readResourceV1beta2DeviceTaintRuleStatusOptions) !ReadResourceV1beta2DeviceTaintRuleStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1beta2DeviceTaintRuleStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2DeviceTaintRuleStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2DeviceTaintRuleStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2DeviceTaintRuleStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2DeviceTaintRuleStatusResponse = union(enum) {
        ok: ReplaceResourceV1beta2DeviceTaintRuleStatusResponse200,
        created: ReplaceResourceV1beta2DeviceTaintRuleStatusResponse201,
        unauthorized: ReplaceResourceV1beta2DeviceTaintRuleStatusResponse401,
        default_response: ReplaceResourceV1beta2DeviceTaintRuleStatusResponseDefault,
    };

    pub const replaceResourceV1beta2DeviceTaintRuleStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified DeviceTaintRule
    pub fn replaceResourceV1beta2DeviceTaintRuleStatus(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule, options: replaceResourceV1beta2DeviceTaintRuleStatusOptions) !ReplaceResourceV1beta2DeviceTaintRuleStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1beta2DeviceTaintRuleStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2DeviceTaintRuleStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.DeviceTaintRule,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2DeviceTaintRuleStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2DeviceTaintRuleStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2DeviceTaintRuleStatusResponse = union(enum) {
        ok: PatchResourceV1beta2DeviceTaintRuleStatusResponse200,
        created: PatchResourceV1beta2DeviceTaintRuleStatusResponse201,
        unauthorized: PatchResourceV1beta2DeviceTaintRuleStatusResponse401,
        default_response: PatchResourceV1beta2DeviceTaintRuleStatusResponseDefault,
    };

    pub const patchResourceV1beta2DeviceTaintRuleStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified DeviceTaintRule
    pub fn patchResourceV1beta2DeviceTaintRuleStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1beta2DeviceTaintRuleStatusOptions) !PatchResourceV1beta2DeviceTaintRuleStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.DeviceTaintRule, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1beta2NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2NamespacedResourceClaimResponse = union(enum) {
        ok: ListResourceV1beta2NamespacedResourceClaimResponse200,
        unauthorized: ListResourceV1beta2NamespacedResourceClaimResponse401,
        default_response: ListResourceV1beta2NamespacedResourceClaimResponseDefault,
    };

    pub const listResourceV1beta2NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceClaim
    pub fn listResourceV1beta2NamespacedResourceClaim(self: *Client, namespace: []const u8, options: listResourceV1beta2NamespacedResourceClaimOptions) !ListResourceV1beta2NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1beta2NamespacedResourceClaim(self: *Client, namespace: []const u8, options: listResourceV1beta2NamespacedResourceClaimOptions) !WatchStream(root.io.k8s.api.resource.v1beta2.ResourceClaim) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1beta2/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/resourceclaims");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1beta2.ResourceClaim){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1beta2NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2NamespacedResourceClaimResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2NamespacedResourceClaimResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2NamespacedResourceClaimResponse = union(enum) {
        ok: CreateResourceV1beta2NamespacedResourceClaimResponse200,
        created: CreateResourceV1beta2NamespacedResourceClaimResponse201,
        accepted: CreateResourceV1beta2NamespacedResourceClaimResponse202,
        unauthorized: CreateResourceV1beta2NamespacedResourceClaimResponse401,
        default_response: CreateResourceV1beta2NamespacedResourceClaimResponseDefault,
    };

    pub const createResourceV1beta2NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ResourceClaim
    pub fn createResourceV1beta2NamespacedResourceClaim(self: *Client, namespace: []const u8, body: root.io.k8s.api.resource.v1beta2.ResourceClaim, options: createResourceV1beta2NamespacedResourceClaimOptions) !CreateResourceV1beta2NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta2CollectionNamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionNamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionNamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionNamespacedResourceClaimResponse = union(enum) {
        ok: DeleteResourceV1beta2CollectionNamespacedResourceClaimResponse200,
        unauthorized: DeleteResourceV1beta2CollectionNamespacedResourceClaimResponse401,
        default_response: DeleteResourceV1beta2CollectionNamespacedResourceClaimResponseDefault,
    };

    pub const deleteResourceV1beta2CollectionNamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ResourceClaim
    pub fn deleteResourceV1beta2CollectionNamespacedResourceClaim(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta2CollectionNamespacedResourceClaimOptions) !DeleteResourceV1beta2CollectionNamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1beta2NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2NamespacedResourceClaimResponse = union(enum) {
        ok: ReadResourceV1beta2NamespacedResourceClaimResponse200,
        unauthorized: ReadResourceV1beta2NamespacedResourceClaimResponse401,
        default_response: ReadResourceV1beta2NamespacedResourceClaimResponseDefault,
    };

    pub const readResourceV1beta2NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ResourceClaim
    pub fn readResourceV1beta2NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, options: readResourceV1beta2NamespacedResourceClaimOptions) !ReadResourceV1beta2NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1beta2NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2NamespacedResourceClaimResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2NamespacedResourceClaimResponse = union(enum) {
        ok: ReplaceResourceV1beta2NamespacedResourceClaimResponse200,
        created: ReplaceResourceV1beta2NamespacedResourceClaimResponse201,
        unauthorized: ReplaceResourceV1beta2NamespacedResourceClaimResponse401,
        default_response: ReplaceResourceV1beta2NamespacedResourceClaimResponseDefault,
    };

    pub const replaceResourceV1beta2NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ResourceClaim
    pub fn replaceResourceV1beta2NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.resource.v1beta2.ResourceClaim, options: replaceResourceV1beta2NamespacedResourceClaimOptions) !ReplaceResourceV1beta2NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta2NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2NamespacedResourceClaimResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2NamespacedResourceClaimResponse = union(enum) {
        ok: DeleteResourceV1beta2NamespacedResourceClaimResponse200,
        accepted: DeleteResourceV1beta2NamespacedResourceClaimResponse202,
        unauthorized: DeleteResourceV1beta2NamespacedResourceClaimResponse401,
        default_response: DeleteResourceV1beta2NamespacedResourceClaimResponseDefault,
    };

    pub const deleteResourceV1beta2NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ResourceClaim
    pub fn deleteResourceV1beta2NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta2NamespacedResourceClaimOptions) !DeleteResourceV1beta2NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1beta2NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2NamespacedResourceClaimResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2NamespacedResourceClaimResponse = union(enum) {
        ok: PatchResourceV1beta2NamespacedResourceClaimResponse200,
        created: PatchResourceV1beta2NamespacedResourceClaimResponse201,
        unauthorized: PatchResourceV1beta2NamespacedResourceClaimResponse401,
        default_response: PatchResourceV1beta2NamespacedResourceClaimResponseDefault,
    };

    pub const patchResourceV1beta2NamespacedResourceClaimOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ResourceClaim
    pub fn patchResourceV1beta2NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1beta2NamespacedResourceClaimOptions) !PatchResourceV1beta2NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1beta2NamespacedResourceClaimStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2NamespacedResourceClaimStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2NamespacedResourceClaimStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2NamespacedResourceClaimStatusResponse = union(enum) {
        ok: ReadResourceV1beta2NamespacedResourceClaimStatusResponse200,
        unauthorized: ReadResourceV1beta2NamespacedResourceClaimStatusResponse401,
        default_response: ReadResourceV1beta2NamespacedResourceClaimStatusResponseDefault,
    };

    pub const readResourceV1beta2NamespacedResourceClaimStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified ResourceClaim
    pub fn readResourceV1beta2NamespacedResourceClaimStatus(self: *Client, name: []const u8, namespace: []const u8, options: readResourceV1beta2NamespacedResourceClaimStatusOptions) !ReadResourceV1beta2NamespacedResourceClaimStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1beta2NamespacedResourceClaimStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2NamespacedResourceClaimStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2NamespacedResourceClaimStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2NamespacedResourceClaimStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2NamespacedResourceClaimStatusResponse = union(enum) {
        ok: ReplaceResourceV1beta2NamespacedResourceClaimStatusResponse200,
        created: ReplaceResourceV1beta2NamespacedResourceClaimStatusResponse201,
        unauthorized: ReplaceResourceV1beta2NamespacedResourceClaimStatusResponse401,
        default_response: ReplaceResourceV1beta2NamespacedResourceClaimStatusResponseDefault,
    };

    pub const replaceResourceV1beta2NamespacedResourceClaimStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified ResourceClaim
    pub fn replaceResourceV1beta2NamespacedResourceClaimStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.resource.v1beta2.ResourceClaim, options: replaceResourceV1beta2NamespacedResourceClaimStatusOptions) !ReplaceResourceV1beta2NamespacedResourceClaimStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1beta2NamespacedResourceClaimStatusResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2NamespacedResourceClaimStatusResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaim,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2NamespacedResourceClaimStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2NamespacedResourceClaimStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2NamespacedResourceClaimStatusResponse = union(enum) {
        ok: PatchResourceV1beta2NamespacedResourceClaimStatusResponse200,
        created: PatchResourceV1beta2NamespacedResourceClaimStatusResponse201,
        unauthorized: PatchResourceV1beta2NamespacedResourceClaimStatusResponse401,
        default_response: PatchResourceV1beta2NamespacedResourceClaimStatusResponseDefault,
    };

    pub const patchResourceV1beta2NamespacedResourceClaimStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified ResourceClaim
    pub fn patchResourceV1beta2NamespacedResourceClaimStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1beta2NamespacedResourceClaimStatusOptions) !PatchResourceV1beta2NamespacedResourceClaimStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaim, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1beta2NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplateList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: ListResourceV1beta2NamespacedResourceClaimTemplateResponse200,
        unauthorized: ListResourceV1beta2NamespacedResourceClaimTemplateResponse401,
        default_response: ListResourceV1beta2NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const listResourceV1beta2NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceClaimTemplate
    pub fn listResourceV1beta2NamespacedResourceClaimTemplate(self: *Client, namespace: []const u8, options: listResourceV1beta2NamespacedResourceClaimTemplateOptions) !ListResourceV1beta2NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplateList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1beta2NamespacedResourceClaimTemplate(self: *Client, namespace: []const u8, options: listResourceV1beta2NamespacedResourceClaimTemplateOptions) !WatchStream(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1beta2/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/resourceclaimtemplates");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1beta2NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2NamespacedResourceClaimTemplateResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2NamespacedResourceClaimTemplateResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: CreateResourceV1beta2NamespacedResourceClaimTemplateResponse200,
        created: CreateResourceV1beta2NamespacedResourceClaimTemplateResponse201,
        accepted: CreateResourceV1beta2NamespacedResourceClaimTemplateResponse202,
        unauthorized: CreateResourceV1beta2NamespacedResourceClaimTemplateResponse401,
        default_response: CreateResourceV1beta2NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const createResourceV1beta2NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ResourceClaimTemplate
    pub fn createResourceV1beta2NamespacedResourceClaimTemplate(self: *Client, namespace: []const u8, body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate, options: createResourceV1beta2NamespacedResourceClaimTemplateOptions) !CreateResourceV1beta2NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta2CollectionNamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionNamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionNamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionNamespacedResourceClaimTemplateResponse = union(enum) {
        ok: DeleteResourceV1beta2CollectionNamespacedResourceClaimTemplateResponse200,
        unauthorized: DeleteResourceV1beta2CollectionNamespacedResourceClaimTemplateResponse401,
        default_response: DeleteResourceV1beta2CollectionNamespacedResourceClaimTemplateResponseDefault,
    };

    pub const deleteResourceV1beta2CollectionNamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ResourceClaimTemplate
    pub fn deleteResourceV1beta2CollectionNamespacedResourceClaimTemplate(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta2CollectionNamespacedResourceClaimTemplateOptions) !DeleteResourceV1beta2CollectionNamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1beta2NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: ReadResourceV1beta2NamespacedResourceClaimTemplateResponse200,
        unauthorized: ReadResourceV1beta2NamespacedResourceClaimTemplateResponse401,
        default_response: ReadResourceV1beta2NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const readResourceV1beta2NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ResourceClaimTemplate
    pub fn readResourceV1beta2NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, options: readResourceV1beta2NamespacedResourceClaimTemplateOptions) !ReadResourceV1beta2NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1beta2NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2NamespacedResourceClaimTemplateResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: ReplaceResourceV1beta2NamespacedResourceClaimTemplateResponse200,
        created: ReplaceResourceV1beta2NamespacedResourceClaimTemplateResponse201,
        unauthorized: ReplaceResourceV1beta2NamespacedResourceClaimTemplateResponse401,
        default_response: ReplaceResourceV1beta2NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const replaceResourceV1beta2NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ResourceClaimTemplate
    pub fn replaceResourceV1beta2NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate, options: replaceResourceV1beta2NamespacedResourceClaimTemplateOptions) !ReplaceResourceV1beta2NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta2NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2NamespacedResourceClaimTemplateResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: DeleteResourceV1beta2NamespacedResourceClaimTemplateResponse200,
        accepted: DeleteResourceV1beta2NamespacedResourceClaimTemplateResponse202,
        unauthorized: DeleteResourceV1beta2NamespacedResourceClaimTemplateResponse401,
        default_response: DeleteResourceV1beta2NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const deleteResourceV1beta2NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ResourceClaimTemplate
    pub fn deleteResourceV1beta2NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta2NamespacedResourceClaimTemplateOptions) !DeleteResourceV1beta2NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1beta2NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2NamespacedResourceClaimTemplateResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: PatchResourceV1beta2NamespacedResourceClaimTemplateResponse200,
        created: PatchResourceV1beta2NamespacedResourceClaimTemplateResponse201,
        unauthorized: PatchResourceV1beta2NamespacedResourceClaimTemplateResponse401,
        default_response: PatchResourceV1beta2NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const patchResourceV1beta2NamespacedResourceClaimTemplateOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ResourceClaimTemplate
    pub fn patchResourceV1beta2NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1beta2NamespacedResourceClaimTemplateOptions) !PatchResourceV1beta2NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListResourceV1beta2ResourceClaimForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2ResourceClaimForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2ResourceClaimForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2ResourceClaimForAllNamespacesResponse = union(enum) {
        ok: ListResourceV1beta2ResourceClaimForAllNamespacesResponse200,
        unauthorized: ListResourceV1beta2ResourceClaimForAllNamespacesResponse401,
        default_response: ListResourceV1beta2ResourceClaimForAllNamespacesResponseDefault,
    };

    pub const listResourceV1beta2ResourceClaimForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceClaim
    pub fn listResourceV1beta2ResourceClaimForAllNamespaces(self: *Client, options: listResourceV1beta2ResourceClaimForAllNamespacesOptions) !ListResourceV1beta2ResourceClaimForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1beta2ResourceClaimForAllNamespaces(self: *Client, options: listResourceV1beta2ResourceClaimForAllNamespacesOptions) !WatchStream(root.io.k8s.api.resource.v1beta2.ResourceClaim) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1beta2/resourceclaims");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1beta2.ResourceClaim){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListResourceV1beta2ResourceClaimTemplateForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceClaimTemplateList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2ResourceClaimTemplateForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2ResourceClaimTemplateForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2ResourceClaimTemplateForAllNamespacesResponse = union(enum) {
        ok: ListResourceV1beta2ResourceClaimTemplateForAllNamespacesResponse200,
        unauthorized: ListResourceV1beta2ResourceClaimTemplateForAllNamespacesResponse401,
        default_response: ListResourceV1beta2ResourceClaimTemplateForAllNamespacesResponseDefault,
    };

    pub const listResourceV1beta2ResourceClaimTemplateForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceClaimTemplate
    pub fn listResourceV1beta2ResourceClaimTemplateForAllNamespaces(self: *Client, options: listResourceV1beta2ResourceClaimTemplateForAllNamespacesOptions) !ListResourceV1beta2ResourceClaimTemplateForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplateList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1beta2ResourceClaimTemplateForAllNamespaces(self: *Client, options: listResourceV1beta2ResourceClaimTemplateForAllNamespacesOptions) !WatchStream(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1beta2/resourceclaimtemplates");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1beta2.ResourceClaimTemplate){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListResourceV1beta2ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceSliceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListResourceV1beta2ResourceSliceResponse = union(enum) {
        ok: ListResourceV1beta2ResourceSliceResponse200,
        unauthorized: ListResourceV1beta2ResourceSliceResponse401,
        default_response: ListResourceV1beta2ResourceSliceResponseDefault,
    };

    pub const listResourceV1beta2ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind ResourceSlice
    pub fn listResourceV1beta2ResourceSlice(self: *Client, options: listResourceV1beta2ResourceSliceOptions) !ListResourceV1beta2ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/resourceslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceSliceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistResourceV1beta2ResourceSlice(self: *Client, options: listResourceV1beta2ResourceSliceOptions) !WatchStream(root.io.k8s.api.resource.v1beta2.ResourceSlice) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/resource.k8s.io/v1beta2/resourceslices");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.resource.v1beta2.ResourceSlice){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateResourceV1beta2ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2ResourceSliceResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2ResourceSliceResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateResourceV1beta2ResourceSliceResponse = union(enum) {
        ok: CreateResourceV1beta2ResourceSliceResponse200,
        created: CreateResourceV1beta2ResourceSliceResponse201,
        accepted: CreateResourceV1beta2ResourceSliceResponse202,
        unauthorized: CreateResourceV1beta2ResourceSliceResponse401,
        default_response: CreateResourceV1beta2ResourceSliceResponseDefault,
    };

    pub const createResourceV1beta2ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a ResourceSlice
    pub fn createResourceV1beta2ResourceSlice(self: *Client, body: root.io.k8s.api.resource.v1beta2.ResourceSlice, options: createResourceV1beta2ResourceSliceOptions) !CreateResourceV1beta2ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/resourceslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta2CollectionResourceSliceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2CollectionResourceSliceResponse = union(enum) {
        ok: DeleteResourceV1beta2CollectionResourceSliceResponse200,
        unauthorized: DeleteResourceV1beta2CollectionResourceSliceResponse401,
        default_response: DeleteResourceV1beta2CollectionResourceSliceResponseDefault,
    };

    pub const deleteResourceV1beta2CollectionResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of ResourceSlice
    pub fn deleteResourceV1beta2CollectionResourceSlice(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta2CollectionResourceSliceOptions) !DeleteResourceV1beta2CollectionResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/resourceslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadResourceV1beta2ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadResourceV1beta2ResourceSliceResponse = union(enum) {
        ok: ReadResourceV1beta2ResourceSliceResponse200,
        unauthorized: ReadResourceV1beta2ResourceSliceResponse401,
        default_response: ReadResourceV1beta2ResourceSliceResponseDefault,
    };

    pub const readResourceV1beta2ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified ResourceSlice
    pub fn readResourceV1beta2ResourceSlice(self: *Client, name: []const u8, options: readResourceV1beta2ResourceSliceOptions) !ReadResourceV1beta2ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceResourceV1beta2ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2ResourceSliceResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceResourceV1beta2ResourceSliceResponse = union(enum) {
        ok: ReplaceResourceV1beta2ResourceSliceResponse200,
        created: ReplaceResourceV1beta2ResourceSliceResponse201,
        unauthorized: ReplaceResourceV1beta2ResourceSliceResponse401,
        default_response: ReplaceResourceV1beta2ResourceSliceResponseDefault,
    };

    pub const replaceResourceV1beta2ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified ResourceSlice
    pub fn replaceResourceV1beta2ResourceSlice(self: *Client, name: []const u8, body: root.io.k8s.api.resource.v1beta2.ResourceSlice, options: replaceResourceV1beta2ResourceSliceOptions) !ReplaceResourceV1beta2ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteResourceV1beta2ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2ResourceSliceResponse202 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteResourceV1beta2ResourceSliceResponse = union(enum) {
        ok: DeleteResourceV1beta2ResourceSliceResponse200,
        accepted: DeleteResourceV1beta2ResourceSliceResponse202,
        unauthorized: DeleteResourceV1beta2ResourceSliceResponse401,
        default_response: DeleteResourceV1beta2ResourceSliceResponseDefault,
    };

    pub const deleteResourceV1beta2ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a ResourceSlice
    pub fn deleteResourceV1beta2ResourceSlice(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteResourceV1beta2ResourceSliceOptions) !DeleteResourceV1beta2ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchResourceV1beta2ResourceSliceResponse200 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2ResourceSliceResponse201 = struct {
        body: root.io.k8s.api.resource.v1beta2.ResourceSlice,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchResourceV1beta2ResourceSliceResponse = union(enum) {
        ok: PatchResourceV1beta2ResourceSliceResponse200,
        created: PatchResourceV1beta2ResourceSliceResponse201,
        unauthorized: PatchResourceV1beta2ResourceSliceResponse401,
        default_response: PatchResourceV1beta2ResourceSliceResponseDefault,
    };

    pub const patchResourceV1beta2ResourceSliceOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified ResourceSlice
    pub fn patchResourceV1beta2ResourceSlice(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchResourceV1beta2ResourceSliceOptions) !PatchResourceV1beta2ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.resource.v1beta2.ResourceSlice, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta2DeviceClassListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2DeviceClassListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2DeviceClassListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2DeviceClassListResponse = union(enum) {
        ok: WatchResourceV1beta2DeviceClassListResponse200,
        unauthorized: WatchResourceV1beta2DeviceClassListResponse401,
        default_response: WatchResourceV1beta2DeviceClassListResponseDefault,
    };

    pub const watchResourceV1beta2DeviceClassListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of DeviceClass. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1beta2DeviceClassList(self: *Client, options: watchResourceV1beta2DeviceClassListOptions) !WatchResourceV1beta2DeviceClassListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/watch/deviceclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta2DeviceClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2DeviceClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2DeviceClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2DeviceClassResponse = union(enum) {
        ok: WatchResourceV1beta2DeviceClassResponse200,
        unauthorized: WatchResourceV1beta2DeviceClassResponse401,
        default_response: WatchResourceV1beta2DeviceClassResponseDefault,
    };

    pub const watchResourceV1beta2DeviceClassOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind DeviceClass. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1beta2DeviceClass(self: *Client, name: []const u8, options: watchResourceV1beta2DeviceClassOptions) !WatchResourceV1beta2DeviceClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/watch/deviceclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta2DeviceTaintRuleListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2DeviceTaintRuleListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2DeviceTaintRuleListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2DeviceTaintRuleListResponse = union(enum) {
        ok: WatchResourceV1beta2DeviceTaintRuleListResponse200,
        unauthorized: WatchResourceV1beta2DeviceTaintRuleListResponse401,
        default_response: WatchResourceV1beta2DeviceTaintRuleListResponseDefault,
    };

    pub const watchResourceV1beta2DeviceTaintRuleListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of DeviceTaintRule. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1beta2DeviceTaintRuleList(self: *Client, options: watchResourceV1beta2DeviceTaintRuleListOptions) !WatchResourceV1beta2DeviceTaintRuleListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/watch/devicetaintrules", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta2DeviceTaintRuleResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2DeviceTaintRuleResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2DeviceTaintRuleResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2DeviceTaintRuleResponse = union(enum) {
        ok: WatchResourceV1beta2DeviceTaintRuleResponse200,
        unauthorized: WatchResourceV1beta2DeviceTaintRuleResponse401,
        default_response: WatchResourceV1beta2DeviceTaintRuleResponseDefault,
    };

    pub const watchResourceV1beta2DeviceTaintRuleOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind DeviceTaintRule. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1beta2DeviceTaintRule(self: *Client, name: []const u8, options: watchResourceV1beta2DeviceTaintRuleOptions) !WatchResourceV1beta2DeviceTaintRuleResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/watch/devicetaintrules/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta2NamespacedResourceClaimListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2NamespacedResourceClaimListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2NamespacedResourceClaimListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2NamespacedResourceClaimListResponse = union(enum) {
        ok: WatchResourceV1beta2NamespacedResourceClaimListResponse200,
        unauthorized: WatchResourceV1beta2NamespacedResourceClaimListResponse401,
        default_response: WatchResourceV1beta2NamespacedResourceClaimListResponseDefault,
    };

    pub const watchResourceV1beta2NamespacedResourceClaimListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceClaim. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1beta2NamespacedResourceClaimList(self: *Client, namespace: []const u8, options: watchResourceV1beta2NamespacedResourceClaimListOptions) !WatchResourceV1beta2NamespacedResourceClaimListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta2NamespacedResourceClaimResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2NamespacedResourceClaimResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2NamespacedResourceClaimResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2NamespacedResourceClaimResponse = union(enum) {
        ok: WatchResourceV1beta2NamespacedResourceClaimResponse200,
        unauthorized: WatchResourceV1beta2NamespacedResourceClaimResponse401,
        default_response: WatchResourceV1beta2NamespacedResourceClaimResponseDefault,
    };

    pub const watchResourceV1beta2NamespacedResourceClaimOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ResourceClaim. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1beta2NamespacedResourceClaim(self: *Client, name: []const u8, namespace: []const u8, options: watchResourceV1beta2NamespacedResourceClaimOptions) !WatchResourceV1beta2NamespacedResourceClaimResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaims/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta2NamespacedResourceClaimTemplateListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2NamespacedResourceClaimTemplateListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2NamespacedResourceClaimTemplateListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2NamespacedResourceClaimTemplateListResponse = union(enum) {
        ok: WatchResourceV1beta2NamespacedResourceClaimTemplateListResponse200,
        unauthorized: WatchResourceV1beta2NamespacedResourceClaimTemplateListResponse401,
        default_response: WatchResourceV1beta2NamespacedResourceClaimTemplateListResponseDefault,
    };

    pub const watchResourceV1beta2NamespacedResourceClaimTemplateListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceClaimTemplate. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1beta2NamespacedResourceClaimTemplateList(self: *Client, namespace: []const u8, options: watchResourceV1beta2NamespacedResourceClaimTemplateListOptions) !WatchResourceV1beta2NamespacedResourceClaimTemplateListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta2NamespacedResourceClaimTemplateResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2NamespacedResourceClaimTemplateResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2NamespacedResourceClaimTemplateResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2NamespacedResourceClaimTemplateResponse = union(enum) {
        ok: WatchResourceV1beta2NamespacedResourceClaimTemplateResponse200,
        unauthorized: WatchResourceV1beta2NamespacedResourceClaimTemplateResponse401,
        default_response: WatchResourceV1beta2NamespacedResourceClaimTemplateResponseDefault,
    };

    pub const watchResourceV1beta2NamespacedResourceClaimTemplateOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ResourceClaimTemplate. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1beta2NamespacedResourceClaimTemplate(self: *Client, name: []const u8, namespace: []const u8, options: watchResourceV1beta2NamespacedResourceClaimTemplateOptions) !WatchResourceV1beta2NamespacedResourceClaimTemplateResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/resourceclaimtemplates/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta2ResourceClaimListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2ResourceClaimListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2ResourceClaimListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2ResourceClaimListForAllNamespacesResponse = union(enum) {
        ok: WatchResourceV1beta2ResourceClaimListForAllNamespacesResponse200,
        unauthorized: WatchResourceV1beta2ResourceClaimListForAllNamespacesResponse401,
        default_response: WatchResourceV1beta2ResourceClaimListForAllNamespacesResponseDefault,
    };

    pub const watchResourceV1beta2ResourceClaimListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceClaim. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1beta2ResourceClaimListForAllNamespaces(self: *Client, options: watchResourceV1beta2ResourceClaimListForAllNamespacesOptions) !WatchResourceV1beta2ResourceClaimListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/watch/resourceclaims", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta2ResourceClaimTemplateListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2ResourceClaimTemplateListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2ResourceClaimTemplateListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2ResourceClaimTemplateListForAllNamespacesResponse = union(enum) {
        ok: WatchResourceV1beta2ResourceClaimTemplateListForAllNamespacesResponse200,
        unauthorized: WatchResourceV1beta2ResourceClaimTemplateListForAllNamespacesResponse401,
        default_response: WatchResourceV1beta2ResourceClaimTemplateListForAllNamespacesResponseDefault,
    };

    pub const watchResourceV1beta2ResourceClaimTemplateListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceClaimTemplate. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1beta2ResourceClaimTemplateListForAllNamespaces(self: *Client, options: watchResourceV1beta2ResourceClaimTemplateListForAllNamespacesOptions) !WatchResourceV1beta2ResourceClaimTemplateListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/watch/resourceclaimtemplates", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta2ResourceSliceListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2ResourceSliceListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2ResourceSliceListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2ResourceSliceListResponse = union(enum) {
        ok: WatchResourceV1beta2ResourceSliceListResponse200,
        unauthorized: WatchResourceV1beta2ResourceSliceListResponse401,
        default_response: WatchResourceV1beta2ResourceSliceListResponseDefault,
    };

    pub const watchResourceV1beta2ResourceSliceListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of ResourceSlice. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchResourceV1beta2ResourceSliceList(self: *Client, options: watchResourceV1beta2ResourceSliceListOptions) !WatchResourceV1beta2ResourceSliceListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/watch/resourceslices", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchResourceV1beta2ResourceSliceResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2ResourceSliceResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2ResourceSliceResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchResourceV1beta2ResourceSliceResponse = union(enum) {
        ok: WatchResourceV1beta2ResourceSliceResponse200,
        unauthorized: WatchResourceV1beta2ResourceSliceResponse401,
        default_response: WatchResourceV1beta2ResourceSliceResponseDefault,
    };

    pub const watchResourceV1beta2ResourceSliceOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind ResourceSlice. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchResourceV1beta2ResourceSlice(self: *Client, name: []const u8, options: watchResourceV1beta2ResourceSliceOptions) !WatchResourceV1beta2ResourceSliceResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/resource.k8s.io/v1beta2/watch/resourceslices/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetSchedulingAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetSchedulingAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetSchedulingAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetSchedulingAPIGroupResponse = union(enum) {
        ok: GetSchedulingAPIGroupResponse200,
        unauthorized: GetSchedulingAPIGroupResponse401,
        default_response: GetSchedulingAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getSchedulingAPIGroup(self: *Client) !GetSchedulingAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetSchedulingV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetSchedulingV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetSchedulingV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetSchedulingV1APIResourcesResponse = union(enum) {
        ok: GetSchedulingV1APIResourcesResponse200,
        unauthorized: GetSchedulingV1APIResourcesResponse401,
        default_response: GetSchedulingV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getSchedulingV1APIResources(self: *Client) !GetSchedulingV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListSchedulingV1PriorityClassResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1.PriorityClassList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1PriorityClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1PriorityClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1PriorityClassResponse = union(enum) {
        ok: ListSchedulingV1PriorityClassResponse200,
        unauthorized: ListSchedulingV1PriorityClassResponse401,
        default_response: ListSchedulingV1PriorityClassResponseDefault,
    };

    pub const listSchedulingV1PriorityClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PriorityClass
    pub fn listSchedulingV1PriorityClass(self: *Client, options: listSchedulingV1PriorityClassOptions) !ListSchedulingV1PriorityClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1/priorityclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1.PriorityClassList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistSchedulingV1PriorityClass(self: *Client, options: listSchedulingV1PriorityClassOptions) !WatchStream(root.io.k8s.api.scheduling.v1.PriorityClass) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/scheduling.k8s.io/v1/priorityclasses");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.scheduling.v1.PriorityClass){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateSchedulingV1PriorityClassResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1.PriorityClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1PriorityClassResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1.PriorityClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1PriorityClassResponse202 = struct {
        body: root.io.k8s.api.scheduling.v1.PriorityClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1PriorityClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1PriorityClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1PriorityClassResponse = union(enum) {
        ok: CreateSchedulingV1PriorityClassResponse200,
        created: CreateSchedulingV1PriorityClassResponse201,
        accepted: CreateSchedulingV1PriorityClassResponse202,
        unauthorized: CreateSchedulingV1PriorityClassResponse401,
        default_response: CreateSchedulingV1PriorityClassResponseDefault,
    };

    pub const createSchedulingV1PriorityClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a PriorityClass
    pub fn createSchedulingV1PriorityClass(self: *Client, body: root.io.k8s.api.scheduling.v1.PriorityClass, options: createSchedulingV1PriorityClassOptions) !CreateSchedulingV1PriorityClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1/priorityclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1.PriorityClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1.PriorityClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1.PriorityClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteSchedulingV1CollectionPriorityClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1CollectionPriorityClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1CollectionPriorityClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1CollectionPriorityClassResponse = union(enum) {
        ok: DeleteSchedulingV1CollectionPriorityClassResponse200,
        unauthorized: DeleteSchedulingV1CollectionPriorityClassResponse401,
        default_response: DeleteSchedulingV1CollectionPriorityClassResponseDefault,
    };

    pub const deleteSchedulingV1CollectionPriorityClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of PriorityClass
    pub fn deleteSchedulingV1CollectionPriorityClass(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteSchedulingV1CollectionPriorityClassOptions) !DeleteSchedulingV1CollectionPriorityClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1/priorityclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadSchedulingV1PriorityClassResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1.PriorityClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1PriorityClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1PriorityClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1PriorityClassResponse = union(enum) {
        ok: ReadSchedulingV1PriorityClassResponse200,
        unauthorized: ReadSchedulingV1PriorityClassResponse401,
        default_response: ReadSchedulingV1PriorityClassResponseDefault,
    };

    pub const readSchedulingV1PriorityClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified PriorityClass
    pub fn readSchedulingV1PriorityClass(self: *Client, name: []const u8, options: readSchedulingV1PriorityClassOptions) !ReadSchedulingV1PriorityClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1/priorityclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1.PriorityClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceSchedulingV1PriorityClassResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1.PriorityClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1PriorityClassResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1.PriorityClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1PriorityClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1PriorityClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1PriorityClassResponse = union(enum) {
        ok: ReplaceSchedulingV1PriorityClassResponse200,
        created: ReplaceSchedulingV1PriorityClassResponse201,
        unauthorized: ReplaceSchedulingV1PriorityClassResponse401,
        default_response: ReplaceSchedulingV1PriorityClassResponseDefault,
    };

    pub const replaceSchedulingV1PriorityClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified PriorityClass
    pub fn replaceSchedulingV1PriorityClass(self: *Client, name: []const u8, body: root.io.k8s.api.scheduling.v1.PriorityClass, options: replaceSchedulingV1PriorityClassOptions) !ReplaceSchedulingV1PriorityClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1/priorityclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1.PriorityClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1.PriorityClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteSchedulingV1PriorityClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1PriorityClassResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1PriorityClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1PriorityClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1PriorityClassResponse = union(enum) {
        ok: DeleteSchedulingV1PriorityClassResponse200,
        accepted: DeleteSchedulingV1PriorityClassResponse202,
        unauthorized: DeleteSchedulingV1PriorityClassResponse401,
        default_response: DeleteSchedulingV1PriorityClassResponseDefault,
    };

    pub const deleteSchedulingV1PriorityClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a PriorityClass
    pub fn deleteSchedulingV1PriorityClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteSchedulingV1PriorityClassOptions) !DeleteSchedulingV1PriorityClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1/priorityclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchSchedulingV1PriorityClassResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1.PriorityClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1PriorityClassResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1.PriorityClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1PriorityClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1PriorityClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1PriorityClassResponse = union(enum) {
        ok: PatchSchedulingV1PriorityClassResponse200,
        created: PatchSchedulingV1PriorityClassResponse201,
        unauthorized: PatchSchedulingV1PriorityClassResponse401,
        default_response: PatchSchedulingV1PriorityClassResponseDefault,
    };

    pub const patchSchedulingV1PriorityClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified PriorityClass
    pub fn patchSchedulingV1PriorityClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchSchedulingV1PriorityClassOptions) !PatchSchedulingV1PriorityClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1/priorityclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1.PriorityClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1.PriorityClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1PriorityClassListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1PriorityClassListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1PriorityClassListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1PriorityClassListResponse = union(enum) {
        ok: WatchSchedulingV1PriorityClassListResponse200,
        unauthorized: WatchSchedulingV1PriorityClassListResponse401,
        default_response: WatchSchedulingV1PriorityClassListResponseDefault,
    };

    pub const watchSchedulingV1PriorityClassListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PriorityClass. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchSchedulingV1PriorityClassList(self: *Client, options: watchSchedulingV1PriorityClassListOptions) !WatchSchedulingV1PriorityClassListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1/watch/priorityclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1PriorityClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1PriorityClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1PriorityClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1PriorityClassResponse = union(enum) {
        ok: WatchSchedulingV1PriorityClassResponse200,
        unauthorized: WatchSchedulingV1PriorityClassResponse401,
        default_response: WatchSchedulingV1PriorityClassResponseDefault,
    };

    pub const watchSchedulingV1PriorityClassOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind PriorityClass. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchSchedulingV1PriorityClass(self: *Client, name: []const u8, options: watchSchedulingV1PriorityClassOptions) !WatchSchedulingV1PriorityClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1/watch/priorityclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetSchedulingV1alpha3APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetSchedulingV1alpha3APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetSchedulingV1alpha3APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetSchedulingV1alpha3APIResourcesResponse = union(enum) {
        ok: GetSchedulingV1alpha3APIResourcesResponse200,
        unauthorized: GetSchedulingV1alpha3APIResourcesResponse401,
        default_response: GetSchedulingV1alpha3APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getSchedulingV1alpha3APIResources(self: *Client) !GetSchedulingV1alpha3APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListSchedulingV1alpha3CompositePodGroupForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroupList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3CompositePodGroupForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3CompositePodGroupForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3CompositePodGroupForAllNamespacesResponse = union(enum) {
        ok: ListSchedulingV1alpha3CompositePodGroupForAllNamespacesResponse200,
        unauthorized: ListSchedulingV1alpha3CompositePodGroupForAllNamespacesResponse401,
        default_response: ListSchedulingV1alpha3CompositePodGroupForAllNamespacesResponseDefault,
    };

    pub const listSchedulingV1alpha3CompositePodGroupForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind CompositePodGroup
    pub fn listSchedulingV1alpha3CompositePodGroupForAllNamespaces(self: *Client, options: listSchedulingV1alpha3CompositePodGroupForAllNamespacesOptions) !ListSchedulingV1alpha3CompositePodGroupForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/compositepodgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroupList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistSchedulingV1alpha3CompositePodGroupForAllNamespaces(self: *Client, options: listSchedulingV1alpha3CompositePodGroupForAllNamespacesOptions) !WatchStream(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/scheduling.k8s.io/v1alpha3/compositepodgroups");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListSchedulingV1alpha3NamespacedCompositePodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroupList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3NamespacedCompositePodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3NamespacedCompositePodGroupResponse = union(enum) {
        ok: ListSchedulingV1alpha3NamespacedCompositePodGroupResponse200,
        unauthorized: ListSchedulingV1alpha3NamespacedCompositePodGroupResponse401,
        default_response: ListSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault,
    };

    pub const listSchedulingV1alpha3NamespacedCompositePodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind CompositePodGroup
    pub fn listSchedulingV1alpha3NamespacedCompositePodGroup(self: *Client, namespace: []const u8, options: listSchedulingV1alpha3NamespacedCompositePodGroupOptions) !ListSchedulingV1alpha3NamespacedCompositePodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/compositepodgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroupList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistSchedulingV1alpha3NamespacedCompositePodGroup(self: *Client, namespace: []const u8, options: listSchedulingV1alpha3NamespacedCompositePodGroupOptions) !WatchStream(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/scheduling.k8s.io/v1alpha3/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/compositepodgroups");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateSchedulingV1alpha3NamespacedCompositePodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedCompositePodGroupResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedCompositePodGroupResponse202 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedCompositePodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedCompositePodGroupResponse = union(enum) {
        ok: CreateSchedulingV1alpha3NamespacedCompositePodGroupResponse200,
        created: CreateSchedulingV1alpha3NamespacedCompositePodGroupResponse201,
        accepted: CreateSchedulingV1alpha3NamespacedCompositePodGroupResponse202,
        unauthorized: CreateSchedulingV1alpha3NamespacedCompositePodGroupResponse401,
        default_response: CreateSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault,
    };

    pub const createSchedulingV1alpha3NamespacedCompositePodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a CompositePodGroup
    pub fn createSchedulingV1alpha3NamespacedCompositePodGroup(self: *Client, namespace: []const u8, body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, options: createSchedulingV1alpha3NamespacedCompositePodGroupOptions) !CreateSchedulingV1alpha3NamespacedCompositePodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/compositepodgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteSchedulingV1alpha3CollectionNamespacedCompositePodGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3CollectionNamespacedCompositePodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3CollectionNamespacedCompositePodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3CollectionNamespacedCompositePodGroupResponse = union(enum) {
        ok: DeleteSchedulingV1alpha3CollectionNamespacedCompositePodGroupResponse200,
        unauthorized: DeleteSchedulingV1alpha3CollectionNamespacedCompositePodGroupResponse401,
        default_response: DeleteSchedulingV1alpha3CollectionNamespacedCompositePodGroupResponseDefault,
    };

    pub const deleteSchedulingV1alpha3CollectionNamespacedCompositePodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of CompositePodGroup
    pub fn deleteSchedulingV1alpha3CollectionNamespacedCompositePodGroup(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteSchedulingV1alpha3CollectionNamespacedCompositePodGroupOptions) !DeleteSchedulingV1alpha3CollectionNamespacedCompositePodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/compositepodgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadSchedulingV1alpha3NamespacedCompositePodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedCompositePodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedCompositePodGroupResponse = union(enum) {
        ok: ReadSchedulingV1alpha3NamespacedCompositePodGroupResponse200,
        unauthorized: ReadSchedulingV1alpha3NamespacedCompositePodGroupResponse401,
        default_response: ReadSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault,
    };

    pub const readSchedulingV1alpha3NamespacedCompositePodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified CompositePodGroup
    pub fn readSchedulingV1alpha3NamespacedCompositePodGroup(self: *Client, name: []const u8, namespace: []const u8, options: readSchedulingV1alpha3NamespacedCompositePodGroupOptions) !ReadSchedulingV1alpha3NamespacedCompositePodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/compositepodgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceSchedulingV1alpha3NamespacedCompositePodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedCompositePodGroupResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedCompositePodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedCompositePodGroupResponse = union(enum) {
        ok: ReplaceSchedulingV1alpha3NamespacedCompositePodGroupResponse200,
        created: ReplaceSchedulingV1alpha3NamespacedCompositePodGroupResponse201,
        unauthorized: ReplaceSchedulingV1alpha3NamespacedCompositePodGroupResponse401,
        default_response: ReplaceSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault,
    };

    pub const replaceSchedulingV1alpha3NamespacedCompositePodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified CompositePodGroup
    pub fn replaceSchedulingV1alpha3NamespacedCompositePodGroup(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, options: replaceSchedulingV1alpha3NamespacedCompositePodGroupOptions) !ReplaceSchedulingV1alpha3NamespacedCompositePodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/compositepodgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteSchedulingV1alpha3NamespacedCompositePodGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3NamespacedCompositePodGroupResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3NamespacedCompositePodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3NamespacedCompositePodGroupResponse = union(enum) {
        ok: DeleteSchedulingV1alpha3NamespacedCompositePodGroupResponse200,
        accepted: DeleteSchedulingV1alpha3NamespacedCompositePodGroupResponse202,
        unauthorized: DeleteSchedulingV1alpha3NamespacedCompositePodGroupResponse401,
        default_response: DeleteSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault,
    };

    pub const deleteSchedulingV1alpha3NamespacedCompositePodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a CompositePodGroup
    pub fn deleteSchedulingV1alpha3NamespacedCompositePodGroup(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteSchedulingV1alpha3NamespacedCompositePodGroupOptions) !DeleteSchedulingV1alpha3NamespacedCompositePodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/compositepodgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchSchedulingV1alpha3NamespacedCompositePodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedCompositePodGroupResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedCompositePodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedCompositePodGroupResponse = union(enum) {
        ok: PatchSchedulingV1alpha3NamespacedCompositePodGroupResponse200,
        created: PatchSchedulingV1alpha3NamespacedCompositePodGroupResponse201,
        unauthorized: PatchSchedulingV1alpha3NamespacedCompositePodGroupResponse401,
        default_response: PatchSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault,
    };

    pub const patchSchedulingV1alpha3NamespacedCompositePodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified CompositePodGroup
    pub fn patchSchedulingV1alpha3NamespacedCompositePodGroup(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchSchedulingV1alpha3NamespacedCompositePodGroupOptions) !PatchSchedulingV1alpha3NamespacedCompositePodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/compositepodgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedCompositePodGroupStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse = union(enum) {
        ok: ReadSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse200,
        unauthorized: ReadSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse401,
        default_response: ReadSchedulingV1alpha3NamespacedCompositePodGroupStatusResponseDefault,
    };

    pub const readSchedulingV1alpha3NamespacedCompositePodGroupStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified CompositePodGroup
    pub fn readSchedulingV1alpha3NamespacedCompositePodGroupStatus(self: *Client, name: []const u8, namespace: []const u8, options: readSchedulingV1alpha3NamespacedCompositePodGroupStatusOptions) !ReadSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/compositepodgroups/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedCompositePodGroupStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse = union(enum) {
        ok: ReplaceSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse200,
        created: ReplaceSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse201,
        unauthorized: ReplaceSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse401,
        default_response: ReplaceSchedulingV1alpha3NamespacedCompositePodGroupStatusResponseDefault,
    };

    pub const replaceSchedulingV1alpha3NamespacedCompositePodGroupStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified CompositePodGroup
    pub fn replaceSchedulingV1alpha3NamespacedCompositePodGroupStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, options: replaceSchedulingV1alpha3NamespacedCompositePodGroupStatusOptions) !ReplaceSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/compositepodgroups/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedCompositePodGroupStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse = union(enum) {
        ok: PatchSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse200,
        created: PatchSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse201,
        unauthorized: PatchSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse401,
        default_response: PatchSchedulingV1alpha3NamespacedCompositePodGroupStatusResponseDefault,
    };

    pub const patchSchedulingV1alpha3NamespacedCompositePodGroupStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified CompositePodGroup
    pub fn patchSchedulingV1alpha3NamespacedCompositePodGroupStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchSchedulingV1alpha3NamespacedCompositePodGroupStatusOptions) !PatchSchedulingV1alpha3NamespacedCompositePodGroupStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/compositepodgroups/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.CompositePodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListSchedulingV1alpha3NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroupList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3NamespacedPodGroupResponse = union(enum) {
        ok: ListSchedulingV1alpha3NamespacedPodGroupResponse200,
        unauthorized: ListSchedulingV1alpha3NamespacedPodGroupResponse401,
        default_response: ListSchedulingV1alpha3NamespacedPodGroupResponseDefault,
    };

    pub const listSchedulingV1alpha3NamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PodGroup
    pub fn listSchedulingV1alpha3NamespacedPodGroup(self: *Client, namespace: []const u8, options: listSchedulingV1alpha3NamespacedPodGroupOptions) !ListSchedulingV1alpha3NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroupList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistSchedulingV1alpha3NamespacedPodGroup(self: *Client, namespace: []const u8, options: listSchedulingV1alpha3NamespacedPodGroupOptions) !WatchStream(root.io.k8s.api.scheduling.v1alpha3.PodGroup) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/scheduling.k8s.io/v1alpha3/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/podgroups");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.scheduling.v1alpha3.PodGroup){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateSchedulingV1alpha3NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedPodGroupResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedPodGroupResponse202 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedPodGroupResponse = union(enum) {
        ok: CreateSchedulingV1alpha3NamespacedPodGroupResponse200,
        created: CreateSchedulingV1alpha3NamespacedPodGroupResponse201,
        accepted: CreateSchedulingV1alpha3NamespacedPodGroupResponse202,
        unauthorized: CreateSchedulingV1alpha3NamespacedPodGroupResponse401,
        default_response: CreateSchedulingV1alpha3NamespacedPodGroupResponseDefault,
    };

    pub const createSchedulingV1alpha3NamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a PodGroup
    pub fn createSchedulingV1alpha3NamespacedPodGroup(self: *Client, namespace: []const u8, body: root.io.k8s.api.scheduling.v1alpha3.PodGroup, options: createSchedulingV1alpha3NamespacedPodGroupOptions) !CreateSchedulingV1alpha3NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteSchedulingV1alpha3CollectionNamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3CollectionNamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3CollectionNamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3CollectionNamespacedPodGroupResponse = union(enum) {
        ok: DeleteSchedulingV1alpha3CollectionNamespacedPodGroupResponse200,
        unauthorized: DeleteSchedulingV1alpha3CollectionNamespacedPodGroupResponse401,
        default_response: DeleteSchedulingV1alpha3CollectionNamespacedPodGroupResponseDefault,
    };

    pub const deleteSchedulingV1alpha3CollectionNamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of PodGroup
    pub fn deleteSchedulingV1alpha3CollectionNamespacedPodGroup(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteSchedulingV1alpha3CollectionNamespacedPodGroupOptions) !DeleteSchedulingV1alpha3CollectionNamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadSchedulingV1alpha3NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedPodGroupResponse = union(enum) {
        ok: ReadSchedulingV1alpha3NamespacedPodGroupResponse200,
        unauthorized: ReadSchedulingV1alpha3NamespacedPodGroupResponse401,
        default_response: ReadSchedulingV1alpha3NamespacedPodGroupResponseDefault,
    };

    pub const readSchedulingV1alpha3NamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified PodGroup
    pub fn readSchedulingV1alpha3NamespacedPodGroup(self: *Client, name: []const u8, namespace: []const u8, options: readSchedulingV1alpha3NamespacedPodGroupOptions) !ReadSchedulingV1alpha3NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceSchedulingV1alpha3NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedPodGroupResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedPodGroupResponse = union(enum) {
        ok: ReplaceSchedulingV1alpha3NamespacedPodGroupResponse200,
        created: ReplaceSchedulingV1alpha3NamespacedPodGroupResponse201,
        unauthorized: ReplaceSchedulingV1alpha3NamespacedPodGroupResponse401,
        default_response: ReplaceSchedulingV1alpha3NamespacedPodGroupResponseDefault,
    };

    pub const replaceSchedulingV1alpha3NamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified PodGroup
    pub fn replaceSchedulingV1alpha3NamespacedPodGroup(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.scheduling.v1alpha3.PodGroup, options: replaceSchedulingV1alpha3NamespacedPodGroupOptions) !ReplaceSchedulingV1alpha3NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteSchedulingV1alpha3NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3NamespacedPodGroupResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3NamespacedPodGroupResponse = union(enum) {
        ok: DeleteSchedulingV1alpha3NamespacedPodGroupResponse200,
        accepted: DeleteSchedulingV1alpha3NamespacedPodGroupResponse202,
        unauthorized: DeleteSchedulingV1alpha3NamespacedPodGroupResponse401,
        default_response: DeleteSchedulingV1alpha3NamespacedPodGroupResponseDefault,
    };

    pub const deleteSchedulingV1alpha3NamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a PodGroup
    pub fn deleteSchedulingV1alpha3NamespacedPodGroup(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteSchedulingV1alpha3NamespacedPodGroupOptions) !DeleteSchedulingV1alpha3NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchSchedulingV1alpha3NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedPodGroupResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedPodGroupResponse = union(enum) {
        ok: PatchSchedulingV1alpha3NamespacedPodGroupResponse200,
        created: PatchSchedulingV1alpha3NamespacedPodGroupResponse201,
        unauthorized: PatchSchedulingV1alpha3NamespacedPodGroupResponse401,
        default_response: PatchSchedulingV1alpha3NamespacedPodGroupResponseDefault,
    };

    pub const patchSchedulingV1alpha3NamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified PodGroup
    pub fn patchSchedulingV1alpha3NamespacedPodGroup(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchSchedulingV1alpha3NamespacedPodGroupOptions) !PatchSchedulingV1alpha3NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadSchedulingV1alpha3NamespacedPodGroupStatusResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedPodGroupStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedPodGroupStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedPodGroupStatusResponse = union(enum) {
        ok: ReadSchedulingV1alpha3NamespacedPodGroupStatusResponse200,
        unauthorized: ReadSchedulingV1alpha3NamespacedPodGroupStatusResponse401,
        default_response: ReadSchedulingV1alpha3NamespacedPodGroupStatusResponseDefault,
    };

    pub const readSchedulingV1alpha3NamespacedPodGroupStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified PodGroup
    pub fn readSchedulingV1alpha3NamespacedPodGroupStatus(self: *Client, name: []const u8, namespace: []const u8, options: readSchedulingV1alpha3NamespacedPodGroupStatusOptions) !ReadSchedulingV1alpha3NamespacedPodGroupStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceSchedulingV1alpha3NamespacedPodGroupStatusResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedPodGroupStatusResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedPodGroupStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedPodGroupStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedPodGroupStatusResponse = union(enum) {
        ok: ReplaceSchedulingV1alpha3NamespacedPodGroupStatusResponse200,
        created: ReplaceSchedulingV1alpha3NamespacedPodGroupStatusResponse201,
        unauthorized: ReplaceSchedulingV1alpha3NamespacedPodGroupStatusResponse401,
        default_response: ReplaceSchedulingV1alpha3NamespacedPodGroupStatusResponseDefault,
    };

    pub const replaceSchedulingV1alpha3NamespacedPodGroupStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified PodGroup
    pub fn replaceSchedulingV1alpha3NamespacedPodGroupStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.scheduling.v1alpha3.PodGroup, options: replaceSchedulingV1alpha3NamespacedPodGroupStatusOptions) !ReplaceSchedulingV1alpha3NamespacedPodGroupStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchSchedulingV1alpha3NamespacedPodGroupStatusResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedPodGroupStatusResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedPodGroupStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedPodGroupStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedPodGroupStatusResponse = union(enum) {
        ok: PatchSchedulingV1alpha3NamespacedPodGroupStatusResponse200,
        created: PatchSchedulingV1alpha3NamespacedPodGroupStatusResponse201,
        unauthorized: PatchSchedulingV1alpha3NamespacedPodGroupStatusResponse401,
        default_response: PatchSchedulingV1alpha3NamespacedPodGroupStatusResponseDefault,
    };

    pub const patchSchedulingV1alpha3NamespacedPodGroupStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified PodGroup
    pub fn patchSchedulingV1alpha3NamespacedPodGroupStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchSchedulingV1alpha3NamespacedPodGroupStatusOptions) !PatchSchedulingV1alpha3NamespacedPodGroupStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListSchedulingV1alpha3NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.WorkloadList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3NamespacedWorkloadResponse = union(enum) {
        ok: ListSchedulingV1alpha3NamespacedWorkloadResponse200,
        unauthorized: ListSchedulingV1alpha3NamespacedWorkloadResponse401,
        default_response: ListSchedulingV1alpha3NamespacedWorkloadResponseDefault,
    };

    pub const listSchedulingV1alpha3NamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Workload
    pub fn listSchedulingV1alpha3NamespacedWorkload(self: *Client, namespace: []const u8, options: listSchedulingV1alpha3NamespacedWorkloadOptions) !ListSchedulingV1alpha3NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.WorkloadList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistSchedulingV1alpha3NamespacedWorkload(self: *Client, namespace: []const u8, options: listSchedulingV1alpha3NamespacedWorkloadOptions) !WatchStream(root.io.k8s.api.scheduling.v1alpha3.Workload) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/scheduling.k8s.io/v1alpha3/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/workloads");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.scheduling.v1alpha3.Workload){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateSchedulingV1alpha3NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedWorkloadResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedWorkloadResponse202 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1alpha3NamespacedWorkloadResponse = union(enum) {
        ok: CreateSchedulingV1alpha3NamespacedWorkloadResponse200,
        created: CreateSchedulingV1alpha3NamespacedWorkloadResponse201,
        accepted: CreateSchedulingV1alpha3NamespacedWorkloadResponse202,
        unauthorized: CreateSchedulingV1alpha3NamespacedWorkloadResponse401,
        default_response: CreateSchedulingV1alpha3NamespacedWorkloadResponseDefault,
    };

    pub const createSchedulingV1alpha3NamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a Workload
    pub fn createSchedulingV1alpha3NamespacedWorkload(self: *Client, namespace: []const u8, body: root.io.k8s.api.scheduling.v1alpha3.Workload, options: createSchedulingV1alpha3NamespacedWorkloadOptions) !CreateSchedulingV1alpha3NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteSchedulingV1alpha3CollectionNamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3CollectionNamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3CollectionNamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3CollectionNamespacedWorkloadResponse = union(enum) {
        ok: DeleteSchedulingV1alpha3CollectionNamespacedWorkloadResponse200,
        unauthorized: DeleteSchedulingV1alpha3CollectionNamespacedWorkloadResponse401,
        default_response: DeleteSchedulingV1alpha3CollectionNamespacedWorkloadResponseDefault,
    };

    pub const deleteSchedulingV1alpha3CollectionNamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Workload
    pub fn deleteSchedulingV1alpha3CollectionNamespacedWorkload(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteSchedulingV1alpha3CollectionNamespacedWorkloadOptions) !DeleteSchedulingV1alpha3CollectionNamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadSchedulingV1alpha3NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1alpha3NamespacedWorkloadResponse = union(enum) {
        ok: ReadSchedulingV1alpha3NamespacedWorkloadResponse200,
        unauthorized: ReadSchedulingV1alpha3NamespacedWorkloadResponse401,
        default_response: ReadSchedulingV1alpha3NamespacedWorkloadResponseDefault,
    };

    pub const readSchedulingV1alpha3NamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Workload
    pub fn readSchedulingV1alpha3NamespacedWorkload(self: *Client, name: []const u8, namespace: []const u8, options: readSchedulingV1alpha3NamespacedWorkloadOptions) !ReadSchedulingV1alpha3NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceSchedulingV1alpha3NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedWorkloadResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1alpha3NamespacedWorkloadResponse = union(enum) {
        ok: ReplaceSchedulingV1alpha3NamespacedWorkloadResponse200,
        created: ReplaceSchedulingV1alpha3NamespacedWorkloadResponse201,
        unauthorized: ReplaceSchedulingV1alpha3NamespacedWorkloadResponse401,
        default_response: ReplaceSchedulingV1alpha3NamespacedWorkloadResponseDefault,
    };

    pub const replaceSchedulingV1alpha3NamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Workload
    pub fn replaceSchedulingV1alpha3NamespacedWorkload(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.scheduling.v1alpha3.Workload, options: replaceSchedulingV1alpha3NamespacedWorkloadOptions) !ReplaceSchedulingV1alpha3NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteSchedulingV1alpha3NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3NamespacedWorkloadResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1alpha3NamespacedWorkloadResponse = union(enum) {
        ok: DeleteSchedulingV1alpha3NamespacedWorkloadResponse200,
        accepted: DeleteSchedulingV1alpha3NamespacedWorkloadResponse202,
        unauthorized: DeleteSchedulingV1alpha3NamespacedWorkloadResponse401,
        default_response: DeleteSchedulingV1alpha3NamespacedWorkloadResponseDefault,
    };

    pub const deleteSchedulingV1alpha3NamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a Workload
    pub fn deleteSchedulingV1alpha3NamespacedWorkload(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteSchedulingV1alpha3NamespacedWorkloadOptions) !DeleteSchedulingV1alpha3NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchSchedulingV1alpha3NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedWorkloadResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1alpha3NamespacedWorkloadResponse = union(enum) {
        ok: PatchSchedulingV1alpha3NamespacedWorkloadResponse200,
        created: PatchSchedulingV1alpha3NamespacedWorkloadResponse201,
        unauthorized: PatchSchedulingV1alpha3NamespacedWorkloadResponse401,
        default_response: PatchSchedulingV1alpha3NamespacedWorkloadResponseDefault,
    };

    pub const patchSchedulingV1alpha3NamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Workload
    pub fn patchSchedulingV1alpha3NamespacedWorkload(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchSchedulingV1alpha3NamespacedWorkloadOptions) !PatchSchedulingV1alpha3NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListSchedulingV1alpha3PodGroupForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.PodGroupList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3PodGroupForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3PodGroupForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3PodGroupForAllNamespacesResponse = union(enum) {
        ok: ListSchedulingV1alpha3PodGroupForAllNamespacesResponse200,
        unauthorized: ListSchedulingV1alpha3PodGroupForAllNamespacesResponse401,
        default_response: ListSchedulingV1alpha3PodGroupForAllNamespacesResponseDefault,
    };

    pub const listSchedulingV1alpha3PodGroupForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PodGroup
    pub fn listSchedulingV1alpha3PodGroupForAllNamespaces(self: *Client, options: listSchedulingV1alpha3PodGroupForAllNamespacesOptions) !ListSchedulingV1alpha3PodGroupForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/podgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.PodGroupList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistSchedulingV1alpha3PodGroupForAllNamespaces(self: *Client, options: listSchedulingV1alpha3PodGroupForAllNamespacesOptions) !WatchStream(root.io.k8s.api.scheduling.v1alpha3.PodGroup) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/scheduling.k8s.io/v1alpha3/podgroups");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.scheduling.v1alpha3.PodGroup){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const WatchSchedulingV1alpha3CompositePodGroupListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3CompositePodGroupListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3CompositePodGroupListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3CompositePodGroupListForAllNamespacesResponse = union(enum) {
        ok: WatchSchedulingV1alpha3CompositePodGroupListForAllNamespacesResponse200,
        unauthorized: WatchSchedulingV1alpha3CompositePodGroupListForAllNamespacesResponse401,
        default_response: WatchSchedulingV1alpha3CompositePodGroupListForAllNamespacesResponseDefault,
    };

    pub const watchSchedulingV1alpha3CompositePodGroupListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of CompositePodGroup. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchSchedulingV1alpha3CompositePodGroupListForAllNamespaces(self: *Client, options: watchSchedulingV1alpha3CompositePodGroupListForAllNamespacesOptions) !WatchSchedulingV1alpha3CompositePodGroupListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/watch/compositepodgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1alpha3NamespacedCompositePodGroupListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedCompositePodGroupListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedCompositePodGroupListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedCompositePodGroupListResponse = union(enum) {
        ok: WatchSchedulingV1alpha3NamespacedCompositePodGroupListResponse200,
        unauthorized: WatchSchedulingV1alpha3NamespacedCompositePodGroupListResponse401,
        default_response: WatchSchedulingV1alpha3NamespacedCompositePodGroupListResponseDefault,
    };

    pub const watchSchedulingV1alpha3NamespacedCompositePodGroupListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of CompositePodGroup. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchSchedulingV1alpha3NamespacedCompositePodGroupList(self: *Client, namespace: []const u8, options: watchSchedulingV1alpha3NamespacedCompositePodGroupListOptions) !WatchSchedulingV1alpha3NamespacedCompositePodGroupListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/compositepodgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1alpha3NamespacedCompositePodGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedCompositePodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedCompositePodGroupResponse = union(enum) {
        ok: WatchSchedulingV1alpha3NamespacedCompositePodGroupResponse200,
        unauthorized: WatchSchedulingV1alpha3NamespacedCompositePodGroupResponse401,
        default_response: WatchSchedulingV1alpha3NamespacedCompositePodGroupResponseDefault,
    };

    pub const watchSchedulingV1alpha3NamespacedCompositePodGroupOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind CompositePodGroup. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchSchedulingV1alpha3NamespacedCompositePodGroup(self: *Client, name: []const u8, namespace: []const u8, options: watchSchedulingV1alpha3NamespacedCompositePodGroupOptions) !WatchSchedulingV1alpha3NamespacedCompositePodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/compositepodgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1alpha3NamespacedPodGroupListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedPodGroupListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedPodGroupListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedPodGroupListResponse = union(enum) {
        ok: WatchSchedulingV1alpha3NamespacedPodGroupListResponse200,
        unauthorized: WatchSchedulingV1alpha3NamespacedPodGroupListResponse401,
        default_response: WatchSchedulingV1alpha3NamespacedPodGroupListResponseDefault,
    };

    pub const watchSchedulingV1alpha3NamespacedPodGroupListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PodGroup. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchSchedulingV1alpha3NamespacedPodGroupList(self: *Client, namespace: []const u8, options: watchSchedulingV1alpha3NamespacedPodGroupListOptions) !WatchSchedulingV1alpha3NamespacedPodGroupListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1alpha3NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedPodGroupResponse = union(enum) {
        ok: WatchSchedulingV1alpha3NamespacedPodGroupResponse200,
        unauthorized: WatchSchedulingV1alpha3NamespacedPodGroupResponse401,
        default_response: WatchSchedulingV1alpha3NamespacedPodGroupResponseDefault,
    };

    pub const watchSchedulingV1alpha3NamespacedPodGroupOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind PodGroup. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchSchedulingV1alpha3NamespacedPodGroup(self: *Client, name: []const u8, namespace: []const u8, options: watchSchedulingV1alpha3NamespacedPodGroupOptions) !WatchSchedulingV1alpha3NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1alpha3NamespacedWorkloadListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedWorkloadListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedWorkloadListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedWorkloadListResponse = union(enum) {
        ok: WatchSchedulingV1alpha3NamespacedWorkloadListResponse200,
        unauthorized: WatchSchedulingV1alpha3NamespacedWorkloadListResponse401,
        default_response: WatchSchedulingV1alpha3NamespacedWorkloadListResponseDefault,
    };

    pub const watchSchedulingV1alpha3NamespacedWorkloadListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Workload. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchSchedulingV1alpha3NamespacedWorkloadList(self: *Client, namespace: []const u8, options: watchSchedulingV1alpha3NamespacedWorkloadListOptions) !WatchSchedulingV1alpha3NamespacedWorkloadListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1alpha3NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3NamespacedWorkloadResponse = union(enum) {
        ok: WatchSchedulingV1alpha3NamespacedWorkloadResponse200,
        unauthorized: WatchSchedulingV1alpha3NamespacedWorkloadResponse401,
        default_response: WatchSchedulingV1alpha3NamespacedWorkloadResponseDefault,
    };

    pub const watchSchedulingV1alpha3NamespacedWorkloadOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Workload. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchSchedulingV1alpha3NamespacedWorkload(self: *Client, name: []const u8, namespace: []const u8, options: watchSchedulingV1alpha3NamespacedWorkloadOptions) !WatchSchedulingV1alpha3NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1alpha3PodGroupListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3PodGroupListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3PodGroupListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3PodGroupListForAllNamespacesResponse = union(enum) {
        ok: WatchSchedulingV1alpha3PodGroupListForAllNamespacesResponse200,
        unauthorized: WatchSchedulingV1alpha3PodGroupListForAllNamespacesResponse401,
        default_response: WatchSchedulingV1alpha3PodGroupListForAllNamespacesResponseDefault,
    };

    pub const watchSchedulingV1alpha3PodGroupListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PodGroup. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchSchedulingV1alpha3PodGroupListForAllNamespaces(self: *Client, options: watchSchedulingV1alpha3PodGroupListForAllNamespacesOptions) !WatchSchedulingV1alpha3PodGroupListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/watch/podgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1alpha3WorkloadListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3WorkloadListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3WorkloadListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1alpha3WorkloadListForAllNamespacesResponse = union(enum) {
        ok: WatchSchedulingV1alpha3WorkloadListForAllNamespacesResponse200,
        unauthorized: WatchSchedulingV1alpha3WorkloadListForAllNamespacesResponse401,
        default_response: WatchSchedulingV1alpha3WorkloadListForAllNamespacesResponseDefault,
    };

    pub const watchSchedulingV1alpha3WorkloadListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Workload. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchSchedulingV1alpha3WorkloadListForAllNamespaces(self: *Client, options: watchSchedulingV1alpha3WorkloadListForAllNamespacesOptions) !WatchSchedulingV1alpha3WorkloadListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/watch/workloads", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListSchedulingV1alpha3WorkloadForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1alpha3.WorkloadList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3WorkloadForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3WorkloadForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1alpha3WorkloadForAllNamespacesResponse = union(enum) {
        ok: ListSchedulingV1alpha3WorkloadForAllNamespacesResponse200,
        unauthorized: ListSchedulingV1alpha3WorkloadForAllNamespacesResponse401,
        default_response: ListSchedulingV1alpha3WorkloadForAllNamespacesResponseDefault,
    };

    pub const listSchedulingV1alpha3WorkloadForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Workload
    pub fn listSchedulingV1alpha3WorkloadForAllNamespaces(self: *Client, options: listSchedulingV1alpha3WorkloadForAllNamespacesOptions) !ListSchedulingV1alpha3WorkloadForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1alpha3/workloads", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1alpha3.WorkloadList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistSchedulingV1alpha3WorkloadForAllNamespaces(self: *Client, options: listSchedulingV1alpha3WorkloadForAllNamespacesOptions) !WatchStream(root.io.k8s.api.scheduling.v1alpha3.Workload) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/scheduling.k8s.io/v1alpha3/workloads");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.scheduling.v1alpha3.Workload){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const GetSchedulingV1beta1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetSchedulingV1beta1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetSchedulingV1beta1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetSchedulingV1beta1APIResourcesResponse = union(enum) {
        ok: GetSchedulingV1beta1APIResourcesResponse200,
        unauthorized: GetSchedulingV1beta1APIResourcesResponse401,
        default_response: GetSchedulingV1beta1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getSchedulingV1beta1APIResources(self: *Client) !GetSchedulingV1beta1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListSchedulingV1beta1NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroupList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1beta1NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1beta1NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1beta1NamespacedPodGroupResponse = union(enum) {
        ok: ListSchedulingV1beta1NamespacedPodGroupResponse200,
        unauthorized: ListSchedulingV1beta1NamespacedPodGroupResponse401,
        default_response: ListSchedulingV1beta1NamespacedPodGroupResponseDefault,
    };

    pub const listSchedulingV1beta1NamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PodGroup
    pub fn listSchedulingV1beta1NamespacedPodGroup(self: *Client, namespace: []const u8, options: listSchedulingV1beta1NamespacedPodGroupOptions) !ListSchedulingV1beta1NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroupList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistSchedulingV1beta1NamespacedPodGroup(self: *Client, namespace: []const u8, options: listSchedulingV1beta1NamespacedPodGroupOptions) !WatchStream(root.io.k8s.api.scheduling.v1beta1.PodGroup) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/scheduling.k8s.io/v1beta1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/podgroups");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.scheduling.v1beta1.PodGroup){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateSchedulingV1beta1NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1beta1NamespacedPodGroupResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1beta1NamespacedPodGroupResponse202 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1beta1NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1beta1NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1beta1NamespacedPodGroupResponse = union(enum) {
        ok: CreateSchedulingV1beta1NamespacedPodGroupResponse200,
        created: CreateSchedulingV1beta1NamespacedPodGroupResponse201,
        accepted: CreateSchedulingV1beta1NamespacedPodGroupResponse202,
        unauthorized: CreateSchedulingV1beta1NamespacedPodGroupResponse401,
        default_response: CreateSchedulingV1beta1NamespacedPodGroupResponseDefault,
    };

    pub const createSchedulingV1beta1NamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a PodGroup
    pub fn createSchedulingV1beta1NamespacedPodGroup(self: *Client, namespace: []const u8, body: root.io.k8s.api.scheduling.v1beta1.PodGroup, options: createSchedulingV1beta1NamespacedPodGroupOptions) !CreateSchedulingV1beta1NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteSchedulingV1beta1CollectionNamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1CollectionNamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1CollectionNamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1CollectionNamespacedPodGroupResponse = union(enum) {
        ok: DeleteSchedulingV1beta1CollectionNamespacedPodGroupResponse200,
        unauthorized: DeleteSchedulingV1beta1CollectionNamespacedPodGroupResponse401,
        default_response: DeleteSchedulingV1beta1CollectionNamespacedPodGroupResponseDefault,
    };

    pub const deleteSchedulingV1beta1CollectionNamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of PodGroup
    pub fn deleteSchedulingV1beta1CollectionNamespacedPodGroup(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteSchedulingV1beta1CollectionNamespacedPodGroupOptions) !DeleteSchedulingV1beta1CollectionNamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadSchedulingV1beta1NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1beta1NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1beta1NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1beta1NamespacedPodGroupResponse = union(enum) {
        ok: ReadSchedulingV1beta1NamespacedPodGroupResponse200,
        unauthorized: ReadSchedulingV1beta1NamespacedPodGroupResponse401,
        default_response: ReadSchedulingV1beta1NamespacedPodGroupResponseDefault,
    };

    pub const readSchedulingV1beta1NamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified PodGroup
    pub fn readSchedulingV1beta1NamespacedPodGroup(self: *Client, name: []const u8, namespace: []const u8, options: readSchedulingV1beta1NamespacedPodGroupOptions) !ReadSchedulingV1beta1NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceSchedulingV1beta1NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1beta1NamespacedPodGroupResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1beta1NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1beta1NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1beta1NamespacedPodGroupResponse = union(enum) {
        ok: ReplaceSchedulingV1beta1NamespacedPodGroupResponse200,
        created: ReplaceSchedulingV1beta1NamespacedPodGroupResponse201,
        unauthorized: ReplaceSchedulingV1beta1NamespacedPodGroupResponse401,
        default_response: ReplaceSchedulingV1beta1NamespacedPodGroupResponseDefault,
    };

    pub const replaceSchedulingV1beta1NamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified PodGroup
    pub fn replaceSchedulingV1beta1NamespacedPodGroup(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.scheduling.v1beta1.PodGroup, options: replaceSchedulingV1beta1NamespacedPodGroupOptions) !ReplaceSchedulingV1beta1NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteSchedulingV1beta1NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1NamespacedPodGroupResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1NamespacedPodGroupResponse = union(enum) {
        ok: DeleteSchedulingV1beta1NamespacedPodGroupResponse200,
        accepted: DeleteSchedulingV1beta1NamespacedPodGroupResponse202,
        unauthorized: DeleteSchedulingV1beta1NamespacedPodGroupResponse401,
        default_response: DeleteSchedulingV1beta1NamespacedPodGroupResponseDefault,
    };

    pub const deleteSchedulingV1beta1NamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a PodGroup
    pub fn deleteSchedulingV1beta1NamespacedPodGroup(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteSchedulingV1beta1NamespacedPodGroupOptions) !DeleteSchedulingV1beta1NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchSchedulingV1beta1NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1beta1NamespacedPodGroupResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1beta1NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1beta1NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1beta1NamespacedPodGroupResponse = union(enum) {
        ok: PatchSchedulingV1beta1NamespacedPodGroupResponse200,
        created: PatchSchedulingV1beta1NamespacedPodGroupResponse201,
        unauthorized: PatchSchedulingV1beta1NamespacedPodGroupResponse401,
        default_response: PatchSchedulingV1beta1NamespacedPodGroupResponseDefault,
    };

    pub const patchSchedulingV1beta1NamespacedPodGroupOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified PodGroup
    pub fn patchSchedulingV1beta1NamespacedPodGroup(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchSchedulingV1beta1NamespacedPodGroupOptions) !PatchSchedulingV1beta1NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadSchedulingV1beta1NamespacedPodGroupStatusResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1beta1NamespacedPodGroupStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1beta1NamespacedPodGroupStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1beta1NamespacedPodGroupStatusResponse = union(enum) {
        ok: ReadSchedulingV1beta1NamespacedPodGroupStatusResponse200,
        unauthorized: ReadSchedulingV1beta1NamespacedPodGroupStatusResponse401,
        default_response: ReadSchedulingV1beta1NamespacedPodGroupStatusResponseDefault,
    };

    pub const readSchedulingV1beta1NamespacedPodGroupStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified PodGroup
    pub fn readSchedulingV1beta1NamespacedPodGroupStatus(self: *Client, name: []const u8, namespace: []const u8, options: readSchedulingV1beta1NamespacedPodGroupStatusOptions) !ReadSchedulingV1beta1NamespacedPodGroupStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceSchedulingV1beta1NamespacedPodGroupStatusResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1beta1NamespacedPodGroupStatusResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1beta1NamespacedPodGroupStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1beta1NamespacedPodGroupStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1beta1NamespacedPodGroupStatusResponse = union(enum) {
        ok: ReplaceSchedulingV1beta1NamespacedPodGroupStatusResponse200,
        created: ReplaceSchedulingV1beta1NamespacedPodGroupStatusResponse201,
        unauthorized: ReplaceSchedulingV1beta1NamespacedPodGroupStatusResponse401,
        default_response: ReplaceSchedulingV1beta1NamespacedPodGroupStatusResponseDefault,
    };

    pub const replaceSchedulingV1beta1NamespacedPodGroupStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified PodGroup
    pub fn replaceSchedulingV1beta1NamespacedPodGroupStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.scheduling.v1beta1.PodGroup, options: replaceSchedulingV1beta1NamespacedPodGroupStatusOptions) !ReplaceSchedulingV1beta1NamespacedPodGroupStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchSchedulingV1beta1NamespacedPodGroupStatusResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1beta1NamespacedPodGroupStatusResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1beta1NamespacedPodGroupStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1beta1NamespacedPodGroupStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1beta1NamespacedPodGroupStatusResponse = union(enum) {
        ok: PatchSchedulingV1beta1NamespacedPodGroupStatusResponse200,
        created: PatchSchedulingV1beta1NamespacedPodGroupStatusResponse201,
        unauthorized: PatchSchedulingV1beta1NamespacedPodGroupStatusResponse401,
        default_response: PatchSchedulingV1beta1NamespacedPodGroupStatusResponseDefault,
    };

    pub const patchSchedulingV1beta1NamespacedPodGroupStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified PodGroup
    pub fn patchSchedulingV1beta1NamespacedPodGroupStatus(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchSchedulingV1beta1NamespacedPodGroupStatusOptions) !PatchSchedulingV1beta1NamespacedPodGroupStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListSchedulingV1beta1NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.WorkloadList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1beta1NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1beta1NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1beta1NamespacedWorkloadResponse = union(enum) {
        ok: ListSchedulingV1beta1NamespacedWorkloadResponse200,
        unauthorized: ListSchedulingV1beta1NamespacedWorkloadResponse401,
        default_response: ListSchedulingV1beta1NamespacedWorkloadResponseDefault,
    };

    pub const listSchedulingV1beta1NamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Workload
    pub fn listSchedulingV1beta1NamespacedWorkload(self: *Client, namespace: []const u8, options: listSchedulingV1beta1NamespacedWorkloadOptions) !ListSchedulingV1beta1NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.WorkloadList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistSchedulingV1beta1NamespacedWorkload(self: *Client, namespace: []const u8, options: listSchedulingV1beta1NamespacedWorkloadOptions) !WatchStream(root.io.k8s.api.scheduling.v1beta1.Workload) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/scheduling.k8s.io/v1beta1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/workloads");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.scheduling.v1beta1.Workload){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateSchedulingV1beta1NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1beta1NamespacedWorkloadResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1beta1NamespacedWorkloadResponse202 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1beta1NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1beta1NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateSchedulingV1beta1NamespacedWorkloadResponse = union(enum) {
        ok: CreateSchedulingV1beta1NamespacedWorkloadResponse200,
        created: CreateSchedulingV1beta1NamespacedWorkloadResponse201,
        accepted: CreateSchedulingV1beta1NamespacedWorkloadResponse202,
        unauthorized: CreateSchedulingV1beta1NamespacedWorkloadResponse401,
        default_response: CreateSchedulingV1beta1NamespacedWorkloadResponseDefault,
    };

    pub const createSchedulingV1beta1NamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a Workload
    pub fn createSchedulingV1beta1NamespacedWorkload(self: *Client, namespace: []const u8, body: root.io.k8s.api.scheduling.v1beta1.Workload, options: createSchedulingV1beta1NamespacedWorkloadOptions) !CreateSchedulingV1beta1NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteSchedulingV1beta1CollectionNamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1CollectionNamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1CollectionNamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1CollectionNamespacedWorkloadResponse = union(enum) {
        ok: DeleteSchedulingV1beta1CollectionNamespacedWorkloadResponse200,
        unauthorized: DeleteSchedulingV1beta1CollectionNamespacedWorkloadResponse401,
        default_response: DeleteSchedulingV1beta1CollectionNamespacedWorkloadResponseDefault,
    };

    pub const deleteSchedulingV1beta1CollectionNamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of Workload
    pub fn deleteSchedulingV1beta1CollectionNamespacedWorkload(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteSchedulingV1beta1CollectionNamespacedWorkloadOptions) !DeleteSchedulingV1beta1CollectionNamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadSchedulingV1beta1NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1beta1NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1beta1NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadSchedulingV1beta1NamespacedWorkloadResponse = union(enum) {
        ok: ReadSchedulingV1beta1NamespacedWorkloadResponse200,
        unauthorized: ReadSchedulingV1beta1NamespacedWorkloadResponse401,
        default_response: ReadSchedulingV1beta1NamespacedWorkloadResponseDefault,
    };

    pub const readSchedulingV1beta1NamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified Workload
    pub fn readSchedulingV1beta1NamespacedWorkload(self: *Client, name: []const u8, namespace: []const u8, options: readSchedulingV1beta1NamespacedWorkloadOptions) !ReadSchedulingV1beta1NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceSchedulingV1beta1NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1beta1NamespacedWorkloadResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1beta1NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1beta1NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceSchedulingV1beta1NamespacedWorkloadResponse = union(enum) {
        ok: ReplaceSchedulingV1beta1NamespacedWorkloadResponse200,
        created: ReplaceSchedulingV1beta1NamespacedWorkloadResponse201,
        unauthorized: ReplaceSchedulingV1beta1NamespacedWorkloadResponse401,
        default_response: ReplaceSchedulingV1beta1NamespacedWorkloadResponseDefault,
    };

    pub const replaceSchedulingV1beta1NamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified Workload
    pub fn replaceSchedulingV1beta1NamespacedWorkload(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.scheduling.v1beta1.Workload, options: replaceSchedulingV1beta1NamespacedWorkloadOptions) !ReplaceSchedulingV1beta1NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteSchedulingV1beta1NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1NamespacedWorkloadResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteSchedulingV1beta1NamespacedWorkloadResponse = union(enum) {
        ok: DeleteSchedulingV1beta1NamespacedWorkloadResponse200,
        accepted: DeleteSchedulingV1beta1NamespacedWorkloadResponse202,
        unauthorized: DeleteSchedulingV1beta1NamespacedWorkloadResponse401,
        default_response: DeleteSchedulingV1beta1NamespacedWorkloadResponseDefault,
    };

    pub const deleteSchedulingV1beta1NamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a Workload
    pub fn deleteSchedulingV1beta1NamespacedWorkload(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteSchedulingV1beta1NamespacedWorkloadOptions) !DeleteSchedulingV1beta1NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchSchedulingV1beta1NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1beta1NamespacedWorkloadResponse201 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.Workload,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1beta1NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1beta1NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchSchedulingV1beta1NamespacedWorkloadResponse = union(enum) {
        ok: PatchSchedulingV1beta1NamespacedWorkloadResponse200,
        created: PatchSchedulingV1beta1NamespacedWorkloadResponse201,
        unauthorized: PatchSchedulingV1beta1NamespacedWorkloadResponse401,
        default_response: PatchSchedulingV1beta1NamespacedWorkloadResponseDefault,
    };

    pub const patchSchedulingV1beta1NamespacedWorkloadOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified Workload
    pub fn patchSchedulingV1beta1NamespacedWorkload(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchSchedulingV1beta1NamespacedWorkloadOptions) !PatchSchedulingV1beta1NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.Workload, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListSchedulingV1beta1PodGroupForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.PodGroupList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1beta1PodGroupForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1beta1PodGroupForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1beta1PodGroupForAllNamespacesResponse = union(enum) {
        ok: ListSchedulingV1beta1PodGroupForAllNamespacesResponse200,
        unauthorized: ListSchedulingV1beta1PodGroupForAllNamespacesResponse401,
        default_response: ListSchedulingV1beta1PodGroupForAllNamespacesResponseDefault,
    };

    pub const listSchedulingV1beta1PodGroupForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind PodGroup
    pub fn listSchedulingV1beta1PodGroupForAllNamespaces(self: *Client, options: listSchedulingV1beta1PodGroupForAllNamespacesOptions) !ListSchedulingV1beta1PodGroupForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/podgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.PodGroupList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistSchedulingV1beta1PodGroupForAllNamespaces(self: *Client, options: listSchedulingV1beta1PodGroupForAllNamespacesOptions) !WatchStream(root.io.k8s.api.scheduling.v1beta1.PodGroup) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/scheduling.k8s.io/v1beta1/podgroups");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.scheduling.v1beta1.PodGroup){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const WatchSchedulingV1beta1NamespacedPodGroupListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1NamespacedPodGroupListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1NamespacedPodGroupListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1NamespacedPodGroupListResponse = union(enum) {
        ok: WatchSchedulingV1beta1NamespacedPodGroupListResponse200,
        unauthorized: WatchSchedulingV1beta1NamespacedPodGroupListResponse401,
        default_response: WatchSchedulingV1beta1NamespacedPodGroupListResponseDefault,
    };

    pub const watchSchedulingV1beta1NamespacedPodGroupListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PodGroup. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchSchedulingV1beta1NamespacedPodGroupList(self: *Client, namespace: []const u8, options: watchSchedulingV1beta1NamespacedPodGroupListOptions) !WatchSchedulingV1beta1NamespacedPodGroupListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1beta1NamespacedPodGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1NamespacedPodGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1NamespacedPodGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1NamespacedPodGroupResponse = union(enum) {
        ok: WatchSchedulingV1beta1NamespacedPodGroupResponse200,
        unauthorized: WatchSchedulingV1beta1NamespacedPodGroupResponse401,
        default_response: WatchSchedulingV1beta1NamespacedPodGroupResponseDefault,
    };

    pub const watchSchedulingV1beta1NamespacedPodGroupOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind PodGroup. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchSchedulingV1beta1NamespacedPodGroup(self: *Client, name: []const u8, namespace: []const u8, options: watchSchedulingV1beta1NamespacedPodGroupOptions) !WatchSchedulingV1beta1NamespacedPodGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/podgroups/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1beta1NamespacedWorkloadListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1NamespacedWorkloadListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1NamespacedWorkloadListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1NamespacedWorkloadListResponse = union(enum) {
        ok: WatchSchedulingV1beta1NamespacedWorkloadListResponse200,
        unauthorized: WatchSchedulingV1beta1NamespacedWorkloadListResponse401,
        default_response: WatchSchedulingV1beta1NamespacedWorkloadListResponseDefault,
    };

    pub const watchSchedulingV1beta1NamespacedWorkloadListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Workload. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchSchedulingV1beta1NamespacedWorkloadList(self: *Client, namespace: []const u8, options: watchSchedulingV1beta1NamespacedWorkloadListOptions) !WatchSchedulingV1beta1NamespacedWorkloadListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1beta1NamespacedWorkloadResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1NamespacedWorkloadResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1NamespacedWorkloadResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1NamespacedWorkloadResponse = union(enum) {
        ok: WatchSchedulingV1beta1NamespacedWorkloadResponse200,
        unauthorized: WatchSchedulingV1beta1NamespacedWorkloadResponse401,
        default_response: WatchSchedulingV1beta1NamespacedWorkloadResponseDefault,
    };

    pub const watchSchedulingV1beta1NamespacedWorkloadOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind Workload. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchSchedulingV1beta1NamespacedWorkload(self: *Client, name: []const u8, namespace: []const u8, options: watchSchedulingV1beta1NamespacedWorkloadOptions) !WatchSchedulingV1beta1NamespacedWorkloadResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/workloads/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1beta1PodGroupListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1PodGroupListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1PodGroupListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1PodGroupListForAllNamespacesResponse = union(enum) {
        ok: WatchSchedulingV1beta1PodGroupListForAllNamespacesResponse200,
        unauthorized: WatchSchedulingV1beta1PodGroupListForAllNamespacesResponse401,
        default_response: WatchSchedulingV1beta1PodGroupListForAllNamespacesResponseDefault,
    };

    pub const watchSchedulingV1beta1PodGroupListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of PodGroup. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchSchedulingV1beta1PodGroupListForAllNamespaces(self: *Client, options: watchSchedulingV1beta1PodGroupListForAllNamespacesOptions) !WatchSchedulingV1beta1PodGroupListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/watch/podgroups", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchSchedulingV1beta1WorkloadListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1WorkloadListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1WorkloadListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchSchedulingV1beta1WorkloadListForAllNamespacesResponse = union(enum) {
        ok: WatchSchedulingV1beta1WorkloadListForAllNamespacesResponse200,
        unauthorized: WatchSchedulingV1beta1WorkloadListForAllNamespacesResponse401,
        default_response: WatchSchedulingV1beta1WorkloadListForAllNamespacesResponseDefault,
    };

    pub const watchSchedulingV1beta1WorkloadListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of Workload. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchSchedulingV1beta1WorkloadListForAllNamespaces(self: *Client, options: watchSchedulingV1beta1WorkloadListForAllNamespacesOptions) !WatchSchedulingV1beta1WorkloadListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/watch/workloads", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListSchedulingV1beta1WorkloadForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.scheduling.v1beta1.WorkloadList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1beta1WorkloadForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1beta1WorkloadForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListSchedulingV1beta1WorkloadForAllNamespacesResponse = union(enum) {
        ok: ListSchedulingV1beta1WorkloadForAllNamespacesResponse200,
        unauthorized: ListSchedulingV1beta1WorkloadForAllNamespacesResponse401,
        default_response: ListSchedulingV1beta1WorkloadForAllNamespacesResponseDefault,
    };

    pub const listSchedulingV1beta1WorkloadForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind Workload
    pub fn listSchedulingV1beta1WorkloadForAllNamespaces(self: *Client, options: listSchedulingV1beta1WorkloadForAllNamespacesOptions) !ListSchedulingV1beta1WorkloadForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/scheduling.k8s.io/v1beta1/workloads", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.scheduling.v1beta1.WorkloadList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistSchedulingV1beta1WorkloadForAllNamespaces(self: *Client, options: listSchedulingV1beta1WorkloadForAllNamespacesOptions) !WatchStream(root.io.k8s.api.scheduling.v1beta1.Workload) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/scheduling.k8s.io/v1beta1/workloads");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.scheduling.v1beta1.Workload){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const GetStorageAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStorageAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStorageAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStorageAPIGroupResponse = union(enum) {
        ok: GetStorageAPIGroupResponse200,
        unauthorized: GetStorageAPIGroupResponse401,
        default_response: GetStorageAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getStorageAPIGroup(self: *Client) !GetStorageAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetStorageV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStorageV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStorageV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStorageV1APIResourcesResponse = union(enum) {
        ok: GetStorageV1APIResourcesResponse200,
        unauthorized: GetStorageV1APIResourcesResponse401,
        default_response: GetStorageV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getStorageV1APIResources(self: *Client) !GetStorageV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListStorageV1CSIDriverResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSIDriverList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1CSIDriverResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1CSIDriverResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1CSIDriverResponse = union(enum) {
        ok: ListStorageV1CSIDriverResponse200,
        unauthorized: ListStorageV1CSIDriverResponse401,
        default_response: ListStorageV1CSIDriverResponseDefault,
    };

    pub const listStorageV1CSIDriverOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind CSIDriver
    pub fn listStorageV1CSIDriver(self: *Client, options: listStorageV1CSIDriverOptions) !ListStorageV1CSIDriverResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csidrivers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIDriverList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistStorageV1CSIDriver(self: *Client, options: listStorageV1CSIDriverOptions) !WatchStream(root.io.k8s.api.storage.v1.CSIDriver) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/storage.k8s.io/v1/csidrivers");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.storage.v1.CSIDriver){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateStorageV1CSIDriverResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSIDriver,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1CSIDriverResponse201 = struct {
        body: root.io.k8s.api.storage.v1.CSIDriver,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1CSIDriverResponse202 = struct {
        body: root.io.k8s.api.storage.v1.CSIDriver,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1CSIDriverResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1CSIDriverResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1CSIDriverResponse = union(enum) {
        ok: CreateStorageV1CSIDriverResponse200,
        created: CreateStorageV1CSIDriverResponse201,
        accepted: CreateStorageV1CSIDriverResponse202,
        unauthorized: CreateStorageV1CSIDriverResponse401,
        default_response: CreateStorageV1CSIDriverResponseDefault,
    };

    pub const createStorageV1CSIDriverOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a CSIDriver
    pub fn createStorageV1CSIDriver(self: *Client, body: root.io.k8s.api.storage.v1.CSIDriver, options: createStorageV1CSIDriverOptions) !CreateStorageV1CSIDriverResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csidrivers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIDriver, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIDriver, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIDriver, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStorageV1CollectionCSIDriverResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionCSIDriverResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionCSIDriverResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionCSIDriverResponse = union(enum) {
        ok: DeleteStorageV1CollectionCSIDriverResponse200,
        unauthorized: DeleteStorageV1CollectionCSIDriverResponse401,
        default_response: DeleteStorageV1CollectionCSIDriverResponseDefault,
    };

    pub const deleteStorageV1CollectionCSIDriverOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of CSIDriver
    pub fn deleteStorageV1CollectionCSIDriver(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStorageV1CollectionCSIDriverOptions) !DeleteStorageV1CollectionCSIDriverResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csidrivers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadStorageV1CSIDriverResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSIDriver,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1CSIDriverResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1CSIDriverResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1CSIDriverResponse = union(enum) {
        ok: ReadStorageV1CSIDriverResponse200,
        unauthorized: ReadStorageV1CSIDriverResponse401,
        default_response: ReadStorageV1CSIDriverResponseDefault,
    };

    pub const readStorageV1CSIDriverOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified CSIDriver
    pub fn readStorageV1CSIDriver(self: *Client, name: []const u8, options: readStorageV1CSIDriverOptions) !ReadStorageV1CSIDriverResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csidrivers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIDriver, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceStorageV1CSIDriverResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSIDriver,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1CSIDriverResponse201 = struct {
        body: root.io.k8s.api.storage.v1.CSIDriver,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1CSIDriverResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1CSIDriverResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1CSIDriverResponse = union(enum) {
        ok: ReplaceStorageV1CSIDriverResponse200,
        created: ReplaceStorageV1CSIDriverResponse201,
        unauthorized: ReplaceStorageV1CSIDriverResponse401,
        default_response: ReplaceStorageV1CSIDriverResponseDefault,
    };

    pub const replaceStorageV1CSIDriverOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified CSIDriver
    pub fn replaceStorageV1CSIDriver(self: *Client, name: []const u8, body: root.io.k8s.api.storage.v1.CSIDriver, options: replaceStorageV1CSIDriverOptions) !ReplaceStorageV1CSIDriverResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csidrivers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIDriver, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIDriver, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStorageV1CSIDriverResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSIDriver,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CSIDriverResponse202 = struct {
        body: root.io.k8s.api.storage.v1.CSIDriver,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CSIDriverResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CSIDriverResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CSIDriverResponse = union(enum) {
        ok: DeleteStorageV1CSIDriverResponse200,
        accepted: DeleteStorageV1CSIDriverResponse202,
        unauthorized: DeleteStorageV1CSIDriverResponse401,
        default_response: DeleteStorageV1CSIDriverResponseDefault,
    };

    pub const deleteStorageV1CSIDriverOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a CSIDriver
    pub fn deleteStorageV1CSIDriver(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStorageV1CSIDriverOptions) !DeleteStorageV1CSIDriverResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csidrivers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIDriver, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIDriver, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchStorageV1CSIDriverResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSIDriver,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1CSIDriverResponse201 = struct {
        body: root.io.k8s.api.storage.v1.CSIDriver,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1CSIDriverResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1CSIDriverResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1CSIDriverResponse = union(enum) {
        ok: PatchStorageV1CSIDriverResponse200,
        created: PatchStorageV1CSIDriverResponse201,
        unauthorized: PatchStorageV1CSIDriverResponse401,
        default_response: PatchStorageV1CSIDriverResponseDefault,
    };

    pub const patchStorageV1CSIDriverOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified CSIDriver
    pub fn patchStorageV1CSIDriver(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchStorageV1CSIDriverOptions) !PatchStorageV1CSIDriverResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csidrivers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIDriver, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIDriver, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListStorageV1CSINodeResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSINodeList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1CSINodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1CSINodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1CSINodeResponse = union(enum) {
        ok: ListStorageV1CSINodeResponse200,
        unauthorized: ListStorageV1CSINodeResponse401,
        default_response: ListStorageV1CSINodeResponseDefault,
    };

    pub const listStorageV1CSINodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind CSINode
    pub fn listStorageV1CSINode(self: *Client, options: listStorageV1CSINodeOptions) !ListStorageV1CSINodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csinodes", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSINodeList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistStorageV1CSINode(self: *Client, options: listStorageV1CSINodeOptions) !WatchStream(root.io.k8s.api.storage.v1.CSINode) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/storage.k8s.io/v1/csinodes");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.storage.v1.CSINode){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateStorageV1CSINodeResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSINode,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1CSINodeResponse201 = struct {
        body: root.io.k8s.api.storage.v1.CSINode,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1CSINodeResponse202 = struct {
        body: root.io.k8s.api.storage.v1.CSINode,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1CSINodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1CSINodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1CSINodeResponse = union(enum) {
        ok: CreateStorageV1CSINodeResponse200,
        created: CreateStorageV1CSINodeResponse201,
        accepted: CreateStorageV1CSINodeResponse202,
        unauthorized: CreateStorageV1CSINodeResponse401,
        default_response: CreateStorageV1CSINodeResponseDefault,
    };

    pub const createStorageV1CSINodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a CSINode
    pub fn createStorageV1CSINode(self: *Client, body: root.io.k8s.api.storage.v1.CSINode, options: createStorageV1CSINodeOptions) !CreateStorageV1CSINodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csinodes", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSINode, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSINode, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSINode, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStorageV1CollectionCSINodeResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionCSINodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionCSINodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionCSINodeResponse = union(enum) {
        ok: DeleteStorageV1CollectionCSINodeResponse200,
        unauthorized: DeleteStorageV1CollectionCSINodeResponse401,
        default_response: DeleteStorageV1CollectionCSINodeResponseDefault,
    };

    pub const deleteStorageV1CollectionCSINodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of CSINode
    pub fn deleteStorageV1CollectionCSINode(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStorageV1CollectionCSINodeOptions) !DeleteStorageV1CollectionCSINodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csinodes", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadStorageV1CSINodeResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSINode,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1CSINodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1CSINodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1CSINodeResponse = union(enum) {
        ok: ReadStorageV1CSINodeResponse200,
        unauthorized: ReadStorageV1CSINodeResponse401,
        default_response: ReadStorageV1CSINodeResponseDefault,
    };

    pub const readStorageV1CSINodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified CSINode
    pub fn readStorageV1CSINode(self: *Client, name: []const u8, options: readStorageV1CSINodeOptions) !ReadStorageV1CSINodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csinodes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSINode, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceStorageV1CSINodeResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSINode,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1CSINodeResponse201 = struct {
        body: root.io.k8s.api.storage.v1.CSINode,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1CSINodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1CSINodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1CSINodeResponse = union(enum) {
        ok: ReplaceStorageV1CSINodeResponse200,
        created: ReplaceStorageV1CSINodeResponse201,
        unauthorized: ReplaceStorageV1CSINodeResponse401,
        default_response: ReplaceStorageV1CSINodeResponseDefault,
    };

    pub const replaceStorageV1CSINodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified CSINode
    pub fn replaceStorageV1CSINode(self: *Client, name: []const u8, body: root.io.k8s.api.storage.v1.CSINode, options: replaceStorageV1CSINodeOptions) !ReplaceStorageV1CSINodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csinodes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSINode, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSINode, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStorageV1CSINodeResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSINode,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CSINodeResponse202 = struct {
        body: root.io.k8s.api.storage.v1.CSINode,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CSINodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CSINodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CSINodeResponse = union(enum) {
        ok: DeleteStorageV1CSINodeResponse200,
        accepted: DeleteStorageV1CSINodeResponse202,
        unauthorized: DeleteStorageV1CSINodeResponse401,
        default_response: DeleteStorageV1CSINodeResponseDefault,
    };

    pub const deleteStorageV1CSINodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a CSINode
    pub fn deleteStorageV1CSINode(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStorageV1CSINodeOptions) !DeleteStorageV1CSINodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csinodes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSINode, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSINode, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchStorageV1CSINodeResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSINode,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1CSINodeResponse201 = struct {
        body: root.io.k8s.api.storage.v1.CSINode,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1CSINodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1CSINodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1CSINodeResponse = union(enum) {
        ok: PatchStorageV1CSINodeResponse200,
        created: PatchStorageV1CSINodeResponse201,
        unauthorized: PatchStorageV1CSINodeResponse401,
        default_response: PatchStorageV1CSINodeResponseDefault,
    };

    pub const patchStorageV1CSINodeOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified CSINode
    pub fn patchStorageV1CSINode(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchStorageV1CSINodeOptions) !PatchStorageV1CSINodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csinodes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSINode, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSINode, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListStorageV1CSIStorageCapacityForAllNamespacesResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSIStorageCapacityList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1CSIStorageCapacityForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1CSIStorageCapacityForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1CSIStorageCapacityForAllNamespacesResponse = union(enum) {
        ok: ListStorageV1CSIStorageCapacityForAllNamespacesResponse200,
        unauthorized: ListStorageV1CSIStorageCapacityForAllNamespacesResponse401,
        default_response: ListStorageV1CSIStorageCapacityForAllNamespacesResponseDefault,
    };

    pub const listStorageV1CSIStorageCapacityForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind CSIStorageCapacity
    pub fn listStorageV1CSIStorageCapacityForAllNamespaces(self: *Client, options: listStorageV1CSIStorageCapacityForAllNamespacesOptions) !ListStorageV1CSIStorageCapacityForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/csistoragecapacities", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIStorageCapacityList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistStorageV1CSIStorageCapacityForAllNamespaces(self: *Client, options: listStorageV1CSIStorageCapacityForAllNamespacesOptions) !WatchStream(root.io.k8s.api.storage.v1.CSIStorageCapacity) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/storage.k8s.io/v1/csistoragecapacities");
        try url_w.writeAll("?watch=true");
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.storage.v1.CSIStorageCapacity){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const ListStorageV1NamespacedCSIStorageCapacityResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSIStorageCapacityList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1NamespacedCSIStorageCapacityResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1NamespacedCSIStorageCapacityResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1NamespacedCSIStorageCapacityResponse = union(enum) {
        ok: ListStorageV1NamespacedCSIStorageCapacityResponse200,
        unauthorized: ListStorageV1NamespacedCSIStorageCapacityResponse401,
        default_response: ListStorageV1NamespacedCSIStorageCapacityResponseDefault,
    };

    pub const listStorageV1NamespacedCSIStorageCapacityOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind CSIStorageCapacity
    pub fn listStorageV1NamespacedCSIStorageCapacity(self: *Client, namespace: []const u8, options: listStorageV1NamespacedCSIStorageCapacityOptions) !ListStorageV1NamespacedCSIStorageCapacityResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/csistoragecapacities", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIStorageCapacityList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistStorageV1NamespacedCSIStorageCapacity(self: *Client, namespace: []const u8, options: listStorageV1NamespacedCSIStorageCapacityOptions) !WatchStream(root.io.k8s.api.storage.v1.CSIStorageCapacity) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/storage.k8s.io/v1/namespaces/");
        try url_w.print("{any}", .{namespace});
        try url_w.writeAll("/csistoragecapacities");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.storage.v1.CSIStorageCapacity){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateStorageV1NamespacedCSIStorageCapacityResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSIStorageCapacity,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1NamespacedCSIStorageCapacityResponse201 = struct {
        body: root.io.k8s.api.storage.v1.CSIStorageCapacity,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1NamespacedCSIStorageCapacityResponse202 = struct {
        body: root.io.k8s.api.storage.v1.CSIStorageCapacity,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1NamespacedCSIStorageCapacityResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1NamespacedCSIStorageCapacityResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1NamespacedCSIStorageCapacityResponse = union(enum) {
        ok: CreateStorageV1NamespacedCSIStorageCapacityResponse200,
        created: CreateStorageV1NamespacedCSIStorageCapacityResponse201,
        accepted: CreateStorageV1NamespacedCSIStorageCapacityResponse202,
        unauthorized: CreateStorageV1NamespacedCSIStorageCapacityResponse401,
        default_response: CreateStorageV1NamespacedCSIStorageCapacityResponseDefault,
    };

    pub const createStorageV1NamespacedCSIStorageCapacityOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a CSIStorageCapacity
    pub fn createStorageV1NamespacedCSIStorageCapacity(self: *Client, namespace: []const u8, body: root.io.k8s.api.storage.v1.CSIStorageCapacity, options: createStorageV1NamespacedCSIStorageCapacityOptions) !CreateStorageV1NamespacedCSIStorageCapacityResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/csistoragecapacities", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIStorageCapacity, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIStorageCapacity, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIStorageCapacity, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStorageV1CollectionNamespacedCSIStorageCapacityResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionNamespacedCSIStorageCapacityResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionNamespacedCSIStorageCapacityResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionNamespacedCSIStorageCapacityResponse = union(enum) {
        ok: DeleteStorageV1CollectionNamespacedCSIStorageCapacityResponse200,
        unauthorized: DeleteStorageV1CollectionNamespacedCSIStorageCapacityResponse401,
        default_response: DeleteStorageV1CollectionNamespacedCSIStorageCapacityResponseDefault,
    };

    pub const deleteStorageV1CollectionNamespacedCSIStorageCapacityOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of CSIStorageCapacity
    pub fn deleteStorageV1CollectionNamespacedCSIStorageCapacity(self: *Client, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStorageV1CollectionNamespacedCSIStorageCapacityOptions) !DeleteStorageV1CollectionNamespacedCSIStorageCapacityResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/csistoragecapacities", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadStorageV1NamespacedCSIStorageCapacityResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSIStorageCapacity,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1NamespacedCSIStorageCapacityResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1NamespacedCSIStorageCapacityResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1NamespacedCSIStorageCapacityResponse = union(enum) {
        ok: ReadStorageV1NamespacedCSIStorageCapacityResponse200,
        unauthorized: ReadStorageV1NamespacedCSIStorageCapacityResponse401,
        default_response: ReadStorageV1NamespacedCSIStorageCapacityResponseDefault,
    };

    pub const readStorageV1NamespacedCSIStorageCapacityOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified CSIStorageCapacity
    pub fn readStorageV1NamespacedCSIStorageCapacity(self: *Client, name: []const u8, namespace: []const u8, options: readStorageV1NamespacedCSIStorageCapacityOptions) !ReadStorageV1NamespacedCSIStorageCapacityResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/csistoragecapacities/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIStorageCapacity, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceStorageV1NamespacedCSIStorageCapacityResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSIStorageCapacity,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1NamespacedCSIStorageCapacityResponse201 = struct {
        body: root.io.k8s.api.storage.v1.CSIStorageCapacity,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1NamespacedCSIStorageCapacityResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1NamespacedCSIStorageCapacityResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1NamespacedCSIStorageCapacityResponse = union(enum) {
        ok: ReplaceStorageV1NamespacedCSIStorageCapacityResponse200,
        created: ReplaceStorageV1NamespacedCSIStorageCapacityResponse201,
        unauthorized: ReplaceStorageV1NamespacedCSIStorageCapacityResponse401,
        default_response: ReplaceStorageV1NamespacedCSIStorageCapacityResponseDefault,
    };

    pub const replaceStorageV1NamespacedCSIStorageCapacityOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified CSIStorageCapacity
    pub fn replaceStorageV1NamespacedCSIStorageCapacity(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.api.storage.v1.CSIStorageCapacity, options: replaceStorageV1NamespacedCSIStorageCapacityOptions) !ReplaceStorageV1NamespacedCSIStorageCapacityResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/csistoragecapacities/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIStorageCapacity, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIStorageCapacity, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStorageV1NamespacedCSIStorageCapacityResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1NamespacedCSIStorageCapacityResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1NamespacedCSIStorageCapacityResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1NamespacedCSIStorageCapacityResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1NamespacedCSIStorageCapacityResponse = union(enum) {
        ok: DeleteStorageV1NamespacedCSIStorageCapacityResponse200,
        accepted: DeleteStorageV1NamespacedCSIStorageCapacityResponse202,
        unauthorized: DeleteStorageV1NamespacedCSIStorageCapacityResponse401,
        default_response: DeleteStorageV1NamespacedCSIStorageCapacityResponseDefault,
    };

    pub const deleteStorageV1NamespacedCSIStorageCapacityOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a CSIStorageCapacity
    pub fn deleteStorageV1NamespacedCSIStorageCapacity(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStorageV1NamespacedCSIStorageCapacityOptions) !DeleteStorageV1NamespacedCSIStorageCapacityResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/csistoragecapacities/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchStorageV1NamespacedCSIStorageCapacityResponse200 = struct {
        body: root.io.k8s.api.storage.v1.CSIStorageCapacity,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1NamespacedCSIStorageCapacityResponse201 = struct {
        body: root.io.k8s.api.storage.v1.CSIStorageCapacity,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1NamespacedCSIStorageCapacityResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1NamespacedCSIStorageCapacityResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1NamespacedCSIStorageCapacityResponse = union(enum) {
        ok: PatchStorageV1NamespacedCSIStorageCapacityResponse200,
        created: PatchStorageV1NamespacedCSIStorageCapacityResponse201,
        unauthorized: PatchStorageV1NamespacedCSIStorageCapacityResponse401,
        default_response: PatchStorageV1NamespacedCSIStorageCapacityResponseDefault,
    };

    pub const patchStorageV1NamespacedCSIStorageCapacityOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified CSIStorageCapacity
    pub fn patchStorageV1NamespacedCSIStorageCapacity(self: *Client, name: []const u8, namespace: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchStorageV1NamespacedCSIStorageCapacityOptions) !PatchStorageV1NamespacedCSIStorageCapacityResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/csistoragecapacities/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIStorageCapacity, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.CSIStorageCapacity, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListStorageV1StorageClassResponse200 = struct {
        body: root.io.k8s.api.storage.v1.StorageClassList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1StorageClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1StorageClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1StorageClassResponse = union(enum) {
        ok: ListStorageV1StorageClassResponse200,
        unauthorized: ListStorageV1StorageClassResponse401,
        default_response: ListStorageV1StorageClassResponseDefault,
    };

    pub const listStorageV1StorageClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind StorageClass
    pub fn listStorageV1StorageClass(self: *Client, options: listStorageV1StorageClassOptions) !ListStorageV1StorageClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/storageclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.StorageClassList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistStorageV1StorageClass(self: *Client, options: listStorageV1StorageClassOptions) !WatchStream(root.io.k8s.api.storage.v1.StorageClass) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/storage.k8s.io/v1/storageclasses");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.storage.v1.StorageClass){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateStorageV1StorageClassResponse200 = struct {
        body: root.io.k8s.api.storage.v1.StorageClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1StorageClassResponse201 = struct {
        body: root.io.k8s.api.storage.v1.StorageClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1StorageClassResponse202 = struct {
        body: root.io.k8s.api.storage.v1.StorageClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1StorageClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1StorageClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1StorageClassResponse = union(enum) {
        ok: CreateStorageV1StorageClassResponse200,
        created: CreateStorageV1StorageClassResponse201,
        accepted: CreateStorageV1StorageClassResponse202,
        unauthorized: CreateStorageV1StorageClassResponse401,
        default_response: CreateStorageV1StorageClassResponseDefault,
    };

    pub const createStorageV1StorageClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a StorageClass
    pub fn createStorageV1StorageClass(self: *Client, body: root.io.k8s.api.storage.v1.StorageClass, options: createStorageV1StorageClassOptions) !CreateStorageV1StorageClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/storageclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.StorageClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.StorageClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.StorageClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStorageV1CollectionStorageClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionStorageClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionStorageClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionStorageClassResponse = union(enum) {
        ok: DeleteStorageV1CollectionStorageClassResponse200,
        unauthorized: DeleteStorageV1CollectionStorageClassResponse401,
        default_response: DeleteStorageV1CollectionStorageClassResponseDefault,
    };

    pub const deleteStorageV1CollectionStorageClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of StorageClass
    pub fn deleteStorageV1CollectionStorageClass(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStorageV1CollectionStorageClassOptions) !DeleteStorageV1CollectionStorageClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/storageclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadStorageV1StorageClassResponse200 = struct {
        body: root.io.k8s.api.storage.v1.StorageClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1StorageClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1StorageClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1StorageClassResponse = union(enum) {
        ok: ReadStorageV1StorageClassResponse200,
        unauthorized: ReadStorageV1StorageClassResponse401,
        default_response: ReadStorageV1StorageClassResponseDefault,
    };

    pub const readStorageV1StorageClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified StorageClass
    pub fn readStorageV1StorageClass(self: *Client, name: []const u8, options: readStorageV1StorageClassOptions) !ReadStorageV1StorageClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/storageclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.StorageClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceStorageV1StorageClassResponse200 = struct {
        body: root.io.k8s.api.storage.v1.StorageClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1StorageClassResponse201 = struct {
        body: root.io.k8s.api.storage.v1.StorageClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1StorageClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1StorageClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1StorageClassResponse = union(enum) {
        ok: ReplaceStorageV1StorageClassResponse200,
        created: ReplaceStorageV1StorageClassResponse201,
        unauthorized: ReplaceStorageV1StorageClassResponse401,
        default_response: ReplaceStorageV1StorageClassResponseDefault,
    };

    pub const replaceStorageV1StorageClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified StorageClass
    pub fn replaceStorageV1StorageClass(self: *Client, name: []const u8, body: root.io.k8s.api.storage.v1.StorageClass, options: replaceStorageV1StorageClassOptions) !ReplaceStorageV1StorageClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/storageclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.StorageClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.StorageClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStorageV1StorageClassResponse200 = struct {
        body: root.io.k8s.api.storage.v1.StorageClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1StorageClassResponse202 = struct {
        body: root.io.k8s.api.storage.v1.StorageClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1StorageClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1StorageClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1StorageClassResponse = union(enum) {
        ok: DeleteStorageV1StorageClassResponse200,
        accepted: DeleteStorageV1StorageClassResponse202,
        unauthorized: DeleteStorageV1StorageClassResponse401,
        default_response: DeleteStorageV1StorageClassResponseDefault,
    };

    pub const deleteStorageV1StorageClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a StorageClass
    pub fn deleteStorageV1StorageClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStorageV1StorageClassOptions) !DeleteStorageV1StorageClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/storageclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.StorageClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.StorageClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchStorageV1StorageClassResponse200 = struct {
        body: root.io.k8s.api.storage.v1.StorageClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1StorageClassResponse201 = struct {
        body: root.io.k8s.api.storage.v1.StorageClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1StorageClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1StorageClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1StorageClassResponse = union(enum) {
        ok: PatchStorageV1StorageClassResponse200,
        created: PatchStorageV1StorageClassResponse201,
        unauthorized: PatchStorageV1StorageClassResponse401,
        default_response: PatchStorageV1StorageClassResponseDefault,
    };

    pub const patchStorageV1StorageClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified StorageClass
    pub fn patchStorageV1StorageClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchStorageV1StorageClassOptions) !PatchStorageV1StorageClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/storageclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.StorageClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.StorageClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListStorageV1VolumeAttachmentResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachmentList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1VolumeAttachmentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1VolumeAttachmentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1VolumeAttachmentResponse = union(enum) {
        ok: ListStorageV1VolumeAttachmentResponse200,
        unauthorized: ListStorageV1VolumeAttachmentResponse401,
        default_response: ListStorageV1VolumeAttachmentResponseDefault,
    };

    pub const listStorageV1VolumeAttachmentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind VolumeAttachment
    pub fn listStorageV1VolumeAttachment(self: *Client, options: listStorageV1VolumeAttachmentOptions) !ListStorageV1VolumeAttachmentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattachments", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachmentList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistStorageV1VolumeAttachment(self: *Client, options: listStorageV1VolumeAttachmentOptions) !WatchStream(root.io.k8s.api.storage.v1.VolumeAttachment) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/storage.k8s.io/v1/volumeattachments");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.storage.v1.VolumeAttachment){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateStorageV1VolumeAttachmentResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1VolumeAttachmentResponse201 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1VolumeAttachmentResponse202 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1VolumeAttachmentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1VolumeAttachmentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1VolumeAttachmentResponse = union(enum) {
        ok: CreateStorageV1VolumeAttachmentResponse200,
        created: CreateStorageV1VolumeAttachmentResponse201,
        accepted: CreateStorageV1VolumeAttachmentResponse202,
        unauthorized: CreateStorageV1VolumeAttachmentResponse401,
        default_response: CreateStorageV1VolumeAttachmentResponseDefault,
    };

    pub const createStorageV1VolumeAttachmentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a VolumeAttachment
    pub fn createStorageV1VolumeAttachment(self: *Client, body: root.io.k8s.api.storage.v1.VolumeAttachment, options: createStorageV1VolumeAttachmentOptions) !CreateStorageV1VolumeAttachmentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattachments", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStorageV1CollectionVolumeAttachmentResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionVolumeAttachmentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionVolumeAttachmentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionVolumeAttachmentResponse = union(enum) {
        ok: DeleteStorageV1CollectionVolumeAttachmentResponse200,
        unauthorized: DeleteStorageV1CollectionVolumeAttachmentResponse401,
        default_response: DeleteStorageV1CollectionVolumeAttachmentResponseDefault,
    };

    pub const deleteStorageV1CollectionVolumeAttachmentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of VolumeAttachment
    pub fn deleteStorageV1CollectionVolumeAttachment(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStorageV1CollectionVolumeAttachmentOptions) !DeleteStorageV1CollectionVolumeAttachmentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattachments", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadStorageV1VolumeAttachmentResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1VolumeAttachmentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1VolumeAttachmentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1VolumeAttachmentResponse = union(enum) {
        ok: ReadStorageV1VolumeAttachmentResponse200,
        unauthorized: ReadStorageV1VolumeAttachmentResponse401,
        default_response: ReadStorageV1VolumeAttachmentResponseDefault,
    };

    pub const readStorageV1VolumeAttachmentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified VolumeAttachment
    pub fn readStorageV1VolumeAttachment(self: *Client, name: []const u8, options: readStorageV1VolumeAttachmentOptions) !ReadStorageV1VolumeAttachmentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattachments/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceStorageV1VolumeAttachmentResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1VolumeAttachmentResponse201 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1VolumeAttachmentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1VolumeAttachmentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1VolumeAttachmentResponse = union(enum) {
        ok: ReplaceStorageV1VolumeAttachmentResponse200,
        created: ReplaceStorageV1VolumeAttachmentResponse201,
        unauthorized: ReplaceStorageV1VolumeAttachmentResponse401,
        default_response: ReplaceStorageV1VolumeAttachmentResponseDefault,
    };

    pub const replaceStorageV1VolumeAttachmentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified VolumeAttachment
    pub fn replaceStorageV1VolumeAttachment(self: *Client, name: []const u8, body: root.io.k8s.api.storage.v1.VolumeAttachment, options: replaceStorageV1VolumeAttachmentOptions) !ReplaceStorageV1VolumeAttachmentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattachments/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStorageV1VolumeAttachmentResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1VolumeAttachmentResponse202 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1VolumeAttachmentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1VolumeAttachmentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1VolumeAttachmentResponse = union(enum) {
        ok: DeleteStorageV1VolumeAttachmentResponse200,
        accepted: DeleteStorageV1VolumeAttachmentResponse202,
        unauthorized: DeleteStorageV1VolumeAttachmentResponse401,
        default_response: DeleteStorageV1VolumeAttachmentResponseDefault,
    };

    pub const deleteStorageV1VolumeAttachmentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a VolumeAttachment
    pub fn deleteStorageV1VolumeAttachment(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStorageV1VolumeAttachmentOptions) !DeleteStorageV1VolumeAttachmentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattachments/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchStorageV1VolumeAttachmentResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1VolumeAttachmentResponse201 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1VolumeAttachmentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1VolumeAttachmentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1VolumeAttachmentResponse = union(enum) {
        ok: PatchStorageV1VolumeAttachmentResponse200,
        created: PatchStorageV1VolumeAttachmentResponse201,
        unauthorized: PatchStorageV1VolumeAttachmentResponse401,
        default_response: PatchStorageV1VolumeAttachmentResponseDefault,
    };

    pub const patchStorageV1VolumeAttachmentOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified VolumeAttachment
    pub fn patchStorageV1VolumeAttachment(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchStorageV1VolumeAttachmentOptions) !PatchStorageV1VolumeAttachmentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattachments/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadStorageV1VolumeAttachmentStatusResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1VolumeAttachmentStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1VolumeAttachmentStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1VolumeAttachmentStatusResponse = union(enum) {
        ok: ReadStorageV1VolumeAttachmentStatusResponse200,
        unauthorized: ReadStorageV1VolumeAttachmentStatusResponse401,
        default_response: ReadStorageV1VolumeAttachmentStatusResponseDefault,
    };

    pub const readStorageV1VolumeAttachmentStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified VolumeAttachment
    pub fn readStorageV1VolumeAttachmentStatus(self: *Client, name: []const u8, options: readStorageV1VolumeAttachmentStatusOptions) !ReadStorageV1VolumeAttachmentStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattachments/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceStorageV1VolumeAttachmentStatusResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1VolumeAttachmentStatusResponse201 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1VolumeAttachmentStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1VolumeAttachmentStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1VolumeAttachmentStatusResponse = union(enum) {
        ok: ReplaceStorageV1VolumeAttachmentStatusResponse200,
        created: ReplaceStorageV1VolumeAttachmentStatusResponse201,
        unauthorized: ReplaceStorageV1VolumeAttachmentStatusResponse401,
        default_response: ReplaceStorageV1VolumeAttachmentStatusResponseDefault,
    };

    pub const replaceStorageV1VolumeAttachmentStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified VolumeAttachment
    pub fn replaceStorageV1VolumeAttachmentStatus(self: *Client, name: []const u8, body: root.io.k8s.api.storage.v1.VolumeAttachment, options: replaceStorageV1VolumeAttachmentStatusOptions) !ReplaceStorageV1VolumeAttachmentStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattachments/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchStorageV1VolumeAttachmentStatusResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1VolumeAttachmentStatusResponse201 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttachment,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1VolumeAttachmentStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1VolumeAttachmentStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1VolumeAttachmentStatusResponse = union(enum) {
        ok: PatchStorageV1VolumeAttachmentStatusResponse200,
        created: PatchStorageV1VolumeAttachmentStatusResponse201,
        unauthorized: PatchStorageV1VolumeAttachmentStatusResponse401,
        default_response: PatchStorageV1VolumeAttachmentStatusResponseDefault,
    };

    pub const patchStorageV1VolumeAttachmentStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified VolumeAttachment
    pub fn patchStorageV1VolumeAttachmentStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchStorageV1VolumeAttachmentStatusOptions) !PatchStorageV1VolumeAttachmentStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattachments/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttachment, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListStorageV1VolumeAttributesClassResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttributesClassList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1VolumeAttributesClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1VolumeAttributesClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStorageV1VolumeAttributesClassResponse = union(enum) {
        ok: ListStorageV1VolumeAttributesClassResponse200,
        unauthorized: ListStorageV1VolumeAttributesClassResponse401,
        default_response: ListStorageV1VolumeAttributesClassResponseDefault,
    };

    pub const listStorageV1VolumeAttributesClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind VolumeAttributesClass
    pub fn listStorageV1VolumeAttributesClass(self: *Client, options: listStorageV1VolumeAttributesClassOptions) !ListStorageV1VolumeAttributesClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattributesclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttributesClassList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistStorageV1VolumeAttributesClass(self: *Client, options: listStorageV1VolumeAttributesClassOptions) !WatchStream(root.io.k8s.api.storage.v1.VolumeAttributesClass) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/storage.k8s.io/v1/volumeattributesclasses");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.storage.v1.VolumeAttributesClass){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateStorageV1VolumeAttributesClassResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttributesClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1VolumeAttributesClassResponse201 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttributesClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1VolumeAttributesClassResponse202 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttributesClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1VolumeAttributesClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1VolumeAttributesClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStorageV1VolumeAttributesClassResponse = union(enum) {
        ok: CreateStorageV1VolumeAttributesClassResponse200,
        created: CreateStorageV1VolumeAttributesClassResponse201,
        accepted: CreateStorageV1VolumeAttributesClassResponse202,
        unauthorized: CreateStorageV1VolumeAttributesClassResponse401,
        default_response: CreateStorageV1VolumeAttributesClassResponseDefault,
    };

    pub const createStorageV1VolumeAttributesClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a VolumeAttributesClass
    pub fn createStorageV1VolumeAttributesClass(self: *Client, body: root.io.k8s.api.storage.v1.VolumeAttributesClass, options: createStorageV1VolumeAttributesClassOptions) !CreateStorageV1VolumeAttributesClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattributesclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttributesClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttributesClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttributesClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStorageV1CollectionVolumeAttributesClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionVolumeAttributesClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionVolumeAttributesClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1CollectionVolumeAttributesClassResponse = union(enum) {
        ok: DeleteStorageV1CollectionVolumeAttributesClassResponse200,
        unauthorized: DeleteStorageV1CollectionVolumeAttributesClassResponse401,
        default_response: DeleteStorageV1CollectionVolumeAttributesClassResponseDefault,
    };

    pub const deleteStorageV1CollectionVolumeAttributesClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of VolumeAttributesClass
    pub fn deleteStorageV1CollectionVolumeAttributesClass(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStorageV1CollectionVolumeAttributesClassOptions) !DeleteStorageV1CollectionVolumeAttributesClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattributesclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadStorageV1VolumeAttributesClassResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttributesClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1VolumeAttributesClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1VolumeAttributesClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStorageV1VolumeAttributesClassResponse = union(enum) {
        ok: ReadStorageV1VolumeAttributesClassResponse200,
        unauthorized: ReadStorageV1VolumeAttributesClassResponse401,
        default_response: ReadStorageV1VolumeAttributesClassResponseDefault,
    };

    pub const readStorageV1VolumeAttributesClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified VolumeAttributesClass
    pub fn readStorageV1VolumeAttributesClass(self: *Client, name: []const u8, options: readStorageV1VolumeAttributesClassOptions) !ReadStorageV1VolumeAttributesClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattributesclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttributesClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceStorageV1VolumeAttributesClassResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttributesClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1VolumeAttributesClassResponse201 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttributesClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1VolumeAttributesClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1VolumeAttributesClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStorageV1VolumeAttributesClassResponse = union(enum) {
        ok: ReplaceStorageV1VolumeAttributesClassResponse200,
        created: ReplaceStorageV1VolumeAttributesClassResponse201,
        unauthorized: ReplaceStorageV1VolumeAttributesClassResponse401,
        default_response: ReplaceStorageV1VolumeAttributesClassResponseDefault,
    };

    pub const replaceStorageV1VolumeAttributesClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified VolumeAttributesClass
    pub fn replaceStorageV1VolumeAttributesClass(self: *Client, name: []const u8, body: root.io.k8s.api.storage.v1.VolumeAttributesClass, options: replaceStorageV1VolumeAttributesClassOptions) !ReplaceStorageV1VolumeAttributesClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattributesclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttributesClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttributesClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStorageV1VolumeAttributesClassResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttributesClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1VolumeAttributesClassResponse202 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttributesClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1VolumeAttributesClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1VolumeAttributesClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStorageV1VolumeAttributesClassResponse = union(enum) {
        ok: DeleteStorageV1VolumeAttributesClassResponse200,
        accepted: DeleteStorageV1VolumeAttributesClassResponse202,
        unauthorized: DeleteStorageV1VolumeAttributesClassResponse401,
        default_response: DeleteStorageV1VolumeAttributesClassResponseDefault,
    };

    pub const deleteStorageV1VolumeAttributesClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a VolumeAttributesClass
    pub fn deleteStorageV1VolumeAttributesClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStorageV1VolumeAttributesClassOptions) !DeleteStorageV1VolumeAttributesClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattributesclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttributesClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttributesClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchStorageV1VolumeAttributesClassResponse200 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttributesClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1VolumeAttributesClassResponse201 = struct {
        body: root.io.k8s.api.storage.v1.VolumeAttributesClass,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1VolumeAttributesClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1VolumeAttributesClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStorageV1VolumeAttributesClassResponse = union(enum) {
        ok: PatchStorageV1VolumeAttributesClassResponse200,
        created: PatchStorageV1VolumeAttributesClassResponse201,
        unauthorized: PatchStorageV1VolumeAttributesClassResponse401,
        default_response: PatchStorageV1VolumeAttributesClassResponseDefault,
    };

    pub const patchStorageV1VolumeAttributesClassOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified VolumeAttributesClass
    pub fn patchStorageV1VolumeAttributesClass(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchStorageV1VolumeAttributesClassOptions) !PatchStorageV1VolumeAttributesClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/volumeattributesclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttributesClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storage.v1.VolumeAttributesClass, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStorageV1CSIDriverListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSIDriverListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSIDriverListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSIDriverListResponse = union(enum) {
        ok: WatchStorageV1CSIDriverListResponse200,
        unauthorized: WatchStorageV1CSIDriverListResponse401,
        default_response: WatchStorageV1CSIDriverListResponseDefault,
    };

    pub const watchStorageV1CSIDriverListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of CSIDriver. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchStorageV1CSIDriverList(self: *Client, options: watchStorageV1CSIDriverListOptions) !WatchStorageV1CSIDriverListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/watch/csidrivers", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStorageV1CSIDriverResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSIDriverResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSIDriverResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSIDriverResponse = union(enum) {
        ok: WatchStorageV1CSIDriverResponse200,
        unauthorized: WatchStorageV1CSIDriverResponse401,
        default_response: WatchStorageV1CSIDriverResponseDefault,
    };

    pub const watchStorageV1CSIDriverOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind CSIDriver. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchStorageV1CSIDriver(self: *Client, name: []const u8, options: watchStorageV1CSIDriverOptions) !WatchStorageV1CSIDriverResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/watch/csidrivers/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStorageV1CSINodeListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSINodeListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSINodeListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSINodeListResponse = union(enum) {
        ok: WatchStorageV1CSINodeListResponse200,
        unauthorized: WatchStorageV1CSINodeListResponse401,
        default_response: WatchStorageV1CSINodeListResponseDefault,
    };

    pub const watchStorageV1CSINodeListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of CSINode. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchStorageV1CSINodeList(self: *Client, options: watchStorageV1CSINodeListOptions) !WatchStorageV1CSINodeListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/watch/csinodes", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStorageV1CSINodeResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSINodeResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSINodeResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSINodeResponse = union(enum) {
        ok: WatchStorageV1CSINodeResponse200,
        unauthorized: WatchStorageV1CSINodeResponse401,
        default_response: WatchStorageV1CSINodeResponseDefault,
    };

    pub const watchStorageV1CSINodeOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind CSINode. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchStorageV1CSINode(self: *Client, name: []const u8, options: watchStorageV1CSINodeOptions) !WatchStorageV1CSINodeResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/watch/csinodes/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStorageV1CSIStorageCapacityListForAllNamespacesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSIStorageCapacityListForAllNamespacesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSIStorageCapacityListForAllNamespacesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1CSIStorageCapacityListForAllNamespacesResponse = union(enum) {
        ok: WatchStorageV1CSIStorageCapacityListForAllNamespacesResponse200,
        unauthorized: WatchStorageV1CSIStorageCapacityListForAllNamespacesResponse401,
        default_response: WatchStorageV1CSIStorageCapacityListForAllNamespacesResponseDefault,
    };

    pub const watchStorageV1CSIStorageCapacityListForAllNamespacesOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of CSIStorageCapacity. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchStorageV1CSIStorageCapacityListForAllNamespaces(self: *Client, options: watchStorageV1CSIStorageCapacityListForAllNamespacesOptions) !WatchStorageV1CSIStorageCapacityListForAllNamespacesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/watch/csistoragecapacities", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStorageV1NamespacedCSIStorageCapacityListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1NamespacedCSIStorageCapacityListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1NamespacedCSIStorageCapacityListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1NamespacedCSIStorageCapacityListResponse = union(enum) {
        ok: WatchStorageV1NamespacedCSIStorageCapacityListResponse200,
        unauthorized: WatchStorageV1NamespacedCSIStorageCapacityListResponse401,
        default_response: WatchStorageV1NamespacedCSIStorageCapacityListResponseDefault,
    };

    pub const watchStorageV1NamespacedCSIStorageCapacityListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of CSIStorageCapacity. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchStorageV1NamespacedCSIStorageCapacityList(self: *Client, namespace: []const u8, options: watchStorageV1NamespacedCSIStorageCapacityListOptions) !WatchStorageV1NamespacedCSIStorageCapacityListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/csistoragecapacities", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStorageV1NamespacedCSIStorageCapacityResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1NamespacedCSIStorageCapacityResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1NamespacedCSIStorageCapacityResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1NamespacedCSIStorageCapacityResponse = union(enum) {
        ok: WatchStorageV1NamespacedCSIStorageCapacityResponse200,
        unauthorized: WatchStorageV1NamespacedCSIStorageCapacityResponse401,
        default_response: WatchStorageV1NamespacedCSIStorageCapacityResponseDefault,
    };

    pub const watchStorageV1NamespacedCSIStorageCapacityOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind CSIStorageCapacity. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchStorageV1NamespacedCSIStorageCapacity(self: *Client, name: []const u8, namespace: []const u8, options: watchStorageV1NamespacedCSIStorageCapacityOptions) !WatchStorageV1NamespacedCSIStorageCapacityResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/watch/namespaces/", .{});
        try urlEncode(url_w, namespace);
        try url_w.print("/csistoragecapacities/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStorageV1StorageClassListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1StorageClassListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1StorageClassListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1StorageClassListResponse = union(enum) {
        ok: WatchStorageV1StorageClassListResponse200,
        unauthorized: WatchStorageV1StorageClassListResponse401,
        default_response: WatchStorageV1StorageClassListResponseDefault,
    };

    pub const watchStorageV1StorageClassListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of StorageClass. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchStorageV1StorageClassList(self: *Client, options: watchStorageV1StorageClassListOptions) !WatchStorageV1StorageClassListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/watch/storageclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStorageV1StorageClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1StorageClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1StorageClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1StorageClassResponse = union(enum) {
        ok: WatchStorageV1StorageClassResponse200,
        unauthorized: WatchStorageV1StorageClassResponse401,
        default_response: WatchStorageV1StorageClassResponseDefault,
    };

    pub const watchStorageV1StorageClassOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind StorageClass. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchStorageV1StorageClass(self: *Client, name: []const u8, options: watchStorageV1StorageClassOptions) !WatchStorageV1StorageClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/watch/storageclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStorageV1VolumeAttachmentListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1VolumeAttachmentListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1VolumeAttachmentListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1VolumeAttachmentListResponse = union(enum) {
        ok: WatchStorageV1VolumeAttachmentListResponse200,
        unauthorized: WatchStorageV1VolumeAttachmentListResponse401,
        default_response: WatchStorageV1VolumeAttachmentListResponseDefault,
    };

    pub const watchStorageV1VolumeAttachmentListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of VolumeAttachment. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchStorageV1VolumeAttachmentList(self: *Client, options: watchStorageV1VolumeAttachmentListOptions) !WatchStorageV1VolumeAttachmentListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/watch/volumeattachments", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStorageV1VolumeAttachmentResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1VolumeAttachmentResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1VolumeAttachmentResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1VolumeAttachmentResponse = union(enum) {
        ok: WatchStorageV1VolumeAttachmentResponse200,
        unauthorized: WatchStorageV1VolumeAttachmentResponse401,
        default_response: WatchStorageV1VolumeAttachmentResponseDefault,
    };

    pub const watchStorageV1VolumeAttachmentOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind VolumeAttachment. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchStorageV1VolumeAttachment(self: *Client, name: []const u8, options: watchStorageV1VolumeAttachmentOptions) !WatchStorageV1VolumeAttachmentResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/watch/volumeattachments/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStorageV1VolumeAttributesClassListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1VolumeAttributesClassListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1VolumeAttributesClassListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1VolumeAttributesClassListResponse = union(enum) {
        ok: WatchStorageV1VolumeAttributesClassListResponse200,
        unauthorized: WatchStorageV1VolumeAttributesClassListResponse401,
        default_response: WatchStorageV1VolumeAttributesClassListResponseDefault,
    };

    pub const watchStorageV1VolumeAttributesClassListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of VolumeAttributesClass. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchStorageV1VolumeAttributesClassList(self: *Client, options: watchStorageV1VolumeAttributesClassListOptions) !WatchStorageV1VolumeAttributesClassListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/watch/volumeattributesclasses", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStorageV1VolumeAttributesClassResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1VolumeAttributesClassResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1VolumeAttributesClassResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStorageV1VolumeAttributesClassResponse = union(enum) {
        ok: WatchStorageV1VolumeAttributesClassResponse200,
        unauthorized: WatchStorageV1VolumeAttributesClassResponse401,
        default_response: WatchStorageV1VolumeAttributesClassResponseDefault,
    };

    pub const watchStorageV1VolumeAttributesClassOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind VolumeAttributesClass. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchStorageV1VolumeAttributesClass(self: *Client, name: []const u8, options: watchStorageV1VolumeAttributesClassOptions) !WatchStorageV1VolumeAttributesClassResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storage.k8s.io/v1/watch/volumeattributesclasses/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetStoragemigrationAPIGroupResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStoragemigrationAPIGroupResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStoragemigrationAPIGroupResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStoragemigrationAPIGroupResponse = union(enum) {
        ok: GetStoragemigrationAPIGroupResponse200,
        unauthorized: GetStoragemigrationAPIGroupResponse401,
        default_response: GetStoragemigrationAPIGroupResponseDefault,
    };

    /// get information of a group
    pub fn getStoragemigrationAPIGroup(self: *Client) !GetStoragemigrationAPIGroupResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIGroup, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetStoragemigrationV1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStoragemigrationV1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStoragemigrationV1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStoragemigrationV1APIResourcesResponse = union(enum) {
        ok: GetStoragemigrationV1APIResourcesResponse200,
        unauthorized: GetStoragemigrationV1APIResourcesResponse401,
        default_response: GetStoragemigrationV1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getStoragemigrationV1APIResources(self: *Client) !GetStoragemigrationV1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListStoragemigrationV1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigrationList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStoragemigrationV1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStoragemigrationV1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStoragemigrationV1StorageVersionMigrationResponse = union(enum) {
        ok: ListStoragemigrationV1StorageVersionMigrationResponse200,
        unauthorized: ListStoragemigrationV1StorageVersionMigrationResponse401,
        default_response: ListStoragemigrationV1StorageVersionMigrationResponseDefault,
    };

    pub const listStoragemigrationV1StorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind StorageVersionMigration
    pub fn listStoragemigrationV1StorageVersionMigration(self: *Client, options: listStoragemigrationV1StorageVersionMigrationOptions) !ListStoragemigrationV1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1/storageversionmigrations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigrationList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistStoragemigrationV1StorageVersionMigration(self: *Client, options: listStoragemigrationV1StorageVersionMigrationOptions) !WatchStream(root.io.k8s.api.storagemigration.v1.StorageVersionMigration) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/storagemigration.k8s.io/v1/storageversionmigrations");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.storagemigration.v1.StorageVersionMigration){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateStoragemigrationV1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStoragemigrationV1StorageVersionMigrationResponse201 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStoragemigrationV1StorageVersionMigrationResponse202 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStoragemigrationV1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStoragemigrationV1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStoragemigrationV1StorageVersionMigrationResponse = union(enum) {
        ok: CreateStoragemigrationV1StorageVersionMigrationResponse200,
        created: CreateStoragemigrationV1StorageVersionMigrationResponse201,
        accepted: CreateStoragemigrationV1StorageVersionMigrationResponse202,
        unauthorized: CreateStoragemigrationV1StorageVersionMigrationResponse401,
        default_response: CreateStoragemigrationV1StorageVersionMigrationResponseDefault,
    };

    pub const createStoragemigrationV1StorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a StorageVersionMigration
    pub fn createStoragemigrationV1StorageVersionMigration(self: *Client, body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration, options: createStoragemigrationV1StorageVersionMigrationOptions) !CreateStoragemigrationV1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1/storageversionmigrations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStoragemigrationV1CollectionStorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1CollectionStorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1CollectionStorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1CollectionStorageVersionMigrationResponse = union(enum) {
        ok: DeleteStoragemigrationV1CollectionStorageVersionMigrationResponse200,
        unauthorized: DeleteStoragemigrationV1CollectionStorageVersionMigrationResponse401,
        default_response: DeleteStoragemigrationV1CollectionStorageVersionMigrationResponseDefault,
    };

    pub const deleteStoragemigrationV1CollectionStorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of StorageVersionMigration
    pub fn deleteStoragemigrationV1CollectionStorageVersionMigration(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStoragemigrationV1CollectionStorageVersionMigrationOptions) !DeleteStoragemigrationV1CollectionStorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1/storageversionmigrations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadStoragemigrationV1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStoragemigrationV1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStoragemigrationV1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStoragemigrationV1StorageVersionMigrationResponse = union(enum) {
        ok: ReadStoragemigrationV1StorageVersionMigrationResponse200,
        unauthorized: ReadStoragemigrationV1StorageVersionMigrationResponse401,
        default_response: ReadStoragemigrationV1StorageVersionMigrationResponseDefault,
    };

    pub const readStoragemigrationV1StorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified StorageVersionMigration
    pub fn readStoragemigrationV1StorageVersionMigration(self: *Client, name: []const u8, options: readStoragemigrationV1StorageVersionMigrationOptions) !ReadStoragemigrationV1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceStoragemigrationV1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1StorageVersionMigrationResponse201 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1StorageVersionMigrationResponse = union(enum) {
        ok: ReplaceStoragemigrationV1StorageVersionMigrationResponse200,
        created: ReplaceStoragemigrationV1StorageVersionMigrationResponse201,
        unauthorized: ReplaceStoragemigrationV1StorageVersionMigrationResponse401,
        default_response: ReplaceStoragemigrationV1StorageVersionMigrationResponseDefault,
    };

    pub const replaceStoragemigrationV1StorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified StorageVersionMigration
    pub fn replaceStoragemigrationV1StorageVersionMigration(self: *Client, name: []const u8, body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration, options: replaceStoragemigrationV1StorageVersionMigrationOptions) !ReplaceStoragemigrationV1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStoragemigrationV1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1StorageVersionMigrationResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1StorageVersionMigrationResponse = union(enum) {
        ok: DeleteStoragemigrationV1StorageVersionMigrationResponse200,
        accepted: DeleteStoragemigrationV1StorageVersionMigrationResponse202,
        unauthorized: DeleteStoragemigrationV1StorageVersionMigrationResponse401,
        default_response: DeleteStoragemigrationV1StorageVersionMigrationResponseDefault,
    };

    pub const deleteStoragemigrationV1StorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a StorageVersionMigration
    pub fn deleteStoragemigrationV1StorageVersionMigration(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStoragemigrationV1StorageVersionMigrationOptions) !DeleteStoragemigrationV1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchStoragemigrationV1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1StorageVersionMigrationResponse201 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1StorageVersionMigrationResponse = union(enum) {
        ok: PatchStoragemigrationV1StorageVersionMigrationResponse200,
        created: PatchStoragemigrationV1StorageVersionMigrationResponse201,
        unauthorized: PatchStoragemigrationV1StorageVersionMigrationResponse401,
        default_response: PatchStoragemigrationV1StorageVersionMigrationResponseDefault,
    };

    pub const patchStoragemigrationV1StorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified StorageVersionMigration
    pub fn patchStoragemigrationV1StorageVersionMigration(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchStoragemigrationV1StorageVersionMigrationOptions) !PatchStoragemigrationV1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadStoragemigrationV1StorageVersionMigrationStatusResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStoragemigrationV1StorageVersionMigrationStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStoragemigrationV1StorageVersionMigrationStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStoragemigrationV1StorageVersionMigrationStatusResponse = union(enum) {
        ok: ReadStoragemigrationV1StorageVersionMigrationStatusResponse200,
        unauthorized: ReadStoragemigrationV1StorageVersionMigrationStatusResponse401,
        default_response: ReadStoragemigrationV1StorageVersionMigrationStatusResponseDefault,
    };

    pub const readStoragemigrationV1StorageVersionMigrationStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified StorageVersionMigration
    pub fn readStoragemigrationV1StorageVersionMigrationStatus(self: *Client, name: []const u8, options: readStoragemigrationV1StorageVersionMigrationStatusOptions) !ReadStoragemigrationV1StorageVersionMigrationStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceStoragemigrationV1StorageVersionMigrationStatusResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1StorageVersionMigrationStatusResponse201 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1StorageVersionMigrationStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1StorageVersionMigrationStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1StorageVersionMigrationStatusResponse = union(enum) {
        ok: ReplaceStoragemigrationV1StorageVersionMigrationStatusResponse200,
        created: ReplaceStoragemigrationV1StorageVersionMigrationStatusResponse201,
        unauthorized: ReplaceStoragemigrationV1StorageVersionMigrationStatusResponse401,
        default_response: ReplaceStoragemigrationV1StorageVersionMigrationStatusResponseDefault,
    };

    pub const replaceStoragemigrationV1StorageVersionMigrationStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified StorageVersionMigration
    pub fn replaceStoragemigrationV1StorageVersionMigrationStatus(self: *Client, name: []const u8, body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration, options: replaceStoragemigrationV1StorageVersionMigrationStatusOptions) !ReplaceStoragemigrationV1StorageVersionMigrationStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchStoragemigrationV1StorageVersionMigrationStatusResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1StorageVersionMigrationStatusResponse201 = struct {
        body: root.io.k8s.api.storagemigration.v1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1StorageVersionMigrationStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1StorageVersionMigrationStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1StorageVersionMigrationStatusResponse = union(enum) {
        ok: PatchStoragemigrationV1StorageVersionMigrationStatusResponse200,
        created: PatchStoragemigrationV1StorageVersionMigrationStatusResponse201,
        unauthorized: PatchStoragemigrationV1StorageVersionMigrationStatusResponse401,
        default_response: PatchStoragemigrationV1StorageVersionMigrationStatusResponseDefault,
    };

    pub const patchStoragemigrationV1StorageVersionMigrationStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified StorageVersionMigration
    pub fn patchStoragemigrationV1StorageVersionMigrationStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchStoragemigrationV1StorageVersionMigrationStatusOptions) !PatchStoragemigrationV1StorageVersionMigrationStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStoragemigrationV1StorageVersionMigrationListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStoragemigrationV1StorageVersionMigrationListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStoragemigrationV1StorageVersionMigrationListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStoragemigrationV1StorageVersionMigrationListResponse = union(enum) {
        ok: WatchStoragemigrationV1StorageVersionMigrationListResponse200,
        unauthorized: WatchStoragemigrationV1StorageVersionMigrationListResponse401,
        default_response: WatchStoragemigrationV1StorageVersionMigrationListResponseDefault,
    };

    pub const watchStoragemigrationV1StorageVersionMigrationListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of StorageVersionMigration. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchStoragemigrationV1StorageVersionMigrationList(self: *Client, options: watchStoragemigrationV1StorageVersionMigrationListOptions) !WatchStoragemigrationV1StorageVersionMigrationListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1/watch/storageversionmigrations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStoragemigrationV1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStoragemigrationV1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStoragemigrationV1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStoragemigrationV1StorageVersionMigrationResponse = union(enum) {
        ok: WatchStoragemigrationV1StorageVersionMigrationResponse200,
        unauthorized: WatchStoragemigrationV1StorageVersionMigrationResponse401,
        default_response: WatchStoragemigrationV1StorageVersionMigrationResponseDefault,
    };

    pub const watchStoragemigrationV1StorageVersionMigrationOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind StorageVersionMigration. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchStoragemigrationV1StorageVersionMigration(self: *Client, name: []const u8, options: watchStoragemigrationV1StorageVersionMigrationOptions) !WatchStoragemigrationV1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1/watch/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetStoragemigrationV1beta1APIResourcesResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStoragemigrationV1beta1APIResourcesResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStoragemigrationV1beta1APIResourcesResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetStoragemigrationV1beta1APIResourcesResponse = union(enum) {
        ok: GetStoragemigrationV1beta1APIResourcesResponse200,
        unauthorized: GetStoragemigrationV1beta1APIResourcesResponse401,
        default_response: GetStoragemigrationV1beta1APIResourcesResponseDefault,
    };

    /// get available resources
    pub fn getStoragemigrationV1beta1APIResources(self: *Client) !GetStoragemigrationV1beta1APIResourcesResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1beta1/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.APIResourceList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ListStoragemigrationV1beta1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigrationList,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStoragemigrationV1beta1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStoragemigrationV1beta1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ListStoragemigrationV1beta1StorageVersionMigrationResponse = union(enum) {
        ok: ListStoragemigrationV1beta1StorageVersionMigrationResponse200,
        unauthorized: ListStoragemigrationV1beta1StorageVersionMigrationResponse401,
        default_response: ListStoragemigrationV1beta1StorageVersionMigrationResponseDefault,
    };

    pub const listStoragemigrationV1beta1StorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// list or watch objects of kind StorageVersionMigration
    pub fn listStoragemigrationV1beta1StorageVersionMigration(self: *Client, options: listStoragemigrationV1beta1StorageVersionMigrationOptions) !ListStoragemigrationV1beta1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1beta1/storageversionmigrations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigrationList, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub fn watchlistStoragemigrationV1beta1StorageVersionMigration(self: *Client, options: listStoragemigrationV1beta1StorageVersionMigrationOptions) !WatchStream(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration) {
        var url_buf: [4096]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.writeAll("/apis/storagemigration.k8s.io/v1beta1/storageversionmigrations");
        try url_w.writeAll("?watch=true");
        if (options.pretty) |val| {
            try url_w.print("&pretty={any}", .{val});
        }
        if (options.allowWatchBookmarks) |val| {
            try url_w.print("&allowWatchBookmarks={any}", .{val});
        }
        if (options.@"continue") |val| {
            try url_w.print("&continue={any}", .{val});
        }
        if (options.fieldSelector) |val| {
            try url_w.print("&fieldSelector={any}", .{val});
        }
        if (options.labelSelector) |val| {
            try url_w.print("&labelSelector={any}", .{val});
        }
        if (options.limit) |val| {
            try url_w.print("&limit={any}", .{val});
        }
        if (options.resourceVersion) |val| {
            try url_w.print("&resourceVersion={any}", .{val});
        }
        if (options.resourceVersionMatch) |val| {
            try url_w.print("&resourceVersionMatch={any}", .{val});
        }
        if (options.sendInitialEvents) |val| {
            try url_w.print("&sendInitialEvents={any}", .{val});
        }
        if (options.shardSelector) |val| {
            try url_w.print("&shardSelector={any}", .{val});
        }
        if (options.timeoutSeconds) |val| {
            try url_w.print("&timeoutSeconds={any}", .{val});
        }

        const req = try self.client.request(.GET, try std.Uri.parse(url_w.context.getWritten()), .{ .headers = .{ .content_type = .{ .override = "application/json" }, .authorization = if (@hasField(AuthConfig, "BearerToken")) if (self.auth_config.BearerToken) |t| .{ .override = try std.fmt.allocPrint(self.allocator, "Bearer {s}", .{t}) } else .omit else .omit } });
        const heap_req = try self.allocator.create(std.http.Client.Request);
        heap_req.* = req;
        errdefer self.allocator.destroy(heap_req);
        try heap_req.sendBodiless();
        var header_buf: [4096]u8 = undefined;
        var res = try heap_req.receiveHead(&header_buf);
        const transfer_buf = try self.allocator.alloc(u8, 4096);
        errdefer self.allocator.free(transfer_buf);
        const reader = res.reader(transfer_buf);
        return WatchStream(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration){ .allocator = self.allocator, .req = heap_req, .transfer_buf = transfer_buf, .reader = reader };
    }

    pub const CreateStoragemigrationV1beta1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStoragemigrationV1beta1StorageVersionMigrationResponse201 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStoragemigrationV1beta1StorageVersionMigrationResponse202 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStoragemigrationV1beta1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStoragemigrationV1beta1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const CreateStoragemigrationV1beta1StorageVersionMigrationResponse = union(enum) {
        ok: CreateStoragemigrationV1beta1StorageVersionMigrationResponse200,
        created: CreateStoragemigrationV1beta1StorageVersionMigrationResponse201,
        accepted: CreateStoragemigrationV1beta1StorageVersionMigrationResponse202,
        unauthorized: CreateStoragemigrationV1beta1StorageVersionMigrationResponse401,
        default_response: CreateStoragemigrationV1beta1StorageVersionMigrationResponseDefault,
    };

    pub const createStoragemigrationV1beta1StorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// create a StorageVersionMigration
    pub fn createStoragemigrationV1beta1StorageVersionMigration(self: *Client, body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, options: createStoragemigrationV1beta1StorageVersionMigrationOptions) !CreateStoragemigrationV1beta1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1beta1/storageversionmigrations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .POST, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "POST", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStoragemigrationV1beta1CollectionStorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1beta1CollectionStorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1beta1CollectionStorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1beta1CollectionStorageVersionMigrationResponse = union(enum) {
        ok: DeleteStoragemigrationV1beta1CollectionStorageVersionMigrationResponse200,
        unauthorized: DeleteStoragemigrationV1beta1CollectionStorageVersionMigrationResponse401,
        default_response: DeleteStoragemigrationV1beta1CollectionStorageVersionMigrationResponseDefault,
    };

    pub const deleteStoragemigrationV1beta1CollectionStorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
    };
    /// delete collection of StorageVersionMigration
    pub fn deleteStoragemigrationV1beta1CollectionStorageVersionMigration(self: *Client, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStoragemigrationV1beta1CollectionStorageVersionMigrationOptions) !DeleteStoragemigrationV1beta1CollectionStorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1beta1/storageversionmigrations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadStoragemigrationV1beta1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStoragemigrationV1beta1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStoragemigrationV1beta1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStoragemigrationV1beta1StorageVersionMigrationResponse = union(enum) {
        ok: ReadStoragemigrationV1beta1StorageVersionMigrationResponse200,
        unauthorized: ReadStoragemigrationV1beta1StorageVersionMigrationResponse401,
        default_response: ReadStoragemigrationV1beta1StorageVersionMigrationResponseDefault,
    };

    pub const readStoragemigrationV1beta1StorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read the specified StorageVersionMigration
    pub fn readStoragemigrationV1beta1StorageVersionMigration(self: *Client, name: []const u8, options: readStoragemigrationV1beta1StorageVersionMigrationOptions) !ReadStoragemigrationV1beta1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1beta1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceStoragemigrationV1beta1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1beta1StorageVersionMigrationResponse201 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1beta1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1beta1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1beta1StorageVersionMigrationResponse = union(enum) {
        ok: ReplaceStoragemigrationV1beta1StorageVersionMigrationResponse200,
        created: ReplaceStoragemigrationV1beta1StorageVersionMigrationResponse201,
        unauthorized: ReplaceStoragemigrationV1beta1StorageVersionMigrationResponse401,
        default_response: ReplaceStoragemigrationV1beta1StorageVersionMigrationResponseDefault,
    };

    pub const replaceStoragemigrationV1beta1StorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace the specified StorageVersionMigration
    pub fn replaceStoragemigrationV1beta1StorageVersionMigration(self: *Client, name: []const u8, body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, options: replaceStoragemigrationV1beta1StorageVersionMigrationOptions) !ReplaceStoragemigrationV1beta1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1beta1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const DeleteStoragemigrationV1beta1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1beta1StorageVersionMigrationResponse202 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Status,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1beta1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1beta1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const DeleteStoragemigrationV1beta1StorageVersionMigrationResponse = union(enum) {
        ok: DeleteStoragemigrationV1beta1StorageVersionMigrationResponse200,
        accepted: DeleteStoragemigrationV1beta1StorageVersionMigrationResponse202,
        unauthorized: DeleteStoragemigrationV1beta1StorageVersionMigrationResponse401,
        default_response: DeleteStoragemigrationV1beta1StorageVersionMigrationResponseDefault,
    };

    pub const deleteStoragemigrationV1beta1StorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// The duration in seconds before the object should be deleted. Value must be non-negative integer. The value zero indicates delete immediately. If this value is nil, the default grace period for the specified type will be used. Defaults to a per object value if not specified. zero means delete immediately.
        gracePeriodSeconds: ?[]const u8 = null,
        /// if set to true, it will trigger an unsafe deletion of the resource in case the normal deletion flow fails with a corrupt object error. A resource is considered corrupt if it can not be retrieved from the underlying storage successfully because of a) its data can not be transformed e.g. decryption failure, or b) it fails to decode into an object. NOTE: unsafe deletion ignores finalizer constraints, skips precondition checks, and removes the object from the storage. WARNING: This may potentially break the cluster if the workload associated with the resource being unsafe-deleted relies on normal deletion flow. Use only if you REALLY know what you are doing. The default value is false, and the user must opt in to enable it
        ignoreStoreReadErrorWithClusterBreakingPotential: ?[]const u8 = null,
        /// Deprecated: please use the PropagationPolicy, this field will be deprecated in 1.7. Should the dependent objects be orphaned. If true/false, the "orphan" finalizer will be added to/removed from the object's finalizers list. Either this field or PropagationPolicy may be set, but not both.
        orphanDependents: ?[]const u8 = null,
        /// Whether and how garbage collection will be performed. Either this field or OrphanDependents may be set, but not both. The default policy is decided by the existing finalizer set in the metadata.finalizers and the resource-specific default policy. Acceptable values are: 'Orphan' - orphan the dependents; 'Background' - allow the garbage collector to delete the dependents in the background; 'Foreground' - a cascading policy that deletes all dependents in the foreground.
        propagationPolicy: ?[]const u8 = null,
    };
    /// delete a StorageVersionMigration
    pub fn deleteStoragemigrationV1beta1StorageVersionMigration(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.DeleteOptions, options: deleteStoragemigrationV1beta1StorageVersionMigrationOptions) !DeleteStoragemigrationV1beta1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1beta1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.gracePeriodSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.ignoreStoreReadErrorWithClusterBreakingPotential) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("ignoreStoreReadErrorWithClusterBreakingPotential=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.orphanDependents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("orphanDependents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.propagationPolicy) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        if (@hasField(@TypeOf(body), "gracePeriodSeconds")) {
            if (body.gracePeriodSeconds) |gp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("gracePeriodSeconds={d}", .{gp});
            }
        }
        if (@hasField(@TypeOf(body), "propagationPolicy")) {
            if (body.propagationPolicy) |pp| {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("propagationPolicy={s}", .{pp});
            }
        }
        if (@hasField(@TypeOf(body), "dryRun")) {
            if (body.dryRun) |dr| {
                for (dr) |d| {
                    if (first_query) {
                        try url_w.writeByte('?');
                        first_query = false;
                    } else {
                        try url_w.writeByte('&');
                    }
                    try url_w.print("dryRun={s}", .{d});
                }
            }
        }
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .DELETE, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "DELETE", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .accepted => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.Status, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .accepted = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchStoragemigrationV1beta1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1beta1StorageVersionMigrationResponse201 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1beta1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1beta1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1beta1StorageVersionMigrationResponse = union(enum) {
        ok: PatchStoragemigrationV1beta1StorageVersionMigrationResponse200,
        created: PatchStoragemigrationV1beta1StorageVersionMigrationResponse201,
        unauthorized: PatchStoragemigrationV1beta1StorageVersionMigrationResponse401,
        default_response: PatchStoragemigrationV1beta1StorageVersionMigrationResponseDefault,
    };

    pub const patchStoragemigrationV1beta1StorageVersionMigrationOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update the specified StorageVersionMigration
    pub fn patchStoragemigrationV1beta1StorageVersionMigration(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchStoragemigrationV1beta1StorageVersionMigrationOptions) !PatchStoragemigrationV1beta1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1beta1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReadStoragemigrationV1beta1StorageVersionMigrationStatusResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStoragemigrationV1beta1StorageVersionMigrationStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStoragemigrationV1beta1StorageVersionMigrationStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReadStoragemigrationV1beta1StorageVersionMigrationStatusResponse = union(enum) {
        ok: ReadStoragemigrationV1beta1StorageVersionMigrationStatusResponse200,
        unauthorized: ReadStoragemigrationV1beta1StorageVersionMigrationStatusResponse401,
        default_response: ReadStoragemigrationV1beta1StorageVersionMigrationStatusResponseDefault,
    };

    pub const readStoragemigrationV1beta1StorageVersionMigrationStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
    };
    /// read status of the specified StorageVersionMigration
    pub fn readStoragemigrationV1beta1StorageVersionMigrationStatus(self: *Client, name: []const u8, options: readStoragemigrationV1beta1StorageVersionMigrationStatusOptions) !ReadStoragemigrationV1beta1StorageVersionMigrationStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1beta1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const ReplaceStoragemigrationV1beta1StorageVersionMigrationStatusResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1beta1StorageVersionMigrationStatusResponse201 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1beta1StorageVersionMigrationStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1beta1StorageVersionMigrationStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const ReplaceStoragemigrationV1beta1StorageVersionMigrationStatusResponse = union(enum) {
        ok: ReplaceStoragemigrationV1beta1StorageVersionMigrationStatusResponse200,
        created: ReplaceStoragemigrationV1beta1StorageVersionMigrationStatusResponse201,
        unauthorized: ReplaceStoragemigrationV1beta1StorageVersionMigrationStatusResponse401,
        default_response: ReplaceStoragemigrationV1beta1StorageVersionMigrationStatusResponseDefault,
    };

    pub const replaceStoragemigrationV1beta1StorageVersionMigrationStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint.
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
    };
    /// replace status of the specified StorageVersionMigration
    pub fn replaceStoragemigrationV1beta1StorageVersionMigrationStatus(self: *Client, name: []const u8, body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, options: replaceStoragemigrationV1beta1StorageVersionMigrationStatusOptions) !ReplaceStoragemigrationV1beta1StorageVersionMigrationStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1beta1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PUT, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PUT", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const PatchStoragemigrationV1beta1StorageVersionMigrationStatusResponse200 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1beta1StorageVersionMigrationStatusResponse201 = struct {
        body: root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1beta1StorageVersionMigrationStatusResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1beta1StorageVersionMigrationStatusResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const PatchStoragemigrationV1beta1StorageVersionMigrationStatusResponse = union(enum) {
        ok: PatchStoragemigrationV1beta1StorageVersionMigrationStatusResponse200,
        created: PatchStoragemigrationV1beta1StorageVersionMigrationStatusResponse201,
        unauthorized: PatchStoragemigrationV1beta1StorageVersionMigrationStatusResponse401,
        default_response: PatchStoragemigrationV1beta1StorageVersionMigrationStatusResponseDefault,
    };

    pub const patchStoragemigrationV1beta1StorageVersionMigrationStatusOptions = struct {
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// When present, indicates that modifications should not be persisted. An invalid or unrecognized dryRun directive will result in an error response and no further processing of the request. Valid values are: - All: all dry run stages will be processed
        dryRun: ?[]const u8 = null,
        /// fieldManager is a name associated with the actor or entity that is making these changes. The value must be less than or 128 characters long, and only contain printable characters, as defined by https://golang.org/pkg/unicode/#IsPrint. This field is required for apply requests (application/apply-patch) but optional for non-apply patch types (JsonPatch, MergePatch, StrategicMergePatch).
        fieldManager: ?[]const u8 = null,
        /// fieldValidation instructs the server on how to handle objects in the request (POST/PUT/PATCH) containing unknown or duplicate fields. Valid values are: - Ignore: This will ignore any unknown fields that are silently dropped from the object, and will ignore all but the last duplicate field that the decoder encounters. This is the default behavior prior to v1.23. - Warn: This will send a warning via the standard warning response header for each unknown field that is dropped from the object, and for each duplicate field that is encountered. The request will still succeed if there are no other errors, and will only persist the last of any duplicate fields. This is the default in v1.23+ - Strict: This will fail the request with a BadRequest error if any unknown fields would be dropped from the object, or if any duplicate fields are present. The error returned from the server will contain all unknown and duplicate fields encountered.
        fieldValidation: ?[]const u8 = null,
        /// Force is going to "force" Apply requests. It means user will re-acquire conflicting fields owned by other people. Force flag must be unset for non-apply patch requests.
        force: ?[]const u8 = null,
    };
    /// partially update status of the specified StorageVersionMigration
    pub fn patchStoragemigrationV1beta1StorageVersionMigrationStatus(self: *Client, name: []const u8, body: root.io.k8s.apimachinery.pkg.apis.meta.v1.Patch, options: patchStoragemigrationV1beta1StorageVersionMigrationStatusOptions) !PatchStoragemigrationV1beta1StorageVersionMigrationStatusResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1beta1/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        try url_w.print("/status", .{});
        var first_query = true;
        _ = &first_query;
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.dryRun) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("dryRun=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldManager) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldManager=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldValidation) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldValidation=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.force) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("force=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();
        if (@hasDecl(@TypeOf(body), "validate")) try body.validate();
        try std.json.Stringify.value(body, .{}, &req_body_writer.writer);

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .PATCH, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "PATCH", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .created => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.api.storagemigration.v1beta1.StorageVersionMigration, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .created = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStoragemigrationV1beta1StorageVersionMigrationListResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStoragemigrationV1beta1StorageVersionMigrationListResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStoragemigrationV1beta1StorageVersionMigrationListResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStoragemigrationV1beta1StorageVersionMigrationListResponse = union(enum) {
        ok: WatchStoragemigrationV1beta1StorageVersionMigrationListResponse200,
        unauthorized: WatchStoragemigrationV1beta1StorageVersionMigrationListResponse401,
        default_response: WatchStoragemigrationV1beta1StorageVersionMigrationListResponseDefault,
    };

    pub const watchStoragemigrationV1beta1StorageVersionMigrationListOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch individual changes to a list of StorageVersionMigration. deprecated: use the 'watch' parameter with a list operation instead.
    pub fn watchStoragemigrationV1beta1StorageVersionMigrationList(self: *Client, options: watchStoragemigrationV1beta1StorageVersionMigrationListOptions) !WatchStoragemigrationV1beta1StorageVersionMigrationListResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1beta1/watch/storageversionmigrations", .{});
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const WatchStoragemigrationV1beta1StorageVersionMigrationResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStoragemigrationV1beta1StorageVersionMigrationResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStoragemigrationV1beta1StorageVersionMigrationResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const WatchStoragemigrationV1beta1StorageVersionMigrationResponse = union(enum) {
        ok: WatchStoragemigrationV1beta1StorageVersionMigrationResponse200,
        unauthorized: WatchStoragemigrationV1beta1StorageVersionMigrationResponse401,
        default_response: WatchStoragemigrationV1beta1StorageVersionMigrationResponseDefault,
    };

    pub const watchStoragemigrationV1beta1StorageVersionMigrationOptions = struct {
        /// allowWatchBookmarks requests watch events with type "BOOKMARK". Servers that do not implement bookmarks may ignore this flag and bookmarks are sent at the server's discretion. Clients should not assume bookmarks are returned at any specific interval, nor may they assume the server will send any BOOKMARK event during a session. If this is not a watch, this field is ignored.
        allowWatchBookmarks: ?[]const u8 = null,
        /// The continue option should be set when retrieving more results from the server. Since this value is server defined, clients may only use the continue value from a previous query result with identical query parameters (except for the value of continue) and the server may reject a continue value it does not recognize. If the specified continue value is no longer valid whether due to expiration (generally five to fifteen minutes) or a configuration change on the server, the server will respond with a 410 ResourceExpired error together with a continue token. If the client needs a consistent list, it must restart their list without the continue field. Otherwise, the client may send another list request with the token received with the 410 error, the server will respond with a list starting from the next key, but from the latest snapshot, which is inconsistent from the previous list results - objects that are created, modified, or deleted after the first list request will be included in the response, as long as their keys are after the "next key".
        ///
        /// This field is not supported when watch is true. Clients may start a watch from the last resourceVersion value returned by the server and not miss any modifications.
        @"continue": ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their fields. Defaults to everything.
        fieldSelector: ?[]const u8 = null,
        /// A selector to restrict the list of returned objects by their labels. Defaults to everything.
        labelSelector: ?[]const u8 = null,
        /// limit is a maximum number of responses to return for a list call. If more items exist, the server will set the `continue` field on the list metadata to a value that can be used with the same initial query to retrieve the next set of results. Setting a limit may return fewer than the requested amount of items (up to zero items) in the event all requested objects are filtered out and clients should only use the presence of the continue field to determine whether more results are available. Servers may choose not to support the limit argument and will return all of the available results. If limit is specified and the continue field is empty, clients may assume that no more results are available. This field is not supported if watch is true.
        ///
        /// The server guarantees that the objects returned when using continue will be identical to issuing a single list call without a limit - that is, no objects created, modified, or deleted after the first request is issued will be included in any subsequent continued requests. This is sometimes referred to as a consistent snapshot, and ensures that a client that is using limit to receive smaller chunks of a very large result can ensure they see all possible objects. If objects are updated during a chunked list the version of the object that was present at the time the first list result was calculated is returned.
        limit: ?[]const u8 = null,
        /// If 'true', then the output is pretty printed. Defaults to 'false' unless the user-agent indicates a browser or command-line HTTP tool (curl and wget).
        pretty: ?[]const u8 = null,
        /// resourceVersion sets a constraint on what resource versions a request may be served from. See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersion: ?[]const u8 = null,
        /// resourceVersionMatch determines how resourceVersion is applied to list calls. It is highly recommended that resourceVersionMatch be set for list calls where resourceVersion is set See https://kubernetes.io/docs/reference/using-api/api-concepts/#resource-versions for details.
        ///
        /// Defaults to unset
        resourceVersionMatch: ?[]const u8 = null,
        /// `sendInitialEvents=true` may be set together with `watch=true`. In that case, the watch stream will begin with synthetic events to produce the current state of objects in the collection. Once all such events have been sent, a synthetic "Bookmark" event  will be sent. The bookmark will report the ResourceVersion (RV) corresponding to the set of objects, and be marked with `"k8s.io/initial-events-end": "true"` annotation. Afterwards, the watch stream will proceed as usual, sending watch events corresponding to changes (subsequent to the RV) to objects watched.
        ///
        /// When `sendInitialEvents` option is set, we require `resourceVersionMatch` option to also be set. The semantic of the watch request is as following: - `resourceVersionMatch` = NotOlderThan
        ///   is interpreted as "data at least as new as the provided `resourceVersion`"
        ///   and the bookmark event is send when the state is synced
        ///   to a `resourceVersion` at least as fresh as the one provided by the ListOptions.
        ///   If `resourceVersion` is unset, this is interpreted as "consistent read" and the
        ///   bookmark event is send when the state is synced at least to the moment
        ///   when request started being processed.
        /// - `resourceVersionMatch` set to any other value or unset
        ///   Invalid error is returned.
        ///
        /// Defaults to true if `resourceVersion=""` or `resourceVersion="0"` (for backward compatibility reasons) and to false otherwise.
        sendInitialEvents: ?[]const u8 = null,
        /// shardSelector restricts the list of returned objects using a CEL-based shard selector expression. The format uses the shardRange() function combined with || (logical OR) to specify one or more hash ranges:
        ///
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000')
        ///   shardRange(object.metadata.uid, '0x0', '0x8000000000000000') || shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///
        /// Field paths use CEL-style object-rooted syntax (e.g. "object.metadata.uid"), NOT the fieldSelector format ("metadata.uid"). Currently supported paths:
        ///   - object.metadata.uid
        ///   - object.metadata.namespace
        ///
        /// hexStart and hexEnd are single-quoted CEL string literals with a '0x' prefix, defining the inclusive lower and exclusive upper bounds over the 64-bit FNV-1a hash space. The full range is [0x0, 0x10000000000000000), where the exclusive upper bound equals 2^64.
        ///
        /// Examples:
        ///   2-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x8000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x8000000000000000', '0x10000000000000000')
        ///   4-shard split:
        ///     shard 0: shardRange(object.metadata.uid, '0x0000000000000000', '0x4000000000000000')
        ///     shard 1: shardRange(object.metadata.uid, '0x4000000000000000', '0x8000000000000000')
        ///     shard 2: shardRange(object.metadata.uid, '0x8000000000000000', '0xc000000000000000')
        ///     shard 3: shardRange(object.metadata.uid, '0xc000000000000000', '0x10000000000000000')
        ///
        /// This is an alpha field and requires enabling the ShardedListAndWatch feature gate.
        shardSelector: ?[]const u8 = null,
        /// Timeout for the list/watch call. This limits the duration of the call, regardless of any activity or inactivity.
        timeoutSeconds: ?[]const u8 = null,
        /// Watch for changes to the described resources and return them as a stream of add, update, and remove notifications. Specify resourceVersion.
        watch: ?[]const u8 = null,
    };
    /// watch changes to an object of kind StorageVersionMigration. deprecated: use the 'watch' parameter with a list operation instead, filtered to a single item with the 'fieldSelector' parameter.
    pub fn watchStoragemigrationV1beta1StorageVersionMigration(self: *Client, name: []const u8, options: watchStoragemigrationV1beta1StorageVersionMigrationOptions) !WatchStoragemigrationV1beta1StorageVersionMigrationResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/apis/storagemigration.k8s.io/v1beta1/watch/storageversionmigrations/", .{});
        try urlEncode(url_w, name);
        var first_query = true;
        _ = &first_query;
        if (options.allowWatchBookmarks) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("allowWatchBookmarks=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.@"continue") |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("continue=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.fieldSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("fieldSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.labelSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("labelSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.limit) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("limit=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.pretty) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("pretty=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersion) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersion=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.resourceVersionMatch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("resourceVersionMatch=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.sendInitialEvents) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("sendInitialEvents=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.shardSelector) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("shardSelector=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.timeoutSeconds) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("timeoutSeconds=", .{});
                try urlEncode(url_w, val);
            }
        }
        if (options.watch) |val| {
            if (val.len > 0) {
                if (first_query) {
                    try url_w.writeByte('?');
                    first_query = false;
                } else {
                    try url_w.writeByte('&');
                }
                try url_w.print("watch=", .{});
                try urlEncode(url_w, val);
            }
        }
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.apis.meta.v1.WatchEvent, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const LogFileListHandlerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const LogFileListHandlerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const LogFileListHandlerResponse = union(enum) {
        unauthorized: LogFileListHandlerResponse401,
        default_response: LogFileListHandlerResponseDefault,
    };

    pub fn logFileListHandler(self: *Client) !LogFileListHandlerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/logs/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const LogFileHandlerResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const LogFileHandlerResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const LogFileHandlerResponse = union(enum) {
        unauthorized: LogFileHandlerResponse401,
        default_response: LogFileHandlerResponseDefault,
    };

    pub fn logFileHandler(self: *Client, logpath: []const u8) !LogFileHandlerResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/logs/", .{});
        try urlEncode(url_w, logpath);
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetServiceAccountIssuerOpenIDKeysetResponse200 = struct {
        body: []const u8,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetServiceAccountIssuerOpenIDKeysetResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetServiceAccountIssuerOpenIDKeysetResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetServiceAccountIssuerOpenIDKeysetResponse = union(enum) {
        ok: GetServiceAccountIssuerOpenIDKeysetResponse200,
        unauthorized: GetServiceAccountIssuerOpenIDKeysetResponse401,
        default_response: GetServiceAccountIssuerOpenIDKeysetResponseDefault,
    };

    /// get service account issuer OpenID JSON Web Key Set (contains public token verification keys)
    pub fn getServiceAccountIssuerOpenIDKeyset(self: *Client) !GetServiceAccountIssuerOpenIDKeysetResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/openid/v1/jwks/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky([]const u8, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    pub const GetCodeVersionResponse200 = struct {
        body: root.io.k8s.apimachinery.pkg.version.Info,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCodeVersionResponse401 = struct {
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCodeVersionResponseDefault = struct {
        body: std.json.Value,
        headers: []const std.http.Header,
        arena: std.heap.ArenaAllocator,
    };
    pub const GetCodeVersionResponse = union(enum) {
        ok: GetCodeVersionResponse200,
        unauthorized: GetCodeVersionResponse401,
        default_response: GetCodeVersionResponseDefault,
    };

    /// get the version information for this server
    pub fn getCodeVersion(self: *Client) !GetCodeVersionResponse {
        var url_buf: [1024]u8 = undefined;
        var url_fbs = std.io.fixedBufferStream(&url_buf);
        const url_w = url_fbs.writer();
        try url_w.print("{s}", .{self.base_url});
        try url_w.print("/version/", .{});
        const url = url_fbs.getWritten();
        var extra_headers = std.ArrayListUnmanaged(std.http.Header){};
        defer extra_headers.deinit(self.allocator);
        var body_list = std.ArrayListUnmanaged(u8){};
        defer body_list.deinit(self.allocator);

        // Serialize body if present
        var req_body_writer = std.io.Writer.Allocating.init(self.allocator);
        defer req_body_writer.deinit();

        var write_buf: [4096]u8 = undefined;
        const ResponseContext = struct {
            writer: std.io.Writer,
            list: *std.ArrayListUnmanaged(u8),
            allocator: std.mem.Allocator,
            fn drain(w: *std.io.Writer, chunks: []const []const u8, len: usize) error{WriteFailed}!usize {
                _ = len;
                const ctx: *@This() = @fieldParentPtr("writer", w);

                // Flush existing buffer
                if (w.end > 0) {
                    const buffered = w.buffer[0..w.end];
                    ctx.list.appendSlice(ctx.allocator, buffered) catch return error.WriteFailed;
                    w.end = 0;
                }

                var total_written: usize = 0;
                for (chunks) |chunk| {
                    if (chunk.len > 0) {
                        ctx.list.appendSlice(ctx.allocator, chunk) catch return error.WriteFailed;
                        total_written += chunk.len;
                    }
                }
                return total_written;
            }
        };
        const vtable = std.io.Writer.VTable{ .drain = ResponseContext.drain };
        var ctx = ResponseContext{
            .writer = std.io.Writer{ .vtable = &vtable, .buffer = &write_buf, .end = 0 },
            .list = &body_list,
            .allocator = self.allocator,
        };

        const fetch_res = try self.client.fetch(.{
            .location = .{ .uri = try std.Uri.parse(url) },
            .method = .GET, // Use format specifier for method
            .extra_headers = extra_headers.items,
            .payload = if (!std.mem.eql(u8, "GET", "DELETE") and req_body_writer.writer.end > 0) req_body_writer.writer.buffer[0..req_body_writer.writer.end] else null,
            .response_writer = &ctx.writer,
        });

        // Flush any remaining data in the writer buffer
        try ctx.writer.vtable.flush(&ctx.writer);

        // Headers are not available from client.fetch, so we return empty headers for now.
        // This is a workaround for Zig 0.15.2 std.http.Client limitations/bugs.
        const headers = try self.allocator.alloc(std.http.Header, 0);
        var arena = std.heap.ArenaAllocator.init(self.allocator);
        errdefer arena.deinit();

        const response = struct { head: struct { status: std.http.Status } }{ .head = .{ .status = fetch_res.status } };
        switch (response.head.status) {
            .ok => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(root.io.k8s.apimachinery.pkg.version.Info, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .ok = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
            .unauthorized => {
                return .{ .unauthorized = .{ .headers = headers, .arena = arena } };
            },
            else => {
                const body_resp = try arena.allocator().dupe(u8, body_list.items);
                const parsed = try std.json.parseFromSliceLeaky(std.json.Value, arena.allocator(), body_resp, .{ .ignore_unknown_fields = true });
                return .{ .default_response = .{ .body = parsed, .headers = headers, .arena = arena } };
            },
        }
    }

    fn urlEncode(writer: anytype, input: []const u8) !void {
        var w = writer;
        for (input) |c| {
            if (std.ascii.isAlphanumeric(c) or c == '-' or c == '.' or c == '_' or c == '~') {
                try w.writeByte(c);
            } else if (c == ' ') {
                try w.writeByte('+');
            } else {
                try w.print("%%{X:0>2}", .{c});
            }
        }
    }
    pub fn resource(self: *Client, comptime T: type) ResourceClient(T) {
        return ResourceClient(T){ .client = self };
    }

    pub fn ResourceClient(comptime T: type) type {
        if (!@hasDecl(T, "api_metadata")) {
            @compileError("Type " ++ @typeName(T) ++ " must have 'api_metadata' decl to be used as a resource.");
        }
        const metadata = T.api_metadata;

        return struct {
            client: *Client,

            const Self = @This();

            pub fn list(self: Self, options: anytype) ![]const T {
                // TODO: Implement proper list logic
                // For now, we assume a standard GET request to the path
                return self.client.request(std.http.Method.GET, metadata.path, options, []const T);
            }

            pub fn create(self: Self, body: T, options: anytype) !T {
                return self.client.request(std.http.Method.POST, metadata.path, .{ .body = body, .query = options }, T);
            }

            pub fn get(self: Self, name: []const u8) !T {
                // Construct path: metadata.path + "/" + name
                var path_buf: [256]u8 = undefined;
                const path = try std.fmt.bufPrint(&path_buf, "{s}/{s}", .{ metadata.path, name });
                return self.client.request(std.http.Method.GET, path, .{}, T);
            }

            pub fn delete(self: Self, name: []const u8, options: anytype) !void {
                var path_buf: [256]u8 = undefined;
                const path = try std.fmt.bufPrint(&path_buf, "{s}/{s}", .{ metadata.path, name });
                return self.client.request(std.http.Method.DELETE, path, .{ .query = options }, void);
            }

            pub fn update(self: Self, name: []const u8, body: T, options: anytype) !T {
                var path_buf: [256]u8 = undefined;
                const path = try std.fmt.bufPrint(&path_buf, "{s}/{s}", .{ metadata.path, name });
                return self.client.request(std.http.Method.PUT, path, .{ .body = body, .query = options }, T);
            }

            pub fn watch(self: Self, options: anytype) !Client.WatchStream(T) {
                return self.client.watch(T, metadata.path, options);
            }
        };
    }
};
